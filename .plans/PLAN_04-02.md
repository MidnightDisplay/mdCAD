---
phase: 04-interaction-math-and-api-expansion
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/math/math_interaction.h
  - src/gizmo/gizmo.h
  - src/gizmo/gizmo_vertex_mode.h
  - src/math_harness.c
autonomous: true
requirements:
  - HOT-03
user_setup: []
must_haves:
  truths:
    - "Gizmo axis/plane drag math uses shared cglm-backed helpers rather than direct legacy math3d intersection helpers"
    - "Vertex-mode world/local delta conversion runs through one shared helper path with stable degenerate handling"
    - "Harness compare checks cover gizmo drag and vertex local-delta behavior-level parity"
  artifacts:
    - path: "src/math/math_interaction.h"
      provides: "axis/plane intersection and world-to-local delta helpers"
      contains: "mdcad_interaction_ray_axis_closest_t"
    - path: "src/gizmo/gizmo.h"
      provides: "gizmo drag pipeline routed through shared helpers"
      contains: "mdcad_interaction_ray_plane_intersect"
    - path: "src/gizmo/gizmo_vertex_mode.h"
      provides: "vertex drag local-delta conversion through shared helper"
      contains: "mdcad_interaction_world_delta_to_local"
    - path: "src/math_harness.c"
      provides: "parity checks for migrated gizmo interaction math"
      contains: "gizmo-axis-drag"
  key_links:
    - from: "src/gizmo/gizmo.h"
      to: "src/math/math_interaction.h"
      via: "axis and plane drag math call shared helper APIs"
      pattern: "mdcad_interaction_ray_axis_closest_t|mdcad_interaction_ray_plane_intersect"
    - from: "src/gizmo/gizmo_vertex_mode.h"
      to: "src/math/math_interaction.h"
      via: "world delta is converted to local delta through one helper"
      pattern: "mdcad_interaction_world_delta_to_local"
    - from: "src/math_harness.c"
      to: "src/gizmo/gizmo.h"
      via: "harness validates migrated drag behavior against frozen legacy baselines"
      pattern: "gizmo-axis-drag|gizmo-plane-drag|gizmo-vertex-local-delta"
---

<objective>
Migrate translation gizmo drag/intersection math to shared cglm-backed helpers while preserving interaction feel.

Purpose: `HOT-03` includes translation gizmo drag math parity on macOS, so this plan moves axis/plane and vertex-delta math off legacy helpers and adds compare coverage.
Output: shared interaction helper expansion, migrated gizmo callsites, and harness compare cases for axis/plane/vertex drag behavior.
</objective>

<execution_context>
@/Users/rodionradchenko/Documents/repos/mdCAD/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rodionradchenko/Documents/repos/mdCAD/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-interaction-math-and-api-expansion/04-CONTEXT.md
@.planning/phases/04-interaction-math-and-api-expansion/04-RESEARCH.md
@.planning/phases/04-interaction-math-and-api-expansion/04-VALIDATION.md
@src/math/math_interaction.h
@src/math3d.h
@src/gizmo/gizmo.h
@src/gizmo/gizmo_vertex_mode.h
@src/math_harness.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand shared interaction helpers for gizmo drag axis/plane and local-delta conversion</name>
  <files>src/math/math_interaction.h</files>
  <read_first>src/math/math_interaction.h, src/math3d.h, src/gizmo/gizmo.h, src/gizmo/gizmo_vertex_mode.h, .planning/phases/04-interaction-math-and-api-expansion/04-RESEARCH.md, docs/MATH_CONVENTIONS.md</read_first>
  <action>In `src/math/math_interaction.h`, add these exact helper functions: `mdcad_interaction_ray_axis_closest_t(...)`, `mdcad_interaction_ray_plane_intersect(...)`, and `mdcad_interaction_world_delta_to_local(...)`. Implement each helper using cglm APIs and preserve deterministic failure behavior: for parallel ray-axis/plane cases return `false` or a fixed `t=0.0f`; for non-invertible world matrices return zero local delta. Keep helper signatures explicit and avoid introducing any new typedef aliases for vendor math types.</action>
  <verify>
    <automated>rg -n "mdcad_interaction_ray_axis_closest_t|mdcad_interaction_ray_plane_intersect|mdcad_interaction_world_delta_to_local|glm_mat4_inv|glms_vec3_dot|glms_vec3_sub" src/math/math_interaction.h</automated>
  </verify>
  <acceptance_criteria>
    - `src/math/math_interaction.h` contains `mdcad_interaction_ray_axis_closest_t`
    - `src/math/math_interaction.h` contains `mdcad_interaction_ray_plane_intersect`
    - `src/math/math_interaction.h` contains `mdcad_interaction_world_delta_to_local`
    - `src/math/math_interaction.h` contains either `glm_mat4_inv` or `glms_mat4_inv`
    - `src/math/math_interaction.h` contains `return false` in plane intersection failure path
    - `src/math/math_interaction.h` contains `0.0f` fallback in axis/local-delta failure path
  </acceptance_criteria>
  <done>Interaction helper surface now includes axis/plane and world/local conversion primitives needed by gizmo drag logic.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate gizmo modules to shared helpers and add compare cases for drag parity</name>
  <files>src/gizmo/gizmo.h, src/gizmo/gizmo_vertex_mode.h, src/math_harness.c</files>
  <read_first>src/gizmo/gizmo.h, src/gizmo/gizmo_vertex_mode.h, src/math_harness.c, src/math/math_interaction.h, src/math3d.h, .planning/phases/04-interaction-math-and-api-expansion/04-VALIDATION.md</read_first>
  <action>Update `src/gizmo/gizmo.h` so drag begin/update calls use `mdcad_interaction_ray_axis_closest_t(...)` and `mdcad_interaction_ray_plane_intersect(...)` instead of direct `ray_axis_closest_t(...)` / `ray_plane_intersect(...)`. Update `src/gizmo/gizmo_vertex_mode.h` to call `mdcad_interaction_world_delta_to_local(...)` instead of open-coded matrix inverse/direction transforms. In `src/math_harness.c`, add compare cases named exactly `gizmo-axis-drag`, `gizmo-plane-drag`, and `gizmo-vertex-local-delta`, each comparing migrated helper outputs against frozen legacy baseline computations.</action>
  <verify>
    <automated>cmake -B build -G Ninja && ninja -C build mdcad_math_harness && ./build/bin/mdcad_math_harness --list | rg -n "gizmo-axis-drag|gizmo-plane-drag|gizmo-vertex-local-delta" && ./build/bin/mdcad_math_harness --mode compare --strict | rg -n "^COMPARE PASS (gizmo-axis-drag|gizmo-plane-drag|gizmo-vertex-local-delta)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/gizmo/gizmo.h` contains `mdcad_interaction_ray_axis_closest_t`
    - `src/gizmo/gizmo.h` contains `mdcad_interaction_ray_plane_intersect`
    - `rg -n "\bray_axis_closest_t\(" src/gizmo/gizmo.h` returns no matches
    - `rg -n "\bray_plane_intersect\(" src/gizmo/gizmo.h` returns no matches
    - `src/gizmo/gizmo_vertex_mode.h` contains `mdcad_interaction_world_delta_to_local`
    - `src/math_harness.c` contains `gizmo-axis-drag`
    - `src/math_harness.c` contains `gizmo-plane-drag`
    - `src/math_harness.c` contains `gizmo-vertex-local-delta`
    - `./build/bin/mdcad_math_harness --mode compare --strict` prints `COMPARE PASS gizmo-axis-drag`
    - `./build/bin/mdcad_math_harness --mode compare --strict` prints `COMPARE PASS gizmo-plane-drag`
    - `./build/bin/mdcad_math_harness --mode compare --strict` prints `COMPARE PASS gizmo-vertex-local-delta`
  </acceptance_criteria>
  <done>Gizmo drag and vertex-mode math now consume shared cglm-backed helpers and parity is covered in harness compare runs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "mdcad_interaction_ray_axis_closest_t|mdcad_interaction_ray_plane_intersect|mdcad_interaction_world_delta_to_local" src/math/math_interaction.h src/gizmo/gizmo.h src/gizmo/gizmo_vertex_mode.h` succeeds
- [ ] `rg -n "\bray_axis_closest_t\(|\bray_plane_intersect\(" src/gizmo/gizmo.h` returns no matches
- [ ] `cmake -B build -G Ninja && ninja -C build mdcad_math_harness` succeeds
- [ ] `./build/bin/mdcad_math_harness --mode compare --strict` prints `COMPARE PASS gizmo-axis-drag`
- [ ] `./build/bin/mdcad_math_harness --mode compare --strict` prints `COMPARE PASS gizmo-plane-drag`
- [ ] `./build/bin/mdcad_math_harness --mode compare --strict` prints `COMPARE PASS gizmo-vertex-local-delta`
- [ ] Manual macOS HOT-03 interaction smoke passes (`./build/bin/mdCAD`; validate gizmo axis/plane/vertex drag feel and accuracy) and `04-02-SUMMARY.md` records `HOT-03 macOS interaction smoke: PASS|FAIL`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Translation gizmo drag interaction math runs through the shared helper boundary with compare-tested parity
  </success_criteria>

<output>
After completion, create `.planning/phases/04-interaction-math-and-api-expansion/04-02-SUMMARY.md`
Also mirror this plan to `.plans/PLAN_04-02.md` (canonical plan remains under `.planning/phases/...`).
</output>
