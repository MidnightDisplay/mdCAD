1. parallel constraint between pairs and multiples of lines not supported yet
2. perpendicular constraint between pairs and multiples of lines not supported yet
3. applying along x/y/z constraints to lines seems to immediately break the solver resulting in an '[ERROR] solve Unsatisfied driving ALONG Z constraint.'
4. general solver robustness - I have added four lines in a shape of a cornerless rectangle (two lines with points along X, two lines with points along Z), then I added an arc in hopes of making a tangent fillet to fill one of the corner gaps - the solver has a hard time making the arc end points tangent to the line end points - it breaks and stops solving.
5. when selecting a sketch line in the active sketch the gizmo appears - the gizmo manipulation, as expected, causes the line entity Transform component coordinates to update. Best if we have the gizmo move the Geometry component's point A and point B coordinates instead (both in sync at the same time) - this should apply only to sketch lines in the active sketch, can we also have the gizmo position show at the line midpoint rather than the transform components coordinates.
6. something inconsistent about some combined shape degrees of freedom based on which which vertex/landmark point is being dragged. I have a shape made of lines that looks like two conjoined triangles (two triangles share the same line as one of their sides), there are some along axis constraints and some length constraints here and there. I am able to move the entire shape as a rigid shape by pulling with the gizmo on one of the vertices, but doing the same move on the other side does not result in any movement - both moves are valid, it's just that the solver is having trouble with one of them. Solver likely needs a review and a robustness dev phase.