---
phase: 05-windows-vulkan-hardening-and-performance-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/gpu/pick_readback_vulkan.c
  - docs/QUICKSTART.md
  - docs/VULKAN_WINDOWS.md
  - .planning/phases/05-windows-vulkan-hardening-and-performance-gates/evidence/windows-mingw-smoke/candidate/commands.log
  - .planning/phases/05-windows-vulkan-hardening-and-performance-gates/evidence/windows-mingw-smoke/candidate/manual-smoke.md
autonomous: true
requirements:
  - HOT-04
user_setup: []
must_haves:
  truths:
    - "Windows MSVC Vulkan remains the hard runtime gate, and validation evidence is captured in a repeatable artifact tree"
    - "Vulkan pick readback no longer blocks the entire queue with `vkQueueWaitIdle`, using fence-based synchronization instead"
    - "Windows Vulkan operator docs contain one explicit Phase 5 hard-gate runbook with build, compare, bench, and manual smoke commands"
  artifacts:
    - path: "src/gpu/pick_readback_vulkan.c"
      provides: "hardened Vulkan pick readback flow with persistent transfer resources and fence wait"
      contains: "vkWaitForFences"
    - path: "docs/QUICKSTART.md"
      provides: "single command sequence for Windows Vulkan Phase 5 hard gate"
      contains: "## Phase 5 Windows Vulkan hard gate"
    - path: "docs/VULKAN_WINDOWS.md"
      provides: "Vulkan-specific troubleshooting and validation notes for pick readback hardening"
      contains: "Phase 5 pick readback hardening"
  key_links:
    - from: "src/gpu/pick_buffer.h"
      to: "src/gpu/pick_readback_vulkan.c"
      via: "pick readback entrypoint keeps synchronous hover semantics while reducing queue-wide stalls"
      pattern: "pick_readback_pixels"
    - from: "docs/QUICKSTART.md"
      to: "src/CMakeLists.txt"
      via: "operator runbook executes existing `mdcad_math_harness` and `math-validation` targets"
      pattern: "mdcad_math_harness|math-validation"
---

<objective>
Close the Windows Vulkan behavior hard gate (`HOT-04`) with concrete evidence, while hardening the known Vulkan pick/readback hitch hotspot.

Purpose: Phase 5 must prove migrated hotspots behave correctly on `MSVC + Vulkan` before completion.
Output: fence-based Vulkan readback hardening, updated Windows runbooks, and a complete Windows hard-gate evidence bundle.
</objective>

<execution_context>
@/Users/rodionradchenko/Documents/repos/mdCAD/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rodionradchenko/Documents/repos/mdCAD/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-windows-vulkan-hardening-and-performance-gates/05-CONTEXT.md
@.planning/phases/05-windows-vulkan-hardening-and-performance-gates/05-RESEARCH.md
@.planning/phases/05-windows-vulkan-hardening-and-performance-gates/05-VALIDATION.md
@docs/VULKAN_WINDOWS.md
@docs/QUICKSTART.md
@src/platform.h
@src/CMakeLists.txt
@src/gpu/pick_buffer.h
@src/gpu/pick_readback_vulkan.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace queue-idle Vulkan pick readback with fence-synchronized persistent transfer resources</name>
  <files>src/gpu/pick_readback_vulkan.c</files>
  <read_first>src/gpu/pick_readback_vulkan.c, src/gpu/pick_buffer.h, src/platform.h, .planning/phases/05-windows-vulkan-hardening-and-performance-gates/05-CONTEXT.md, .planning/phases/05-windows-vulkan-hardening-and-performance-gates/05-RESEARCH.md</read_first>
  <action>In `src/gpu/pick_readback_vulkan.c`, add a file-static cache struct named exactly `pick_vk_readback_cache_t` with persistent `VkBuffer`, `VkDeviceMemory`, `VkCommandPool`, `VkCommandBuffer`, `VkFence`, and size tracking fields. Add helper functions named exactly `pick_vk_readback_cache_prepare(...)` and `pick_vk_readback_cache_dispose(...)` to (1) recreate resources when device or queue family changes and (2) grow staging allocation when `width * height * 4` exceeds cached capacity. In `pick_readback_pixels(...)`, replace per-call command-pool creation and `vkQueueWaitIdle(queue)` with `vkResetFences`, `vkResetCommandBuffer`, `vkQueueSubmit(queue, 1, &submit_info, cache.submit_fence)`, and `vkWaitForFences(device, 1, &cache.submit_fence, VK_TRUE, 1000000000ULL)`. Keep existing image layout transitions and fallback zero-fill behavior unchanged.</action>
  <verify>
    <automated>rg -n "pick_vk_readback_cache_t|pick_vk_readback_cache_prepare|pick_vk_readback_cache_dispose|vkResetFences|vkResetCommandBuffer|vkWaitForFences|vkQueueSubmit\(queue, 1, &submit_info" src/gpu/pick_readback_vulkan.c && if rg -n "vkQueueWaitIdle" src/gpu/pick_readback_vulkan.c; then echo "vkQueueWaitIdle still present"; exit 1; fi</automated>
  </verify>
  <acceptance_criteria>
    - `src/gpu/pick_readback_vulkan.c` contains `typedef struct pick_vk_readback_cache_t`
    - `src/gpu/pick_readback_vulkan.c` contains `pick_vk_readback_cache_prepare`
    - `src/gpu/pick_readback_vulkan.c` contains `pick_vk_readback_cache_dispose`
    - `src/gpu/pick_readback_vulkan.c` contains `vkWaitForFences`
    - `src/gpu/pick_readback_vulkan.c` contains `vkResetCommandBuffer`
    - `rg -n "vkQueueWaitIdle" src/gpu/pick_readback_vulkan.c` returns no matches
  </acceptance_criteria>
  <done>Vulkan readback path is hardened with fence synchronization and persistent transfer resources, removing queue-wide idle stalls.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the Windows MSVC Vulkan hard gate and codify the operator runbook</name>
  <files>docs/QUICKSTART.md, docs/VULKAN_WINDOWS.md, .planning/phases/05-windows-vulkan-hardening-and-performance-gates/evidence/windows-vulkan-msvc/candidate/commands.log, .planning/phases/05-windows-vulkan-hardening-and-performance-gates/evidence/windows-vulkan-msvc/candidate/compare.txt, .planning/phases/05-windows-vulkan-hardening-and-performance-gates/evidence/windows-vulkan-msvc/candidate/manual-smoke.md, .planning/phases/05-windows-vulkan-hardening-and-performance-gates/evidence/windows-mingw-smoke/candidate/commands.log, .planning/phases/05-windows-vulkan-hardening-and-performance-gates/evidence/windows-mingw-smoke/candidate/manual-smoke.md</files>
  <read_first>docs/QUICKSTART.md, docs/VULKAN_WINDOWS.md, src/CMakeLists.txt, src/math_harness.c, .planning/phases/05-windows-vulkan-hardening-and-performance-gates/05-VALIDATION.md, .planning/phases/05-windows-vulkan-hardening-and-performance-gates/05-RESEARCH.md</read_first>
  <action>Add a new section titled exactly `## Phase 5 Windows Vulkan hard gate` to `docs/QUICKSTART.md` containing these commands in order: `cmake -B build-vulkan -G "Visual Studio 18" -DUSE_VULKAN=ON`, `cmake --build build-vulkan --config Release --target mdcad_math_harness`, `.\build-vulkan\bin\Release\mdcad_math_harness.exe --mode compare --strict`, `.\build-vulkan\bin\Release\mdcad_math_harness.exe --mode bench --iterations 20000`, `cmake --build build-vulkan --config Release --target math-validation`, and `.\build-vulkan\bin\Release\mdCAD.exe`. In the same section add explicit MinGW smoke-only commands in order: `cmake -B build-mingw -G "MinGW Makefiles"`, `cmake --build build-mingw`, and `.\build-mingw\bin\mdCAD.exe`. In `docs/VULKAN_WINDOWS.md`, add a section titled exactly `## Phase 5 pick readback hardening` that references the fence-based readback path and required HOT-04 smoke workflows. Run the MSVC Vulkan hard-gate commands on Windows and tee outputs to `.planning/phases/05-windows-vulkan-hardening-and-performance-gates/evidence/windows-vulkan-msvc/candidate/commands.log` and `compare.txt`; create `manual-smoke.md` with one `PASS|FAIL` line for each required workflow: camera navigation, pick hover/click, gizmo axis drag, gizmo plane drag, vertex drag, undo/redo, import/save/reload. Run the MinGW smoke-only commands and tee outputs to `.planning/phases/05-windows-vulkan-hardening-and-performance-gates/evidence/windows-mingw-smoke/candidate/commands.log`; create `.planning/phases/05-windows-vulkan-hardening-and-performance-gates/evidence/windows-mingw-smoke/candidate/manual-smoke.md` with `PASS|FAIL` rows for launch, camera navigation, pick hover/click, gizmo drag, and undo/redo, and include the literal marker `smoke-only` in both MinGW artifacts.</action>
  <verify>
    <automated>rg -n "## Phase 5 Windows Vulkan hard gate|mdcad_math_harness\.exe --mode compare --strict|mdcad_math_harness\.exe --mode bench --iterations 20000|math-validation|mdCAD\.exe|cmake -B build-mingw -G \"MinGW Makefiles\"|cmake --build build-mingw|\.\\build-mingw\\bin\\mdCAD\.exe" docs/QUICKSTART.md && rg -n "## Phase 5 pick readback hardening|vkWaitForFences|HOT-04" docs/VULKAN_WINDOWS.md && test -f .planning/phases/05-windows-vulkan-hardening-and-performance-gates/evidence/windows-vulkan-msvc/candidate/commands.log && test -f .planning/phases/05-windows-vulkan-hardening-and-performance-gates/evidence/windows-vulkan-msvc/candidate/compare.txt && test -f .planning/phases/05-windows-vulkan-hardening-and-performance-gates/evidence/windows-vulkan-msvc/candidate/manual-smoke.md && rg -n "camera navigation|pick hover/click|gizmo axis drag|gizmo plane drag|vertex drag|undo/redo|import/save/reload" .planning/phases/05-windows-vulkan-hardening-and-performance-gates/evidence/windows-vulkan-msvc/candidate/manual-smoke.md && test -f .planning/phases/05-windows-vulkan-hardening-and-performance-gates/evidence/windows-mingw-smoke/candidate/commands.log && test -f .planning/phases/05-windows-vulkan-hardening-and-performance-gates/evidence/windows-mingw-smoke/candidate/manual-smoke.md && rg -n "smoke-only" .planning/phases/05-windows-vulkan-hardening-and-performance-gates/evidence/windows-mingw-smoke/candidate/commands.log .planning/phases/05-windows-vulkan-hardening-and-performance-gates/evidence/windows-mingw-smoke/candidate/manual-smoke.md && rg -n "launch|camera navigation|pick hover/click|gizmo drag|undo/redo" .planning/phases/05-windows-vulkan-hardening-and-performance-gates/evidence/windows-mingw-smoke/candidate/manual-smoke.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/QUICKSTART.md` contains `## Phase 5 Windows Vulkan hard gate`
    - `docs/QUICKSTART.md` contains `.\build-vulkan\bin\Release\mdcad_math_harness.exe --mode compare --strict`
    - `docs/QUICKSTART.md` contains `.\build-vulkan\bin\Release\mdcad_math_harness.exe --mode bench --iterations 20000`
    - `docs/VULKAN_WINDOWS.md` contains `## Phase 5 pick readback hardening`
    - `.planning/phases/05-windows-vulkan-hardening-and-performance-gates/evidence/windows-vulkan-msvc/candidate/commands.log` exists
    - `.planning/phases/05-windows-vulkan-hardening-and-performance-gates/evidence/windows-vulkan-msvc/candidate/compare.txt` exists
    - `.planning/phases/05-windows-vulkan-hardening-and-performance-gates/evidence/windows-vulkan-msvc/candidate/manual-smoke.md` exists and includes all seven required checklist rows
    - `docs/QUICKSTART.md` contains the MinGW smoke-only command trio (`cmake -B build-mingw ...`, `cmake --build build-mingw`, `.\build-mingw\bin\mdCAD.exe`)
    - `.planning/phases/05-windows-vulkan-hardening-and-performance-gates/evidence/windows-mingw-smoke/candidate/commands.log` exists and includes `smoke-only`
    - `.planning/phases/05-windows-vulkan-hardening-and-performance-gates/evidence/windows-mingw-smoke/candidate/manual-smoke.md` exists with `PASS|FAIL` rows for launch/camera/pick/gizmo/undo-redo and includes `smoke-only`
  </acceptance_criteria>
  <done>Windows Vulkan hard-gate evidence is captured with a repeatable runbook and explicit manual smoke outcomes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "pick_vk_readback_cache_t|vkWaitForFences|vkResetFences|vkResetCommandBuffer" src/gpu/pick_readback_vulkan.c` succeeds
- [ ] `rg -n "vkQueueWaitIdle" src/gpu/pick_readback_vulkan.c` returns no matches
- [ ] On Windows MSVC Vulkan: `cmake -B build-vulkan -G "Visual Studio 18" -DUSE_VULKAN=ON` and `cmake --build build-vulkan --config Release --target mdcad_math_harness math-validation` succeed
- [ ] `.\build-vulkan\bin\Release\mdcad_math_harness.exe --mode compare --strict` exits 0 and output is stored at `.../evidence/windows-vulkan-msvc/candidate/compare.txt`
- [ ] `docs/QUICKSTART.md` contains `## Phase 5 Windows Vulkan hard gate`
- [ ] `docs/VULKAN_WINDOWS.md` contains `## Phase 5 pick readback hardening`
- [ ] `manual-smoke.md` contains `PASS|FAIL` result lines for all seven HOT-04 workflows
- [ ] MinGW smoke-only command artifacts exist at `.../evidence/windows-mingw-smoke/candidate/` and include launch/camera/pick/gizmo/undo-redo PASS|FAIL rows
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Windows MSVC Vulkan hard-gate evidence proves HOT-04 behavior readiness with the readback hitch hotspot hardened
  </success_criteria>

<output>
After completion, create `.planning/phases/05-windows-vulkan-hardening-and-performance-gates/05-01-SUMMARY.md`.
Also mirror this plan to `.plans/PLAN_05-01.md` (canonical plan remains under `.planning/phases/...`).
</output>
