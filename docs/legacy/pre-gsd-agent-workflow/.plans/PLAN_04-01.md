---
phase: 04-interaction-math-and-api-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/math/math_interaction.h
  - src/app.c
  - src/gpu/pick_buffer.h
  - src/math_harness.c
autonomous: true
requirements:
  - HOT-03
user_setup: []
must_haves:
  truths:
    - "Pick/unproject/ray construction is centralized behind one cglm-backed helper boundary under src/math/"
    - "src/app.c uses one shared screen-ray helper for both drag begin and drag update paths"
    - "Pick MVP windowing logic in src/gpu/pick_buffer.h is computed through the same helper boundary used by the runtime"
  artifacts:
    - path: "src/math/math_interaction.h"
      provides: "shared interaction math helpers for screen-ray and pick MVP"
      contains: "mdcad_interaction_screen_ray_from_viewport"
    - path: "src/app.c"
      provides: "unified runtime use of the shared screen-ray helper"
      contains: "mdcad_interaction_screen_ray_from_viewport"
    - path: "src/gpu/pick_buffer.h"
      provides: "shared pick MVP computation path"
      contains: "mdcad_interaction_compute_pick_mvp"
    - path: "src/math_harness.c"
      provides: "behavior-level parity checks for migrated pick/ray math"
      contains: "pick-mvp-window"
  key_links:
    - from: "src/app.c"
      to: "src/math/math_interaction.h"
      via: "drag interaction code constructs mouse rays through one shared helper"
      pattern: "mdcad_interaction_screen_ray_from_viewport"
    - from: "src/gpu/pick_buffer.h"
      to: "src/math/math_interaction.h"
      via: "pick render math uses shared pick-MVP helper"
      pattern: "mdcad_interaction_compute_pick_mvp"
    - from: "src/math_harness.c"
      to: "src/app.c"
      via: "harness compares the migrated production helper path against frozen legacy baselines"
      pattern: "screen-ray-unproject|pick-mvp-window"
---

<objective>
Migrate pick/unproject/ray math to the new foundation through a single shared helper boundary and wire runtime callers to it.

Purpose: `HOT-03` requires interaction-critical pick/unproject/ray math to run on cglm-backed helpers without visible macOS regressions.
Output: one shared interaction helper module, migrated app/pick callsites, and harness compare coverage for pick/ray parity.
</objective>

<execution_context>
@/Users/rodionradchenko/Documents/repos/mdCAD/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rodionradchenko/Documents/repos/mdCAD/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-interaction-math-and-api-expansion/04-CONTEXT.md
@.planning/phases/04-interaction-math-and-api-expansion/04-RESEARCH.md
@.planning/phases/04-interaction-math-and-api-expansion/04-VALIDATION.md
@docs/MATH_CONVENTIONS.md
@src/math/cglm_entry.h
@src/math3d.h
@src/app.c
@src/gpu/pick_buffer.h
@src/math_harness.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared cglm-backed interaction helpers for screen-ray and pick MVP</name>
  <files>src/math/math_interaction.h</files>
  <read_first>src/math/cglm_entry.h, src/math3d.h, src/app.c, src/gpu/pick_buffer.h, .planning/phases/04-interaction-math-and-api-expansion/04-CONTEXT.md, .planning/phases/04-interaction-math-and-api-expansion/04-RESEARCH.md, docs/MATH_CONVENTIONS.md</read_first>
  <action>Create or extend `src/math/math_interaction.h` as a header-only module that includes `"cglm_entry.h"` and exposes these exact functions: `mdcad_interaction_screen_ray_from_viewport(...)`, `mdcad_interaction_unproject_point(...)`, and `mdcad_interaction_compute_pick_mvp(...)`. Implement the functions with cglm APIs (`glms_unprojecti`, `glms_mat4_inv`, `glms_mat4_mul`, or equivalent raw `glm_*` where needed) while keeping external caller inputs/outputs compatible with existing `vec3_t`/`mat4_t` bridges. Add explicit guards for degenerate viewport width/height and failed inverse cases so the helper returns deterministic fallback values (no NaN propagation).</action>
  <verify>
    <automated>rg -n "mdcad_interaction_screen_ray_from_viewport|mdcad_interaction_unproject_point|mdcad_interaction_compute_pick_mvp|glms_unprojecti|glms_mat4_inv|glms_mat4_mul" src/math/math_interaction.h</automated>
  </verify>
  <acceptance_criteria>
    - `src/math/math_interaction.h` contains `mdcad_interaction_screen_ray_from_viewport`
    - `src/math/math_interaction.h` contains `mdcad_interaction_unproject_point`
    - `src/math/math_interaction.h` contains `mdcad_interaction_compute_pick_mvp`
    - `src/math/math_interaction.h` contains either `glms_unprojecti` or `glm_unprojecti`
    - `src/math/math_interaction.h` contains either `glms_mat4_inv` or `glm_mat4_inv`
    - `src/math/math_interaction.h` contains either `glms_mat4_mul` or `glm_mat4_mul`
    - `src/math/math_interaction.h` contains `if (viewport_width <= 0` or `if (vp_width <= 0`
  </acceptance_criteria>
  <done>Shared interaction math helpers exist and define a single cglm-backed path for screen-ray and pick-MVP math.</done>
</task>

<task type="auto">
  <name>Task 2: Route app and pick runtime callsites to the helper boundary and extend compare coverage</name>
  <files>src/app.c, src/gpu/pick_buffer.h, src/math_harness.c</files>
  <read_first>src/app.c, src/gpu/pick_buffer.h, src/math_harness.c, src/math/math_interaction.h, src/math3d.h, .planning/phases/04-interaction-math-and-api-expansion/04-VALIDATION.md</read_first>
  <action>Update `src/app.c` drag-begin and drag-update interaction blocks to call `mdcad_interaction_screen_ray_from_viewport(...)` instead of open-coding inverse-VP plus `ray_from_screen(...)`. Update `src/gpu/pick_buffer.h` so `pick_buffer_compute_mvp(...)` uses `mdcad_interaction_compute_pick_mvp(...)` and remove duplicated matrix windowing math from that function body. In `src/math_harness.c`, keep `screen-ray-unproject` and add a new compare case named exactly `pick-mvp-window` that compares migrated helper outputs to a frozen legacy baseline from equivalent pre-migration formulas.</action>
  <verify>
    <automated>cmake -B build -G Ninja && ninja -C build mdcad_math_harness && ./build/bin/mdcad_math_harness --list | rg -n "screen-ray-unproject|pick-mvp-window" && ./build/bin/mdcad_math_harness --mode compare --strict | rg -n "^COMPARE PASS (screen-ray-unproject|pick-mvp-window)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app.c` contains `mdcad_interaction_screen_ray_from_viewport`
    - `rg -n "\bray_from_screen\(" src/app.c` returns no matches
    - `src/gpu/pick_buffer.h` contains `mdcad_interaction_compute_pick_mvp`
    - `src/math_harness.c` contains `pick-mvp-window`
    - `./build/bin/mdcad_math_harness --list` prints `pick-mvp-window`
    - `./build/bin/mdcad_math_harness --mode compare --strict` prints `COMPARE PASS screen-ray-unproject`
    - `./build/bin/mdcad_math_harness --mode compare --strict` prints `COMPARE PASS pick-mvp-window`
  </acceptance_criteria>
  <done>Runtime pick/ray callsites are migrated to one helper boundary and protected by behavior-level compare checks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "mdcad_interaction_screen_ray_from_viewport|mdcad_interaction_unproject_point|mdcad_interaction_compute_pick_mvp" src/math/math_interaction.h src/app.c src/gpu/pick_buffer.h` succeeds
- [ ] `cmake -B build -G Ninja && ninja -C build mdcad_math_harness` succeeds
- [ ] `./build/bin/mdcad_math_harness --mode compare --strict` prints `COMPARE PASS screen-ray-unproject`
- [ ] `./build/bin/mdcad_math_harness --mode compare --strict` prints `COMPARE PASS pick-mvp-window`
- [ ] `rg -n "\bray_from_screen\(" src/app.c` returns no matches
- [ ] Manual macOS HOT-03 interaction smoke passes (`./build/bin/mdCAD`; validate pick/unproject/ray drag behavior) and `04-01-SUMMARY.md` records `HOT-03 macOS interaction smoke: PASS|FAIL`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Pick/unproject/ray runtime math is centralized on cglm-backed helpers without interaction regressions in compare checks
  </success_criteria>

<output>
After completion, create `.planning/phases/04-interaction-math-and-api-expansion/04-01-SUMMARY.md`
Also mirror this plan to `.plans/PLAN_04-01.md` (canonical plan remains under `.planning/phases/...`).
</output>
