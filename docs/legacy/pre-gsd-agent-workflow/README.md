# Pre-GSD Agent Workflow Archive

This folder preserves the repo's pre-GSD planning and agent docs as frozen historical context.

- Active planning, continuity, and project-memory docs now live under `.planning/`, with `.planning/STATE.md` as the main current status entry point.
- `AGENTS.md`, `CHECKPOINT.md`, and `.plans/` are archived here for reference only and should not be treated as live workflow inputs.
