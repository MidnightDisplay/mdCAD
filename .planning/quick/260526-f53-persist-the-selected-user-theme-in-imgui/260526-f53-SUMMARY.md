---
phase: quick-260526-f53
plan: 01
summary_type: execution
status: complete
---

# Quick Task 260526-f53 Summary

Persisted the selected theme in the active ImGui settings stream via a custom `[mdCAD][Theme]` section, so shad-ui remains the fallback while standalone, embedded, and viewport-only launches restore the last saved or manually edited theme token.

## Completed Work

1. Added stable theme tokens and a custom ImGui settings handler in `src/ui/ui_theme.h` that reads/writes the selected theme through the active ini stream.
2. Registered the handler from `src/imgui_storage.h` before any ini load, kept `UI_THEME_DEFAULT` in `src/app.c` as the no-entry fallback, and added a one-time standalone clear-color resync after the automatic ini restore point so the viewport background matches the restored theme on first frame.
3. Marked theme changes dirty from `src/ui/ui_controls.h` so runtime selections flush through the existing ImGui save path.
4. Added `src/tests/theme_persistence_contract_test.c` plus `src/CMakeLists.txt` coverage for handler registration, default-vs-restore behavior, manual token edits, the dirty-mark contract, and portable source-path lookup.

## Commits

- `db51d7d` — `feat(quick-260526-f53): persist theme selection in active imgui ini`
- `0f2817d` — `feat(quick-260526-f53): add theme persistence contract coverage`
- `e503ffb` — `fix(quick-260526-f53): sync restored theme state`

## Deviations from Plan

None - plan executed as written.

## Verification

- `cmake --build build-vulkan --config Release --target mdCAD theme_persistence_contract_test` ✅
- `ctest --test-dir build-vulkan -C Release --output-on-failure -R "theme_persistence_contract_test|embed_layout_state_test|viewport_only_app_contract_test|camera_shortcut_app_contract_test"` ✅

## Self-Check: PASSED

- Summary file exists at `.planning/quick/260526-f53-persist-the-selected-user-theme-in-imgui/260526-f53-SUMMARY.md`.
- Verified commits `db51d7d`, `0f2817d`, and `e503ffb` exist in git history.
