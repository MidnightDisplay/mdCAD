---
phase: 45-startup-jsonl-auto-import
plan: 02
type: execute
wave: 2
depends_on:
  - 45-01
files_modified:
  - src/app.c
  - src/tests/startup_jsonl_app_contract_test.c
  - src/CMakeLists.txt
autonomous: true
requirements:
  - JSON-01
  - JSON-03
must_haves:
  truths:
    - "Startup JSONL import begins from the app frame loop even when Scene Hierarchy is hidden in the embedded viewer-first layout."
    - "If startup JSONL import fails, mdCAD keeps a usable viewer open and shows a clear app-level error state instead of exiting or silently doing nothing."
    - "Successful startup import dirties the hierarchy/app scene cache so imported content becomes visible without opening the Scene Hierarchy panel."
  artifacts:
    - path: "src/app.c"
      provides: "App-owned startup controller integration, frame-loop ticking, cleanup, and app-level startup status/error overlay."
      contains: "startup_jsonl_import_controller"
    - path: "src/tests/startup_jsonl_app_contract_test.c"
      provides: "Source-contract coverage that startup import is owned by `app.c` outside the Scene Hierarchy draw gate."
      contains: "test_startup_jsonl_app_"
  key_links:
    - from: "src/app.c"
      to: "src/startup_jsonl_import_controller.h"
      via: "init/arm/tick/reset from the non-UI startup seam"
      pattern: "startup_jsonl_import_controller_"
    - from: "src/app.c"
      to: "src/ui/ui_scene_hierarchy.h"
      via: "successful startup import marks the hierarchy cache dirty without moving import ownership back into the panel"
      pattern: "ui_scene_hierarchy_mark_dirty"
    - from: "src/app.c"
      to: "embedded viewer runtime"
      via: "app-level overlay is drawn outside the `state.ui_visible` panel gate and never calls quit on startup import failure"
      pattern: "Startup JSONL Import"
---

<objective>
Wire the startup controller into `app.c` and surface a clear embedded-safe status/error overlay.

Purpose: Phase 45 is only complete when launch-time JSONL import works from the app lifecycle itself and bad files still leave the embedded viewer usable.
Output: `app.c` owns startup import init/frame/cleanup plus a visible non-panel failure surface.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45-startup-jsonl-auto-import/45-RESEARCH.md
@.planning/phases/43-embed-contract-child-window-bootstrap/43-01-SUMMARY.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-10-SUMMARY.md
@src/app.c
@src/startup_jsonl_import_controller.h
@src/ui/ui_scene_hierarchy.h
@src/jsonl_import_job.h
@src/CMakeLists.txt

<interfaces>
From src/app.c:
```c
static struct {
    bool ui_visible;
    mdcad_launch_config_t launch;
    ui_scene_hierarchy_state_t scene_hierarchy;
    ecs_scene_t ecs_scene;
} state;

static void init(void);
static void cleanup(void);
static void frame(void);
```

Expected from src/startup_jsonl_import_controller.h after 45-01:
```c
typedef struct startup_jsonl_import_controller_t startup_jsonl_import_controller_t;

static inline void startup_jsonl_import_controller_init(...);
static inline bool startup_jsonl_import_controller_arm(...);
static inline bool startup_jsonl_import_controller_tick(...);
static inline void startup_jsonl_import_controller_reset(...);
static inline void startup_jsonl_import_controller_dismiss_error(...);
```

From src/ui/ui_scene_hierarchy.h:
```c
static inline void ui_scene_hierarchy_mark_dirty(ui_scene_hierarchy_state_t *state);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add an app-owned startup import contract test and CTest wiring</name>
  <files>src/tests/startup_jsonl_app_contract_test.c, src/CMakeLists.txt</files>
  <behavior>
    - Test 1: `app.c` owns a startup-controller state member and initializes/arms it during `init()`.
    - Test 2: `frame()` starts/ticks startup import after `simgui_new_frame(...)` and before the `if (state.ui_visible)` gate so embedded hidden-panel launches still progress.
    - Test 3: `app.c` draws a startup-status/error surface outside the Scene Hierarchy panel path and exposes a dismiss path for latched failures.
    - Test 4: `cleanup()` resets or cancels startup import state so Phase 44 embedded lifecycle behavior stays intact.
  </behavior>
  <action>Create `startup_jsonl_app_contract_test.c` as a fast source-contract test that reads `src/app.c` and asserts the init/frame/cleanup ownership seam plus the app-level overlay anchors. Register it in `src/CMakeLists.txt` using the same standalone test pattern as the other `embed_*` and `jsonl_*` contract tests. Keep this test focused on app ownership and failure-surface wiring; do not attempt to automate the Avalonia host or Phase 47 launch-path integration here.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target startup_jsonl_app_contract_test &amp;&amp; ctest --test-dir build-vulkan -C Release -N -R "startup_jsonl_app_contract_test"</automated>
  </verify>
  <done>A runnable app-contract test exists that locks the non-UI startup seam and app-level error surface before `app.c` is changed.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate startup import into `app.c` with an embedded-safe status/error overlay</name>
  <files>src/app.c</files>
  <action>Add startup-controller state to the main app struct and initialize it during `init()` after core scene state exists. If `state.launch.startup_jsonl_path[0] != '\0'`, arm the controller there, but defer actual job start/tick to `frame()`. In `frame()`, run the controller immediately after `simgui_new_frame(...)` and before the `if (state.ui_visible)` panel gate; when the controller reports a successful completion or other scene mutation, call `ui_scene_hierarchy_mark_dirty(&state.scene_hierarchy)`. Draw a small non-modal `Startup JSONL Import` app-level window or banner from `app.c` regardless of Scene Hierarchy visibility: show progress/status while running, show red latched error text plus a dismiss control on failure, and never call `exit()`/`sapp_quit()` because of import failure. In `cleanup()`, cancel/reset any still-running startup job through the controller. Do not move ownership back into `ui_scene_hierarchy_draw()`, do not enable live refresh, do not wire the Avalonia sample to pass `--jsonl`, and do not alter the embedded focus/runtime-guard code proven in Phase 44.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD startup_jsonl_app_contract_test startup_jsonl_import_controller_test embed_launch_config_test jsonl_flat_import_options_contract_test jsonl_flat_anchor_scoped_ingest_test &amp;&amp; ctest --test-dir build-vulkan -C Release --output-on-failure -R "startup_jsonl_app_contract_test|startup_jsonl_import_controller_test|embed_launch_config_test|jsonl_flat_import_options_contract_test|jsonl_flat_anchor_scoped_ingest_test"</automated>
  </verify>
  <done>Startup JSONL import is app-owned, begins without requiring Scene Hierarchy visibility, and leaves the viewer usable with a clear error state when import fails.</done>
</task>

</tasks>

<verification>
- `cmake --build build-vulkan --config Release --target mdCAD startup_jsonl_app_contract_test startup_jsonl_import_controller_test embed_launch_config_test jsonl_flat_import_options_contract_test jsonl_flat_anchor_scoped_ingest_test`
- `ctest --test-dir build-vulkan -C Release --output-on-failure -R "startup_jsonl_app_contract_test|startup_jsonl_import_controller_test|embed_launch_config_test|jsonl_flat_import_options_contract_test|jsonl_flat_anchor_scoped_ingest_test"`
- Manual smoke after automation: launch mdCAD with a known absolute JSONL fixture and with a missing absolute JSONL path; confirm the viewer stays open in both cases and only the bad-path launch shows the red startup error overlay.
</verification>

<success_criteria>
- Phase 45 startup import ownership lives in `app.c`, not behind Scene Hierarchy visibility.
- A bad launch-time JSONL path no longer kills the viewer session.
- Automated contract/regression tests stay green while preserving the Phase 44 embedded lifecycle boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/45-startup-jsonl-auto-import/45-02-SUMMARY.md`
</output>
