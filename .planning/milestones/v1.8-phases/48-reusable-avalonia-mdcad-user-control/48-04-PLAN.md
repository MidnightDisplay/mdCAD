---
phase: 48-reusable-avalonia-mdcad-user-control
plan: 04
type: execute
wave: 4
depends_on:
  - 48-03
files_modified:
  - .planning/phases/48-reusable-avalonia-mdcad-user-control/48-MANUAL-CHECKLIST.md
  - samples/avalonia-host/MainWindow.axaml
  - samples/avalonia-host/MainWindow.axaml.cs
  - samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml
  - samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs
autonomous: false
requirements:
  - P48-06
  - P48-07
must_haves:
  truths:
    - "Sealed mode is the default bare viewer surface, while diagnostic mode is explicit opt-in and shows the host-owned controls/status surface above the viewer."
    - "A bad requested JSONL path remains visibly warned in sealed mode without blocking a usable viewer launch."
    - "The sample host proves runtime copy, auto-start, explicit start, relaunch, and orphan-free cleanup using the reusable control instead of the old window-owned embedding scaffold."
    - "Native embed/JSONL regression tests from prior phases still pass after the reusable-control extraction."
  artifacts:
    - path: "samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml"
      provides: "Sealed vs diagnostic control layout and warning surface."
      contains: "Diagnostic"
    - path: "samples/avalonia-host/MainWindow.axaml"
      provides: "Diagnostic harness consuming the reusable control."
      contains: "MdCadEmbeddedControl"
    - path: ".planning/phases/48-reusable-avalonia-mdcad-user-control/48-MANUAL-CHECKLIST.md"
      provides: "Manual/UAT proof for output copy, start modes, relaunch, sealed/diagnostic behavior, and orphan cleanup."
      contains: "AutoStart=false"
  key_links:
    - from: "samples/avalonia-host/MainWindow.axaml"
      to: "samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml"
      via: "sample harness hosts the reusable control in diagnostic mode"
      pattern: "PresentationMode|MdCadEmbeddedControl"
    - from: "samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs"
      to: ".planning/phases/48-reusable-avalonia-mdcad-user-control/48-MANUAL-CHECKLIST.md"
      via: "visible warning/status/start-stop behaviors exercised by the manual checklist"
      pattern: "warning|StartAsync|StopAsync|PresentationMode"
---

<objective>
Close the phase by adding sealed/diagnostic presentation behavior, converting the sample app into the diagnostic proof consumer, and capturing the final manual/regression evidence.

Purpose: P48-06 and P48-07 are the user-visible closure gates. The control must feel productized in sealed mode, remain debuggable in diagnostic mode, and preserve all prior embedded launch contracts under the reusable-control packaging seam.
Output: Final control presentation, sample-host proof harness, `48-MANUAL-CHECKLIST.md`, and a blocking manual verification checkpoint backed by the full regression suite.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/48-reusable-avalonia-mdcad-user-control/48-CONTEXT.md
@.planning/phases/48-reusable-avalonia-mdcad-user-control/48-RESEARCH.md
@.planning/phases/48-reusable-avalonia-mdcad-user-control/48-VALIDATION.md
@.planning/phases/47-sample-host-workflow-proof/47-MANUAL-CHECKLIST.md
@samples/avalonia-host/MainWindow.axaml
@samples/avalonia-host/MainWindow.axaml.cs
@samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs

<interfaces>
Expected reusable control contract available from prior plans:
```csharp
public Task StartAsync();
public Task StopAsync();
public string? JsonlPath { get; set; }
public bool StartupLiveRefreshEnabled { get; set; }
public bool AutoStart { get; set; }
public MdCadPresentationMode PresentationMode { get; set; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement sealed vs diagnostic presentation and convert the sample app into the diagnostic consumer</name>
  <read_first>
    - samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs
    - samples/avalonia-host/MainWindow.axaml
    - samples/avalonia-host/MainWindow.axaml.cs
    - .planning/phases/47-sample-host-workflow-proof/47-MANUAL-CHECKLIST.md
    - .planning/phases/48-reusable-avalonia-mdcad-user-control/48-CONTEXT.md
    - .planning/phases/48-reusable-avalonia-mdcad-user-control/48-RESEARCH.md
  </read_first>
  <files>samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml, samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs, samples/avalonia-host/MainWindow.axaml, samples/avalonia-host/MainWindow.axaml.cs</files>
  <action>Finish the reusable control UI per D-09 through D-16. In `MdCadEmbeddedControl`, keep `Sealed` as the default bare viewer surface with only a compact visible warning region when a requested JSONL path is bad; do not leak the full diagnostic chrome into sealed mode. Add explicit `Diagnostic` mode that carries over the Phase 47 sample host controls/status/failure surface above the mdCAD host region: launch/start, stop/close, launch/attach/jsonl/live-refresh lines, and failure text remain host-owned phrasing, not IPC-derived success claims. Then convert `samples/avalonia-host/MainWindow.axaml` / `.cs` into the in-repo diagnostic consumer of the reusable control by instantiating `MdCadEmbeddedControl` from XAML, exposing harness inputs that let a tester switch `PresentationMode`, toggle `AutoStart`, set valid/empty/invalid `JsonlPath` values, and invoke explicit `StartAsync` / `StopAsync`. Keep test-mode hooks and any extra scenario scaffolding inside the sample harness only; do not expose them from the reusable control API.</action>
  <verify>
    <automated>dotnet build samples/avalonia-host/AvaloniaHost.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `MdCadEmbeddedControl.axaml` contains distinct sealed and diagnostic regions
    - sealed mode remains the default and does not show the full management chrome
    - diagnostic mode shows the host-owned launch/attach/jsonl/live-refresh/failure surface above the viewer
    - the sample host consumes `MdCadEmbeddedControl` from XAML and can drive auto-start, explicit start/stop, and JSONL-path scenarios
  </acceptance_criteria>
  <done>The reusable control has the locked two-mode presentation contract, and the sample app now proves the control as an actual consumer instead of owning the embedding implementation itself.</done>
</task>

<task type="auto">
  <name>Task 2: Write the Phase 48 manual checklist for runtime copy, launch modes, relaunch, and warning proof</name>
  <read_first>
    - .planning/phases/48-reusable-avalonia-mdcad-user-control/48-VALIDATION.md
    - .planning/phases/47-sample-host-workflow-proof/47-MANUAL-CHECKLIST.md
    - samples/avalonia-host/MainWindow.axaml
    - samples/avalonia-host/MainWindow.axaml.cs
  </read_first>
  <files>.planning/phases/48-reusable-avalonia-mdcad-user-control/48-MANUAL-CHECKLIST.md</files>
  <action>Create `.planning/phases/48-reusable-avalonia-mdcad-user-control/48-MANUAL-CHECKLIST.md` as the final UAT contract for Phase 48. Include at minimum: (1) preflight build plus copied-runtime existence check for `samples/avalonia-host/bin/Release/net8.0-windows10.0.19041.0/mdcad-runtime/mdCAD.exe`; (2) default sealed auto-start with a valid JSONL path; (3) `AutoStart=false` plus explicit `StartAsync` / `StopAsync` and orphan inspection with `Get-Process mdCAD -ErrorAction SilentlyContinue`; (4) launch-affecting property changes while running causing one coalesced relaunch using the newest snapshot only; and (5) sealed-vs-diagnostic behavior with empty, missing, and unreadable `JsonlPath` requests. Reuse the honest status wording pattern from Phase 47 and make the checklist directly runnable against the converted sample harness.</action>
  <verify>
    <automated>rg -n "mdcad-runtime|AutoStart=false|StartAsync|StopAsync|sealed|diagnostic|JsonlPath|relaunch" .planning/phases/48-reusable-avalonia-mdcad-user-control/48-MANUAL-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - `48-MANUAL-CHECKLIST.md` includes a copied-runtime existence check
    - the checklist covers auto-start, explicit start/stop, relaunch, sealed/diagnostic mode, and bad-path warning scenarios
    - the checklist includes orphan-process inspection via `Get-Process mdCAD -ErrorAction SilentlyContinue`
  </acceptance_criteria>
  <done>Phase 48 has an explicit manual/UAT contract that matches the validation strategy and can be executed without rediscovering scenarios.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the final harness proof and regression gate</name>
  <read_first>
    - .planning/phases/48-reusable-avalonia-mdcad-user-control/48-MANUAL-CHECKLIST.md
    - .planning/phases/48-reusable-avalonia-mdcad-user-control/48-VALIDATION.md
    - samples/avalonia-host/MainWindow.axaml
    - samples/avalonia-host/MainWindow.axaml.cs
  </read_first>
  <files>.planning/phases/48-reusable-avalonia-mdcad-user-control/48-MANUAL-CHECKLIST.md</files>
  <action>Automate the build/regression pre-steps, then execute the new Phase 48 checklist against the sample harness. This checkpoint closes D-09 through D-16 and preserves the established Phase 43-47 launch boundary: mdCAD must stay a separate launched process, no IPC/status events are introduced, and Windows-only behavior remains intact. Record PASS/FAIL/BLOCKED notes directly in `48-MANUAL-CHECKLIST.md` before resuming.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release &amp;&amp; ctest --test-dir build-vulkan -C Release --output-on-failure &amp;&amp; dotnet build samples/avalonia-host/AvaloniaHost.csproj -c Release</automated>
  </verify>
  <what-built>Final reusable-control harness proof, copied-runtime proof, and native regression gate for Phase 48.</what-built>
  <how-to-verify>
    1) Run `dotnet run --project samples/avalonia-host/AvaloniaHost.csproj -c Release`.
    2) Follow `48-MANUAL-CHECKLIST.md` exactly: verify copied runtime exists, sealed auto-start attaches correctly, `AutoStart=false` waits for explicit start, property changes coalesce to one relaunch, and sealed vs diagnostic behavior matches the checklist.
    3) After every stop/close scenario, run `Get-Process mdCAD -ErrorAction SilentlyContinue` and confirm no orphaned `mdCAD.exe` remains.
    4) Mark the checklist PASS/FAIL/BLOCKED with notes, then reply with the resulting overall status.
  </how-to-verify>
  <resume-signal>Reply with "approved" if the checklist passes, or provide the failing checklist item IDs and observed behavior.</resume-signal>
  <acceptance_criteria>Manual proof records explicit outcomes for runtime copy, start modes, relaunch behavior, sealed/diagnostic presentation, warning visibility, and orphan cleanup; the automated native regression gate is green.</acceptance_criteria>
  <done>Phase 48 is manually and automatically proven without reopening the no-IPC, separate-process, Windows-only embedding contract.</done>
</task>

</tasks>

<verification>
- `dotnet build samples/avalonia-host/AvaloniaHost.csproj -c Release`
- `cmake --build build-vulkan --config Release && ctest --test-dir build-vulkan -C Release --output-on-failure && dotnet build samples/avalonia-host/AvaloniaHost.csproj -c Release`
- Confirm `48-MANUAL-CHECKLIST.md` captures the final PASS/FAIL/BLOCKED evidence after the checkpoint.
</verification>

<success_criteria>
- Sealed mode is default and minimal; diagnostic mode is explicit and carries the host-owned control/status chrome.
- A bad requested JSONL path stays visible without breaking viewer usability.
- The sample host proves copied-runtime launch, explicit start/stop, coalesced relaunch, and orphan-free teardown with the reusable control.
- Native regression tests from prior embedding phases remain green.
</success_criteria>

<output>
After completion, create `.planning/phases/48-reusable-avalonia-mdcad-user-control/48-04-SUMMARY.md`
</output>
