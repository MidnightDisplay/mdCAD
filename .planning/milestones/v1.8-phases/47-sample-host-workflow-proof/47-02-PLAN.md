---
phase: 47-sample-host-workflow-proof
plan: 02
type: execute
wave: 2
depends_on:
  - 47-01
files_modified:
  - samples/avalonia-host/MainWindow.axaml
  - samples/avalonia-host/MainWindow.axaml.cs
  - .planning/phases/47-sample-host-workflow-proof/47-MANUAL-CHECKLIST.md
autonomous: true
requirements:
  - HOST-03
  - HOST-04
must_haves:
  truths:
    - "The sample host shows separate host-owned status text for launch, attach, JSONL request, and live-refresh request without inventing import-confirmation IPC."
    - "A developer can launch, close, and relaunch an embedded mdCAD session from the same host window while resize and focus behavior continue to follow the already-proven Phase 44 rules."
    - "Closing a session or the host window leaves no orphaned `mdCAD.exe` process behind after the graceful-wait/fallback-cleanup seam finishes."
  artifacts:
    - path: "samples/avalonia-host/MainWindow.axaml"
      provides: "Minimal workflow-focused host controls and explicit status surfaces."
      contains: "Launch Session"
    - path: "samples/avalonia-host/MainWindow.axaml.cs"
      provides: "Reusable session controller for repeated launch/close/relaunch, placeholder recreation, and host-owned lifecycle status."
      contains: "Close Session"
    - path: ".planning/phases/47-sample-host-workflow-proof/47-MANUAL-CHECKLIST.md"
      provides: "Manual proof steps for bundled example launch, repeated lifecycle loops, focus/resize checks, and orphan-process inspection."
      contains: "Get-Process mdCAD"
  key_links:
    - from: "samples/avalonia-host/MainWindow.axaml"
      to: "samples/avalonia-host/MainWindow.axaml.cs"
      via: "Launch Session / Close Session / live-refresh controls bound into the host session controller"
      pattern: "Launch Session|Close Session|Live refresh"
    - from: "samples/avalonia-host/MainWindow.axaml.cs"
      to: "samples/avalonia-host/MainWindow.axaml.cs"
      via: "relaunch path reuses graceful teardown, resets process state, and recreates the placeholder before a new launch"
      pattern: "InvalidatePlaceholderForTeardown|WaitForGracefulExitOnClose|CreateNativeControlCore"
    - from: ".planning/phases/47-sample-host-workflow-proof/47-MANUAL-CHECKLIST.md"
      to: "samples/avalonia-host/MainWindow.axaml.cs"
      via: "manual conformance steps target the exact host controls and status text shipped by the harness"
      pattern: "Launch Session|Close Session|launch:|attach:"
---

<objective>
Turn the sample host into the minimal conformance harness for repeated embedded lifecycle proof and clear host-side workflow status.

Purpose: HOST-03 and HOST-04 close only when the host can report what it really knows, reuse the proven teardown/focus seams, and support repeated launch/close cycles without orphaning mdCAD.
Output: Minimal host controls/status UI, a repeatable session controller in `MainWindow.axaml.cs`, and a Phase 47 manual checklist bound to the MSVC Vulkan validation loop.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/47-sample-host-workflow-proof/47-RESEARCH.md
@.planning/phases/47-sample-host-workflow-proof/47-VALIDATION.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-03-SUMMARY.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-06-SUMMARY.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-07-SUMMARY.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-08-SUMMARY.md
@samples/avalonia-host/MainWindow.axaml
@samples/avalonia-host/MainWindow.axaml.cs

<interfaces>
From samples/avalonia-host/MainWindow.axaml.cs:
```csharp
private const string StatusLaunching = "launching";
private const string StatusWaitingForChildAttach = "waiting for child attach";
private const string StatusAttached = "attached";
private const string StatusTeardownCleanup = "teardown/cleanup";
private const string StatusTimeoutFailure = "timeout/failure";

private void LaunchEmbeddedViewer()
private void InvalidatePlaceholderForTeardown(string detail)
private void WaitForGracefulExitOnClose(string detail)
private void FinalizeTeardownStatus(int exitCode)
private void OnHostChromePointerPressed(object? sender, PointerPressedEventArgs e)
```

From samples/avalonia-host/MainWindow.axaml:
```xml
<TextBlock x:Name="StatusTextBlock" />
<TextBlock x:Name="ModeTextBlock" />
<TextBlock x:Name="FailureTextBlock" />
<local:EmbedNativeControlHost x:Name="EmbedSurface" />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add minimal session controls and refactor the host into a repeatable launch/close controller</name>
  <files>samples/avalonia-host/MainWindow.axaml, samples/avalonia-host/MainWindow.axaml.cs</files>
  <read_first>samples/avalonia-host/MainWindow.axaml, samples/avalonia-host/MainWindow.axaml.cs, .planning/phases/47-sample-host-workflow-proof/47-RESEARCH.md, .planning/phases/47-sample-host-workflow-proof/47-VALIDATION.md, .planning/phases/44-embedded-resize-focus-viewer-layout/44-03-SUMMARY.md, .planning/phases/44-embedded-resize-focus-viewer-layout/44-06-SUMMARY.md, .planning/phases/44-embedded-resize-focus-viewer-layout/44-07-SUMMARY.md, .planning/phases/44-embedded-resize-focus-viewer-layout/44-08-SUMMARY.md</read_first>
  <action>Keep the host minimal and workflow-focused. Update `MainWindow.axaml` to add exactly these developer-facing controls/status surfaces above the embed region: a `Launch Session` button, a `Close Session` button, a `Live refresh` checkbox, and separate status text lines for `launch:`, `attach:`, `jsonl:`, and `live refresh:`. Refactor `MainWindow.axaml.cs` so the host no longer behaves as a one-shot auto-launch scaffold: centralize session reset, stop timers and clear cached failure/teardown/process state between runs, reuse the existing graceful teardown + fallback cleanup seam before relaunch, and recreate the placeholder/host surface before starting a second embedded session because the current teardown destroys that HWND. Preserve the locked boundaries from earlier phases: no new IPC surface, no host-side import-success claims, live refresh remains explicit/default-off, and focus policy stays unchanged (host focus only through Avalonia APIs; mdCAD still claims focus on first click). Preserve the HOST-02 proof detail from 47-01 by keeping the exact resolved absolute example path visible in host-owned pre-launch JSONL status/detail text whenever the bundled example resolves successfully. Use host-owned wording such as `launch: idle|launching|close requested|cleanup complete`, `attach: waiting|attached|timed out|child lost`, `jsonl: example resolved -> <absolute-path>; --jsonl requested|example missing|viewer-managed after launch`, and `live refresh: off|requested|viewer-managed after launch`.</action>
  <verify>
    <automated>dotnet build samples/avalonia-host/AvaloniaHost.csproj -c Release &amp;&amp; ctest --test-dir build-vulkan -C Release --output-on-failure -R "(embed_launch_config_test|embed_input_state_test|embed_layout_state_test)"</automated>
  </verify>
  <acceptance_criteria>
    - `samples/avalonia-host/MainWindow.axaml` contains `Launch Session`
    - `samples/avalonia-host/MainWindow.axaml` contains `Close Session`
    - `samples/avalonia-host/MainWindow.axaml` contains `Live refresh`
    - `samples/avalonia-host/MainWindow.axaml.cs` contains `launch:`
    - `samples/avalonia-host/MainWindow.axaml.cs` contains `attach:`
    - `samples/avalonia-host/MainWindow.axaml.cs` contains `jsonl:`
    - `samples/avalonia-host/MainWindow.axaml.cs` contains `live refresh:`
    - `samples/avalonia-host/MainWindow.axaml.cs` contains `example resolved ->`
    - `samples/avalonia-host/MainWindow.axaml.cs` contains `_mdcadProcess = null;`
  </acceptance_criteria>
  <done>The host can repeatedly launch and close embedded sessions from the same window while showing honest, host-owned lifecycle state, preserving the resolved-example path proof, and keeping the proven focus/resize/teardown seams.</done>
</task>

<task type="auto">
  <name>Task 2: Write the Phase 47 manual proof checklist and bind it to the MSVC Vulkan validation loop</name>
  <files>.planning/phases/47-sample-host-workflow-proof/47-MANUAL-CHECKLIST.md</files>
  <read_first>.planning/phases/47-sample-host-workflow-proof/47-VALIDATION.md, .planning/phases/47-sample-host-workflow-proof/47-RESEARCH.md, .planning/phases/44-embedded-resize-focus-viewer-layout/44-MANUAL-CHECKLIST.md, samples/avalonia-host/MainWindow.axaml, samples/avalonia-host/MainWindow.axaml.cs</read_first>
  <action>Create `.planning/phases/47-sample-host-workflow-proof/47-MANUAL-CHECKLIST.md` with numbered scenarios that exactly match the Phase 47 validation contract: (1) bundled example launch with live refresh off, including confirmation that host-visible JSONL status/detail text shows the resolved absolute example path before launch, (2) bundled example launch with live refresh on, (3) repeated `Launch Session` → resize/focus → `Close Session` → relaunch cycle, (4) orphan-process inspection after close using `Get-Process mdCAD -ErrorAction SilentlyContinue`, and (5) bundled-example failure-path validation where the copied example is temporarily unavailable and the host reports the issue before launch. Use the MSVC Vulkan build path in every command: `cmake --build build-vulkan --config Release`, `dotnet run --project samples/avalonia-host/AvaloniaHost.csproj -c Release`, and the exact phase-gate commands from `47-VALIDATION.md`. Make the checklist explicitly distinguish host-inferred status from viewer-managed runtime import/refresh truth.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release &amp;&amp; ctest --test-dir build-vulkan -C Release --output-on-failure &amp;&amp; dotnet build samples/avalonia-host/AvaloniaHost.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/47-sample-host-workflow-proof/47-MANUAL-CHECKLIST.md` contains `Scenario 1`
    - `.planning/phases/47-sample-host-workflow-proof/47-MANUAL-CHECKLIST.md` contains `Launch Session`
    - `.planning/phases/47-sample-host-workflow-proof/47-MANUAL-CHECKLIST.md` contains `Close Session`
    - `.planning/phases/47-sample-host-workflow-proof/47-MANUAL-CHECKLIST.md` contains `Get-Process mdCAD -ErrorAction SilentlyContinue`
    - `.planning/phases/47-sample-host-workflow-proof/47-MANUAL-CHECKLIST.md` contains `build-vulkan`
    - `.planning/phases/47-sample-host-workflow-proof/47-MANUAL-CHECKLIST.md` contains `resolved absolute example path`
    - `.planning/phases/47-sample-host-workflow-proof/47-MANUAL-CHECKLIST.md` contains `viewer-managed`
  </acceptance_criteria>
  <done>The phase ships with an explicit manual proof artifact that covers bundled launch, repeated lifecycle behavior, failure-path validation, and orphan-process inspection using the MSVC Vulkan workflow.</done>
</task>

</tasks>

<verification>
- `dotnet build samples/avalonia-host/AvaloniaHost.csproj -c Release`
- `ctest --test-dir build-vulkan -C Release --output-on-failure -R "(embed_launch_config_test|embed_input_state_test|embed_layout_state_test)"`
- `cmake --build build-vulkan --config Release && ctest --test-dir build-vulkan -C Release --output-on-failure && dotnet build samples/avalonia-host/AvaloniaHost.csproj -c Release`
- Follow `.planning/phases/47-sample-host-workflow-proof/47-MANUAL-CHECKLIST.md` to prove repeated launch/resize/focus/close/relaunch leaves no orphaned `mdCAD.exe`.
</verification>

<success_criteria>
- The host shows distinct launch, attach, JSONL, and live-refresh status text without adding IPC.
- The same host window can launch, close, and relaunch mdCAD while preserving Phase 44 focus/resize behavior and cleanup guarantees.
- Phase 47 has a checklist-bound manual proof path that uses the MSVC Vulkan `build-vulkan` workflow and checks for orphan-free shutdown.
</success_criteria>

<output>
After completion, create `.planning/phases/47-sample-host-workflow-proof/47-02-SUMMARY.md`
</output>
