---
phase: 46-launch-time-live-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app_launch_config.h
  - src/startup_jsonl_import_controller.h
  - src/tests/embed_launch_config_test.c
  - src/tests/startup_jsonl_import_controller_test.c
autonomous: true
requirements:
  - JSON-02
  - JSON-04
must_haves:
  truths:
    - "Developer can opt into startup live refresh only with the explicit `--jsonl-live-refresh` companion flag."
    - "Plain `--jsonl <absolute-path>` keeps startup import behavior default-off for live refresh."
    - "Startup import passes live-refresh intent into the existing observer contract instead of creating a second refresh implementation."
  artifacts:
    - path: "src/app_launch_config.h"
      provides: "Launch parser support for `--jsonl-live-refresh` with `--jsonl` dependency enforcement."
      contains: "startup_jsonl_live_refresh"
    - path: "src/startup_jsonl_import_controller.h"
      provides: "Header-only startup controller that captures live-refresh intent and forwards it to `jsonl_import_job_set_observer_contract(...)`."
      contains: "live_refresh_enabled"
    - path: "src/tests/embed_launch_config_test.c"
      provides: "Automated parser coverage for explicit opt-in, duplicate flag rejection, and `--jsonl` dependency failures."
      contains: "jsonl_live_refresh"
    - path: "src/tests/startup_jsonl_import_controller_test.c"
      provides: "Automated controller coverage for default-off and opt-in observer-contract bridging."
      contains: "startup_jsonl_controller"
  key_links:
    - from: "src/app_launch_config.h"
      to: "src/startup_jsonl_import_controller.h"
      via: "explicit `startup_jsonl_live_refresh` bool copied from launch config into controller arm state"
      pattern: "startup_jsonl_live_refresh|startup_jsonl_import_controller_arm"
    - from: "src/startup_jsonl_import_controller.h"
      to: "src/jsonl_import_job.h"
      via: "controller-owned observer contract reuse"
      pattern: "jsonl_import_job_set_observer_contract"
---

<objective>
Lock the explicit launch flag and controller contract for startup-linked live refresh.

Purpose: Phase 46 starts by adding the opt-in contract without changing Phase 45 default-off startup import behavior or Phase 35/39 observer semantics.
Output: A tested `--jsonl-live-refresh` parser path plus a startup controller that forwards live-refresh intent into the existing observer contract.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/46-launch-time-live-refresh/46-RESEARCH.md
@.planning/phases/46-launch-time-live-refresh/46-VALIDATION.md
@.planning/phases/45-startup-jsonl-auto-import/45-01-SUMMARY.md
@src/app_launch_config.h
@src/startup_jsonl_import_controller.h
@src/jsonl_import_job.h
@src/tests/embed_launch_config_test.c
@src/tests/startup_jsonl_import_controller_test.c

<interfaces>
From src/app_launch_config.h:
```c
typedef struct {
    bool embedded;
    uintptr_t parent_hwnd_value;
    char startup_jsonl_path[MDCAD_STARTUP_JSONL_PATH_MAX];
} mdcad_launch_config_t;

static inline bool mdcad_launch_config_parse(int argc,
                                             char** argv,
                                             mdcad_launch_config_t* out_cfg,
                                             char* error_buffer,
                                             size_t error_buffer_size);
```

From src/startup_jsonl_import_controller.h:
```c
typedef struct {
    startup_jsonl_import_state_t state;
    jsonl_import_job_t job;
    char requested_path[MDCAD_STARTUP_JSONL_PATH_MAX];
    char status_text[128];
    char error_text[128];
    bool scene_dirty;
} startup_jsonl_import_controller_t;

static inline bool startup_jsonl_import_controller_arm(startup_jsonl_import_controller_t *controller,
                                                       const char *path);
```

From src/jsonl_import_job.h:
```c
static inline void jsonl_import_job_set_observer_contract(jsonl_import_job_t *job,
                                                          bool link_enabled,
                                                          const char *source_path);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend parser and controller contracts for explicit startup live refresh</name>
  <files>src/tests/embed_launch_config_test.c, src/tests/startup_jsonl_import_controller_test.c</files>
  <behavior>
    - Test 1: `--jsonl --jsonl-live-refresh` parses successfully in standalone and embedded launches, with the live-refresh bit latched in launch config.
    - Test 2: `--jsonl-live-refresh` alone fails fast, duplicate `--jsonl-live-refresh` fails fast, and plain `--jsonl` still parses with live refresh OFF by default.
    - Test 3: Startup controller arm/tick uses `jsonl_import_job_set_observer_contract(..., false, path)` when live refresh is not requested.
    - Test 4: Startup controller arm/tick uses `jsonl_import_job_set_observer_contract(..., true, path)` when live refresh is requested, without changing flat import defaults.
  </behavior>
  <action>Extend `embed_launch_config_test.c` with success/failure cases for a valueless `--jsonl-live-refresh` flag that is scoped to startup JSONL only. Extend `startup_jsonl_import_controller_test.c` so it asserts both default-off and opt-in observer-contract behavior after `startup_jsonl_import_controller_tick(...)` completes a tiny fixture import. Keep the contract strict: no generic `--live-refresh`, no filesystem probing at parse time, no app/runtime overlay assertions here, and no host/sample wiring.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target embed_launch_config_test startup_jsonl_import_controller_test &amp;&amp; ctest --test-dir build-vulkan -C Release --output-on-failure -R "(embed_launch_config_test|startup_jsonl_import_controller_test)"</automated>
  </verify>
  <done>The test suite describes the explicit opt-in CLI contract and the controller's default-off versus opt-in observer handoff before implementation is finalized.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the `--jsonl-live-refresh` launch flag and controller opt-in bridge</name>
  <files>src/app_launch_config.h, src/startup_jsonl_import_controller.h</files>
  <action>Add `bool startup_jsonl_live_refresh` to `mdcad_launch_config_t` and parse `--jsonl-live-refresh` as a valueless flag that may appear at most once, requires `--jsonl`, and otherwise leaves the Phase 43 unknown-flag tolerance unchanged. Extend `startup_jsonl_import_controller_t` and `startup_jsonl_import_controller_arm(...)` to store `live_refresh_enabled` explicitly, then forward that bit through `jsonl_import_job_set_observer_contract(&controller->job, controller->live_refresh_enabled, controller->requested_path)`. Preserve all Phase 45 defaults: scale `1.0f`, JSONL colours ON, white fallback colour, no center shift, zero rotation, mesh mode `0`, sync-on-tiny-files, and non-fatal runtime errors. Do not add a second polling loop, do not touch `app.c` yet, and do not enable live refresh when only `--jsonl` is present.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target embed_launch_config_test startup_jsonl_import_controller_test jsonl_flat_observer_manual_refresh_test jsonl_flat_observer_auto_safety_test &amp;&amp; ctest --test-dir build-vulkan -C Release --output-on-failure -R "(embed_launch_config_test|startup_jsonl_import_controller_test|jsonl_flat_observer_manual_refresh_test|jsonl_flat_observer_auto_safety_test)"</automated>
  </verify>
  <done>The launch parser and startup controller can opt into linked refresh explicitly while plain startup import remains unlinked and existing observer semantics stay untouched.</done>
</task>

</tasks>

<verification>
- `cmake --build build-vulkan --config Release --target embed_launch_config_test startup_jsonl_import_controller_test jsonl_flat_observer_manual_refresh_test jsonl_flat_observer_auto_safety_test`
- `ctest --test-dir build-vulkan -C Release --output-on-failure -R "(embed_launch_config_test|startup_jsonl_import_controller_test|jsonl_flat_observer_manual_refresh_test|jsonl_flat_observer_auto_safety_test)"`
</verification>

<success_criteria>
- `--jsonl-live-refresh` exists as an explicit companion flag, not a default behavior change.
- Startup controller bridges only intent into the proven observer contract.
- Focused parser/controller/observer regression tests stay green on `build-vulkan`.
</success_criteria>

<output>
After completion, create `.planning/phases/46-launch-time-live-refresh/46-01-SUMMARY.md`
</output>
