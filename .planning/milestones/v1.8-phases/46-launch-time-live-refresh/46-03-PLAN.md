---
phase: 46-launch-time-live-refresh
plan: 03
type: execute
wave: 3
depends_on:
  - 46-01
  - 46-02
files_modified:
  - src/tests/jsonl_flat_observer_auto_safety_test.c
  - src/tests/jsonl_flat_observer_manual_refresh_test.c
autonomous: true
requirements:
  - JSON-02
  - JSON-04
must_haves:
  truths:
    - "Startup-style import without the explicit live-refresh opt-in stays idle when the source file changes."
    - "Startup-style import with the explicit opt-in reuses the same baseline stamping, auto-refresh, coalescing, and safety-disable behavior as existing linked flat imports."
    - "Commit-on-success and manual recovery semantics remain unchanged for startup-linked roots."
  artifacts:
    - path: "src/tests/jsonl_flat_observer_auto_safety_test.c"
      provides: "Automated runtime coverage for startup-style default-off versus linked auto-refresh semantics."
      contains: "test_flat_"
    - path: "src/tests/jsonl_flat_observer_manual_refresh_test.c"
      provides: "Automated runtime coverage for startup-linked commit-on-success, last-good preservation, and manual recovery."
      contains: "test_flat_"
  key_links:
    - from: "src/tests/jsonl_flat_observer_auto_safety_test.c"
      to: "src/jsonl_observer_system.h"
      via: "real observer ticks prove startup-linked roots use existing auto-refresh semantics"
      pattern: "jsonl_observer_system_tick|jsonl_observer_tick_flat_refreshes"
    - from: "src/tests/jsonl_flat_observer_manual_refresh_test.c"
      to: "src/jsonl_observer_system.h"
      via: "manual refresh and failure recovery continue to use existing commit-on-success behavior"
      pattern: "jsonl_observer_manual_reparse|jsonl_observer_request_flat_refresh"
---

<objective>
Close Phase 46 with runtime regression tests that prove default-off safety and observer-semantic reuse.

Purpose: The phase is only complete when automated coverage shows that startup-linked refresh behaves exactly like existing linked flat imports while plain startup import stays unchanged.
Output: Extended observer regression tests plus the focused/full validation commands needed for phase closure.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/46-launch-time-live-refresh/46-RESEARCH.md
@.planning/phases/46-launch-time-live-refresh/46-VALIDATION.md
@.planning/phases/45-startup-jsonl-auto-import/45-01-SUMMARY.md
@.planning/phases/45-startup-jsonl-auto-import/45-02-SUMMARY.md
@src/jsonl_import_job.h
@src/jsonl_observer_system.h
@src/tests/startup_jsonl_import_controller_test.c
@src/tests/jsonl_flat_observer_auto_safety_test.c
@src/tests/jsonl_flat_observer_manual_refresh_test.c

<interfaces>
From src/jsonl_import_job.h:
```c
static inline void jsonl_import_job_set_observer_contract(jsonl_import_job_t *job,
                                                          bool link_enabled,
                                                          const char *source_path);
```

From src/jsonl_observer_system.h:
```c
static inline bool jsonl_observer_is_flat_refresh_running(ecs_scene_t *scene, ecs_entity_t root_entity);
static inline void jsonl_observer_tick_flat_refreshes(ecs_scene_t *scene);
static inline void jsonl_observer_system_tick(ecs_scene_t *scene,
                                              uint64_t now_ms,
                                              selection_buffer_t *selection);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend automatic observer safety coverage for startup-style linked versus unlinked imports</name>
  <files>src/tests/jsonl_flat_observer_auto_safety_test.c</files>
  <action>Add startup-style regression cases that import through the existing flat import helpers with `link_enabled=false` and `link_enabled=true` to mirror Phase 46 launch behavior. For the unlinked case, mutate the source file and prove the imported root stays idle with no auto-refresh activity and no baseline-driven observer state. For the linked case, prove baseline stamping, idle-before-change behavior, one-change auto-refresh, coalescing, and retry/auto-disable safety still flow through `jsonl_observer_system_tick(...)` and `jsonl_observer_tick_flat_refreshes(...)` exactly as they do for existing linked flat imports. Do not add app/CLI assertions here; this plan is runtime regression proof only.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target jsonl_flat_observer_auto_safety_test startup_jsonl_import_controller_test &amp;&amp; ctest --test-dir build-vulkan -C Release --output-on-failure -R "(jsonl_flat_observer_auto_safety_test|startup_jsonl_import_controller_test)"</automated>
  </verify>
  <done>Automated runtime coverage now proves startup-style unlinked imports stay default-off while linked startup imports reuse the existing auto-refresh safety pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: Extend manual refresh regression coverage and run the phase gate suite</name>
  <files>src/tests/jsonl_flat_observer_manual_refresh_test.c</files>
  <action>Add startup-linked regression cases that mutate a startup-style linked source and prove commit-on-success replacement, last-good preservation on failed refresh, and manual recovery after auto-disable still hold for the imported root. Reuse existing fixture helpers and selection/anchor assertions so the new cases prove startup-linked roots are not a special observer class. After the new cases are in place, run the Phase 46 quick and full validation commands from `46-VALIDATION.md`; if a pre-existing unrelated failure appears, stop and document it in the summary instead of weakening the new assertions.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target jsonl_flat_observer_manual_refresh_test jsonl_flat_observer_auto_safety_test startup_jsonl_import_controller_test embed_launch_config_test startup_jsonl_app_contract_test mdCAD &amp;&amp; ctest --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>The full Phase 46 regression gate proves startup-linked refresh preserves existing commit-on-success/manual-recovery semantics and the wider suite still passes.</done>
</task>

</tasks>

<verification>
- `ctest --test-dir build-vulkan -C Release --output-on-failure -R "(embed_launch_config_test|startup_jsonl_import_controller_test|startup_jsonl_app_contract_test|jsonl_flat_observer_manual_refresh_test|jsonl_flat_observer_auto_safety_test)"`
- `ctest --test-dir build-vulkan -C Release --output-on-failure`
- Manual follow-up for `/gsd-verify-work`: launch once with `--jsonl <absolute-path>` and once with `--jsonl <absolute-path> --jsonl-live-refresh`, edit the file on disk, and confirm only the opt-in launch auto-refreshes.
</verification>

<success_criteria>
- Plain startup import is still default-off for live refresh.
- Opt-in startup-linked refresh reuses the existing observer runtime and commit-on-success behavior.
- Phase 46 closes with focused and full `build-vulkan` CTest coverage green.
</success_criteria>

<output>
After completion, create `.planning/phases/46-launch-time-live-refresh/46-03-SUMMARY.md`
</output>
