---
phase: 44-embedded-resize-focus-viewer-layout
plan: 07
type: execute
wave: 1
depends_on:
  - 44-03
  - 44-05
files_modified:
  - vendors/libsokol/patches/0001-win32-embed-child-window-bootstrap.patch
  - samples/avalonia-host/MainWindow.axaml
  - samples/avalonia-host/MainWindow.axaml.cs
autonomous: true
gap_closure: true
requirements:
  - INPT-02
must_haves:
  truths:
    - "Embedded attach still does not auto-focus mdCAD."
    - "One deliberate click inside the embedded viewer transfers Win32 keyboard focus into the mdCAD child HWND so text entry, Tab, clipboard shortcuts, and undo work."
    - "Clicking host chrome returns keyboard focus to the Avalonia host instead of leaving it stuck on the embedded child."
  artifacts:
    - path: "vendors/libsokol/patches/0001-win32-embed-child-window-bootstrap.patch"
      provides: "An embedded-only native first-click focus acquisition path that runs inside mdCAD's own Win32 window procedure."
      contains: "WM_MOUSEACTIVATE|WM_LBUTTONDOWN|SetFocus"
    - path: "samples/avalonia-host/MainWindow.axaml"
      provides: "A focusable host-chrome surface that can deliberately reclaim Avalonia focus."
      contains: "Focusable=|PointerPressed="
    - path: "samples/avalonia-host/MainWindow.axaml.cs"
      provides: "Host-chrome focus-return helpers without forcing Win32 focus into the mdCAD child."
      contains: "OnHostChromePointerPressed|Focus"
  key_links:
    - from: "vendors/libsokol/patches/0001-win32-embed-child-window-bootstrap.patch"
      to: "src/app.c"
      via: "the mdCAD child HWND claims focus from its own native click path before embedded input state and shortcut gates react to the first click"
      pattern: "WM_MOUSEACTIVATE|WM_LBUTTONDOWN|SetFocus"
    - from: "samples/avalonia-host/MainWindow.axaml"
      to: "samples/avalonia-host/MainWindow.axaml.cs"
      via: "pointer-driven host-chrome focus return"
      pattern: "PointerPressed"
---

<objective>
Close the remaining first-click keyboard-ownership gap through the native child-window activation seam, not through host-forced focus.

Purpose: Honor D-01, D-02, D-03, and D-04 after the human re-check showed that mouse input reaches mdCAD but the mdCAD child HWND still never becomes the Win32 focus window. The fix must live where research says it belongs: inside mdCAD's own embedded Win32 path, with only optional host-side focus return for host chrome.
Output: mdCAD claims focus from its own first-click native path, attach stays unfocused, and host chrome can reclaim focus without proxying keyboard input.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-CONTEXT.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-HUMAN-UAT.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-MANUAL-CHECKLIST.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-03-SUMMARY.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-05-SUMMARY.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-RESEARCH.md
@vendors/libsokol/patches/0001-win32-embed-child-window-bootstrap.patch
@samples/avalonia-host/MainWindow.axaml
@samples/avalonia-host/MainWindow.axaml.cs

<interfaces>
From vendors/libsokol/patches/0001-win32-embed-child-window-bootstrap.patch:
```diff
_SOKOL_PRIVATE LRESULT CALLBACK _sapp_win32_wndproc(HWND hWnd, UINT uMsg, WPARAM wParam, LPARAM lParam)
_SOKOL_PRIVATE void _sapp_win32_create_window(void)
```

From samples/avalonia-host/MainWindow.axaml:
```xml
<StackPanel Spacing="4">
  <TextBlock x:Name="StatusTextBlock" />
  <TextBlock x:Name="ModeTextBlock" />
  <TextBlock x:Name="FailureTextBlock" />
</StackPanel>
<local:EmbedNativeControlHost x:Name="EmbedSurface" />
```

From samples/avalonia-host/MainWindow.axaml.cs:
```csharp
private readonly EmbedNativeControlHost _embedSurface;
private void OnWindowClosed(object? sender, EventArgs e);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an embedded-only native first-click focus acquisition path inside mdCAD's Win32 child window</name>
  <files>vendors/libsokol/patches/0001-win32-embed-child-window-bootstrap.patch</files>
  <action>Update the Sokol embed patch so the embedded mdCAD child HWND claims keyboard focus from its own native click/activation path instead of relying on Avalonia to force focus across processes. Extend the patched Win32 window procedure with an embedded-only first-click activation seam such as `WM_MOUSEACTIVATE` and/or client button-down handling that runs in mdCAD's own thread, preserves the original click, and calls `SetFocus(hWnd)` only from inside the child-window procedure when the window is embedded and valid. Do not auto-focus at launch, attach, resize, or timer-driven points. Do not add host-side keyboard forwarding, synthetic keystrokes, or host-side `SetFocus`/`SetActiveWindow` into the external child HWND. Keep the existing embedded unfocus/deactivate cancellation behavior intact so `WM_KILLFOCUS` still clears mdCAD-owned interaction state when focus leaves the child.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD embed_input_state_test &amp;&amp; ctest --test-dir build-vulkan -C Release --output-on-failure -R "embed_input_state_test" &amp;&amp; python -c "from pathlib import Path; import re; patch=Path(r'vendors/libsokol/patches/0001-win32-embed-child-window-bootstrap.patch').read_text(encoding='utf-8'); assert re.search(r'WM_MOUSEACTIVATE|WM_[LRM]BUTTONDOWN', patch), 'missing native first-click activation seam'; assert 'SetFocus' in patch, 'missing child-thread focus acquisition'; host=Path(r'samples/avalonia-host/MainWindow.axaml.cs').read_text(encoding='utf-8'); assert 'SetActiveWindow' not in host and 'SetFocus(' not in host, 'host must not force external child focus'; print('native-embedded-focus-seam-wired')"</automated>
  </verify>
  <done>Attach still starts unfocused, and one deliberate click now causes the mdCAD child HWND to own Win32 keyboard focus from within its own message path so typing, Tab, clipboard shortcuts, and undo can work.</done>
</task>

<task type="auto">
  <name>Task 2: Give host chrome an explicit Avalonia-side focus return path</name>
  <files>samples/avalonia-host/MainWindow.axaml, samples/avalonia-host/MainWindow.axaml.cs</files>
  <action>If host-chrome clicks do not already reclaim focus reliably after Task 1, make the status/chrome region focusable and wire a small Avalonia pointer handler that restores focus to the host side when the user clicks outside the embedded viewer. Keep this strictly on the Avalonia side: reclaim host focus with Avalonia focus APIs or window focus, do not touch child HWND focus from host code, and do not add extra visible controls.</action>
  <verify>
    <automated>dotnet build samples/avalonia-host/AvaloniaHost.csproj -c Release &amp;&amp; python -c "from pathlib import Path; axaml=Path(r'samples/avalonia-host/MainWindow.axaml').read_text(encoding='utf-8'); cs=Path(r'samples/avalonia-host/MainWindow.axaml.cs').read_text(encoding='utf-8'); assert 'PointerPressed' in axaml or 'AddHandler' in cs, 'missing host-chrome focus-return hook'; assert 'Focusable=' in axaml or '.Focus(' in cs, 'missing host-side focus return'; print('host-focus-return-wired')"</automated>
  </verify>
  <done>Users can click back onto the host chrome and mdCAD cleanly loses keyboard ownership without any host-side proxying into the embedded child.</done>
</task>

</tasks>

<verification>
Re-run Human UAT Scenario 2 with `dotnet run --project samples/avalonia-host/AvaloniaHost.csproj -c Release`: confirm attach does not auto-focus mdCAD, one deliberate click inside the viewer enables typing plus `Tab`/clipboard/undo inside mdCAD, and clicking the host status/chrome area returns focus to the host.
</verification>

<success_criteria>
- Human UAT Gap 1 is closed.
- The first-click focus fix lives in mdCAD's native embedded Win32 path, not in host-side `SetFocus`/`SetActiveWindow` calls.
- D-04 remains intact: mdCAD is still not auto-focused at attach.
</success_criteria>

<output>
After completion, create `.planning/phases/44-embedded-resize-focus-viewer-layout/44-07-SUMMARY.md`
</output>
