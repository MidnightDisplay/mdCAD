---
phase: 44-embedded-resize-focus-viewer-layout
plan: 06
type: execute
wave: 1
depends_on:
  - 44-02
  - 44-03
  - 44-04
files_modified:
  - samples/avalonia-host/MainWindow.axaml.cs
  - src/imgui_storage.h
  - .planning/phases/44-embedded-resize-focus-viewer-layout/44-MANUAL-CHECKLIST.md
autonomous: true
gap_closure: true
requirements:
  - EMBD-04
  - INPT-04
must_haves:
  truths:
    - "Destroyed-parent mode waits for mdCAD's invalid-parent self-exit before using host fallback cleanup, unless self-exit truly fails."
    - "Closing the host gives embedded mdCAD enough time to flush `imgui.embedded.ini` before `Process.Kill(true)` is used."
    - "Phase 44 layout-persistence verification resets and inspects the real embedded store location under `build-vulkan\\bin\\Release`, not repo root."
  artifacts:
    - path: "samples/avalonia-host/MainWindow.axaml.cs"
      provides: "Deterministic self-exit wait and graceful-close fallback handling for embedded teardown."
      contains: "EnsureAttachedLifecycleIsHealthy|OnWindowClosed"
    - path: "src/imgui_storage.h"
      provides: "Explicit native embedded-store path contract for `imgui.embedded.ini`."
      contains: "imgui_storage_native_"
    - path: ".planning/phases/44-embedded-resize-focus-viewer-layout/44-MANUAL-CHECKLIST.md"
      provides: "Manual verification steps that target the real embedded ini path and the corrected close behavior."
      contains: "build-vulkan\\bin\\Release\\imgui.embedded.ini"
  key_links:
    - from: "samples/avalonia-host/MainWindow.axaml.cs"
      to: "mdCAD.exe"
      via: "wait for self-exit first, then fallback `Process.Kill(true)` only on verified failure"
      pattern: "WaitForExit|Kill\\(true\\)"
    - from: "samples/avalonia-host/MainWindow.axaml.cs"
      to: "src/imgui_storage.h"
      via: "host-close grace gives `imgui_storage_shutdown()` time to flush the embedded store"
      pattern: "OnWindowClosed|imgui.embedded.ini"
    - from: ".planning/phases/44-embedded-resize-focus-viewer-layout/44-MANUAL-CHECKLIST.md"
      to: "build-vulkan\\bin\\Release\\imgui.embedded.ini"
      via: "explicit reset/inspection commands"
      pattern: "imgui\\.embedded\\.ini"
---

<objective>
Close the remaining teardown and layout-persistence UAT gaps by letting the host prefer mdCAD self-exit and by verifying the actual embedded ini location.

Purpose: Restore D-10 and D-08 in real host-close and destroyed-parent flows so embedded layout persistence survives normal teardown.
Output: The Avalonia host waits for graceful mdCAD exit before fallback kill, and Phase 44 verification points at the real `imgui.embedded.ini` path.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-UAT.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-MANUAL-CHECKLIST.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-CONTEXT.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-03-SUMMARY.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-04-SUMMARY.md
@samples/avalonia-host/MainWindow.axaml.cs
@src/imgui_storage.h

<interfaces>
From samples/avalonia-host/MainWindow.axaml.cs:
```csharp
private static readonly TimeSpan DestroyedParentSelfExitGrace = TimeSpan.FromMilliseconds(750);
private bool EnsureAttachedLifecycleIsHealthy(string origin);
private void EnsureAttachedProcessTeardown(string detail);
private void FinalizeTeardownStatus(int exitCode);
private void OnWindowClosed(object? sender, EventArgs e);
```

From src/imgui_storage.h:
```c
static inline void imgui_storage_configure(const embed_layout_policy_t* policy);
static inline bool imgui_storage_native_store_exists(const char* path);
static inline char* imgui_storage_native_load(const char* path);
static inline void imgui_storage_native_save(const char* path, const char* ini_data);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace fixed teardown races with deterministic graceful-exit waiting in the Avalonia host</name>
  <files>samples/avalonia-host/MainWindow.axaml.cs</files>
  <action>Refactor `samples/avalonia-host/MainWindow.axaml.cs` so destroyed-parent and normal host-close teardown both prefer the mdCAD-side invalid-parent exit path per D-10 instead of racing straight to `Process.Kill(true)`. Replace the fixed `DestroyedParentSelfExitGrace` behavior with a deterministic wait helper that observes `_mdcadProcess` exit after placeholder invalidation / host close, gives mdCAD long enough to run `imgui_storage_shutdown()`, and only falls back to `Kill(true)` after the helper proves the process is still alive past the grace condition. Reuse that path from both `EnsureAttachedLifecycleIsHealthy(...)` and `OnWindowClosed(...)`, keep `destroy-after-attach` as the deliberate fallback-cleanup scenario, and keep status text aligned with the actual observed path (`mdCAD self-exit observed...` vs `Host fallback cleanup completed...`). Do not add any host-side input proxying or standalone fallback logic.</action>
  <verify>
    <automated>dotnet build samples/avalonia-host/AvaloniaHost.csproj -c Release</automated>
  </verify>
  <done>Destroyed-parent and normal host-close paths wait for mdCAD self-exit first, only use fallback kill on verified failure, and report the correct teardown origin.</done>
</task>

<task type="auto">
  <name>Task 2: Make the embedded ini path explicit and bind Scenario 7 to the real store location</name>
  <files>src/imgui_storage.h, .planning/phases/44-embedded-resize-focus-viewer-layout/44-MANUAL-CHECKLIST.md</files>
  <action>In `src/imgui_storage.h`, make the native embedded store contract explicit per D-08: keep the filename `imgui.embedded.ini`, but resolve/store it as the actual runtime path used by embedded mdCAD on desktop (the working directory / executable directory used by the host launch path, i.e. `build-vulkan\\bin\\Release` for the default harness). Then update Scenario 7 in `44-MANUAL-CHECKLIST.md` so its reset and inspection commands target that real file location instead of repo-root `imgui.embedded.ini`, and explicitly note that host close should allow graceful layout flush before fallback kill. Do not rename the file, do not merge embedded persistence back into standalone `imgui.ini`, and keep standalone isolation intact.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD &amp;&amp; python -c "from pathlib import Path; t=Path(r'.planning/phases/44-embedded-resize-focus-viewer-layout/44-MANUAL-CHECKLIST.md').read_text(encoding='utf-8'); required=[r'build-vulkan\\bin\\Release\\imgui.embedded.ini','imgui.embedded.ini']; missing=[x for x in required if x not in t]; assert not missing, missing; print('embedded-store-path-bound-ok')"</automated>
  </verify>
  <done>The embedded store path is explicit in code/docs, Scenario 7 resets the correct file, and embedded persistence verification no longer inspects the wrong location.</done>
</task>

</tasks>

<verification>
1. Run `dotnet run --project samples/avalonia-host/AvaloniaHost.csproj -c Release -- --embed-test-mode destroyed-parent` and confirm UAT Scenario 5 ends with mdCAD self-exit status unless fallback kill is genuinely required.
2. Run `dotnet run --project samples/avalonia-host/AvaloniaHost.csproj -c Release`, rearrange the embedded panels, close the host normally, relaunch, and verify Scenario 7 against `build-vulkan\bin\Release\imgui.embedded.ini`.
</verification>

<success_criteria>
- UAT Gap 2 is closed: destroyed-parent mode prefers mdCAD self-exit and reports the real exit path.
- UAT Gap 3 is closed: host close no longer prevents `imgui.embedded.ini` from persisting, and manual verification checks the actual embedded-store file.
</success_criteria>

<output>
After completion, create `.planning/phases/44-embedded-resize-focus-viewer-layout/44-06-SUMMARY.md`
</output>
