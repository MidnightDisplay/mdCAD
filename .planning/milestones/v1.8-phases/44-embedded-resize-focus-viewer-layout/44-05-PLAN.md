---
phase: 44-embedded-resize-focus-viewer-layout
plan: 05
type: execute
wave: 1
depends_on:
  - 44-02
files_modified:
  - src/app.c
autonomous: true
gap_closure: true
requirements:
  - INPT-02
must_haves:
  truths:
    - "The first deliberate click inside mdCAD activates embedded keyboard ownership and mdCAD receives keyboard shortcuts."
    - "`Tab` stays inside mdCAD after ownership is granted instead of bouncing focus back to the host."
  artifacts:
    - path: "src/app.c"
      provides: "Embedded shortcut gating that honors reducer-owned keyboard state instead of blanket-blocking `WantCaptureKeyboard` frames."
      contains: "mdcad_embedded_shortcuts_allowed"
  key_links:
    - from: "src/app.c"
      to: "state.embed_input"
      via: "embedded shortcut gate uses reducer-owned keyboard state"
      pattern: "embed_input_state_allows_shortcuts"
    - from: "src/app.c"
      to: "ImGui shortcut handling"
      via: "keyboard shortcut block guarded by `mdcad_embedded_shortcuts_allowed(io)`"
      pattern: "ImGuiKey_Tab|mdcad_embedded_shortcuts_allowed"
---

<objective>
Close the failed first-click keyboard UAT truth so embedded mdCAD actually receives shortcuts after ownership is granted.

Purpose: Restore the D-01/D-03/D-04 focus contract without changing the host/child input boundary.
Output: `src/app.c` no longer blocks embedded shortcuts solely because `ImGuiIO.WantCaptureKeyboard` is true on owned-focus frames.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-UAT.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-CONTEXT.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-RESEARCH.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-02-SUMMARY.md
@src/app.c

<interfaces>
From src/app.c:
```c
static void mdcad_handle_embedded_input_trigger(embed_input_trigger_t trigger, bool cancel_interactions);
static void mdcad_sync_embedded_interaction_state(void);
static bool mdcad_embedded_shortcuts_allowed(const ImGuiIO* io);
static void event(const sapp_event* ev);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Let reducer-owned embedded keyboard focus reach the mdCAD shortcut block</name>
  <files>src/app.c</files>
  <action>Update `mdcad_embedded_shortcuts_allowed(const ImGuiIO* io)` in `src/app.c` so embedded mode follows the reducer-owned keyboard state per D-01, D-03, and D-04 instead of returning false for every frame where `io->WantCaptureKeyboard` is true. Keep the pre-first-click block and host-focus-return block intact, keep standalone behavior unchanged, and keep `state.embed_input` / `embed_input_state_apply(...)` as the sole authority for when shortcuts are enabled. Do not add host-side keyboard proxying or any new focus-forcing path; this closes UAT Gap 1 from `44-UAT.md` by allowing the first deliberate click to unlock `Tab` and other mdCAD shortcuts even when ImGui wants the keyboard.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD embed_input_state_test &amp;&amp; ctest --test-dir build-vulkan -C Release --output-on-failure -R "embed_input_state_test|embed_launch_config_test"</automated>
  </verify>
  <done>Embedded mdCAD starts with shortcuts blocked, the first deliberate click enables them, and `WantCaptureKeyboard` no longer suppresses owned-focus shortcut handling.</done>
</task>

</tasks>

<verification>
Build and run the existing Phase 44 host harness with `dotnet run --project samples/avalonia-host/AvaloniaHost.csproj -c Release`, then re-check UAT Scenario 2: first attach shows no auto-focus, one deliberate click inside mdCAD enables keyboard ownership, `Tab` stays in mdCAD, and clicking host UI returns focus to the host.
</verification>

<success_criteria>
- UAT Gap 1 is closed: embedded mode receives keyboard shortcuts after the first deliberate click.
- The fix preserves the locked host/child boundary and does not reintroduce host-side input forwarding.
</success_criteria>

<output>
After completion, create `.planning/phases/44-embedded-resize-focus-viewer-layout/44-05-SUMMARY.md`
</output>
