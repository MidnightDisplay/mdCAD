---
phase: 44-embedded-resize-focus-viewer-layout
plan: 09
type: execute
wave: 1
depends_on:
  - 44-05
  - 44-07
files_modified:
  - src/app.c
autonomous: true
gap_closure: true
requirements:
  - INPT-02
must_haves:
  truths:
    - "After embedded focus is acquired, mdCAD-global non-text shortcuts such as `Tab` and `Ctrl+Z` work inside the hosted viewer."
    - "Active text-entry widgets keep owning their own `Tab` and text-edit shortcuts instead of being overridden by mdCAD-global shortcut handling."
    - "Standalone mdCAD shortcut behavior remains unchanged."
  artifacts:
    - path: "src/app.c"
      provides: "An embedded-only shortcut-routing helper that uses ImGui's shortcut API instead of raw key polling for mdCAD-global shortcuts."
      contains: "igShortcut_Nil|ImGuiInputFlags_RouteGlobal"
  key_links:
    - from: "src/app.c"
      to: "state.embed_input"
      via: "embedded shortcut routing stays gated by reducer-owned keyboard ownership before routed ImGui shortcuts are evaluated"
      pattern: "mdcad_embedded_shortcuts_allowed|igShortcut_Nil"
---

<objective>
Close the last remaining embedded keyboard gap by routing mdCAD-global shortcuts through ImGui's shortcut API.

Purpose: The final human UAT proved that Win32 focus and text-entry ownership now work, but the app-level global shortcut block still misses non-text `Tab` and `Ctrl+Z` after focus is acquired. The fix belongs in the embedded ImGui shortcut-routing seam, not in the host or native child-window focus path.
Output: Embedded mdCAD uses routed ImGui shortcuts for its global actions so docked/focused windows can still trigger `Tab`, undo, and redo while active text widgets keep owning their own keys.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-CONTEXT.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-HUMAN-UAT.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-07-SUMMARY.md
@src/app.c
@build-vulkan/_deps/libcimgui-src/cimgui.h

<interfaces>
From src/app.c:
```c
static bool mdcad_embedded_shortcuts_allowed(const ImGuiIO* io);
// global shortcut block in frame() around undo/redo, Tab, C, and delete/backspace
```

From cimgui.h:
```c
CIMGUI_API bool igShortcut_Nil(ImGuiKeyChord key_chord, ImGuiInputFlags flags);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route embedded global shortcuts through ImGui instead of raw key polling</name>
  <files>src/app.c</files>
  <action>Refactor the embedded global shortcut block in `src/app.c` so embedded mode no longer relies on raw `igIsKeyPressed_Bool(...)` polling for mdCAD-global shortcuts. Introduce an embedded-only helper that uses `igShortcut_Nil(...)` with a global route for mdCAD-global actions such as `Tab`, undo, redo, and any other non-text global shortcut handled by this block. Use the global route because this shortcut block runs outside any specific ImGui window, preserve the existing reducer-owned `mdcad_embedded_shortcuts_allowed(...)` gate, keep active text-entry widgets in control of their own `Tab` and text-edit shortcuts, and leave standalone shortcut behavior unchanged. Do not touch the Win32 host seam or the focus-transfer code from `44-07` and `44-08`.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD embed_input_state_test &amp;&amp; ctest --test-dir build-vulkan -C Release --output-on-failure -R "embed_input_state_test" &amp;&amp; python -c "from pathlib import Path; t=Path(r'src/app.c').read_text(encoding='utf-8'); assert 'igShortcut_Nil' in t, 'missing routed shortcut helper'; assert 'ImGuiInputFlags_RouteGlobal' in t, 'missing routed shortcut flags'; print('embedded-shortcut-routing-wired')"</automated>
  </verify>
  <done>Embedded mdCAD now routes its global shortcuts through ImGui so non-text `Tab` and undo/redo work after focus is acquired, while text widgets keep their own shortcut ownership.</done>
</task>

</tasks>

<verification>
Re-run Human UAT Scenario 2 with `dotnet run --project samples/avalonia-host/AvaloniaHost.csproj -c Release`: confirm attach does not auto-focus mdCAD, one deliberate click inside the viewer enables the global mdCAD `Tab` shortcut and `Ctrl+Z` undo, text fields still keep their own text-edit shortcuts, and clicking the host status/chrome area returns focus to the host.
</verification>

<success_criteria>
- The only remaining Scenario 2 gap is closed.
- Embedded mdCAD-global shortcuts work after native focus is acquired.
- Text-entry widgets are not regressed by the shortcut-routing change.
</success_criteria>

<output>
After completion, create `.planning/phases/44-embedded-resize-focus-viewer-layout/44-09-SUMMARY.md`
</output>
