---
phase: 44-embedded-resize-focus-viewer-layout
plan: 04
type: execute
wave: 3
depends_on:
  - 44-01
  - 44-02
files_modified:
  - src/app.c
  - src/imgui_storage.h
autonomous: true
requirements:
  - INPT-04
must_haves:
  truths:
    - "Embedded mode opens with the approved viewer-first dock layout on first run."
    - "Embedded layout changes persist across embedded relaunches without overwriting standalone `imgui.ini` behavior."
    - "Pick/slot/FPS debug windows stay off by default while the approved panel set remains available in embedded mode."
  artifacts:
    - path: "src/imgui_storage.h"
      provides: "Embedded-aware native desktop ImGui ini load/save path"
      contains: "imgui.embedded.ini or equivalent embedded-only store"
    - path: "src/app.c"
      provides: "First-run DockBuilder recipe and embedded layout bootstrapping"
      contains: "DockBuilder"
  key_links:
    - from: "src/app.c"
      to: "src/imgui_storage.h"
      via: "embedded mode chooses manual embedded settings store before dock layout loads"
      pattern: "imgui_storage_"
    - from: "src/app.c"
      to: "ImGui dockspace"
      via: "first-run embedded DockBuilder recipe"
      pattern: "DockBuilder"
---

<objective>
Separate embedded docking persistence from standalone mode and seed the approved embedded viewer layout on first run.

Purpose: Deliver INPT-04 without corrupting existing standalone layout state.
Output: Embedded-only ImGui settings persistence plus a one-time DockBuilder recipe for the approved panel arrangement.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-CONTEXT.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-RESEARCH.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-VALIDATION.md
@src/app.c
@src/imgui_storage.h
@src/embed_layout_state.h
@src/tests/embed_layout_state_test.c

<interfaces>
From src/imgui_storage.h:
```c
static inline void imgui_storage_init(void);
static inline void imgui_storage_frame(void);
static inline void imgui_storage_shutdown(void);
static inline void imgui_storage_mark_should_save(void);
```

From src/app.c:
```c
simgui_setup(&(simgui_desc_t){
#ifndef PLATFORM_WEB
    .ini_filename = "imgui.ini",
#endif
    .logger.func = slog_func,
});

igDockSpaceOverViewport(0, NULL, ImGuiDockNodeFlags_None, NULL);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split embedded ImGui settings persistence from standalone layout state</name>
  <files>src/app.c, src/imgui_storage.h</files>
  <read_first>
src/app.c
src/imgui_storage.h
src/embed_layout_state.h
src/tests/embed_layout_state_test.c
.planning/phases/44-embedded-resize-focus-viewer-layout/44-CONTEXT.md
.planning/phases/44-embedded-resize-focus-viewer-layout/44-RESEARCH.md
  </read_first>
  <behavior>
    - D-08: embedded mode loads/saves a distinct settings store instead of the shared native desktop `imgui.ini`.
    - Standalone mode keeps its existing automatic `imgui.ini` behavior unchanged.
    - Embedded save requests still fire on shutdown/deactivation so rearranged panels persist.
  </behavior>
  <acceptance_criteria>
    - `src/app.c` no longer hardcodes `"imgui.ini"` for embedded mode.
    - `src/imgui_storage.h` manually loads/saves an embedded-only store on native desktop when `state.launch.embedded` is true.
    - `embed_layout_state_test` passes and proves the embedded-vs-standalone store split.
    - The implementation does not change iOS/Android/Web persistence behavior.
  </acceptance_criteria>
  <action>Use `src/embed_layout_state.h` as the source of truth for deciding whether the launch is standalone or embedded. In `src/app.c`, configure `simgui_setup()` so native embedded mode does not use the shared automatic `imgui.ini` filename. In `src/imgui_storage.h`, add native desktop embedded-mode support that manually loads and saves a separate embedded store (use an explicit embedded-only filename such as `imgui.embedded.ini`, located alongside the existing native ini behavior) while preserving the current iOS/Android/Web code paths unchanged. Make sure `imgui_storage_frame()` and `imgui_storage_shutdown()` persist embedded changes when the embedded session is active.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD embed_layout_state_test &amp;&amp; ctest --test-dir build-vulkan -C Release --output-on-failure -R embed_layout_state_test</automated>
  </verify>
  <done>Embedded launches use their own persisted ImGui layout store, and standalone launches keep using the existing shared `imgui.ini` path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seed the approved embedded DockBuilder recipe only on first embedded run</name>
  <files>src/app.c</files>
  <read_first>
src/app.c
src/embed_layout_state.h
src/tests/embed_layout_state_test.c
.planning/phases/44-embedded-resize-focus-viewer-layout/44-CONTEXT.md
.planning/phases/44-embedded-resize-focus-viewer-layout/44-RESEARCH.md
  </read_first>
  <behavior>
    - D-07: first embedded launch docks `3D Viewport` center, `Scene Hierarchy` below, and `Entity Inspector`/`Controls`/`Visibility`/`Camera Debug` in the right side of that bottom dock.
    - D-09: the approved embedded panel set remains available while Pick Buffer Debug, Slot Buffer Debug, and FPS Debug stay off by default.
    - Relaunching embedded mode with an existing embedded store reuses saved layout instead of rebuilding the DockBuilder recipe every frame.
  </behavior>
  <acceptance_criteria>
    - `src/app.c` contains a dedicated embedded DockBuilder helper or equivalent one-time block rather than hardcoded per-frame panel rectangles.
    - The helper only runs when the embedded store is absent or empty.
    - The dock recipe names match the existing ImGui window titles exactly: `3D Viewport`, `Scene Hierarchy`, `Entity Inspector`, `Controls`, `Visibility`, `Camera Debug`.
    - `embed_layout_state_test` still passes after the runtime dock bootstrapping is added.
  </acceptance_criteria>
  <action>Add a one-time embedded dock setup helper in `src/app.c` that runs after the dockspace exists and only when `src/embed_layout_state.h` reports “embedded store missing / first run.” Use DockBuilder, not manual rectangles. Dock `3D Viewport` in the main center node, split a bottom region, put `Scene Hierarchy` on the left of that bottom region, and stack `Entity Inspector`, `Controls`, `Visibility`, and `Camera Debug` into the right side of the same bottom region per D-07. Preserve the embedded defaults from Phase 43/44 by keeping Pick Buffer Debug, Slot Buffer Debug, and FPS Debug closed by default per D-09.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD embed_layout_state_test &amp;&amp; ctest --test-dir build-vulkan -C Release --output-on-failure -R embed_layout_state_test</automated>
  </verify>
  <done>The embedded viewer opens with the approved dock layout on first run and then reuses its own saved embedded layout thereafter.</done>
</task>

</tasks>

<verification>
Run `ctest --test-dir build-vulkan -C Release --output-on-failure -R embed_layout_state_test`, then complete the manual checklist rows for default embedded layout, rearrange/relaunch persistence, and standalone-layout isolation.
</verification>

<success_criteria>
- INPT-04 is satisfied with a separate embedded store and a deterministic first-run dock recipe.
- Standalone layout state is not overwritten by embedded launches.
</success_criteria>

<output>
After completion, create `.planning/phases/44-embedded-resize-focus-viewer-layout/44-04-SUMMARY.md`
</output>
