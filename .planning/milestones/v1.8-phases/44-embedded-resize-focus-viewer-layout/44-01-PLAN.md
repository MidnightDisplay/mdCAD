---
phase: 44-embedded-resize-focus-viewer-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/embed_input_state.h
  - src/embed_layout_state.h
  - src/tests/embed_input_state_test.c
  - src/tests/embed_layout_state_test.c
  - src/CMakeLists.txt
  - .planning/phases/44-embedded-resize-focus-viewer-layout/44-MANUAL-CHECKLIST.md
autonomous: true
requirements:
  - EMBD-04
  - INPT-01
  - INPT-02
  - INPT-03
  - INPT-04
must_haves:
  truths:
    - "Phase 44 work has fast native checks for embedded input-state transitions before app-level focus/capture changes land."
    - "Phase 44 work has a separate fast native check for embedded layout-store selection before dock persistence changes land."
    - "Manual resize/focus/orphan verification is scripted in a single checklist before later plans rely on it."
  artifacts:
    - path: "src/embed_input_state.h"
      provides: "Pure embedded-focus and cancel-state reducer shared by tests and runtime code"
      contains: "embedded first-click ownership and deactivate-cancel decisions"
    - path: "src/embed_layout_state.h"
      provides: "Pure embedded layout-store selection and first-run layout decision helper"
      contains: "standalone vs embedded persistence choice"
    - path: "src/tests/embed_input_state_test.c"
      provides: "Unit coverage for D-01..D-06 transition rules"
      contains: "embed_input_state_test"
    - path: "src/tests/embed_layout_state_test.c"
      provides: "Unit coverage for D-07..D-08 layout-store rules"
      contains: "embed_layout_state_test"
    - path: ".planning/phases/44-embedded-resize-focus-viewer-layout/44-MANUAL-CHECKLIST.md"
      provides: "Manual evidence script for resize, focus, Alt+Tab, and orphan shutdown"
      contains: "Scenario 1"
  key_links:
    - from: "src/CMakeLists.txt"
      to: "src/tests/embed_input_state_test.c"
      via: "CTest target registration"
      pattern: "add_test\\(NAME embed_input_state_test"
    - from: "src/CMakeLists.txt"
      to: "src/tests/embed_layout_state_test.c"
      via: "CTest target registration"
      pattern: "add_test\\(NAME embed_layout_state_test"
    - from: ".planning/phases/44-embedded-resize-focus-viewer-layout/44-MANUAL-CHECKLIST.md"
      to: "samples/avalonia-host/MainWindow.axaml.cs"
      via: "documented dotnet run harness commands"
      pattern: "dotnet run --project samples/avalonia-host/AvaloniaHost.csproj -c Release"
---

<objective>
Front-load the Wave 0 validation seams required by `44-VALIDATION.md` so the later focus/capture/layout plans can ship against fast tests instead of blind manual iteration.

Purpose: Establish executable contracts for D-01..D-10 before risky runtime edits land.
Output: Header-only helper seams, two native CTest targets, and a Phase 44 manual checklist shell.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-CONTEXT.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-RESEARCH.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-VALIDATION.md
@.planning/phases/43-embed-contract-child-window-bootstrap/43-02-SUMMARY.md
@src/CMakeLists.txt
@src/tests/embed_launch_config_test.c

<interfaces>
From src/CMakeLists.txt:
```cmake
add_executable(embed_launch_config_test tests/embed_launch_config_test.c)
target_compile_features(embed_launch_config_test PRIVATE c_std_11)
target_include_directories(embed_launch_config_test PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}
)
add_test(NAME embed_launch_config_test COMMAND embed_launch_config_test)
```

From 44-VALIDATION.md Wave 0 contract:
```text
src/tests/embed_input_state_test.c
src/tests/embed_layout_state_test.c
.planning/phases/44-embedded-resize-focus-viewer-layout/44-MANUAL-CHECKLIST.md
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create pure Wave 0 helper seams and register native tests</name>
  <files>src/embed_input_state.h, src/embed_layout_state.h, src/tests/embed_input_state_test.c, src/tests/embed_layout_state_test.c, src/CMakeLists.txt</files>
  <read_first>
src/CMakeLists.txt
src/tests/embed_launch_config_test.c
.planning/phases/44-embedded-resize-focus-viewer-layout/44-CONTEXT.md
.planning/phases/44-embedded-resize-focus-viewer-layout/44-RESEARCH.md
.planning/phases/44-embedded-resize-focus-viewer-layout/44-VALIDATION.md
  </read_first>
  <behavior>
    - `embed_input_state_test` proves D-01/D-04: embedded keyboard ownership starts disabled on attach and does not become active on focus alone.
    - `embed_input_state_test` proves D-02/D-03/D-05: first deliberate mouse-down inside mdCAD enables mdCAD-owned keyboard and leaves ownership active during off-surface drag while the host app stays active.
    - `embed_input_state_test` proves D-06: host-app deactivation/unfocus returns a cancel/reset decision immediately.
    - `embed_layout_state_test` proves D-07/D-08: embedded mode selects a separate persisted store and flags first-run layout seeding only when the embedded store is absent.
  </behavior>
  <acceptance_criteria>
    - `src/embed_input_state.h` exports a header-only reducer with no Win32, ImGui, or Sokol dependency so later runtime code can call it directly.
    - `src/embed_layout_state.h` exports a header-only helper that distinguishes standalone vs embedded layout storage and first-run dock seeding.
    - `src/CMakeLists.txt` contains `embed_input_state_test` and `embed_layout_state_test` targets plus matching `add_test(...)` entries.
    - `ctest --test-dir build-vulkan -C Release --output-on-failure -R "embed_input_state_test|embed_layout_state_test"` passes after building the new targets.
  </acceptance_criteria>
  <action>Create `src/embed_input_state.h` and `src/embed_layout_state.h` as project-owned header-only seams for the Phase 44 rules instead of baking those decisions directly into `app.c`. In `src/tests/embed_input_state_test.c`, encode D-01..D-06 as pure state transitions: attach/focus alone does not grant keyboard ownership, first deliberate click grants it, click-away/unfocus clears it only when the host takes focus, off-surface drag remains active while the app stays active, and app deactivation returns an explicit cancel/reset result. In `src/tests/embed_layout_state_test.c`, encode D-07..D-08 as pure path/seed rules: embedded mode resolves a distinct store and only requests the DockBuilder recipe when no embedded store exists yet. Wire both tests into `src/CMakeLists.txt` using the same fast native CTest pattern as `embed_launch_config_test` so Wave 0 exists before Plans 44-02..44-04 modify runtime code.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target embed_input_state_test embed_layout_state_test &amp;&amp; ctest --test-dir build-vulkan -C Release --output-on-failure -R "embed_input_state_test|embed_layout_state_test"</automated>
  </verify>
  <done>Both new tests are compiled, registered with CTest, and pass against the pure helper seams they are meant to protect.</done>
</task>

<task type="auto">
  <name>Task 2: Write the Phase 44 manual checklist shell before runtime changes</name>
  <files>.planning/phases/44-embedded-resize-focus-viewer-layout/44-MANUAL-CHECKLIST.md</files>
  <read_first>
.planning/phases/43-embed-contract-child-window-bootstrap/43-MANUAL-CHECKLIST.md
.planning/phases/44-embedded-resize-focus-viewer-layout/44-CONTEXT.md
.planning/phases/44-embedded-resize-focus-viewer-layout/44-RESEARCH.md
.planning/phases/44-embedded-resize-focus-viewer-layout/44-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - The file contains scenarios for resize stress, first-click activation/no auto-focus, click-away focus return, Alt+Tab or host deactivation cancel, host close teardown, and invalid/destroyed parent teardown.
    - Each scenario includes an exact command line, expected status text or visible outcome, and a verdict placeholder.
    - The checklist includes the future deterministic teardown mode for placeholder destruction without host-window close.
  </acceptance_criteria>
  <action>Create `.planning/phases/44-embedded-resize-focus-viewer-layout/44-MANUAL-CHECKLIST.md` as the Wave 0 shell referenced by `44-VALIDATION.md`. Mirror the concrete structure used in Phase 43, but add Phase 44-specific sections for INPT-01..04 and EMBD-04: aggressive resize/snap/fullscreen transitions, no auto-focus on attach per D-04, first-click ownership per D-01, click-away host return per D-02, `Tab` remaining mdCAD-owned per D-03, deactivation cancel per D-06, and immediate embedded exit on host close or invalid parent per D-10. Include exact `dotnet run --project samples/avalonia-host/AvaloniaHost.csproj -c Release ...` commands for `valid`, `invalid-parent`, `destroyed-parent`, and the planned placeholder-destroy harness mode so later plans can fill evidence without inventing the checklist shape.</action>
  <verify>
    <automated>python -c "from pathlib import Path; p=Path(r'.planning/phases/44-embedded-resize-focus-viewer-layout/44-MANUAL-CHECKLIST.md'); t=p.read_text(encoding='utf-8'); required=['Scenario 1','resize','first-click','Alt+Tab','destroy']; missing=[x for x in required if x not in t]; assert not missing, missing; print('checklist-shell-ok')"</automated>
  </verify>
  <done>The Phase 44 manual checklist exists and predefines every manual evidence lane called out by `44-VALIDATION.md`.</done>
</task>

</tasks>

<verification>
Run `ctest --test-dir build-vulkan -C Release --output-on-failure -R "embed_launch_config_test|embed_.*_test"` after this plan. Confirm `44-MANUAL-CHECKLIST.md` exists before any runtime focus/layout plan begins.
</verification>

<success_criteria>
- Wave 0 no longer has missing test/checklist artifacts for input-state and layout-state validation.
- Later plans can reference concrete helper seams instead of inventing focus/layout rules ad hoc.
</success_criteria>

<output>
After completion, create `.planning/phases/44-embedded-resize-focus-viewer-layout/44-01-SUMMARY.md`
</output>
