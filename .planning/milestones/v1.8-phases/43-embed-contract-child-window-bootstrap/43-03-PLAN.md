---
phase: 43-embed-contract-child-window-bootstrap
plan: 03
type: execute
wave: 2
depends_on: [43-01, 43-02]
files_modified:
  - samples/avalonia-host/AvaloniaHost.csproj
  - samples/avalonia-host/Program.cs
  - samples/avalonia-host/App.axaml
  - samples/avalonia-host/App.axaml.cs
  - samples/avalonia-host/MainWindow.axaml
  - samples/avalonia-host/MainWindow.axaml.cs
  - .planning/phases/43-embed-contract-child-window-bootstrap/43-MANUAL-CHECKLIST.md
autonomous: false
requirements: [HOST-01, EMBD-03]
must_haves:
  truths:
    - "A developer can build and run a minimal Avalonia host that auto-launches mdCAD into a `NativeControlHost` child region per D-04."
    - "The host visibly steps through `launching`, `waiting for child attach`, `attached`, and `timeout/failure` bootstrap states per D-05."
    - "When the host attaches mdCAD successfully, mdCAD appears only inside the host region with no silent standalone fallback and with Pick/Slot/FPS debug windows OFF by default."
    - "When the host deliberately launches mdCAD with an invalid or destroyed parent HWND, mdCAD reports an explicit embed startup failure and still never falls back to a standalone top-level window per D-02 and D-03."
  artifacts:
    - path: "samples/avalonia-host/AvaloniaHost.csproj"
      provides: "Minimal Windows Avalonia host project"
      contains: "Avalonia.Desktop"
    - path: "samples/avalonia-host/MainWindow.axaml"
      provides: "Minimal host UI with native embed surface and status text"
      contains: "NativeControlHost"
    - path: "samples/avalonia-host/MainWindow.axaml.cs"
      provides: "Auto-launch, attach polling, invalid/destroyed parent-HWND smoke modes, captured embed-failure output, and best-effort cleanup"
      contains: "EnumChildWindows"
    - path: ".planning/phases/43-embed-contract-child-window-bootstrap/43-MANUAL-CHECKLIST.md"
      provides: "Manual attach/failure proof for Phase 43 closeout including invalid parent-HWND evidence"
      contains: "Embed failure evidence:"
  key_links:
    - from: "samples/avalonia-host/MainWindow.axaml.cs"
      to: "src/app_launch_config.h"
      via: "launch args `--embedded --parent-hwnd {hwnd}`"
      pattern: "--embedded --parent-hwnd"
    - from: "samples/avalonia-host/MainWindow.axaml.cs"
      to: "samples/avalonia-host/MainWindow.axaml"
      via: "status text updates for `launching`, `waiting for child attach`, `attached`, and `timeout/failure`"
      pattern: "launching|waiting for child attach|attached|timeout/failure"
    - from: ".planning/phases/43-embed-contract-child-window-bootstrap/43-MANUAL-CHECKLIST.md"
      to: "samples/avalonia-host/MainWindow.axaml.cs"
      via: "manual proof of valid attach plus invalid/destroyed parent-HWND failure behavior"
      pattern: "--embed-test-mode|Embedded startup failed:"
---

<objective>
Build the minimal Avalonia proof harness for the Phase 43 bootstrap contract and capture its blocking smoke evidence.

Purpose: Satisfy HOST-01 plus EMBD-03 end-to-end proof with the smallest possible Windows host while keeping Phase 44 resize/focus/input hardening and Phase 45 JSONL launch flags out of scope.
Output: A buildable sample host, a bootstrap status surface, captured embed-failure evidence, and a manual checklist proving attached and invalid-parent failure behavior.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-embed-contract-child-window-bootstrap/43-CONTEXT.md
@.planning/phases/43-embed-contract-child-window-bootstrap/43-RESEARCH.md
@.planning/phases/43-embed-contract-child-window-bootstrap/43-VALIDATION.md
@.planning/phases/43-embed-contract-child-window-bootstrap/43-01-SUMMARY.md
@.planning/phases/43-embed-contract-child-window-bootstrap/43-02-SUMMARY.md
@src/app_launch_config.h
@src/platform/win32_embed.h

<interfaces>
From `src/app_launch_config.h`:
```c
// mdCAD launch contract used by the sample host
--embedded --parent-hwnd <decimal-or-0xHWND>
```

Plan 43-03 host must surface these exact states:
```text
launching
waiting for child attach
attached
timeout/failure
```

Wave 0 scaffold from Plan 43-01 must already reserve these harness flags:
```text
--mdcad-exe <path>
--embed-test-mode valid|invalid-parent|destroyed-parent
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete the minimal Avalonia host with attach polling and invalid-parent smoke modes</name>
  <read_first>
    - .planning/phases/43-embed-contract-child-window-bootstrap/43-CONTEXT.md
    - .planning/phases/43-embed-contract-child-window-bootstrap/43-RESEARCH.md
    - .planning/phases/43-embed-contract-child-window-bootstrap/43-02-SUMMARY.md
    - src/app_launch_config.h
    - src/platform/win32_embed.h
  </read_first>
  <files>samples/avalonia-host/AvaloniaHost.csproj, samples/avalonia-host/Program.cs, samples/avalonia-host/App.axaml, samples/avalonia-host/App.axaml.cs, samples/avalonia-host/MainWindow.axaml, samples/avalonia-host/MainWindow.axaml.cs</files>
  <action>Complete the Wave 0 scaffold under `samples/avalonia-host/` using `net8.0-windows10.0.19041.0` and consistent `Avalonia`, `Avalonia.Desktop`, and `Avalonia.Themes.Fluent` `11.3.*` package references. Keep the UI minimal: one `NativeControlHost` region, one bootstrap status text block, and one read-only failure text surface if needed. Implement the placeholder HWND and Win32 P/Invoke helpers inside `MainWindow.axaml.cs` if that keeps the project lean. The host must auto-launch mdCAD as soon as the native host handle exists per D-04, using `--embedded --parent-hwnd {hwnd}` and no JSONL flags. Resolve `mdCAD.exe` by honoring an optional host CLI override `--mdcad-exe <path>` first, else probe `build-vulkan/bin/Release/mdCAD.exe` and `build-vulkan/bin/mdCAD.exe` relative to repo root. Preserve the Wave 0 `--embed-test-mode valid|invalid-parent|destroyed-parent` harness: `valid` uses the live placeholder child host; `invalid-parent` deliberately passes an incompatible/bad HWND value such as `0x1`; `destroyed-parent` captures the placeholder HWND, destroys it immediately before process launch, then passes the stale handle. Redirect mdCAD stdout/stderr so the first line containing `Embedded startup failed:` is captured and surfaced in the host for failure scenarios. Set the displayed bootstrap state exactly to `launching`, then `waiting for child attach`, then `attached` once `EnumChildWindows` + `GetWindowThreadProcessId` finds a child HWND belonging to the launched mdCAD process, or `timeout/failure` if the executable cannot be resolved, the process exits early, or 10 seconds elapse without attach. Poll every 100 ms with `DispatcherTimer`, keep the host intentionally status-only (no rich controls), and on host close stop the timer and best-effort terminate the launched mdCAD process tree if it is still running so smoke runs do not orphan a viewer.</action>
  <acceptance_criteria>`samples/avalonia-host/MainWindow.axaml` contains `NativeControlHost`; `samples/avalonia-host/MainWindow.axaml.cs` contains the exact strings `launching`, `waiting for child attach`, `attached`, and `timeout/failure`, calls `EnumChildWindows` and `GetWindowThreadProcessId`, launches mdCAD with `--embedded --parent-hwnd`, supports `--embed-test-mode valid|invalid-parent|destroyed-parent`, and captures `Embedded startup failed:` text from the child process; and `samples/avalonia-host/AvaloniaHost.csproj` references `Avalonia.Desktop`.</acceptance_criteria>
  <verify>
    <automated>dotnet build samples/avalonia-host/AvaloniaHost.csproj -c Release</automated>
  </verify>
  <done>The repo contains a buildable minimal Avalonia host that can auto-launch mdCAD into a native child region and expose the staged bootstrap states required by D-05.</done>
</task>

<task type="auto">
  <name>Task 2: Write the Phase 43 bootstrap smoke checklist with invalid/destroyed parent-HWND proof</name>
  <read_first>
    - .planning/phases/43-embed-contract-child-window-bootstrap/43-VALIDATION.md
    - samples/avalonia-host/MainWindow.axaml
    - samples/avalonia-host/MainWindow.axaml.cs
    - src/app.c
  </read_first>
  <files>.planning/phases/43-embed-contract-child-window-bootstrap/43-MANUAL-CHECKLIST.md</files>
  <action>Create `43-MANUAL-CHECKLIST.md` as the exact Phase 43 bootstrap proof script. Include three named scenarios: `Scenario 1 - valid attach`, `Scenario 2 - invalid parent HWND failure`, and `Scenario 3 - destroyed parent HWND failure`. For Scenario 1, require a successful native build plus `dotnet run --project samples/avalonia-host/AvaloniaHost.csproj -c Release`, then record the observed status sequence, whether mdCAD appeared only inside the host region, whether any standalone mdCAD window appeared, and whether `Pick Buffer Debug`, `Slot Buffer Debug`, and `FPS Debug` stayed closed by default. For Scenario 2, require `dotnet run --project samples/avalonia-host/AvaloniaHost.csproj -c Release -- --embed-test-mode invalid-parent`, then record the observed state sequence, the captured `Embedded startup failed:` evidence, whether any child window attached, and whether any standalone fallback window appeared. For Scenario 3, require `dotnet run --project samples/avalonia-host/AvaloniaHost.csproj -c Release -- --embed-test-mode destroyed-parent`, then record the same failure evidence and no-fallback results for the stale/destroyed HWND path. Give each scenario the exact fields `Observed states:`, `Embed failure evidence:`, `Child window result:`, `Standalone fallback result:`, `Debug-window baseline:`, and `Scenario verdict:`; end the file with `Checklist verdict:`; initialize every evidence field to `TBD`.</action>
  <acceptance_criteria>`.planning/phases/43-embed-contract-child-window-bootstrap/43-MANUAL-CHECKLIST.md` exists, contains `Scenario 1 - valid attach`, `Scenario 2 - invalid parent HWND failure`, `Scenario 3 - destroyed parent HWND failure`, the exact field names `Observed states:`, `Embed failure evidence:`, `Child window result:`, `Standalone fallback result:`, `Debug-window baseline:`, `Scenario verdict:`, and `Checklist verdict:`, and includes at least one `TBD` for each scenario before the checkpoint runs.</acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; t=Path('.planning/phases/43-embed-contract-child-window-bootstrap/43-MANUAL-CHECKLIST.md').read_text(encoding='utf-8'); required=['Scenario 1 - valid attach','Scenario 2 - invalid parent HWND failure','Scenario 3 - destroyed parent HWND failure','Observed states:','Embed failure evidence:','Child window result:','Standalone fallback result:','Debug-window baseline:','Scenario verdict:','Checklist verdict:']; assert all(x in t for x in required); assert t.count('TBD') >= 9"</automated>
  </verify>
  <done>The exact attach/failure smoke pass now proves EMBD-03 from the host workflow using both invalid and destroyed parent-HWND launches, with explicit failure evidence fields instead of an implicit checklist.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Run the host bootstrap smoke pass and record the result</name>
  <read_first>
    - .planning/phases/43-embed-contract-child-window-bootstrap/43-MANUAL-CHECKLIST.md
    - samples/avalonia-host/MainWindow.axaml
    - samples/avalonia-host/MainWindow.axaml.cs
    - src/app.c
  </read_first>
  <files>.planning/phases/43-embed-contract-child-window-bootstrap/43-MANUAL-CHECKLIST.md</files>
  <what-built>A minimal Avalonia host that auto-launches mdCAD into a native child region and exposes staged bootstrap states for attach and failure paths.</what-built>
  <action>Run the checklist exactly as written. First build the native app and the sample host. For Scenario 1, run the sample host normally and record the observed state sequence from `launching` through `waiting for child attach` to `attached`, then confirm mdCAD appears only inside the host control and that `Pick Buffer Debug`, `Slot Buffer Debug`, and `FPS Debug` are not open by default. For Scenario 2, rerun the host with `--embed-test-mode invalid-parent`; record that the status reaches `timeout/failure`, capture the surfaced `Embedded startup failed:` evidence, and confirm no child window attaches and no standalone mdCAD window appears. For Scenario 3, rerun the host with `--embed-test-mode destroyed-parent`; again capture the explicit embed failure evidence and confirm no child attach and no standalone fallback. Replace every `TBD`, mark each `Scenario verdict:` as `PASS` or `FAIL`, and set `Checklist verdict:` to `PASS` only if all three scenarios pass with no standalone fallback.</action>
  <how-to-verify>
    1) Run `cmake --build build-vulkan --config Release --target mdCAD`.
    2) Run `dotnet build samples/avalonia-host/AvaloniaHost.csproj -c Release`.
    3) Run `dotnet run --project samples/avalonia-host/AvaloniaHost.csproj -c Release` and confirm the UI shows `launching`, then `waiting for child attach`, then `attached`.
    4) While Scenario 1 is open, confirm mdCAD appears only inside the host region, no separate standalone mdCAD window appears, and the Pick/Slot/FPS debug windows are not visible by default.
    5) Close the host, then run `dotnet run --project samples/avalonia-host/AvaloniaHost.csproj -c Release -- --embed-test-mode invalid-parent`.
    6) Confirm the host reaches `timeout/failure`, surfaces `Embedded startup failed:`, does not attach a child window, and does not spawn a standalone mdCAD window.
    7) Run `dotnet run --project samples/avalonia-host/AvaloniaHost.csproj -c Release -- --embed-test-mode destroyed-parent`.
    8) Confirm the host again reaches `timeout/failure`, surfaces `Embedded startup failed:`, does not attach a child window, and does not spawn a standalone mdCAD window.
    9) Fill the checklist, remove all `TBD` markers, and end with `Checklist verdict: PASS` or `Checklist verdict: FAIL`.
  </how-to-verify>
  <acceptance_criteria>`.planning/phases/43-embed-contract-child-window-bootstrap/43-MANUAL-CHECKLIST.md` contains no remaining `TBD` markers, records all three scenario outcomes, and ends with an explicit `Checklist verdict: PASS` or `Checklist verdict: FAIL` line.</acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; t=Path('.planning/phases/43-embed-contract-child-window-bootstrap/43-MANUAL-CHECKLIST.md').read_text(encoding='utf-8'); assert 'TBD' not in t; assert ('Checklist verdict: PASS' in t) or ('Checklist verdict: FAIL' in t)"</automated>
  </verify>
  <done>Phase 43 has explicit attach plus invalid/destroyed parent-HWND failure smoke evidence for the minimal host instead of an unrecorded manual claim.</done>
  <resume-signal>Type "approved" after the checklist is filled and acceptable, or reply with the recorded failures.</resume-signal>
</task>

</tasks>

<verification>
Run:
- `dotnet build samples/avalonia-host/AvaloniaHost.csproj -c Release`
- `python -c "from pathlib import Path; t=Path('.planning/phases/43-embed-contract-child-window-bootstrap/43-MANUAL-CHECKLIST.md').read_text(encoding='utf-8'); assert 'TBD' not in t"`
</verification>

<success_criteria>
- The sample host builds on Windows and auto-launches mdCAD into a `NativeControlHost`.
- The host surfaces the exact staged bootstrap states requested by D-05.
- Manual evidence proves valid attach, explicit invalid/destroyed parent-HWND failure behavior, no standalone fallback, and the requested embedded debug-window baseline.
</success_criteria>

<output>
After completion, create `.planning/phases/43-embed-contract-child-window-bootstrap/43-03-SUMMARY.md`
</output>
