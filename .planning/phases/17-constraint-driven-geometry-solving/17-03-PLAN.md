---
phase: 17-constraint-driven-geometry-solving
plan: 03
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - src/ecs/ecs_scene.h
  - src/app.c
  - src/ui/ui_entity_inspector.h
  - src/tests/scene_solver_drag_test.c
  - src/tests/scene_solver_diagnostics_test.c
  - .planning/phases/17-constraint-driven-geometry-solving/17-VALIDATION.md
autonomous: true
requirements: [D-03, D-04, D-06, D-07, D-08]
must_haves:
  truths:
    - "Live drag uses constrained solve projection when feasible and remains responsive under bounded per-frame budget."
    - "Unsatisfiable drag keeps last valid solved geometry pose and emits immediate deterministic feedback."
    - "Diagnostics log appends attempts while suppressing identical consecutive entries."
    - "Validation document maps all D-01..D-12 decisions to automated/manual evidence."
  artifacts:
    - path: "src/ecs/ecs_scene.h"
      provides: "Live drag bounded solve + rejection behavior and diagnostics dedupe core"
      contains: "scene_solver_can_apply_drag + diagnostic dedupe logic"
    - path: "src/tests/scene_solver_drag_test.c"
      provides: "Feasible drag projection and unsat last-valid-state tests"
      contains: "scene_solver_drag"
    - path: ".planning/phases/17-constraint-driven-geometry-solving/17-VALIDATION.md"
      provides: "Nyquist-compliant Phase 17 evidence map"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "src/app.c"
      to: "src/ecs/ecs_scene.h"
      via: "gizmo drag loop consumes bounded solver decision contract"
      pattern: "scene_solver_can_apply_drag"
    - from: "src/ui/ui_entity_inspector.h"
      to: "src/ecs/ecs_scene.h"
      via: "diagnostics panel reflects deduped append-only events and clear action"
      pattern: "scene_solver_diagnostic_"
---

<objective>
Finalize constrained drag behavior and diagnostics quality gates, then close validation traceability for all Phase 17 locked decisions.

Purpose: complete user-facing constrained interaction guarantees and make acceptance evidence audit-ready.
Output: bounded live drag solve behavior, deterministic diagnostic dedupe, and updated Phase 17 validation compliance.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-constraint-driven-geometry-solving/17-CONTEXT.md
@.planning/phases/17-constraint-driven-geometry-solving/17-VALIDATION.md
@.planning/phases/17-constraint-driven-geometry-solving/17-01-SUMMARY.md
@src/app.c
@src/ecs/ecs_scene.h
@src/ui/ui_entity_inspector.h
@src/tests/scene_solver_drag_test.c
@src/tests/scene_solver_diagnostics_test.c
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement bounded live constrained drag with last-valid-state guarantees</name>
  <read_first>
    - src/app.c
    - src/ecs/ecs_scene.h
    - src/tests/scene_solver_drag_test.c
    - .planning/phases/17-constraint-driven-geometry-solving/17-CONTEXT.md
  </read_first>
  <files>
    - src/ecs/ecs_scene.h
    - src/app.c
    - src/tests/scene_solver_drag_test.c
  </files>
  <action>Extend the drag contract so feasible drags execute constrained projection/solve in real time (D-03) within a bounded per-frame solve budget and deterministic degrade path (D-07). When unsatisfiable, preserve last valid solved pose with zero partial invalid commits and emit immediate deterministic block feedback payload (D-04, D-05 linkage). Keep solver authority inside `scene_solver_*` and app loop as thin caller.</action>
  <acceptance_criteria>Drag tests verify projected feasible movement, bounded execution behavior, and unsat preservation of last-valid geometry state.</acceptance_criteria>
  <verify>
    <automated>ctest -R scene_solver_drag --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>Constrained drag semantics satisfy D-03, D-04, and D-07 in code and tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add diagnostics consecutive dedupe and deterministic ordering gates</name>
  <read_first>
    - src/ecs/ecs_scene.h
    - src/ui/ui_entity_inspector.h
    - src/tests/scene_solver_diagnostics_test.c
    - .planning/phases/17-constraint-driven-geometry-solving/17-VALIDATION.md
  </read_first>
  <files>
    - src/ecs/ecs_scene.h
    - src/ui/ui_entity_inspector.h
    - src/tests/scene_solver_diagnostics_test.c
    - .planning/phases/17-constraint-driven-geometry-solving/17-VALIDATION.md
  </files>
  <action>Implement append-with-dedupe behavior that suppresses identical consecutive diagnostics while retaining explicit user clear action (D-06). Ensure implicated constraint/entity ordering is deterministic for repeated identical failures (D-05, D-08). Update `17-VALIDATION.md` from draft to compliant by marking Wave-0 complete, adding command evidence rows for all required tests, and mapping each D-01..D-12 decision to automated/manual validation references.</action>
  <acceptance_criteria>Diagnostics tests pass for dedupe/order guarantees and Phase 17 validation file is nyquist-compliant with explicit command mapping.</acceptance_criteria>
  <verify>
    <automated>ctest -R scene_solver_diagnostics --test-dir build-vulkan -C Release --output-on-failure && ctest -R "scene_solver_contract|scene_solver_drag|endpoint_pick|scene_solver_diagnostics" --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>D-06 and D-08 are enforced by deterministic diagnostics tests and validation evidence is complete.</done>
</task>

</tasks>

<verification>
- `ctest -R scene_solver_drag --test-dir build-vulkan -C Release --output-on-failure`
- `ctest -R scene_solver_diagnostics --test-dir build-vulkan -C Release --output-on-failure`
- `ctest -R "solver|script_roundtrip" --test-dir build-vulkan -C Release --output-on-failure`
- `ctest --test-dir build-vulkan -C Release --output-on-failure`
</verification>

<success_criteria>
- Live drag is constrained, bounded, and deterministic with last-valid-state protection on unsat.
- Diagnostics history behaves as append-only with identical-consecutive dedupe and explicit clear.
- `17-VALIDATION.md` is upgraded to compliant with full D-01..D-12 traceability and executable evidence commands.
</success_criteria>

<output>
After completion, create `.planning/phases/17-constraint-driven-geometry-solving/17-03-SUMMARY.md`
</output>

