---
phase: 17-constraint-driven-geometry-solving
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - src/components/selectable_comp.h
  - src/ecs/ecs_scene.h
  - src/app.c
  - src/gpu/pick_buffer.h
  - src/ui/ui_viewport.h
  - src/tests/endpoint_pick_test.c
autonomous: false
requirements: [D-09, D-10, D-11, D-12]
must_haves:
  truths:
    - "User can select line/arc endpoints directly in normal viewport flow without Tab gizmo vertex mode."
    - "Coincident authoring supports endpoint-to-endpoint pairing across line/arc endpoints."
    - "Endpoint pick targets remain reliably rendered/pickable above continuous primitives."
  artifacts:
    - path: "src/ecs/ecs_scene.h"
      provides: "Endpoint/sub-entity pick ID mapping and participant extraction"
      contains: "endpoint pick decode/encode helpers"
    - path: "src/app.c"
      provides: "Viewport click path that routes endpoint picks into constraint menu participants"
      contains: "constraint_menu_participants endpoint-aware assembly"
    - path: "src/gpu/pick_buffer.h"
      provides: "Overlay endpoint pick rendering priority"
      contains: "pick_buffer_add_overlay_point"
  key_links:
    - from: "src/app.c"
      to: "src/ecs/ecs_scene.h"
      via: "viewport pick_id -> endpoint participant conversion"
      pattern: "endpoint|sub-entity|participant"
    - from: "src/ecs/ecs_scene.h"
      to: "src/gpu/pick_buffer.h"
      via: "populate pick buffer with endpoint overlays"
      pattern: "pick_buffer_add_overlay_point"
---

<objective>
Implement direct endpoint/sub-entity authoring in normal viewport flow and wire Coincident endpoint-to-endpoint creation without relying on gizmo vertex mode.

Purpose: close the structural endpoint authoring gap blocking robust chain/loop Coincident workflows.
Output: endpoint pick contracts, viewport selection integration, and endpoint-pick validation.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-constraint-driven-geometry-solving/17-CONTEXT.md
@.planning/phases/17-constraint-driven-geometry-solving/17-RESEARCH.md
@.planning/phases/17-constraint-driven-geometry-solving/17-VALIDATION.md
@.planning/phases/17-constraint-driven-geometry-solving/17-01-SUMMARY.md
@src/app.c
@src/ecs/ecs_scene.h
@src/gpu/pick_buffer.h
@src/gizmo/gizmo_vertex_mode.h
@src/ui/ui_viewport.h

<interfaces>
From `src/gpu/pick_buffer.h`:
```c
static inline void pick_buffer_add_overlay_point(pick_buffer_t *pb, vec3_t center, uint32_t pick_id);
```

From `src/app.c`:
```c
static bool collect_constraint_menu_selection_signature(..., ecs_entity_t *out_participants, uint32_t *out_participant_count, constraint_selection_signature_t *out_sig);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add endpoint/sub-entity pick contracts and viewport authoring integration</name>
  <read_first>
    - src/app.c
    - src/ecs/ecs_scene.h
    - src/gpu/pick_buffer.h
    - src/gizmo/gizmo_vertex_mode.h
    - .planning/phases/17-constraint-driven-geometry-solving/17-CONTEXT.md
  </read_first>
  <files>
    - src/components/selectable_comp.h
    - src/ecs/ecs_scene.h
    - src/app.c
    - src/gpu/pick_buffer.h
    - src/ui/ui_viewport.h
  </files>
  <action>Add first-class endpoint pick IDs and metadata plumbing so line/arc endpoints are hoverable/selectable in normal viewport flow (D-09, D-10). Route endpoint picks through the existing constraint-menu participant path and construct participant descriptors for endpoint roles; Coincident selection legality must accept endpoint-to-endpoint pairings across line/arc endpoints (D-11). Keep Tab key behavior unchanged as gizmo mode toggle, but ensure endpoint authoring path does not require it (D-09 explicit). Preserve existing entity picks and constraint glyph picks while introducing endpoint targets.</action>
  <acceptance_criteria>Viewport click path can collect endpoint participants without entering gizmo geometry mode, and Coincident creation accepts endpoint-to-endpoint pairs.</acceptance_criteria>
  <verify>
    <automated>ctest -R endpoint_pick --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>Direct endpoint authoring path exists and is independent from gizmo vertex mode.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce endpoint-over-primitive pick layering and regression coverage</name>
  <read_first>
    - src/gpu/pick_buffer.h
    - src/ecs/ecs_scene.h
    - src/tests/endpoint_pick_test.c
  </read_first>
  <files>
    - src/gpu/pick_buffer.h
    - src/ecs/ecs_scene.h
    - src/tests/endpoint_pick_test.c
  </files>
  <action>Use overlay endpoint point submissions and deterministic pick ordering so endpoint targets win against overlapping line/arc/circle primitives in both render visibility and pick decode (D-12). Expand `endpoint_pick_test.c` fixtures to cover precedence collisions and repeated sampling determinism. Keep implementation in existing pick buffer overlay mechanism rather than adding a second pick pipeline.</action>
  <acceptance_criteria>Endpoint precedence test cases pass and demonstrate stable endpoint pick IDs over overlapping continuous geometry.</acceptance_criteria>
  <verify>
    <automated>ctest -R endpoint_pick --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>D-12 reliability contract is locked by deterministic automated tests.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify endpoint chain/loop Coincident authoring UX</name>
  <action>Run the automated endpoint-pick regression first, then perform focused interactive verification of direct endpoint Coincident authoring flow per D-09..D-12.</action>
  <what-built>Endpoint/sub-entity picking and Coincident endpoint authoring in standard viewport flow.</what-built>
  <how-to-verify>
    1. Build and run app: `cmake --build build-vulkan --config Release --target mdCAD`.
    2. Create a sketch with multiple lines/arcs forming an open chain, then a near-closed loop.
    3. Without pressing Tab, select endpoints directly in viewport and apply Coincident constraints to connect chain segments.
    4. Repeat for loop closure using endpoint-to-endpoint picks including arc endpoints.
    5. Confirm endpoint points remain easy to pick when line/arc primitives overlap.
  </how-to-verify>
  <verify>
    <automated>ctest -R endpoint_pick --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>Human confirms direct endpoint authoring reliability or returns specific repro steps for any failure.</done>
  <resume-signal>Type "approved" if endpoint authoring is reliable, or provide failing interaction details.</resume-signal>
</task>

</tasks>

<verification>
- Automated: `ctest -R endpoint_pick --test-dir build-vulkan -C Release --output-on-failure`
- Manual UX row from `17-VALIDATION.md`: endpoint chain/loop Coincident authoring usability.
</verification>

<success_criteria>
- Endpoint/sub-entity picks are usable in standard viewport flow with no Tab dependency.
- Coincident endpoint-to-endpoint authoring works for line/arc endpoint combinations.
- Endpoint pick precedence over continuous primitives is deterministic and test-backed.
</success_criteria>

<output>
After completion, create `.planning/phases/17-constraint-driven-geometry-solving/17-02-SUMMARY.md`
</output>

