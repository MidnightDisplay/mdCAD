---
phase: 17-constraint-driven-geometry-solving
plan: 04
type: execute
wave: 3
depends_on: [17-01, 17-03]
files_modified:
  - src/components/endpoints_comp.h
  - src/components/component_types.h
  - src/ecs/ecs_world.h
  - src/ecs/ecs_scene.h
  - src/components/selectable_comp.h
  - src/tests/endpoint_pick_test.c
autonomous: true
gap_closure: true
requirements: [D-09, D-10, D-12]
must_haves:
  truths:
    - "Sketch-created lines/arcs show visible endpoint points in normal viewport flow."
    - "Endpoint points are first-class selectable entities without requiring Tab gizmo vertex mode."
    - "Endpoint points remain pick-prioritized over overlapping continuous primitives during normal hover/click."
    - "Bare non-sketch lines/arcs keep existing behavior with no injected endpoint entities."
  artifacts:
    - path: "src/components/endpoints_comp.h"
      provides: "Sketch-scoped EndPoints contract for notable-vertex endpoint entities"
      contains: "endpoint owner/role/index metadata and sync contract"
    - path: "src/ecs/ecs_scene.h"
      provides: "Endpoint entity spawn/sync lifecycle parented to sketch geometry entities"
      contains: "sketch-only endpoint creation + update hooks"
    - path: "src/tests/endpoint_pick_test.c"
      provides: "Deterministic endpoint visibility/pick-priority coverage for native point entities"
      contains: "native endpoint entity precedence assertions"
  key_links:
    - from: "src/ecs/ecs_scene.h"
      to: "src/components/endpoints_comp.h"
      via: "sketch geometry notable-vertex -> endpoint point entity spawn/sync"
      pattern: "EndPoints|endpoint.*sync|endpoint.*owner"
    - from: "src/ecs/ecs_scene.h"
      to: "src/tests/endpoint_pick_test.c"
      via: "native endpoint entity pick IDs in normal viewport flow"
      pattern: "endpoint.*pick|overlay.*point"
    - from: "src/ecs/ecs_scene.h"
      to: "src/components/selectable_comp.h"
      via: "endpoint entities registered as first-class selectable participants"
      pattern: "selectable|hover|pick"
---

<objective>
Replace synthetic endpoint tail-range behavior with sketch-scoped native endpoint point entities so endpoint authoring is visible and first-class in normal viewport selection flow.

Purpose: close the failed checkpoint gap by implementing the user-approved redesign direction while preserving non-sketch behavior.
Output: EndPoints component contract, sketch-only endpoint entity lifecycle/sync, and deterministic native endpoint pick tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-constraint-driven-geometry-solving/17-CONTEXT.md
@.planning/phases/17-constraint-driven-geometry-solving/17-VALIDATION.md
@.planning/phases/17-constraint-driven-geometry-solving/17-02-SUMMARY.md
@src/components/component_types.h
@src/components/selectable_comp.h
@src/ecs/ecs_world.h
@src/ecs/ecs_scene.h
@src/tests/endpoint_pick_test.c
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define sketch-scoped EndPoints component contract and registration</name>
  <read_first>
    - .planning/phases/17-constraint-driven-geometry-solving/17-CONTEXT.md
    - .planning/phases/17-constraint-driven-geometry-solving/17-02-SUMMARY.md
    - src/components/component_types.h
    - src/ecs/ecs_world.h
  </read_first>
  <files>
    - src/components/endpoints_comp.h
    - src/components/component_types.h
    - src/ecs/ecs_world.h
  </files>
  <action>Create a new sketch-scoped EndPoints component/contract that models notable vertices as native point entities parented to sketch geometry entities (per D-10 redesign direction and failed-checkpoint hard requirement). Register this component in ECS/component type wiring. Include explicit metadata to map endpoint point entity -> owning geometry entity + notable vertex role/index so later constraint authoring uses point semantics (per D-09). Add scope guard in contract comments and usage notes: this component is attached only to sketch-created lines/arcs, not bare non-sketch geometry.</action>
  <acceptance_criteria>EndPoints contract exists, is registered, and encodes sketch-only endpoint ownership/role metadata needed for first-class selection and sync.</acceptance_criteria>
  <verify>
    <automated>ctest -R endpoint_pick --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>Codebase has a concrete EndPoints contract ready for native endpoint entity lifecycle implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Spawn/sync native endpoint point entities for sketch lines/arcs and lock pick precedence</name>
  <read_first>
    - src/components/endpoints_comp.h
    - src/components/selectable_comp.h
    - src/ecs/ecs_scene.h
    - src/tests/endpoint_pick_test.c
    - .planning/phases/17-constraint-driven-geometry-solving/17-02-SUMMARY.md
  </read_first>
  <files>
    - src/ecs/ecs_scene.h
    - src/components/selectable_comp.h
    - src/tests/endpoint_pick_test.c
  </files>
  <action>Implement endpoint entity lifecycle in sketch scene flow: create notable-vertex point entities as children of sketch line/arc entities, keep them transform-synced as geometry changes, and expose them as normal selectable/hoverable entities in viewport path (per D-09, D-10). Remove reliance on synthetic endpoint tail-range pick decoding by routing through native point entity picks. Ensure pick/render precedence still resolves endpoint points over continuous primitives in overlapping regions (D-12). Enforce scope guard explicitly in creation/update code: do not create/alter endpoint behavior for bare non-sketch lines/arcs.</action>
  <acceptance_criteria>Sketch lines/arcs have synced native endpoint point entities that are visible/selectable in normal flow and tested for precedence without synthetic tail-range coupling.</acceptance_criteria>
  <verify>
    <automated>ctest -R endpoint_pick --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>Endpoint visibility and first-class selection foundation is implemented with sketch-only scope preserved.</done>
</task>

</tasks>

<verification>
- `ctest -R endpoint_pick --test-dir build-vulkan -C Release --output-on-failure`
- Confirm no regressions in non-endpoint solver flows: `ctest -R "scene_solver_contract|scene_solver_drag|scene_solver_diagnostics" --test-dir build-vulkan -C Release --output-on-failure`
</verification>

<success_criteria>
- Endpoint points are visibly rendered from native point entities during normal viewport flow.
- Endpoint authoring no longer depends on Tab gizmo vertex mode.
- Endpoint pick precedence remains deterministic over overlapping primitives.
- Bare non-sketch line/arc behavior remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/17-constraint-driven-geometry-solving/17-04-SUMMARY.md`
</output>
