---
phase: 17-constraint-driven-geometry-solving
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/constraint_comp.h
  - src/constraints/constraint_types.h
  - src/ecs/ecs_scene.h
  - src/tests/scene_solver_contract_test.c
  - src/tests/scene_solver_drag_test.c
  - src/tests/endpoint_pick_test.c
  - src/tests/scene_solver_diagnostics_test.c
  - src/CMakeLists.txt
autonomous: true
requirements: [D-01, D-02, D-05, D-08, D-09, D-10, D-11]
must_haves:
  truths:
    - "Constraint solves mutate sketch geometry only when the full solve succeeds."
    - "Contradictory solves fail deterministically with no scene mutation and stable implication payload."
    - "Constraint participants can represent endpoint/sub-entity targets needed by direct Coincident endpoint authoring."
    - "Phase 17 Wave-0 solver/endpoint/diagnostic tests exist and are runnable through CTest."
  artifacts:
    - path: "src/components/constraint_comp.h"
      provides: "Constraint participant schema with explicit sub-entity role/index support"
      contains: "participant descriptor type used by constraints"
    - path: "src/ecs/ecs_scene.h"
      provides: "Transactional solve-apply path and deterministic failure contract"
      contains: "scene_solver_request_recalculate transactional logic"
    - path: "src/tests/scene_solver_contract_test.c"
      provides: "Deterministic solve success/failure no-mutation tests"
      contains: "scene_solver_contract"
    - path: "src/CMakeLists.txt"
      provides: "CTest registration for Phase 17 solver/pick/diagnostic tests"
      contains: "add_test(NAME scene_solver_contract"
  key_links:
    - from: "src/ecs/ecs_scene.h"
      to: "src/components/constraint_comp.h"
      via: "participant descriptor usage in solve + implication paths"
      pattern: "participant.*role|participant.*index"
    - from: "src/CMakeLists.txt"
      to: "src/tests/scene_solver_contract_test.c"
      via: "CTest executable registration"
      pattern: "add_test\\(NAME scene_solver_contract"
---

<objective>
Establish Phase 17 solver and participant contracts: transactional solve-apply behavior plus endpoint/sub-entity participant representation and Wave-0 deterministic tests.

Purpose: create the non-negotiable foundation for direct endpoint authoring and deterministic solver behavior before viewport wiring.
Output: participant model contract, transactional recalc path, and four registered CTest targets required by `17-VALIDATION.md`.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-constraint-driven-geometry-solving/17-CONTEXT.md
@.planning/phases/17-constraint-driven-geometry-solving/17-RESEARCH.md
@.planning/phases/17-constraint-driven-geometry-solving/17-VALIDATION.md
@src/components/constraint_comp.h
@src/constraints/constraint_types.h
@src/ecs/ecs_scene.h
@src/CMakeLists.txt

<interfaces>
From `src/ecs/ecs_scene.h`:
```c
static inline bool scene_solver_request_recalculate(ecs_scene_t *scene, ecs_entity_t sketch);
static inline bool scene_solver_set_failure_implication(ecs_scene_t *scene, ecs_entity_t sketch, const ecs_entity_t *implicated_constraints, int implicated_constraint_count, const char *reason);
static inline bool scene_solver_can_apply_drag(ecs_scene_t *scene, ecs_entity_t sketch, const ecs_entity_t *drag_entities, int drag_entity_count, vec3_t requested_delta, scene_solver_drag_decision_t *out_decision);
```

From `src/constraints/constraint_types.h`:
```c
typedef enum {
    CONSTRAINT_PARTICIPANT_ROLE_UNSPECIFIED = 0,
    CONSTRAINT_PARTICIPANT_ROLE_ENTITY = 1,
    CONSTRAINT_PARTICIPANT_ROLE_POINT_A = 2,
    CONSTRAINT_PARTICIPANT_ROLE_POINT_B = 3,
    CONSTRAINT_PARTICIPANT_ROLE_CENTER = 4
} constraint_participant_role_t;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Wave-0 deterministic test scaffolds and CTest registration</name>
  <read_first>
    - .planning/phases/17-constraint-driven-geometry-solving/17-VALIDATION.md
    - src/CMakeLists.txt
    - src/tests/script_roundtrip_tests.c
  </read_first>
  <files>
    - src/tests/scene_solver_contract_test.c
    - src/tests/scene_solver_drag_test.c
    - src/tests/endpoint_pick_test.c
    - src/tests/scene_solver_diagnostics_test.c
    - src/CMakeLists.txt
  </files>
  <action>Create missing Wave-0 test executables and `add_test(...)` entries exactly matching validation target names (`scene_solver_contract`, `scene_solver_drag`, `endpoint_pick`, `scene_solver_diagnostics`) per D-08. Start with deterministic RED cases (failing or TODO assertions allowed) that explicitly codify: transactional no-mutation on failure (D-05), solve-apply success mutation (D-01, D-02), endpoint priority contract baseline (D-10, D-12), and diagnostics dedupe baseline (D-06). Keep tests native C test executables following existing `script_roundtrip_tests` style; do not add external frameworks.</action>
  <acceptance_criteria>All four test source files exist and are registered in `src/CMakeLists.txt`; each target can be invoked independently by CTest name.</acceptance_criteria>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD && ctest -R "scene_solver_contract|scene_solver_drag|endpoint_pick|scene_solver_diagnostics" --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>Wave-0 requirements in `17-VALIDATION.md` are materially present and executable for this phase.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor participant and solve contracts for endpoint-aware transactional solve</name>
  <read_first>
    - src/components/constraint_comp.h
    - src/constraints/constraint_types.h
    - src/ecs/ecs_scene.h
    - .planning/phases/17-constraint-driven-geometry-solving/17-CONTEXT.md
  </read_first>
  <files>
    - src/components/constraint_comp.h
    - src/constraints/constraint_types.h
    - src/ecs/ecs_scene.h
    - src/tests/scene_solver_contract_test.c
  </files>
  <action>Implement endpoint/sub-entity participant representation so constraints can carry entity + sub-entity role/index metadata (D-09, D-10, D-11), then update solver implication/selection-relevant accessors to preserve deterministic ordering (D-05). In `scene_solver_request_recalculate(...)`, implement transactional solve behavior: stage candidate geometry state, validate full active constraint set (D-01), commit atomically on success with immediate geometry update (D-02), and on failure emit deterministic implication reason with zero geometry mutation (D-05). Explicitly avoid multi-sketch/global solve and multi-backend switching (deferred scope).</action>
  <acceptance_criteria>Constraint participant model supports endpoint/sub-entity semantics, and recalculate path guarantees atomic commit/no-mutation failure behavior under deterministic contract tests.</acceptance_criteria>
  <verify>
    <automated>ctest -R scene_solver_contract --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>D-01, D-02, D-05, D-09, D-10, and D-11 foundational contracts are enforced in code and test fixtures.</done>
</task>

</tasks>

<verification>
- `ctest -R scene_solver_contract --test-dir build-vulkan -C Release --output-on-failure`
- `ctest -R "scene_solver_drag|endpoint_pick|scene_solver_diagnostics" --test-dir build-vulkan -C Release --output-on-failure`
</verification>

<success_criteria>
- Recalculate path is transactional and deterministic, with no invalid partial mutation.
- Participant model can encode endpoint/sub-entity selections for downstream viewport authoring.
- All four Phase 17 validation test targets exist and run via CTest.
</success_criteria>

<output>
After completion, create `.planning/phases/17-constraint-driven-geometry-solving/17-01-SUMMARY.md`
</output>

