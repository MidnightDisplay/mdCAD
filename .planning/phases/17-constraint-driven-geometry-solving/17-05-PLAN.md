---
phase: 17-constraint-driven-geometry-solving
plan: 05
type: execute
wave: 4
depends_on: [17-04]
files_modified:
  - src/app.c
  - src/ui/ui_viewport.h
  - src/constraints/constraint_types.h
  - src/tests/endpoint_pick_test.c
  - .planning/phases/17-constraint-driven-geometry-solving/17-VALIDATION.md
autonomous: false
gap_closure: true
requirements: [D-10, D-11, D-12]
must_haves:
  truths:
    - "Left-clicking endpoint points in normal viewport selects point participants without hidden C/Tab mode coupling."
    - "Point-selected context/authoring options are semantically valid for point entities and exclude invalid line-only options."
    - "Coincident endpoint-to-endpoint authoring works through native point selection for line/arc endpoint chains and loop closure."
    - "Endpoint UX closure is verified by human checkpoint after automated endpoint regression pass."
  artifacts:
    - path: "src/app.c"
      provides: "Endpoint point selection/context routing through normal viewport interaction flow"
      contains: "constraint participant/context assembly for point entities"
    - path: "src/constraints/constraint_types.h"
      provides: "Constraint legality/menu semantics that distinguish point participant context"
      contains: "point-context legality filtering"
    - path: ".planning/phases/17-constraint-driven-geometry-solving/17-VALIDATION.md"
      provides: "Updated endpoint UX closure evidence and remaining/open rows"
      contains: "manual checkpoint result for D-09..D-12"
  key_links:
    - from: "src/app.c"
      to: "src/constraints/constraint_types.h"
      via: "selection signature -> legal constraint menu options for point context"
      pattern: "constraint.*legal|participant.*role|point"
    - from: "src/app.c"
      to: "src/ui/ui_viewport.h"
      via: "normal viewport click/hover path for endpoint points"
      pattern: "viewport|pick|selection"
    - from: "src/tests/endpoint_pick_test.c"
      to: "src/app.c"
      via: "automated regression for endpoint context semantics and authoring path"
      pattern: "endpoint|context|coincident"
---

<objective>
Finish endpoint UX gap closure by correcting point-context semantics and validating end-to-end Coincident endpoint authoring in normal viewport interaction.

Purpose: resolve failed human-checkpoint behaviors (invalid context options and hidden mode dependency) and produce closure evidence.
Output: point-context legality fixes, endpoint authoring flow validation, and updated phase validation status.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-constraint-driven-geometry-solving/17-CONTEXT.md
@.planning/phases/17-constraint-driven-geometry-solving/17-VALIDATION.md
@.planning/phases/17-constraint-driven-geometry-solving/17-02-SUMMARY.md
@.planning/phases/17-constraint-driven-geometry-solving/17-04-SUMMARY.md
@src/app.c
@src/ui/ui_viewport.h
@src/constraints/constraint_types.h
@src/tests/endpoint_pick_test.c
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Correct point-context selection/menu semantics for native endpoint entities</name>
  <read_first>
    - .planning/phases/17-constraint-driven-geometry-solving/17-02-SUMMARY.md
    - src/app.c
    - src/constraints/constraint_types.h
    - src/ui/ui_viewport.h
  </read_first>
  <files>
    - src/app.c
    - src/ui/ui_viewport.h
    - src/constraints/constraint_types.h
    - src/tests/endpoint_pick_test.c
  </files>
  <action>Fix endpoint point selection semantics so simple endpoint clicks in normal viewport flow resolve as point participant selection and do not trigger hidden C/Tab-derived behavior (per D-10 and checkpoint failure). Update legality filtering so single point selection shows only semantically valid point-context constraints and excludes line-oriented options (Fixed/Along X/Y/Z/Length mismatch observed in failure). Ensure Coincident endpoint-to-endpoint authoring remains legal and explicit for line/arc endpoint chains/loops (D-11). Add regression coverage in endpoint tests for point-context menu legality/signature behavior.</action>
  <acceptance_criteria>Endpoint point selection yields correct point-context options, invalid line-only options are absent for point context, and Coincident endpoint pairing path remains valid.</acceptance_criteria>
  <verify>
    <automated>ctest -R endpoint_pick --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>Point-context semantics are consistent with native endpoint entity model and failed-checkpoint menu mismatch is closed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify native endpoint authoring UX closure in viewport</name>
  <read_first>
    - .planning/phases/17-constraint-driven-geometry-solving/17-VALIDATION.md
    - .planning/phases/17-constraint-driven-geometry-solving/17-02-SUMMARY.md
  </read_first>
  <action>Run automated endpoint regression first, then validate endpoint visibility, normal-flow point selection semantics, and Coincident chain/loop authoring directly in app.</action>
  <acceptance_criteria>Human confirms endpoints are visibly first-class in normal flow, context semantics are point-correct, and endpoint Coincident authoring works without Tab dependency.</acceptance_criteria>
  <verify>
    <automated>ctest -R endpoint_pick --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <how_to_verify>
    1. Build and launch app: `cmake --build build-vulkan --config Release --target mdCAD`.
    2. Create a sketch and add multiple sketch lines/arcs; verify endpoint points are visibly rendered without pressing Tab.
    3. Left-click a single endpoint point; verify selection is point-context and menu does not show invalid line-only options.
    4. Author Coincident constraints by selecting endpoint-to-endpoint pairs across line-line and line-arc/arc-arc endpoints.
    5. Build an open chain then close a loop using endpoint Coincident; confirm reliability under overlapping geometry.
    6. Add a bare non-sketch line/arc and verify its behavior is unchanged from baseline (no sketch endpoint injection).
  </how_to_verify>
  <done>Checkpoint approved with explicit note in summary/validation, or blocked with reproducible failure details.</done>
  <resume-signal>Type "approved" or provide exact failing steps/screens.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record endpoint gap-closure evidence in Phase 17 validation</name>
  <read_first>
    - .planning/phases/17-constraint-driven-geometry-solving/17-VALIDATION.md
    - .planning/phases/17-constraint-driven-geometry-solving/17-02-SUMMARY.md
  </read_first>
  <files>
    - .planning/phases/17-constraint-driven-geometry-solving/17-VALIDATION.md
  </files>
  <action>Update validation evidence rows for D-09..D-12 to reflect native endpoint entity redesign outcome, include automated command evidence reruns, and replace failed-checkpoint addendum status with closure status once approved. If checkpoint fails, keep rows open and document exact failure reason/repro for next gap cycle.</action>
  <acceptance_criteria>Validation file clearly states endpoint UX closure state with command evidence and requirement-level disposition.</acceptance_criteria>
  <verify>
    <automated>ctest -R "endpoint_pick|scene_solver_contract|scene_solver_drag|scene_solver_diagnostics" --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>Phase validation artifact is synchronized with actual endpoint gap-closure outcome.</done>
</task>

</tasks>

<verification>
- `ctest -R endpoint_pick --test-dir build-vulkan -C Release --output-on-failure`
- `ctest -R "scene_solver_contract|scene_solver_drag|scene_solver_diagnostics" --test-dir build-vulkan -C Release --output-on-failure`
</verification>

<success_criteria>
- Endpoint points are visibly first-class selectable in normal viewport flow.
- No dependency on Tab gizmo vertex mode exists for endpoint authoring.
- Point-context constraint semantics are correct for point selections.
- Coincident endpoint-to-endpoint authoring is reliable for sketch line/arc chains and loop closure.
- Bare non-sketch lines/arcs remain unaffected.
</success_criteria>

<output>
After completion, create `.planning/phases/17-constraint-driven-geometry-solving/17-05-SUMMARY.md`
</output>
