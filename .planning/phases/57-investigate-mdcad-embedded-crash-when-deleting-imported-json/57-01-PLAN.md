---
phase: 57-investigate-mdcad-embedded-crash-when-deleting-imported-json
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/CMakeLists.txt
  - src/tests/startup_jsonl_delete_regression_test.c
  - src/tests/win32_embed_diagnostics_test.c
  - src/ui/ui_scene_hierarchy.h
  - src/undo_redo_exec.h
  - src/ecs/ecs_scene.h
  - src/jsonl_observer_system.h
  - src/platform/win32_embed.h
  - src/app.c
autonomous: true
requirements:
  - P57-01
  - P57-02
  - P57-03
must_haves:
  truths:
    - "Deleting a startup-imported JSONL node from the embedded scene hierarchy no longer crashes mdCAD."
    - "Both plain startup import and startup live-refresh delete paths stay stable when the imported selection is removed."
    - "An embedded fatal startup/runtime failure writes a stable working-directory crash record and emits one truthful stderr summary line."
  artifacts:
    - path: "src/tests/startup_jsonl_delete_regression_test.c"
      provides: "Native regression coverage for the exact startup-imported delete path in both startup JSONL modes"
      min_lines: 80
    - path: "src/tests/win32_embed_diagnostics_test.c"
      provides: "Native coverage for crash-record writing and stderr summary formatting"
      min_lines: 60
    - path: "src/platform/win32_embed.h"
      provides: "Embedded-only crash record writer, stderr summary helper, and fatal hook glue"
      contains: "mdcad_win32_embed"
    - path: "src/ui/ui_scene_hierarchy.h"
      provides: "Canonical imported-entity delete flow"
      contains: "ui_scene_hierarchy_delete_entities"
  key_links:
    - from: "src/ui/ui_scene_hierarchy.h"
      to: "src/undo_redo_exec.h"
      via: "canonicalized entity list before undo snapshot"
      pattern: "undo_cmd_bulk_delete_entities"
    - from: "src/ui/ui_scene_hierarchy.h"
      to: "src/jsonl_observer_system.h"
      via: "refresh-anchor cancellation before teardown"
      pattern: "jsonl_observer_cancel_flat_refresh_for_root"
    - from: "src/app.c"
      to: "src/platform/win32_embed.h"
      via: "embedded startup/fatal helper installation"
      pattern: "mdcad_win32_embed"
---

<objective>
Close the native root cause first, per D-01, by making the startup-imported delete path safe in both startup JSONL modes and by adding the working-directory crash/error record required by D-03 and D-08.

Purpose: eliminate the embedded mdCAD crash without changing the external-process child-HWND architecture from D-02.
Output: native regression tests, the delete-path fix, and embedded-only crash logging that hosts can observe through the existing stderr seam from D-07.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-investigate-mdcad-embedded-crash-when-deleting-imported-json/57-CONTEXT.md
@.planning/phases/57-investigate-mdcad-embedded-crash-when-deleting-imported-json/57-RESEARCH.md
@src/ui/ui_scene_hierarchy.h
@src/undo_redo_exec.h
@src/ecs/ecs_scene.h
@src/jsonl_observer_system.h
@src/platform/win32_embed.h
@src/app.c

<interfaces>
From src/ui/ui_scene_hierarchy.h:
`static inline void ui_scene_hierarchy_delete_entities(ui_scene_hierarchy_state_t *state, const ecs_entity_t *entities, int count)`

From src/undo_redo_exec.h:
`static inline void undo_cmd_bulk_delete_entities(undo_redo_t *ur, ecs_entity_t *entities, int count)`

From src/ecs/ecs_scene.h:
`static inline void scene_remove_entity(ecs_scene_t *scene, ecs_entity_t e)`

From src/jsonl_observer_system.h:
`static inline void jsonl_observer_cancel_flat_refresh_for_root(ecs_scene_t *scene, ecs_entity_t root_entity)`

From src/platform/win32_embed.h:
`static inline void mdcad_win32_embed_fail_startup(const char* reason)`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the startup-imported delete regression and harden the native delete path</name>
  <files>src/CMakeLists.txt, src/tests/startup_jsonl_delete_regression_test.c, src/ui/ui_scene_hierarchy.h, src/undo_redo_exec.h, src/ecs/ecs_scene.h, src/jsonl_observer_system.h</files>
  <behavior>
    - Test 1: per D-04, deleting a startup-imported JSONL child selected in the hierarchy completes without crashing.
    - Test 2: per D-06, the same delete contract holds for plain startup import and startup live-refresh arming.
    - Test 3: deleting an imported root cancels any observer/root bookkeeping once and leaves selection/scene state clean.
  </behavior>
  <action>Create `startup_jsonl_delete_regression_test.c` and register it in `src/CMakeLists.txt` so the repo has a direct native proof lane for the exact D-04 repro. Build the fixture around a startup-imported parent/child JSONL scene and cover both `StartupLiveRefreshEnabled=false` and `true` per D-06. Then harden `ui_scene_hierarchy_delete_entities` to canonicalize requested deletions to unique live roots before undo snapshotting, resolve and cancel any JSONL refresh anchor/root exactly once before teardown, drop stale selection entries before removal, and only invoke recursive removal for still-live entities. If `undo_cmd_bulk_delete_entities` or `scene_remove_entity` still assumes duplicate/dead descendants can appear, tighten those helpers so imported subtree expansion and teardown stay idempotent instead of crashing. Do not add host callbacks, new IPC, or architecture changes; this remains a native-first fix per D-01, D-02, and D-07.</action>
  <verify>
    <automated>cmake --build .\build --target startup_jsonl_delete_regression_test -j 4 &amp;&amp; ctest --test-dir .\build -R startup_jsonl_delete_regression_test --output-on-failure</automated>
  </verify>
  <done>The exact startup-imported delete repro is covered by an automated native test, both startup JSONL modes are exercised, and the delete path completes without crashing or leaving dead selection/observer state behind.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit a stable embedded crash record and stderr summary from the native seam</name>
  <files>src/CMakeLists.txt, src/tests/win32_embed_diagnostics_test.c, src/platform/win32_embed.h, src/app.c</files>
  <behavior>
    - Test 1: embedded startup failure writes/overwrites `mdcad-embed-crash.log` in the working directory and emits one stable stderr summary line.
    - Test 2: embedded fatal-runtime formatting includes only observed facts: failure class, exit/exception data when known, and the log-file path.
    - Test 3: non-embedded startup remains unchanged.
  </behavior>
  <action>Factor the embedded failure formatting/writing path into testable helpers in `win32_embed.h`, then add `win32_embed_diagnostics_test.c` and register it in `src/CMakeLists.txt`. Use those helpers from the existing startup-parse failure path in `src/app.c` and from an embedded-only fatal hook path so mdCAD overwrites a stable working-directory file named `mdcad-embed-crash.log` per D-03 and D-08. The record must include timestamp, failure class, the exact summary text, and exit/exception data when known; stderr must emit one summary line that points to the log file so the Avalonia/WPF backends can reuse stdout/stderr plus unexpected-session-loss exactly as required by D-07. Keep desktop/non-embedded behavior untouched, and keep the summary truthful per D-09.</action>
  <verify>
    <automated>cmake --build .\build --target win32_embed_diagnostics_test -j 4 &amp;&amp; ctest --test-dir .\build -R win32_embed_diagnostics_test --output-on-failure</automated>
  </verify>
  <done>Embedded startup/runtime failures produce a stable working-directory crash record plus a single parseable stderr summary line, and that contract is covered by native automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| scene hierarchy UI → native delete path | Untrusted selection/delete requests cross into recursive entity teardown. |
| embedded mdCAD process → working-directory diagnostics | Fatal native state is persisted to disk for host/operator recovery. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-01 | Denial of Service | `ui_scene_hierarchy_delete_entities` | mitigate | Canonicalize to live roots, cancel refresh state once, and prove both startup JSONL modes with `startup_jsonl_delete_regression_test`. |
| T-57-02 | Tampering | `mdcad-embed-crash.log` | mitigate | Overwrite the file atomically per embedded failure, stamp it with failure class/timestamp, and keep the filename stable so stale content is not mistaken for the current crash. |
| T-57-03 | Repudiation | stderr crash summary | mitigate | Emit one deterministic summary line from the same helper that writes the crash record so hosts see the same observed facts the log contains. |
| T-57-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are part of Phase 57. |
</threat_model>

<verification>
Run the two native proof lanes after each task, then run them together before advancing:

- `ctest --test-dir .\build -R "startup_jsonl_delete_regression_test|win32_embed_diagnostics_test" --output-on-failure`
</verification>

<success_criteria>
- The D-04 delete repro is automated and green for both startup JSONL modes.
- The delete chain is safe without changing the embedded-process architecture or adding IPC.
- Embedded failures create `mdcad-embed-crash.log` in the working directory and emit a single stderr summary line that later host plans can surface verbatim.
</success_criteria>

<output>
Create `.planning/phases/57-investigate-mdcad-embedded-crash-when-deleting-imported-json/57-01-SUMMARY.md` when done
</output>
