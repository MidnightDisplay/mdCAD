---
phase: 57-investigate-mdcad-embedded-crash-when-deleting-imported-json
plan: 03
type: execute
wave: 2
depends_on:
  - 57-01
files_modified:
  - samples/wpf-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs
  - samples/wpf-mdcad-control/MdCadEmbeddedControl.xaml.cs
  - samples/wpf-mdcad-control.tests/WindowsMdCadEmbedBackendTests.cs
  - samples/wpf-mdcad-control.tests/MdCadEmbeddedControlTests.cs
  - samples/wpf-host/MainWindow.xaml.cs
  - samples/wpf-host.tests/WpfHost.Tests.csproj
  - samples/wpf-host.tests/MainWindowHarnessStatusTests.cs
autonomous: true
requirements:
  - P57-04
  - P57-05
must_haves:
  truths:
    - "WPF hosts surface the native crash/log detail after unexpected embedded session loss without inventing a cause."
    - "The sealed WPF control remains the minimal-host warning owner."
    - "The full WPF host mirrors the same observed detail in its harness status text."
  artifacts:
    - path: "samples/wpf-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs"
      provides: "Unexpected-exit detail capture and composition from stderr plus exit state"
      contains: "UnexpectedSessionLoss"
    - path: "samples/wpf-mdcad-control/MdCadEmbeddedControl.xaml.cs"
      provides: "Control-owned warning relay plus host-facing notification seam"
      contains: "SetLaunchWarning"
    - path: "samples/wpf-host/MainWindow.xaml.cs"
      provides: "Full-host mirror of observed crash/log detail"
      contains: "BuildHarnessStatus"
  key_links:
    - from: "samples/wpf-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs"
      to: "samples/wpf-mdcad-control/MdCadEmbeddedControl.xaml.cs"
      via: "UnexpectedSessionLoss event"
      pattern: "UnexpectedSessionLoss"
    - from: "samples/wpf-mdcad-control/MdCadEmbeddedControl.xaml.cs"
      to: "samples/wpf-host/MainWindow.xaml.cs"
      via: "public relay seam for observed warning detail"
      pattern: "UnexpectedSessionLoss"
---

<objective>
Mirror the Phase 57 diagnostics contract through the WPF backend, control, and full host so both .NET host stacks stay behaviorally aligned after the native fix.

Purpose: D-03, D-05, D-07, and D-09 apply to WPF just as strongly as Avalonia.
Output: WPF backend/control/full-host crash-detail surfacing plus tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-investigate-mdcad-embedded-crash-when-deleting-imported-json/57-CONTEXT.md
@.planning/phases/57-investigate-mdcad-embedded-crash-when-deleting-imported-json/57-RESEARCH.md
@.planning/phases/57-investigate-mdcad-embedded-crash-when-deleting-imported-json/57-01-SUMMARY.md
@samples/wpf-mdcad-control/Host/IMdCadEmbedBackend.cs
@samples/wpf-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs
@samples/wpf-mdcad-control/MdCadEmbeddedControl.xaml.cs
@samples/wpf-host/MainWindow.xaml.cs

<interfaces>
From samples/wpf-mdcad-control/Host/IMdCadEmbedBackend.cs:
`event Action<string>? UnexpectedSessionLoss;`

From samples/wpf-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs:
`public event Action<string>? UnexpectedSessionLoss;`
`private void CleanupAfterUnexpectedSessionLoss(string detail)`

From samples/wpf-mdcad-control/MdCadEmbeddedControl.xaml.cs:
`private void SetLaunchWarning(string? warningText)`

From samples/wpf-host/MainWindow.xaml.cs:
`private string BuildHarnessStatus()`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture truthful native failure detail in the WPF backend</name>
  <files>samples/wpf-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs, samples/wpf-mdcad-control.tests/WindowsMdCadEmbedBackendTests.cs</files>
  <behavior>
    - Test 1: startup failure still reports the captured native summary line.
    - Test 2: post-attach unexpected exit prefers the stable native fatal line/log path from 57-01 when present.
    - Test 3: if no fatal line was observed, the backend falls back to truthful exit/attach-state wording instead of inventing a crash cause.
  </behavior>
  <action>Update the WPF Windows backend to match the Phase 57 native stderr contract from 57-01, not just startup-only lines. Preserve the last relevant fatal summary across attach, then use it when `CleanupAfterUnexpectedSessionLoss` runs after attach loss, child-parent breakage, or timeout. Compose detail strictly from observed stderr text, known log-file location, and exit code so D-09 stays truthful. Reuse the existing stdout/stderr capture plus `UnexpectedSessionLoss` event per D-07; do not add a second channel or background probe. Extend backend tests for startup failure, post-attach fatal stderr, and generic exit fallback.</action>
  <verify>
    <automated>dotnet test .\samples\wpf-mdcad-control.tests\MdCad.Wpf.Control.Tests.csproj -c Release --filter "FullyQualifiedName~WindowsMdCadEmbedBackendTests"</automated>
  </verify>
  <done>The WPF backend now forwards truthful native failure detail for both startup and post-attach unexpected exit paths, and focused backend tests cover the behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Relay the same detail through the WPF control and full host</name>
  <files>samples/wpf-mdcad-control/MdCadEmbeddedControl.xaml.cs, samples/wpf-mdcad-control.tests/MdCadEmbeddedControlTests.cs, samples/wpf-host/MainWindow.xaml.cs</files>
  <behavior>
    - Test 1: the control warning surface stores and shows the unexpected-session-loss detail after attach loss.
    - Test 2: the control exposes a small host-facing relay seam so the full host can mirror the same text.
    - Test 3: an automated WPF host-level test injects relayed crash detail and proves the full host harness status uses that detail verbatim, while the minimal/sealed host path remains control-owned.
  </behavior>
  <action>Add the same public relay seam on the WPF control as the Avalonia control so both host stacks stay aligned. Keep `SetLaunchWarning` as the single control-owned warning source, preserve sealed/minimal behavior for D-05, and update `samples/wpf-host/MainWindow.xaml.cs` so `BuildHarnessStatus()` appends the exact observed crash/log detail coming from the control. If the current `MainWindow` composition is too private to test directly, extract the harness-status composition into a small internal helper/view-model seam and cover that seam with a dedicated `samples/wpf-host.tests` project instead of relying on build-only proof. Do not let the host invent a cause or override the control warning text; D-09 requires that the host only mirrors what the backend/control actually observed. Add control tests for the relay seam and warning persistence plus a host-level test for harness mirroring, then build the full WPF host.</action>
  <verify>
    <automated>dotnet test .\samples\wpf-mdcad-control.tests\MdCad.Wpf.Control.Tests.csproj -c Release --filter "FullyQualifiedName~MdCadEmbeddedControlTests" &amp;&amp; dotnet test .\samples\wpf-host.tests\WpfHost.Tests.csproj -c Release --filter "FullyQualifiedName~HarnessStatus|FullyQualifiedName~MainWindow" &amp;&amp; dotnet build .\samples\wpf-host\WpfHost.csproj -c Release</automated>
  </verify>
  <done>WPF minimal consumers continue using the control warning surface, the full WPF host mirrors the same observed detail in harness status, and control plus host behavior are both covered by automated proof instead of build-only confidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native stderr/process exit → WPF backend | Untrusted child-process output crosses into host diagnostics. |
| control warning state → full-host status text | Internal warning detail is re-exposed to the diagnostic host UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-07 | Repudiation | WPF backend failure text | mitigate | Only surface captured stderr/log-path/exit facts; tests must prove fallback wording never invents a cause. |
| T-57-08 | Information Disclosure | Full-host harness status | mitigate | Mirror only the same warning text already shown by the control and keep it scoped to local diagnostic UI. |
| T-57-09 | Tampering | backend → control relay | mitigate | Keep `SetLaunchWarning` as the single source of truth and relay the same string rather than recomputing a second message. |
| T-57-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are part of Phase 57. |
</threat_model>

<verification>
- `dotnet test .\samples\wpf-mdcad-control.tests\MdCad.Wpf.Control.Tests.csproj -c Release --filter "FullyQualifiedName~WindowsMdCadEmbedBackendTests|FullyQualifiedName~MdCadEmbeddedControlTests"`
- `dotnet test .\samples\wpf-host.tests\WpfHost.Tests.csproj -c Release --filter "FullyQualifiedName~HarnessStatus|FullyQualifiedName~MainWindow"`
- `dotnet build .\samples\wpf-host\WpfHost.csproj -c Release`
</verification>

<success_criteria>
- WPF backend unexpected-session-loss detail reuses the native stderr/log contract from 57-01.
- Sealed/minimal WPF consumers still rely on the control warning surface.
- The full WPF host mirrors the same observed detail without inventing state or causes.
</success_criteria>

<output>
Create `.planning/phases/57-investigate-mdcad-embedded-crash-when-deleting-imported-json/57-03-SUMMARY.md` when done
</output>
