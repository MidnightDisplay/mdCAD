---
phase: 57-investigate-mdcad-embedded-crash-when-deleting-imported-json
plan: 02
type: execute
wave: 2
depends_on:
  - 57-01
files_modified:
  - samples/avalonia-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs
  - samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs
  - samples/avalonia-mdcad-control.tests/WindowsMdCadEmbedBackendTests.cs
  - samples/avalonia-mdcad-control.tests/MdCadEmbeddedControlTests.cs
  - samples/avalonia-host/MainWindow.axaml.cs
  - samples/avalonia-host.tests/AvaloniaHost.Tests.csproj
  - samples/avalonia-host.tests/MainWindowHarnessStatusTests.cs
autonomous: true
requirements:
  - P57-04
  - P57-05
must_haves:
  truths:
    - "Avalonia hosts surface the native crash/log detail after unexpected embedded session loss without inventing a cause."
    - "The sealed Avalonia control still owns the minimal-host warning surface."
    - "The full Avalonia host mirrors the same observed detail in its harness status text."
  artifacts:
    - path: "samples/avalonia-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs"
      provides: "Unexpected-exit detail capture and composition from stderr plus exit state"
      contains: "UnexpectedSessionLoss"
    - path: "samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs"
      provides: "Control-owned warning relay plus host-facing notification seam"
      contains: "SetLaunchWarning"
    - path: "samples/avalonia-host/MainWindow.axaml.cs"
      provides: "Full-host mirror of observed crash/log detail"
      contains: "BuildHarnessStatus"
  key_links:
    - from: "samples/avalonia-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs"
      to: "samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs"
      via: "UnexpectedSessionLoss event"
      pattern: "UnexpectedSessionLoss"
    - from: "samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs"
      to: "samples/avalonia-host/MainWindow.axaml.cs"
      via: "public relay seam for observed warning detail"
      pattern: "UnexpectedSessionLoss"
---

<objective>
Carry the native diagnostics contract through the Avalonia backend, control, and full host without widening the embed architecture beyond the existing stderr/unexpected-session-loss seam from D-07.

Purpose: operators need the second half of D-03 in Avalonia once the native fix/log contract from 57-01 is in place.
Output: Avalonia backend/control/full-host crash-detail surfacing plus tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/57-investigate-mdcad-embedded-crash-when-deleting-imported-json/57-CONTEXT.md
@.planning/phases/57-investigate-mdcad-embedded-crash-when-deleting-imported-json/57-RESEARCH.md
@.planning/phases/57-investigate-mdcad-embedded-crash-when-deleting-imported-json/57-01-SUMMARY.md
@samples/avalonia-mdcad-control/Host/IMdCadEmbedBackend.cs
@samples/avalonia-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs
@samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs
@samples/avalonia-host/MainWindow.axaml.cs

<interfaces>
From samples/avalonia-mdcad-control/Host/IMdCadEmbedBackend.cs:
`event Action<string>? UnexpectedSessionLoss;`

From samples/avalonia-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs:
`public event Action<string>? UnexpectedSessionLoss;`
`private void CleanupAfterUnexpectedSessionLoss(string detail)`

From samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs:
`internal void SetLaunchWarning(string? warningText)`

From samples/avalonia-host/MainWindow.axaml.cs:
`private string BuildHarnessStatus()`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture truthful native failure detail in the Avalonia backend</name>
  <files>samples/avalonia-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs, samples/avalonia-mdcad-control.tests/WindowsMdCadEmbedBackendTests.cs</files>
  <behavior>
    - Test 1: startup failure still reports the captured native summary line.
    - Test 2: post-attach unexpected exit prefers the stable native fatal line/log path from 57-01 when present.
    - Test 3: if no fatal line was observed, the backend falls back to truthful exit/attach-state wording instead of inventing a crash cause.
  </behavior>
  <action>Extend the Avalonia Windows backend to capture the stable embedded failure summary from 57-01 instead of only `Embedded startup failed:` lines. Preserve the last relevant fatal line across attach, then use it when `CleanupAfterUnexpectedSessionLoss` runs after attach loss, child-parent breakage, or timeout. Compose detail from observed stderr text, known working-directory log file name, and exit code only when those facts exist so D-09 stays truthful. Reuse the existing process stdout/stderr capture plus `UnexpectedSessionLoss` event per D-07; do not add a new IPC channel, sidecar, or polling path. Add focused backend tests for startup failure, post-attach fatal stderr, and generic exit fallback.</action>
  <verify>
    <automated>dotnet test .\samples\avalonia-mdcad-control.tests\MdCad.Avalonia.Control.Tests.csproj -c Release --filter "FullyQualifiedName~WindowsMdCadEmbedBackendTests"</automated>
  </verify>
  <done>The Avalonia backend carries through the native failure detail from stderr/exit state without claiming unobserved root causes, and backend tests cover startup plus post-attach loss.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Relay the same detail through the Avalonia control and full host</name>
  <files>samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs, samples/avalonia-mdcad-control.tests/MdCadEmbeddedControlTests.cs, samples/avalonia-host/MainWindow.axaml.cs</files>
  <behavior>
    - Test 1: the control warning surface stores and shows the unexpected-session-loss detail after attach loss.
    - Test 2: the control exposes a small host-facing relay seam so the full host can mirror the same text.
    - Test 3: an automated Avalonia host-level test injects relayed crash detail and proves the full host harness status uses that detail verbatim, while the minimal/sealed host path remains control-owned.
  </behavior>
  <action>Add a small public relay seam on the Avalonia control (a read-only last-detail property plus an event or equivalent observable hook) so full hosts can mirror unexpected embedded loss without scraping internal UI state. Reuse `SetLaunchWarning` as the single control-owned source of truth, keep the sealed/minimal warning surface intact for D-05, and update `samples/avalonia-host/MainWindow.axaml.cs` so `BuildHarnessStatus()` appends the observed crash/log detail verbatim when the control reports it. If `BuildHarnessStatus()` is not directly testable, extract the composition into a small internal helper/view-model seam and cover that seam with a dedicated `samples/avalonia-host.tests` project rather than relying on build-only proof. Do not claim a crash reason unless the backend actually observed it, per D-09, and do not move warning ownership out of the control. Add control tests for the relay seam and warning persistence plus a host-level test for harness mirroring.</action>
  <verify>
    <automated>dotnet test .\samples\avalonia-mdcad-control.tests\MdCad.Avalonia.Control.Tests.csproj -c Release --filter "FullyQualifiedName~MdCadEmbeddedControlTests" &amp;&amp; dotnet test .\samples\avalonia-host.tests\AvaloniaHost.Tests.csproj -c Release --filter "FullyQualifiedName~HarnessStatus|FullyQualifiedName~MainWindow" &amp;&amp; dotnet build .\samples\avalonia-host\AvaloniaHost.csproj -c Release</automated>
  </verify>
  <done>Avalonia minimal consumers continue using the control warning surface, the full Avalonia host mirrors the same observed detail in harness status, and control plus host behavior are both covered by automated proof instead of build-only confidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native stderr/process exit → Avalonia backend | Untrusted child-process output crosses into host diagnostics. |
| control warning state → full-host status text | Internal warning detail is re-exposed to the diagnostic host UI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-57-04 | Repudiation | Avalonia backend failure text | mitigate | Only surface captured stderr/log-path/exit facts; tests must prove fallback wording never invents a cause. |
| T-57-05 | Information Disclosure | Full-host harness status | mitigate | Mirror only the same warning text already shown by the control and keep it scoped to local diagnostic UI. |
| T-57-06 | Tampering | backend → control relay | mitigate | Keep `SetLaunchWarning` as the single source of truth and relay the same string rather than recomputing a second message. |
| T-57-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are part of Phase 57. |
</threat_model>

<verification>
- `dotnet test .\samples\avalonia-mdcad-control.tests\MdCad.Avalonia.Control.Tests.csproj -c Release --filter "FullyQualifiedName~WindowsMdCadEmbedBackendTests|FullyQualifiedName~MdCadEmbeddedControlTests"`
- `dotnet test .\samples\avalonia-host.tests\AvaloniaHost.Tests.csproj -c Release --filter "FullyQualifiedName~HarnessStatus|FullyQualifiedName~MainWindow"`
- `dotnet build .\samples\avalonia-host\AvaloniaHost.csproj -c Release`
</verification>

<success_criteria>
- Avalonia backend unexpected-session-loss detail reuses the native stderr/log contract from 57-01.
- Sealed/minimal Avalonia consumers still rely on the control warning surface.
- The full Avalonia host mirrors the same observed detail without inventing state or causes.
</success_criteria>

<output>
Create `.planning/phases/57-investigate-mdcad-embedded-crash-when-deleting-imported-json/57-02-SUMMARY.md` when done
</output>
