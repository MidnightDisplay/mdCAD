---
phase: 21-traceability-closure-and-re-audit-readiness
plan: 04
type: execute
wave: 4
depends_on: [21-03]
files_modified:
  - src/ui/ui_entity_inspector.h
  - src/app.c
  - src/tests/script_roundtrip_tests.c
autonomous: true
requirements: [SKCH-01]
gap_closure: true
must_haves:
  truths:
    - "Adding sketch geometry from GeometryManager updates Scene Hierarchy immediately without waiting for later manipulation."
    - "Both sketch-geometry entrypoints (Add Entity and GeometryManager) now trigger identical hierarchy-refresh behavior."
  artifacts:
    - path: "src/ui/ui_entity_inspector.h"
      provides: "Inspector-side mutation hook that can invalidate Scene Hierarchy cache on sketch geometry changes"
      contains: "callback field/setter and invocation on sketch geometry mutations"
    - path: "src/app.c"
      provides: "Runtime wiring from inspector mutation hook to hierarchy dirty-mark function"
      contains: "ui_entity_inspector_* callback registration using ui_scene_hierarchy_mark_dirty"
  key_links:
    - from: "src/ui/ui_entity_inspector.h"
      to: "src/ui/ui_scene_hierarchy.h"
      via: "callback registered in app state"
      pattern: "ui_scene_hierarchy_mark_dirty"
---

<objective>
Fix the SKCH-01 product defect causing delayed Scene Hierarchy refresh after GeometryManager-based sketch geometry add.

Purpose: Close the real runtime blocker captured in `21-VERIFICATION.md` instead of masking it in docs.
Output: Inspector mutation events correctly invalidate hierarchy cache through app wiring.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-traceability-closure-and-re-audit-readiness/21-CONTEXT.md
@.planning/phases/21-traceability-closure-and-re-audit-readiness/21-VERIFICATION.md
@.planning/phases/10-sketch-foundations-managers/10-HUMAN-UAT.md
@src/ui/ui_entity_inspector.h
@src/ui/ui_scene_hierarchy.h
@src/app.c

<interfaces>
From `src/ui/ui_scene_hierarchy.h`:
```c
static inline void ui_scene_hierarchy_mark_dirty(ui_scene_hierarchy_state_t *state);
```

From `src/ui/ui_entity_inspector.h`:
```c
typedef struct {
    // ...
    selection_buffer_t *selection;
    ecs_world_state_t *world;
    undo_redo_t *undo_redo;
} ui_entity_inspector_state_t;

static inline void ui_entity_inspector_init(ui_entity_inspector_state_t *state,
                                             selection_buffer_t *selection,
                                             ecs_world_state_t *world);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add inspector-to-hierarchy dirty callback contract</name>
  <files>src/ui/ui_entity_inspector.h, src/app.c, src/tests/script_roundtrip_tests.c</files>
  <action>Add a callback hook in `ui_entity_inspector_state_t` for scene-tree invalidation and expose a setter (e.g., callback + user-data). Wire it in `app.c` so inspector-side sketch geometry mutations can call `ui_scene_hierarchy_mark_dirty` through this hook. Keep existing inspector APIs backward-compatible by updating in-repo callsites (`script_roundtrip_tests.c`) with null/default callback setup. This implements D-01/D-03 by preparing product-code closure rather than document-only edits.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD script_roundtrip_tests</automated>
  </verify>
  <done>Inspector state can notify the app when hierarchy cache must be refreshed, and build/tests compile with updated signatures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Invoke dirty callback on GeometryManager sketch mutations</name>
  <files>src/ui/ui_entity_inspector.h</files>
  <behavior>
    - GeometryManager `Add Point/Line/Arc/Circle` mutation path triggers inspector dirty callback in same frame.
    - GeometryManager bulk `Fix/Unfix/Delete` mutation path triggers inspector dirty callback when mutation succeeds.
    - Non-mutating selection clicks do not trigger the callback.
  </behavior>
  <action>Call the new dirty callback from every successful sketch-geometry mutation path inside `ui_entity_inspector_draw_sketch_geometry_manager` (at minimum: Add Point, Add Line, Add Arc/Circle, bulk Fix/Unfix/Delete). Keep callback guarded (null-safe) and avoid unconditional calls on non-mutating UI events. This directly addresses the SKCH-01 dual-entrypoint refresh gap from 21-VERIFICATION.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD && ctest -R "script_roundtrip_tests" --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>GeometryManager mutation paths invalidate Scene Hierarchy immediately via app wiring, removing delayed-refresh behavior in product code.</done>
</task>

</tasks>

<verification>
- `cmake --build build-vulkan --config Release --target mdCAD script_roundtrip_tests`
- `ctest -R "script_roundtrip_tests" --test-dir build-vulkan -C Release --output-on-failure`
</verification>

<success_criteria>
- SKCH-01 blocker has a concrete product-code fix path (not docs-only).
- Inspector and hierarchy now have explicit mutation invalidation wiring.
- Code compiles and targeted tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/21-traceability-closure-and-re-audit-readiness/21-04-SUMMARY.md`
</output>
