---
phase: 14-script-io-api-undo-integration
plan: 03
type: execute
wave: 3
depends_on:
  - 14-01
  - 14-02
files_modified:
  - src/ui/ui_entity_inspector.h
  - src/app.c
  - src/tests/script_roundtrip_tests.c
autonomous: false
requirements:
  - SCRP-05
  - SCRP-04
  - API-02
must_haves:
  truths:
    - "User can open a dedicated Script IO window from SketchManager beside Open Script Editor."
    - "Inputs are editable numeric controls and outputs are read-only values with immediate sync."
    - "Each IO edit auto-applies transactionally and failed edits roll back with diagnostics."
  artifacts:
    - path: "src/ui/ui_entity_inspector.h"
      provides: "SketchManager IO window open/toggle request wiring"
      contains: "Script IO"
    - path: "src/app.c"
      provides: "Dedicated Script IO window state + draw lifecycle"
      contains: "mdcad_draw_script_io_window"
    - path: "src/tests/script_roundtrip_tests.c"
      provides: "Regression tests for IO window request path and live apply contract hooks"
      contains: "script_io"
  key_links:
    - from: "src/ui/ui_entity_inspector.h"
      to: "src/app.c"
      via: "toggle request consumed by app loop to open IO window"
      pattern: "consume_.*script_io"
    - from: "src/app.c"
      to: "src/ecs/ecs_scene.h"
      via: "IO input edit calls scene_script_io apply façade"
      pattern: "scene_script_io_.*apply"
    - from: "src/app.c"
      to: "src/undo_redo_exec.h"
      via: "IO auto-apply results in one undo entry per committed edit through shared pipeline"
      pattern: "undo_redo_(undo|redo)|scene_script_apply_commit"
---

<objective>
Ship user-facing Script IO window UX with live transactional auto-apply and diagnostics while preserving shared scene API and undo safety.

Purpose: Complete SCRP-05 user interaction surface and verify SCRP-04/API-02 behavior in interactive flow.
Output: Inspector toggle, dedicated IO window in app loop, automated + human verification evidence.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-script-io-api-undo-integration/14-CONTEXT.md
@.planning/phases/14-script-io-api-undo-integration/14-RESEARCH.md
@.planning/phases/14-script-io-api-undo-integration/14-01-SUMMARY.md
@.planning/phases/14-script-io-api-undo-integration/14-02-SUMMARY.md
@src/ui/ui_entity_inspector.h
@src/app.c

<interfaces>
Expected from Plan 14-02 (`src/ecs/ecs_scene.h`):
```c
// names may vary; use exact exported symbols from 14-02 implementation
scene_script_io_*  // descriptor/read/apply façade methods
```

Existing from `src/ui/ui_entity_inspector.h`:
```c
static inline void ui_entity_inspector_request_script_editor(...);
static inline bool ui_entity_inspector_consume_script_editor_open_request(...);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add inspector/app-level tests for IO window toggle and live apply contracts (per D-07, D-08, D-09)</name>
  <read_first>@src/tests/script_roundtrip_tests.c, @src/ui/ui_entity_inspector.h, @src/app.c, @.planning/phases/14-script-io-api-undo-integration/14-CONTEXT.md</read_first>
  <files>src/tests/script_roundtrip_tests.c</files>
  <acceptance_criteria>
    - `git --no-pager grep -n "test_script_io_window_request_is_exposed_from_inspector_state" src/tests/script_roundtrip_tests.c` returns one test.
    - `git --no-pager grep -n "test_script_io_live_edit_uses_transaction_pipeline" src/tests/script_roundtrip_tests.c` returns one test.
    - `ctest --test-dir build-vulkan -C Release -R script_roundtrip_tests --output-on-failure` fails before Task 2, then passes after Task 2.
  </acceptance_criteria>
  <action>Write failing regression tests for new Script IO open-request path in inspector state (mirroring existing Script Editor request pattern) and for live IO edit behavior invoking the shared transactional pipeline with rollback on failure (D-09). Keep tests deterministic and non-visual; validate state transitions/scene contracts instead of pixel checks.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R script_roundtrip_tests --output-on-failure</automated>
  </verify>
  <done>Automated tests capture IO launch wiring and live transactional semantics before UI implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement dedicated Script IO window + SketchManager toggle with transactional auto-apply (per D-07, D-08, D-09)</name>
  <read_first>@src/ui/ui_entity_inspector.h, @src/app.c, @src/ecs/ecs_scene.h, @src/tests/script_roundtrip_tests.c</read_first>
  <files>src/ui/ui_entity_inspector.h, src/app.c, src/tests/script_roundtrip_tests.c</files>
  <acceptance_criteria>
    - `git --no-pager grep -n "Open Script IO" src/ui/ui_entity_inspector.h` shows dedicated toggle near Script Editor button.
    - `git --no-pager grep -n "mdcad_draw_script_io_window\\|Script IO" src/app.c` shows dedicated IO window lifecycle.
    - `ctest --test-dir build-vulkan -C Release -R script_roundtrip_tests --output-on-failure` passes.
  </acceptance_criteria>
  <action>Add Script IO request/consume state to inspector and render a dedicated Script IO window in `app.c` (not embedded in Script Editor) opened from SketchManager next to `Open Script Editor` (D-07). In window, render editable numeric inputs and read-only outputs with immediate sync (D-08). On every input edit, call scene IO apply façade from 14-02 so each edit uses validate->atomic apply->rollback semantics with diagnostics on failure (D-09, D-03). Keep non-numeric widgets absent (D-06 deferred) and preserve existing Script Editor behavior.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R script_roundtrip_tests --output-on-failure</automated>
  </verify>
  <done>User can launch and use dedicated Script IO window with transactional live edits and read-only outputs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify Script IO UX and transactional undo feel (SCRP-05, SCRP-04, API-02)</name>
  <read_first>@src/app.c, @src/ui/ui_entity_inspector.h, @.planning/phases/14-script-io-api-undo-integration/14-CONTEXT.md, @.planning/phases/14-script-io-api-undo-integration/14-VALIDATION.md</read_first>
  <files>N/A (checkpoint verification only; validates Task 1/2 outputs)</files>
  <acceptance_criteria>
    - IO window opens from SketchManager button beside Script Editor.
    - Input controls are editable and outputs are read-only.
    - Invalid input edit shows diagnostics and restores prior valid state.
    - Undo/redo after valid IO edit behaves as one-step transactional action.
  </acceptance_criteria>
  <action>After automation is complete, run app and verify end-user behavior for dedicated IO UX and rollback semantics per D-07..D-09, D-12..D-14. Capture pass/fail notes and concrete repro if any mismatch is observed.</action>
  <how-to-verify>
    1) Build + launch app.
    2) Open a sketch in Entity Inspector; click `Open Script IO` beside `Open Script Editor`.
    3) Confirm window is separate/dedicated and lists editable numeric inputs + read-only outputs.
    4) Change an input with valid value; confirm scene/script/output refresh immediately.
    5) Enter an invalid value that violates parser/validation; confirm explicit diagnostic and no committed scene corruption.
    6) Perform one valid IO edit, press undo once (Ctrl+Z) and redo once (Ctrl+Shift+Z); confirm full-state restore/apply each in single step.
  </how-to-verify>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>Human confirms Script IO UX/rollback/undo behavior matches D-07..D-09 and D-12..D-14, or returns concrete defects for rework.</done>
  <resume-signal>Type "approved" if all checks pass, or provide failing step(s) with observed behavior.</resume-signal>
</task>

</tasks>

<verification>
- Automated gate: `ctest --test-dir build-vulkan -C Release -R script_roundtrip_tests --output-on-failure`
- Full suite before plan close: `ctest --test-dir build-vulkan -C Release --output-on-failure`
- Human gate: transactional IO UX checklist in Task 3
</verification>

<success_criteria>
- SCRP-05: dedicated Script IO window is usable, numeric-only, and metadata-aware.
- SCRP-04/API-02: live IO edits preserve rollback safety and one-step undo/redo behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/14-script-io-api-undo-integration/14-03-SUMMARY.md`
</output>

