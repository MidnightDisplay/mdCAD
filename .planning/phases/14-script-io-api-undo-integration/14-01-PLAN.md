---
phase: 14-script-io-api-undo-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/undo_redo.h
  - src/undo_redo_exec.h
  - src/ecs/ecs_scene.h
  - src/app.c
  - src/tests/script_roundtrip_tests.c
autonomous: true
requirements:
  - SCRP-04
  - API-02
must_haves:
  truths:
    - "Failed script apply leaves the sketch at last-valid committed state."
    - "One successful Apply Script operation is undone/redone in exactly one step."
    - "Undo after script apply restores geometry, constraints, and solver-impacting sketch state together."
  artifacts:
    - path: "src/undo_redo.h"
      provides: "Explicit undo command type for script transactions"
      contains: "CMD_SCRIPT_APPLY_TRANSACTION"
    - path: "src/undo_redo_exec.h"
      provides: "Apply/unapply handlers for script transaction snapshots"
      contains: "undo_cmd_script_apply_transaction"
    - path: "src/ecs/ecs_scene.h"
      provides: "Scene-level script apply entrypoint that can push one undo transaction"
      contains: "scene_script_apply_commit"
  key_links:
    - from: "src/app.c"
      to: "src/ecs/ecs_scene.h"
      via: "Apply Script button calls one scene façade apply function"
      pattern: "scene_script_apply_commit"
    - from: "src/ecs/ecs_scene.h"
      to: "src/undo_redo_exec.h"
      via: "successful commit records exactly one undo command"
      pattern: "undo_.*script"
    - from: "src/tests/script_roundtrip_tests.c"
      to: "src/undo_redo_exec.h"
      via: "tests prove one-step undo/redo and full-state restore"
      pattern: "undo_redo_(undo|redo)"
---

<objective>
Add transactional undo/redo integration to script apply so all script-driven mutations are atomic and recoverable.

Purpose: Satisfy strict apply safety and one-apply-one-undo behavior for SCRP-04/API-02.
Output: Script transaction undo command wiring, scene façade integration, and regression tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-script-io-api-undo-integration/14-CONTEXT.md
@.planning/phases/14-script-io-api-undo-integration/14-RESEARCH.md
@.planning/phases/13-script-round-trip-baseline/13-03-SUMMARY.md
@src/undo_redo.h
@src/undo_redo_exec.h
@src/ecs/ecs_scene.h
@src/tests/script_roundtrip_tests.c

<interfaces>
From `src/ecs/ecs_scene.h`:
```c
static inline bool scene_script_preview_parse(ecs_scene_t *scene, ecs_entity_t sketch,
                                              const char *script_text, sketch_script_error_t *out_error);
static inline bool scene_script_apply_commit(ecs_scene_t *scene, ecs_entity_t sketch,
                                             const char *script_text, sketch_script_error_t *out_error);
```

From `src/undo_redo_exec.h`:
```c
bool undo_redo_undo(undo_redo_t *ur);
bool undo_redo_redo(undo_redo_t *ur);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing transaction tests for script apply atomic undo/redo (per D-01, D-12, D-13)</name>
  <read_first>@src/tests/script_roundtrip_tests.c, @src/undo_redo.h, @src/undo_redo_exec.h, @.planning/phases/14-script-io-api-undo-integration/14-CONTEXT.md</read_first>
  <files>src/tests/script_roundtrip_tests.c</files>
  <acceptance_criteria>
    - `git --no-pager grep -n "test_script_apply_undo_redo_single_step" src/tests/script_roundtrip_tests.c` returns one test function.
    - `git --no-pager grep -n "test_script_apply_failure_preserves_last_valid_state" src/tests/script_roundtrip_tests.c` returns one test function.
    - Test run initially fails before implementation, then passes after Task 2.
  </acceptance_criteria>
  <action>Add two explicit regression tests that lock API-02 behavior: (1) successful script apply pushes exactly one undo command and one redo command; (2) failed parse/apply after a valid commit preserves full prior sketch state (geometry/constraints counts and key IDs). Use existing `scene_script_apply_commit`, `undo_redo_undo`, and `undo_redo_redo` entrypoints; do not assert via UI text.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R script_roundtrip_tests --output-on-failure</automated>
  </verify>
  <done>Script transaction behavior is codified in failing-then-passing automated tests for SCRP-04 and API-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement script-transaction undo command and wire Apply Script to single-step undo (per D-02, D-12, D-13)</name>
  <read_first>@src/undo_redo.h, @src/undo_redo_exec.h, @src/ecs/ecs_scene.h, @src/app.c, @src/scripting/sketch_script_apply.h</read_first>
  <files>src/undo_redo.h, src/undo_redo_exec.h, src/ecs/ecs_scene.h, src/app.c, src/tests/script_roundtrip_tests.c</files>
  <acceptance_criteria>
    - `git --no-pager grep -n "CMD_SCRIPT_APPLY_TRANSACTION" src/undo_redo.h src/undo_redo_exec.h` finds enum + handlers.
    - `git --no-pager grep -n "scene_script_apply_commit" src/app.c` shows Apply Script still routed through scene façade only.
    - `ctest --test-dir build-vulkan -C Release -R script_roundtrip_tests --output-on-failure` passes.
  </acceptance_criteria>
  <action>Introduce one dedicated undo command for script apply transaction snapshots in undo system and execution logic; snapshot full sketch subtree before apply and restored state for redo/unapply semantics so one successful apply equals one undo unit (D-12). Keep validation/apply failure behavior transactional and diagnostic-preserving for both editor and future IO callers (D-01, D-02, D-03, D-13). Maintain UI as thin caller through `scene_script_apply_commit` in `app.c` (D-10), not direct undo stack manipulation in panel code.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R script_roundtrip_tests --output-on-failure && ctest --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>Apply Script commits are atomic, undoable/redoable in one step, and failure paths restore full last-valid state.</done>
</task>

</tasks>

<verification>
- Targeted regression: `ctest --test-dir build-vulkan -C Release -R script_roundtrip_tests --output-on-failure`
- Wave gate: `ctest --test-dir build-vulkan -C Release --output-on-failure`
</verification>

<success_criteria>
- SCRP-04: failed apply cannot corrupt committed sketch state.
- API-02: one successful script apply equals one undo/redo transaction.
</success_criteria>

<output>
After completion, create `.planning/phases/14-script-io-api-undo-integration/14-01-SUMMARY.md`
</output>

