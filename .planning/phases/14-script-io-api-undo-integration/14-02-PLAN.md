---
phase: 14-script-io-api-undo-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - src/scripting/sketch_script_parse.h
  - src/scripting/sketch_script_emit.h
  - src/scripting/sketch_script_apply.h
  - src/ecs/ecs_scene.h
  - src/tests/script_roundtrip_tests.c
autonomous: true
requirements:
  - SCRP-05
  - API-01
must_haves:
  truths:
    - "Script definitions can declare numeric inputs and outputs."
    - "Optional min/max/step metadata drives slider-capable numeric input behavior."
    - "Script IO apply path uses scene-level APIs rather than direct UI/ECS mutation."
  artifacts:
    - path: "src/scripting/sketch_script_parse.h"
      provides: "Parser model for numeric-only inputs/outputs schema"
      contains: "inputs"
    - path: "src/scripting/sketch_script_emit.h"
      provides: "Deterministic emit support for IO declarations"
      contains: "outputs"
    - path: "src/ecs/ecs_scene.h"
      provides: "Scene façade entrypoints for IO read/update/apply workflow"
      exports: ["scene_script_io_*"]
  key_links:
    - from: "src/ecs/ecs_scene.h"
      to: "src/scripting/sketch_script_apply.h"
      via: "IO edit path reuses transactional preview/commit pipeline"
      pattern: "scene_script_io_.*apply|scene_script_apply_commit"
    - from: "src/scripting/sketch_script_parse.h"
      to: "src/scripting/sketch_script_emit.h"
      via: "same IO model fields round-trip parse->emit deterministically"
      pattern: "input|output|min|max|step"
    - from: "src/tests/script_roundtrip_tests.c"
      to: "src/ecs/ecs_scene.h"
      via: "tests invoke only façade API for script IO behavior"
      pattern: "scene_script_io_"
---

<objective>
Implement numeric script IO contracts and scene-level API entrypoints that power dynamic controls without breaking deterministic scripting behavior.

Purpose: Deliver SCRP-05 IO capability and API-01 façade coherence before UI wiring.
Output: IO schema parse/emit/apply support, scene façade methods, and tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/14-script-io-api-undo-integration/14-CONTEXT.md
@.planning/phases/14-script-io-api-undo-integration/14-RESEARCH.md
@.planning/phases/14-script-io-api-undo-integration/14-01-SUMMARY.md
@src/scripting/sketch_script_parse.h
@src/scripting/sketch_script_emit.h
@src/scripting/sketch_script_apply.h
@src/ecs/ecs_scene.h

<interfaces>
From `src/scripting/sketch_script_contract.h`:
```c
typedef struct {
    int line;
    int column;
    char message[192];
} sketch_script_error_t;
```

From existing façade in `src/ecs/ecs_scene.h`:
```c
static inline bool scene_script_preview_parse(...);
static inline bool scene_script_apply_commit(...);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tests for numeric IO parse/emit/apply contracts (per D-04, D-05, D-06)</name>
  <read_first>@src/tests/script_roundtrip_tests.c, @src/scripting/sketch_script_parse.h, @src/scripting/sketch_script_emit.h, @.planning/phases/14-script-io-api-undo-integration/14-CONTEXT.md</read_first>
  <files>src/tests/script_roundtrip_tests.c</files>
  <acceptance_criteria>
    - `git --no-pager grep -n "test_script_io_numeric_schema_roundtrip" src/tests/script_roundtrip_tests.c` returns one test.
    - `git --no-pager grep -n "test_script_io_rejects_non_numeric" src/tests/script_roundtrip_tests.c` returns one test.
    - `ctest --test-dir build-vulkan -C Release -R script_roundtrip_tests --output-on-failure` captures RED then GREEN over this plan.
  </acceptance_criteria>
  <action>Author failing tests that assert: parser accepts numeric-only `inputs`/`outputs`; `min/max/step` metadata survives parse+emit deterministically; non-numeric IO declarations are rejected as out-of-scope (D-06). Include one test that calls new scene IO façade update API and verifies it routes through transactional apply semantics rather than direct mutation assumptions.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R script_roundtrip_tests --output-on-failure</automated>
  </verify>
  <done>IO contract expectations are executable and fail until implementation lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement numeric IO schema + scene façade APIs for script workflows (per D-04, D-05, D-09, D-10, D-11)</name>
  <read_first>@src/scripting/sketch_script_parse.h, @src/scripting/sketch_script_emit.h, @src/scripting/sketch_script_apply.h, @src/ecs/ecs_scene.h, @src/tests/script_roundtrip_tests.c</read_first>
  <files>src/scripting/sketch_script_parse.h, src/scripting/sketch_script_emit.h, src/scripting/sketch_script_apply.h, src/ecs/ecs_scene.h, src/tests/script_roundtrip_tests.c</files>
  <acceptance_criteria>
    - `git --no-pager grep -n "scene_script_io_" src/ecs/ecs_scene.h` finds dedicated IO façade functions.
    - `git --no-pager grep -n "min\\|max\\|step" src/scripting/sketch_script_parse.h src/scripting/sketch_script_emit.h` shows IO metadata handling.
    - `ctest --test-dir build-vulkan -C Release -R script_roundtrip_tests --output-on-failure` passes.
  </acceptance_criteria>
  <action>Extend script model/parser/emitter to support numeric-only `inputs` + `outputs` declarations (D-04) with optional `min/max/step` metadata preserved in deterministic text output (D-05). Add scene-level script IO entrypoints in `ecs_scene.h` (D-10, D-11) for: enumerate IO descriptors, read current IO state, and apply one input edit through the same validate->atomic-apply path as manual script apply (D-09). Keep non-numeric IO unsupported with explicit diagnostics (D-03, D-06). Ensure this API layer becomes canonical for upcoming UI window integration (API-01).</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R script_roundtrip_tests --output-on-failure && ctest --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>Scene API now exposes script IO workflows with deterministic numeric IO round-trip behavior and shared transactional apply semantics.</done>
</task>

</tasks>

<verification>
- Targeted suite: `ctest --test-dir build-vulkan -C Release -R script_roundtrip_tests --output-on-failure`
- Full wave check: `ctest --test-dir build-vulkan -C Release --output-on-failure`
</verification>

<success_criteria>
- SCRP-05: numeric IO declarations and metadata are parsed/emitted and operable via scene API.
- API-01: script IO flows are callable through scene façade entrypoints, not panel-specific mutation logic.
</success_criteria>

<output>
After completion, create `.planning/phases/14-script-io-api-undo-integration/14-02-SUMMARY.md`
</output>

