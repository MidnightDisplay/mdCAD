---
phase: 39-automatic-observer-safety-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tests/jsonl_flat_observer_auto_safety_test.c
  - src/tests/jsonl_flat_observer_inspector_contract_test.c
  - src/CMakeLists.txt
autonomous: true
requirements: [OBSF-04]
must_haves:
  truths:
    - "Wave 0 auto-safety test coverage exists and is runnable in CTest before runtime changes."
    - "Flat inspector contract enforces a separate Observe toggle and safety-status copy requirements."
  artifacts:
    - path: "src/tests/jsonl_flat_observer_auto_safety_test.c"
      provides: "Dedicated Phase 39 auto safety-loop test harness"
      contains: "test_flat_auto_"
    - path: "src/CMakeLists.txt"
      provides: "Build/CTest registration for jsonl_flat_observer_auto_safety_test"
      contains: "jsonl_flat_observer_auto_safety_test"
    - path: "src/tests/jsonl_flat_observer_inspector_contract_test.c"
      provides: "Inspector contract checks for Observe control and safety status"
      contains: "Observe automatically"
  key_links:
    - from: "src/CMakeLists.txt"
      to: "src/tests/jsonl_flat_observer_auto_safety_test.c"
      via: "add_executable/add_test wiring"
      pattern: "add_test\\(NAME jsonl_flat_observer_auto_safety_test"
    - from: "src/tests/jsonl_flat_observer_inspector_contract_test.c"
      to: "src/ui/ui_entity_inspector.h"
      via: "required-string contract assertions"
      pattern: "contains_required\\(src, \"Observe automatically"
---

<objective>
Establish Wave 0 safety-loop test infrastructure and tighten inspector contract guards before implementing runtime behavior.

Purpose: Prevent Phase 39 runtime work from shipping without executable OBSF-04 regression coverage.
Output: New auto-safety test target + updated inspector contract assertions.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-automatic-observer-safety-loop/39-CONTEXT.md
@.planning/phases/39-automatic-observer-safety-loop/39-RESEARCH.md
@.planning/phases/39-automatic-observer-safety-loop/39-VALIDATION.md
@src/tests/jsonl_flat_observer_manual_refresh_test.c
@src/tests/jsonl_flat_observer_inspector_contract_test.c
@src/CMakeLists.txt
</context>

<tasks>

<task type="auto">
  <name>Task 0: Add Wave 0 auto-safety CTest scaffold for OBSF-04</name>
  <read_first>
    - .planning/phases/39-automatic-observer-safety-loop/39-VALIDATION.md
    - src/tests/jsonl_flat_observer_manual_refresh_test.c
    - src/CMakeLists.txt
  </read_first>
  <files>src/tests/jsonl_flat_observer_auto_safety_test.c, src/CMakeLists.txt</files>
  <action>Create `jsonl_flat_observer_auto_safety_test.c` with reusable fixture helpers and initial auto-loop test harness entry points, then wire `add_executable(...)` + `add_test(...)` for `jsonl_flat_observer_auto_safety_test` in `src/CMakeLists.txt` (Wave 0 gap from validation, per OBSF-04). Keep the test binary deterministic and non-interactive.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target jsonl_flat_observer_auto_safety_test</automated>
  </verify>
  <acceptance_criteria>CTest target exists, builds cleanly, and can be selected by regex `jsonl_flat_observer_auto_safety_test`.</acceptance_criteria>
  <done>Wave 0 gap is closed: repo contains a dedicated auto-safety test file and CTest registration.</done>
</task>

<task type="auto">
  <name>Task 1: Extend flat inspector contract tests for explicit observe safety controls</name>
  <read_first>
    - .planning/phases/39-automatic-observer-safety-loop/39-CONTEXT.md
    - src/tests/jsonl_flat_observer_inspector_contract_test.c
    - src/ui/ui_entity_inspector.h
  </read_first>
  <files>src/tests/jsonl_flat_observer_inspector_contract_test.c</files>
  <action>Add required-string contract assertions for: separate `Observe automatically` control (D-03), warning/status surface for invalid source while observe is ON (D-04), and persistent `Re-import now` control visibility even when auto-observe is disabled (D-06). Keep existing flat inspector minimum-surface assertions intact.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_inspector_contract_test" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>Inspector contract test fails if observe toggle or required safety/manual controls are removed from flat inspector source.</acceptance_criteria>
  <done>Executable UI contract now protects D-03/D-04/D-06 behavior boundaries needed for OBSF-04.</done>
</task>

</tasks>

<verification>
Run:
- `ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_(inspector_contract_test|auto_safety_test)" --output-on-failure`
</verification>

<success_criteria>
- Wave 0 test gap from 39-VALIDATION is resolved with a real CTest target.
- Flat inspector contract tests lock explicit Observe controls and safety/manual visibility rules.
</success_criteria>

<output>
After completion, create `.planning/phases/39-automatic-observer-safety-loop/39-01-SUMMARY.md`
</output>
