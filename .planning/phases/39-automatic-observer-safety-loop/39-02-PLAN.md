---
phase: 39-automatic-observer-safety-loop
plan: 02
type: execute
wave: 2
depends_on: [39-01]
files_modified:
  - src/jsonl_observer_system.h
  - src/ui/ui_entity_inspector.h
  - src/tests/jsonl_flat_observer_auto_safety_test.c
  - src/tests/jsonl_flat_observer_manual_refresh_test.c
autonomous: true
requirements: [OBSF-04]
must_haves:
  truths:
    - "When flat root link is active and source path exists, automatic observe starts and schedules refresh attempts."
    - "Turning Link OFF stops automatic observing immediately while preserving the stored source path."
    - "Observe ON with missing/invalid source stays idle with warning and does not consume retry budget."
    - "Unstable write windows use debounce/retry/auto-disable safety behavior and still allow manual Re-import."
  artifacts:
    - path: "src/jsonl_observer_system.h"
      provides: "Flat-root automatic safety tick and scheduling"
      exports: ["jsonl_observer_system_tick", "jsonl_observer_request_flat_refresh", "jsonl_observer_tick_flat_refreshes"]
    - path: "src/ui/ui_entity_inspector.h"
      provides: "Flat observer link/observe controls and safety status UI"
      contains: "Observe automatically##jsonl_flat_root_observe_auto"
    - path: "src/tests/jsonl_flat_observer_auto_safety_test.c"
      provides: "OBSF-04 behavior assertions for debounce/retry/auto-disable/idle warning"
      contains: "auto-disabled"
    - path: "src/tests/jsonl_flat_observer_manual_refresh_test.c"
      provides: "Manual refresh availability regression after auto-disable"
      contains: "Re-import"
  key_links:
    - from: "src/jsonl_observer_system.h"
      to: "src/jsonl_sketch_import_job.h"
      via: "jsonl_observer_source_changed/jsonl_observer_stamp_source_state"
      pattern: "jsonl_observer_source_(changed|stamp_source_state)"
    - from: "src/jsonl_observer_system.h"
      to: "src/jsonl_import_job.h"
      via: "flat refresh scheduling through existing transactional slot path"
      pattern: "jsonl_observer_request_flat_refresh"
    - from: "src/ui/ui_entity_inspector.h"
      to: "src/components/jsonl_observer_comp.h"
      via: "linked/observe_enabled toggles"
      pattern: "observer->(linked|observe_enabled)"
---

<objective>
Implement the Phase 39 flat-root automatic observer safety loop with explicit controls and regression coverage.

Purpose: Deliver OBSF-04 safely without breaking Phase 38 transactional/manual guarantees.
Output: Runtime auto-loop policy, inspector control behavior, and passing OBSF-04 tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-automatic-observer-safety-loop/39-CONTEXT.md
@.planning/phases/39-automatic-observer-safety-loop/39-RESEARCH.md
@.planning/phases/39-automatic-observer-safety-loop/39-VALIDATION.md
@.planning/phases/39-automatic-observer-safety-loop/39-01-SUMMARY.md
@src/jsonl_observer_system.h
@src/ui/ui_entity_inspector.h
@src/components/jsonl_observer_comp.h
@src/tests/jsonl_flat_observer_auto_safety_test.c
@src/tests/jsonl_flat_observer_manual_refresh_test.c

<interfaces>
From `src/components/jsonl_observer_comp.h`:
```c
typedef struct {
    bool linked;
    bool observe_enabled;
    uint32_t interval_ms;
    uint32_t max_retries;
    uint32_t retry_count;
    uint64_t next_retry_at_ms;
    char source_path[JSONL_OBSERVER_PATH_MAX];
    bool source_state_valid;
    uint64_t last_source_size_bytes;
    uint64_t last_source_mtime_unix_ns;
    uint64_t last_source_hash;
} JsonlObserverComp;
```

From `src/jsonl_observer_system.h`:
```c
bool jsonl_observer_request_flat_refresh(ecs_scene_t *scene, ecs_entity_t root_entity, selection_buffer_t *selection);
bool jsonl_observer_is_flat_refresh_running(ecs_scene_t *scene, ecs_entity_t root_entity);
void jsonl_observer_tick_flat_refreshes(ecs_scene_t *scene);
void jsonl_observer_system_tick(ecs_scene_t *scene, uint64_t now_ms);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement flat auto observer tick policy and scheduling gates</name>
  <read_first>
    - .planning/phases/39-automatic-observer-safety-loop/39-CONTEXT.md
    - .planning/phases/39-automatic-observer-safety-loop/39-RESEARCH.md
    - src/jsonl_observer_system.h
    - src/components/jsonl_observer_comp.h
  </read_first>
  <files>src/jsonl_observer_system.h</files>
  <behavior>
    - Test: linked flat root with observe enabled and changed source requests one transactional flat refresh.
    - Test: missing/invalid source with observe enabled stays idle, emits warning, and does not increment retry_count.
    - Test: source check failures use interval debounce + retry_count increments and auto-disable at max_retries.
  </behavior>
  <action>Add `jsonl_observer_tick_one_flat(...)` and update `jsonl_observer_system_tick(...)` to process non-sketch roots with `JsonlObserverComp` (D-07). Enforce D-01 by enabling auto schedule when linked + valid path exist, D-04 by treating empty/invalid paths as idle warning (no retry budget burn), and D-05 by mirroring sketch debounce/retry/auto-disable thresholds and message behavior. Route all refresh starts through `jsonl_observer_request_flat_refresh(...)` only, guard with `jsonl_observer_is_flat_refresh_running(...)`, and stamp source state after successful flat commit to avoid repeated false positives.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_auto_safety_test" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>Flat roots (non-sketch) run automatic safety loop; sketch observer path remains intact; retry/disable behavior matches Phase 35 policy semantics.</acceptance_criteria>
  <done>Runtime implements OBSF-04 safety loop behavior for flat observer roots only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add flat inspector Observe toggle and link/observe lifecycle behavior</name>
  <read_first>
    - .planning/phases/39-automatic-observer-safety-loop/39-CONTEXT.md
    - src/ui/ui_entity_inspector.h
    - src/jsonl_observer_system.h
    - src/tests/jsonl_flat_observer_inspector_contract_test.c
  </read_first>
  <files>src/ui/ui_entity_inspector.h</files>
  <behavior>
    - Test: link OFF clears active observing immediately but preserves source_path.
    - Test: separate `Observe automatically` control is visible for flat roots.
    - Test: manual `Re-import now` remains enabled by path availability, even after auto-disable.
  </behavior>
  <action>In `ui_entity_inspector_draw_jsonl_flat_observer_controls(...)`, add `Observe automatically##jsonl_flat_root_observe_auto` (D-03), set `observe_enabled=false` immediately when link is turned OFF while keeping `source_path` unchanged (D-02), default observe ON when link is ON and source path exists (D-01), show explicit idle-warning copy for observe ON with empty/invalid path (D-04), and keep `Re-import now` gated only by source path + in-flight status so manual operation survives auto-disable (D-06).</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_inspector_contract_test" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>Inspector exposes independent link/observe controls with required lifecycle semantics and warning visibility.</acceptance_criteria>
  <done>Flat inspector behavior matches D-01 through D-04 and preserves D-06 manual contract.</done>
</task>

<task type="auto">
  <name>Task 3: Lock OBSF-04 with auto-loop and manual-after-disable regression tests</name>
  <read_first>
    - src/tests/jsonl_flat_observer_auto_safety_test.c
    - src/tests/jsonl_flat_observer_manual_refresh_test.c
    - .planning/phases/39-automatic-observer-safety-loop/39-VALIDATION.md
  </read_first>
  <files>src/tests/jsonl_flat_observer_auto_safety_test.c, src/tests/jsonl_flat_observer_manual_refresh_test.c</files>
  <action>Implement concrete assertions for OBSF-04: changed source triggers auto refresh, debounce prevents rapid re-trigger thrash, repeated source-check/refresh failures increment retries then auto-disable observe, missing/invalid source path warning does not consume retries, and manual `jsonl_observer_request_flat_refresh(...)` still succeeds after auto-disable when source path is valid. Keep tests deterministic with local fixture rewrites and bounded tick loops.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_(manual_refresh_test|inspector_contract_test|auto_safety_test)" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>Targeted phase test slice passes and fails if debounce/retry/auto-disable/manual guarantees regress.</acceptance_criteria>
  <done>OBSF-04 is fully covered by executable tests aligned to locked decisions D-01..D-07.</done>
</task>

</tasks>

<verification>
Run:
- `ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_(manual_refresh_test|inspector_contract_test|auto_safety_test)" --output-on-failure`
- `ctest --test-dir build-vulkan -C Release -R "jsonl_(observer_state_test|flat_observer_manual_refresh_test|flat_observer_inspector_contract_test|flat_observer_auto_safety_test)" --output-on-failure`
</verification>

<success_criteria>
- Auto refresh for flat roots is safe under unstable writes (debounce/retry/auto-disable).
- Link/Observe lifecycle behavior matches locked decisions with no deferred-scope creep.
- Manual re-import remains available after auto-disable.
</success_criteria>

<output>
After completion, create `.planning/phases/39-automatic-observer-safety-loop/39-02-SUMMARY.md`
</output>
