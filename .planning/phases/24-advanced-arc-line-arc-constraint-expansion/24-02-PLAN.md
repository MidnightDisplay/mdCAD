---
phase: 24-advanced-arc-line-arc-constraint-expansion
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - src/ecs/ecs_scene.h
  - src/ui/ui_entity_inspector.h
  - src/tests/scene_solver_contract_test.c
  - src/tests/scene_solver_pass_policy_test.c
  - src/tests/scene_solver_diagnostics_test.c
autonomous: false
requirements: [ARCI-01, ARCI-02, ARCI-03, ARCI-04]
must_haves:
  truths:
    - "User can solve arc-axis-vs-line constraints with deterministic arc-primary behavior."
    - "User can solve line-end/arc-end tangency using coincidence-first, then tangency ordering."
    - "User can edit arc endpoint-angle and solver honors first-selected endpoint as anchor."
    - "User sees explicit ARCI family diagnostics with transactional rollback on unsatisfied solve."
  artifacts:
    - path: "src/ecs/ecs_scene.h"
      provides: "ARCI solve branches, deterministic fixed/free policy, transactional diagnostics behavior"
    - path: "src/tests/scene_solver_contract_test.c"
      provides: "ARCI success/unsat transactional contract coverage"
    - path: "src/tests/scene_solver_pass_policy_test.c"
      provides: "Deterministic repeated-recalc and pass-policy assertions with ARCI families"
    - path: "src/tests/scene_solver_diagnostics_test.c"
      provides: "Explicit family-specific ARCI diagnostics assertions"
  key_links:
    - from: "src/ecs/ecs_scene.h"
      to: "scene_solver_set_failure_implication(...)"
      via: "ARCI unsatisfied path"
      pattern: "scene_solver_set_failure_implication"
    - from: "src/ecs/ecs_scene.h"
      to: "scene_solver_add_diagnostic(...)"
      via: "family-specific unsatisfied messages per D-13"
      pattern: "scene_solver_add_diagnostic"
    - from: "src/ui/ui_entity_inspector.h"
      to: "scene_solver_request_recalculate(...)"
      via: "thin recalc caller path"
      pattern: "scene_solver_request_recalculate"
---

<objective>
Implement ARCI runtime solve semantics and deterministic diagnostics closure.

Purpose: satisfy D-02/D-06/D-11 solve contracts and D-12/D-13 deterministic failure behavior without breaking existing scene-owned solver architecture.
Output: ARCI solver branches + deterministic diagnostics + executable + human verification closure.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-advanced-arc-line-arc-constraint-expansion/24-CONTEXT.md
@.planning/phases/24-advanced-arc-line-arc-constraint-expansion/24-RESEARCH.md
@.planning/phases/24-advanced-arc-line-arc-constraint-expansion/24-VALIDATION.md
@.planning/phases/24-advanced-arc-line-arc-constraint-expansion/24-01-PLAN.md
@src/ecs/ecs_scene.h
@src/ui/ui_entity_inspector.h
@src/tests/scene_solver_contract_test.c
@src/tests/scene_solver_pass_policy_test.c
@src/tests/scene_solver_diagnostics_test.c

<interfaces>
From src/ecs/ecs_scene.h:
```c
static inline bool scene_solver_request_recalculate(ecs_scene_t *scene, ecs_entity_t sketch);
static inline bool scene_solver_set_failure_implication(...);
static inline bool scene_solver_add_diagnostic(...);
```

From src/ui/ui_entity_inspector.h:
```c
static inline bool ui_entity_inspector_draw_sketch_constraint_manager(...);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement ARCI solve branches with locked ordering and anchor semantics</name>
  <read_first>@.planning/phases/24-advanced-arc-line-arc-constraint-expansion/24-CONTEXT.md, @src/ecs/ecs_scene.h, @src/components/endpoints_comp.h</read_first>
  <files>src/ecs/ecs_scene.h, src/tests/scene_solver_contract_test.c</files>
  <behavior>
    - ARCI-01 enforces arc-normal axis parallel/anti-parallel to line direction, arc-primary when both editable (D-02, D-03).
    - ARCI-02 enforces coincidence first then tangency at shared point; fixed participant anchors and free side absorbs delta (D-06, D-07).
    - ARCI-03 enforces [0,π] sweep in radians, anchored-first endpoint reference, moving second endpoint role (D-10, D-11).
  </behavior>
  <action>Add dedicated ARCI branches in `scene_solver_request_recalculate(...)` using existing candidate staging so all geometry mutations remain transactional. For ARCI-01, treat line as reference and rotate/reorient arc unless fixed-state prevents solve. For ARCI-02, apply deterministic two-step solve (coincidence then tangent) and reject fallback downgrades. For ARCI-03, clamp/normalize dimensional value to `[0, π]`, preserve participant order from descriptors, keep first endpoint fixed as anchor, and update second endpoint role. Add/extend `scene_solver_contract_test.c` for satisfiable + fixed/fixed unsatisfied cases with no-mutation assertions.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target scene_solver_contract && ctest -R scene_solver_contract --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <acceptance_criteria>ARCI solves satisfy locked ordering/anchor contracts and unsatisfied fixed-state cases remain transactional with zero partial mutation.</acceptance_criteria>
  <done>Runtime ARCI behavior is deterministic and contract-aligned for both success and failure scenarios.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic ARCI diagnostics and pass-policy regression gates</name>
  <read_first>@src/ecs/ecs_scene.h, @src/tests/scene_solver_pass_policy_test.c, @src/tests/scene_solver_diagnostics_test.c</read_first>
  <files>src/ecs/ecs_scene.h, src/ui/ui_entity_inspector.h, src/tests/scene_solver_pass_policy_test.c, src/tests/scene_solver_diagnostics_test.c</files>
  <behavior>
    - Unsatisfied ARCI families emit explicit family-specific diagnostics (D-13).
    - Failure implication highlighting remains active with deterministic repeated recalculate results (D-12).
    - Inspector remains thin caller; no solver policy in UI layer.
  </behavior>
  <action>Introduce dedicated ARCI unsatisfied diagnostic strings in solver failure paths (arc-axis relation, line-arc endpoint tangency, arc endpoint-angle) and wire each to implication payload population. Preserve existing dedupe/sorting behavior and deterministic recalc serial semantics. Add diagnostics assertions in `scene_solver_diagnostics_test.c` and deterministic repeated-recalc/pass-cap assertions in `scene_solver_pass_policy_test.c` for ARCI cases. Ensure any inspector touch keeps `scene_solver_request_recalculate(...)` as sole solve trigger path (per D-12).</action>
  <verify>
    <automated>ctest -R "scene_solver_contract|scene_solver_pass_policy|scene_solver_diagnostics|scene_solver_trigger|scene_solver_drag" --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <acceptance_criteria>Each ARCI family has explicit unsatisfied diagnostics; repeated recalc on unchanged state remains deterministic and transactional.</acceptance_criteria>
  <done>ARCI-04 deterministic diagnostics policy is enforced and regression-protected.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify end-to-end ARCI authoring and deterministic failure UX</name>
  <read_first>@.planning/phases/24-advanced-arc-line-arc-constraint-expansion/24-CONTEXT.md, @.planning/phases/24-advanced-arc-line-arc-constraint-expansion/24-VALIDATION.md</read_first>
  <action>Run the five-step interactive validation sequence for ARCI-01..04, capture pass/fail per step, and block completion until any failures are either fixed and re-checked or documented for follow-up.</action>
  <acceptance_criteria>All five interactive checks pass: legal menu gating, ARCI-01 solve behavior, ARCI-02 coincidence-first tangency, ARCI-03 anchored endpoint-angle edits, and explicit ARCI-family unsat diagnostics with implication highlight.</acceptance_criteria>
  <what-built>ARCI legality + solver + diagnostics behavior for ARCI-01..04 with deterministic transactional recalculate.</what-built>
  <how-to-verify>
    1) In sketch workspace, validate menu gating:
       - select line entity + arc entity → only ARCI-01 available.
       - select line endpoint + arc endpoint → ARCI-02 available.
       - select same-arc endpoint pair in order → ARCI-03 available.
       - invalid shapes (raw entities for ARCI-02/03, mixed-arc pair for ARCI-03) show no ARCI option.
    2) Apply ARCI-01 and recalculate repeatedly; verify arc orientation tracks line axis relation while line remains reference if both editable (D-02, D-03).
    3) Apply ARCI-02 on mismatched endpoint/arc endpoint; verify solver first coincides shared point, then enforces tangency, with deterministic repeat recalc (D-06).
    4) Apply ARCI-03 then edit value (degrees in UI); verify first-selected endpoint stays anchored while second endpoint moves and behavior is deterministic (D-10, D-11).
    5) Create fixed/fixed unsatisfied ARCI setups for each family; recalc and verify no geometry mutation, explicit family-specific diagnostic text, and implication highlight (D-12, D-13).
  </how-to-verify>
  <verify>
    <automated>ctest -R "scene_solver_contract|scene_solver_pass_policy|scene_solver_diagnostics|endpoint_pick" --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <resume-signal>Type "approved" if all checks pass, or return failing step numbers with observed behavior.</resume-signal>
  <done>Human confirms ARCI-01..04 behavior is correct and deterministic in interactive workflow.</done>
</task>

</tasks>

<verification>
Run full Phase 24 targeted suite:
`ctest -R "scene_solver_contract|scene_solver_pass_policy|scene_solver_diagnostics|scene_solver_trigger|scene_solver_drag|endpoint_pick" --test-dir build-vulkan -C Release --output-on-failure`
</verification>

<success_criteria>
- ARCI-01/02/03 runtime solve behavior follows locked decisions D-01..D-11.
- ARCI-04 deterministic transactional diagnostics policy is explicit and test-covered.
- Human checkpoint approves interactive behavior and failure UX.
</success_criteria>

<output>
After completion, create `.planning/phases/24-advanced-arc-line-arc-constraint-expansion/24-02-SUMMARY.md`
</output>

