---
phase: 24-advanced-arc-line-arc-constraint-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/constraint_comp.h
  - src/constraints/constraint_types.h
  - src/app.c
  - src/scripting/sketch_script_apply.h
  - src/tests/endpoint_pick_test.c
autonomous: true
requirements: [ARCI-01, ARCI-02, ARCI-03]
must_haves:
  truths:
    - "User can only author arc-axis-vs-line using exactly one line entity plus one arc entity."
    - "User can only author line-end/arc-end tangency using exactly one line endpoint role plus one arc endpoint role."
    - "User can only author arc endpoint-angle from two ordered endpoints on the same arc."
  artifacts:
    - path: "src/components/constraint_comp.h"
      provides: "Dedicated ARCI constraint type identifiers"
    - path: "src/constraints/constraint_types.h"
      provides: "Centralized ARCI legality contracts shared by UI and scripting"
    - path: "src/app.c"
      provides: "Constraint menu gating through descriptor signatures"
    - path: "src/tests/endpoint_pick_test.c"
      provides: "Legality regression matrix for ARCI signatures and ordered endpoint semantics"
  key_links:
    - from: "src/app.c"
      to: "src/constraints/constraint_types.h"
      via: "constraint_type_is_selection_legal(...) menu filtering"
      pattern: "constraint_type_is_selection_legal"
    - from: "src/scripting/sketch_script_apply.h"
      to: "src/constraints/constraint_types.h"
      via: "shared legality validation path"
      pattern: "sketch_script_apply_validate_model_links"
---

<objective>
Establish ARCI authoring contracts and shared legality plumbing before runtime solve changes.

Purpose: lock D-01, D-05, D-09 constraints at creation time so invalid shapes never enter solver state.
Output: ARCI types + legality matrix + authoring/script parity tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-advanced-arc-line-arc-constraint-expansion/24-CONTEXT.md
@.planning/phases/24-advanced-arc-line-arc-constraint-expansion/24-RESEARCH.md
@src/components/constraint_comp.h
@src/constraints/constraint_types.h
@src/app.c
@src/scripting/sketch_script_apply.h
@src/tests/endpoint_pick_test.c

<interfaces>
From src/constraints/constraint_types.h:
```c
static inline bool constraint_type_is_selection_legal(constraint_selection_signature_t *sig,
                                                      constraint_type_t type);
```

From src/app.c:
```c
static bool mdcad_collect_constraint_context(...);
scene_add_constraint_to_sketch_with_descriptors(...)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dedicated ARCI constraint families and legality signatures</name>
  <read_first>@.planning/phases/24-advanced-arc-line-arc-constraint-expansion/24-CONTEXT.md, @src/components/constraint_comp.h, @src/constraints/constraint_types.h</read_first>
  <files>src/components/constraint_comp.h, src/constraints/constraint_types.h, src/tests/endpoint_pick_test.c</files>
  <behavior>
    - ARCI-01 legality accepts only {LINE:ENTITY, ARC:ENTITY} (per D-01).
    - ARCI-02 legality accepts only {LINE:POINT_A|POINT_B, ARC:POINT_A|POINT_B} (per D-05).
    - ARCI-03 legality accepts only two endpoints on the same arc and preserves participant order (per D-09).
  </behavior>
  <action>Extend `constraint_type_t` with three explicit ARCI families (arc-axis-vs-line, line-end-arc-end tangency, arc endpoint-angle) and update display-name/min-participant helpers. Implement legality gates in `constraint_type_is_selection_legal(...)` exactly per D-01, D-05, D-09; reject center role for ARCI-03 and any raw entity signatures for ARCI-02/03. Add executable legality tests in `endpoint_pick_test.c` for positive and negative signatures, explicitly labeling D-01/D-05/D-09 cases.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target endpoint_pick && ctest -R endpoint_pick --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <acceptance_criteria>ARCI constraints can only be created from allowed descriptor signatures, and legality regressions fail fast in endpoint_pick tests.</acceptance_criteria>
  <done>Constraint type system and legality matrix enforce ARCI creation boundaries deterministically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce interactive/script authoring parity for ARCI legality</name>
  <read_first>@src/app.c, @src/scripting/sketch_script_apply.h, @src/constraints/constraint_types.h</read_first>
  <files>src/app.c, src/scripting/sketch_script_apply.h, src/tests/endpoint_pick_test.c</files>
  <behavior>
    - Constraint menu only offers ARCI families when signatures are legal (per D-01, D-05, D-09).
    - Script apply uses shared legality (no script-only bypass), preserving deterministic outcomes.
    - ARCI-03 retains participant order as selected so first participant remains semantic anchor input for solver (per D-09).
  </behavior>
  <action>Route ARCI menu exposure entirely through `constraint_type_is_selection_legal(...)` in `app.c`, without local ad-hoc checks. Keep descriptor ordering from `mdcad_collect_constraint_context(...)` unchanged through creation call. In `sketch_script_apply_validate_model_links(...)`, keep centralized legality invocation and add ARCI-aware signature construction so scripting honors the same constraints as UI. Expand endpoint_pick legality tests to assert UI/script-parity assumptions.</action>
  <verify>
    <automated>ctest -R "endpoint_pick|scene_solver_contract" --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <acceptance_criteria>Interactive authoring and script validation accept/reject identical ARCI signatures with ordered participants preserved.</acceptance_criteria>
  <done>ARCI authoring entrypoints are unified and cannot drift between UI and script paths.</done>
</task>

</tasks>

<verification>
Run legality gate:
`ctest -R "endpoint_pick|scene_solver_contract" --test-dir build-vulkan -C Release --output-on-failure`
</verification>

<success_criteria>
- ARCI-01/02/03 creation contracts are enforced at legality level.
- ARCI options appear only for legal selections.
- Script and UI legality remain in lockstep.
</success_criteria>

<output>
After completion, create `.planning/phases/24-advanced-arc-line-arc-constraint-expansion/24-01-SUMMARY.md`
</output>

