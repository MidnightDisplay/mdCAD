---
phase: 28-tangency-drag-robustness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ecs/ecs_scene.h
  - src/tests/scene_solver_contract_test.c
  - src/tests/scene_solver_drag_test.c
  - src/tests/scene_solver_diagnostics_test.c
autonomous: true
requirements: [TRDG-01, TRDG-02, TRDG-03]
must_haves:
  truths:
    - "User can author feasible line-end/arc-end tangency and get solved geometry without unexpected anchor override."
    - "User can drag both shared tangency points and adjacent handles in feasible setups, with the dragged participant remaining authoritative within DoF."
    - "Infeasible tangency edits rollback transactionally, keep explicit line-arc endpoint tangency diagnostics, and allow immediate subsequent successful edits."
  artifacts:
    - path: "src/ecs/ecs_scene.h"
      provides: "Tangency drag-authority reconciliation, shared/adjacent feasibility handling, and transactional failure propagation."
    - path: "src/tests/scene_solver_contract_test.c"
      provides: "Tangency feasibility, transactional rollback, and dragged-anchor authority regression fixtures."
    - path: "src/tests/scene_solver_drag_test.c"
      provides: "Tangency-specific drag matrix coverage for shared versus adjacent participant drags."
    - path: "src/tests/scene_solver_diagnostics_test.c"
      provides: "Family-specific unsatisfied tangency diagnostics plus post-failure responsiveness assertions."
  key_links:
    - from: "src/tests/scene_solver_contract_test.c"
      to: "src/ecs/ecs_scene.h"
      via: "scene_solver_request_recalculate tangency branch"
      pattern: "CONSTRAINT_LINE_ARC_ENDPOINT_TANGENCY"
    - from: "src/tests/scene_solver_drag_test.c"
      to: "src/ecs/ecs_scene.h"
      via: "scene_solver_can_apply_drag and endpoint drag application"
      pattern: "SCENE_SOLVER_DRAG_(FEASIBLE|UNSATISFIABLE)"
    - from: "src/tests/scene_solver_diagnostics_test.c"
      to: "src/ecs/ecs_scene.h"
      via: "scene_solver_set_failure_implication reason checks"
      pattern: "Unsatisfied line-arc endpoint tangency constraint\\."
---

<objective>
Harden tangency drag behavior so direct manipulation remains authoritative in feasible setups while unsatisfied edits fail transactionally with explicit diagnostics and no solver stall.

Purpose: Close TRDG-01/02/03 by enforcing D-01..D-06 and D-09..D-11 in runtime behavior and regression suites.
Output: Updated tangency solver behavior and targeted contract/drag/diagnostics tests proving feasibility, rollback integrity, and post-failure responsiveness.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-tangency-drag-robustness/28-CONTEXT.md
@.planning/phases/27-principal-axis-line-along-reliability/27-02-SUMMARY.md
@src/ecs/ecs_scene.h
@src/tests/scene_solver_contract_test.c
@src/tests/scene_solver_drag_test.c
@src/tests/scene_solver_diagnostics_test.c
@src/CMakeLists.txt

<interfaces>
From src/ecs/ecs_scene.h:
```c
static inline bool scene_solver_set_drag_anchor(ecs_scene_t *scene,
                                                ecs_entity_t sketch,
                                                ecs_entity_t owner_entity,
                                                constraint_participant_role_t role,
                                                uint8_t sub_index);
static inline bool scene_solver_clear_drag_anchor(ecs_scene_t *scene, ecs_entity_t sketch);
static inline bool scene_solver_request_recalculate(ecs_scene_t *scene, ecs_entity_t sketch);
static inline bool scene_solver_can_apply_drag(ecs_scene_t *scene,
                                               ecs_entity_t sketch,
                                               const ecs_entity_t *drag_entities,
                                               int drag_entity_count,
                                               vec3_t requested_delta,
                                               scene_solver_drag_decision_t *out_decision);
static inline bool scene_solver_drag_make_rejected_diagnostic(const scene_solver_drag_decision_t *decision,
                                                              scene_solver_drag_diagnostic_event_t *out_event);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED tangency drag matrix and rollback/responsiveness tests</name>
  <read_first>@.planning/phases/28-tangency-drag-robustness/28-CONTEXT.md, @src/tests/scene_solver_contract_test.c, @src/tests/scene_solver_drag_test.c, @src/tests/scene_solver_diagnostics_test.c</read_first>
  <files>src/tests/scene_solver_contract_test.c, src/tests/scene_solver_drag_test.c, src/tests/scene_solver_diagnostics_test.c</files>
  <behavior>
    - Feasible line-end/arc-end tangency creation remains solved with explicit coincidence+tangent assertions (TRDG-01).
    - Shared tangency handle drags and adjacent non-shared handle drags are both feasible when geometry allows, with dragged participant authority preserved per D-01/D-04/D-05.
    - Infeasible tangency drags rollback with unchanged geometry, keep family-specific reason text, and an immediate follow-up feasible edit succeeds per D-09/D-10/D-11.
  </behavior>
  <action>Create failing tests that explicitly cover tangency-specific shared/adjacent drag matrix gaps called out in phase context. Extend existing tangency contract fixture style and add drag test cases for: (1) shared-point drag feasible, (2) adjacent-handle drag feasible, (3) infeasible drag transaction rollback, (4) immediate post-failure successful drag to prove responsiveness. Keep diagnostic expectation exact as "Unsatisfied line-arc endpoint tangency constraint." per D-10; do not add generalized cross-family drag policy tests (deferred idea).</action>
  <verify>
    <automated>ctest --test-dir build -C Release -R "scene_solver_contract|scene_solver_drag|scene_solver_diagnostics" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>New tangency matrix and responsiveness tests exist and fail before runtime changes, with grep-visible test names containing `tangency` and either `shared`, `adjacent`, or `post_failure`.</acceptance_criteria>
  <done>Regression harness defines TRDG-01/02/03 behavior contracts before solver implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement tangency drag-authority reconciliation with transactional failure integrity</name>
  <read_first>@src/ecs/ecs_scene.h, @.planning/phases/28-tangency-drag-robustness/28-CONTEXT.md, @src/tests/scene_solver_contract_test.c, @src/tests/scene_solver_drag_test.c</read_first>
  <files>src/ecs/ecs_scene.h</files>
  <action>Update `CONSTRAINT_LINE_ARC_ENDPOINT_TANGENCY` solve handling and drag-anchor usage so the directly dragged participant remains authoritative within current DoF (D-01, D-02), including both shared and adjacent handles when feasible (D-04, D-05), and avoiding prior aggressively pinned shared-point behavior (D-06). Preserve strict transactional rollback semantics for infeasible edits (D-09), retain explicit family-specific tangency failure reasons (D-10), and ensure failure paths do not leave solver drag state stuck for subsequent edits (D-11). If any heuristic tradeoff is needed, keep user-locked behavior over alternative research heuristics and note in code comments. Keep scope limited to tangency robustness; do not generalize to all constraint families (deferred).</action>
  <verify>
    <automated>ctest --test-dir build -C Release -R "scene_solver_contract|scene_solver_drag|scene_solver_diagnostics" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>All new tangency matrix tests pass, and `git --no-pager grep -n "Unsatisfied line-arc endpoint tangency constraint\\." src/ecs/ecs_scene.h src/tests/scene_solver_diagnostics_test.c` still reports explicit family-specific reason coverage.</acceptance_criteria>
  <done>Tangency drag behavior now honors dragged-participant authority in feasible edits while preserving transactional and diagnostic contracts in infeasible edits.</done>
</task>

<task type="auto">
  <name>Task 3: Run deterministic rerun gate for tangency robustness slice</name>
  <read_first>@src/CMakeLists.txt, @src/tests/scene_solver_contract_test.c, @src/tests/scene_solver_drag_test.c, @src/tests/scene_solver_diagnostics_test.c</read_first>
  <files>src/tests/scene_solver_contract_test.c, src/tests/scene_solver_drag_test.c, src/tests/scene_solver_diagnostics_test.c</files>
  <action>Execute the targeted tangency reliability gate twice back-to-back to enforce deterministic behavior and no post-failure stall: first run validates correctness, second run confirms rerun stability (D-08, D-11). If failures appear, fix only tangency drag-authority, rollback, or diagnostics causes in this phase scope.</action>
  <verify>
    <automated>ctest --test-dir build -C Release -R "scene_solver_contract|scene_solver_drag|scene_solver_diagnostics|scene_solver_pass_policy" --output-on-failure && ctest --test-dir build -C Release -R "scene_solver_contract|scene_solver_drag|scene_solver_diagnostics|scene_solver_pass_policy" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>Two consecutive targeted runs pass with no new tangency-related failures and no rerun drift.</acceptance_criteria>
  <done>TRDG-01/02/03 has deterministic automated evidence suitable for phase verification handoff.</done>
</task>

</tasks>

<verification>
Run twice for deterministic confidence:
`ctest --test-dir build -C Release -R "scene_solver_contract|scene_solver_drag|scene_solver_diagnostics|scene_solver_pass_policy" --output-on-failure`
</verification>

<success_criteria>
- TRDG-01: Feasible line-end/arc-end tangency authoring remains stably solvable.
- TRDG-02: Shared and adjacent tangency-participant drags are both feasible when geometry allows, driven by dragged-handle authority.
- TRDG-03: Infeasible tangency edits rollback transactionally with explicit tangency diagnostics and immediate subsequent solver responsiveness.
</success_criteria>

<output>
After completion, create `.planning/phases/28-tangency-drag-robustness/28-01-SUMMARY.md`
</output>

