---
phase: 41-linked-import-convergence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/jsonl_import_job.h
  - src/tests/jsonl_flat_observer_auto_safety_test.c
  - src/tests/jsonl_flat_observer_manual_refresh_test.c
autonomous: true
requirements: [FIMP-05, FIMP-06]
must_haves:
  truths:
    - "A linked flat import stamps its own source file as baseline before automatic observe can treat anything as changed."
    - "Observer ticks after the first linked import stay idle until the file changes externally."
    - "If baseline arming fails after import, imported geometry and source path stay attached while automatic observe turns OFF with a warning."
    - "Deterministic automated tests fail if initial linked-import counts drift or a self-refresh starts immediately after import."
  artifacts:
    - path: "src/jsonl_import_job.h"
      provides: "Initial linked-import baseline arming and safe fallback at import commit time"
      contains: "jsonl_observer_stamp_source_state"
    - path: "src/tests/jsonl_flat_observer_auto_safety_test.c"
      provides: "No-self-refresh and stable-count regressions for initial linked imports"
      contains: "test_flat_linked_import_stamps_baseline_and_stays_idle"
    - path: "src/tests/jsonl_flat_observer_manual_refresh_test.c"
      provides: "Baseline-arm failure fallback regression that keeps committed geometry"
      contains: "test_flat_linked_import_baseline_failure_keeps_geometry_and_disables_observe"
  key_links:
    - from: "src/jsonl_import_job.h"
      to: "src/jsonl_sketch_import_job.h"
      via: "baseline stamp at flat import completion"
      pattern: "jsonl_observer_stamp_source_state"
    - from: "src/jsonl_import_job.h"
      to: "src/components/jsonl_observer_comp.h"
      via: "observe_enabled/source_path/message state stored on the committed root"
      pattern: "observe_enabled|source_path|message"
    - from: "src/tests/jsonl_flat_observer_auto_safety_test.c"
      to: "src/jsonl_observer_system.h"
      via: "post-import idle observer tick assertions"
      pattern: "jsonl_observer_system_tick|jsonl_observer_is_flat_refresh_running"
---

<objective>
Lock the initial linked flat-import commit into a stable observer baseline so the first successful import does not refresh itself and does not lose committed geometry.

Purpose: Satisfy FIMP-05 and FIMP-06 under D-01, D-02, D-03, and D-04 before any manual lamp-file confirmation.
Output: Import-commit observer baseline arming, safe fallback semantics, and deterministic automated regressions for linked-import convergence.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-linked-import-convergence/41-CONTEXT.md
@.planning/phases/36-flat-import-entry-configuration/36-02-SUMMARY.md
@.planning/phases/38-observable-link-manual-transactional-refresh/38-01-SUMMARY.md
@.planning/phases/39-automatic-observer-safety-loop/39-02-SUMMARY.md
@.planning/phases/40-large-dump-stability-interaction-coherence/40-01-SUMMARY.md
@src/jsonl_import_job.h
@src/jsonl_observer_system.h
@src/jsonl_sketch_import_job.h
@src/components/jsonl_observer_comp.h
@src/tests/jsonl_flat_observer_auto_safety_test.c
@src/tests/jsonl_flat_observer_manual_refresh_test.c

<interfaces>
From `src/jsonl_import_job.h`:
```c
typedef struct {
    bool captured;
    bool link_enabled;
    char source_path[JSONL_OBSERVER_PATH_MAX];
} jsonl_import_observer_contract_t;

static inline void jsonl_import_job_set_observer_contract(jsonl_import_job_t *job,
                                                          bool link_enabled,
                                                          const char *source_path);
static inline bool jsonl_import_job_tick(jsonl_import_job_t *job, ecs_scene_t *scene);
```

From `src/jsonl_sketch_import_job.h`:
```c
static inline bool jsonl_observer_stamp_source_state(JsonlObserverComp *observer, const char *filepath);
static inline bool jsonl_observer_source_changed(JsonlObserverComp *observer,
                                                 const char *filepath,
                                                 bool *out_changed);
```

From `src/components/jsonl_observer_comp.h`:
```c
typedef struct {
    bool linked;
    bool observe_enabled;
    bool source_state_valid;
    uint64_t last_source_size_bytes;
    uint64_t last_source_mtime_unix_ns;
    uint64_t last_source_hash;
    char source_path[JSONL_OBSERVER_PATH_MAX];
    uint32_t message_count;
    uint8_t message_severity[JSONL_OBSERVER_MESSAGE_HISTORY];
    char messages[JSONL_OBSERVER_MESSAGE_HISTORY][JSONL_OBSERVER_MESSAGE_MAX];
} JsonlObserverComp;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Arm the linked-import baseline at commit time with D-04 fallback</name>
  <read_first>
    - .planning/phases/41-linked-import-convergence/41-CONTEXT.md
    - src/jsonl_import_job.h
    - src/jsonl_sketch_import_job.h
    - src/components/jsonl_observer_comp.h
  </read_first>
  <files>src/jsonl_import_job.h</files>
  <behavior>
    - Test 1: A linked flat import commits with `source_state_valid=true`, keeps `observe_enabled=true`, and records a baseline-ready message when source stamping succeeds.
    - Test 2: Repeated observer ticks against the untouched source do not queue a flat refresh after the initial linked import.
    - Test 3: If source stamping fails after import commit, the root entity and imported geometry remain, `source_path` stays populated, `observe_enabled=false`, and a warning message is recorded.
  </behavior>
  <action>In the `JSONL_JOB_PARENTING_ENTITIES` completion path, after building the root `JsonlObserverComp` for flat imports, add the D-01/D-02 baseline arm step before the final component is stored. When `observer_contract.captured=true`, `observer_contract.link_enabled=true`, and `source_path` is non-empty, call `jsonl_observer_stamp_source_state(&observer, source_path)` on the just-imported file. On success, keep the linked source path, preserve automatic observe ON, and push an INFO message that the linked import baseline is armed and waiting for a later external file change (D-01, D-02). On failure, do not delete the root or rollback any imported subtree; instead keep `linked=true`, keep `source_path`, set `observe_enabled=false`, push a WARNING message, and rewrite `job->status_message` so the import reports success-with-safety-disable rather than an import error (D-04). Do not queue or request any refresh from the import-complete path; the first automatic refresh must still require a later real external file change (D-02, D-03).</action>
  <acceptance_criteria>`src/jsonl_import_job.h` contains an explicit `jsonl_observer_stamp_source_state(...)` call in flat-import completion, an `observe_enabled=false` fallback branch, and success/warning status text that distinguishes safe fallback from import failure.</acceptance_criteria>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target jsonl_flat_observer_auto_safety_test jsonl_flat_observer_manual_refresh_test</automated>
  </verify>
  <done>Initial linked flat imports create a committed observer baseline instead of entering an immediate self-refresh loop, and D-04 fallback preserves the imported root on stamp failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic regressions for idle-after-import convergence and baseline-arm failure</name>
  <read_first>
    - .planning/phases/41-linked-import-convergence/41-CONTEXT.md
    - src/tests/jsonl_flat_observer_auto_safety_test.c
    - src/tests/jsonl_flat_observer_manual_refresh_test.c
    - src/jsonl_observer_system.h
    - src/jsonl_import_job.h
  </read_first>
  <files>src/tests/jsonl_flat_observer_auto_safety_test.c, src/tests/jsonl_flat_observer_manual_refresh_test.c</files>
  <behavior>
    - Test 1: A linked import with a large deterministic fixture (for example 1200 points) keeps the same geometry count and starts zero refresh slots across repeated observer ticks when the file is unchanged.
    - Test 2: The committed observer on that linked import reports a valid stamped source state and does not spend retry budget while idle.
    - Test 3: A simulated baseline-arm failure leaves geometry/root intact, preserves `source_path`, disables automatic observe, and records a warning.
  </behavior>
  <action>Add `test_flat_linked_import_stamps_baseline_and_stays_idle` to `src/tests/jsonl_flat_observer_auto_safety_test.c` using a deterministic large point fixture (use `write_jsonl_point_fixture(..., 1200)` or an equivalent large-but-fast count). Import with link enabled, capture the initial geometry count under the root, run `jsonl_observer_system_tick(...)` repeatedly without rewriting the file, and assert all of the following per D-01/D-02/D-03: `observer->source_state_valid == true`, `jsonl_observer_is_flat_refresh_running(...) == false`, geometry count under the root is unchanged, no extra active refresh slots appear, and retry count stays at zero. In `src/tests/jsonl_flat_observer_manual_refresh_test.c`, add `test_flat_linked_import_baseline_failure_keeps_geometry_and_disables_observe` that exercises the D-04 fallback through the same import-complete code path or a narrow extracted helper seam: force the baseline stamp to fail with a known-missing path after the import has otherwise succeeded, then assert the root is still alive, geometry count is unchanged, `source_path` is preserved, `observe_enabled == false`, and a warning message is present. Keep the scope on initial linked-import convergence only; do not add new delete cleanup or later-change hardening assertions beyond keeping existing phase-39/40 tests green.</action>
  <acceptance_criteria>`src/tests/jsonl_flat_observer_auto_safety_test.c` contains a no-self-refresh linked-import test, `src/tests/jsonl_flat_observer_manual_refresh_test.c` contains a baseline-failure fallback test, and `ctest` output shows those tests passing instead of drifting counts or starting an immediate refresh.</acceptance_criteria>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_(auto_safety_test|manual_refresh_test)" --output-on-failure</automated>
  </verify>
  <done>Automated coverage deterministically protects initial linked-import convergence and D-04 fallback behavior for FIMP-05/FIMP-06.</done>
</task>

</tasks>

<verification>
Run:
- `cmake --build build-vulkan --config Release --target jsonl_flat_observer_auto_safety_test jsonl_flat_observer_manual_refresh_test`
- `ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_(auto_safety_test|manual_refresh_test|inspector_contract_test)" --output-on-failure`
</verification>

<success_criteria>
- Linked flat imports arm their observer baseline before automatic observe can treat the source as changed.
- Unchanged-source observer ticks after import do not start a self-refresh or change committed counts.
- Baseline-arm failure keeps imported geometry/source metadata and disables automatic observe with a warning.
</success_criteria>

<output>
After completion, create `.planning/phases/41-linked-import-convergence/41-01-SUMMARY.md`
</output>
