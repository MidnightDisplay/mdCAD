---
phase: 41-linked-import-convergence
plan: 02
type: execute
wave: 2
depends_on: [41-01]
files_modified:
  - .planning/phases/41-linked-import-convergence/41-MANUAL-CHECKLIST.md
autonomous: false
requirements: [FIMP-05, FIMP-06]
must_haves:
  truths:
    - "Using the real `lamp_11.jsonl` file with linking enabled leaves the full committed geometry visible after import settles."
    - "Scene Hierarchy totals converge to one committed final count instead of continuing to climb after first visibility."
    - "Slot Buffer Debug stays consistent with the committed import instead of dropping an early-slot chunk or collapsing to a tail subset."
  artifacts:
    - path: ".planning/phases/41-linked-import-convergence/41-MANUAL-CHECKLIST.md"
      provides: "Exact lamp_11 linked-import verification script and evidence fields"
      contains: "lamp_11.jsonl"
  key_links:
    - from: "src/ui/ui_scene_hierarchy.h"
      to: ".planning/phases/41-linked-import-convergence/41-MANUAL-CHECKLIST.md"
      via: "`Import JSONL (Flat Large Dump)...` with `Link file for refresh (optional)` enabled"
      pattern: "Import JSONL \\(Flat Large Dump\\)|Link file for refresh"
    - from: "src/ui/ui_slot_buffer_debug.h"
      to: ".planning/phases/41-linked-import-convergence/41-MANUAL-CHECKLIST.md"
      via: "slot occupancy confirmation during and after import settle"
      pattern: "Slot Buffer Debug|Total slots"
    - from: "src/ui/ui_entity_inspector.h"
      to: ".planning/phases/41-linked-import-convergence/41-MANUAL-CHECKLIST.md"
      via: "observer status/message review after linked import"
      pattern: "Last refresh result|Observe automatically"
---

<objective>
Capture the exact manual convergence proof for the real lamp dataset so Phase 41 closes with the locked acceptance evidence from D-05.

Purpose: Turn the automated fix from 41-01 into reproducible real-file confirmation for FIMP-05 and FIMP-06 without drifting into Phase 42 scope.
Output: Manual lamp_11 checklist/evidence file and one blocking human verification checkpoint.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-linked-import-convergence/41-CONTEXT.md
@.planning/phases/41-linked-import-convergence/41-01-PLAN.md
@.planning/phases/40-large-dump-stability-interaction-coherence/40-STRESS-PROTOCOL.md
@src/ui/ui_scene_hierarchy.h
@src/ui/ui_entity_inspector.h
@src/ui/ui_slot_buffer_debug.h

<interfaces>
From `src/ui/ui_scene_hierarchy.h`:
```c
bool jsonl_flat_link_file_for_refresh;
jsonl_import_job_t jsonl_flat_import_job;
bool jsonl_flat_import_progress_popup_open;
```

From `src/ui/ui_entity_inspector.h`:
```c
static inline void ui_entity_inspector_draw_jsonl_flat_observer_controls(ui_entity_inspector_state_t *state,
                                                                         ecs_world_state_t *w,
                                                                         ecs_entity_t root_entity,
                                                                         ecs_scene_t *scene);
```

From `src/ui/ui_slot_buffer_debug.h`:
```c
static inline void ui_slot_buffer_debug_draw(ui_slot_buffer_debug_state_t* state);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the exact lamp_11 linked-import convergence checklist</name>
  <read_first>
    - .planning/phases/41-linked-import-convergence/41-CONTEXT.md
    - .planning/phases/40-large-dump-stability-interaction-coherence/40-STRESS-PROTOCOL.md
    - src/ui/ui_scene_hierarchy.h
    - src/ui/ui_entity_inspector.h
    - src/ui/ui_slot_buffer_debug.h
  </read_first>
  <files>.planning/phases/41-linked-import-convergence/41-MANUAL-CHECKLIST.md</files>
  <action>Create `41-MANUAL-CHECKLIST.md` with the exact Phase 41 manual pass script from D-05. Include the full required source path `C:\Users\RodionRadchenko\source\repos\ParkerSteel-GenerateDimensionedDrawing\ParkerSteel.Drawing.Test\bin\Debug\net8.0-windows\lamp_11.jsonl`; import steps using `File -> Import JSONL (Flat Large Dump)...` with `Link file for refresh (optional)` enabled; an explicit instruction to make no external file edits during this pass; Slot Buffer Debug checks for stable slot occupancy after import settles; Scene Hierarchy checks that record the first committed total and the final settled total; Entity Inspector checks that confirm linked source path and observe state; and result fields named `Viewport result:`, `Scene Hierarchy first committed total:`, `Scene Hierarchy settled total:`, `Slot Buffer note:`, `Observer status note:`, and `Final verdict:`. Use `PASS` / `FAIL` / `TBD` markers so the later checkpoint can be verified by file contents.</action>
  <acceptance_criteria>`41-MANUAL-CHECKLIST.md` exists and contains the exact lamp path, the no-source-edit rule, Slot Buffer Debug and Scene Hierarchy convergence steps, and the named result fields with `PASS/FAIL/TBD` markers.</acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; p=Path('.planning/phases/41-linked-import-convergence/41-MANUAL-CHECKLIST.md'); t=p.read_text(encoding='utf-8'); assert 'lamp_11.jsonl' in t and 'Slot Buffer note:' in t and 'Scene Hierarchy settled total:' in t and 'Final verdict:' in t and 'TBD' in t"</automated>
  </verify>
  <done>The real-file acceptance script is written down exactly enough that another agent or the user can run the same convergence check without interpretation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the lamp_11 linked-import convergence pass and record the evidence</name>
  <read_first>
    - .planning/phases/41-linked-import-convergence/41-MANUAL-CHECKLIST.md
    - src/ui/ui_scene_hierarchy.h
    - src/ui/ui_entity_inspector.h
    - src/ui/ui_slot_buffer_debug.h
  </read_first>
  <files>.planning/phases/41-linked-import-convergence/41-MANUAL-CHECKLIST.md</files>
  <what-built>Phase 41 automated linked-import convergence fix plus a real-file checklist for D-05.</what-built>
  <action>Use the checklist exactly as written. Import `lamp_11.jsonl` through the flat large-dump flow with linking enabled, let the import settle without editing the source file afterward, then record the observed viewport, Scene Hierarchy, Slot Buffer Debug, and observer-status results into the checklist. Mark each result field `PASS` or `FAIL`, replace all `TBD` markers, and set `Final verdict:` to `PASS` only if the full geometry remains visible, Scene Hierarchy totals converge instead of climbing, and slot-buffer occupancy stays stable instead of collapsing to a later tail subset (D-05).</action>
  <how-to-verify>
    1) Run the automated regression suite from 41-01 and confirm it passes before the manual pass.
    2) Launch the native build, open `File -> Import JSONL (Flat Large Dump)...`, choose the exact `lamp_11.jsonl` path, and enable `Link file for refresh (optional)`.
    3) Let the import finish and keep the source file untouched; verify the viewport stays fully populated after settle.
    4) Record the first committed Scene Hierarchy total and the later settled total; they must converge to the same final count instead of continuing to climb.
    5) Open Slot Buffer Debug and record whether slot occupancy stays stable with no disappearing early-slot chunk or tail-only collapse.
    6) Review the linked root in Entity Inspector and record the observer status note.
    7) Fill every checklist result field and set the final verdict.
  </how-to-verify>
  <acceptance_criteria>`41-MANUAL-CHECKLIST.md` contains no remaining `TBD` markers, includes recorded viewport/Scene Hierarchy/slot-buffer/observer notes, and ends with an explicit `Final verdict: PASS` or `Final verdict: FAIL` line.</acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; t=Path('.planning/phases/41-linked-import-convergence/41-MANUAL-CHECKLIST.md').read_text(encoding='utf-8'); assert 'TBD' not in t and ('Final verdict: PASS' in t or 'Final verdict: FAIL' in t)"</automated>
  </verify>
  <done>The real lamp_11 convergence evidence is recorded in one file and Phase 41 can be approved or rejected on explicit evidence instead of memory.</done>
  <resume-signal>Type "approved" after the checklist is filled and the result is acceptable, or reply with the recorded failure notes.</resume-signal>
</task>

</tasks>

<verification>
Run:
- `ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_(auto_safety_test|manual_refresh_test|inspector_contract_test)" --output-on-failure`
- `python -c "from pathlib import Path; t=Path('.planning/phases/41-linked-import-convergence/41-MANUAL-CHECKLIST.md').read_text(encoding='utf-8'); assert 'TBD' not in t"`
</verification>

<success_criteria>
- The real `lamp_11.jsonl` linked import has a single recorded convergence verdict.
- Manual evidence explicitly covers viewport completeness, Scene Hierarchy convergence, and slot-buffer stability.
- Phase 41 closes only after automated regressions are green and the checklist is filled.
</success_criteria>

<output>
After completion, create `.planning/phases/41-linked-import-convergence/41-02-SUMMARY.md`
</output>
