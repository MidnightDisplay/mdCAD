---
phase: 12-solver-control-and-constrained-interaction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sketch_comp.h
  - src/ecs/ecs_scene.h
  - src/ui/ui_entity_inspector.h
autonomous: true
requirements:
  - SOLV-01
  - SOLV-02
  - SOLV-03
must_haves:
  truths:
    - "User can toggle auto-solve per sketch and manually trigger Recalculate from SketchManager."
    - "User can see current sketch solver status (`solved`, `loose`, `fixed`, `error`) and timestamped INFO/WARNING/ERROR diagnostics."
    - "User can see the single active solver backend type in solver controls with no backend-switch UX."
  artifacts:
    - path: "src/components/sketch_comp.h"
      provides: "Per-sketch solver policy/runtime metadata defaults including auto-solve ON"
      contains: "auto_solve_enabled"
    - path: "src/ecs/ecs_scene.h"
      provides: "Scene-level solver request/recalculate/diagnostics APIs with bounded rolling log"
      contains: "scene_solver_"
    - path: "src/ui/ui_entity_inspector.h"
      provides: "SketchManager solver controls and diagnostics panel UI"
      contains: "Recalculate Sketch"
  key_links:
    - from: "src/ui/ui_entity_inspector.h"
      to: "src/ecs/ecs_scene.h"
      via: "solver control actions call scene_solver APIs"
      pattern: "scene_solver_"
    - from: "src/ecs/ecs_scene.h"
      to: "src/components/sketch_comp.h"
      via: "per-sketch runtime/status/diagnostics state updates"
      pattern: "SketchComp|auto_solve|diagnostic"
---

<objective>
Implement the Phase 12 solver control foundation: per-sketch auto/manual solve controls, authoritative solver statuses, bounded diagnostics history, and fixed backend identity display.

Purpose: Satisfy `SOLV-01`, `SOLV-02`, and `SOLV-03` with scene-centric orchestration so UI and interaction code use one source of truth.
Output: Solver runtime contract in scene/component layers and SketchManager solver/diagnostics surfaces in inspector.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-solver-control-and-constrained-interaction/12-CONTEXT.md
@.planning/phases/12-solver-control-and-constrained-interaction/12-RESEARCH.md
@.planning/phases/12-solver-control-and-constrained-interaction/12-UI-SPEC.md
@.planning/phases/12-solver-control-and-constrained-interaction/12-VALIDATION.md
@.planning/phases/10-sketch-foundations-managers/10-03-SUMMARY.md
@.planning/phases/11-constraint-authoring-ux/11-02-SUMMARY.md
@.planning/phases/16-constraint-ux-closure-and-verification/16-01-SUMMARY.md

<interfaces>
From `src/components/sketch_comp.h`:
```c
typedef enum { SKETCH_STATUS_SOLVED, SKETCH_STATUS_LOOSE, SKETCH_STATUS_FIXED, SKETCH_STATUS_ERROR } sketch_status_t;
typedef struct { bool is_sketch; sketch_status_t status; uint32_t geometry_count; uint32_t fixed_geometry_count; uint32_t constraint_count; uint32_t color; } SketchComp;
```

From `src/ecs/ecs_scene.h`:
```c
static inline void scene_refresh_sketch_metadata(ecs_scene_t *scene, ecs_entity_t sketch);
static inline sketch_status_t scene_derive_sketch_status(ecs_scene_t *scene, ecs_entity_t sketch);
```

From `src/ui/ui_entity_inspector.h`:
```c
static inline void ui_entity_inspector_draw_single(ui_entity_inspector_state_t *state, ecs_entity_t e);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add per-sketch solver runtime contract and scene solver APIs</name>
  <read_first>
    - .planning/phases/12-solver-control-and-constrained-interaction/12-CONTEXT.md
    - .planning/phases/12-solver-control-and-constrained-interaction/12-RESEARCH.md
    - src/components/sketch_comp.h
    - src/ecs/ecs_scene.h
    - src/components/constraint_comp.h
  </read_first>
  <files>
    - src/components/sketch_comp.h
    - src/ecs/ecs_scene.h
  </files>
  <behavior>
    - Test 1: New sketch defaults to auto-solve enabled and supports manual recalc request path (D-01).
    - Test 2: Auto-solve request path debounces/coalesces sketch-affecting edits when auto-solve is ON (D-02).
    - Test 3: Diagnostics use per-sketch rolling history capped at exactly 100 entries and are not auto-cleared per solve (D-03, D-04, D-05).
    - Test 4: Scene exposes one fixed backend identity string/id for v1.2 read-only display (SOLV-03).
  </behavior>
  <action>Extend `SketchComp` and scene solver state in `src/components/sketch_comp.h` + `src/ecs/ecs_scene.h` to include `auto_solve_enabled` defaulting true (per D-01), solver backend identifier/name, last-solve timestamp, and diagnostics ring buffer with hard cap 100 (per D-03/D-04). Add explicit scene APIs for auto-trigger request, manual recalc, diagnostics append, and clear-history action where clear is user-invoked only (per D-05). Keep status authority solver-driven and preserve allowed status set (`solved`, `loose`, `fixed`, `error`) used by existing sketch UX.</action>
  <acceptance_criteria>
    - `grep -n "auto_solve_enabled" src/components/sketch_comp.h` returns at least one match.
    - `grep -n "100" src/ecs/ecs_scene.h` in solver diagnostics context confirms hard history cap is encoded.
    - `grep -n "scene_solver_" src/ecs/ecs_scene.h` returns scene-level solver API entrypoints (request/recalc/log/clear/back-end identity).
    - `grep -n "INFO\\|WARNING\\|ERROR" src/ecs/ecs_scene.h` confirms severity-level diagnostics contract is represented.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "auto_solve_enabled\|scene_solver_\|INFO\|WARNING\|ERROR" src/components/sketch_comp.h src/ecs/ecs_scene.h</automated>
  </verify>
  <done>Scene layer owns solver policy/status/diagnostics state and exposes reusable APIs required by UI and interaction paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement SketchManager solver controls and diagnostics panel</name>
  <read_first>
    - .planning/phases/12-solver-control-and-constrained-interaction/12-UI-SPEC.md
    - .planning/phases/12-solver-control-and-constrained-interaction/12-CONTEXT.md
    - src/ui/ui_entity_inspector.h
    - src/ecs/ecs_scene.h
    - src/components/sketch_comp.h
  </read_first>
  <files>
    - src/ui/ui_entity_inspector.h
  </files>
  <behavior>
    - Test 1: Solver controls show auto-solve toggle + `Recalculate Sketch` action and invoke scene APIs (SOLV-01, D-01).
    - Test 2: Sketch panel shows backend identity and status plus timestamped diagnostics rows with INFO/WARNING/ERROR markers (SOLV-02, SOLV-03).
    - Test 3: Diagnostics clear action is explicit user action with confirmation copy; history is not auto-cleared on new solves (D-05).
  </behavior>
  <action>Extend SketchManager block in `src/ui/ui_entity_inspector.h` with solver section that displays read-only backend identity, auto-solve toggle, and primary `Recalculate Sketch` action using UI-SPEC copy. Render current status and diagnostics history rows with timestamp + severity badge (`INFO`, `WARNING`, `ERROR`) and add explicit `Clear Diagnostics History` confirmation UX using UI-SPEC destructive text. Do not add any backend selection control (deferred out of scope); display active backend only, per SOLV-03 and deferred-idea guardrail.</action>
  <acceptance_criteria>
    - `grep -n "Recalculate Sketch" src/ui/ui_entity_inspector.h` returns the primary CTA string.
    - `grep -n "Clear Diagnostics History" src/ui/ui_entity_inspector.h` returns destructive confirmation/action text.
    - `grep -n "INFO\\|WARNING\\|ERROR" src/ui/ui_entity_inspector.h` returns diagnostics severity rendering.
    - `grep -n "backend" src/ui/ui_entity_inspector.h` shows read-only backend display and no backend-switch control labels/options.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "Recalculate Sketch\|Clear Diagnostics History\|INFO\|WARNING\|ERROR\|backend" src/ui/ui_entity_inspector.h</automated>
  </verify>
  <done>Users can control solving and inspect solver status/diagnostics directly in SketchManager with all locked decision constraints preserved.</done>
</task>

</tasks>

<verification>
Run build+test gate and verify solver-control strings/anchors exist:
- `cmake --build build-vulkan --config Release --target mdCAD`
- `ctest --test-dir build-vulkan -C Release --output-on-failure`
- `grep -n "Recalculate Sketch\\|Clear Diagnostics History" src/ui/ui_entity_inspector.h`
- `grep -n "scene_solver_" src/ecs/ecs_scene.h`
</verification>

<success_criteria>
- SOLV-01: Auto-solve toggle and manual recalc are implemented per sketch.
- SOLV-02: Solver statuses and timestamped INFO/WARNING/ERROR diagnostics are visible and bounded.
- SOLV-03: One active backend identity is displayed with no backend-switch UX.
</success_criteria>

<output>
After completion, create `.planning/phases/12-solver-control-and-constrained-interaction/12-01-SUMMARY.md`
</output>

