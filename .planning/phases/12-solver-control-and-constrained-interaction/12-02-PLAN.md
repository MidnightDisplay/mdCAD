---
phase: 12-solver-control-and-constrained-interaction
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - src/ecs/ecs_scene.h
  - src/app.c
  - src/gizmo/gizmo.h
autonomous: true
requirements:
  - SOLV-04
  - API-03
must_haves:
  truths:
    - "When solve fails or is invalid, implicated constraints and participant geometries are highlighted and first implicated constraint is focused."
    - "Failure implication highlighting persists until next successful solve."
    - "During gizmo drag, feasible constrained movement is projected in real time; unsatisfiable movement is blocked (not temporarily applied)."
    - "Unsatisfiable drag immediately shows feedback via viewport toast, diagnostics entry, and implication highlight."
  artifacts:
    - path: "src/ecs/ecs_scene.h"
      provides: "Failure implication payload extraction/persistence and drag feasibility APIs"
      contains: "scene_solver_can_apply_drag"
    - path: "src/app.c"
      provides: "Drag-loop integration for constrained projection/blocking and failure focus/highlight feedback"
      contains: "gizmo_update_drag"
    - path: "src/gizmo/gizmo.h"
      provides: "Constrained drag integration contract used by app drag loop"
      contains: "gizmo_update_drag"
  key_links:
    - from: "src/app.c"
      to: "src/ecs/ecs_scene.h"
      via: "drag update path calls solver feasibility/projection and failure handlers"
      pattern: "scene_solver_can_apply_drag|scene_solver_"
    - from: "src/ecs/ecs_scene.h"
      to: "src/constraints/constraint_selection.h"
      via: "failure implication uses shared participant highlighting helper path"
      pattern: "constraint_selection_apply_participants|participant"
---

<objective>
Implement failure implication behavior and constrained gizmo interaction so active constraints are enforced during manipulation and solver failures are actionable.

Purpose: Satisfy `SOLV-04` and `API-03` without regressing established Phase 16 participant-highlighting parity.
Output: Scene failure implication/drag-feasibility APIs plus app drag-loop wiring for projection/blocking + immediate feedback.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-solver-control-and-constrained-interaction/12-CONTEXT.md
@.planning/phases/12-solver-control-and-constrained-interaction/12-RESEARCH.md
@.planning/phases/12-solver-control-and-constrained-interaction/12-UI-SPEC.md
@.planning/phases/12-solver-control-and-constrained-interaction/12-VALIDATION.md
@.planning/phases/12-solver-control-and-constrained-interaction/12-01-SUMMARY.md
@.planning/phases/16-constraint-ux-closure-and-verification/16-01-SUMMARY.md
@src/constraints/constraint_selection.h

<interfaces>
From `src/constraints/constraint_selection.h`:
```c
static inline uint32_t constraint_selection_apply_participants(
    selection_buffer_t *selection,
    ecs_world_t *world,
    ecs_entity_t constraint_entity);
```

From `src/app.c`:
```c
vec3_t delta = gizmo_update_drag(&state.gizmo, mouse_ray);
```

From `src/undo_redo_exec.h`:
```c
static inline void undo_cmd_set_position(...);
static inline void undo_cmd_set_geometry_vertices(...);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add solver failure implication and constrained-drag feasibility APIs</name>
  <read_first>
    - .planning/phases/12-solver-control-and-constrained-interaction/12-CONTEXT.md
    - src/ecs/ecs_scene.h
    - src/constraints/constraint_selection.h
    - src/components/constraint_comp.h
    - src/components/sketch_comp.h
  </read_first>
  <files>
    - src/ecs/ecs_scene.h
  </files>
  <behavior>
    - Test 1: Failure result stores implicated constraints and exposes first-constraint focus target (D-06, D-07, SOLV-04).
    - Test 2: Implication highlight state persists across failed/invalid attempts and only clears on successful solve (D-08).
    - Test 3: Drag-feasibility API returns projected delta when satisfiable and explicit unsatisfiable outcome when blocked (D-09, D-10, API-03).
  </behavior>
  <action>In `src/ecs/ecs_scene.h`, add explicit solver-failure implication payload state (implicated constraint ids, primary focus id, participant entity visibility contract) and scene APIs to set/clear/query implication state. Ensure failure path retains highlights until the next successful solve (D-08), auto-focuses first implicated constraint (D-07), and includes both implicated constraints plus participants for highlighting (D-06). Add constrained-drag feasibility helper(s) returning either projected delta for feasible movement (D-09) or unsatisfiable block reason for rejected movement (D-10), and append diagnostics event payload suitable for D-11 feedback path.</action>
  <acceptance_criteria>
    - `grep -n "implicat" src/ecs/ecs_scene.h` returns implication payload/state helpers.
    - `grep -n "scene_solver_can_apply_drag\\|projected" src/ecs/ecs_scene.h` returns constrained drag feasibility API.
    - `grep -n "first" src/ecs/ecs_scene.h` in implication context shows primary focus first-constraint handling.
    - `grep -n "success" src/ecs/ecs_scene.h` in implication-clearing context shows clear-on-success behavior.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "implicat\|scene_solver_can_apply_drag\|projected\|first\|success" src/ecs/ecs_scene.h</automated>
  </verify>
  <done>Scene layer provides deterministic failure implication and constrained drag decision contracts consumable by app/UI code.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire gizmo drag loop to constrained projection/blocking with immediate failure UX</name>
  <read_first>
    - src/app.c
    - src/gizmo/gizmo.h
    - src/ecs/ecs_scene.h
    - src/constraints/constraint_selection.h
    - .planning/phases/12-solver-control-and-constrained-interaction/12-UI-SPEC.md
    - .planning/phases/12-solver-control-and-constrained-interaction/12-CONTEXT.md
  </read_first>
  <files>
    - src/app.c
    - src/gizmo/gizmo.h
  </files>
  <behavior>
    - Test 1: Drag update applies projected constrained delta when feasible, preserving current drag lifecycle and undo boundary behavior (D-09, API-03).
    - Test 2: Unsatisfiable drag does not move geometry and emits immediate toast + diagnostics entry + implication highlight (D-10, D-11).
    - Test 3: Failure path auto-selects first implicated constraint and highlights participants through shared helper parity path (D-06, D-07, D-08, SOLV-04).
  </behavior>
  <action>In `src/app.c` drag update path around `gizmo_update_drag`, replace unconditional delta application with scene solver feasibility call: if feasible, apply projected delta; if unsatisfiable, block movement entirely (no temporary invalid placement), emit viewport toast text `Movement blocked by active constraints.`, append diagnostics message `Drag rejected: active constraints make this move invalid.`, and apply implication highlight/focus using shared participant selection helper (`constraint_selection_apply_participants`) to preserve Phase 16 parity. Keep existing undo strategy at interaction boundaries (single command per completed drag), and avoid introducing per-frame undo commands.</action>
  <acceptance_criteria>
    - `grep -n "Movement blocked by active constraints\\." src/app.c` returns the toast copy string.
    - `grep -n "Drag rejected: active constraints make this move invalid\\." src/app.c` returns diagnostics message copy.
    - `grep -n "constraint_selection_apply_participants" src/app.c` confirms failure highlight uses shared helper.
    - `grep -n "scene_solver_can_apply_drag" src/app.c` confirms drag gating calls scene feasibility API before mutation.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "Movement blocked by active constraints\.\|Drag rejected: active constraints make this move invalid\.\|constraint_selection_apply_participants\|scene_solver_can_apply_drag" src/app.c</automated>
  </verify>
  <done>Constrained interaction respects active constraints in real time and failure states are immediately actionable through consistent highlight/diagnostics feedback.</done>
</task>

</tasks>

<verification>
Run gates and key contract greps:
- `cmake --build build-vulkan --config Release --target mdCAD`
- `ctest --test-dir build-vulkan -C Release --output-on-failure`
- `grep -n "Movement blocked by active constraints\\.|Drag rejected: active constraints make this move invalid\\." src/app.c`
- `grep -n "scene_solver_can_apply_drag\\|implicat" src/ecs/ecs_scene.h`
</verification>

<success_criteria>
- SOLV-04: Users can identify implicated constraints/geometries and first implicated constraint focus on failure.
- API-03: Gizmo manipulation applies feasible constrained motion and blocks unsatisfiable moves.
- D-11 path is complete: blocked drag emits toast + diagnostics + implication highlighting immediately.
</success_criteria>

<output>
After completion, create `.planning/phases/12-solver-control-and-constrained-interaction/12-02-SUMMARY.md`
</output>

