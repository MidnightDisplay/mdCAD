---
phase: 12-solver-control-and-constrained-interaction
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ecs/ecs_scene.h
autonomous: true
requirements:
  - SOLV-04
  - API-03
gap_closure: true
must_haves:
  truths:
    - "Failure implication highlighting persists across failure/invalid states and clears on the next successful solve."
    - "Manual recalculate and active solve completion paths both apply the same clear-on-success implication lifecycle."
  artifacts:
    - path: "src/ecs/ecs_scene.h"
      provides: "Unified solve-status application path that clears failure implication when status becomes solved"
      contains: "scene_solver_apply_status"
  key_links:
    - from: "scene_solver_request_recalculate"
      to: "scene_solver_apply_status"
      via: "recalculate completion status update call"
      pattern: "scene_solver_request_recalculate[\\s\\S]*scene_solver_apply_status"
    - from: "scene_solver_apply_status"
      to: "scene_solver_clear_failure_implication"
      via: "clear-on-solved lifecycle hook"
      pattern: "status == SKETCH_STATUS_SOLVED"
---

<objective>
Close the Phase 12 verification blocker by wiring clear-on-success failure implication lifecycle into the active solve/recalculate path.

Purpose: Ensure D-08 behavior is true in runtime code paths, not only in helper functions, so implication highlighting clears immediately after successful solve.
Output: `src/ecs/ecs_scene.h` status-update flow routes through `scene_solver_apply_status(...)` and includes a regression guard check for solved-state implication clear.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-solver-control-and-constrained-interaction/12-CONTEXT.md
@.planning/phases/12-solver-control-and-constrained-interaction/12-VALIDATION.md
@.planning/phases/12-solver-control-and-constrained-interaction/12-VERIFICATION.md
@.planning/phases/12-solver-control-and-constrained-interaction/12-02-SUMMARY.md
@src/ecs/ecs_scene.h
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire recalculate completion to solver status lifecycle hook</name>
  <read_first>
    - .planning/phases/12-solver-control-and-constrained-interaction/12-VERIFICATION.md
    - .planning/phases/12-solver-control-and-constrained-interaction/12-CONTEXT.md
    - src/ecs/ecs_scene.h
  </read_first>
  <files>
    - src/ecs/ecs_scene.h
  </files>
  <behavior>
    - Test 1: When recalculate computes `SKETCH_STATUS_SOLVED`, failure implication state is cleared via status lifecycle hook (D-08).
    - Test 2: Recalculate no longer writes `sk->status` directly; it routes through `scene_solver_apply_status(...)`.
    - Test 3: Non-solved statuses do not clear implication payload.
  </behavior>
  <action>Update `scene_solver_request_recalculate(...)` in `src/ecs/ecs_scene.h` to call `scene_solver_apply_status(scene, sketch, derived_status)` instead of assigning `sk->status` directly, explicitly implementing D-08 in the active solve path identified by 12-VERIFICATION gap. Keep existing solve timestamp/serial/backend updates unchanged. Do not add new solver backend switching or unrelated status heuristics.</action>
  <acceptance_criteria>
    - `scene_solver_request_recalculate(...)` contains a call to `scene_solver_apply_status(...)`.
    - Direct assignment pattern `sk->status = scene_derive_sketch_status(scene, sketch);` is removed from recalculate path.
    - `scene_solver_apply_status(...)` remains the single place that clears implication on `SKETCH_STATUS_SOLVED`.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "scene_solver_request_recalculate\|scene_solver_apply_status\|SKETCH_STATUS_SOLVED" src/ecs/ecs_scene.h</automated>
  </verify>
  <done>Active recalculate path uses the same clear-on-success implication lifecycle as solver status API contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression guard for solved-status implication clear behavior</name>
  <read_first>
    - .planning/phases/12-solver-control-and-constrained-interaction/12-VERIFICATION.md
    - .planning/phases/12-solver-control-and-constrained-interaction/12-VALIDATION.md
    - src/ecs/ecs_scene.h
  </read_first>
  <files>
    - src/ecs/ecs_scene.h
  </files>
  <behavior>
    - Test 1: A check/assertion path verifies implication state is cleared after applying solved status.
    - Test 2: Build and CTest gates still pass after lifecycle wiring changes.
  </behavior>
  <action>Add a lightweight internal regression guard in `src/ecs/ecs_scene.h` solve-status flow to validate that when status transitions to `SKETCH_STATUS_SOLVED`, `scene_solver_failure_implication(...)->active` is false for the same sketch context. Keep this guard non-invasive (debug assert or explicit condition check) and scoped to implication lifecycle only, aligning with 12-VERIFICATION missing item #2.</action>
  <acceptance_criteria>
    - File includes explicit solved-path implication-clear check near status application path.
    - Guard/check references `scene_solver_failure_implication` state after solved-status handling.
    - Native build and test gates pass.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "scene_solver_apply_status\|SKETCH_STATUS_SOLVED\|scene_solver_failure_implication\|assert" src/ecs/ecs_scene.h</automated>
  </verify>
  <done>A regression guard exists to prevent future reintroduction of solved-state implication-clear wiring gaps, and project gates remain green.</done>
</task>

</tasks>

<verification>
- `grep -n "scene_solver_request_recalculate\|scene_solver_apply_status\|SKETCH_STATUS_SOLVED" src/ecs/ecs_scene.h`
- `cmake --build build-vulkan --config Release --target mdCAD && ctest --test-dir build-vulkan -C Release --output-on-failure`
</verification>

<success_criteria>
- The failed verification truth from 12-VERIFICATION (“clear on next successful solve”) is now implementable via active solve path wiring.
- `scene_solver_request_recalculate` no longer bypasses solved-status lifecycle hooks.
- SOLV-04/API-03 failure implication behavior remains consistent with constrained interaction flows.
</success_criteria>

<output>
After completion, create `.planning/phases/12-solver-control-and-constrained-interaction/12-03-SUMMARY.md`
</output>
