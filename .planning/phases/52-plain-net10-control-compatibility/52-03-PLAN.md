---
phase: 52-plain-net10-control-compatibility
plan: 03
type: execute
wave: 3
depends_on:
  - 52-02
files_modified:
  - .planning/phases/52-plain-net10-control-compatibility/52-VALIDATION.md
  - samples/avalonia-host/AvaloniaHost.csproj
  - samples/avalonia-mdcad-control.tests/MdCad.Avalonia.Control.Tests.csproj
autonomous: true
requirements:
  - HOSTC-01
  - HOSTC-02
must_haves:
  truths:
    - "The existing control test lane remains green after the host-facing contract widens."
    - "The Windows diagnostic harness still builds against the widened control contract."
    - "Phase 52 closes with the compatibility fix proven without drifting into runtime-refresh automation, broader proof work, or docs changes."
  artifacts:
    - path: "samples/avalonia-mdcad-control.tests/MdCad.Avalonia.Control.Tests.csproj"
      provides: "Existing Windows-targeted control regression lane still referencing the widened control."
      contains: "ProjectReference Include=\"..\\avalonia-mdcad-control\\MdCad.Avalonia.Control.csproj\""
    - path: "samples/avalonia-host/AvaloniaHost.csproj"
      provides: "Existing Windows diagnostic harness consumer of the widened control contract."
      contains: "<TargetFramework>net10.0-windows10.0.19041.0</TargetFramework>"
    - path: ".planning/phases/52-plain-net10-control-compatibility/52-VALIDATION.md"
      provides: "Task 52-03-01 and phase sign-off aligned to the executed regression lane."
      contains: "52-03-01"
  key_links:
    - from: "samples/avalonia-mdcad-control.tests/MdCad.Avalonia.Control.Tests.csproj"
      to: "samples/avalonia-mdcad-control/MdCad.Avalonia.Control.csproj"
      via: "ProjectReference regression lane for the widened public contract"
      pattern: "ProjectReference Include=\"..\\\\avalonia-mdcad-control\\\\MdCad.Avalonia.Control.csproj\""
    - from: "samples/avalonia-host/AvaloniaHost.csproj"
      to: "samples/avalonia-mdcad-control/MdCad.Avalonia.Control.csproj"
      via: "Windows diagnostic harness still consuming the control after the retarget"
      pattern: "ProjectReference Include=\"..\\\\avalonia-mdcad-control\\\\MdCad.Avalonia.Control.csproj\""
    - from: ".planning/phases/52-plain-net10-control-compatibility/52-VALIDATION.md"
      to: "samples/avalonia-mdcad-control.tests/MdCad.Avalonia.Control.Tests.csproj"
      via: "full regression command remains the authoritative closeout lane"
      pattern: "dotnet test .*MdCad\\.Avalonia\\.Control\\.Tests\\.csproj.*dotnet build .*AvaloniaHost\\.csproj"
---

<objective>
Re-close the existing Windows regression lane after the compatibility widening lands.

Purpose: Phase 52 cannot weaken the already-locked Windows diagnostic/runtime truth. This plan revalidates the existing test and diagnostic consumers against the widened contract and records the result in the phase validation artifact.
Output: Green control tests, a green Windows diagnostic harness build, and aligned `52-VALIDATION.md` closeout for Task 52-03-01.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/52-plain-net10-control-compatibility/52-RESEARCH.md
@.planning/phases/52-plain-net10-control-compatibility/52-VALIDATION.md
@.planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-03-SUMMARY.md
@.planning/phases/51-unsupported-platform-contract/51-03-SUMMARY.md
@.planning/phases/52-plain-net10-control-compatibility/52-02-PLAN.md
@samples/avalonia-host/AvaloniaHost.csproj
@samples/avalonia-mdcad-control.tests/MdCad.Avalonia.Control.Tests.csproj

<interfaces>
From samples/avalonia-host/AvaloniaHost.csproj:
```xml
<PropertyGroup>
  <OutputType>WinExe</OutputType>
  <TargetFramework>net10.0-windows10.0.19041.0</TargetFramework>
  <ApplicationManifest>app.manifest</ApplicationManifest>
</PropertyGroup>

<ItemGroup>
  <ProjectReference Include="..\avalonia-mdcad-control\MdCad.Avalonia.Control.csproj" />
</ItemGroup>
```

From samples/avalonia-mdcad-control.tests/MdCad.Avalonia.Control.Tests.csproj:
```xml
<PropertyGroup>
  <TargetFramework>net10.0-windows10.0.19041.0</TargetFramework>
  <IsTestProject>true</IsTestProject>
</PropertyGroup>

<ItemGroup>
  <ProjectReference Include="..\avalonia-mdcad-control\MdCad.Avalonia.Control.csproj" />
</ItemGroup>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-close the control test lane and Windows diagnostic harness against the widened contract</name>
  <files>.planning/phases/52-plain-net10-control-compatibility/52-VALIDATION.md, samples/avalonia-host/AvaloniaHost.csproj, samples/avalonia-mdcad-control.tests/MdCad.Avalonia.Control.Tests.csproj</files>
  <action>Run the Phase 52 regression lane after Plans 01-02 land. Keep `samples/avalonia-mdcad-control.tests/MdCad.Avalonia.Control.Tests.csproj` and `samples/avalonia-host/AvaloniaHost.csproj` on their existing Windows-targeted consumer TFMs unless the widened plain-net10 control exposes a concrete compatibility/build blocker; if it does, make the smallest project-file correction in these consumers only so they still reference the control cleanly. Do not change runtime bundle paths, diagnostic harness behavior, post-build runtime-refresh automation, broader consumer proof scope, or docs wording. When the exact validation command passes, update `52-VALIDATION.md` row `52-03-01`, the wave/full-suite status, and the approval/sign-off fields so the phase ledger matches reality.</action>
  <verify>
    <automated>dotnet test .\samples\avalonia-mdcad-control.tests\MdCad.Avalonia.Control.Tests.csproj -c Release -v minimal && dotnet build .\samples\avalonia-host-minimal\AvaloniaHostMinimal.csproj -c Release && dotnet build .\samples\avalonia-host\AvaloniaHost.csproj -c Release</automated>
  </verify>
  <done>The control test suite passes, the Windows diagnostic harness builds against the widened contract, and `52-VALIDATION.md` records Task 52-03-01 plus phase closeout as green.</done>
</task>

</tasks>

<verification>
- `dotnet test .\samples\avalonia-mdcad-control.tests\MdCad.Avalonia.Control.Tests.csproj -c Release -v minimal && dotnet build .\samples\avalonia-host-minimal\AvaloniaHostMinimal.csproj -c Release && dotnet build .\samples\avalonia-host\AvaloniaHost.csproj -c Release`
- Confirm `samples/avalonia-host/AvaloniaHost.csproj` and `samples/avalonia-mdcad-control.tests/MdCad.Avalonia.Control.Tests.csproj` still reference `..\avalonia-mdcad-control\MdCad.Avalonia.Control.csproj`.
</verification>

<success_criteria>
- Existing control tests remain green after the contract widening.
- The Windows diagnostic harness still builds with no runtime-bundle contract drift.
- Phase 52 validation is aligned to the executed regression lane.
</success_criteria>

<output>
After completion, create `.planning/phases/52-plain-net10-control-compatibility/52-03-SUMMARY.md`
</output>
