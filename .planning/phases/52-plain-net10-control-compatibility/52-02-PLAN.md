---
phase: 52-plain-net10-control-compatibility
plan: 02
type: execute
wave: 2
depends_on:
  - 52-01
files_modified:
  - .planning/phases/52-plain-net10-control-compatibility/52-VALIDATION.md
  - samples/avalonia-host-minimal/App.axaml.cs
  - samples/avalonia-host-minimal/AvaloniaHostMinimal.csproj
  - samples/avalonia-host-minimal/MainWindow.axaml
  - samples/avalonia-host-minimal/MainWindow.axaml.cs
autonomous: false
requirements:
  - HOSTC-01
  - HOSTC-02
must_haves:
  truths:
    - "The smallest proof host can build as plain `net10.0` against the widened control contract."
    - "The proof host still instantiates `MdCadEmbeddedControl` from shared XAML/code rather than a platform-specific fork."
    - "Launching the proof host no longer crashes during startup or control construction."
  artifacts:
    - path: "samples/avalonia-host-minimal/AvaloniaHostMinimal.csproj"
      provides: "Plain-net10 proof host project that references the reusable control per D-01."
      contains: "<TargetFramework>net10.0</TargetFramework>"
    - path: "samples/avalonia-host-minimal/MainWindow.axaml"
      provides: "Shared XAML proof surface that instantiates `MdCadEmbeddedControl`."
      contains: "MdCadEmbeddedControl"
    - path: ".planning/phases/52-plain-net10-control-compatibility/52-VALIDATION.md"
      provides: "Task 52-02-01 status aligned to the proof-host build lane."
      contains: "52-02-01"
  key_links:
    - from: "samples/avalonia-host-minimal/AvaloniaHostMinimal.csproj"
      to: "samples/avalonia-mdcad-control/MdCad.Avalonia.Control.csproj"
      via: "ProjectReference from the D-01 proof host to the widened control contract"
      pattern: "ProjectReference Include=\"..\\\\avalonia-mdcad-control\\\\MdCad.Avalonia.Control.csproj\""
    - from: "samples/avalonia-host-minimal/MainWindow.axaml"
      to: "MdCad.Avalonia.Control"
      via: "shared XAML namespace import and direct control instantiation"
      pattern: "assembly=MdCad.Avalonia.Control|MdCadEmbeddedControl"
    - from: "samples/avalonia-host-minimal/App.axaml.cs"
      to: "samples/avalonia-host-minimal/MainWindow.axaml.cs"
      via: "desktop application bootstrap still opens the proof host window"
      pattern: "desktop.MainWindow = new MainWindow\\("
---

<objective>
Use the user-selected minimal host as the plain-net10 proof surface and smoke the shared control instantiation path.

Purpose: Per D-01 from the phase brief, `samples/avalonia-host-minimal` is the smallest acceptable proof host for HOSTC-01/HOSTC-02. This plan proves the widened contract is consumable from shared XAML/code without broadening Phase 53 proof scope.
Output: A plain-`net10.0` minimal host build, aligned validation for Task 52-02-01, and one manual startup checkpoint for the no-crash instantiation truth.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/52-plain-net10-control-compatibility/52-RESEARCH.md
@.planning/phases/52-plain-net10-control-compatibility/52-VALIDATION.md
@.planning/phases/51-unsupported-platform-contract/51-03-SUMMARY.md
@.planning/phases/52-plain-net10-control-compatibility/52-01-PLAN.md
@samples/avalonia-host-minimal/AvaloniaHostMinimal.csproj
@samples/avalonia-host-minimal/MainWindow.axaml
@samples/avalonia-host-minimal/MainWindow.axaml.cs
@samples/avalonia-host-minimal/App.axaml.cs

<interfaces>
From samples/avalonia-host-minimal/AvaloniaHostMinimal.csproj:
```xml
<PropertyGroup>
  <OutputType>WinExe</OutputType>
  <TargetFramework>net10.0-windows10.0.19041.0</TargetFramework>
  <ApplicationManifest>app.manifest</ApplicationManifest>
</PropertyGroup>

<ItemGroup>
  <ProjectReference Include="..\avalonia-mdcad-control\MdCad.Avalonia.Control.csproj" />
</ItemGroup>
```

From samples/avalonia-host-minimal/MainWindow.axaml:
```xml
<mdcad:MdCadEmbeddedControl PresentationMode="Sealed"
                            AutoStart="True"
                            JsonlPath="{Binding JsonlPath}" />
```

From samples/avalonia-host-minimal/App.axaml.cs:
```csharp
if (ApplicationLifetime is IClassicDesktopStyleApplicationLifetime desktop)
{
    desktop.MainWindow = new MainWindow();
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retarget the minimal proof host to plain net10 while keeping shared XAML control usage intact</name>
  <files>.planning/phases/52-plain-net10-control-compatibility/52-VALIDATION.md, samples/avalonia-host-minimal/App.axaml.cs, samples/avalonia-host-minimal/AvaloniaHostMinimal.csproj, samples/avalonia-host-minimal/MainWindow.axaml, samples/avalonia-host-minimal/MainWindow.axaml.cs</files>
  <action>Retarget `samples/avalonia-host-minimal/AvaloniaHostMinimal.csproj` to plain `net10.0` per D-01 so this sample becomes the smallest Phase 52 proof host. Keep the `ProjectReference` to `MdCad.Avalonia.Control` and preserve the existing shared XAML declaration of `MdCadEmbeddedControl` in `MainWindow.axaml`. If the plain-net10 host build exposes a host-only bootstrap issue, confine the fix to the files listed here and keep it minimal: adjust `OutputType`, `ApplicationManifest`, or bootstrap wiring only as required for the build/startup path to succeed. Do not add a new proof host, do not move work into Phase 53, and do not change the control's public API or the unsupported-platform messaging behavior from Phase 51. After the host builds, update `52-VALIDATION.md` row `52-02-01` from pending to green.</action>
  <verify>
    <automated>dotnet build .\samples\avalonia-host-minimal\AvaloniaHostMinimal.csproj -c Release</automated>
  </verify>
  <done>`samples/avalonia-host-minimal` builds as plain `net10.0`, still instantiates `MdCadEmbeddedControl` from shared XAML/code, and `52-VALIDATION.md` records Task 52-02-01 as green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manually smoke the plain-net10 proof host startup path</name>
  <action>Run the manual startup smoke from `52-VALIDATION.md` after Task 1 completes. This checkpoint is verification-only: do not broaden scope, do not add a new UI automation harness, and do not change code unless the user reports a concrete startup failure that must be fixed in a follow-up execution pass.</action>
  <what-built>Plain-net10 minimal host consumption of `MdCadEmbeddedControl` via the D-01 proof host.</what-built>
  <how-to-verify>
    1. On Windows, run `dotnet run --project .\samples\avalonia-host-minimal\AvaloniaHostMinimal.csproj -c Release`.
    2. Confirm the app opens without a startup crash or type-load failure and the embedded control instantiates successfully.
    3. If testing on an unsupported platform is available, confirm the app still opens and the control shows the existing unsupported-platform presentation instead of crashing.
  </how-to-verify>
  <verify>
    <automated>dotnet build .\samples\avalonia-host-minimal\AvaloniaHostMinimal.csproj -c Release</automated>
  </verify>
  <done>The user confirms the startup smoke passes, or returns a concrete failure description that can drive a narrow follow-up fix.</done>
  <resume-signal>Type "approved" if the startup smoke passes, or describe the crash/issue that still needs fixing.</resume-signal>
</task>

</tasks>

<verification>
- `dotnet build .\samples\avalonia-host-minimal\AvaloniaHostMinimal.csproj -c Release`
- Manual smoke per `52-VALIDATION.md`: `dotnet run --project .\samples\avalonia-host-minimal\AvaloniaHostMinimal.csproj -c Release`
</verification>

<success_criteria>
- `samples/avalonia-host-minimal` is the plain-`net10.0` proof host per D-01.
- Shared XAML/code instantiation of `MdCadEmbeddedControl` survives the retarget.
- Manual startup smoke confirms no startup/type-load crash.
</success_criteria>

<output>
After completion, create `.planning/phases/52-plain-net10-control-compatibility/52-02-SUMMARY.md`
</output>
