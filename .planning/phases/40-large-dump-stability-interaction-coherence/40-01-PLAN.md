---
phase: 40-large-dump-stability-interaction-coherence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/jsonl_observer_system.h
  - src/ui/ui_entity_inspector.h
  - src/tests/jsonl_flat_observer_auto_safety_test.c
autonomous: true
requirements: [PERF-01]
must_haves:
  truths:
    - "Bursty source updates do not create unbounded refresh queues or refresh storms."
    - "Large flat refresh work remains operational (no lockup/crash) while updates are being coalesced."
    - "Timing evidence is visible as advisory diagnostics and never blocks refresh completion."
  artifacts:
    - path: "src/jsonl_observer_system.h"
      provides: "Single-flight + one coalesced pending rerun policy with advisory timing capture"
      exports: ["jsonl_observer_request_flat_refresh", "jsonl_observer_tick_flat_refreshes", "jsonl_observer_is_flat_refresh_running"]
    - path: "src/ui/ui_entity_inspector.h"
      provides: "Inspector advisory timing/status surface for flat observer refresh"
      contains: "Last refresh result"
    - path: "src/tests/jsonl_flat_observer_auto_safety_test.c"
      provides: "Deterministic burst-coalescing and advisory timing regression coverage"
      contains: "coalesced pending rerun"
  key_links:
    - from: "src/jsonl_observer_system.h"
      to: "src/jsonl_import_job.h"
      via: "single active slot + pending rerun replay"
      pattern: "jsonl_import_job_(start|tick)"
    - from: "src/jsonl_observer_system.h"
      to: "src/ui/ui_entity_inspector.h"
      via: "observer messages/timing diagnostics"
      pattern: "status_message|last_iteration_time_ms"
---

<objective>
Harden large-dump flat refresh runtime behavior so burst updates stay bounded and performance evidence is captured without introducing hard SLA failure gates.

Purpose: Satisfy PERF-01 with D-01/D-02/D-03/D-04 and preserve phase-39 observer safety guarantees.
Output: Coalesced rerun runtime policy, advisory timing surfacing, and deterministic coalescing regressions.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-large-dump-stability-interaction-coherence/40-CONTEXT.md
@.planning/phases/40-large-dump-stability-interaction-coherence/40-RESEARCH.md
@.planning/phases/40-large-dump-stability-interaction-coherence/40-VALIDATION.md
@.planning/phases/38-observable-link-manual-transactional-refresh/38-02-SUMMARY.md
@.planning/phases/39-automatic-observer-safety-loop/39-02-SUMMARY.md
@src/jsonl_observer_system.h
@src/ui/ui_entity_inspector.h
@src/tests/jsonl_flat_observer_auto_safety_test.c

<interfaces>
From `src/jsonl_observer_system.h`:
```c
bool jsonl_observer_request_flat_refresh(ecs_scene_t *scene, ecs_entity_t root_entity, selection_buffer_t *selection);
bool jsonl_observer_is_flat_refresh_running(ecs_scene_t *scene, ecs_entity_t root_entity);
void jsonl_observer_tick_flat_refreshes(ecs_scene_t *scene);
```

From `src/components/jsonl_observer_comp.h`:
```c
typedef struct {
    bool linked;
    bool observe_enabled;
    uint32_t retry_count;
    uint32_t max_retries;
    char status_message[JSONL_OBSERVER_STATUS_MAX];
} JsonlObserverComp;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement single-flight + coalesced pending rerun slot policy</name>
  <read_first>
    - .planning/phases/40-large-dump-stability-interaction-coherence/40-CONTEXT.md
    - .planning/phases/40-large-dump-stability-interaction-coherence/40-RESEARCH.md
    - src/jsonl_observer_system.h
    - src/tests/jsonl_flat_observer_auto_safety_test.c
  </read_first>
  <files>src/jsonl_observer_system.h</files>
  <action>Extend flat refresh slot state to track exactly one `pending_rerun` per root and consume it immediately after active completion. When a request arrives during active refresh, set `pending_rerun=true` (D-03) instead of dropping silently or allocating another slot; reject any third+ request while pending is already set (D-04). Keep queue depth bounded to one active + one pending rerun (D-04) and preserve existing transactional commit/rollback semantics from phase 38. Keep the hard gate behavior focused on stability (D-01).</action>
  <acceptance_criteria>Repeated request bursts while one refresh is active produce at most one follow-up rerun and never allocate unbounded queue state.</acceptance_criteria>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_auto_safety_test" --output-on-failure</automated>
  </verify>
  <done>Runtime enforces bounded coalescing policy with no queue-growth path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add advisory timing capture and non-gating inspector messaging</name>
  <read_first>
    - .planning/phases/40-large-dump-stability-interaction-coherence/40-CONTEXT.md
    - src/jsonl_observer_system.h
    - src/ui/ui_entity_inspector.h
    - src/tests/jsonl_flat_observer_inspector_contract_test.c
  </read_first>
  <files>src/jsonl_observer_system.h, src/ui/ui_entity_inspector.h</files>
  <action>Capture flat refresh elapsed timing per run and append advisory warning/status text without failing refresh completion (D-02, D-10). Surface the latest timing result in flat observer inspector status so manual stress runs can record evidence. Keep messaging additive and advisory only; do not introduce hard numeric pass/fail checks (deferred idea guard + D-10).</action>
  <acceptance_criteria>Inspector shows advisory timing/results for flat refresh runs and refresh success/failure semantics remain transactional and non-SLA-gated.</acceptance_criteria>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_inspector_contract_test" --output-on-failure</automated>
  </verify>
  <done>Timing evidence is available for PERF-01 without introducing new hard failure gates.</done>
</task>

<task type="auto">
  <name>Task 3: Lock burst coalescing and advisory timing behavior in deterministic tests</name>
  <read_first>
    - src/tests/jsonl_flat_observer_auto_safety_test.c
    - src/jsonl_observer_system.h
    - .planning/phases/40-large-dump-stability-interaction-coherence/40-VALIDATION.md
  </read_first>
  <files>src/tests/jsonl_flat_observer_auto_safety_test.c</files>
  <action>Add explicit tests that issue burst refresh triggers against one root and assert one active run + at most one coalesced rerun (D-03/D-04). Add assertions that timing/status diagnostics are emitted as evidence while refresh completion remains behaviorally gated by no-lock/no-crash semantics (D-01/D-02/D-10). Keep fixture sizes deterministic (small/medium/large tiers) per D-08.</action>
  <acceptance_criteria>Targeted auto-safety test fails if coalescing exceeds one pending rerun or if advisory timing is treated as hard failure.</acceptance_criteria>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_auto_safety_test" --output-on-failure</automated>
  </verify>
  <done>PERF-01 burst scheduling and advisory-gate behavior are enforceable by deterministic tests.</done>
</task>

</tasks>

<verification>
Run:
- `ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_auto_safety_test" --output-on-failure`
- `ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_(auto_safety_test|manual_refresh_test|inspector_contract_test)" --output-on-failure`
</verification>

<success_criteria>
- Flat observer refresh scheduling is bounded to single-flight + one coalesced rerun.
- Large-run timing evidence is visible but advisory-only.
- PERF-01 automated slice is green.
</success_criteria>

<output>
After completion, create `.planning/phases/40-large-dump-stability-interaction-coherence/40-01-SUMMARY.md`
</output>
