---
phase: 40-large-dump-stability-interaction-coherence
plan: 02
type: execute
wave: 2
depends_on: [40-01]
files_modified:
  - src/tests/jsonl_flat_observer_manual_refresh_test.c
  - src/tests/jsonl_flat_observer_inspector_contract_test.c
  - src/ui/ui_scene_hierarchy.h
  - .planning/phases/40-large-dump-stability-interaction-coherence/40-STRESS-PROTOCOL.md
autonomous: false
requirements: [PERF-03, PERF-01]
must_haves:
  truths:
    - "Across repeated refresh cycles, root anchor remains the stable interaction pivot."
    - "When selected descendants are replaced, selection remaps to root anchor instead of clearing."
    - "Hierarchy visibility/expanded interaction and inspector continuity remain coherent while refresh repeats."
    - "Manual very-large stress pass evidence is captured with advisory timing logs."
  artifacts:
    - path: "src/tests/jsonl_flat_observer_manual_refresh_test.c"
      provides: "Repeated-cycle coherence assertions for root stability and selection remap"
      contains: "selection_get(&selection, 0) != root"
    - path: "src/tests/jsonl_flat_observer_inspector_contract_test.c"
      provides: "Inspector continuity contract checks for anchor workflow"
      contains: "Last refresh result"
    - path: "src/ui/ui_scene_hierarchy.h"
      provides: "Hierarchy interaction continuity safeguards during repeated flat refresh updates"
      contains: "ui_scene_hierarchy_draw_entity_tree"
    - path: ".planning/phases/40-large-dump-stability-interaction-coherence/40-STRESS-PROTOCOL.md"
      provides: "Manual very-large stress execution/evidence template"
      contains: "Very-large JSONL"
  key_links:
    - from: "src/jsonl_observer_system.h"
      to: "src/selection.h"
      via: "descendant replacement remaps selection to root anchor"
      pattern: "selection_set_single"
    - from: "src/ui/ui_scene_hierarchy.h"
      to: "src/jsonl_observer_system.h"
      via: "refresh-running coherence in hierarchy interaction"
      pattern: "jsonl_observer_is_flat_refresh_running"
    - from: "src/tests/jsonl_flat_observer_manual_refresh_test.c"
      to: "src/jsonl_observer_system.h"
      via: "repeated refresh loop assertions"
      pattern: "jsonl_observer_request_flat_refresh"
---

<objective>
Prove and harden anchor-stable interaction coherence during repeated large refresh cycles, then collect required manual very-large stress evidence.

Purpose: Satisfy PERF-03 (and finalize PERF-01 evidence) under D-05/D-06/D-07/D-08/D-09/D-10.
Output: Repeated-cycle coherence regressions, hierarchy continuity guard, and manual stress protocol/evidence checkpoint.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-large-dump-stability-interaction-coherence/40-CONTEXT.md
@.planning/phases/40-large-dump-stability-interaction-coherence/40-RESEARCH.md
@.planning/phases/40-large-dump-stability-interaction-coherence/40-VALIDATION.md
@.planning/phases/40-large-dump-stability-interaction-coherence/40-01-PLAN.md
@src/jsonl_observer_system.h
@src/ui/ui_scene_hierarchy.h
@src/ui/ui_entity_inspector.h
@src/tests/jsonl_flat_observer_manual_refresh_test.c
@src/tests/jsonl_flat_observer_inspector_contract_test.c

<interfaces>
From `src/selection.h`:
```c
static inline int selection_count(selection_buffer_t *sel);
static inline ecs_entity_t selection_get(selection_buffer_t *sel, int index);
static inline void selection_set_single(selection_buffer_t *sel, ecs_entity_t e);
```

From `src/ui/ui_entity_inspector.h`:
```c
static inline void ui_entity_inspector_draw_jsonl_flat_observer_controls(ui_entity_inspector_state_t *state, ecs_entity_t root_entity, JsonlObserverComp *observer);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add repeated-cycle anchor coherence regression coverage</name>
  <read_first>
    - .planning/phases/40-large-dump-stability-interaction-coherence/40-CONTEXT.md
    - src/tests/jsonl_flat_observer_manual_refresh_test.c
    - src/tests/jsonl_flat_observer_inspector_contract_test.c
    - src/jsonl_observer_system.h
  </read_first>
  <files>src/tests/jsonl_flat_observer_manual_refresh_test.c, src/tests/jsonl_flat_observer_inspector_contract_test.c</files>
  <action>Add deterministic repeated-refresh loops (small/medium/large fixtures) that assert root entity identity remains stable (D-05), replaced-child selection remaps to root each cycle (D-06), and inspector contract strings required for anchor continuity remain present through refresh-status evolution (D-07). Keep tests deterministic and bounded per D-08.</action>
  <acceptance_criteria>Tests fail on any regression in root stability, descendant-to-root remap, or inspector continuity across repeated refresh cycles.</acceptance_criteria>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_(manual_refresh_test|inspector_contract_test)" --output-on-failure</automated>
  </verify>
  <done>PERF-03 coherence behavior is protected by repeated-cycle automated tests.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve hierarchy interaction continuity for anchor workflow during refresh cycles</name>
  <read_first>
    - src/ui/ui_scene_hierarchy.h
    - src/jsonl_observer_system.h
    - src/tests/jsonl_flat_observer_manual_refresh_test.c
  </read_first>
  <files>src/ui/ui_scene_hierarchy.h, .planning/phases/40-large-dump-stability-interaction-coherence/40-STRESS-PROTOCOL.md</files>
  <action>Harden hierarchy draw/cache behavior so repeated flat refresh replacements do not collapse or detach the stable root-anchor interaction path (D-07), while keeping root selection pivot policy unchanged (D-05). In the same task, create `40-STRESS-PROTOCOL.md` with concrete steps, dataset characteristics, evidence fields (timings + responsiveness notes), and pass/fail interpretation aligned to hybrid gate policy (D-01/D-02/D-09/D-10).</action>
  <acceptance_criteria>Hierarchy remains usable around the same root anchor through repeated refresh; stress protocol exists and explicitly records advisory timings and stability outcomes.</acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; p=Path('.planning/phases/40-large-dump-stability-interaction-coherence/40-STRESS-PROTOCOL.md'); t=p.read_text(encoding='utf-8'); assert 'Very-large JSONL' in t and 'advisory' in t.lower() and 'no lockup/crash' in t.lower()"</automated>
  </verify>
  <done>Anchor workflow coherence has explicit hierarchy guardrails and executable manual stress protocol artifact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Execute and record one manual very-large stress pass</name>
  <read_first>
    - .planning/phases/40-large-dump-stability-interaction-coherence/40-STRESS-PROTOCOL.md
    - src/ui/ui_entity_inspector.h
  </read_first>
  <files>.planning/phases/40-large-dump-stability-interaction-coherence/40-STRESS-PROTOCOL.md</files>
  <what-built>Repeated-cycle coherence guards and protocol for very-large manual stress evidence.</what-built>
  <action>Run one manual stress pass using the protocol: import a very-large JSONL file, trigger repeated source updates/refreshes, keep anchor workflow active in hierarchy + inspector, and capture observed responsiveness plus advisory timing logs (D-09). Evaluate with hybrid gate: no lockup/crash is required pass; timing is recorded as evidence-only warning channel (D-01/D-02/D-10).</action>
  <how-to-verify>
    1) Launch mdCAD native build and import the very-large flat JSONL dataset.
    2) Trigger repeated refresh cycles while root anchor remains selected/visible.
    3) Confirm no crash/hard lock; record any stalls and recovery behavior.
    4) Copy advisory timing/status evidence into `40-STRESS-PROTOCOL.md` results section.
  </how-to-verify>
  <acceptance_criteria>One manual stress run is documented with stability verdict (hard gate) and timing evidence (advisory), and any issues are explicitly listed for follow-up.</acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; t=Path('.planning/phases/40-large-dump-stability-interaction-coherence/40-STRESS-PROTOCOL.md').read_text(encoding='utf-8'); assert 'Manual run result:' in t and 'TBD' not in t"</automated>
  </verify>
  <done>Manual very-large run evidence is recorded and checkpoint status is acknowledged by user.</done>
  <resume-signal>Type "approved" after evidence is recorded, or provide failure notes for follow-up fixes.</resume-signal>
</task>

</tasks>

<verification>
Run:
- `ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_(manual_refresh_test|inspector_contract_test|auto_safety_test)" --output-on-failure`
- Manual checkpoint evidence completed in `40-STRESS-PROTOCOL.md`
</verification>

<success_criteria>
- PERF-03 automated regressions prove anchor-stable coherence over repeated refreshes.
- Hierarchy + inspector continuity is preserved for anchor workflow.
- D-09 manual very-large stress evidence is captured using hybrid gate semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/40-large-dump-stability-interaction-coherence/40-02-SUMMARY.md`
</output>
