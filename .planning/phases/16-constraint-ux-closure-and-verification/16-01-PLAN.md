---
phase: 16-constraint-ux-closure-and-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/constraints/constraint_selection.h
  - src/app.c
  - src/ui/ui_entity_inspector.h
autonomous: true
requirements: [SKCH-04, CONS-03]
gap_closure: true
must_haves:
  truths:
    - "Clicking a constraint glyph highlights all alive participant geometry on the same frame."
    - "Selecting a ConstraintManager row and clicking a glyph produce the same participant highlight set."
    - "Dimensional glyph double-click still opens the dimension popup while participant highlighting remains active."
  artifacts:
    - path: "src/constraints/constraint_selection.h"
      provides: "Shared helper for selecting all constraint participants into selection buffer"
      contains: "constraint_selection_apply_participants"
    - path: "src/app.c"
      provides: "Glyph click routing that uses shared participant selection helper"
      contains: "constraint_selection_apply_participants"
    - path: "src/ui/ui_entity_inspector.h"
      provides: "ConstraintManager row selection path using same helper as glyph flow"
      contains: "constraint_selection_apply_participants"
  key_links:
    - from: "src/app.c"
      to: "src/constraints/constraint_selection.h"
      via: "glyph click handler"
      pattern: "constraint_selection_apply_participants"
    - from: "src/ui/ui_entity_inspector.h"
      to: "src/constraints/constraint_selection.h"
      via: "ConstraintManager row click"
      pattern: "constraint_selection_apply_participants"
---

<objective>
Unify constraint participant highlighting so viewport glyph selection and ConstraintManager row selection behave identically for SKCH-04/CONS-03 closure.

Purpose: Close the 10→11 integration gap where glyph click selected only the constraint entity instead of all participants.
Output: Shared participant-selection helper and consistent call sites in viewport + inspector paths.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-constraint-ux-closure-and-verification/16-RESEARCH.md
@.planning/phases/16-constraint-ux-closure-and-verification/16-UI-SPEC.md
@src/app.c
@src/ui/ui_entity_inspector.h
@src/selection.h

<interfaces>
From `src/selection.h`:
```c
static inline void selection_clear(selection_buffer_t *sel);
static inline void selection_add(selection_buffer_t *sel, ecs_entity_t e);
static inline void selection_set_single(selection_buffer_t *sel, ecs_entity_t e);
```

From `src/components/constraint_comp.h` (consumed via ECS getters):
```c
typedef struct {
    uint32_t participant_count;
    uint64_t participants[8];
    ConstraintType type;
    float value;
    bool driven;
} ConstraintComp;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared constraint participant selection helper</name>
  <read_first>
    - src/selection.h
    - src/components/constraint_comp.h
    - src/ecs/ecs_world.h
  </read_first>
  <files>
    - src/constraints/constraint_selection.h
  </files>
  <action>Create `constraint_selection_apply_participants(...)` as a header-only helper that clears selection then adds every alive participant entity from the target constraint (SKCH-04, CONS-03). Include null/invalid guards and return a deterministic status (e.g., participant count added) so call sites can branch safely. Do not create glyph-specific logic in this helper; it must be reusable by both inspector and viewport paths.</action>
  <acceptance_criteria>Helper exists, compiles, and encapsulates participant highlighting semantics in one place with no duplicated row/glyph loops.</acceptance_criteria>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD</automated>
  </verify>
  <done>Shared helper is callable from both app and inspector code without changing rendering/pick subsystems.</done>
</task>

<task type="auto">
  <name>Task 2: Route glyph and manager selection through shared helper</name>
  <read_first>
    - src/app.c
    - src/ui/ui_entity_inspector.h
    - .planning/phases/16-constraint-ux-closure-and-verification/16-UI-SPEC.md
  </read_first>
  <files>
    - src/app.c
    - src/ui/ui_entity_inspector.h
  </files>
  <action>Replace manual participant-loop logic in ConstraintManager row select and glyph click `selection_set_single(clicked_constraint)` path with the new shared helper (SKCH-04, CONS-03). Preserve `state.selected_constraint_entity` and dimensional popup behavior on double-click. Keep keybinding behavior unchanged (`C` opens constraint menu, `Tab` remains gizmo mode toggle) per UI-SPEC contract. Ensure no legacy path leaves constraint-entity-only selection on glyph click.</action>
  <acceptance_criteria>Both entrypoints call the same helper; glyph click immediately highlights all participants while dimensional popup still opens for LENGTH/ANGLE double-click.</acceptance_criteria>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD && ctest --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>Participant highlighting is consistent between viewport and manager, satisfying the integration gap closure target.</done>
</task>

</tasks>

<verification>
Run Windows MSVC+Vulkan gate commands and verify no build/test regressions:
- `cmake --build build-vulkan --config Release --target mdCAD`
- `ctest --test-dir build-vulkan -C Release --output-on-failure`
</verification>

<success_criteria>
- Glyph click no longer selects only constraint entity; it highlights all alive participants immediately.
- ConstraintManager and glyph flows share one participant-selection implementation path.
- Build/test gates pass on `build-vulkan` after refactor.
</success_criteria>

<output>
After completion, create `.planning/phases/16-constraint-ux-closure-and-verification/16-01-SUMMARY.md`
</output>

