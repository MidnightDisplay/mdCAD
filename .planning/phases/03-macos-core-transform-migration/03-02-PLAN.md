---
phase: 03-macos-core-transform-migration
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/components/transform_comp.h
  - src/ecs/ecs_scene.h
  - src/math_harness.c
autonomous: true
requirements:
  - HOT-02
user_setup: []
must_haves:
  truths:
    - "`TransformComp` local/world matrix composition is computed by `cglm` while cached storage remains compatible with existing render consumers"
    - "Dense world-point application in `src/ecs/ecs_scene.h` no longer relies on scattered direct `mat4_transform_point(...)` calls"
    - "Harness transform comparisons validate the live migrated production helpers against frozen legacy baselines"
  artifacts:
    - path: "src/components/transform_comp.h"
      provides: "cglm-backed transform composition with stable cached storage"
      contains: "glm_translate_make"
    - path: "src/ecs/ecs_scene.h"
      provides: "centralized cglm-backed world-point helper"
      contains: "ecs_scene_transform_point_world"
    - path: "src/math_harness.c"
      provides: "production-backed transform parity checks"
      contains: "hierarchy-world-transform"
  key_links:
    - from: "src/components/transform_comp.h"
      to: "vendors/cglm/include/cglm/affine.h"
      via: "transform composition uses raw `cglm` affine helpers with the exact `T * Rz * Ry * Rx * S` order"
      pattern: "glm_translate_make|glm_rotate_x|glm_rotate_y|glm_rotate_z|glm_scale_make"
    - from: "src/ecs/ecs_scene.h"
      to: "vendors/cglm/include/cglm/mat4.h"
      via: "world-point application uses `glm_mat4_mulv3(...)` through one shared helper"
      pattern: "glm_mat4_mulv3"
    - from: "src/math_harness.c"
      to: "src/components/transform_comp.h"
      via: "the harness validates the migrated production transform helpers"
      pattern: "transform_comp_compute_local"
---

<objective>
Migrate ECS transform composition and world-matrix-dependent scene update/render inputs to the new math foundation without widening into a broad storage rewrite.

Purpose: `HOT-02` requires `TransformComp` and render-matrix-dependent scene math to move to `cglm` while the visible macOS scene output remains stable.
Output: cglm-backed transform composition, a centralized ECS world-point helper, and production-backed transform parity checks in the harness.
</objective>

<execution_context>
@/Users/rodionradchenko/Documents/repos/mdCAD/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rodionradchenko/Documents/repos/mdCAD/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-macos-core-transform-migration/03-CONTEXT.md
@.planning/phases/03-macos-core-transform-migration/03-RESEARCH.md
@.planning/phases/03-macos-core-transform-migration/03-VALIDATION.md
@.planning/phases/03-macos-core-transform-migration/03-01-PLAN.md
@docs/MATH_CONVENTIONS.md
@src/components/transform_comp.h
@src/ecs/ecs_scene.h
@src/gpu/geometry_batch.h
@src/math_harness.c
@src/math3d.h
@vendors/cglm/include/cglm/affine.h
@vendors/cglm/include/cglm/mat4.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate `TransformComp` composition to raw `cglm` helpers while preserving cached storage</name>
  <files>src/components/transform_comp.h</files>
  <read_first>src/components/transform_comp.h, src/math3d.h, docs/MATH_CONVENTIONS.md, .planning/phases/03-macos-core-transform-migration/03-CONTEXT.md, .planning/phases/03-macos-core-transform-migration/03-RESEARCH.md, vendors/cglm/include/cglm/affine.h, vendors/cglm/include/cglm/mat4.h</read_first>
  <action>Update `src/components/transform_comp.h` so local/world matrix computation is backed by the `cglm` array API while cached field types remain unchanged. Include `"../math/cglm_entry.h"` plus `<string.h>`. Add narrow bridge helpers that copy between `mat4_t` and temporary `cglm` `mat4` buffers with `memcpy` rather than changing `TransformComp.local_matrix` or `TransformComp.world_matrix` field types. Reimplement `transform_comp_compute_local(...)` with `glm_translate_make`, `glm_rotate_x`, `glm_rotate_y`, `glm_rotate_z`, `glm_scale_make`, and `glm_mat4_mul` in the exact order `T * Rz * Ry * Rx * S`. Reimplement the parent-child multiply in `transform_comp_update_with_parent(...)` with `glm_mat4_mul(...)` on temporary `cglm` matrices, then copy the result back into `t->world_matrix`. Keep `position`, `rotation`, `scale`, `local_matrix`, and `world_matrix` field types unchanged in Phase 3.</action>
  <verify>
    <automated>rg -n "glm_translate_make|glm_rotate_x|glm_rotate_y|glm_rotate_z|glm_scale_make|glm_mat4_mul|memcpy" src/components/transform_comp.h</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/transform_comp.h` contains `glm_translate_make`
    - `src/components/transform_comp.h` contains `glm_rotate_x`
    - `src/components/transform_comp.h` contains `glm_rotate_y`
    - `src/components/transform_comp.h` contains `glm_rotate_z`
    - `src/components/transform_comp.h` contains `glm_scale_make`
    - `src/components/transform_comp.h` contains `glm_mat4_mul`
    - `src/components/transform_comp.h` contains `memcpy`
    - `src/components/transform_comp.h` still contains `mat4_t local_matrix`
    - `src/components/transform_comp.h` still contains `mat4_t world_matrix`
  </acceptance_criteria>
  <done>`TransformComp` now computes local/world matrices through `cglm` without forcing a wider Phase 3 storage rewrite.</done>
</task>

<task type="auto">
  <name>Task 2: Centralize cglm-backed world-point application in ECS scene sync and harness coverage</name>
  <files>src/ecs/ecs_scene.h, src/math_harness.c</files>
  <read_first>src/ecs/ecs_scene.h, src/math_harness.c, src/components/transform_comp.h, src/gpu/geometry_batch.h, src/math3d.h, .planning/phases/03-macos-core-transform-migration/03-CONTEXT.md, .planning/phases/03-macos-core-transform-migration/03-RESEARCH.md, vendors/cglm/include/cglm/mat4.h</read_first>
  <action>In `src/ecs/ecs_scene.h`, add one helper named `ecs_scene_transform_point_world(const mat4_t *world_matrix, vec3_t local_point)` near the transform/render helpers. That helper must load the cached `mat4_t` into a temporary `cglm` `mat4`, call `glm_mat4_mulv3(..., 1.0f, ...)`, and return the resulting `vec3_t`. Replace the repeated `mat4_transform_point(t->world_matrix, ...)` call sites in scene update, render upload, light collection, and pick-population paths with `ecs_scene_transform_point_world(&t->world_matrix, ...)` so Phase 3 does not leave a fragmented mixed path. In `src/math_harness.c`, keep `transform-compose` but make its production side call the live migrated transform helpers, and add a new compare identifier `hierarchy-world-transform` that constructs parent/child `TransformComp` values, updates them through `transform_comp_update_with_parent(...)`, transforms representative sample points through `ecs_scene_transform_point_world(...)`, and compares the results against a frozen legacy baseline built with `mat4_mul(...)` and `mat4_transform_point(...)`.</action>
  <verify>
    <automated>cmake -B build -G Ninja && ninja -C build mdcad_math_harness && ./build/bin/mdcad_math_harness --mode compare --strict | rg -n "^COMPARE PASS (transform-compose|hierarchy-world-transform)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/ecs/ecs_scene.h` contains `ecs_scene_transform_point_world`
    - `src/ecs/ecs_scene.h` contains `glm_mat4_mulv3`
    - `rg -n "mat4_transform_point\\(t->world_matrix" src/ecs/ecs_scene.h` returns no matches
    - `src/math_harness.c` contains `hierarchy-world-transform`
    - `src/math_harness.c` contains `transform_comp_update_with_parent`
    - `./build/bin/mdcad_math_harness --list` prints `hierarchy-world-transform`
    - `./build/bin/mdcad_math_harness --mode compare --strict` prints `COMPARE PASS transform-compose`
    - `./build/bin/mdcad_math_harness --mode compare --strict` prints `COMPARE PASS hierarchy-world-transform`
  </acceptance_criteria>
  <done>ECS world-matrix application now runs through one cglm-backed helper, and the harness validates both single-transform and parent-child parity.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "glm_translate_make|glm_mat4_mul|ecs_scene_transform_point_world|glm_mat4_mulv3" src/components/transform_comp.h src/ecs/ecs_scene.h` succeeds
- [ ] `cmake -B build -G Ninja && ninja -C build mdcad_math_harness` succeeds
- [ ] `./build/bin/mdcad_math_harness --mode compare --strict` passes for `transform-compose` and `hierarchy-world-transform`
- [ ] `TransformComp` cached field types stay `vec3_t` / `mat4_t` in this phase
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The ECS transform and world-point hot paths are migrated to `cglm` without a broader render-storage rewrite
  </success_criteria>

<output>
After completion, create `.planning/phases/03-macos-core-transform-migration/03-02-SUMMARY.md`
</output>
