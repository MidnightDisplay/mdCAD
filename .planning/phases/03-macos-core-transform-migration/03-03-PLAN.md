---
phase: 03-macos-core-transform-migration
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - src/math_harness.c
  - docs/QUICKSTART.md
autonomous: true
requirements:
  - HOT-01
  - HOT-02
user_setup: []
must_haves:
  truths:
    - "Phase 3 parity checks explicitly exercise the migrated production camera and transform paths on macOS"
    - "The harness-first workflow remains the primary automated gate, with a concrete manual macOS smoke pass documented as secondary"
    - "Another agent can run the Phase 3 parity workflow from checked-in docs without local tribal knowledge"
  artifacts:
    - path: "src/math_harness.c"
      provides: "Phase 3 production parity coverage in the standalone harness"
      contains: "hierarchy-world-transform"
    - path: "docs/QUICKSTART.md"
      provides: "Concrete Phase 3 macOS parity workflow"
      contains: "Phase 3 macOS parity smoke"
  key_links:
    - from: "src/math_harness.c"
      to: "src/orbit_camera.h"
      via: "the harness validates the live migrated camera path"
      pattern: "orbit-camera-view"
    - from: "src/math_harness.c"
      to: "src/components/transform_comp.h"
      via: "the harness validates migrated transform composition and hierarchy behavior"
      pattern: "hierarchy-world-transform"
    - from: "docs/QUICKSTART.md"
      to: "src/math_harness.c"
      via: "the documented commands map directly to the harness-driven parity workflow"
      pattern: "math-validation|mdcad_math_harness"
---

<objective>
Lock a repeatable macOS parity workflow around the migrated camera and transform paths and document the manual smoke pass clearly.

Purpose: `HOT-01` and `HOT-02` both require regression-free behavior on the active macOS workflow, which means the repo needs explicit production-backed parity checks plus a short manual smoke checklist.
Output: tightened harness parity coverage and documented Phase 3 macOS validation steps in Quickstart.
</objective>

<execution_context>
@/Users/rodionradchenko/Documents/repos/mdCAD/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rodionradchenko/Documents/repos/mdCAD/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-macos-core-transform-migration/03-CONTEXT.md
@.planning/phases/03-macos-core-transform-migration/03-RESEARCH.md
@.planning/phases/03-macos-core-transform-migration/03-VALIDATION.md
@.planning/phases/03-macos-core-transform-migration/03-01-PLAN.md
@.planning/phases/03-macos-core-transform-migration/03-02-PLAN.md
@docs/QUICKSTART.md
@src/math_harness.c
@src/orbit_camera.h
@src/components/transform_comp.h
@src/ecs/ecs_scene.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize production-backed Phase 3 parity coverage in the harness</name>
  <files>src/math_harness.c</files>
  <read_first>src/math_harness.c, src/orbit_camera.h, src/components/transform_comp.h, src/ecs/ecs_scene.h, .planning/phases/03-macos-core-transform-migration/03-CONTEXT.md, .planning/phases/03-macos-core-transform-migration/03-RESEARCH.md, .planning/phases/03-macos-core-transform-migration/03-VALIDATION.md</read_first>
  <action>Audit the existing compare suite in `src/math_harness.c` after Plans 03-01 and 03-02 land. Ensure `orbit-camera-view`, `view-projection-roundtrip`, `transform-compose`, and `hierarchy-world-transform` all use the live migrated production helpers on the production side and frozen legacy math on the baseline side. If any compare case still derives both sides from migrated code, rewrite it so the baseline is local to the harness. Keep the exact `COMPARE PASS` / `COMPARE FAIL` prefixes and include `hierarchy-world-transform` in the default `--list` and compare run path.</action>
  <verify>
    <automated>cmake -B build -G Ninja && ninja -C build mdcad_math_harness && ./build/bin/mdcad_math_harness --list | rg -n "orbit-camera-view|view-projection-roundtrip|transform-compose|hierarchy-world-transform" && ./build/bin/mdcad_math_harness --mode compare --strict</automated>
  </verify>
  <acceptance_criteria>
    - `./build/bin/mdcad_math_harness --list` prints `orbit-camera-view`
    - `./build/bin/mdcad_math_harness --list` prints `view-projection-roundtrip`
    - `./build/bin/mdcad_math_harness --list` prints `transform-compose`
    - `./build/bin/mdcad_math_harness --list` prints `hierarchy-world-transform`
    - `./build/bin/mdcad_math_harness --mode compare --strict` exits 0
    - `src/math_harness.c` still contains `COMPARE PASS`
    - `src/math_harness.c` contains `hierarchy-world-transform`
  </acceptance_criteria>
  <done>The standalone harness now functions as the production-backed parity gate for the full Phase 3 hotspot set.</done>
</task>

<task type="auto">
  <name>Task 2: Document the exact Phase 3 macOS parity workflow and smoke checklist</name>
  <files>docs/QUICKSTART.md</files>
  <read_first>docs/QUICKSTART.md, src/math_harness.c, .planning/phases/03-macos-core-transform-migration/03-CONTEXT.md, .planning/phases/03-macos-core-transform-migration/03-RESEARCH.md, .planning/phases/03-macos-core-transform-migration/03-VALIDATION.md</read_first>
  <action>Update `docs/QUICKSTART.md` by extending the existing native math validation section with a new subsection titled exactly `## Phase 3 macOS parity smoke`. Document these exact commands in order: `cmake -B build -G Ninja && ninja -C build math-validation`, `./build/bin/mdcad_math_harness --mode compare --strict`, and `./build/bin/mdCAD`. In prose, state that the harness-driven validation remains the primary automated gate for this phase and the app launch is a manual smoke step. Add an explicit checklist that says to orbit with left-drag, pan with shift+left or middle-drag, zoom with the wheel, and confirm visible geometry remains stable while moving the camera. Also state that if parented entities are present in the current scene, child geometry should continue following the parent without visible drift.</action>
  <verify>
    <automated>rg -n "Phase 3 macOS parity smoke|math-validation|mdcad_math_harness --mode compare --strict|orbit with left-drag|pan with shift\\+left or middle-drag|zoom with the wheel|without visible drift" docs/QUICKSTART.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/QUICKSTART.md` contains `## Phase 3 macOS parity smoke`
    - `docs/QUICKSTART.md` contains `cmake -B build -G Ninja && ninja -C build math-validation`
    - `docs/QUICKSTART.md` contains `./build/bin/mdcad_math_harness --mode compare --strict`
    - `docs/QUICKSTART.md` contains `./build/bin/mdCAD`
    - `docs/QUICKSTART.md` contains `orbit with left-drag`
    - `docs/QUICKSTART.md` contains `pan with shift+left or middle-drag`
    - `docs/QUICKSTART.md` contains `zoom with the wheel`
    - `docs/QUICKSTART.md` contains `without visible drift`
  </acceptance_criteria>
  <done>The repo now records a concrete Phase 3 macOS parity workflow that another agent can run directly from checked-in docs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmake -B build -G Ninja && ninja -C build mdcad_math_harness` succeeds
- [ ] `./build/bin/mdcad_math_harness --list` includes `orbit-camera-view`, `view-projection-roundtrip`, `transform-compose`, and `hierarchy-world-transform`
- [ ] `./build/bin/mdcad_math_harness --mode compare --strict` succeeds
- [ ] `docs/QUICKSTART.md` documents the exact Phase 3 macOS parity workflow and manual smoke checklist
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo has a repeatable Phase 3 macOS parity workflow that protects the migrated camera and transform hotspots
  </success_criteria>

<output>
After completion, create `.planning/phases/03-macos-core-transform-migration/03-03-SUMMARY.md`
</output>
