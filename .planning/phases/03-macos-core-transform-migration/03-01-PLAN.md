---
phase: 03-macos-core-transform-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/orbit_camera.h
  - src/app.c
  - src/math_harness.c
autonomous: true
requirements:
  - HOT-01
user_setup: []
must_haves:
  truths:
    - "Orbit camera eye/view math and app-side view/projection/MVP construction run through `cglm` on the live macOS path"
    - "Legacy bridge matrices remain explicit at unchanged subsystem boundaries instead of hiding fresh `math3d.h` work inside the migrated path"
    - "The harness compares the migrated production camera/projection path against frozen legacy baselines"
  artifacts:
    - path: "src/orbit_camera.h"
      provides: "cglm-backed orbit camera helpers"
      contains: "orbit_camera_get_view_matrix_cglm"
    - path: "src/app.c"
      provides: "cglm-backed app-side view/projection/MVP construction"
      contains: "glms_perspective_rh_zo"
    - path: "src/math_harness.c"
      provides: "frozen legacy camera/projection baselines for parity checks"
      contains: "mdcad_harness_legacy_projection"
  key_links:
    - from: "src/orbit_camera.h"
      to: "vendors/cglm/include/cglm/struct/cam.h"
      via: "orbit camera view construction uses `glms_lookat_rh_zo(...)` on the migrated path"
      pattern: "glms_lookat_rh_zo"
    - from: "src/app.c"
      to: "src/orbit_camera.h"
      via: "app-side view/projection/MVP construction consumes the new cglm camera helper"
      pattern: "orbit_camera_get_view_matrix_cglm"
    - from: "src/math_harness.c"
      to: "src/app.c"
      via: "the harness validates the live migrated app-side camera/projection path against frozen legacy helpers"
      pattern: "view-projection-roundtrip"
---

<objective>
Migrate orbit camera and app-side viewport matrix construction to the new math foundation without changing visible macOS navigation behavior.

Purpose: `HOT-01` requires the live orbit-camera and viewport matrix path to move onto `cglm` while preserving user-visible parity.
Output: cglm-backed orbit camera helpers, cglm-backed app-side view/projection/MVP construction, and harness parity coverage that still compares against frozen legacy formulas.
</objective>

<execution_context>
@/Users/rodionradchenko/Documents/repos/mdCAD/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rodionradchenko/Documents/repos/mdCAD/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-macos-core-transform-migration/03-CONTEXT.md
@.planning/phases/03-macos-core-transform-migration/03-RESEARCH.md
@.planning/phases/03-macos-core-transform-migration/03-VALIDATION.md
@docs/MATH_BACKEND_DECISION.md
@docs/MATH_CONVENTIONS.md
@src/orbit_camera.h
@src/app.c
@src/math_harness.c
@src/math/cglm_entry.h
@vendors/cglm/include/cglm/struct/cam.h
@vendors/cglm/include/cglm/struct/mat4.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cglm-backed orbit camera helpers while keeping source-compatible bridges</name>
  <files>src/orbit_camera.h</files>
  <read_first>src/orbit_camera.h, src/math/cglm_entry.h, src/math3d.h, docs/MATH_CONVENTIONS.md, .planning/phases/03-macos-core-transform-migration/03-CONTEXT.md, .planning/phases/03-macos-core-transform-migration/03-RESEARCH.md, vendors/cglm/include/cglm/struct/cam.h, vendors/cglm/include/cglm/struct/mat4.h</read_first>
  <action>Update `src/orbit_camera.h` so the migrated camera path is explicitly `cglm`-backed. Include `"math/cglm_entry.h"` before using vendor helpers. Add `static inline vec3s orbit_camera_get_eye_position_cglm(const orbit_camera_t *cam)` that computes the eye position from `distance`, `azimuth`, `elevation`, and `target` using the existing scalar formulas but returns a `vec3s`. Add `static inline mat4s orbit_camera_get_view_matrix_cglm(const orbit_camera_t *cam)` that returns `glms_lookat_rh_zo(orbit_camera_get_eye_position_cglm(cam), ((vec3s){{ cam->target.x, cam->target.y, cam->target.z }}), GLMS_YUP)`. Keep `orbit_camera_get_eye_position(...)` and `orbit_camera_get_view_matrix(...)` as thin compatibility wrappers that call the new cglm helpers and copy the raw float results back into `vec3_t` and `mat4_t` so unchanged UI/debug code still compiles during this slice. Do not change the layout of `orbit_camera_t` in Phase 3.</action>
  <verify>
    <automated>rg -n "orbit_camera_get_eye_position_cglm|orbit_camera_get_view_matrix_cglm|glms_lookat_rh_zo|orbit_camera_get_view_matrix\\(const orbit_camera_t\\* cam\\)" src/orbit_camera.h</automated>
  </verify>
  <acceptance_criteria>
    - `src/orbit_camera.h` contains `orbit_camera_get_eye_position_cglm`
    - `src/orbit_camera.h` contains `orbit_camera_get_view_matrix_cglm`
    - `src/orbit_camera.h` contains `glms_lookat_rh_zo`
    - `src/orbit_camera.h` does not contain `return mat4_lookat(` inside `orbit_camera_get_view_matrix`
    - `src/orbit_camera.h` still contains `typedef struct {` for `orbit_camera_t`
  </acceptance_criteria>
  <done>Orbit camera view construction now originates from `cglm` while unchanged callers retain a thin compatibility bridge for this phase.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate app-side view/projection/MVP construction and freeze legacy parity baselines in the harness</name>
  <files>src/app.c, src/math_harness.c</files>
  <read_first>src/app.c, src/math_harness.c, src/orbit_camera.h, src/math3d.h, .planning/phases/03-macos-core-transform-migration/03-CONTEXT.md, .planning/phases/03-macos-core-transform-migration/03-RESEARCH.md, vendors/cglm/include/cglm/struct/cam.h, vendors/cglm/include/cglm/struct/mat4.h</read_first>
  <action>In `src/app.c`, replace the live view/projection/MVP setup with `cglm` struct API calls: compute `mat4s view = orbit_camera_get_view_matrix_cglm(&state.camera);`, `mat4s proj = glms_perspective_rh_zo(0.785398f, (float)vp_width / (float)vp_height, 0.1f, 100.0f);`, `mat4s vp = glms_mat4_mul(proj, view);`, and an identity-based `mat4s mvp` for the current render path. Add explicit bridge copies such as `mat4_t view_legacy`, `proj_legacy`, `vp_legacy`, and `mvp_legacy` populated from the cglm matrices so unchanged consumers like `pick_buffer_needs_rebuild(...)`, `pick_buffer_compute_mvp(...)`, `pick_buffer_render(...)`, `ray_from_screen(...)`, `geom_triangle_params_t`, and `gizmo_rendering_draw(...)` still receive `mat4_t` values without recomputing them via `math3d.h`. In `src/math_harness.c`, keep the compare identifiers `orbit-camera-view` and `view-projection-roundtrip`, but freeze the old formulas into local helpers named `mdcad_harness_legacy_projection` and `mdcad_harness_legacy_orbit_view` so the compare path validates the live migrated helpers instead of comparing migrated code against itself.</action>
  <verify>
    <automated>cmake -B build -G Ninja && ninja -C build mdcad_math_harness && ./build/bin/mdcad_math_harness --mode compare --strict | rg -n "^COMPARE PASS (orbit-camera-view|view-projection-roundtrip)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app.c` contains `orbit_camera_get_view_matrix_cglm(&state.camera)`
    - `src/app.c` contains `glms_perspective_rh_zo`
    - `src/app.c` contains `glms_mat4_mul`
    - `src/app.c` contains `view_legacy` and `proj_legacy`
    - `src/app.c` does not contain `mat4_t proj = mat4_perspective(`
    - `src/math_harness.c` contains `mdcad_harness_legacy_projection`
    - `src/math_harness.c` contains `mdcad_harness_legacy_orbit_view`
    - `./build/bin/mdcad_math_harness --mode compare --strict` prints `COMPARE PASS orbit-camera-view`
    - `./build/bin/mdcad_math_harness --mode compare --strict` prints `COMPARE PASS view-projection-roundtrip`
  </acceptance_criteria>
  <done>The live macOS camera/projection path now runs through `cglm`, and the harness still proves parity against pre-migration formulas.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "orbit_camera_get_view_matrix_cglm|glms_lookat_rh_zo|glms_perspective_rh_zo|glms_mat4_mul|mdcad_harness_legacy_projection" src/orbit_camera.h src/app.c src/math_harness.c` succeeds
- [ ] `cmake -B build -G Ninja && ninja -C build mdcad_math_harness` succeeds
- [ ] `./build/bin/mdcad_math_harness --mode compare --strict` passes for `orbit-camera-view` and `view-projection-roundtrip`
- [ ] `src/app.c` uses explicit bridge copies for unchanged `mat4_t` consumers instead of recomputing view/projection with `math3d.h`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The live macOS camera/view/projection path is migrated to `cglm` without dropping parity coverage
  </success_criteria>

<output>
After completion, create `.planning/phases/03-macos-core-transform-migration/03-01-SUMMARY.md`
</output>
