---
phase: 18-add-undo-steps-for-endpoint-moves
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/undo_redo.h
  - src/undo_redo_exec.h
  - src/app.c
  - src/ui/ui_entity_inspector.h
  - src/tests/endpoint_pick_test.c
autonomous: true
requirements: [PH18-01]
must_haves:
  truths:
    - "Dragging a sketch endpoint in transform mode creates exactly one meaningful undo step at drag-end."
    - "Undoing endpoint drag reverts endpoint geometry state instead of no-op TransformComp position."
    - "Non-endpoint transform undo behavior remains unchanged."
  artifacts:
    - path: "src/undo_redo.h"
      provides: "Endpoint-aware undo command contract and payload definitions"
      contains: "CMD_MOVE_ENDPOINT_PARTICIPANT"
    - path: "src/app.c"
      provides: "Drag-end undo recorder routing endpoint picks to endpoint-aware command"
      contains: "gizmo drag-end command recording path"
    - path: "src/ui/ui_entity_inspector.h"
      provides: "Inspector endpoint edit commit routing to endpoint-aware command"
      contains: "endpoint edit deactivation commit"
  key_links:
    - from: "src/app.c"
      to: "src/undo_redo_exec.h"
      via: "drag-end undo record helper"
      pattern: "undo_cmd_.*endpoint"
    - from: "src/ui/ui_entity_inspector.h"
      to: "src/undo_redo_exec.h"
      via: "endpoint edit undo record helper"
      pattern: "undo_cmd_.*endpoint"
---

<objective>
Define and wire endpoint-aware undo recording so endpoint moves produce correct one-step undo entries at drag/session boundaries.

Purpose: Close the current semantic mismatch where endpoint moves mutate owner geometry but undo records transform position.
Output: New endpoint undo command contract plus recorder wiring in gizmo and inspector flows.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-add-undo-steps-for-endpoint-moves/18-RESEARCH.md
@.planning/phases/18-add-undo-steps-for-endpoint-moves/18-VALIDATION.md
@.planning/phases/17-constraint-driven-geometry-solving/17-04-SUMMARY.md
@src/app.c
@src/ui/ui_entity_inspector.h
@src/undo_redo.h
@src/undo_redo_exec.h
@src/ecs/ecs_scene.h
@src/tests/endpoint_pick_test.c

<interfaces>
From `src/ecs/ecs_scene.h`:
```c
static inline bool scene_apply_transform_delta_for_selection(ecs_scene_t *scene, const ecs_entity_t *selected_entities, uint32_t selected_count, vec3_t world_delta);
static inline bool scene_sync_owner_geometry_from_endpoint_entity(ecs_scene_t *scene, ecs_entity_t endpoint_entity);
static inline void scene_sync_endpoint_entities_for_owner(ecs_scene_t *scene, ecs_entity_t owner_entity);
```

From `src/undo_redo.h`:
```c
typedef enum {
    CMD_SET_POSITION,
    CMD_SET_LINE_ENDPOINTS,
    CMD_SET_POINT_POSITION
} UndoCommandType;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add endpoint-aware undo command contract and helpers</name>
  <read_first>
    - src/undo_redo.h
    - src/undo_redo_exec.h
    - src/components/endpoints_comp.h
    - src/ecs/ecs_scene.h
  </read_first>
  <files>
    - src/undo_redo.h
    - src/undo_redo_exec.h
    - src/tests/endpoint_pick_test.c
  </files>
  <behavior>
    - Test 1: Endpoint participant move command stores owner+role identity with old/new local coordinates and round-trips payload through undo stack.
    - Test 2: Existing CMD_SET_POSITION/CMD_SET_POINT_POSITION paths still serialize/execute unchanged for non-endpoint entities.
  </behavior>
  <action>Add a dedicated endpoint move undo command payload keyed by endpoint owner metadata (owner entity + endpoint role/sub-index + old/new local point) per PH18-01. Add record helper(s) in undo API for endpoint move commits. Keep existing command values and script transaction command semantics intact (do not alter `CMD_SCRIPT_APPLY_TRANSACTION` behavior). Extend endpoint_pick tests to fail first when endpoint move recording falls back to transform no-op semantics.</action>
  <acceptance_criteria>Undo contract can represent endpoint move intent in owner-geometry space and tests prove legacy command paths remain unaffected.</acceptance_criteria>
  <verify>
    <automated>ctest -R endpoint_pick --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>Endpoint-aware undo command exists with record helper and failing regression turned green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire drag-end and inspector commit to endpoint-aware recorder</name>
  <read_first>
    - src/app.c
    - src/ui/ui_entity_inspector.h
    - src/undo_redo_exec.h
    - .planning/phases/18-add-undo-steps-for-endpoint-moves/18-RESEARCH.md
  </read_first>
  <files>
    - src/app.c
    - src/ui/ui_entity_inspector.h
    - src/tests/endpoint_pick_test.c
  </files>
  <behavior>
    - Test 1: Gizmo endpoint drag over multiple frames creates one undo entry on drag release.
    - Test 2: Inspector endpoint edit commit records endpoint-aware command only at deactivation boundary.
  </behavior>
  <action>At drag-end undo commit in `app.c`, detect endpoint-point selections and record endpoint-aware command instead of `undo_cmd_set_position` (per PH18-01). Preserve coalesced drag-end granularity exactly (one step per drag). In inspector endpoint edit commit path, record endpoint-aware command at session boundary while preserving existing non-endpoint edit behavior. Do not alter non-sketch geometry flow; endpoint command routing must stay sketch-scoped.</action>
  <acceptance_criteria>Endpoint moves now produce meaningful coalesced undo records from both gizmo and inspector paths while non-endpoint paths are unchanged.</acceptance_criteria>
  <verify>
    <automated>ctest -R endpoint_pick --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>Recording path mismatch is removed and endpoint undo steps exist at the intended interaction boundary.</done>
</task>

</tasks>

<verification>
- `ctest -R endpoint_pick --test-dir build-vulkan -C Release --output-on-failure`
- `ctest -R "scene_solver_contract|scene_solver_diagnostics" --test-dir build-vulkan -C Release --output-on-failure`
</verification>

<success_criteria>
- Endpoint drag and endpoint inspector edits each emit one undo step per interaction session.
- Ctrl+Z after endpoint drag reverts owner geometry endpoint location in one step.
- Non-endpoint transform undo semantics remain unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/18-add-undo-steps-for-endpoint-moves/18-01-SUMMARY.md`
</output>

