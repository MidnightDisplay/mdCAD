---
phase: 18-add-undo-steps-for-endpoint-moves
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - src/undo_redo_exec.h
  - src/ecs/ecs_scene.h
  - src/tests/endpoint_pick_test.c
  - src/tests/scene_solver_contract_test.c
autonomous: true
requirements: [PH18-02]
must_haves:
  truths:
    - "Undo/redo of endpoint move keeps endpoint entity and owner geometry synchronized."
    - "Sketch endpoint undo/redo triggers expected sketch side effects (auto-solver request and script re-emit)."
    - "Undo/redo does not alter script-apply transactional command semantics."
  artifacts:
    - path: "src/undo_redo_exec.h"
      provides: "Endpoint move apply/unapply execution via scene helper flow"
      contains: "CMD_MOVE_ENDPOINT_PARTICIPANT apply/unapply cases"
    - path: "src/tests/endpoint_pick_test.c"
      provides: "Replay regression for endpoint-owner sync after undo then redo"
      contains: "undo/redo endpoint consistency assertions"
    - path: "src/tests/scene_solver_contract_test.c"
      provides: "Sketch-side effect regression coverage for endpoint replay"
      contains: "solver/script side-effect assertions"
  key_links:
    - from: "src/undo_redo_exec.h"
      to: "src/ecs/ecs_scene.h"
      via: "scene sync helpers in apply/unapply"
      pattern: "scene_sync_owner_geometry_from_endpoint_entity|scene_sync_endpoint_entities_for_owner"
    - from: "src/undo_redo_exec.h"
      to: "src/ecs/ecs_scene.h"
      via: "sketch-only side effects"
      pattern: "scene_solver_request_auto|scene_script_reemit_for_sketch"
---

<objective>
Implement endpoint move undo replay semantics through scene-authoritative helpers so apply/unapply keeps endpoint/owner state coherent and sketch side effects intact.

Purpose: Ensure undo/redo behavior matches live endpoint mutation path and does not bypass sync/solver/script contracts.
Output: Endpoint command executor cases plus replay regression coverage.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-add-undo-steps-for-endpoint-moves/18-RESEARCH.md
@.planning/phases/18-add-undo-steps-for-endpoint-moves/18-VALIDATION.md
@.planning/phases/18-add-undo-steps-for-endpoint-moves/18-01-SUMMARY.md
@src/undo_redo_exec.h
@src/ecs/ecs_scene.h
@src/tests/endpoint_pick_test.c
@src/tests/scene_solver_contract_test.c

<interfaces>
From `src/undo_redo_exec.h`:
```c
static inline bool undo_apply_command(undo_redo_t *ur, ecs_scene_t *scene, const undo_command_t *cmd);
static inline bool undo_unapply_command(undo_redo_t *ur, ecs_scene_t *scene, const undo_command_t *cmd);
```

From `src/ecs/ecs_scene.h`:
```c
static inline bool scene_sync_owner_geometry_from_endpoint_entity(ecs_scene_t *scene, ecs_entity_t endpoint_entity);
static inline void scene_sync_endpoint_entities_for_owner(ecs_scene_t *scene, ecs_entity_t owner_entity);
static inline bool scene_solver_request_auto(ecs_scene_t *scene, ecs_entity_t sketch);
static inline bool scene_script_reemit_for_sketch(ecs_scene_t *scene, ecs_entity_t sketch);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add endpoint command apply/unapply path via scene sync helpers</name>
  <read_first>
    - src/undo_redo_exec.h
    - src/ecs/ecs_scene.h
    - src/components/endpoints_comp.h
    - .planning/phases/18-add-undo-steps-for-endpoint-moves/18-RESEARCH.md
  </read_first>
  <files>
    - src/undo_redo_exec.h
    - src/tests/endpoint_pick_test.c
  </files>
  <behavior>
    - Test 1: Undo endpoint command restores old endpoint location and owner geometry endpoint state.
    - Test 2: Redo endpoint command reapplies new endpoint location and owner geometry endpoint state.
  </behavior>
  <action>Implement `CMD_MOVE_ENDPOINT_PARTICIPANT` apply/unapply in undo executor by mutating endpoint participant through scene-authoritative flow (not raw component writes) per PH18-02. Replay must resync endpoint entities and owner geometry consistently through existing helper path. Guard for missing/dead entities and fail safely without corrupting undo stack order.</action>
  <acceptance_criteria>Endpoint replay produces deterministic old/new geometry state and keeps endpoint-owner synchronization valid.</acceptance_criteria>
  <verify>
    <automated>ctest -R "endpoint_pick|scene_solver_contract" --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>Undo executor handles endpoint command reliably using scene helper semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce sketch-only side effects in endpoint replay without touching script transaction semantics</name>
  <read_first>
    - src/undo_redo_exec.h
    - src/ecs/ecs_scene.h
    - src/tests/scene_solver_contract_test.c
    - src/tests/script_roundtrip_tests.c
  </read_first>
  <files>
    - src/undo_redo_exec.h
    - src/tests/scene_solver_contract_test.c
    - src/tests/endpoint_pick_test.c
  </files>
  <behavior>
    - Test 1: Sketch endpoint undo/redo triggers solver auto-request and script re-emit side effects.
    - Test 2: Non-sketch endpoint-adjacent paths remain untouched and script transaction undo command behavior remains stable.
  </behavior>
  <action>After endpoint replay, invoke sketch side effects only when owner belongs to a sketch: endpoint sync + `scene_solver_request_auto(...)` + `scene_script_reemit_for_sketch(...)` (PH18-02 and project constraint). Explicitly avoid changes to `CMD_SCRIPT_APPLY_TRANSACTION` branches and script-IO transaction coalescing behavior. Add/extend tests to detect side-effect omission and transaction regression.</action>
  <acceptance_criteria>Sketch replay side effects run correctly, non-sketch behavior is unchanged, and script transaction semantics are unaffected.</acceptance_criteria>
  <verify>
    <automated>ctest -R "endpoint_pick|scene_solver_contract|scene_solver_diagnostics" --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>Endpoint replay parity with live sketch editing is achieved without script transaction regressions.</done>
</task>

</tasks>

<verification>
- `ctest -R "endpoint_pick|scene_solver_contract|scene_solver_diagnostics" --test-dir build-vulkan -C Release --output-on-failure`
- `ctest -R script_roundtrip_tests --test-dir build-vulkan -C Release --output-on-failure`
</verification>

<success_criteria>
- Endpoint undo/redo maintains endpoint-owner sync and deterministic replay state.
- Sketch endpoint replay triggers solver/script side effects through existing scene APIs.
- Script apply transaction behavior remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/18-add-undo-steps-for-endpoint-moves/18-02-SUMMARY.md`
</output>

