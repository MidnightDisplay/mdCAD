---
phase: 18-add-undo-steps-for-endpoint-moves
plan: 03
type: execute
wave: 3
depends_on: [18-01, 18-02]
files_modified:
  - src/tests/endpoint_pick_test.c
  - src/tests/script_roundtrip_tests.c
  - .planning/phases/18-add-undo-steps-for-endpoint-moves/18-VALIDATION.md
autonomous: false
requirements: [PH18-03]
must_haves:
  truths:
    - "Endpoint undo integration does not regress non-sketch manipulation behavior."
    - "Script IO/apply transactional undo semantics remain intact after endpoint undo changes."
    - "Endpoint drag undo granularity remains one coalesced step per drag-end interaction."
  artifacts:
    - path: "src/tests/endpoint_pick_test.c"
      provides: "Regression assertions for non-sketch invariants and drag-end coalescing"
      contains: "mixed endpoint/non-sketch undo coverage"
    - path: "src/tests/script_roundtrip_tests.c"
      provides: "Regression assertions for script transaction invariants"
      contains: "script apply undo atomicity checks"
    - path: ".planning/phases/18-add-undo-steps-for-endpoint-moves/18-VALIDATION.md"
      provides: "Requirement-level evidence for PH18-01..PH18-03 closure"
      contains: "explicit command outputs and status updates"
  key_links:
    - from: "src/tests/endpoint_pick_test.c"
      to: "src/app.c"
      via: "drag-end undo coalescing behavior"
      pattern: "undo.*entry|drag.*end"
    - from: "src/tests/script_roundtrip_tests.c"
      to: "src/undo_redo_exec.h"
      via: "script transaction undo unchanged"
      pattern: "CMD_SCRIPT_APPLY_TRANSACTION|undo|redo"
---

<objective>
Harden regression coverage and close phase validation evidence for endpoint undo without collateral behavior drift.

Purpose: Prove endpoint undo improvements do not break non-sketch behavior or script transactional guarantees.
Output: Expanded regression tests plus finalized validation evidence.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-add-undo-steps-for-endpoint-moves/18-RESEARCH.md
@.planning/phases/18-add-undo-steps-for-endpoint-moves/18-VALIDATION.md
@.planning/phases/18-add-undo-steps-for-endpoint-moves/18-01-SUMMARY.md
@.planning/phases/18-add-undo-steps-for-endpoint-moves/18-02-SUMMARY.md
@src/tests/endpoint_pick_test.c
@src/tests/script_roundtrip_tests.c
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add regression gates for non-sketch invariants and undo coalescing boundary</name>
  <read_first>
    - src/tests/endpoint_pick_test.c
    - src/tests/scene_solver_drag_test.c
    - .planning/phases/18-add-undo-steps-for-endpoint-moves/18-RESEARCH.md
  </read_first>
  <files>
    - src/tests/endpoint_pick_test.c
  </files>
  <behavior>
    - Test 1: Non-sketch line transform/move behavior matches baseline after endpoint undo integration.
    - Test 2: Endpoint drag across multiple frames yields one undo entry committed at drag-end.
  </behavior>
  <action>Extend endpoint regression suite to assert non-sketch behavior does not route through endpoint-only replay paths (PH18-03). Add explicit coalescing checks so endpoint drag updates during active drag do not push per-frame undo entries and only commit one step on drag release. Keep test fixture deterministic and aligned with existing endpoint_pick conventions.</action>
  <acceptance_criteria>Tests fail if endpoint logic leaks to non-sketch entities or if drag granularity regresses into multi-entry undo spam.</acceptance_criteria>
  <verify>
    <automated>ctest -R "endpoint_pick|scene_solver_drag" --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>Regression suite enforces both scope guard and drag-end granularity contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock script transaction invariants and update phase validation evidence</name>
  <read_first>
    - src/tests/script_roundtrip_tests.c
    - .planning/phases/18-add-undo-steps-for-endpoint-moves/18-VALIDATION.md
    - .planning/phases/18-add-undo-steps-for-endpoint-moves/18-RESEARCH.md
  </read_first>
  <files>
    - src/tests/script_roundtrip_tests.c
    - .planning/phases/18-add-undo-steps-for-endpoint-moves/18-VALIDATION.md
  </files>
  <behavior>
    - Test 1: Script apply remains atomic undo/redo transaction after endpoint undo changes.
    - Test 2: Script IO-related undo coalescing remains unchanged from established behavior.
  </behavior>
  <action>Add focused script regression assertions ensuring endpoint undo additions did not modify `CMD_SCRIPT_APPLY_TRANSACTION` semantics (PH18-03, project constraint). Then update `18-VALIDATION.md` rows/status for PH18-01..PH18-03 with explicit automated command evidence and mark `nyquist_compliant: true` once all gates are passing.</action>
  <acceptance_criteria>Script transaction behavior remains stable and phase validation document is fully evidenced and compliance-complete.</acceptance_criteria>
  <verify>
    <automated>ctest -R "endpoint_pick|script_roundtrip_tests" --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>Phase 18 has requirement-level evidence showing no regression in non-sketch or script transactional behaviors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify endpoint drag undo feel and single-step granularity</name>
  <read_first>
    - .planning/phases/18-add-undo-steps-for-endpoint-moves/18-VALIDATION.md
  </read_first>
  <action>Confirm UX-level undo granularity in viewport after automated regressions pass.</action>
  <acceptance_criteria>Endpoint drag interaction reverts/restores in a single undo/redo step and feels consistent with prior drag semantics.</acceptance_criteria>
  <verify>
    <automated>ctest -R endpoint_pick --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <how_to_verify>
    1. Launch app (`cmake --build build-vulkan --config Release --target mdCAD` then run executable).
    2. In a sketch, drag a visible endpoint through multiple frames before releasing.
    3. Press undo once and verify the full drag reverts in one step.
    4. Press redo once and verify full drag reapplies in one step.
    5. Repeat with a non-sketch line move and verify behavior matches baseline (no endpoint-specific side effects).
  </how_to_verify>
  <done>Human confirms coalesced drag-end granularity and no obvious interaction regression, or provides exact failing repro.</done>
  <resume-signal>Type "approved" or provide failing step details.</resume-signal>
</task>

</tasks>

<verification>
- `ctest -R "endpoint_pick|scene_solver_contract|scene_solver_drag|scene_solver_diagnostics|script_roundtrip_tests" --test-dir build-vulkan -C Release --output-on-failure`
</verification>

<success_criteria>
- Regression tests prove non-sketch behavior is unchanged.
- Script apply transaction semantics remain untouched.
- Human checkpoint confirms one-step endpoint drag undo/redo interaction.
- Validation artifact is updated with explicit command evidence and compliant status.
</success_criteria>

<output>
After completion, create `.planning/phases/18-add-undo-steps-for-endpoint-moves/18-03-SUMMARY.md`
</output>

