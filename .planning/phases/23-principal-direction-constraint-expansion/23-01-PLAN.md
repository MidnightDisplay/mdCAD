---
phase: 23-principal-direction-constraint-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/constraints/constraint_types.h
  - src/scripting/sketch_script_apply.h
  - src/tests/endpoint_pick_test.c
  - src/tests/scene_solver_contract_test.c
autonomous: true
requirements: [AXIS-01, AXIS-02, AXIS-03, AXIS-04]
must_haves:
  truths:
    - "User can author ALONG X/Y/Z constraints for point pairs and point groups."
    - "Directional legality is identical across inspector authoring and script apply validation."
    - "Directional legality accepts standalone points, line endpoints, and arc landmark points per participant descriptors."
  artifacts:
    - path: "src/constraints/constraint_types.h"
      provides: "Authoritative ALONG X/Y/Z legality matrix for point-participant signatures"
    - path: "src/scripting/sketch_script_apply.h"
      provides: "Script apply legality path continues using shared legality helper (no forked rules)"
    - path: "src/tests/endpoint_pick_test.c"
      provides: "Executable legality coverage for mixed participant roles and negative cases"
    - path: "src/tests/scene_solver_contract_test.c"
      provides: "Contract test scaffolding for ALONG legality entrypoints before runtime solve branch"
  key_links:
    - from: "src/constraints/constraint_types.h"
      to: "src/scripting/sketch_script_apply.h"
      via: "constraint_type_is_selection_legal(...)"
      pattern: "constraint_type_is_selection_legal"
    - from: "src/tests/endpoint_pick_test.c"
      to: "src/constraints/constraint_types.h"
      via: "legality matrix assertions"
      pattern: "CONSTRAINT_ALONG_X|CONSTRAINT_ALONG_Y|CONSTRAINT_ALONG_Z"
---

<objective>
Lock Phase 23 legality contracts first so ALONG X/Y/Z authoring semantics are deterministic and shared across UI and script validation before runtime solver changes.

Purpose: De-risk AXIS-01..04 by preventing legality drift and by adding executable test gates ahead of solve-path expansion.
Output: Updated legality matrix + script-validation alignment + Wave-1 legality tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-principal-direction-constraint-expansion/23-CONTEXT.md
@.planning/phases/23-principal-direction-constraint-expansion/23-RESEARCH.md
@.planning/phases/23-principal-direction-constraint-expansion/23-VALIDATION.md
@src/constraints/constraint_types.h
@src/scripting/sketch_script_apply.h
@src/tests/endpoint_pick_test.c
@src/tests/scene_solver_contract_test.c
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand directional legality to descriptor-based point participant signatures</name>
  <files>src/constraints/constraint_types.h, src/scripting/sketch_script_apply.h</files>
  <behavior>
    - ALONG X/Y/Z accepts participant counts 2..CONSTRAINT_MAX_PARTICIPANTS (per D-01, D-04).
    - ALONG X/Y/Z accepts standalone points and sub-entity roles POINT_A/POINT_B/CENTER (per D-02, D-05).
    - Script apply continues to validate using the same legality helper with no divergent table (per D-06).
  </behavior>
  <action>Update `constraint_type_is_selection_legal(...)` for `CONSTRAINT_ALONG_X/Y/Z` so legality is role+geometry signature based, not single-line-only. Allow valid signatures: `GEOM_POINT + ENTITY`, `GEOM_LINE + POINT_A/POINT_B`, `GEOM_ARC + POINT_A/POINT_B/CENTER`; reject raw line/entity and arc/entity ALONG signatures to keep axis constraints point-participant only. Keep existing backward-compatible accepted signatures where already legal (per D-03). Ensure `sketch_script_apply_validate_model_links(...)` remains on shared legality path and does not introduce special-case directional logic.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target endpoint_pick scene_solver_contract && ctest -R "endpoint_pick|scene_solver_contract" --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>Directional legality contract is centralized, descriptor-aware, and shared by UI/script validation with no duplicate rule source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add executable legality matrix tests for ALONG X/Y/Z mixed participant roles</name>
  <files>src/tests/endpoint_pick_test.c, src/tests/scene_solver_contract_test.c</files>
  <behavior>
    - Tests assert ALONG X/Y/Z legality for point pairs/groups (AXIS-01/02/03).
    - Tests assert legality for standalone point, line endpoint, and arc landmark roles including CENTER (AXIS-04).
    - Tests assert illegal signatures remain rejected (raw line/entity ALONG, arc/entity ALONG) to avoid contract drift.
  </behavior>
  <action>Extend `endpoint_pick_test.c` with focused legality matrix cases for directional constraints over participant descriptors and role combinations, including negative assertions. Add complementary fixture assertions in `scene_solver_contract_test.c` that validate directional legality entrypoints in scene-side constraint creation paths using descriptors. Tag new test names/comments with decision IDs (D-01..D-06) for traceability.</action>
  <verify>
    <automated>ctest -R "endpoint_pick|scene_solver_contract" --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>Wave-1 legality coverage fails on regressions and proves AXIS authoring contracts before runtime solve implementation begins.</done>
</task>

</tasks>

<verification>
Run quick phase legality gate:
`ctest -R "endpoint_pick|scene_solver_contract|scene_solver_pass_policy" --test-dir build-vulkan -C Release --output-on-failure`
</verification>

<success_criteria>
- ALONG legality supports pair/group point participants for X/Y/Z with descriptor roles.
- UI/script legality behavior is shared from one authority function.
- Automated legality tests cover positive + negative signatures for AXIS-01..04.
</success_criteria>

<output>
After completion, create `.planning/phases/23-principal-direction-constraint-expansion/23-01-SUMMARY.md`
</output>

