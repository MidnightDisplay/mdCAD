---
phase: 23-principal-direction-constraint-expansion
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - src/ecs/ecs_scene.h
  - src/tests/scene_solver_contract_test.c
  - src/tests/scene_solver_pass_policy_test.c
autonomous: true
requirements: [SRLV-04, AXIS-01, AXIS-02, AXIS-03, AXIS-04]
must_haves:
  truths:
    - "User can recalculate sketches with ALONG X/Y/Z constraints and receive deterministic outcomes."
    - "User can solve directional constraints across standalone points, line endpoints, and arc landmark points."
    - "User can recalculate sketches containing ALONG and ANGLE together without broken solve pathways."
    - "Unsatisfied directional solves produce explicit diagnostics and no partial geometry mutation."
  artifacts:
    - path: "src/ecs/ecs_scene.h"
      provides: "Scene-owned ALONG solver branch + participant resolution updates + transactional failure handling"
      exports: ["scene_solver_request_recalculate", "scene_solver_ensure_point_candidate"]
    - path: "src/tests/scene_solver_contract_test.c"
      provides: "Deterministic ALONG solve + ALONG/ANGLE coexistence contract tests"
    - path: "src/tests/scene_solver_pass_policy_test.c"
      provides: "ALONG participation in tolerance-stop and max-pass-stop policy gates"
  key_links:
    - from: "scene_solver_request_recalculate"
      to: "scene_solver_ensure_point_candidate"
      via: "descriptor-first participant resolution"
      pattern: "scene_solver_ensure_point_candidate"
    - from: "scene_solver_request_recalculate"
      to: "scene_solver_set_failure_implication"
      via: "unsatisfied ALONG/ANGLE failure path"
      pattern: "scene_solver_set_failure_implication"
    - from: "src/tests/scene_solver_contract_test.c"
      to: "src/ecs/ecs_scene.h"
      via: "ALONG X/Y/Z and coexistence recalc assertions"
      pattern: "CONSTRAINT_ALONG_X|CONSTRAINT_ALONG_Y|CONSTRAINT_ALONG_Z|CONSTRAINT_ANGLE"
---

<objective>
Implement deterministic runtime solve support for ALONG X/Y/Z across mixed point participants while preserving scene_solver authority, transactional behavior, and explicit diagnostics.

Purpose: Close SRLV-04 and runtime AXIS semantics after legality is locked by Wave 1.
Output: Scene solver ALONG branch + candidate resolution updates + runtime regression gates.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-principal-direction-constraint-expansion/23-CONTEXT.md
@.planning/phases/23-principal-direction-constraint-expansion/23-RESEARCH.md
@.planning/phases/23-principal-direction-constraint-expansion/23-VALIDATION.md
@.planning/phases/23-principal-direction-constraint-expansion/23-01-SUMMARY.md
@src/ecs/ecs_scene.h
@src/tests/scene_solver_contract_test.c
@src/tests/scene_solver_pass_policy_test.c

<interfaces>
From src/ecs/ecs_scene.h:
```c
static inline int scene_solver_ensure_point_candidate(ecs_scene_t *scene,
                                                      scene_solver_point_candidate_t *entries,
                                                      int max_entries,
                                                      int *io_entry_count,
                                                      ecs_entity_t entity,
                                                      constraint_participant_role_t role);
static inline bool scene_solver_request_recalculate(ecs_scene_t *scene, ecs_entity_t sketch);
static inline bool scene_solver_set_failure_implication(ecs_scene_t *scene,
                                                        ecs_entity_t sketch,
                                                        const ecs_entity_t *implicated_constraints,
                                                        int implicated_constraint_count,
                                                        const char *reason);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend scene solver participant resolution and ALONG X/Y/Z transactional solve branch</name>
  <files>src/ecs/ecs_scene.h</files>
  <behavior>
    - Candidate resolution accepts arc CENTER role in addition to existing endpoint roles (D-02, D-05).
    - Recalculate includes explicit ALONG X/Y/Z branch before unsupported-type fallback (D-07).
    - ALONG branch enforces deterministic axis-equality projection for pair/group participants (D-01, D-08).
    - Failure path reports explicit diagnostic + implication and leaves geometry unchanged (D-08).
    - Solver authority remains exclusively in `scene_solver_*` APIs (D-09).
  </behavior>
  <action>In `scene_solver_ensure_point_candidate(...)`, permit `CONSTRAINT_PARTICIPANT_ROLE_CENTER` for `GEOM_ARC` and preserve deterministic role normalization for point entities. In `scene_solver_request_recalculate(...)`, add transactional handling for `CONSTRAINT_ALONG_X/Y/Z`: resolve participant candidates from descriptors, compute deterministic target coordinate per axis, apply updates only to movable candidates, and accumulate residuals into existing convergence/pass policy flow. If unsatisfied (e.g., all fixed with residual > tolerance), emit explicit ALONG diagnostic message and set failure implication for the failing constraint; do not commit candidate geometry. Keep unsupported-type fallback intact for truly unsupported constraints.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target scene_solver_contract scene_solver_pass_policy && ctest -R "scene_solver_contract|scene_solver_pass_policy" --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>Scene-owned recalc solves ALONG X/Y/Z deterministically for descriptor-based point participants with transactional fail-safe behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression coverage for ALONG solve semantics and ALONG+ANGLE coexistence</name>
  <files>src/tests/scene_solver_contract_test.c, src/tests/scene_solver_pass_policy_test.c</files>
  <behavior>
    - Contract tests cover ALONG X/Y/Z on pair/group standalone points (AXIS-01/02/03).
    - Contract tests cover mixed participants including line endpoint and arc endpoint/center descriptors (AXIS-04).
    - Contract tests prove recalc remains functional with ALONG + ANGLE in the same sketch (SRLV-04, D-11).
    - Pass-policy tests verify ALONG constraints obey tolerance-stop and max-pass-stop deterministic behavior.
  </behavior>
  <action>Extend `scene_solver_contract_test.c` with new fixtures for ALONG X/Y/Z pair/group solves, mixed participant descriptor cases, and coexistence with `CONSTRAINT_ANGLE` under repeated recalc to assert deterministic status/geometry. Extend `scene_solver_pass_policy_test.c` with ALONG-inclusive pass cap/tolerance tests and explicit max-pass diagnostic assertions. Ensure tests include negative unsatisfied-fixed cases for explicit diagnostic behavior and no-mutation contracts (per D-08).</action>
  <verify>
    <automated>ctest -R "scene_solver_contract|scene_solver_pass_policy" --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>Automated runtime tests lock SRLV-04 and AXIS runtime semantics for deterministic recalculate behavior.</done>
</task>

</tasks>

<verification>
Run runtime solve gate:
`ctest -R "scene_solver_contract|scene_solver_pass_policy|scene_solver_trigger|scene_solver_diagnostics|endpoint_pick" --test-dir build-vulkan -C Release --output-on-failure`
</verification>

<success_criteria>
- ALONG X/Y/Z constraints solve deterministically in transactional recalc.
- Mixed point participant sources (point, line endpoint, arc endpoint/center) are solved through scene solver APIs.
- ALONG + ANGLE coexistence path is executable and green with explicit diagnostics on unsat.
</success_criteria>

<output>
After completion, create `.planning/phases/23-principal-direction-constraint-expansion/23-02-SUMMARY.md`
</output>

