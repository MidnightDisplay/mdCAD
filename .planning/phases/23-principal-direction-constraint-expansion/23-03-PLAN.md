---
phase: 23-principal-direction-constraint-expansion
plan: 03
type: execute
wave: 3
depends_on: [23-02]
files_modified:
  - src/ui/ui_entity_inspector.h
  - src/tests/endpoint_pick_test.c
  - src/tests/scene_solver_contract_test.c
  - src/tests/scene_solver_pass_policy_test.c
autonomous: false
requirements: [AXIS-01, AXIS-02, AXIS-03, AXIS-04, SRLV-04]
must_haves:
  truths:
    - "User can author ALONG X/Y/Z constraints from valid point-pair and point-group selections."
    - "User can author directional constraints using standalone points, line endpoints, and arc landmarks."
    - "User can recalculate sketches containing ALONG and ANGLE constraints and get deterministic results or explicit diagnostics."
  artifacts:
    - path: "src/ui/ui_entity_inspector.h"
      provides: "Constraint authoring/selection UX consistency for directional legality contracts"
    - path: "src/tests/endpoint_pick_test.c"
      provides: "Final legality + participant pick regression coverage for directional authoring"
    - path: "src/tests/scene_solver_contract_test.c"
      provides: "Final coexistence and deterministic recalc contract coverage"
    - path: "src/tests/scene_solver_pass_policy_test.c"
      provides: "Final ALONG-inclusive pass policy regression gate"
  key_links:
    - from: "src/ui/ui_entity_inspector.h"
      to: "src/constraints/constraint_types.h"
      via: "constraint menu legality filtering"
      pattern: "constraint_type_is_selection_legal"
    - from: "src/ui/ui_entity_inspector.h"
      to: "src/ecs/ecs_scene.h"
      via: "manual recalculate action"
      pattern: "scene_solver_request_recalculate"
    - from: "src/ui/ui_entity_inspector.h"
      to: "src/ecs/ecs_scene.h"
      via: "recalculate + diagnostics consumption"
      pattern: "scene_solver_request_recalculate|scene_solver_diagnostic_"
---

<objective>
Finalize Phase 23 user-visible authoring and verification closure: directional UX legality exposure, recalc coexistence behavior, and human validation of deterministic outcomes.

Purpose: Confirm implemented legality/runtime behavior is observable and trustworthy in actual sketch workflow.
Output: Inspector/authoring UX alignment updates + final automated regression rerun + blocking human verification checkpoint.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-principal-direction-constraint-expansion/23-CONTEXT.md
@.planning/phases/23-principal-direction-constraint-expansion/23-RESEARCH.md
@.planning/phases/23-principal-direction-constraint-expansion/23-VALIDATION.md
@.planning/phases/23-principal-direction-constraint-expansion/23-02-SUMMARY.md
@src/ui/ui_entity_inspector.h
@src/tests/endpoint_pick_test.c
@src/tests/scene_solver_contract_test.c
@src/tests/scene_solver_pass_policy_test.c

<interfaces>
From src/ui/ui_entity_inspector.h:
```c
static inline void ui_entity_inspector_draw_solver_section(...);
static inline bool ui_entity_inspector_draw_sketch_constraint_manager(...);
```

From src/ecs/ecs_scene.h:
```c
static inline bool scene_solver_request_recalculate(ecs_scene_t *scene, ecs_entity_t sketch);
static inline int scene_solver_diagnostic_count(ecs_scene_t *scene, ecs_entity_t sketch);
static inline const sketch_solver_diagnostic_t* scene_solver_diagnostic_at(ecs_scene_t *scene, ecs_entity_t sketch, int index);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align inspector constraint authoring and diagnostics UX with Phase 23 directional contracts</name>
  <files>src/ui/ui_entity_inspector.h</files>
  <action>Update inspector/constraint-manager presentation as needed so ALONG X/Y/Z options and participant context follow the legality decisions from Wave 1 (D-01..D-06) without introducing UI-owned solver logic (D-09). Keep manual recalc as a thin call to `scene_solver_request_recalculate(...)` and ensure diagnostic surfacing remains explicit for unsatisfied ALONG/ANGLE combinations (D-07, D-08). Do not add ARCI behavior (deferred).</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD endpoint_pick scene_solver_contract scene_solver_pass_policy && ctest -R "endpoint_pick|scene_solver_contract|scene_solver_pass_policy|scene_solver_trigger|scene_solver_diagnostics" --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>Inspector authoring surface reflects directional legality contracts and keeps scene solver authority and diagnostics behavior intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify ALONG X/Y/Z mixed-participant workflow and ALONG+ANGLE recalc behavior</name>
  <files>.planning/phases/23-principal-direction-constraint-expansion/23-03-SUMMARY.md, .planning/phases/23-principal-direction-constraint-expansion/23-VALIDATION.md</files>
  <action>Execute the five-step manual directional constraint verification sequence, capture pass/fail by step number, and record final checkpoint outcome in the phase summary/validation artifacts before closure.</action>
  <what-built>Directional legality + runtime solve support for ALONG X/Y/Z across point pairs/groups and mixed participant sources, with deterministic recalc and explicit diagnostics.</what-built>
  <how-to-verify>
    1) Open sketch workspace and create/select valid participant sets:
       - two standalone points
       - line endpoint + standalone point
       - arc endpoint or arc center + standalone point
       Confirm ALONG X/Y/Z authoring options appear for valid sets and remain hidden for illegal raw line/entity sets (AXIS-01..04, D-04/D-05).
    2) Apply ALONG X then recalculate; verify points align on common X-axis coordinate behavior deterministically across repeated recalc.
    3) Repeat for ALONG Y and ALONG Z with pair and group selections; verify deterministic outcomes on repeated recalc (AXIS-01/02/03).
    4) Build a sketch containing ALONG and ANGLE constraints together; run recalculate repeatedly and confirm it remains functional with stable deterministic results when satisfiable (SRLV-04, D-07).
    5) Create an unsatisfied ALONG/ANGLE case (e.g., fixed participants); verify explicit diagnostic message appears and geometry does not partially mutate (D-08).
  </how-to-verify>
  <verify>
    <automated>ctest -R "endpoint_pick|scene_solver_contract|scene_solver_pass_policy|scene_solver_trigger|scene_solver_diagnostics|scene_solver_drag" --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <resume-signal>Type "approved" if all five checks pass, or provide failed step numbers with observed behavior.</resume-signal>
  <done>Human confirms AXIS-01/02/03/04 and SRLV-04 behavior end-to-end in the interactive workflow.</done>
</task>

</tasks>

<verification>
Before requesting approval, run:
`ctest -R "endpoint_pick|scene_solver_contract|scene_solver_pass_policy|scene_solver_trigger|scene_solver_diagnostics|scene_solver_drag" --test-dir build-vulkan -C Release --output-on-failure`
</verification>

<success_criteria>
- Directional authoring UX reflects legality contracts for supported participant sources.
- ALONG + ANGLE recalc remains functional and deterministic in interactive use.
- Human verification approves end-to-end behavior with explicit diagnostics on unsat cases.
</success_criteria>

<output>
After completion, create `.planning/phases/23-principal-direction-constraint-expansion/23-03-SUMMARY.md`
</output>

