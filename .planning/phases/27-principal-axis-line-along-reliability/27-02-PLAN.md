---
phase: 27-principal-axis-line-along-reliability
plan: 02
type: execute
wave: 2
depends_on: [27-01]
files_modified:
  - src/tests/scene_solver_contract_test.c
  - src/tests/scene_solver_pass_policy_test.c
  - src/tests/scene_solver_diagnostics_test.c
  - src/ecs/ecs_scene.h
autonomous: true
requirements: [ALIN-04]
must_haves:
  truths:
    - "User can combine ALONG with LENGTH, ANGLE, and connectivity constraints and get deterministic solved outcomes in feasible cases."
    - "Equivalent mixed-constraint setups rerun to identical outcomes without selection-order drift."
    - "Infeasible fixed mixed ALONG cases fail transactionally with explicit ALONG family diagnostics."
  artifacts:
    - path: "src/tests/scene_solver_contract_test.c"
      provides: "Mixed ALONG+LENGTH+ANGLE+connectivity deterministic feasible and transactional infeasible tests."
    - path: "src/tests/scene_solver_pass_policy_test.c"
      provides: "Deterministic pass-policy coverage for mixed ALONG stacks."
    - path: "src/tests/scene_solver_diagnostics_test.c"
      provides: "ALONG X/Y/Z unsatisfied diagnostics assertions with family/type clarity."
    - path: "src/ecs/ecs_scene.h"
      provides: "Any minimal runtime ordering/implication fixes required for mixed-constraint determinism."
  key_links:
    - from: "src/tests/scene_solver_contract_test.c"
      to: "scene_solver_request_recalculate in src/ecs/ecs_scene.h"
      via: "mixed constraint fixture solve + immediate rerun equality checks"
      pattern: "CONSTRAINT_(ALONG_|LENGTH|ANGLE|COINCIDENT)"
    - from: "src/tests/scene_solver_diagnostics_test.c"
      to: "scene_solver_set_failure_implication in src/ecs/ecs_scene.h"
      via: "family-specific unsatisfied reason assertions"
      pattern: "Unsatisfied driving ALONG (X|Y|Z) constraint\\."
---

<objective>
Harden mixed-constraint determinism and diagnostics for ALONG line behavior under ALONG+LENGTH+ANGLE+connectivity compositions.

Purpose: Complete ALIN-04 with deterministic rerun proof and explicit transactional failure diagnostics.
Output: Mixed-stack contract/pass-policy/diagnostics coverage and any minimal runtime stabilization needed.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-principal-axis-line-along-reliability/27-CONTEXT.md
@.planning/phases/27-principal-axis-line-along-reliability/27-RESEARCH.md
@.planning/phases/27-principal-axis-line-along-reliability/27-VALIDATION.md
@.planning/phases/27-principal-axis-line-along-reliability/27-01-SUMMARY.md
@src/ecs/ecs_scene.h
@src/tests/scene_solver_contract_test.c
@src/tests/scene_solver_pass_policy_test.c
@src/tests/scene_solver_diagnostics_test.c

<interfaces>
From src/ecs/ecs_scene.h:
```c
static inline bool scene_solver_request_recalculate(ecs_scene_t *scene, ecs_entity_t sketch);
static inline void scene_solver_set_failure_implication(ecs_scene_t *scene,
                                                         ecs_entity_t sketch,
                                                         ecs_entity_t constraint_entity,
                                                         const char *reason,
                                                         const constraint_participant_descriptor_t *participants,
                                                         uint32_t participant_count);
```

From src/tests/scene_solver_contract_test.c (pattern to preserve):
```c
static bool vec3_exact_eq(const vec3_t *a, const vec3_t *b);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing mixed ALONG+LENGTH+ANGLE+connectivity deterministic tests</name>
  <read_first>@.planning/phases/27-principal-axis-line-along-reliability/27-CONTEXT.md, @src/tests/scene_solver_contract_test.c, @src/tests/scene_solver_pass_policy_test.c</read_first>
  <files>src/tests/scene_solver_contract_test.c, src/tests/scene_solver_pass_policy_test.c</files>
  <behavior>
    - Feasible mixed setup with ALONG + LENGTH + ANGLE + connectivity solves and remains stable under immediate rerun (ALIN-04).
    - Equivalent participant ordering/setup variants produce identical solved endpoint results (D-07).
    - Infeasible fixed mixed setup reports unsatisfied without mutating geometry (D-06, D-08).
  </behavior>
  <action>Create RED tests that encode deterministic equality (`vec3_exact_eq` where existing suite uses exact rerun checks) and transactional no-mutation assertions for infeasible fixed mixed stacks. Include at least one X, Y, and Z family mixed case so ALONG axis behavior stays consistent with D-01 and D-02. Keep test fixtures strictly in Phase 27 ALONG reliability scope.</action>
  <verify>
    <automated>ctest --test-dir build -C Release -R "scene_solver_contract|scene_solver_pass_policy" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>New mixed-stack tests fail before runtime stabilization and clearly isolate ALIN-04 deterministic/transactional expectations.</acceptance_criteria>
  <done>ALIN-04 contract and pass-policy RED coverage exists for mixed ALONG line scenarios.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stabilize mixed ALONG runtime ordering and add family-specific diagnostics assertions</name>
  <read_first>@src/ecs/ecs_scene.h, @src/tests/scene_solver_diagnostics_test.c, @.planning/phases/27-principal-axis-line-along-reliability/27-RESEARCH.md</read_first>
  <files>src/ecs/ecs_scene.h, src/tests/scene_solver_diagnostics_test.c</files>
  <action>Implement only minimal solver adjustments required for ALIN-04 failures exposed by Task 1, preserving transactional candidate staging and fixed-anchor policy (D-06, D-08). Keep deterministic participant handling/order invariance intact (D-07). Add diagnostics tests asserting explicit ALONG X/Y/Z family unsatisfied reasons so mixed failures are not ambiguous (D-05). Do not alter legality contracts except where required to preserve D-04 parity and do not modify non-ALONG families.</action>
  <verify>
    <automated>ctest --test-dir build -C Release -R "scene_solver_contract|scene_solver_pass_policy|scene_solver_diagnostics" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>Mixed deterministic tests pass and diagnostics tests confirm ALONG family-specific failure messages for transactional unsatisfied paths.</acceptance_criteria>
  <done>ALIN-04 runtime and diagnostics behavior is deterministic, explicit, and transaction-safe.</done>
</task>

<task type="auto">
  <name>Task 3: Execute full Phase 27 deterministic transactional closure gate</name>
  <read_first>@.planning/phases/27-principal-axis-line-along-reliability/27-VALIDATION.md, @src/tests/scene_solver_contract_test.c, @src/tests/scene_solver_diagnostics_test.c</read_first>
  <files>src/tests/scene_solver_contract_test.c, src/tests/scene_solver_pass_policy_test.c, src/tests/scene_solver_diagnostics_test.c</files>
  <action>Run the targeted Phase 27 closure gate with deterministic rerun discipline: execute contract+pass-policy+diagnostics+endpoint suites, then rerun same command to confirm stable outcomes and no flaky drift. Treat any mismatch as failure and fix only deterministic-test or ALONG-runtime causes within Phase 27 scope.</action>
  <verify>
    <automated>ctest --test-dir build -C Release -R "scene_solver_contract|scene_solver_pass_policy|scene_solver_diagnostics|endpoint_pick" --output-on-failure && ctest --test-dir build -C Release -R "scene_solver_contract|scene_solver_pass_policy|scene_solver_diagnostics|endpoint_pick" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>Both back-to-back gate runs are green with identical outcomes, confirming deterministic and transactional ALONG reliability under mixed constraints.</acceptance_criteria>
  <done>Phase 27 ALONG line reliability is closure-ready with ALIN-04 evidence.</done>
</task>

</tasks>

<verification>
Run targeted closure suite twice for deterministic proof:
`ctest --test-dir build -C Release -R "scene_solver_contract|scene_solver_pass_policy|scene_solver_diagnostics|endpoint_pick" --output-on-failure`
</verification>

<success_criteria>
- ALIN-04 mixed-constraint determinism is proven by passing rerun-stable tests.
- Mixed ALONG failures remain transactional and produce explicit ALONG family diagnostics.
- No out-of-scope tangency, gizmo, or documentation changes are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/27-principal-axis-line-along-reliability/27-02-SUMMARY.md`
</output>

