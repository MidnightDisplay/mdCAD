---
phase: 27-principal-axis-line-along-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ecs/ecs_scene.h
  - src/tests/scene_solver_contract_test.c
  - src/tests/endpoint_pick_test.c
autonomous: true
requirements: [ALIN-01, ALIN-02, ALIN-03]
must_haves:
  truths:
    - "User can apply ALONG X to a sketch line and solve does not fail immediately in feasible setups."
    - "User can apply ALONG Y and ALONG Z to sketch lines with the same no-immediate-failure behavior."
    - "Legacy legal line ENTITY ALONG authoring is resolved through descriptor-authoritative endpoint behavior, not runtime rejection."
  artifacts:
    - path: "src/ecs/ecs_scene.h"
      provides: "ALONG X/Y/Z solve-time participant normalization for line ENTITY -> POINT_A/POINT_B and axis-parallel enforcement."
    - path: "src/tests/scene_solver_contract_test.c"
      provides: "Regression tests for ALONG X/Y/Z single-line feasibility and transactional behavior."
    - path: "src/tests/endpoint_pick_test.c"
      provides: "Legality/runtime parity assertions for ALONG signatures including legacy line ENTITY acceptance."
  key_links:
    - from: "src/ecs/ecs_scene.h"
      to: "scene_solver_ensure_point_candidate in src/ecs/ecs_scene.h"
      via: "normalized participant descriptor expansion before candidate lookup"
      pattern: "CONSTRAINT_ALONG_(X|Y|Z)"
    - from: "src/tests/endpoint_pick_test.c"
      to: "constraint_type_is_selection_legal in src/constraints/constraint_types.h"
      via: "ALONG signature legality contracts"
      pattern: "constraint_type_is_selection_legal"
---

<objective>
Deliver reliable ALONG X/Y/Z line solving for feasible sketches by fixing legality/runtime parity and descriptor normalization in the ALONG branch.

Purpose: Remove immediate unsatisfied failures for legal ALONG line authoring while preserving transactional solver behavior.
Output: ALONG participant normalization implementation + ALIN-01/02/03 regression coverage.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-principal-axis-line-along-reliability/27-CONTEXT.md
@.planning/phases/27-principal-axis-line-along-reliability/27-RESEARCH.md
@.planning/phases/27-principal-axis-line-along-reliability/27-VALIDATION.md
@.planning/phases/26-line-line-constraint-coverage/26-02-SUMMARY.md
@src/ecs/ecs_scene.h
@src/tests/scene_solver_contract_test.c
@src/tests/endpoint_pick_test.c

<interfaces>
From src/ecs/ecs_scene.h:
```c
static inline ecs_entity_t scene_add_constraint_to_sketch_with_descriptors(
    ecs_scene_t *scene, ecs_entity_t sketch, constraint_type_t type,
    const constraint_participant_descriptor_t *participants, uint32_t participant_count,
    float value, bool has_value);

static inline bool scene_solver_request_recalculate(ecs_scene_t *scene, ecs_entity_t sketch);

static inline int scene_solver_ensure_point_candidate(ecs_scene_t *scene,
                                                       scene_solver_point_candidate_t *candidates,
                                                       uint32_t candidate_cap,
                                                       uint32_t *candidate_count,
                                                       const constraint_participant_descriptor_t *desc);
```

From src/constraints/constraint_types.h:
```c
static inline bool constraint_type_is_selection_legal(constraint_selection_signature_t *sig,
                                                       constraint_type_t type);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing ALONG X/Y/Z line reliability tests for legacy line signatures</name>
  <read_first>@.planning/phases/27-principal-axis-line-along-reliability/27-CONTEXT.md, @src/tests/scene_solver_contract_test.c, @src/tests/endpoint_pick_test.c</read_first>
  <files>src/tests/scene_solver_contract_test.c, src/tests/endpoint_pick_test.c</files>
  <behavior>
    - ALONG X on a feasible single sketch line created via legacy line ENTITY participant solves successfully (ALIN-01).
    - ALONG Y and ALONG Z on equivalent feasible single-line setups solve successfully (ALIN-02, ALIN-03).
    - ALONG legality acceptance and runtime solvability remain aligned for line signatures (D-04, D-05).
  </behavior>
  <action>Create RED tests that explicitly encode D-01 and D-02 axis-parallel expectations, and D-03 descriptor-authoritative behavior by asserting line ENTITY signatures are resolved through endpoint roles during solve. Include a transactional assertion that unsatisfied all-fixed ALONG still fails without geometry mutation (D-06, D-08). Keep scope strictly ALONG line reliability only (no tangency/gizmo/docs changes).</action>
  <verify>
    <automated>ctest --test-dir build -C Release -R "scene_solver_contract|endpoint_pick" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>Tests fail before implementation because current ALONG line runtime path cannot reliably satisfy legal legacy line signatures.</acceptance_criteria>
  <done>Executable ALIN-01/02/03 regression tests exist and define deterministic/transactional ALONG line reliability contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic ALONG participant normalization in solver runtime</name>
  <read_first>@src/ecs/ecs_scene.h, @.planning/phases/27-principal-axis-line-along-reliability/27-RESEARCH.md, @.planning/phases/27-principal-axis-line-along-reliability/27-CONTEXT.md</read_first>
  <files>src/ecs/ecs_scene.h</files>
  <action>In `scene_solver_request_recalculate(...)`, update ALONG X/Y/Z branch to normalize participants before point-candidate lookup: keep point descriptors as-is, keep line POINT_A/POINT_B descriptors as-is, and expand line ENTITY into two deterministic descriptors (POINT_A, POINT_B) per D-03. Deduplicate normalized descriptors deterministically by entity/role/sub-index to keep rerun stability (D-07). Enforce axis-parallel semantics per D-01/D-02 using orthogonal coordinate locks (ALONG X locks Y/Z, ALONG Y locks X/Z, ALONG Z locks X/Y). Preserve transactional behavior and fixed-anchor policy exactly: unsatisfied all-fixed setups return failure with no geometry mutation (D-06, D-08).</action>
  <verify>
    <automated>ctest --test-dir build -C Release -R "scene_solver_contract|endpoint_pick" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>ALONG X/Y/Z feasible single-line tests pass; legality/runtime parity remains intact; fixed unsatisfied ALONG remains transactional.</acceptance_criteria>
  <done>ALONG line runtime reliability is restored for X/Y/Z with deterministic descriptor normalization.</done>
</task>

<task type="auto">
  <name>Task 3: Run deterministic and transactional gate for ALONG line reliability slice</name>
  <read_first>@.planning/phases/27-principal-axis-line-along-reliability/27-VALIDATION.md, @src/tests/scene_solver_contract_test.c</read_first>
  <files>src/tests/scene_solver_contract_test.c, src/tests/endpoint_pick_test.c</files>
  <action>Execute the targeted ALONG reliability suite twice back-to-back to prove deterministic behavior (D-07), and confirm transactional failure semantics for unsatisfied fixed ALONG setups (D-06) through contract assertions. If assertions are brittle, only adjust test stability without changing ALONG semantics. Do not touch out-of-scope tangency drag, line gizmo behavior, or docs closure work.</action>
  <verify>
    <automated>ctest --test-dir build -C Release -R "scene_solver_contract|endpoint_pick" --output-on-failure && ctest --test-dir build -C Release -R "scene_solver_contract|endpoint_pick" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>Two consecutive targeted runs are green with no selection-order or rerun drift in ALONG X/Y/Z line tests.</acceptance_criteria>
  <done>ALIN-01/02/03 slice is deterministic and transaction-safe for execution handoff.</done>
</task>

</tasks>

<verification>
Run deterministic ALONG line reliability gate twice:
`ctest --test-dir build -C Release -R "scene_solver_contract|endpoint_pick" --output-on-failure`
</verification>

<success_criteria>
- ALIN-01, ALIN-02, and ALIN-03 are covered by passing regression tests for line ALONG X/Y/Z reliability.
- Solver ALONG runtime matches legality expectations and no longer immediately fails feasible legal line setups.
- Transactional and deterministic guarantees remain intact for ALONG line behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/27-principal-axis-line-along-reliability/27-01-SUMMARY.md`
</output>

