---
phase: 51-unsupported-platform-contract
plan: 03
type: execute
wave: 2
depends_on:
  - 51-02
files_modified:
  - samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml
  - samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs
  - samples/avalonia-mdcad-control.tests/MdCadEmbeddedControlTests.cs
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
  - PLAT-03
must_haves:
  truths:
    - "On unsupported hosts, the control shows the same core Windows-only truth immediately instead of a blank or misleading embed surface."
    - "In diagnostic mode, unsupported launch intent is shown only as secondary information while the launch action is disabled up front."
    - "Unsupported truth outranks Windows-style JSONL/path warnings, while the proven Phase 50 Windows behavior remains unchanged."
  artifacts:
    - path: "samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs"
      provides: "Factory-based backend selection, warning precedence, and disabled unsupported diagnostic actions."
      contains: "MdCadEmbedBackendFactory"
    - path: "samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml"
      provides: "Minimal sealed surface and richer diagnostic surface for unsupported runtime messaging."
      contains: "DiagnosticStartButton"
    - path: "samples/avalonia-mdcad-control.tests/MdCadEmbeddedControlTests.cs"
      provides: "Automated control-shell coverage for unsupported attach-time truth, warning precedence, and disabled diagnostic actions."
      contains: "UnsupportedState_ShowsCanonicalWarningImmediately"
  key_links:
    - from: "samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs"
      to: "samples/avalonia-mdcad-control/Host/MdCadEmbedBackendFactory.cs"
      via: "the shell now selects backend implementations internally instead of constructing Windows directly"
      pattern: "MdCadEmbedBackendFactory\\.Create"
    - from: "samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs"
      to: "samples/avalonia-mdcad-control/Host/MdCadUnsupportedRuntime.cs"
      via: "warning/detail text uses the canonical unsupported truth"
      pattern: "UnsupportedRuntimeMessage|StartBlockedReason"
    - from: "samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs"
      to: "samples/avalonia-mdcad-control/Host/MdCadLaunchSnapshot.cs"
      via: "unsupported warning precedence beats snapshot warnings while JSONL/live-refresh remain secondary diagnostics only"
      pattern: "requested ->|warning-only request omitted|viewer-managed"
    - from: "samples/avalonia-mdcad-control.tests/MdCadEmbeddedControlTests.cs"
      to: "samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs"
      via: "lightweight control-shell tests exercise attach-time unsupported warning, property-change updates, and disabled diagnostic launch affordance"
      pattern: "UnsupportedState_ShowsCanonicalWarningImmediately|UnsupportedState_RequestedInputsRemainSecondaryDiagnostics|UnsupportedState_DisablesDiagnosticLaunch"
---

<objective>
Wire the unsupported backend into the public control shell and surface the Phase 51 UI contract without changing the Phase 50 Windows behavior lock.

Purpose: This is the user-visible closeout for PLAT-01/02/03. The control must tell the truth immediately on unsupported hosts, keep sealed mode minimal, keep diagnostic mode informative, and ensure Windows-shaped snapshot warnings never outrank the unsupported message.
Output: Factory-based shell wiring, unsupported warning precedence, disabled unsupported diagnostic launch action, and a minimal unsupported placeholder surface.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/51-unsupported-platform-contract/51-CONTEXT.md
@.planning/phases/51-unsupported-platform-contract/51-RESEARCH.md
@.planning/phases/51-unsupported-platform-contract/51-VALIDATION.md
@.planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-03-SUMMARY.md
@.planning/phases/51-unsupported-platform-contract/51-02-PLAN.md
@samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml
@samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs
@samples/avalonia-mdcad-control/Host/MdCadLaunchSnapshot.cs

<interfaces>
Expected internal contract from Plan 51-02:
```csharp
internal interface IMdCadEmbedBackend
{
    Control Surface { get; }
    bool CanStartSession { get; }
    string? StartBlockedReason { get; }
    IntPtr PlaceholderHandle { get; }
    bool HasActiveSession { get; }
}

internal static class MdCadEmbedBackendFactory
{
    internal static IMdCadEmbedBackend Create(..., Func<bool>? isWindows = null);
}
```

Current shell entrypoints to preserve:
```csharp
public Task StartAsync();
public Task StopAsync();
public string? JsonlPath { get; set; }
public bool StartupLiveRefreshEnabled { get; set; }
public bool AutoStart { get; set; }
public MdCadPresentationMode PresentationMode { get; set; }
```

Planned automated shell proof for this plan:
```csharp
public sealed class MdCadEmbeddedControlTests
{
    [Fact]
    public void UnsupportedState_ShowsCanonicalWarningImmediately();

    [Fact]
    public void UnsupportedState_RequestedInputsRemainSecondaryDiagnostics();

    [Fact]
    public void UnsupportedState_DisablesDiagnosticLaunch();
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace direct Windows backend construction with internal factory wiring and unsupported warning precedence</name>
  <read_first>
    - .planning/phases/51-unsupported-platform-contract/51-CONTEXT.md
    - .planning/phases/51-unsupported-platform-contract/51-RESEARCH.md
    - .planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-03-SUMMARY.md
    - .planning/phases/51-unsupported-platform-contract/51-02-PLAN.md
    - samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs
    - samples/avalonia-mdcad-control/Host/MdCadEmbedBackendFactory.cs
    - samples/avalonia-mdcad-control/Host/MdCadLaunchSnapshot.cs
    - samples/avalonia-mdcad-control/Host/MdCadSessionCoordinator.cs
    - samples/avalonia-mdcad-control.tests/MdCad.Avalonia.Control.Tests.csproj
  </read_first>
  <files>samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs, samples/avalonia-mdcad-control.tests/MdCadEmbeddedControlTests.cs</files>
  <behavior>
    - Unsupported attach-time state shows the canonical D-03 message immediately when the control enters the visual tree.
    - Unsupported auto-start and launch-setting changes update descriptive state only and never request backend launch work.
    - Windows JSONL/path warnings remain secondary whenever `StartBlockedReason` is present.
  </behavior>
  <action>Update `MdCadEmbeddedControl.axaml.cs` to create the backend through `MdCadEmbedBackendFactory.Create(...)` instead of directly instantiating `WindowsMdCadEmbedBackend`, preserving the Phase 50 Windows lock unchanged. Pass the new blocked-reason delegate into `MdCadSessionCoordinator`, then add a shell helper that computes the primary warning/detail truth as `backend.StartBlockedReason ?? snapshot.WarningText` so D-10 and D-11 hold. On attach and on launch-affecting property changes, use that helper to refresh warning/detail text immediately per D-07 and D-08; do not let Windows-style snapshot warnings or missing-path messages outrank the unsupported truth. In the same task, add `MdCadEmbeddedControlTests.cs` in the existing test project to prove the shell wiring directly: force the unsupported branch without widening TFMs, assert the canonical warning appears immediately on attach, assert requested JSONL/live-refresh text stays secondary informational detail per D-09 and D-11, and assert the diagnostic launch path stays blocked. Keep `StartAsync()`/`StopAsync()` signatures unchanged, keep `RunCoordinatorTaskAsync(...)` plumbing, and do not add any public capability property, public test hook, or custom placeholder API.</action>
  <verify>
    <automated>dotnet test .\samples\avalonia-mdcad-control.tests\MdCad.Avalonia.Control.Tests.csproj --no-restore -v minimal --filter "FullyQualifiedName~MdCadEmbeddedControlTests|FullyQualifiedName~UnsupportedMdCadEmbedBackendTests|FullyQualifiedName~MdCadSessionCoordinatorTests"</automated>
  </verify>
  <acceptance_criteria>
    - `MdCadEmbeddedControl.axaml.cs` contains `MdCadEmbedBackendFactory.Create`
    - direct `new WindowsMdCadEmbedBackend(` construction is gone from the control shell
    - warning/detail helpers prefer `StartBlockedReason` over `snapshot.WarningText`
    - `MdCadEmbeddedControlTests.cs` exists with unsupported attach/property-change/disabled-launch coverage
    - unsupported property changes still call reconcile/update state but do not add launch work outside the coordinator
  </acceptance_criteria>
  <done>The public control shell now selects the correct backend internally and treats unsupported messaging as the primary truth without altering Windows behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Keep sealed mode minimal while diagnostic mode exposes secondary unsupported detail and disabled launch affordance</name>
  <read_first>
    - .planning/phases/51-unsupported-platform-contract/51-CONTEXT.md
    - .planning/phases/51-unsupported-platform-contract/51-RESEARCH.md
    - samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml
    - samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs
    - samples/avalonia-mdcad-control/Host/UnsupportedMdCadEmbedBackend.cs
    - samples/avalonia-mdcad-control.tests/MdCadEmbeddedControlTests.cs
  </read_first>
  <files>samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml, samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs, samples/avalonia-mdcad-control.tests/MdCadEmbeddedControlTests.cs</files>
  <action>Finish the narrow UI contract for PLAT-01 through PLAT-03 without inventing a UI-SPEC or broadening scope. Keep sealed mode minimal per D-02: the warning surface plus the unsupported backend’s inert placeholder should make the Windows-only truth obvious without turning the sealed control into a management panel. In diagnostic mode, disable `DiagnosticStartButton` up front when `StartBlockedReason` is present per D-12, keep stop disabled when no session exists, and rewrite the JSONL/live-refresh status lines as informational-only requested intent per D-09 and D-11. Extend `MdCadEmbeddedControlTests.cs` as needed so the control-shell proof covers the disabled diagnostic button and the secondary informational text directly rather than relying on grep. If you need extra placeholder chrome, keep it internal and minimal in `MdCadEmbeddedControl.axaml`; do not add a custom placeholder API, no new public properties, no Phase 52 TFM work, and no Phase 52.1/53 automation or proof tasks.</action>
  <verify>
    <automated>dotnet test .\samples\avalonia-mdcad-control.tests\MdCad.Avalonia.Control.Tests.csproj --no-restore -v minimal --filter "FullyQualifiedName~MdCadEmbeddedControlTests"</automated>
  </verify>
  <acceptance_criteria>
    - sealed mode remains free of extra management controls beyond the existing warning surface
    - diagnostic start is disabled when runtime activation is impossible
    - unsupported JSONL/live-refresh text is informational-only and does not replace the primary unsupported warning
    - no new public property, event, or API is added to `MdCadEmbeddedControl`
  </acceptance_criteria>
  <done>The control now tells the unsupported-platform truth clearly in both presentation modes while preserving the locked Windows host behavior and public API shape.</done>
</task>

</tasks>

<verification>
- `dotnet test .\samples\avalonia-mdcad-control.tests\MdCad.Avalonia.Control.Tests.csproj --no-restore -v minimal --filter "FullyQualifiedName~MdCadEmbeddedControlTests|FullyQualifiedName~UnsupportedMdCadEmbedBackendTests|FullyQualifiedName~MdCadSessionCoordinatorTests"`
- Confirm `MdCadEmbeddedControl.axaml.cs` uses factory selection and blocked warning precedence, and that `MdCadEmbeddedControl.axaml` keeps the existing sealed/diagnostic shell structure.
</verification>

<success_criteria>
- Unsupported hosts show one canonical Windows-only message immediately and never attempt background launch work.
- Diagnostic mode shows requested JSONL/live-refresh only as secondary information and disables launch up front.
- The control stays inside Phase 51 scope: no TFM widening, no runtime automation, no new public capability API, and no Windows behavior drift.
</success_criteria>

<output>
After completion, create `.planning/phases/51-unsupported-platform-contract/51-03-SUMMARY.md`
</output>
