---
phase: 51-unsupported-platform-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 51-01
files_modified:
  - samples/avalonia-mdcad-control/Host/IMdCadEmbedBackend.cs
  - samples/avalonia-mdcad-control/Host/MdCadEmbedBackendFactory.cs
  - samples/avalonia-mdcad-control/Host/MdCadSessionCoordinator.cs
  - samples/avalonia-mdcad-control/Host/MdCadUnsupportedRuntime.cs
  - samples/avalonia-mdcad-control/Host/UnsupportedMdCadEmbedBackend.cs
  - samples/avalonia-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs
  - samples/avalonia-mdcad-control.tests/MdCadSessionCoordinatorTests.cs
  - samples/avalonia-mdcad-control.tests/UnsupportedMdCadEmbedBackendTests.cs
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
  - PLAT-03
must_haves:
  truths:
    - "The internal backend selector can choose an explicit unsupported backend without changing the public control API or widening TFMs."
    - "Unsupported explicit start fails immediately with the same canonical message later shown by the control shell."
    - "Unsupported auto-start and launch-setting reconcile paths remain descriptive only and never invoke launch work."
  artifacts:
    - path: "samples/avalonia-mdcad-control/Host/MdCadEmbedBackendFactory.cs"
      provides: "Single internal supported-vs-unsupported backend selection seam."
      contains: "OperatingSystem.IsWindows"
    - path: "samples/avalonia-mdcad-control/Host/UnsupportedMdCadEmbedBackend.cs"
      provides: "Inert unsupported backend with canonical blocked reason and safe lifecycle methods."
      contains: "PlatformNotSupportedException"
    - path: "samples/avalonia-mdcad-control/Host/MdCadSessionCoordinator.cs"
      provides: "Blocked-reason aware lifecycle gate that suppresses unsupported auto-start/property-change launches."
      contains: "StartBlockedReason"
  key_links:
    - from: "samples/avalonia-mdcad-control/Host/MdCadEmbedBackendFactory.cs"
      to: "samples/avalonia-mdcad-control/Host/UnsupportedMdCadEmbedBackend.cs"
      via: "internal unsupported selection when the platform probe is false"
      pattern: "Create\\(|UnsupportedMdCadEmbedBackend"
    - from: "samples/avalonia-mdcad-control/Host/MdCadSessionCoordinator.cs"
      to: "samples/avalonia-mdcad-control/Host/IMdCadEmbedBackend.cs"
      via: "blocked reason is consulted before any launch work"
      pattern: "StartBlockedReason|PlatformNotSupportedException"
    - from: "samples/avalonia-mdcad-control.tests/UnsupportedMdCadEmbedBackendTests.cs"
      to: "samples/avalonia-mdcad-control/Host/MdCadUnsupportedRuntime.cs"
      via: "canonical message stays single-source for tests, backend throws, and later shell warnings"
      pattern: "UnsupportedRuntimeMessage"
---

<objective>
Implement the internal unsupported backend contract and blocked-reason seam without disturbing the Phase 50 Windows lock.

Purpose: This plan turns the Wave 0 tests into executable internal behavior: a factory-selected unsupported backend, one canonical Windows-only message, and coordinator semantics that stop unsupported auto-start/property changes from launching anything.
Output: Internal selector, unsupported backend, canonical message helper, blocked-reason-aware coordinator, and green targeted unit tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/51-unsupported-platform-contract/51-CONTEXT.md
@.planning/phases/51-unsupported-platform-contract/51-RESEARCH.md
@.planning/phases/51-unsupported-platform-contract/51-VALIDATION.md
@.planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-03-SUMMARY.md
@.planning/phases/51-unsupported-platform-contract/51-01-PLAN.md
@samples/avalonia-mdcad-control/Host/IMdCadEmbedBackend.cs
@samples/avalonia-mdcad-control/Host/MdCadSessionCoordinator.cs
@samples/avalonia-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs
@samples/avalonia-mdcad-control.tests/MdCadSessionCoordinatorTests.cs
@samples/avalonia-mdcad-control.tests/UnsupportedMdCadEmbedBackendTests.cs

<interfaces>
Implement these internal contracts in this plan:
```csharp
internal interface IMdCadEmbedBackend
{
    Control Surface { get; }
    bool CanStartSession { get; }
    string? StartBlockedReason { get; }
    IntPtr PlaceholderHandle { get; }
    bool HasActiveSession { get; }
    event Action? StateChanged;
    event Action<string>? UnexpectedSessionLoss;
    Task StartSessionAsync(MdCadLaunchSnapshot snapshot, CancellationToken cancellationToken);
    Task StopSessionAsync(CancellationToken cancellationToken);
    Task RecreateSurfaceAsync(CancellationToken cancellationToken);
}

internal static class MdCadEmbedBackendFactory
{
    internal static IMdCadEmbedBackend Create(..., Func<bool>? isWindows = null);
}
```

Canonical message from research:
```csharp
internal const string UnsupportedRuntimeMessage =
    "This host/control is valid, but embedded mdCAD viewing is Windows-only and will not launch on the current platform.";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the internal backend factory, canonical unsupported message, and inert unsupported backend</name>
  <read_first>
    - .planning/phases/51-unsupported-platform-contract/51-CONTEXT.md
    - .planning/phases/51-unsupported-platform-contract/51-RESEARCH.md
    - .planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-03-SUMMARY.md
    - .planning/phases/51-unsupported-platform-contract/51-01-PLAN.md
    - samples/avalonia-mdcad-control/Host/IMdCadEmbedBackend.cs
    - samples/avalonia-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs
    - samples/avalonia-mdcad-control.tests/UnsupportedMdCadEmbedBackendTests.cs
  </read_first>
  <files>samples/avalonia-mdcad-control/Host/IMdCadEmbedBackend.cs, samples/avalonia-mdcad-control/Host/MdCadEmbedBackendFactory.cs, samples/avalonia-mdcad-control/Host/MdCadUnsupportedRuntime.cs, samples/avalonia-mdcad-control/Host/UnsupportedMdCadEmbedBackend.cs, samples/avalonia-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs, samples/avalonia-mdcad-control.tests/UnsupportedMdCadEmbedBackendTests.cs</files>
  <behavior>
    - Test 1: the factory returns `UnsupportedMdCadEmbedBackend` when the injected platform probe is false.
    - Test 2: `StartSessionAsync(...)` on the unsupported backend throws immediately with the canonical D-03/D-05 message.
    - Test 3: `StopSessionAsync(...)` and `RecreateSurfaceAsync(...)` on the unsupported backend complete without changing session state.
  </behavior>
  <action>Implement the research-backed internal selector by adding `MdCadEmbedBackendFactory.cs` and `UnsupportedMdCadEmbedBackend.cs`, plus a single-source `MdCadUnsupportedRuntime.cs` that holds the canonical D-03/D-05 message. Extend `IMdCadEmbedBackend` with `string? StartBlockedReason` so the shell/coordinator can treat unsupported as a first-class blocked state. Keep `WindowsMdCadEmbedBackend` behavior locked by returning `null` for `StartBlockedReason` and leaving runtime resolution, attach polling, resize sync, relaunch logic, and all Phase 50 behavior untouched. The unsupported backend should expose an inert Avalonia surface, `CanStartSession = false`, `HasActiveSession = false`, `PlaceholderHandle = IntPtr.Zero`, throw `PlatformNotSupportedException` immediately from `StartSessionAsync(...)`, and keep stop/recreate safe no-ops per D-04 and D-06. Do not add any public capability property, custom placeholder text API, TFM changes, or runtime automation.</action>
  <verify>
    <automated>dotnet test .\samples\avalonia-mdcad-control.tests\MdCad.Avalonia.Control.Tests.csproj --no-restore -v minimal --filter "FullyQualifiedName~UnsupportedMdCadEmbedBackendTests"</automated>
  </verify>
  <acceptance_criteria>
    - `IMdCadEmbedBackend.cs` contains `string? StartBlockedReason`
    - `MdCadEmbedBackendFactory.cs` exists and branches on `OperatingSystem.IsWindows()` or the injected probe
    - `MdCadUnsupportedRuntime.cs` contains the canonical Windows-only message once
    - `UnsupportedMdCadEmbedBackend.cs` throws `PlatformNotSupportedException` from `StartSessionAsync`
    - `WindowsMdCadEmbedBackend.cs` stays functionally Windows-only with no launch-path changes beyond `StartBlockedReason`
  </acceptance_criteria>
  <done>The codebase can explicitly represent unsupported runtime selection internally without changing the public control contract or Phase 50 Windows behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the coordinator blocked-reason aware so unsupported reconcile paths never launch</name>
  <read_first>
    - .planning/phases/51-unsupported-platform-contract/51-CONTEXT.md
    - .planning/phases/51-unsupported-platform-contract/51-RESEARCH.md
    - .planning/phases/51-unsupported-platform-contract/51-01-PLAN.md
    - samples/avalonia-mdcad-control/Host/MdCadSessionCoordinator.cs
    - samples/avalonia-mdcad-control.tests/MdCadSessionCoordinatorTests.cs
    - samples/avalonia-mdcad-control.tests/UnsupportedMdCadEmbedBackendTests.cs
  </read_first>
  <files>samples/avalonia-mdcad-control/Host/MdCadSessionCoordinator.cs, samples/avalonia-mdcad-control.tests/MdCadSessionCoordinatorTests.cs</files>
  <behavior>
    - Test 1: blocked auto-start/request-reconcile updates warning state but never invokes the start delegate.
    - Test 2: launch-setting changes on a blocked backend keep the canonical unsupported truth primary and do not trigger stop/recreate/start work.
    - Test 3: explicit `StartAsync()` throws immediately with the blocked reason while `StopAsync()` stays safe and preserves unsupported state.
  </behavior>
  <action>Update `MdCadSessionCoordinator` to consume the new blocked-reason seam while preserving the Phase 50 lifecycle gate. Add an optional injected `Func<string?> getStartBlockedReason` (or equivalent non-breaking constructor extension) so existing compile-time callers keep building until Plan 51-03 rewires the shell. During reconcile, if a blocked reason exists and there is no active session, apply that warning immediately per D-07, suppress all launch work per D-08, and keep stop/recreate counters unchanged. For explicit `StartAsync()`, throw `PlatformNotSupportedException` with the canonical message before calling the start delegate. `StopAsync()` must remain a safe no-op when no session exists and must not clear the blocked warning. Do not change the generation coalescing, session-running bookkeeping, or Windows restart behavior proven in Phase 50.</action>
  <verify>
    <automated>dotnet test .\samples\avalonia-mdcad-control.tests\MdCad.Avalonia.Control.Tests.csproj --no-restore -v minimal --filter "FullyQualifiedName~UnsupportedMdCadEmbedBackendTests|FullyQualifiedName~MdCadSessionCoordinatorTests"</automated>
  </verify>
  <acceptance_criteria>
    - `MdCadSessionCoordinator.cs` consults a blocked reason before placeholder/start checks
    - explicit blocked start throws `PlatformNotSupportedException`
    - blocked `RequestReconcileAsync()` never calls the start delegate in the test harness
    - blocked `StopAsync()` leaves warning state intact and does not require a placeholder handle
  </acceptance_criteria>
  <done>Unsupported lifecycle behavior is explicit and deterministic in the shared coordinator while Windows sequencing remains locked.</done>
</task>

</tasks>

<verification>
- `dotnet test .\samples\avalonia-mdcad-control.tests\MdCad.Avalonia.Control.Tests.csproj --no-restore -v minimal --filter "FullyQualifiedName~UnsupportedMdCadEmbedBackendTests|FullyQualifiedName~MdCadSessionCoordinatorTests"`
- Confirm `WindowsMdCadEmbedBackend.cs` still contains the existing start/stop/recreate/attach logic and only adds the blocked-reason surface.
</verification>

<success_criteria>
- The unsupported backend is selected internally through one factory seam.
- Unsupported start/stop/reconcile behavior is now deterministic and unit-tested.
- Windows-only backend behavior stays intact and Phase 51 still avoids public API widening.
</success_criteria>

<output>
After completion, create `.planning/phases/51-unsupported-platform-contract/51-02-SUMMARY.md`
</output>
