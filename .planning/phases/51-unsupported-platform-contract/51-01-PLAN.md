---
phase: 51-unsupported-platform-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - samples/avalonia-mdcad-control.tests/MdCadSessionCoordinatorTests.cs
  - samples/avalonia-mdcad-control.tests/UnsupportedMdCadEmbedBackendTests.cs
autonomous: true
requirements:
  - PLAT-01
  - PLAT-02
  - PLAT-03
must_haves:
  truths:
    - "Phase 51 has executable proof that unsupported platforms show a canonical Windows-only truth instead of silently idling."
    - "Phase 51 has executable proof that unsupported auto-start and launch-setting reconcile paths never invoke backend launch work."
    - "Phase 51 has executable proof that unsupported explicit start fails immediately while stop remains a safe no-op."
  artifacts:
    - path: "samples/avalonia-mdcad-control.tests/UnsupportedMdCadEmbedBackendTests.cs"
      provides: "Wave 0 contract tests for backend selection, canonical unsupported messaging, and inert lifecycle behavior."
      contains: "BackendFactory_SelectsUnsupportedBackend_WhenPlatformProbeFalse"
    - path: "samples/avalonia-mdcad-control.tests/MdCadSessionCoordinatorTests.cs"
      provides: "Wave 0 coordinator tests for blocked no-launch reconcile and immediate unsupported start failure."
      contains: "AutoStart_WhenBackendBlocked_NeverInvokesStartDelegate"
  key_links:
    - from: "samples/avalonia-mdcad-control.tests/UnsupportedMdCadEmbedBackendTests.cs"
      to: "samples/avalonia-mdcad-control/Host/MdCadEmbedBackendFactory.cs"
      via: "forced unsupported branch selection on the existing Windows-targeted test lane"
      pattern: "BackendFactory_SelectsUnsupportedBackend_WhenPlatformProbeFalse"
    - from: "samples/avalonia-mdcad-control.tests/MdCadSessionCoordinatorTests.cs"
      to: "samples/avalonia-mdcad-control/Host/MdCadSessionCoordinator.cs"
      via: "blocked-reason reconcile and explicit start assertions"
      pattern: "AutoStart_WhenBackendBlocked_NeverInvokesStartDelegate|StartAsync_WhenBackendBlocked_ThrowsImmediately"
---

<objective>
Create Wave 0 proof for the unsupported-platform contract before implementation changes land.

Purpose: Per D-04 through D-12, Phase 51 should not rely on interpretation. The executor needs named failing tests that pin canonical unsupported messaging, no-launch reconcile behavior, and safe stop semantics before wiring new backend internals.
Output: New unsupported-backend tests plus coordinator additions that define the exact PLAT-01/02/03 contract.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/51-unsupported-platform-contract/51-CONTEXT.md
@.planning/phases/51-unsupported-platform-contract/51-RESEARCH.md
@.planning/phases/51-unsupported-platform-contract/51-VALIDATION.md
@.planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-CONTEXT.md
@.planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-03-SUMMARY.md
@samples/avalonia-mdcad-control.tests/MdCadSessionCoordinatorTests.cs
@samples/avalonia-mdcad-control/Host/IMdCadEmbedBackend.cs
@samples/avalonia-mdcad-control/Host/MdCadSessionCoordinator.cs
@samples/avalonia-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs

<interfaces>
From samples/avalonia-mdcad-control/Host/IMdCadEmbedBackend.cs:
```csharp
internal interface IMdCadEmbedBackend
{
    Control Surface { get; }
    bool CanStartSession { get; }
    IntPtr PlaceholderHandle { get; }
    bool HasActiveSession { get; }
    event Action? StateChanged;
    event Action<string>? UnexpectedSessionLoss;
    Task StartSessionAsync(MdCadLaunchSnapshot snapshot, CancellationToken cancellationToken);
    Task StopSessionAsync(CancellationToken cancellationToken);
    Task RecreateSurfaceAsync(CancellationToken cancellationToken);
}
```

From samples/avalonia-mdcad-control/Host/MdCadSessionCoordinator.cs:
```csharp
public Task RequestReconcileAsync();
public Task StartAsync();
public Task StopAsync();
public bool IsSessionRunning { get; }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add unsupported backend contract tests for factory selection and inert lifecycle behavior</name>
  <read_first>
    - .planning/phases/51-unsupported-platform-contract/51-CONTEXT.md
    - .planning/phases/51-unsupported-platform-contract/51-RESEARCH.md
    - .planning/phases/51-unsupported-platform-contract/51-VALIDATION.md
    - samples/avalonia-mdcad-control/Host/IMdCadEmbedBackend.cs
    - samples/avalonia-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs
    - samples/avalonia-mdcad-control.tests/WindowsMdCadEmbedBackendTests.cs
  </read_first>
  <files>samples/avalonia-mdcad-control.tests/UnsupportedMdCadEmbedBackendTests.cs</files>
  <behavior>
    - Test 1: `BackendFactory_SelectsUnsupportedBackend_WhenPlatformProbeFalse` proves the internal selector can force the unsupported branch without widening the public TFM.
    - Test 2: `StartSessionAsync_WhenUnsupported_ThrowsImmediately_WithCanonicalMessage` proves D-04 and D-05 with one canonical Windows-only message.
    - Test 3: `StopAndRecreateSurface_WhenUnsupported_AreSafeNoOps` proves D-06 and keeps the backend lifecycle-compatible but inert.
  </behavior>
  <action>Create `UnsupportedMdCadEmbedBackendTests.cs` as the Wave 0 contract file required by `51-VALIDATION.md`. Write the tests first and keep them Windows-targeted by forcing unsupported selection internally per the research guidance. The tests must reference D-03, D-04, D-05, D-06, D-07, and D-10 explicitly in comments or method names, lock one canonical unsupported-runtime message, and avoid any public `IsRuntimeSupported` capability or custom placeholder API work because those are deferred. Do not implement the backend or factory in this task.</action>
  <verify>
    <automated>MISSING — Task 1 defines the failing contract first; after 51-02 lands, run `dotnet test .\samples\avalonia-mdcad-control.tests\MdCad.Avalonia.Control.Tests.csproj --no-restore -v minimal --filter "FullyQualifiedName~UnsupportedMdCadEmbedBackendTests"`</automated>
  </verify>
  <acceptance_criteria>
    - `samples/avalonia-mdcad-control.tests/UnsupportedMdCadEmbedBackendTests.cs` exists
    - file contains `BackendFactory_SelectsUnsupportedBackend_WhenPlatformProbeFalse`
    - file contains `StartSessionAsync_WhenUnsupported_ThrowsImmediately_WithCanonicalMessage`
    - file contains `StopAndRecreateSurface_WhenUnsupported_AreSafeNoOps`
    - no test introduces a new public capability property or placeholder customization API
  </acceptance_criteria>
  <done>Wave 0 unsupported-backend tests exist and clearly pin the internal selector plus inert unsupported lifecycle contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add coordinator tests for blocked no-launch reconcile and immediate unsupported imperative behavior</name>
  <read_first>
    - .planning/phases/51-unsupported-platform-contract/51-CONTEXT.md
    - .planning/phases/51-unsupported-platform-contract/51-RESEARCH.md
    - .planning/phases/51-unsupported-platform-contract/51-VALIDATION.md
    - samples/avalonia-mdcad-control.tests/MdCadSessionCoordinatorTests.cs
    - samples/avalonia-mdcad-control/Host/MdCadSessionCoordinator.cs
    - samples/avalonia-mdcad-control/Host/MdCadLaunchSnapshot.cs
  </read_first>
  <files>samples/avalonia-mdcad-control.tests/MdCadSessionCoordinatorTests.cs</files>
  <behavior>
    - Test 1: `AutoStart_WhenBackendBlocked_NeverInvokesStartDelegate` proves D-07 and PLAT-02.
    - Test 2: `LaunchSettingChanges_WhenBackendBlocked_UpdateWarningWithoutStarting` proves D-08 through D-11 and keeps unsupported truth primary.
    - Test 3: `StartAsync_WhenBackendBlocked_ThrowsImmediately_AndStopAsync_RemainsSafe` proves D-04, D-05, and D-06.
  </behavior>
  <action>Extend `MdCadSessionCoordinatorTests.cs` with failing coverage for the blocked-reason seam described in the research. The harness should model an internal unsupported backend on the existing Windows test lane: no placeholder dependency, no start delegate invocation during `RequestReconcileAsync()`, immediate failure for explicit `StartAsync()`, and safe `StopAsync()` when no session exists. Keep the assertions scoped to Phase 51 only: no TFM widening, no Avalonia headless harness, no runtime automation, and no Phase 53 proof expansion.</action>
  <verify>
    <automated>MISSING — Task 2 defines the failing contract first; after 51-02 lands, run `dotnet test .\samples\avalonia-mdcad-control.tests\MdCad.Avalonia.Control.Tests.csproj --no-restore -v minimal --filter "FullyQualifiedName~MdCadSessionCoordinatorTests"`</automated>
  </verify>
  <acceptance_criteria>
    - `MdCadSessionCoordinatorTests.cs` contains `AutoStart_WhenBackendBlocked_NeverInvokesStartDelegate`
    - `MdCadSessionCoordinatorTests.cs` contains `LaunchSettingChanges_WhenBackendBlocked_UpdateWarningWithoutStarting`
    - `MdCadSessionCoordinatorTests.cs` contains `StartAsync_WhenBackendBlocked_ThrowsImmediately_AndStopAsync_RemainsSafe`
    - assertions prove warning-state preservation and zero start invocations for unsupported reconcile
  </acceptance_criteria>
  <done>Wave 0 coordinator tests define the unsupported no-launch and imperative API contract before implementation begins.</done>
</task>

</tasks>

<verification>
- Confirm `samples/avalonia-mdcad-control.tests/UnsupportedMdCadEmbedBackendTests.cs` exists with the three named tests.
- Confirm `samples/avalonia-mdcad-control.tests/MdCadSessionCoordinatorTests.cs` includes blocked unsupported-path coverage.
- Leave the suite red until Plan 51-02 implements the new internal contract.
</verification>

<success_criteria>
- Wave 0 explicitly maps PLAT-01, PLAT-02, and PLAT-03 into named automated tests.
- Unsupported behavior is specified through unit tests, not through manual interpretation.
- The tests stay inside the existing Windows-targeted test project and do not widen runtime claims.
</success_criteria>

<output>
After completion, create `.planning/phases/51-unsupported-platform-contract/51-01-SUMMARY.md`
</output>
