---
phase: 10-sketch-foundations-managers
plan: 03
type: execute
wave: 3
depends_on:
  - 10-02
files_modified:
  - src/ui/ui_entity_inspector.h
  - .planning/phases/10-sketch-foundations-managers/evidence/10-sketch-managers-smoke-checklist.md
autonomous: false
requirements:
  - SKCH-03
must_haves:
  truths:
    - "User can see a flat GeometryManager list with type, name, and fixed/loose status for sketch geometry."
    - "User can single-select and multi-select sketch geometry rows and run fix/unfix/delete."
    - "One multi-select manager action creates one undo step and list state refreshes immediately."
  artifacts:
    - path: "src/ui/ui_entity_inspector.h"
      provides: "GeometryManager row rendering, multi-select handling, and bulk action wiring."
    - path: ".planning/phases/10-sketch-foundations-managers/evidence/10-sketch-managers-smoke-checklist.md"
      provides: "Repeatable manual verification script for SKCH-01..03 interactions."
  key_links:
    - from: "src/ui/ui_entity_inspector.h"
      to: "src/undo_redo_exec.h"
      via: "bulk fix/unfix/delete command recording"
      pattern: "undo_cmd_bulk_.*sketch|undo_cmd_bulk_delete"
    - from: "src/ui/ui_entity_inspector.h"
      to: "src/ecs/ecs_scene.h"
      via: "geometry list refresh after scene mutations"
      pattern: "scene_get_children|scene_remove_entity"
---

<objective>
Complete GeometryManager interaction workflows so sketch geometry can be managed through flat-list, multi-select, and atomic undo behavior.

Purpose: Deliver full SKCH-03 user behavior and close the Phase 10 manager interaction loop.
Output: GeometryManager list/actions in inspector plus checklist evidence artifact for consistent validation.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sketch-foundations-managers/10-CONTEXT.md
@.planning/phases/10-sketch-foundations-managers/10-RESEARCH.md
@.planning/phases/10-sketch-foundations-managers/10-VALIDATION.md
@.planning/phases/10-sketch-foundations-managers/10-UI-SPEC.md
@.planning/phases/10-sketch-foundations-managers/10-02-SUMMARY.md

<interfaces>
From src/ui/ui_entity_inspector.h:
```c
static inline void ui_entity_inspector_draw_single(ui_entity_inspector_state_t *state, ecs_entity_t e);
```

From src/selection.h:
```c
static inline void selection_handle_click(selection_buffer_t *sel, ecs_entity_t clicked_entity, bool shift_held, bool ctrl_held);
```

From Plan 10-01 undo contracts:
```c
CMD_BULK_SET_SKETCH_FIXED
CMD_BULK_DELETE_ENTITIES
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement flat GeometryManager list with row status and multi-select behavior</name>
  <read_first>
    - src/ui/ui_entity_inspector.h
    - src/selection.h
    - .planning/phases/10-sketch-foundations-managers/10-CONTEXT.md
    - .planning/phases/10-sketch-foundations-managers/10-UI-SPEC.md
  </read_first>
  <files>src/ui/ui_entity_inspector.h</files>
  <action>Add a sketch-only GeometryManager section that renders a flat row list per D-03 with geometry type + name + fixed/loose label on each row (D-07). Support single-select and multi-select row behavior per D-04 using ctrl/shift conventions consistent with existing selection patterns. Keep row spacing/token usage aligned to UI-SPEC and do not introduce tree/grouped layout.</action>
  <acceptance_criteria>
    - GeometryManager rows are rendered as a flat list (not nested/grouped) with type, name, and fixed/loose status (D-03, D-07).
    - Click/ctrl/shift interactions produce single and multi-select state within GeometryManager (D-04).
    - List refreshes immediately after selection or model changes (D-05 immediate refresh expectation).
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD</automated>
  </verify>
  <done>GeometryManager list interaction model is complete and visually aligned with Phase 10 contract.</done>
</task>

<task type="auto">
  <name>Task 2: Wire GeometryManager fix/unfix/delete actions to atomic bulk undo and publish smoke checklist</name>
  <read_first>
    - src/ui/ui_entity_inspector.h
    - src/undo_redo.h
    - src/undo_redo_exec.h
    - .planning/phases/10-sketch-foundations-managers/10-CONTEXT.md
  </read_first>
  <files>src/ui/ui_entity_inspector.h, .planning/phases/10-sketch-foundations-managers/evidence/10-sketch-managers-smoke-checklist.md</files>
  <action>Add `Fix`, `Unfix`, and `Delete` bulk actions to GeometryManager for selected rows and wire them to the bulk undo commands from Plan 10-01. Ensure each user action emits one undo command per D-05 and refreshes manager state immediately after execution. Use confirmation copy from UI-SPEC for delete actions, keep fixed-state visual/manager semantics only per D-06, and avoid any transform/gizmo enforcement logic. In the same task, create a repeatable smoke checklist artifact covering SKCH-01/02/03 interaction checks, explicit D-05 one-step undo checks, and out-of-scope reminders for deferred Phase 11/12 behaviors.</action>
  <acceptance_criteria>
    - Fix/Unfix/Delete are available for selected geometry rows and work for single or multi-selection (SKCH-03, D-04).
    - One click on a bulk action creates one undo stack entry regardless of selection size (D-05).
    - Delete action uses UI-SPEC destructive confirmation wording and updates list state immediately.
    - Checklist file exists under phase evidence directory with sections for SKCH-01, SKCH-02, and SKCH-03.
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target math-validation</automated>
  </verify>
  <done>Manager actions perform correct mutations with atomic undo semantics and repeatable validation script exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify GeometryManager SKCH-03 interaction and undo semantics</name>
  <read_first>
    - .planning/phases/10-sketch-foundations-managers/evidence/10-sketch-managers-smoke-checklist.md
    - .planning/phases/10-sketch-foundations-managers/10-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - Flat list rows show type/name/fixed status.
    - Multi-select with ctrl/shift works and bulk actions apply to all selected rows.
    - One Undo reverses the full bulk action set.
  </acceptance_criteria>
  <files>src/ui/ui_entity_inspector.h, .planning/phases/10-sketch-foundations-managers/evidence/10-sketch-managers-smoke-checklist.md</files>
  <action>Execute the checklist-driven manual verification for GeometryManager interaction and undo semantics; if any expected behavior fails, document the failing step and return to implementation before marking approved.</action>
  <what-built>GeometryManager flat-list operations, bulk fix/unfix/delete, and one-step undo behavior.</what-built>
  <how-to-verify>
    1. Build/run `mdCAD`.
    2. Create a sketch and add at least 3 geometry items.
    3. In GeometryManager, select rows using single click, Ctrl+click, and Shift+click.
    4. Run `Fix` then `Undo` once; verify all affected rows revert together.
    5. Run `Unfix` then `Undo` once; verify all affected rows revert together.
    6. Run `Delete` on multi-selection, confirm dialog text, then `Undo` once; verify all deleted rows restore together.
  </how-to-verify>
  <resume-signal>Type "approved" if all manager interactions pass, or provide failing step numbers.</resume-signal>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD</automated>
  </verify>
  <done>Human confirms SKCH-03 workflows are complete and undo semantics match D-05.</done>
</task>

</tasks>

<verification>
Execute checklist from `.planning/phases/10-sketch-foundations-managers/evidence/10-sketch-managers-smoke-checklist.md` after automated build commands pass.
</verification>

<success_criteria>
- GeometryManager is flat-list based and shows required per-row metadata.
- Single and multi-select fix/unfix/delete workflows operate with immediate refresh and one-step undo.
- Phase 10 delivers SKCH-03 without introducing deferred constraint/solver behaviors.
</success_criteria>

<output>
After completion, create `.planning/phases/10-sketch-foundations-managers/10-03-SUMMARY.md`.
</output>
