---
phase: 10-sketch-foundations-managers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sketch_comp.h
  - src/components/sketch_geometry_state_comp.h
  - src/ecs/ecs_world.h
  - src/ecs/ecs_scene.h
  - src/undo_redo.h
  - src/undo_redo_exec.h
autonomous: true
requirements:
  - SKCH-01
  - SKCH-03
must_haves:
  truths:
    - "User can create a sketch container entity and attach point/line/arc geometry to it."
    - "Geometry fix/unfix/delete operations can be executed in bulk and undone in one undo step."
  artifacts:
    - path: "src/components/sketch_comp.h"
      provides: "Sketch status/color policy/constraint count data contract."
    - path: "src/components/sketch_geometry_state_comp.h"
      provides: "Per-geometry fixed/loose state metadata for manager actions."
    - path: "src/ecs/ecs_scene.h"
      provides: "Sketch creation + geometry attach/query helpers."
    - path: "src/undo_redo_exec.h"
      provides: "Bulk fixed-state and bulk delete apply/unapply command execution."
  key_links:
    - from: "src/ecs/ecs_world.h"
      to: "src/components/sketch_comp.h"
      via: "component registration in ecs_world_init"
      pattern: "SketchComp_id|SketchGeometryStateComp_id"
    - from: "src/ecs/ecs_scene.h"
      to: "src/components/sketch_comp.h"
      via: "scene sketch helper reads/writes sketch metadata"
      pattern: "scene_add_sketch|scene_attach_geometry_to_sketch"
    - from: "src/undo_redo_exec.h"
      to: "src/components/sketch_geometry_state_comp.h"
      via: "bulk fixed/unfixed apply and unapply"
      pattern: "CMD_BULK_SET_SKETCH_FIXED|CMD_BULK_DELETE_ENTITIES"
---

<objective>
Establish Phase 10 sketch foundation contracts and mutation infrastructure so UI plans can build manager workflows without inventing data models.

Purpose: Deliver the ECS-level sketch container model, fixed-state model, and atomic bulk undo primitives required by SKCH-01 and SKCH-03.
Output: New sketch components, sketch scene APIs, and undo command types for one-click bulk operations.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sketch-foundations-managers/10-CONTEXT.md
@.planning/phases/10-sketch-foundations-managers/10-RESEARCH.md
@.planning/phases/10-sketch-foundations-managers/10-UI-SPEC.md

<interfaces>
From src/ecs/ecs_scene.h:
```c
static inline ecs_entity_t scene_add_anchor(ecs_scene_t *scene, const char *name, const char *desc);
static inline void scene_set_parent(ecs_scene_t *scene, ecs_entity_t child, ecs_entity_t parent);
static inline void scene_remove_entity(ecs_scene_t *scene, ecs_entity_t e);
static inline int scene_count_children(ecs_scene_t *scene, ecs_entity_t parent);
```

From src/undo_redo.h:
```c
typedef enum {
    CMD_BULK_SET_COLOR,
    CMD_BULK_SET_VISIBLE
} undo_cmd_type_t;
```

From src/undo_redo_exec.h:
```c
static inline void undo_cmd_bulk_set_color(undo_redo_t *ur, ecs_entity_t *entities, vec4_t *old_colors, int count, vec4_t new_color);
static inline void undo_cmd_bulk_set_visible(undo_redo_t *ur, ecs_entity_t *entities, bool *old_visible, int count, bool new_visible);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define sketch ECS contracts and register components</name>
  <read_first>
    - src/components/geometry_comp.h
    - src/ecs/ecs_world.h
    - .planning/phases/10-sketch-foundations-managers/10-CONTEXT.md
  </read_first>
  <files>src/components/sketch_comp.h, src/components/sketch_geometry_state_comp.h, src/ecs/ecs_world.h</files>
  <action>Create `SketchComp` and `SketchGeometryStateComp` contracts and register them in `ecs_world_init`/world state accessors. Implement locked decisions: status taxonomy `solved|loose|fixed|error` per D-08, visible fixed-state labeling support per D-07, and sketch color inheritance rule (non-black geometry RGB overrides inherited sketch color) per D-09. Keep header-only `static inline` style and avoid any Phase 11 constraint authoring behavior.</action>
  <acceptance_criteria>
    - `src/components/sketch_comp.h` exports sketch status enum and color-policy helper(s) implementing D-09.
    - `src/components/sketch_geometry_state_comp.h` exports fixed-state data used by managers (D-07).
    - `src/ecs/ecs_world.h` registers both components and exposes getter helper(s).
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD</automated>
  </verify>
  <done>Sketch component model compiles and is queryable through ecs_world helpers.</done>
</task>

<task type="auto">
  <name>Task 2: Add sketch scene helpers for creation, attachment, and status/count derivation</name>
  <read_first>
    - src/ecs/ecs_scene.h
    - src/ui/ui_scene_hierarchy.h
    - .planning/phases/10-sketch-foundations-managers/10-RESEARCH.md
  </read_first>
  <files>src/ecs/ecs_scene.h</files>
  <action>Add explicit sketch helpers (`scene_add_sketch`, geometry attach-to-sketch helpers for point/line/arc, geometry/constraint count readers, and placeholder status derivation) to satisfy D-01, D-02, D-06, and D-08. Keep fixed-state semantics manager/visual only (no gizmo/transform enforcement per D-06). Use ECS parent-child ownership as the source of geometry counts and avoid ad hoc registries.</action>
  <acceptance_criteria>
    - Scene API includes sketch creation and geometry attach helpers used by both Add Entity and manager UI flows (D-01, D-02).
    - Sketch status helper returns only `solved|loose|fixed|error` labels with consistent placeholder rules (D-08).
    - Count helpers derive geometry from sketch-owned children and provide stable constraint count contract for inspector.
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD</automated>
  </verify>
  <done>Scene layer exposes stable sketch APIs that downstream UI can consume directly.</done>
</task>

<task type="auto">
  <name>Task 3: Extend undo command model for atomic multi-select fix/unfix/delete</name>
  <read_first>
    - src/undo_redo.h
    - src/undo_redo_exec.h
    - .planning/phases/10-sketch-foundations-managers/10-CONTEXT.md
  </read_first>
  <files>src/undo_redo.h, src/undo_redo_exec.h</files>
  <action>Add dedicated bulk command types and record/apply/unapply handlers for multi-select fixed-state toggle and multi-delete actions so one user action maps to one undo step (D-04, D-05). Reuse existing bulk-command memory ownership patterns, keep command naming consistent, and do not split one manager click into per-entity undo entries.</action>
  <acceptance_criteria>
    - Undo command enum/data includes bulk fixed-state and bulk delete command payloads.
    - `undo_redo_exec.h` can apply and unapply both new bulk commands deterministically.
    - Command labels/readouts represent one combined action, not N actions for N entities (D-05).
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD</automated>
  </verify>
  <done>Bulk manager mutations are undo-safe and atomic at command-stack level.</done>
</task>

</tasks>

<verification>
Run `cmake --build build-vulkan --config Release --target math-validation` after Task 3 to ensure no regressions in existing validation target wiring.
</verification>

<success_criteria>
- Sketch ECS and scene APIs exist for creation + geometry attachment (SKCH-01).
- Undo system supports atomic bulk fix/unfix/delete semantics needed for GeometryManager multi-select flows (SKCH-03).
- Phase 10 contracts are implemented without introducing Phase 11 constraint-authoring interactions.
</success_criteria>

<output>
After completion, create `.planning/phases/10-sketch-foundations-managers/10-01-SUMMARY.md`.
</output>
