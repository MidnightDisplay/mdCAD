---
phase: 10-sketch-foundations-managers
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/ui/ui_scene_hierarchy.h
  - src/ui/ui_entity_inspector.h
autonomous: false
requirements:
  - SKCH-01
  - SKCH-02
must_haves:
  truths:
    - "User can create a sketch from Add Entity and add point/line/arc geometry to the active sketch from both entry points."
    - "User can open Entity Inspector for a sketch and see status, color policy summary, geometry count, and constraint count."
  artifacts:
    - path: "src/ui/ui_scene_hierarchy.h"
      provides: "Sketch creation/attach controls in Add Entity + GeometryManager add controls."
    - path: "src/ui/ui_entity_inspector.h"
      provides: "SketchManager and sketch-level status/count surfaces."
  key_links:
    - from: "src/ui/ui_scene_hierarchy.h"
      to: "src/ecs/ecs_scene.h"
      via: "Create Sketch and add geometry menu actions"
      pattern: "scene_add_sketch|scene_attach_.*_to_sketch"
    - from: "src/ui/ui_entity_inspector.h"
      to: "src/components/sketch_comp.h"
      via: "inspector sketch status/color/count rendering"
      pattern: "SketchComp|solved|loose|fixed|error"
---

<objective>
Deliver sketch creation and inspector visibility UX so users can create sketch containers, attach core geometry, and inspect sketch health metadata.

Purpose: Satisfy SKCH-01 and SKCH-02 interaction surfaces using Phase 10 UI contract and locked behavior decisions.
Output: Add Entity sketch entrypoint, GeometryManager add controls, and sketch inspector sections with required status/count/policy fields.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-sketch-foundations-managers/10-CONTEXT.md
@.planning/phases/10-sketch-foundations-managers/10-RESEARCH.md
@.planning/phases/10-sketch-foundations-managers/10-UI-SPEC.md
@.planning/phases/10-sketch-foundations-managers/10-01-SUMMARY.md

<interfaces>
From src/ui/ui_scene_hierarchy.h:
```c
static inline void ui_scene_hierarchy_draw_add_menu(ui_scene_hierarchy_state_t *state);
```

From src/ui/ui_entity_inspector.h:
```c
static inline void ui_entity_inspector_draw_single(ui_entity_inspector_state_t *state, ecs_entity_t e);
static inline void ui_entity_inspector_draw(ui_entity_inspector_state_t *state);
```

From Plan 10-01 contracts:
```c
static inline ecs_entity_t scene_add_sketch(...);
static inline ecs_entity_t scene_attach_point_to_sketch(...);
static inline ecs_entity_t scene_attach_line_to_sketch(...);
static inline ecs_entity_t scene_attach_arc_to_sketch(...);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Add Entity sketch creation and active-sketch geometry attachment</name>
  <read_first>
    - src/ui/ui_scene_hierarchy.h
    - src/ecs/ecs_scene.h
    - .planning/phases/10-sketch-foundations-managers/10-UI-SPEC.md
  </read_first>
  <files>src/ui/ui_scene_hierarchy.h</files>
  <action>Extend `Add Entity` menu with `Create Sketch` as the sketch entrypoint per D-01 and route point/line/arc(circle) creation to active sketch attachment when a sketch is active per D-02/SKCH-01. Respect UI-SPEC copy and emphasis contract: keep label exactly `Create Sketch`, avoid new icon dependency, and preserve current menu-first flow conventions.</action>
  <acceptance_criteria>
    - `Add Entity` menu contains `Create Sketch` entry (D-01/UI-SPEC Primary CTA).
    - Point/Line/Arc creation paths can attach to active sketch ownership without changing non-sketch behavior.
    - Actions create exactly one undo create entry per user action.
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD</automated>
  </verify>
  <done>User can create sketch containers and add core geometry through menu flow in one pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add SketchManager/GeometryManager add controls and sketch inspector surfaces</name>
  <read_first>
    - src/ui/ui_entity_inspector.h
    - src/ui/ui_scene_hierarchy.h
    - .planning/phases/10-sketch-foundations-managers/10-CONTEXT.md
    - .planning/phases/10-sketch-foundations-managers/10-UI-SPEC.md
  </read_first>
  <files>src/ui/ui_entity_inspector.h, src/ui/ui_scene_hierarchy.h</files>
  <action>Add sketch-focused inspector sections that show per-sketch solve status labels (`solved|loose|fixed|error`) per D-08, color policy summary implementing D-09, geometry count + constraint count (SKCH-02), and fixed-state clarity per D-07. Add GeometryManager local add controls (Point/Line/Arc/Circle) as second attachment entrypoint per D-02 while keeping phase boundary clear (no constraint authoring). UI spacing/typography must follow UI-SPEC tokens (4/8/16/24) and label hierarchy.</action>
  <acceptance_criteria>
    - Inspector for sketch entity shows status label, color policy text, geometry count, and constraint count (SKCH-02).
    - GeometryManager includes local add controls attaching to current sketch (D-02).
    - Fixed-state indicators are visibly labeled in sketch surfaces even without transform enforcement (D-07, D-06).
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD</automated>
  </verify>
  <done>Sketch-level visibility contract is complete in inspector and local manager add controls are available.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Phase 10 sketch creation and inspector UX contract</name>
  <read_first>
    - .planning/phases/10-sketch-foundations-managers/10-UI-SPEC.md
    - .planning/phases/10-sketch-foundations-managers/10-CONTEXT.md
  </read_first>
  <acceptance_criteria>
    - `Create Sketch` appears under Add Entity and successfully creates a sketch container.
    - Adding Point/Line/Arc from Add Entity and GeometryManager both attach to the active sketch.
    - Selecting sketch shows status label, color policy summary, geometry count, and constraint count in Entity Inspector.
    - UI text and destructive language match UI-SPEC copy contract.
  </acceptance_criteria>
  <files>src/ui/ui_scene_hierarchy.h, src/ui/ui_entity_inspector.h</files>
  <action>Run the Phase 10 UI verification flow against the implemented sketch entrypoints and inspector surfaces; if any step fails, capture the mismatch and return control to implementation before approval.</action>
  <what-built>Sketch creation/attachment entrypoints and inspector sketch status surfaces for SKCH-01/SKCH-02.</what-built>
  <how-to-verify>
    1. Build and run app: `cmake --build build-vulkan --config Release --target mdCAD`.
    2. Open `Add Entity` and click `Create Sketch`.
    3. Add Point, Line, and Arc from `Add Entity`; confirm they belong to selected sketch.
    4. In sketch-focused manager controls, add another geometry item and confirm same sketch attachment.
    5. Select sketch entity and verify inspector shows status (`solved|loose|fixed|error` taxonomy), color policy, geometry count, and constraint count.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or list exact mismatches to fix.</resume-signal>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD</automated>
  </verify>
  <done>Human confirms UI contract and behavior alignment for sketch creation + inspector surfaces.</done>
</task>

</tasks>

<verification>
After implementation tasks, run `cmake --build build-vulkan --config Release --target math-validation` before checkpoint handoff.
</verification>

<success_criteria>
- SKCH-01 creation and attachment workflow is available from both required entrypoints.
- SKCH-02 inspector truth surfaces are visible and consistent with locked taxonomy/copy contract.
- No Phase 11 constraint authoring behavior is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/10-sketch-foundations-managers/10-02-SUMMARY.md`.
</output>
