---
phase: 13-script-round-trip-baseline
plan: 03
type: execute
wave: 3
depends_on:
  - 13-01
  - 13-02
files_modified:
  - src/ui/ui_entity_inspector.h
  - src/app.c
  - src/tests/script_roundtrip_tests.c
autonomous: false
requirements:
  - SCRP-01
  - SCRP-03
must_haves:
  truths:
    - "User can open a standalone Script Editor window from SketchManager."
    - "Script editor supports auto-apply preview diagnostics without corrupting committed scene state."
    - "Apply Script commits only when script is fully valid, and deterministic script output refreshes after scene edits."
  artifacts:
    - path: "src/ui/ui_entity_inspector.h"
      provides: "SketchManager launch controls and Script Editor UI sections"
      contains: "Apply Script"
    - path: "src/app.c"
      provides: "Standalone Script Editor window lifecycle and per-frame orchestration"
      contains: "Script Editor"
    - path: "src/tests/script_roundtrip_tests.c"
      provides: "Editor flow tests for preview/apply + deterministic re-emit trigger points"
      contains: "preview"
  key_links:
    - from: "src/ui/ui_entity_inspector.h"
      to: "src/ecs/ecs_scene.h"
      via: "preview/apply button handlers call scene scripting façade"
      pattern: "scene_.*script"
    - from: "src/app.c"
      to: "src/ui/ui_entity_inspector.h"
      via: "standalone Script Editor open state and draw orchestration"
      pattern: "active_sketch|script"
    - from: "src/ui/ui_entity_inspector.h"
      to: "src/scripting/sketch_script_emit.h"
      via: "editor text reset/refresh uses deterministic emitted output"
      pattern: "emit"
---

<objective>
Ship the Script Editor UX entrypoint and interaction loop for Phase 13 baseline.

Purpose: Expose the script round-trip engine to users through the required standalone editor workflow.
Output: SketchManager launch action, standalone Script Editor window, preview/apply diagnostics behavior, and user verification checkpoint.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-script-round-trip-baseline/13-CONTEXT.md
@.planning/phases/13-script-round-trip-baseline/13-UI-SPEC.md
@.planning/phases/13-script-round-trip-baseline/13-RESEARCH.md
@.planning/phases/13-script-round-trip-baseline/13-02-SUMMARY.md
@.planning/phases/10-sketch-foundations-managers/10-03-SUMMARY.md

<interfaces>
From `src/ui/ui_entity_inspector.h`:
```c
typedef struct {
    ecs_entity_t active_sketch;
    bool active_sketch_workspace_open;
} ui_entity_inspector_state_t;
static inline void ui_entity_inspector_draw(ui_entity_inspector_state_t *state);
```

From `src/ecs/ecs_scene.h` (from 13-02):
```c
bool scene_script_preview_parse(ecs_scene_t *scene, ecs_entity_t sketch, const char *script_text, ...);
bool scene_script_apply_commit(ecs_scene_t *scene, ecs_entity_t sketch, const char *script_text, ...);
bool scene_script_emit_for_sketch(ecs_scene_t *scene, ecs_entity_t sketch, ...);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement standalone Script Editor launch + preview/apply flow (D-04, D-05, D-06, D-07)</name>
  <files>src/ui/ui_entity_inspector.h, src/app.c, src/tests/script_roundtrip_tests.c</files>
  <behavior>
    - Test 1: SketchManager exposes a control that opens a standalone Script Editor window (SCRP-01, D-04).
    - Test 2: Auto-apply preview runs continuously on edits, surfaces diagnostics, and preserves last committed valid scene on parse failure (D-05, D-06).
    - Test 3: `Apply Script` performs atomic all-or-nothing commit only when parse/reference validation fully succeeds (D-07).
  </behavior>
  <action>Add SketchManager launch action in `ui_entity_inspector.h` to open a dedicated `Script Editor` window managed in `app.c` (standalone, not embedded) per D-04. Implement editor layout per UI-SPEC hierarchy and copy: primary script text area, diagnostics panel, and persistent action row with `Apply Script` as accent CTA. Wire edit loop to scene preview API for D-05; on preview failure show diagnostics and keep committed scene unchanged per D-06. Wire `Apply Script` to commit API with all-or-nothing behavior per D-07, including unsaved-edit/discard/reset confirmations from UI-SPEC copy.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD && ctest --test-dir build-vulkan -C Release --output-on-failure -R script_roundtrip_tests</automated>
  </verify>
  <done>User can open and use standalone Script Editor with preview diagnostics and atomic apply semantics tied to scene scripting APIs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Script Editor UX and deterministic update loop</name>
  <what-built>Standalone Script Editor launch path, preview/apply diagnostics behavior, and deterministic script refresh after UI-side edits.</what-built>
  <action>Run automated build/test gates first, then perform the interactive verification script to confirm standalone window behavior and preview/apply UX semantics match D-04..D-07 and UI-SPEC copy.</action>
  <how-to-verify>
    1. Build and run app: `cmake --build build-vulkan --config Release --target mdCAD` then launch executable from `build-vulkan/bin`.
    2. In Entity Inspector SketchManager, open `Script Editor` and confirm it appears as an independent window (not inside inspector) per D-04.
    3. Edit script with an intentional syntax/reference error; confirm diagnostics panel shows error copy and scene remains at last committed valid state per D-06.
    4. Fix script and click `Apply Script`; confirm commit succeeds only when fully valid and sketch updates atomically per D-07.
    5. Perform a UI-side sketch edit (add/move geometry or constraint value change) and confirm script text refreshes deterministically (stable ordering/number formatting) per D-10/D-11.
    6. Exercise close/reset confirmations to ensure copy matches UI-SPEC destructive text.
  </how-to-verify>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD && ctest --test-dir build-vulkan -C Release --output-on-failure -R script_roundtrip_tests</automated>
  </verify>
  <done>Human approves that Script Editor workflow and deterministic round-trip behavior satisfy interactive acceptance checks for SCRP-01 and SCRP-03.</done>
  <resume-signal>Type "approved" if all checks pass, or provide exact failing step(s) and observed behavior.</resume-signal>
</task>

</tasks>

<verification>
- Automated: `cmake --build build-vulkan --config Release --target mdCAD`
- Automated: `ctest --test-dir build-vulkan -C Release --output-on-failure -R script_roundtrip_tests`
- Manual checkpoint approval for SCRP-01 interactive flow
</verification>

<success_criteria>
- SCRP-01: Standalone Script Editor window is discoverable from SketchManager and usable.
- SCRP-03: Preview/apply loop integrates with deterministic script update path without scene corruption on preview failures.
- Locked UI/interaction decisions D-04..D-07 are implemented exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/13-script-round-trip-baseline/13-03-SUMMARY.md`
</output>

