---
phase: 13-script-round-trip-baseline
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - src/scripting/sketch_script_emit.h
  - src/scripting/sketch_script_parse.h
  - src/scripting/sketch_script_apply.h
  - src/ecs/ecs_scene.h
  - src/tests/script_roundtrip_tests.c
autonomous: true
requirements:
  - SCRP-02
  - SCRP-03
must_haves:
  truths:
    - "Supported sketch sub-scene can be reconstructed from valid declarative script data."
    - "Reference links are resolved atomically after entity creation using a two-pass strategy."
    - "No-op UI-side edits produce byte-stable script output ordering and number formatting."
  artifacts:
    - path: "src/scripting/sketch_script_apply.h"
      provides: "Two-pass reconstruction and atomic apply entrypoints"
      exports: ["sketch_script_apply_preview", "sketch_script_apply_commit"]
    - path: "src/scripting/sketch_script_emit.h"
      provides: "Deterministic script emitter with stable ordering and canonical numeric formatting"
      contains: "stable sort"
    - path: "src/scripting/sketch_script_parse.h"
      provides: "Declarative parser into intermediate model for supported v1.2 sketch scope"
      contains: "parse"
  key_links:
    - from: "src/scripting/sketch_script_apply.h"
      to: "src/ecs/ecs_scene.h"
      via: "scene façade applies parsed model through create-then-link passes"
      pattern: "scene_.*script|scene_.*constraint"
    - from: "src/ecs/ecs_scene.h"
      to: "src/scripting/sketch_script_emit.h"
      via: "UI mutations trigger canonical re-emit through one scene-owned path"
      pattern: "emit"
    - from: "src/scripting/sketch_script_parse.h"
      to: "src/scripting/sketch_script_apply.h"
      via: "intermediate model handoff for atomic reconstruction"
      pattern: "model"
---

<objective>
Implement deterministic script parse/apply/emit for the supported Phase 13 sketch scope.

Purpose: Deliver the core round-trip engine so scripts reconstruct scene state and scene edits emit stable scripts.
Output: Scene-owned scripting façade with parser, two-pass reconstruction, deterministic emitter, and tests proving round-trip behavior.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-script-round-trip-baseline/13-CONTEXT.md
@.planning/phases/13-script-round-trip-baseline/13-RESEARCH.md
@.planning/phases/13-script-round-trip-baseline/13-01-SUMMARY.md
@.planning/phases/10-sketch-foundations-managers/10-03-SUMMARY.md

<interfaces>
From `src/scripting/sketch_script_contract.h` (created in 13-01):
```c
typedef struct sketch_script_model_t sketch_script_model_t;
typedef struct {
    int line;
    int column;
    char message[192];
} sketch_script_error_t;
bool sketch_script_contract_validate(lua_State *L, sketch_script_error_t *out_error);
```

From `src/components/script_identity_comp.h` (created in 13-01):
```c
#define SCRIPT_LOCAL_ID_MAX 64
typedef struct {
    char script_local_id[SCRIPT_LOCAL_ID_MAX];
} ScriptIdentityComp;
```

From `src/scene_serializer.h`:
```c
static inline bool scene_serialize_to_file(ecs_world_state_t *w, const char *path);
static inline bool scene_load_from_file(ecs_world_state_t *w, const char *path);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement parser + two-pass reconstruction façade for supported sketch scope (D-02, D-09)</name>
  <files>src/scripting/sketch_script_parse.h, src/scripting/sketch_script_apply.h, src/ecs/ecs_scene.h, src/tests/script_roundtrip_tests.c</files>
  <behavior>
    - Test 1: Valid script model reconstructs points, lines, arcs/circles, constraints, values, and participant links (SCRP-02).
    - Test 2: Forward references resolve because apply runs create pass before link pass (D-09).
    - Test 3: Explicit Apply commit is all-or-nothing and refuses partial commits on unresolved references (D-07).
  </behavior>
  <action>Add parser entrypoint that converts validated declarative Lua table into an intermediate sketch model for Phase 13 scope only per D-02. Implement apply module with two-pass reconstruction per D-09: pass-1 creates sketch entities/constraints with script-local IDs, pass-2 resolves participants/links and constraint attachments, then commits atomically only when all references validate per D-07. Expose scene-level façade functions in `src/ecs/ecs_scene.h` so UI/app use a thin orchestration call path rather than direct parser logic.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target script_roundtrip_tests && ctest --test-dir build-vulkan -C Release --output-on-failure -R script_roundtrip_tests</automated>
  </verify>
  <done>Scene can reconstruct supported sketch entities and links from script parse output via a two-pass atomic apply flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic scene->script emitter and mutation-triggered re-emit (D-10, D-11)</name>
  <files>src/scripting/sketch_script_emit.h, src/ecs/ecs_scene.h, src/tests/script_roundtrip_tests.c</files>
  <behavior>
    - Test 1: Emitter orders output deterministically by stable script-local IDs within type groups (D-10, SCRP-03).
    - Test 2: Numeric text uses fixed-decimal with trimming and no scientific notation (D-11).
    - Test 3: Repeated emit after no-op mutation produces identical script text byte-for-byte (SCRP-03).
  </behavior>
  <action>Create canonical emitter module that traverses sketch entities/constraints through stable, explicit ordering (type groups + script-local ID sort) per D-10 and emits numeric values through one shared formatter that enforces fixed-decimal trimmed output and disallows scientific notation per D-11. Wire scene mutation paths in `src/ecs/ecs_scene.h` to call a single scene-owned re-emit function so all UI-side geometry/constraint edits update script output deterministically (SCRP-03) without duplicating formatting logic.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target script_roundtrip_tests && ctest --test-dir build-vulkan -C Release --output-on-failure -R script_roundtrip_tests</automated>
  </verify>
  <done>Deterministic script output is produced from scene state and remains stable across no-op edit cycles.</done>
</task>

</tasks>

<verification>
- `cmake --build build-vulkan --config Release --target script_roundtrip_tests`
- `ctest --test-dir build-vulkan -C Release --output-on-failure -R script_roundtrip_tests`
- Optional smoke: `cmake --build build-vulkan --config Release --target mdCAD`
</verification>

<success_criteria>
- SCRP-02: Parse output can reconstruct supported sub-scene entities, constraints, values, and links with atomic commit semantics.
- SCRP-03: UI-side sketch/geometry/constraint edits route through one deterministic emitter and produce stable script output.
</success_criteria>

<output>
After completion, create `.planning/phases/13-script-round-trip-baseline/13-02-SUMMARY.md`
</output>

