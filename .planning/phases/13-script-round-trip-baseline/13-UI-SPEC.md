---
phase: 13
slug: script-round-trip-baseline
status: approved
shadcn_initialized: false
preset: none
created: 2026-04-01
reviewed_at: 2026-04-01T00:00:00Z
---

# Phase 13 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (native Dear ImGui/cimgui stack; source: 13-RESEARCH.md Standard Stack + src/ui/*) |
| Preset | not applicable |
| Component library | none (custom ImGui widgets and header-only UI modules; source: src/ui/ui_entity_inspector.h, src/ui/ui_theme.h) |
| Icon library | none |
| Font | ImGui default font (no custom font load detected; source: repo grep for AddFont/io.Fonts returned no hits) |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Inline control padding, tight label/value separation |
| sm | 8px | Small control spacing and row gutters |
| md | 16px | Default control-group spacing |
| lg | 24px | Section separation inside script editor window |
| xl | 32px | Large panel inset and grouped block spacing |
| 2xl | 48px | Major section breaks in standalone editor |
| 3xl | 64px | Full-window macro layout spacing (reserved) |

Exceptions: none. This phase allows only the declared 4px-based tokens above. Replace legacy non-token values with nearest approved tokens during implementation: `ItemSpacing={8,6}` → `{8,8}` and `igDummy({0,10})` → `igDummy({0,8})` (or `{0,16}` when stronger section separation is needed). Keep `WindowPadding={12,12}` as-is only where inherited globally from app theme and not part of phase-specific layout spacing.

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px | 400 | 1.5 |
| Label | 14px | 600 | 1.4 |
| Heading | 20px | 600 | 1.2 |
| Display | 28px | 600 | 1.2 |

Notes: keep to exactly 4 sizes and 2 weights for this phase. This contract is prescriptive for new script-editor UI surfaces; existing ImGui default rendering may remain until font-loading infrastructure is introduced.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | #252526 | Main window background and primary panel surfaces (Visual Studio theme default; source: `src/ui/ui_theme.h`) |
| Secondary (30%) | #333337 | Input frames, button base states, secondary containers |
| Accent (10%) | #0077C8 | Apply success affordance, focused input outline, active tab/header in script editor |
| Destructive | #E78284 | Delete/clear/reset actions and destructive confirmations only (source: destructive button override in `src/ui/ui_entity_inspector.h`) |

Accent reserved for: **Apply Script** button (commit-ready only), active script editor tab/header, selected diagnostics row, and focused parse-location jump controls. Do **not** use accent for neutral controls.

---

## Visual Hierarchy & Focal Point

Primary focal point for the script editor screen is the **editable script text area**. Visual hierarchy order is fixed:
1. **Script text editor (primary, highest emphasis)** — largest surface area and first keyboard focus target.
2. **Diagnostics panel (secondary)** — directly associated with editor errors/warnings; visually grouped beneath or adjacent to editor.
3. **Action row with Apply Script CTA (tertiary but high-contrast accent)** — persistent, easy to scan, accent reserved to the primary commit action.
4. **Supporting managers/inspector controls (quaternary)** — neutral styling, lower emphasis than editing and diagnostics workflow.

Implementation rule: if layout constraints require tradeoffs, preserve editor prominence first, then diagnostics legibility, then action discoverability.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA | Apply Script |
| Empty state heading | No script content yet |
| Empty state body | Start by editing sketch geometry or constraints, then generated script will appear here. |
| Error state | Script parse failed. Review diagnostics, fix highlighted lines, then run Apply Script again. |
| Destructive confirmation | Clear diagnostics history: “Clear all saved script diagnostics for this sketch? This action cannot be undone.” |

Additional destructive actions in this phase:
- **Close script window with unsaved edits** → confirmation copy: “Discard unapplied script edits and close the Script Editor?”
- **Reset editor to last emitted script** → confirmation copy: “Discard current edits and restore last deterministic script output?”

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable (shadcn not initialized; non-React native ImGui stack) |
| third-party | none | not applicable |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
