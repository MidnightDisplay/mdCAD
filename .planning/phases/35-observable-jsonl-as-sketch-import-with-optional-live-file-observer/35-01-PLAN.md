---
phase: 35-observable-jsonl-as-sketch-import-with-optional-live-file-observer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/CMakeLists.txt
  - src/tests/jsonl_sketch_import_test.c
  - src/tests/jsonl_sketch_mapping_test.c
  - src/tests/jsonl_observer_state_test.c
  - src/tests/jsonl_reparse_transaction_test.c
  - src/tests/jsonl_label_contract_test.c
  - src/components/jsonl_observer_comp.h
  - src/jsonl_sketch_import_job.h
  - src/jsonl_observer_system.h
autonomous: true
requirements: [P35-01, P35-02, P35-03, P35-04, P35-06]
must_haves:
  truths:
    - "Developer can run named `jsonl_*` tests and get explicit pass/fail feedback for import, mapping, observer state, reparse transaction, and label contract behavior."
    - "Developer can read compile-visible JSONL sketch import/observer contracts and implement features without guessing hidden field or function names."
  artifacts:
    - path: "src/tests/jsonl_sketch_import_test.c"
      provides: "Import entrypoint behavior test scaffold for D-17/D-18/D-19/D-20."
      contains: "test_jsonl_sketch_import_creates_sketch_entity"
    - path: "src/tests/jsonl_observer_state_test.c"
      provides: "Observer default/persistence/retry behavior test scaffold for D-01/D-03/D-10/D-11/D-12/D-13."
      contains: "test_jsonl_observer_defaults_and_retry_policy"
    - path: "src/components/jsonl_observer_comp.h"
      provides: "Persisted observer component contract with file-link metadata and message ring."
      contains: "typedef struct JsonlObserverComp"
    - path: "src/jsonl_sketch_import_job.h"
      provides: "Sketch-only import contract and options signatures."
      contains: "jsonl_sketch_import_job_start"
  key_links:
    - from: "src/CMakeLists.txt"
      to: "src/tests/jsonl_*_test.c"
      via: "add_executable/add_test entries for each new Phase 35 test"
      pattern: "add_test\\(NAME jsonl_"
    - from: "src/components/jsonl_observer_comp.h"
      to: "src/jsonl_observer_system.h"
      via: "observer runtime consumes persisted settings contract"
      pattern: "JsonlObserverComp"
---

<objective>
Establish Phase 35 executable contracts first: test harness coverage plus explicit interfaces for sketch import and observer runtime.

Purpose: Prevent scavenger-hunt implementation and satisfy Nyquist requirements before feature code lands.
Output: New Phase 35 tests wired into CTest and new interface headers for observer/import workflows.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-observable-jsonl-as-sketch-import-with-optional-live-file-observer/35-CONTEXT.md
@.planning/phases/35-observable-jsonl-as-sketch-import-with-optional-live-file-observer/35-RESEARCH.md
@.planning/phases/35-observable-jsonl-as-sketch-import-with-optional-live-file-observer/35-VALIDATION.md
@src/CMakeLists.txt
@src/jsonl_import_job.h
@src/scene_serializer.h
@src/ecs/ecs_world.h

<interfaces>
From src/CMakeLists.txt:
```cmake
add_executable(script_roundtrip_tests tests/script_roundtrip_tests.c)
add_test(NAME script_roundtrip_tests COMMAND script_roundtrip_tests)
```

From src/jsonl_import_job.h:
```c
typedef struct { ... } jsonl_import_job_t;
static inline bool jsonl_import_job_start(...);
static inline bool jsonl_import_job_update(jsonl_import_job_t *job, ecs_scene_t *scene);
```

From src/scene_serializer.h:
```c
static inline bool scene_save_json(ecs_scene_t *scene, const char *filepath);
static inline bool scene_load_json(ecs_scene_t *scene, const char *filepath, bool clear_existing);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Wave 0 Phase 35 test scaffolds and wire them into CTest</name>
  <read_first>@src/CMakeLists.txt, @.planning/phases/35-observable-jsonl-as-sketch-import-with-optional-live-file-observer/35-VALIDATION.md, @src/tests/script_roundtrip_tests.c, @src/tests/scene_solver_contract_test.c</read_first>
  <files>src/CMakeLists.txt, src/tests/jsonl_sketch_import_test.c, src/tests/jsonl_sketch_mapping_test.c, src/tests/jsonl_observer_state_test.c, src/tests/jsonl_reparse_transaction_test.c, src/tests/jsonl_label_contract_test.c</files>
  <behavior>
    - Test 1: JSONL sketch import path creates a sketch entity and sketch-owned geometry (D-17/D-18/D-19/D-20).
    - Test 2: Observer defaults/persistence/retry semantics are asserted (D-01/D-03/D-10/D-11/D-12/D-13).
    - Test 3: Reparse transaction keeps last-good state on failure and replaces authoritatively on success (D-04/D-05/D-06/D-14).
    - Test 4: Label name/description follow filename/fullpath contract and relink update behavior (D-16).
  </behavior>
  <action>Create the five test files listed in Wave 0 with concrete test-function names and RED-first assertions matching Phase 35 decisions. Register each file in `src/CMakeLists.txt` via `add_executable(...)` and `add_test(...)` so each validation command in 35-VALIDATION.md resolves. Keep initial assertions intentionally failing or TODO-marked with deterministic failure messages until implementation plans satisfy them.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -N -R "jsonl_sketch_import_test|jsonl_sketch_mapping_test|jsonl_observer_state_test|jsonl_reparse_transaction_test|jsonl_label_contract_test"</automated>
  </verify>
  <acceptance_criteria>`git --no-pager grep -n "add_test\\(NAME jsonl_" src/CMakeLists.txt` returns five new tests; each `src/tests/jsonl_*_test.c` file exists and contains at least one `static int test_` function matching its behavior contract.</acceptance_criteria>
  <done>Phase 35 test harness exists and is executable from CTest with named test targets.</done>
</task>

<task type="auto">
  <name>Task 2: Publish interface contracts for JSONL sketch import and observer runtime</name>
  <read_first>@src/jsonl_import_job.h, @src/ecs/ecs_world.h, @src/scene_serializer.h, @.planning/phases/35-observable-jsonl-as-sketch-import-with-optional-live-file-observer/35-CONTEXT.md</read_first>
  <files>src/components/jsonl_observer_comp.h, src/jsonl_sketch_import_job.h, src/jsonl_observer_system.h</files>
  <action>Create Phase 35 interface-first headers only (no full implementation internals yet): (1) `JsonlObserverComp` contract including observe toggle default-on semantics per D-01, retry counters per D-10, rate-limit field per D-11, and two-message display buffer contract per D-09; (2) sketch-only import job options/result contracts for flatten+mapping path per D-17..D-20 plus label/relink metadata inputs per D-16; (3) observer system function signatures for tick/update/manual-reparse/auto-disable semantics per D-12/D-13. Keep these as stable contracts for downstream plans.</action>
  <verify>
    <automated>git --no-pager grep -n "typedef struct JsonlObserverComp\\|jsonl_sketch_import_job_start\\|jsonl_observer_system_tick" src/components/jsonl_observer_comp.h src/jsonl_sketch_import_job.h src/jsonl_observer_system.h</automated>
  </verify>
  <acceptance_criteria>`git --no-pager grep -n "typedef struct JsonlObserverComp\\|jsonl_sketch_import_job_start\\|jsonl_observer_system_tick" src/components/jsonl_observer_comp.h src/jsonl_sketch_import_job.h src/jsonl_observer_system.h` returns contract declarations; build succeeds.</acceptance_criteria>
  <done>Downstream implementation plans have explicit compile-visible contracts for observer and sketch import subsystems.</done>
</task>

</tasks>

<verification>
- `cmake --build build-vulkan --config Release`
- `ctest --test-dir build-vulkan -C Release -R "jsonl_sketch_import_test|jsonl_sketch_mapping_test|jsonl_observer_state_test|jsonl_reparse_transaction_test|jsonl_label_contract_test" --output-on-failure`
</verification>

<success_criteria>
- All Phase 35 Wave 0 tests exist and are registered in CTest.
- Import and observer interfaces are explicit and compileable.
- No downstream task needs to invent contract names or field keys.
</success_criteria>

<output>
After completion, create `.planning/phases/35-observable-jsonl-as-sketch-import-with-optional-live-file-observer/35-01-SUMMARY.md`
</output>

