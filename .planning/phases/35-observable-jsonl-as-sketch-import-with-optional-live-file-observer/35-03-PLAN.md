---
phase: 35-observable-jsonl-as-sketch-import-with-optional-live-file-observer
plan: 03
type: execute
wave: 3
depends_on: [35-01, 35-02]
files_modified:
  - src/ui/ui_scene_hierarchy.h
  - src/ui/ui_entity_inspector.h
  - src/app.c
  - src/jsonl_sketch_import_job.h
  - src/tests/jsonl_sketch_import_test.c
  - src/tests/jsonl_sketch_mapping_test.c
autonomous: false
requirements: [P35-01, P35-02, P35-05]
must_haves:
  truths:
    - "User can import JSONL into a real sketch (not root dump) with observer linked and ON by default."
    - "Import converts supported JSONL geometry types into sketch geometry, flattens into one sketch, ignores meshes, and leaves geometry unconstrained."
    - "Observer controls appear in Entity Inspector when sketch is inactive, and move to Active Sketch Workspace when active."
    - "Script overwrite warning is visible while JSONL link is active."
  artifacts:
    - path: "src/ui/ui_scene_hierarchy.h"
      provides: "New File menu action and import dialog wiring for JSONL-as-sketch path."
      contains: "Import JSONL as Sketch"
    - path: "src/jsonl_sketch_import_job.h"
      provides: "Concrete import implementation for D-17..D-20 and transactional import failure on observer setup."
      contains: "jsonl_sketch_import_job_start"
    - path: "src/ui/ui_entity_inspector.h"
      provides: "Inactive sketch observer controls rendering and active-workspace suppression behavior."
      contains: "Observer controls moved to Active Sketch Workspace"
    - path: "src/app.c"
      provides: "Active sketch workspace observer controls and explicit script overwrite warning."
      contains: "can be overwritten by next successful JSONL re-parse"
  key_links:
    - from: "src/ui/ui_scene_hierarchy.h"
      to: "src/jsonl_sketch_import_job.h"
      via: "menu action dispatch + import options pipeline"
      pattern: "jsonl_sketch_import_job_start"
    - from: "src/ui/ui_entity_inspector.h"
      to: "src/app.c"
      via: "active sketch relocation flag and shared observer UI controls"
      pattern: "active_sketch_workspace_open|observer"
---

<objective>
Ship user-facing JSONL-as-sketch import and observer UX placement/warnings with full mapping semantics.

Purpose: Deliver the actual end-user feature contract for Phase 35 while preserving deterministic behavior.
Output: Menu/UX wiring, import mapping implementation, and regression/manual verification closure.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-observable-jsonl-as-sketch-import-with-optional-live-file-observer/35-CONTEXT.md
@.planning/phases/35-observable-jsonl-as-sketch-import-with-optional-live-file-observer/35-RESEARCH.md
@.planning/phases/35-observable-jsonl-as-sketch-import-with-optional-live-file-observer/35-VALIDATION.md
@.planning/phases/35-observable-jsonl-as-sketch-import-with-optional-live-file-observer/35-01-SUMMARY.md
@.planning/phases/35-observable-jsonl-as-sketch-import-with-optional-live-file-observer/35-02-SUMMARY.md
@src/ui/ui_scene_hierarchy.h
@src/ui/ui_entity_inspector.h
@src/app.c
@src/jsonl_loader.h
@src/ecs/ecs_scene.h

<interfaces>
From src/ecs/ecs_scene.h:
```c
static inline ecs_entity_t scene_add_sketch(ecs_scene_t *scene, const char *name, const char *description, vec4_t color);
static inline ecs_entity_t scene_add_point_to_sketch(...);
static inline ecs_entity_t scene_add_line_to_sketch(...);
static inline ecs_entity_t scene_add_arc_to_sketch(...);
static inline void scene_script_reemit_for_sketch(ecs_scene_t *scene, ecs_entity_t sketch);
```

From src/jsonl_loader.h:
```c
typedef enum { JSONL_GEOM_POINT, JSONL_GEOM_LINE, JSONL_GEOM_ARC, JSONL_GEOM_POLYLINE, JSONL_GEOM_POLYGON, JSONL_GEOM_MESH } jsonl_geom_type_t;
```

From src/ui/ui_entity_inspector.h:
```c
state->active_sketch_workspace_open
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement JSONL-as-sketch import path and geometry mapping contract</name>
  <read_first>@src/ui/ui_scene_hierarchy.h, @src/jsonl_sketch_import_job.h, @src/jsonl_loader.h, @src/ecs/ecs_scene.h, @src/tests/jsonl_sketch_import_test.c, @src/tests/jsonl_sketch_mapping_test.c</read_first>
  <files>src/ui/ui_scene_hierarchy.h, src/jsonl_sketch_import_job.h, src/tests/jsonl_sketch_import_test.c, src/tests/jsonl_sketch_mapping_test.c</files>
  <behavior>
    - Test 1: File action creates sketch import flow and observer link defaults to ON (D-01).
    - Test 2: If observer-link setup fails, import aborts transactionally with no partial sketch (D-02).
    - Test 3: Mapping contract enforced: Line/Arc/Circle/Point map to sketch APIs; Polyline/Polygon flatten into lines; Mesh ignored; unconstrained output (D-17..D-20).
  </behavior>
  <action>Add a distinct File menu/import popup path for “Import JSONL as Sketch” (do not alter existing root JSONL importer behavior). Implement sketch import job execution that creates one sketch per file, flattens entries into that sketch, applies color carry-over, and calls sketch APIs only (per D-17/D-18). Skip mesh elements entirely per D-19 and do not synthesize constraints per D-20. Initialize observer link ON by default per D-01, fail transactionally if observer setup fails per D-02, and ensure import transform settings (scale/rotation/shift-to-center defaults) are captured to persisted observer settings so later observer/manual reparse reuses the same values unless user changes them.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "jsonl_sketch_import_test|jsonl_sketch_mapping_test" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`git --no-pager grep -n "Import JSONL as Sketch\\|JSONL_GEOM_MESH\\|scene_add_.*_to_sketch\\|scale\\|rotation\\|shift_to_center" src/ui/ui_scene_hierarchy.h src/jsonl_sketch_import_job.h` shows dedicated import path, mesh ignore, sketch API mapping, and persisted transform-setting capture; targeted tests pass.</acceptance_criteria>
  <done>JSONL-as-sketch import works per D-01/D-02 and D-17..D-20 without touching root JSONL import semantics.</done>
</task>

<task type="auto">
  <name>Task 2: Wire observer controls relocation and script overwrite warning UX</name>
  <read_first>@src/ui/ui_entity_inspector.h, @src/app.c, @.planning/phases/35-observable-jsonl-as-sketch-import-with-optional-live-file-observer/35-CONTEXT.md, @.planning/phases/35-observable-jsonl-as-sketch-import-with-optional-live-file-observer/35-VALIDATION.md</read_first>
  <files>src/ui/ui_entity_inspector.h, src/app.c</files>
  <action>Render observer controls in Entity Inspector when linked sketch is inactive per D-07. When linked sketch is active, hide editable duplicate controls in inspector and move controls to top of Active Sketch Workspace per D-08. In both script editor vicinity and observer controls area, show explicit overwrite warning text per D-15. Ensure observer message display uses only the two most recent messages per D-09 and remains consistent with component/system state from prior plans. Expose persisted import setting controls (scale/rotation/shift-to-center defaults and current values) in the active observer control surface and ensure manual reparse uses stored values, not hardcoded one-off UI state.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "script_roundtrip_tests" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`git --no-pager grep -n "Observer controls moved to Active Sketch Workspace\\|two most recent\\|overwritten by next successful JSONL re-parse\\|scale\\|rotation\\|shift_to_center" src/ui/ui_entity_inspector.h src/app.c` returns relocation/warning strings and persisted import-setting controls; targeted smoke suite passes.</acceptance_criteria>
  <done>Observer controls and overwrite warnings follow active/inactive workspace contract and messaging requirements.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify end-to-end JSONL sketch observer UX and relocation behavior</name>
  <read_first>@.planning/phases/35-observable-jsonl-as-sketch-import-with-optional-live-file-observer/35-VALIDATION.md, @src/ui/ui_scene_hierarchy.h, @src/ui/ui_entity_inspector.h, @src/app.c</read_first>
  <action>Run the numbered verification script exactly as written and compare observed behavior against D-07/D-08/D-09/D-15 UX requirements plus D-17..D-20 import outcomes. Report pass/fail with failing step numbers so follow-up fixes are unambiguous.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "jsonl_sketch_import_test|jsonl_sketch_mapping_test|jsonl_observer_state_test|jsonl_reparse_transaction_test|jsonl_label_contract_test|script_roundtrip_tests" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>Human confirms all three manual checks in 35-VALIDATION.md pass: observer relocation inactive/active, script overwrite warning visibility, and two-message display behavior.</acceptance_criteria>
  <what-built>Dedicated JSONL-as-sketch import path with linked observer defaults, geometry mapping, and relocated controls/warnings.</what-built>
  <how-to-verify>
    1. Launch app and use File -> Import JSONL as Sketch on a representative JSONL with mixed Point/Line/Arc/Polyline/Polygon and mesh entries.
    2. Confirm created sketch label uses filename (no extension) and description shows full path; confirm geometry appears under sketch and no mesh entities are imported.
    3. With sketch inactive, open Entity Inspector and confirm observer controls are present there.
    4. Activate the same sketch and open Active Sketch Workspace; confirm observer controls move to workspace top and inspector shows non-editable relocation hint.
    5. Open Script Editor for linked sketch and confirm explicit overwrite warning near script/observer controls.
    6. Trigger at least 3 observer events (e.g., lock/read/parse warnings) and confirm only the latest two messages are displayed.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or provide failing step numbers with observed behavior.</resume-signal>
  <done>Human verification is explicitly approved, or concrete failing steps are captured for a gap-closure follow-up plan.</done>
</task>

</tasks>

<verification>
- `ctest --test-dir build-vulkan -C Release -R "jsonl_sketch_import_test|jsonl_sketch_mapping_test|jsonl_observer_state_test|jsonl_reparse_transaction_test|jsonl_label_contract_test|script_roundtrip_tests|scene_solver_contract" --output-on-failure`
</verification>

<success_criteria>
- Import behavior satisfies D-01/D-02 and D-17..D-20.
- Observer UI placement and warnings satisfy D-07/D-08/D-09/D-15.
- Manual verification confirms operator-visible flow end-to-end.
</success_criteria>

<output>
After completion, create `.planning/phases/35-observable-jsonl-as-sketch-import-with-optional-live-file-observer/35-03-SUMMARY.md`
</output>

