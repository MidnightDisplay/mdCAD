---
phase: 35-observable-jsonl-as-sketch-import-with-optional-live-file-observer
plan: 02
type: execute
wave: 2
depends_on: [35-01]
files_modified:
  - src/jsonl_sketch_import_job.h
  - src/components/jsonl_observer_comp.h
  - src/jsonl_observer_system.h
  - src/ecs/ecs_world.h
  - src/scene_serializer.h
  - src/tests/jsonl_observer_state_test.c
  - src/tests/jsonl_reparse_transaction_test.c
  - src/tests/jsonl_label_contract_test.c
autonomous: true
requirements: [P35-03, P35-04, P35-06]
must_haves:
  truths:
    - "Observer settings survive save/load per linked sketch and preserve observe toggle + rate-limit."
    - "Locked/retry failures stop continuous observe after 50 attempts while still allowing manual re-parse."
    - "Successful re-parse fully replaces linked sketch content; failed parse keeps last good state."
    - "Sketch label metadata follows filename/full-path contract and relink updates both fields."
  artifacts:
    - path: "src/components/jsonl_observer_comp.h"
      provides: "Persistent observer metadata and retry/message state fields."
      contains: "max_retries"
    - path: "src/scene_serializer.h"
      provides: "Serialization and parse support for observer component persistence."
      contains: "\"jsonl_observer\""
    - path: "src/jsonl_observer_system.h"
      provides: "Observer tick + manual reparse orchestration with retry exhaustion behavior."
      contains: "jsonl_observer_system_tick"
  key_links:
    - from: "src/scene_serializer.h"
      to: "src/components/jsonl_observer_comp.h"
      via: "component write/read path"
      pattern: "jsonl_observer"
    - from: "src/jsonl_observer_system.h"
      to: "src/jsonl_sketch_import_job.h"
      via: "manual/auto reparse dispatch"
      pattern: "reparse|import"
---

<objective>
Publish observer/import interface headers and implement persisted observer + transactional reparse core behavior for linked JSONL sketches.

Purpose: Deliver deterministic source-of-truth replacement and safe failure semantics without partial corruption.
Output: Header contracts plus observer runtime, persistence wiring, and regression coverage for D-03..D-06, D-10..D-14, D-16.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/35-observable-jsonl-as-sketch-import-with-optional-live-file-observer/35-CONTEXT.md
@.planning/phases/35-observable-jsonl-as-sketch-import-with-optional-live-file-observer/35-RESEARCH.md
@.planning/phases/35-observable-jsonl-as-sketch-import-with-optional-live-file-observer/35-01-SUMMARY.md
@src/components/jsonl_observer_comp.h
@src/jsonl_sketch_import_job.h
@src/jsonl_observer_system.h
@src/scene_serializer.h
@src/ecs/ecs_world.h
@src/scripting/sketch_script_apply.h

<interfaces>
From src/components/label_comp.h:
```c
#define LABEL_NAME_MAX 128
#define LABEL_DESC_MAX 128
typedef struct { char name[LABEL_NAME_MAX]; char description[LABEL_DESC_MAX]; } LabelComp;
```

From src/scene_serializer.h:
```c
static inline bool scene_save_json(ecs_scene_t *scene, const char *filepath);
static inline bool scene_load_json(ecs_scene_t *scene, const char *filepath, bool clear_existing);
```

From src/scripting/sketch_script_apply.h:
```c
// Transactional remove/apply/rollback patterns used by script apply.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish observer/import header contracts and enforce persisted retry/auto-disable behavior</name>
  <read_first>@src/components/jsonl_observer_comp.h, @src/scene_serializer.h, @src/ecs/ecs_world.h, @src/tests/jsonl_observer_state_test.c, @.planning/phases/35-observable-jsonl-as-sketch-import-with-optional-live-file-observer/35-CONTEXT.md</read_first>
  <files>src/components/jsonl_observer_comp.h, src/ecs/ecs_world.h, src/scene_serializer.h, src/jsonl_observer_system.h, src/tests/jsonl_observer_state_test.c</files>
  <behavior>
    - Test 1: observe toggle + rate-limit persist roundtrip through scene save/load (D-03).
    - Test 2: retry attempts hard-stop at 50 and auto-toggle observe OFF after exhaustion (D-10/D-12).
    - Test 3: retry debounce uses observer rate-limit value (D-11); manual reparse remains callable when auto-disabled (D-13).
    - Test 4: observer message area state retains latest two events semantics (D-09).
  </behavior>
  <action>First publish/confirm compile-visible contracts in `src/components/jsonl_observer_comp.h`, `src/jsonl_sketch_import_job.h`, and `src/jsonl_observer_system.h` (moved from 35-01 scope) so downstream implementation uses stable names/fields. Then implement observer component registration and access helpers in ECS world, add serializer write/read branches for `jsonl_observer`, and implement observer-system retry handling with fixed max retries=50 per D-10. Use the same interval field as debounce source per D-11, auto-disable observe at exhaustion per D-12, and keep `jsonl_observer_manual_reparse(...)` operational regardless of observe flag per D-13. Persist observer import/reparse settings and re-apply them after load and after successful relink/reparse (including observe-enabled toggle default policy and scale/rotation/shift-to-center import transform fields, with defaults reapplied when settings are absent) so linked behavior is deterministic across sessions per D-03.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "jsonl_observer_state_test" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`git --no-pager grep -n "jsonl_observer\\|max_retries\\|retry_count\\|observe_enabled\\|scale\\|rotation\\|shift_to_center" src/scene_serializer.h src/components/jsonl_observer_comp.h src/jsonl_observer_system.h` shows persistence + retry + import-setting fields/logic; `jsonl_observer_state_test` passes.</acceptance_criteria>
  <done>Observer state is persisted and runtime retry/auto-disable behavior matches D-03 and D-10..D-13.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement authoritative reparse transaction and label relink contract</name>
  <read_first>@src/jsonl_sketch_import_job.h, @src/tests/jsonl_reparse_transaction_test.c, @src/tests/jsonl_label_contract_test.c, @src/scripting/sketch_script_apply.h, @.planning/phases/35-observable-jsonl-as-sketch-import-with-optional-live-file-observer/35-CONTEXT.md</read_first>
  <files>src/jsonl_sketch_import_job.h, src/jsonl_observer_system.h, src/tests/jsonl_reparse_transaction_test.c, src/tests/jsonl_label_contract_test.c</files>
  <behavior>
    - Test 1: successful reparse fully replaces linked sketch geometry/constraints from source JSONL (D-04/D-05/D-14).
    - Test 2: parse/read failure preserves prior good sketch state and appends observer warning/error (D-06).
    - Test 3: label metadata updates to filename/path on import and relink (D-16).
  </behavior>
  <action>Implement transactional replace flow for linked reparse: snapshot current sketch-owned script-managed children, attempt rebuild from JSONL, commit only on success, and rollback on failure while preserving last-good state per D-06. Ensure script content follows recreated state after success per D-14. Apply strict label contract per D-16 (`Label.name`=file stem, `Label.description`=full path) and update both fields when relinking file path. Persist and re-apply observer import settings (scale/rotation/shift-to-center and observe/rate-limit state) when relink path changes and when reparse executes, so replacement always uses stored settings unless user explicitly changes them. Explicitly document/handle long-path storage in observer metadata if Label buffers truncate display text.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "jsonl_reparse_transaction_test|jsonl_label_contract_test" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`git --no-pager grep -n "authoritative\\|rollback\\|relink\\|Label\\.name\\|Label\\.description\\|scale\\|rotation\\|shift_to_center" src/jsonl_sketch_import_job.h src/jsonl_observer_system.h src/tests/jsonl_reparse_transaction_test.c src/tests/jsonl_label_contract_test.c` returns transaction + relink + setting reapply logic and tests; targeted tests pass.</acceptance_criteria>
  <done>Linked reparse semantics are authoritative, transactional, and label metadata contract-compliant.</done>
</task>

</tasks>

<verification>
- `ctest --test-dir build-vulkan -C Release -R "jsonl_observer_state_test|jsonl_reparse_transaction_test|jsonl_label_contract_test" --output-on-failure`
- `ctest --test-dir build-vulkan -C Release -R "script_roundtrip_tests|scene_solver_contract" --output-on-failure`
</verification>

<success_criteria>
- Observer persistence and retry semantics satisfy D-03 and D-10..D-13.
- Reparse semantics satisfy D-04..D-06 and D-14.
- Label/relink semantics satisfy D-16.
</success_criteria>

<output>
After completion, create `.planning/phases/35-observable-jsonl-as-sketch-import-with-optional-live-file-observer/35-02-SUMMARY.md`
</output>

