---
phase: 38-observable-link-manual-transactional-refresh
plan: 02
type: execute
wave: 2
depends_on: [38-01]
files_modified:
  - src/jsonl_observer_system.h
  - src/ui/ui_entity_inspector.h
  - src/app.c
  - src/tests/jsonl_flat_observer_manual_refresh_test.c
  - src/tests/jsonl_flat_observer_inspector_contract_test.c
autonomous: true
requirements: [OBSF-02, OBSF-03, OBSF-05, PERF-02]
must_haves:
  truths:
    - "User can trigger manual refresh from Inspector for a flat root anchor (D-03, D-04)."
    - "Refresh runs in background without modal progress popup and reports status in Inspector only (D-05, D-06)."
    - "Only one in-flight refresh per anchor is active; repeated clicks while running are ignored (D-07)."
    - "Successful refresh keeps same root entity and replaces only imported subtree; failures preserve last-good subtree (D-08, D-09)."
    - "If selected entity is replaced, selection moves to root anchor (D-10)."
    - "Relink updates source and root label metadata (D-13)."
  artifacts:
    - path: "src/jsonl_observer_system.h"
      provides: "Flat manual refresh request/tick/commit-rollback logic."
    - path: "src/ui/ui_entity_inspector.h"
      provides: "Flat-root observer control section with minimal controls."
    - path: "src/app.c"
      provides: "Frame-loop integration for background flat refresh ticking."
    - path: "src/tests/jsonl_flat_observer_manual_refresh_test.c"
      provides: "Transactional same-root refresh + rollback + selection fallback assertions."
  key_links:
    - from: "src/ui/ui_entity_inspector.h"
      to: "src/jsonl_observer_system.h"
      via: "Re-import now and relink actions"
      pattern: "jsonl_observer_.*flat"
    - from: "src/app.c"
      to: "src/jsonl_observer_system.h"
      via: "per-frame background tick"
      pattern: "jsonl_observer_.*tick"
    - from: "src/jsonl_observer_system.h"
      to: "src/jsonl_import_job.h"
      via: "staged flat import job for transactional swap"
      pattern: "jsonl_import_job_(start|tick|reset)"
---

<objective>
Implement inspector-driven manual flat refresh with transactional subtree swap and same-root identity, using the flat import pipeline only.

Purpose: Deliver Phase 38 manual safe refresh while explicitly excluding Phase 39 auto-loop behavior.
Output: Working background manual refresh flow + inspector controls + green tests for D-03..D-13 in scope.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-observable-link-manual-transactional-refresh/38-CONTEXT.md
@.planning/phases/38-observable-link-manual-transactional-refresh/38-RESEARCH.md
@src/jsonl_observer_system.h
@src/ui/ui_entity_inspector.h
@src/app.c

<interfaces>
From src/jsonl_observer_system.h (current):
- jsonl_observer_relink(...)
- jsonl_observer_manual_reparse(...)
- jsonl_observer_system_tick(...)

From src/app.c:
- jsonl_observer_system_tick(&state.ecs_scene, scene_solver_now_ms());

From src/ui/ui_entity_inspector.h:
- ui_entity_inspector_draw_jsonl_observer_controls(...)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement background transactional flat refresh runtime</name>
  <read_first>
    - .planning/phases/38-observable-link-manual-transactional-refresh/38-CONTEXT.md (D-02, D-05, D-07, D-08, D-09, D-10)
    - src/jsonl_import_job.h
    - src/jsonl_observer_system.h
    - src/app.c
  </read_first>
  <files>
    src/jsonl_observer_system.h
    src/app.c
    src/tests/jsonl_flat_observer_manual_refresh_test.c
  </files>
  <behavior>
    - Test 1: Manual refresh preserves root entity ID and replaces only children on success (D-02, D-08).
    - Test 2: Failed refresh keeps previous subtree intact (D-09).
    - Test 3: Re-trigger during running refresh is ignored (D-07).
    - Test 4: Selected replaced child falls back to root selection (D-10).
  </behavior>
  <action>
    Add a flat-refresh path in `jsonl_observer_system.h` that does NOT call sketch reparse:
    - Add explicit APIs:
      `jsonl_observer_request_flat_refresh(...)`,
      `jsonl_observer_tick_flat_refreshes(...)`,
      `jsonl_observer_is_flat_refresh_running(...)`.
    - Use staged `jsonl_import_job_t` per active root; run via app frame tick in `app.c` (background, no popup) per D-05.
    - Commit algorithm: after staged job success, reparent staged children to target root in deferred block, then remove old subtree + temporary stage root; keep target root unchanged (D-08, PERF-02).
    - Failure algorithm: delete only staged temporary subtree/job artifacts, retain original target subtree untouched (D-09).
    - Guard exactly one in-flight refresh per root; return false/no-op on repeated request while running (D-07).
    - Keep auto observer loop changes out of flat path (no debounce/retry/auto-disable additions for flat in this phase).
  </action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_manual_refresh_test|jsonl_reparse_transaction_test|jsonl_flat_anchor_scoped_ingest_test" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - Manual flat refresh runs to completion without modal UI dependency.
    - Same-root transactional semantics pass in automated tests.
    - No sketch/script reparse function is invoked by flat refresh path (PERF-02).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add minimal flat-root inspector controls and relink workflow</name>
  <read_first>
    - .planning/phases/38-observable-link-manual-transactional-refresh/38-CONTEXT.md (D-03, D-04, D-06, D-11, D-12, D-13)
    - src/ui/ui_entity_inspector.h
    - src/components/jsonl_observer_comp.h
    - src/tests/jsonl_flat_observer_inspector_contract_test.c
  </read_first>
  <files>
    src/ui/ui_entity_inspector.h
    src/jsonl_observer_system.h
    src/tests/jsonl_flat_observer_inspector_contract_test.c
  </files>
  <behavior>
    - Test 1: Flat root inspector shows only link toggle, source path, Re-import now, and last refresh message (D-03, D-04, D-06).
    - Test 2: Anchor initially unlinked can be linked later by choosing a file path (D-11).
    - Test 3: Relinking updates label metadata to new source stem/path (D-13).
    - Test 4: Replay settings are not editable in inspector (D-12).
  </behavior>
  <action>
    Implement flat-root observer UI in `ui_entity_inspector.h`:
    - Render controls only when selected entity is flat root with `JsonlObserverComp` and not a sketch entity (D-03).
    - Render minimal control surface exactly:
      1) `Link file for refresh` checkbox
      2) `Source path` text
      3) `Choose source...` button for link-later/relink
      4) `Re-import now` button
      5) Last result message block
      (D-04)
    - Status must remain inspector-local; do not write to global status/toast channels (D-06).
    - Disable `Re-import now` while running and ignore duplicate clicks (D-07).
    - Keep replay fields read-only/not rendered as editable controls (D-12).
    - On relink, call relink helper and update root label metadata immediately (D-13).
  </action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_inspector_contract_test|jsonl_flat_observer_manual_refresh_test|jsonl_flat_import_ui_contract_test" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - Inspector contract test passes for minimal surface and status locality.
    - Link-later and relink behavior pass with label metadata update.
    - No Phase 39 automatic observer behavior is introduced.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run phase slice:
- ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_manual_refresh_test|jsonl_flat_observer_inspector_contract_test|jsonl_flat_import_ui_contract_test|jsonl_flat_import_options_contract_test|jsonl_flat_anchor_scoped_ingest_test" --output-on-failure
</verification>

<success_criteria>
- OBSF-03, OBSF-05, PERF-02 behaviors are implemented and green.
- All locked decisions D-01..D-13 are either implemented directly or preserved with explicit no-regression checks.
- Flat manual refresh is transactional, same-root, and inspector-driven.
</success_criteria>

<output>
After completion, create `.planning/phases/38-observable-link-manual-transactional-refresh/38-02-SUMMARY.md`
</output>
