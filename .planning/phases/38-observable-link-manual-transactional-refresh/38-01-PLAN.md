---
phase: 38-observable-link-manual-transactional-refresh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tests/jsonl_flat_observer_manual_refresh_test.c
  - src/tests/jsonl_flat_observer_inspector_contract_test.c
  - src/CMakeLists.txt
  - src/components/jsonl_observer_comp.h
  - src/jsonl_import_job.h
  - src/scene_serializer.h
autonomous: true
requirements: [OBSF-01, OBSF-02]
must_haves:
  truths:
    - "Flat import keeps observer opt-in default OFF at import time (D-01)."
    - "Flat root anchor stores refresh metadata even when link is OFF (D-11)."
    - "Replay settings used for refresh are persisted and reload correctly (D-12)."
  artifacts:
    - path: "src/components/jsonl_observer_comp.h"
      provides: "Observer metadata schema includes flat-refresh replay fields."
    - path: "src/jsonl_import_job.h"
      provides: "Flat import completion writes observer metadata to root anchor."
    - path: "src/scene_serializer.h"
      provides: "jsonl_observer persistence/load for flat root anchors regardless of linked flag."
    - path: "src/tests/jsonl_flat_observer_manual_refresh_test.c"
      provides: "Contract tests for metadata durability and root observer presence."
    - path: "src/tests/jsonl_flat_observer_inspector_contract_test.c"
      provides: "Inspector surface contract guardrails for Phase 38."
  key_links:
    - from: "src/jsonl_import_job.h"
      to: "src/components/jsonl_observer_comp.h"
      via: "root JsonlObserverComp initialization after import completion"
      pattern: "ecs_world_set_jsonl_observer"
    - from: "src/scene_serializer.h"
      to: "JsonlObserverComp"
      via: "serialize/parse jsonl_observer object for flat anchors"
      pattern: "\"jsonl_observer\""
---

<objective>
Lock Phase 38 contracts and implement durable flat-anchor observer metadata so manual refresh can run on stable persisted inputs.

Purpose: Remove ambiguity before runtime wiring; ensure D-01/D-11/D-12 are enforced by tests and persistence.
Output: New Phase 38 tests + metadata/persistence implementation in import + serializer paths.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-observable-link-manual-transactional-refresh/38-CONTEXT.md
@.planning/phases/38-observable-link-manual-transactional-refresh/38-RESEARCH.md
@.planning/phases/38-observable-link-manual-transactional-refresh/38-VALIDATION.md
@src/components/jsonl_observer_comp.h
@src/jsonl_import_job.h
@src/scene_serializer.h
@src/CMakeLists.txt

<interfaces>
From src/jsonl_import_job.h:
- jsonl_import_job_start(...)
- jsonl_import_job_tick(...)
- jsonl_import_job_set_observer_contract(jsonl_import_job_t *job, bool link_enabled, const char *source_path)
- jsonl_import_job_set_mesh_mode(jsonl_import_job_t *job, int mode)

From src/components/jsonl_observer_comp.h:
- typedef struct JsonlObserverComp { ... }
- jsonl_observer_comp_default()
- jsonl_observer_comp_set_path(...)
- jsonl_observer_comp_push_message(...)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RED-first Phase 38 observer metadata and inspector contract tests</name>
  <read_first>
    - .planning/phases/38-observable-link-manual-transactional-refresh/38-CONTEXT.md (D-01, D-03, D-04, D-06, D-11, D-12)
    - src/tests/jsonl_flat_import_options_contract_test.c
    - src/tests/jsonl_flat_anchor_scoped_ingest_test.c
  </read_first>
  <files>
    src/tests/jsonl_flat_observer_manual_refresh_test.c
    src/tests/jsonl_flat_observer_inspector_contract_test.c
    src/CMakeLists.txt
  </files>
  <action>
    Create two new tests and wire both in CMake:
    1) jsonl_flat_observer_manual_refresh_test:
       - Assert flat import default link intent remains OFF (D-01) by checking `jsonl_flat_link_file_for_refresh = false` and observer contract mapping.
       - Assert root anchor receives JsonlObserverComp with stored replay values (scale, rotation_x/y/z, shift_to_center, use_jsonl_colours, mesh_import_mode) even when `linked=false` (D-11/D-12).
       - Assert save/load preserves those values.
    2) jsonl_flat_observer_inspector_contract_test:
       - Assert inspector flat-root section includes only: link toggle, source path, `Re-import now`, and last refresh message text (D-04).
       - Assert no global toast/status string anchors are introduced for refresh state (D-06).
    Register executables and add_test entries in `src/CMakeLists.txt`.
  </action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target jsonl_flat_observer_manual_refresh_test jsonl_flat_observer_inspector_contract_test</automated>
  </verify>
  <acceptance_criteria>
    - Both test executables are buildable and discoverable by CTest.
    - Tests fail initially where implementation is still missing (RED allowed at this step).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement flat-anchor observer metadata capture and persistence</name>
  <read_first>
    - src/components/jsonl_observer_comp.h
    - src/jsonl_import_job.h
    - src/scene_serializer.h
    - .planning/phases/38-observable-link-manual-transactional-refresh/38-RESEARCH.md (pitfalls 1-2)
  </read_first>
  <files>
    src/components/jsonl_observer_comp.h
    src/jsonl_import_job.h
    src/scene_serializer.h
  </files>
  <behavior>
    - Test 1: Import with link OFF still stores replay metadata on the flat root anchor (D-11, D-12).
    - Test 2: Serializer round-trip preserves observer metadata for flat roots even when `linked=false`.
    - Test 3: Existing default OFF import toggle behavior remains unchanged (D-01).
  </behavior>
  <action>
    Implement concrete schema + persistence:
    - Extend `JsonlObserverComp` with explicit flat replay fields:
      `bool use_jsonl_colours; int mesh_import_mode;`
      and initialize defaults to `true` and `0`.
    - In `jsonl_import_job_tick` completion path, after root creation succeeds, set root `JsonlObserverComp` from import options:
      scale, rotation_x/y/z, shift_to_center, use_jsonl_colours, mesh_import_mode, source_path/link flag from observer_contract (D-11/D-12).
    - In `scene_serializer.h`, serialize/deserialize `jsonl_observer` when component exists (not only when linked=true), and include the two new fields.
    - Preserve Phase 36 behavior by not changing UI default values or menu flow (D-01).
  </action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_manual_refresh_test|jsonl_flat_observer_inspector_contract_test|jsonl_flat_import_options_contract_test" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>
    - New Phase 38 tests pass for metadata capture and persistence.
    - Existing flat-import option contract tests remain green.
    - No Phase 39/40 auto-observer behavior is added.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run targeted Phase 38 suite:
- ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_manual_refresh_test|jsonl_flat_observer_inspector_contract_test|jsonl_flat_import_ui_contract_test|jsonl_flat_import_options_contract_test" --output-on-failure
</verification>

<success_criteria>
- OBSF-01 and OBSF-02 are test-covered and implemented.
- Flat root anchors persist refresh metadata needed by later manual refresh.
- Contracts for minimal inspector surface are locked before runtime wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/38-observable-link-manual-transactional-refresh/38-01-SUMMARY.md`
</output>
