---
phase: 26-line-line-constraint-coverage
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - src/constraints/constraint_types.h
  - src/ecs/ecs_scene.h
  - src/app.c
  - src/tests/scene_solver_contract_test.c
  - src/tests/endpoint_pick_test.c
  - src/tests/script_roundtrip_tests.c
  - src/tests/scene_solver_diagnostics_test.c
autonomous: true
requirements: [LCON-02, LCON-04, LCON-05]
must_haves:
  truths:
    - "User can apply PARALLEL to multi-line groups and all selected lines satisfy a shared parallel relation."
    - "User can apply PERPENDICULAR to supported 3+ line selections using deterministic anchor semantics independent of selection order."
    - "Invalid multi-line line-line selections are rejected with explicit type-specific legality feedback and unsatisfied diagnostics remain family-specific."
  artifacts:
    - path: "src/ecs/ecs_scene.h"
      provides: "Canonicalized participant ordering + multi-line PARALLEL/PERPENDICULAR runtime branches."
    - path: "src/constraints/constraint_types.h"
      provides: "Legality contract for supported multi-line PERPENDICULAR + explicit line-line signature checks."
    - path: "src/app.c"
      provides: "Constraint menu explicit invalid line-line messaging for unsupported signatures."
    - path: "src/tests/scene_solver_contract_test.c"
      provides: "Group PARALLEL and anchor-based PERPENDICULAR deterministic contract coverage."
    - path: "src/tests/script_roundtrip_tests.c"
      provides: "Parallel/Perpendicular script apply/emit roundtrip coverage."
  key_links:
    - from: "src/ecs/ecs_scene.h"
      to: "src/tests/scene_solver_contract_test.c"
      via: "selection-order permutations for deterministic canonical-anchor outcomes"
      pattern: "CONSTRAINT_PERPENDICULAR"
    - from: "src/app.c"
      to: "src/constraints/constraint_types.h"
      via: "constraint menu legality reason text"
      pattern: "constraint_type_is_selection_legal"
    - from: "src/tests/script_roundtrip_tests.c"
      to: "scene_add_constraint_to_sketch_with_descriptors"
      via: "script parse/apply/emit of Parallel/Perpendicular"
      pattern: "Parallel|Perpendicular"
---

<objective>
Complete Phase 26 group behavior: deterministic multi-line PARALLEL and anchor-contract multi-line PERPENDICULAR, with explicit legality/UI diagnostics and roundtrip regression safety.

Purpose: Satisfy LCON-02/04/05 while preserving solver transactionality, deterministic reruns, and thin app callers.
Output: Group line-line runtime + legality behavior with deterministic test evidence and script roundtrip coverage.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-line-line-constraint-coverage/26-CONTEXT.md
@.planning/phases/26-line-line-constraint-coverage/26-RESEARCH.md
@.planning/phases/26-line-line-constraint-coverage/26-VALIDATION.md
@.planning/phases/26-line-line-constraint-coverage/26-01-SUMMARY.md
@src/constraints/constraint_types.h
@src/ecs/ecs_scene.h
@src/app.c
@src/tests/scene_solver_contract_test.c
@src/tests/endpoint_pick_test.c
@src/tests/scene_solver_diagnostics_test.c
@src/tests/script_roundtrip_tests.c

<interfaces>
From src/ecs/ecs_scene.h:
```c
static inline ecs_entity_t scene_add_constraint_to_sketch_with_descriptors(
    ecs_scene_t *scene, ecs_entity_t sketch, constraint_type_t type,
    const constraint_participant_descriptor_t *participants, uint32_t participant_count,
    float value, bool has_value);
```

From src/components/constraint_comp.h:
```c
typedef struct {
    uint64_t entity;
    uint8_t role;
    uint8_t sub_index;
} constraint_participant_descriptor_t;
```

From src/constraints/constraint_types.h:
```c
static inline bool constraint_type_is_selection_legal(constraint_selection_signature_t *sig,
                                                       constraint_type_t type);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing deterministic group tests for PARALLEL and anchor-based PERPENDICULAR (LCON-02, LCON-04)</name>
  <read_first>@.planning/phases/26-line-line-constraint-coverage/26-CONTEXT.md, @src/tests/scene_solver_contract_test.c, @src/tests/endpoint_pick_test.c, @src/tests/script_roundtrip_tests.c</read_first>
  <files>src/tests/scene_solver_contract_test.c, src/tests/endpoint_pick_test.c, src/tests/script_roundtrip_tests.c, src/tests/scene_solver_diagnostics_test.c</files>
  <behavior>
    - Group PARALLEL keeps all participating lines parallel after solve (LCON-02, D-02).
    - Group PERPENDICULAR (3+ lines) enforces one canonical anchor line with all others perpendicular to anchor (LCON-04, D-01).
    - Canonical anchor is stable entity-id order and invariant to selection order permutations (D-03, D-04).
    - Invalid multi-line signatures produce explicit legality and diagnostic expectations (LCON-05, D-05, D-06, D-07).
  </behavior>
  <action>Create deterministic failing tests that permute participant order and assert identical canonical-anchor outcome. Add script roundtrip tests for `Parallel`/`Perpendicular` multi-line constraints to prevent serialization/apply regressions.</action>
  <verify>
    <automated>ctest --test-dir build -R "scene_solver_contract|endpoint_pick|script_roundtrip|scene_solver_diagnostics" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>New group tests fail before implementation and explicitly encode D-01..D-07 contracts.</acceptance_criteria>
  <done>Executable test contracts exist for LCON-02/04/05 group behavior and determinism.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement group canonicalization, anchor semantics, and explicit legality/UI diagnostics (LCON-02, LCON-04, LCON-05)</name>
  <read_first>@src/constraints/constraint_types.h, @src/ecs/ecs_scene.h, @src/app.c, @.planning/phases/26-line-line-constraint-coverage/26-CONTEXT.md</read_first>
  <files>src/constraints/constraint_types.h, src/ecs/ecs_scene.h, src/app.c</files>
  <action>Implement multi-line runtime + legality behavior per locked decisions: support group PARALLEL (D-02), support 3+ PERPENDICULAR with anchor-line contract (D-01), canonical anchor by stable entity-id ordering independent of selection order (D-03, D-04), and fixed participants as hard anchors with transactional fail when all-fixed unsatisfied (D-08, D-09, D-10). In `app.c`, surface explicit type-specific invalid line-line legality messaging (D-05) and ensure no generic silent/no-op path remains (D-07). Keep scene_solver APIs authoritative and UI/app thin.</action>
  <verify>
    <automated>ctest --test-dir build -R "scene_solver_contract|endpoint_pick|scene_solver_diagnostics|script_roundtrip" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>All new group tests pass; selection-order permutations converge identically; invalid cases produce explicit line-line reasons and family-named diagnostics.</acceptance_criteria>
  <done>Phase 26 behavioral contracts are fully implemented for pair/group line-line constraints.</done>
</task>

<task type="auto">
  <name>Task 3: Run deterministic rerun gate for Phase 26 target suite and lock regression evidence (LCON-02, LCON-04, LCON-05)</name>
  <read_first>@.planning/phases/26-line-line-constraint-coverage/26-VALIDATION.md, @src/tests/scene_solver_contract_test.c</read_first>
  <files>src/tests/scene_solver_contract_test.c, src/tests/scene_solver_diagnostics_test.c, src/tests/endpoint_pick_test.c, src/tests/script_roundtrip_tests.c</files>
  <action>Execute the targeted Phase 26 suite twice back-to-back and fix only flake-inducing assertion instability if needed. Maintain strict phase scope: no ALONG-line fixes, no tangency hardening, no gizmo semantic work.</action>
  <verify>
    <automated>ctest --test-dir build -R "scene_solver_contract|scene_solver_diagnostics|scene_solver_pass_policy|endpoint_pick|script_roundtrip" --output-on-failure && ctest --test-dir build -R "scene_solver_contract|scene_solver_diagnostics|scene_solver_pass_policy|endpoint_pick|script_roundtrip" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>Two consecutive targeted suite runs are green with deterministic outcomes and no out-of-scope changes.</acceptance_criteria>
  <done>Phase 26 implementation is closure-ready with deterministic rerun evidence.</done>
</task>

</tasks>

<verification>
Run full targeted line-line suite twice:
`ctest --test-dir build -R "scene_solver_contract|scene_solver_diagnostics|scene_solver_pass_policy|endpoint_pick|script_roundtrip" --output-on-failure`
</verification>

<success_criteria>
- LCON-02 and LCON-04 are met with deterministic group behavior using stable canonical anchor semantics.
- LCON-05 explicit legality and family-specific diagnostics are enforced for invalid/unsatisfied line-line paths.
- Behavior is selection-order invariant and deterministic across immediate reruns.
</success_criteria>

<output>
After completion, create `.planning/phases/26-line-line-constraint-coverage/26-02-SUMMARY.md`
</output>

