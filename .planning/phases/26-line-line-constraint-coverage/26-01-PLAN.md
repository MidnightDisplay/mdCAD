---
phase: 26-line-line-constraint-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/constraints/constraint_types.h
  - src/ecs/ecs_scene.h
  - src/tests/scene_solver_contract_test.c
  - src/tests/endpoint_pick_test.c
  - src/tests/scene_solver_diagnostics_test.c
autonomous: true
requirements: [LCON-01, LCON-03, LCON-05]
must_haves:
  truths:
    - "User can apply PARALLEL between two feasible sketch lines and solver converges."
    - "User can apply PERPENDICULAR between two feasible sketch lines and solver converges."
    - "Invalid line-line selections are rejected by legality checks with explicit family-specific diagnostics instead of generic unsupported failure."
  artifacts:
    - path: "src/constraints/constraint_types.h"
      provides: "Type-specific legality policy for line-line constraints, including explicit PERPENDICULAR participant contract."
    - path: "src/ecs/ecs_scene.h"
      provides: "Transactional pair PARALLEL/PERPENDICULAR solve branches with fixed-participant hard-anchor behavior."
    - path: "src/tests/scene_solver_contract_test.c"
      provides: "Pair line-line solve + all-fixed unsatisfied transactional tests."
    - path: "src/tests/endpoint_pick_test.c"
      provides: "Legality/invalid-selection tests for line-line applicability."
    - path: "src/tests/scene_solver_diagnostics_test.c"
      provides: "Family-specific unsatisfied diagnostic assertions for PARALLEL/PERPENDICULAR."
  key_links:
    - from: "src/constraints/constraint_types.h"
      to: "scene_add_constraint_to_sketch_with_descriptors in src/ecs/ecs_scene.h"
      via: "constraint_type_is_selection_legal"
      pattern: "constraint_type_is_selection_legal"
    - from: "src/ecs/ecs_scene.h"
      to: "src/tests/scene_solver_contract_test.c"
      via: "scene_solver_request_recalculate pair PARALLEL/PERPENDICULAR behavior"
      pattern: "CONSTRAINT_(PARALLEL|PERPENDICULAR)"
---

<objective>
Implement Phase 26 pair line-line behavior foundation: legal, deterministic, transactional PARALLEL and PERPENDICULAR solving with explicit diagnostics.

Purpose: Close the existing legality/runtime parity gap for pair line-line authoring while honoring locked contracts D-03..D-10.
Output: Pair solve branches, legality enforcement updates, and tests proving success + explicit invalid/unsatisfied behavior.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/26-line-line-constraint-coverage/26-CONTEXT.md
@.planning/phases/26-line-line-constraint-coverage/26-RESEARCH.md
@.planning/phases/26-line-line-constraint-coverage/26-VALIDATION.md
@src/constraints/constraint_types.h
@src/ecs/ecs_scene.h
@src/tests/scene_solver_contract_test.c
@src/tests/scene_solver_diagnostics_test.c
@src/tests/endpoint_pick_test.c

<interfaces>
From src/constraints/constraint_types.h:
```c
static inline bool constraint_type_is_selection_legal(constraint_selection_signature_t *sig,
                                                       constraint_type_t type);
```

From src/ecs/ecs_scene.h:
```c
static inline ecs_entity_t scene_add_constraint_to_sketch_with_descriptors(
    ecs_scene_t *scene, ecs_entity_t sketch, constraint_type_t type,
    const constraint_participant_descriptor_t *participants, uint32_t participant_count,
    float value, bool has_value);

static inline bool scene_solver_request_recalculate(ecs_scene_t *scene, ecs_entity_t sketch);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing pair line-line contract and legality tests (LCON-01, LCON-03, LCON-05)</name>
  <read_first>@.planning/phases/26-line-line-constraint-coverage/26-CONTEXT.md, @src/tests/scene_solver_contract_test.c, @src/tests/endpoint_pick_test.c, @src/tests/scene_solver_diagnostics_test.c</read_first>
  <files>src/tests/scene_solver_contract_test.c, src/tests/endpoint_pick_test.c, src/tests/scene_solver_diagnostics_test.c</files>
  <behavior>
    - Pair PARALLEL between feasible lines solves and both lines remain parallel (LCON-01).
    - Pair PERPENDICULAR between feasible lines solves and both lines become perpendicular (LCON-03).
    - Invalid line-line selection signatures are rejected explicitly at legality boundary (LCON-05).
    - All-fixed unsatisfied pair setup fails transactionally with no geometry mutation and family-specific message (D-08, D-09, D-06).
  </behavior>
  <action>Create targeted failing tests for pair line-line behavior before implementation, including deterministic assertions that do not depend on selection order (per D-04). Keep fixtures strictly in line-line scope; do not add ALONG/tangency/gizmo coverage.</action>
  <verify>
    <automated>ctest --test-dir build -R "scene_solver_contract|endpoint_pick|scene_solver_diagnostics" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>New tests fail initially for missing pair PARALLEL/PERPENDICULAR runtime support and pass only when explicit legality + diagnostics behavior is present.</acceptance_criteria>
  <done>Executable regression contract exists for LCON-01/03/05 pair behavior with transactional unsatisfied checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement pair legality/runtime parity with fixed-anchor transactional behavior (LCON-01, LCON-03)</name>
  <read_first>@src/constraints/constraint_types.h, @src/ecs/ecs_scene.h, @.planning/phases/26-line-line-constraint-coverage/26-CONTEXT.md</read_first>
  <files>src/constraints/constraint_types.h, src/ecs/ecs_scene.h</files>
  <action>Implement pair PARALLEL/PERPENDICULAR solve branches in `scene_solver_request_recalculate(...)` so supported pair constraints no longer fall through to generic unsupported diagnostics (per D-07). Enforce fixed participants as hard anchors (D-08), and if both participants are fixed and unsatisfied, fail transactionally without mutating geometry (D-09). Keep solver authority in scene APIs and UI/app as thin callers per project convention.</action>
  <verify>
    <automated>ctest --test-dir build -R "scene_solver_contract|scene_solver_diagnostics" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>Pair PARALLEL/PERPENDICULAR tests pass; unsatisfied all-fixed tests prove no geometry changes and explicit family-specific unsat reason.</acceptance_criteria>
  <done>Pair line-line constraints are deterministic, legal, and transaction-safe with explicit diagnostics.</done>
</task>

<task type="auto">
  <name>Task 3: Close deterministic rerun proof for pair line-line slice (LCON-05 determinism guard)</name>
  <read_first>@.planning/phases/26-line-line-constraint-coverage/26-VALIDATION.md, @src/tests/scene_solver_contract_test.c</read_first>
  <files>src/tests/scene_solver_contract_test.c, src/tests/scene_solver_diagnostics_test.c</files>
  <action>Refine assertions/messages only as needed so pair line-line tests are stable across immediate reruns. Ensure diagnostics explicitly name PARALLEL or PERPENDICULAR family and implicated entities (D-06), and do not rely on selection order (D-03, D-04).</action>
  <verify>
    <automated>ctest --test-dir build -R "scene_solver_contract|scene_solver_diagnostics" --output-on-failure && ctest --test-dir build -R "scene_solver_contract|scene_solver_diagnostics" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>Two consecutive targeted runs produce identical pass/fail outcomes with no flaky line-line diagnostics behavior.</acceptance_criteria>
  <done>Pair line-line slice is deterministically green and ready for group semantics extension.</done>
</task>

</tasks>

<verification>
Run targeted pair and diagnostics checks twice:
`ctest --test-dir build -R "scene_solver_contract|scene_solver_diagnostics|endpoint_pick" --output-on-failure`
</verification>

<success_criteria>
- LCON-01 and LCON-03 pair behavior is solved in feasible setups.
- LCON-05 explicit legality/unsatisfied diagnostics are present for pair line-line constraints.
- Locked decisions D-03..D-10 are enforced for pair scope with transactional integrity preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/26-line-line-constraint-coverage/26-01-SUMMARY.md`
</output>

