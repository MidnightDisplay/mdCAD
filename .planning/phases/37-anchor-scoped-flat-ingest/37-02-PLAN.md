---
phase: 37-anchor-scoped-flat-ingest
plan: 02
type: execute
wave: 2
depends_on: [37-01]
files_modified:
  - src/jsonl_import_job.h
  - src/tests/jsonl_flat_anchor_scoped_ingest_test.c
autonomous: true
requirements: [FIMP-03]
must_haves:
  truths:
    - "Each flat import run creates one root anchor containing imported plain scene geometry under entry anchors."
    - "Entry anchors are created only for entries that actually produce geometry."
    - "Root and entry label contracts match filename/path and entry Name/Description values."
    - "Re-importing the same file creates a new root with deterministic numeric suffixes and does not remove prior roots."
    - "Current selection is unchanged by successful flat ingest."
  artifacts:
    - path: "src/jsonl_import_job.h"
      provides: "Anchor-scoped hierarchy creation, naming collision handling, and parenting flow for flat ingest."
      contains: "jsonl_import_job_tick"
    - path: "src/tests/jsonl_flat_anchor_scoped_ingest_test.c"
      provides: "Green regression coverage for D-02..D-08."
      contains: "test_flat_import_reimport_creates_suffixed_new_root"
  key_links:
    - from: "src/jsonl_import_job.h"
      to: "src/ecs/ecs_scene.h"
      via: "scene_add_anchor during parse-complete and lazy entry-anchor creation"
      pattern: "scene_add_anchor"
    - from: "src/jsonl_import_job.h"
      to: "src/ecs/ecs_world.h"
      via: "EcsChildOf parenting geometry->entry and entry->root"
      pattern: "ecs_add_pair\\(.*EcsChildOf"
---

<objective>
Implement Phase 37 flat ingest contracts in import-job internals so FIMP-03 behavior is deterministic and regression-tested.

Purpose: Make imported flat JSONL data land under a controlled root anchor hierarchy without changing Phase 36 entry UX.
Output: Green Phase 37 contract tests and ingest logic honoring D-01..D-08.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/37-anchor-scoped-flat-ingest/37-CONTEXT.md
@.planning/phases/37-anchor-scoped-flat-ingest/37-RESEARCH.md
@.planning/phases/37-anchor-scoped-flat-ingest/37-VALIDATION.md
@.planning/phases/37-anchor-scoped-flat-ingest/37-01-SUMMARY.md
@src/jsonl_import_job.h
@src/ecs/ecs_scene.h
@src/ecs/ecs_world.h
@src/components/label_comp.h
@src/ui/ui_scene_hierarchy.h

<interfaces>
From src/jsonl_import_job.h:
```c
static inline bool jsonl_import_job_start(jsonl_import_job_t *job, const char *filepath, float scale,
                                          bool use_jsonl_colours, vec4_t default_colour, bool shift_to_com,
                                          float rotation_x, float rotation_y, float rotation_z);
static inline bool jsonl_import_job_tick(jsonl_import_job_t *job, ecs_scene_t *scene);
```

From src/ecs/ecs_scene.h:
```c
static inline ecs_entity_t scene_add_anchor(ecs_scene_t *scene, const char *name, const char *desc);
```

From src/ecs/ecs_world.h:
```c
static inline bool ecs_world_is_selected(ecs_world_state_t *s, ecs_entity_t e);
static inline ecs_entity_t ecs_world_get_parent(ecs_world_state_t *s, ecs_entity_t e);
static inline int ecs_world_get_children(ecs_world_state_t *s, ecs_entity_t parent, ecs_entity_t *out_children, int max_count);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement anchor-scoped root/entry hierarchy and naming contracts in import job</name>
  <read_first>@src/jsonl_import_job.h, @src/ecs/ecs_scene.h, @src/components/label_comp.h, @.planning/phases/37-anchor-scoped-flat-ingest/37-CONTEXT.md, @src/tests/jsonl_flat_anchor_scoped_ingest_test.c</read_first>
  <files>src/jsonl_import_job.h</files>
  <behavior>
    - Root anchor name resolves to source filename stem and description to full source path (D-04).
    - Root collisions use deterministic numeric suffixes on re-import while preserving prior roots (D-06, D-07).
    - Entry anchors are created lazily only when at least one geometry entity is created for that entry (D-02, D-03).
    - Entry labels use entry Name and Description (D-05).
    - Geometry remains plain non-sketch scene entities under entry anchors under one root (FIMP-03 / D-02).
  </behavior>
  <action>Update `jsonl_import_job_tick(...)` parse-complete and parenting flow to satisfy D-02..D-07: (1) derive root base name from filepath stem and assign full filepath to root description per D-04; (2) before root creation, scan existing labels to resolve deterministic suffix collisions `name`, `name (2)`, `name (3)` per D-07 while preserving existing roots per D-06; (3) stop eager creation of all entry anchors and instead create each entry anchor on first successfully created geometry for that entry, using entry name/description per D-05; (4) parent only existing entry anchors to root and geometry to their entry anchors; (5) keep import as plain geometry creation path (no sketch conversion) per FIMP-03. Keep Phase 36 menu/import-path behavior untouched per D-01 and do not implement refresh/observer-loop/perf features (Phases 38-40).</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "jsonl_flat_anchor_scoped_ingest_test" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>All D-02..D-07 assertions in `jsonl_flat_anchor_scoped_ingest_test` pass with root/entry hierarchy and naming behavior deterministic across reruns.</acceptance_criteria>
  <done>Import job now builds the correct anchor-scoped flat hierarchy for Phase 37.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve selection invariance and lock no-regression verification slice</name>
  <read_first>@src/tests/jsonl_flat_anchor_scoped_ingest_test.c, @src/jsonl_import_job.h, @src/tests/jsonl_flat_import_ui_contract_test.c, @src/tests/jsonl_flat_import_options_contract_test.c</read_first>
  <files>src/tests/jsonl_flat_anchor_scoped_ingest_test.c, src/jsonl_import_job.h</files>
  <behavior>
    - Selected entity before import remains selected after successful import (D-08).
    - Phase 36 flat entry/config contract remains intact while implementing Phase 37 internals (D-01).
  </behavior>
  <action>Finalize/adjust tests and import-job internals so selection state is preserved per D-08 (no selection hijack). Verify this phase only changes ingest internals and does not repurpose UI flows from Phase 36 per D-01 by running Phase 36 + Phase 37 targeted contracts together. If research suggests additional refactors, defer them because Phase 37 scope is ingest-only.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "jsonl_flat_anchor_scoped_ingest_test|jsonl_flat_import_ui_contract_test|jsonl_flat_import_options_contract_test" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>Selection invariance test passes and combined targeted suite confirms no regression in dedicated flat-import entry/config flows.</acceptance_criteria>
  <done>Phase 37 behavior is green and scoped, with prior phase contracts preserved.</done>
</task>

</tasks>

<verification>
- `cmake --build build-vulkan --config Release`
- `ctest --test-dir build-vulkan -C Release -R "jsonl_flat_anchor_scoped_ingest_test|jsonl_flat_import_ui_contract_test|jsonl_flat_import_options_contract_test" --output-on-failure`
</verification>

<success_criteria>
- One root anchor is created per import run with deterministic name/description contract.
- Hierarchy is `root -> entry -> geometry` with empty entries skipped.
- Re-import creates new suffixed roots while preserving prior anchors.
- Successful ingest preserves existing selection.
- No Phase 38/39/40 behavior is introduced in this phase.
</success_criteria>

<output>
After completion, create `.planning/phases/37-anchor-scoped-flat-ingest/37-02-SUMMARY.md`
</output>
