---
phase: 37-anchor-scoped-flat-ingest
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tests/jsonl_flat_anchor_scoped_ingest_test.c
  - src/CMakeLists.txt
autonomous: true
requirements: [FIMP-03]
must_haves:
  truths:
    - "Phase 37 has executable contract coverage for root->entry->geometry flat ingest behavior."
    - "CTest discovers the Phase 37 anchor-scoped ingest test by exact name."
  artifacts:
    - path: "src/tests/jsonl_flat_anchor_scoped_ingest_test.c"
      provides: "Executable assertions for D-02..D-08 ingest contracts."
      contains: "test_flat_import_creates_root_entry_geometry_hierarchy"
    - path: "src/CMakeLists.txt"
      provides: "Build + ctest wiring for jsonl_flat_anchor_scoped_ingest_test."
      contains: "add_test(NAME jsonl_flat_anchor_scoped_ingest_test"
  key_links:
    - from: "src/CMakeLists.txt"
      to: "src/tests/jsonl_flat_anchor_scoped_ingest_test.c"
      via: "add_executable/add_test"
      pattern: "jsonl_flat_anchor_scoped_ingest_test"
---

<objective>
Create Wave 1 executable contract tests for Phase 37 anchor-scoped flat ingest semantics before implementation.

Purpose: Lock FIMP-03 and D-02..D-08 behavior in automated tests so implementation stays in scope.
Output: New Phase 37 test target and CTest registration.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/37-anchor-scoped-flat-ingest/37-CONTEXT.md
@.planning/phases/37-anchor-scoped-flat-ingest/37-RESEARCH.md
@.planning/phases/37-anchor-scoped-flat-ingest/37-VALIDATION.md
@.planning/phases/36-flat-import-entry-configuration/36-02-SUMMARY.md
@src/jsonl_import_job.h
@src/ecs/ecs_scene.h
@src/ecs/ecs_world.h
@src/CMakeLists.txt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author RED-first anchor-scoped ingest contract tests</name>
  <read_first>@.planning/phases/37-anchor-scoped-flat-ingest/37-CONTEXT.md, @.planning/phases/37-anchor-scoped-flat-ingest/37-RESEARCH.md, @src/tests/jsonl_sketch_import_test.c, @src/jsonl_import_job.h, @src/ecs/ecs_world.h</read_first>
  <files>src/tests/jsonl_flat_anchor_scoped_ingest_test.c</files>
  <behavior>
    - Test 1: Import creates two-level hierarchy `root -> entry -> geometry` (D-02) and imports plain non-sketch geometry.
    - Test 2: Entries with zero importable geometry do not create entry anchors (D-03).
    - Test 3: Root label uses filename stem as `Label.name` and full source path as `Label.description`; entry labels mirror JSONL Name/Description (D-04, D-05).
    - Test 4: Re-importing same source preserves prior roots and creates a new root with deterministic numeric suffix collision handling (`name`, `name (2)`, ...) (D-06, D-07).
    - Test 5: Existing selected entity remains selected after successful import (D-08).
  </behavior>
  <action>Implement a new native C test file that initializes `ecs_world_t`/`ecs_scene_t`, writes JSONL fixtures, runs `jsonl_import_job_start(...)` + tick-to-completion, and asserts hierarchy/label/selection contracts per D-02..D-08. Include explicit phase boundary checks in assertions/comments: do not add observer refresh execution, observer safety loop, or perf hardening behaviors (defer to Phases 38-40). Also assert no sketch entities are created for imported geometry to satisfy FIMP-03.</action>
  <verify>
    <automated>git --no-pager grep -n "test_flat_import_.*hierarchy\|test_flat_import_.*reimport\|test_flat_import_.*selection" src/tests/jsonl_flat_anchor_scoped_ingest_test.c</automated>
  </verify>
  <acceptance_criteria>New test file exists with deterministic test cases covering D-02 through D-08 and explicit FIMP-03 non-sketch assertions.</acceptance_criteria>
  <done>Phase 37 ingest contracts are codified in executable RED-first tests.</done>
</task>

<task type="auto">
  <name>Task 2: Register Phase 37 ingest contract test in CMake/CTest</name>
  <read_first>@src/CMakeLists.txt, @src/tests/jsonl_flat_import_options_contract_test.c</read_first>
  <files>src/CMakeLists.txt</files>
  <action>Add `add_executable(...)`, include directories, target link libraries, and `add_test(...)` entries for `jsonl_flat_anchor_scoped_ingest_test` using the same pattern as existing `jsonl_*` tests. Keep registration scoped to this new test only (no unrelated test target churn).</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -N -R "jsonl_flat_anchor_scoped_ingest_test"</automated>
  </verify>
  <acceptance_criteria>CTest lists `jsonl_flat_anchor_scoped_ingest_test` by exact name.</acceptance_criteria>
  <done>Wave 1 test contract is runnable in the existing build/test pipeline.</done>
</task>

</tasks>

<verification>
- `cmake --build build-vulkan --config Release --target jsonl_flat_anchor_scoped_ingest_test`
- `ctest --test-dir build-vulkan -C Release -N -R "jsonl_flat_anchor_scoped_ingest_test"`
</verification>

<success_criteria>
- Phase 37 has a dedicated executable contract test for anchor-scoped ingest semantics.
- Test discovery for `jsonl_flat_anchor_scoped_ingest_test` is green.
- Scope remains Phase 37 only (ingest structure), with refresh/observer-loop/perf work deferred.
</success_criteria>

<output>
After completion, create `.planning/phases/37-anchor-scoped-flat-ingest/37-01-SUMMARY.md`
</output>
