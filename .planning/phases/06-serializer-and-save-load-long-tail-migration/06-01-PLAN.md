---
phase: 06-serializer-and-save-load-long-tail-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scene_serializer.h
  - src/ui/ui_scene_hierarchy.h
autonomous: true
requirements:
  - TAIL-01
must_haves:
  truths:
    - "Serializer defaults and parse/write math paths in scope are cglm-backed and do not rely on legacy math3d helper calls (D-09, D-10)."
    - "Save output uses the cleaned Phase 6 schema and load path consumes that schema (D-04, D-05)."
    - "Save/load UI messages remain clear and narrowly scoped to serializer workflow clarity (D-01, D-02)."
  artifacts:
    - path: "src/scene_serializer.h"
      provides: "Serializer-local math migration, cleaned schema write/read, and two-pass load retention."
      contains: "SCENE_JSON_VERSION 2"
    - path: "src/ui/ui_scene_hierarchy.h"
      provides: "Save/load status text updates with serializer-format clarity only."
      contains: "save/load status strings"
  key_links:
    - from: "src/ui/ui_scene_hierarchy.h"
      to: "scene_save_to_file / scene_load_from_file"
      via: "file browser callback save/load branch"
      pattern: "FILE_BROWSER_MODE_SAVE|scene_load_from_file"
    - from: "src/scene_serializer.h"
      to: "src/math/cglm_entry.h"
      via: "serializer-local cglm-backed helper usage in defaults/transform paths"
      pattern: "cglm_entry|SCENE_JSON_VERSION"
---

<objective>
Migrate Phase 6 serializer internals to cglm-backed math paths, introduce cleaned scene schema handling, and keep save/load UI changes tightly scoped.

Purpose: Implement the core TAIL-01 migration boundary in serializer scope while preserving existing load architecture and avoiding scope creep.
Output: Updated serializer + serializer-adjacent UI callsite/status plumbing ready for converter/check tooling in later plans.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-serializer-and-save-load-long-tail-migration/06-CONTEXT.md
@.planning/phases/06-serializer-and-save-load-long-tail-migration/06-RESEARCH.md
@src/scene_serializer.h
@src/ui/ui_scene_hierarchy.h
@src/math/cglm_entry.h

<interfaces>
From src/scene_serializer.h:
```c
static inline bool scene_save_to_file(ecs_scene_t *scene, const char *filepath);
static inline int scene_load_from_file(ecs_scene_t *scene, const char *filepath, bool clear_existing);
static inline int scene_load_from_string(ecs_scene_t *scene, const char *json, bool clear_existing);
```

From src/ui/ui_scene_hierarchy.h:
```c
if (state->file_browser.mode == FILE_BROWSER_MODE_SAVE) { scene_save_to_file(...); }
else { int count = scene_load_from_file(..., state->clear_on_load); }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retire serializer-scope legacy helper defaults and wire cglm-backed defaults</name>
  <read_first>
    - src/scene_serializer.h
    - src/math/cglm_entry.h
    - .planning/phases/06-serializer-and-save-load-long-tail-migration/06-CONTEXT.md
  </read_first>
  <files>src/scene_serializer.h</files>
  <action>Per D-09/D-10, replace serializer-internal default initialization usages of legacy helper constructors (e.g., vec3_make/vec4_make) with serializer-local cglm-backed value construction that keeps existing vec3_t/vec4_t storage contracts intact. Keep all changes inside serializer scope; do not touch importer/undo/editor files per D-03.</action>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path src/scene_serializer.h -Pattern 'vec3_make\\(|vec4_make\\(' | Measure-Object | % Count"</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String src/scene_serializer.h -Pattern 'vec3_make\(|vec4_make\('` returns zero matches for serializer default initialization paths.
    - `Select-String src/scene_serializer.h -Pattern 'json_parse_transform|json_parse_geometry|json_parse_light'` still returns active parse functions.
    - `Select-String src/scene_serializer.h -Pattern 'scene_set_parents_batch'` still returns the second-pass parenting call.
  </acceptance_criteria>
  <done>Serializer math defaults in scope are migrated off legacy helper constructors while preserving current parse and load control flow.</done>
</task>

<task type="auto">
  <name>Task 2: Apply cleaned scene schema contract in serializer save/load path</name>
  <read_first>
    - src/scene_serializer.h
    - .planning/phases/06-serializer-and-save-load-long-tail-migration/06-CONTEXT.md
    - .planning/phases/06-serializer-and-save-load-long-tail-migration/06-RESEARCH.md
  </read_first>
  <files>src/scene_serializer.h</files>
  <action>Per D-04/D-05, introduce a cleaned Phase 6 schema contract in save/load (e.g., explicit format marker + version bump to 2). Update save writer and load parser to use the cleaned contract and keep old-format compatibility out-of-phase. Preserve entity create-first + batch-parent-link second pass and transform assignment semantics unchanged.</action>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path src/scene_serializer.h -Pattern 'SCENE_JSON_VERSION\\s+2|format|version|scene_load_from_string' | ForEach-Object { $_.Line }"</automated>
  </verify>
  <acceptance_criteria>
    - `Select-String src/scene_serializer.h -Pattern '#define SCENE_JSON_VERSION 2'` returns exactly one match.
    - `Select-String src/scene_serializer.h -Pattern '"version"|format'` shows cleaned schema write/read fields.
    - `Select-String src/scene_serializer.h -Pattern 'scene_set_parents_batch'` still returns one active batch parenting path.
  </acceptance_criteria>
  <done>Serializer reads/writes cleaned schema v2 and keeps hierarchy + transform behavior architecture intact.</done>
</task>

<task type="auto">
  <name>Task 3: Apply minimal save/load clarity polish in hierarchy UI callsite</name>
  <read_first>
    - src/ui/ui_scene_hierarchy.h
    - src/scene_serializer.h
    - .planning/phases/06-serializer-and-save-load-long-tail-migration/06-CONTEXT.md
  </read_first>
  <files>src/ui/ui_scene_hierarchy.h</files>
  <action>Per D-01/D-02, update only serializer-related save/load status strings and failure guidance in the file-browser callback block to improve format clarity (including converter hint when load fails), without changing broader hierarchy UI behavior or unrelated importer flows.</action>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path src/ui/ui_scene_hierarchy.h -Pattern 'Saved|Loaded|Failed to load|converter|format' | ForEach-Object { \"{0}:{1}\" -f $_.LineNumber, $_.Line.Trim() }"</automated>
  </verify>
  <acceptance_criteria>
    - Save branch still calls `scene_save_to_file` and load branch still calls `scene_load_from_file`.
    - Status strings include explicit save/load clarity wording tied to serializer format handling.
    - No new importer/undo/editor logic is introduced in this task.
  </acceptance_criteria>
  <done>UI callsite messaging is clearer for serializer save/load outcomes while staying inside the approved scope cutline.</done>
</task>

</tasks>

<verification>
- Build gate command is runnable after code changes: `cmake --build build-vulkan --config Release --target mdcad_math_harness`
- Serializer source audit confirms migration in scope and preserved two-pass parent linking.
</verification>

<success_criteria>
- `src/scene_serializer.h` reflects cleaned v2 schema and serializer-scope math migration.
- `src/ui/ui_scene_hierarchy.h` save/load messaging is updated with narrow, migration-relevant clarity only.
- No importer/undo/editor migration work is included.
</success_criteria>

<output>
After completion, create `.planning/phases/06-serializer-and-save-load-long-tail-migration/06-01-SUMMARY.md`
</output>

