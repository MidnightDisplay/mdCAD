---
phase: 32-explicit-coincidence-authoring-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/constraint_comp.h
  - src/ecs/ecs_scene.h
  - src/app.c
  - src/tests/scene_solver_contract_test.c
autonomous: true
requirements: [COIN-01, COIN-02]
must_haves:
  truths:
    - "Authoring ArcAxisLine creates an explicit coincident relation between arc center and the chosen line endpoint, not implicit-only coupling."
    - "ArcAxisLine endpoint binding is deterministic at creation (nearest endpoint) and remains stable through subsequent edits."
    - "Authoring line-end/arc-end tangency creates an explicit endpoint coincident relation alongside tangency."
    - "If explicit paired coincidence cannot be created, composite authoring fails transactionally with explicit diagnostics."
  artifacts:
    - path: "src/ecs/ecs_scene.h"
      provides: "Composite authoring helper that creates owner + paired explicit coincidence in one transaction."
      contains: "deterministic nearest-endpoint selection and stable pairing metadata assignment"
    - path: "src/components/constraint_comp.h"
      provides: "Constraint metadata fields for owner↔paired coincidence linkage."
      contains: "link fields persisted on both owner and paired coincidence constraints"
    - path: "src/app.c"
      provides: "Constraint menu integration routing ArcAxisLine and line-arc endpoint tangency through composite authoring helper."
      contains: "explicit failure path when paired coincidence creation fails"
    - path: "src/tests/scene_solver_contract_test.c"
      provides: "Regression assertions for explicit paired coincidence authoring semantics."
      contains: "ArcAxisLine nearest-endpoint + stable-binding + tangency paired-coincidence creation tests"
  key_links:
    - from: "src/app.c"
      to: "src/ecs/ecs_scene.h"
      via: "constraint context menu authoring branch for ArcAxisLine and line-end/arc-end tangency"
      pattern: "CONSTRAINT_ARC_AXIS_LINE|CONSTRAINT_LINE_ARC_ENDPOINT_TANGENCY"
    - from: "src/ecs/ecs_scene.h"
      to: "src/components/constraint_comp.h"
      via: "owner/paired coincidence link metadata write on creation"
      pattern: "pair|paired|coincident"
---

<objective>
Implement explicit composite authoring semantics for ArcAxisLine and line-end/arc-end tangency, including deterministic pairing metadata and UI integration.

Purpose: Satisfy COIN-01 and the authoring half of COIN-02 by removing hidden implicit coupling and enforcing explicit, durable pair creation semantics.
Output: Composite create path in scene + menu integration + authoring contract regressions.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-explicit-coincidence-authoring-semantics/32-CONTEXT.md
@.planning/phases/32-explicit-coincidence-authoring-semantics/32-RESEARCH.md
@.planning/phases/32-explicit-coincidence-authoring-semantics/32-VALIDATION.md
@src/app.c
@src/components/constraint_comp.h
@src/constraints/constraint_types.h
@src/ecs/ecs_scene.h
@src/tests/scene_solver_contract_test.c

<interfaces>
From src/app.c:
```c
static bool mdcad_collect_constraint_context(...);
static void mdcad_draw_constraint_context_menu(void);
```

From src/ecs/ecs_scene.h:
```c
static inline ecs_entity_t scene_add_constraint_to_sketch_with_descriptors(...);
static inline ecs_entity_t scene_add_constraint_to_sketch(...);
```

From src/components/constraint_comp.h:
```c
typedef struct {
    constraint_type_t type;
    uint32_t participant_count;
    uint64_t participants[CONSTRAINT_MAX_PARTICIPANTS];
    constraint_participant_descriptor_t participant_descriptors[CONSTRAINT_MAX_PARTICIPANTS];
} ConstraintComp;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add composite pairing metadata and transactional scene authoring helper</name>
  <read_first>@src/components/constraint_comp.h, @src/ecs/ecs_scene.h, @.planning/phases/32-explicit-coincidence-authoring-semantics/32-CONTEXT.md</read_first>
  <files>src/components/constraint_comp.h, src/ecs/ecs_scene.h</files>
  <action>Add explicit owner↔paired-coincident linkage metadata in `ConstraintComp` and introduce a scene-level composite authoring helper that creates paired `CONSTRAINT_COINCIDENT` + owner relation atomically for `CONSTRAINT_ARC_AXIS_LINE` and `CONSTRAINT_LINE_ARC_ENDPOINT_TANGENCY`. Implement nearest-line-endpoint selection for ArcAxisLine per D-02, persist stable endpoint role/sub-index per D-03, enforce explicit pairing (no implicit fallback) per D-01/D-05/D-07, and return deterministic failure when coincidence creation cannot be established per D-04.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "scene_solver_contract" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`git --no-pager grep -n "pair" src/components/constraint_comp.h src/ecs/ecs_scene.h` shows explicit pair-link fields/helpers; `git --no-pager grep -n "nearest" src/ecs/ecs_scene.h` shows deterministic ArcAxisLine endpoint selection logic; targeted `scene_solver_contract` ctest slice passes.</acceptance_criteria>
  <done>Scene layer can create composite owner + explicit coincidence as one deterministic transaction with stable pairing metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route constraint menu authoring through composite helper for ArcAxisLine/tangency</name>
  <read_first>@src/app.c, @src/ecs/ecs_scene.h, @.planning/phases/32-explicit-coincidence-authoring-semantics/32-CONTEXT.md</read_first>
  <files>src/app.c, src/ecs/ecs_scene.h</files>
  <action>Update `mdcad_draw_constraint_context_menu` so `CONSTRAINT_ARC_AXIS_LINE` and `CONSTRAINT_LINE_ARC_ENDPOINT_TANGENCY` call the new composite helper instead of direct single-constraint creation. Keep non-composite constraints on existing path. Wire explicit transactional failure handling for missing paired coincidence per D-04 and enforce no hidden implicit fallback behavior per D-07. Ensure ArcAxisLine/tangency menu create path preserves descriptor role/sub-index selected at author-time per D-03/D-05.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "scene_solver_contract" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`git --no-pager grep -n "CONSTRAINT_ARC_AXIS_LINE\\|CONSTRAINT_LINE_ARC_ENDPOINT_TANGENCY" src/app.c` shows explicit composite create branching; `git --no-pager grep -n "scene_add_constraint_to_sketch_with_descriptors" src/app.c` confirms non-composite path remains; targeted ctest command passes.</acceptance_criteria>
  <done>UI authoring now always produces explicit paired coincidence semantics for ArcAxisLine and line-end/arc-end tangency creation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add authoring contract regressions for explicit paired coincidence creation and stability</name>
  <read_first>@src/tests/scene_solver_contract_test.c, @src/ecs/ecs_scene.h, @src/constraints/constraint_types.h, @.planning/phases/32-explicit-coincidence-authoring-semantics/32-CONTEXT.md</read_first>
  <files>src/tests/scene_solver_contract_test.c</files>
  <action>Add contract tests that verify (1) ArcAxisLine authoring creates an explicit paired `CONSTRAINT_COINCIDENT(arc.center, nearest line endpoint)` per D-01/D-02, (2) selected endpoint binding does not auto-switch after edits/recalc per D-03, and (3) line-end/arc-end tangency authoring creates explicit paired endpoint coincidence per D-05. Add transactional failure assertion for composite creation when explicit coincidence setup fails per D-04 (owner and pair not partially created).</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "scene_solver_contract" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`git --no-pager grep -n "coin_\\|coincidence\\|arc_axis\\|tangency" src/tests/scene_solver_contract_test.c` shows new explicit-pair tests; running the same ctest command twice yields pass/pass with identical test set execution.</acceptance_criteria>
  <done>Authoring behavior for explicit pair creation and stable binding is locked by deterministic contract tests.</done>
</task>

</tasks>

<verification>
Run:
- `ctest --test-dir build-vulkan -C Release -R "scene_solver_contract" --output-on-failure`
- Immediately rerun the identical command and confirm pass/pass parity.
</verification>

<success_criteria>
- COIN-01: ArcAxisLine authoring creates explicit center↔endpoint coincidence with stable endpoint binding.
- COIN-02 (authoring portion): line-end/arc-end tangency authoring creates explicit endpoint coincidence, not hidden implicit coupling.
- Composite create failures are transactional and explicit, with no partial owner/pair artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/32-explicit-coincidence-authoring-semantics/32-01-SUMMARY.md`
</output>

