---
phase: 32-explicit-coincidence-authoring-semantics
plan: 02
type: execute
wave: 2
depends_on: [32-01]
files_modified:
  - src/ecs/ecs_scene.h
  - src/scripting/sketch_script_emit.h
  - src/scripting/sketch_script_apply.h
  - src/tests/scene_solver_diagnostics_test.c
  - src/tests/script_roundtrip_tests.c
autonomous: true
requirements: [COIN-01, COIN-02]
must_haves:
  truths:
    - "Deleting ArcAxisLine or tangency owner does not delete paired explicit coincidence."
    - "Deleting paired explicit coincidence leaves owner present but explicitly unsatisfied, without implicit fallback."
    - "Script emit/apply persists explicit owner↔paired coincidence linkage deterministically across repeated reapply."
    - "Diagnostics remain family-specific and deterministic for missing-pair/unsatisfied owner behavior."
  artifacts:
    - path: "src/ecs/ecs_scene.h"
      provides: "Solver/lifecycle policy enforcing paired coincidence authority and non-cascade behavior."
      contains: "owner unsatisfied behavior when paired coincidence missing"
    - path: "src/scripting/sketch_script_emit.h"
      provides: "Deterministic emission of pairing metadata for composite owner constraints."
      contains: "stable serialization ordering/fields for pair linkage"
    - path: "src/scripting/sketch_script_apply.h"
      provides: "Deterministic reapply restoration of explicit pair links without heuristic recomputation."
      contains: "link reconstruction validation on apply"
    - path: "src/tests/scene_solver_diagnostics_test.c"
      provides: "Lifecycle and diagnostics assertions for owner/pair deletion semantics."
      contains: "family-specific explicit diagnostics when pair is missing"
    - path: "src/tests/script_roundtrip_tests.c"
      provides: "Roundtrip and repeat-apply determinism checks for pair linkage metadata."
      contains: "same-input repeated reapply linkage parity assertions"
  key_links:
    - from: "src/ecs/ecs_scene.h"
      to: "src/tests/scene_solver_diagnostics_test.c"
      via: "owner remains unsatisfied after paired coincidence removal"
      pattern: "Unsatisfied .*constraint|coincident"
    - from: "src/scripting/sketch_script_emit.h"
      to: "src/scripting/sketch_script_apply.h"
      via: "pair metadata roundtrip (emit -> parse/apply) for deterministic reconstruction"
      pattern: "pair|paired|owner"
    - from: "src/tests/script_roundtrip_tests.c"
      to: "src/scripting/sketch_script_emit.h"
      via: "deterministic repeated apply/emit parity gate"
      pattern: "repeat|deterministic|reapply"
---

<objective>
Implement lifecycle, solver/diagnostic, and script roundtrip durability semantics for explicit paired coincidence constraints.

Purpose: Complete COIN-01/COIN-02 durability by enforcing explicit pair authority through edits/deletes and deterministic script emit/reapply persistence.
Output: Lifecycle/solver behavior changes + script persistence plumbing + deterministic diagnostics/roundtrip regressions.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-explicit-coincidence-authoring-semantics/32-CONTEXT.md
@.planning/phases/32-explicit-coincidence-authoring-semantics/32-RESEARCH.md
@.planning/phases/32-explicit-coincidence-authoring-semantics/32-VALIDATION.md
@.planning/phases/32-explicit-coincidence-authoring-semantics/32-01-SUMMARY.md
@src/ecs/ecs_scene.h
@src/scripting/sketch_script_emit.h
@src/scripting/sketch_script_apply.h
@src/tests/scene_solver_diagnostics_test.c
@src/tests/script_roundtrip_tests.c

<interfaces>
From src/ecs/ecs_scene.h:
```c
static inline bool scene_remove_constraint(ecs_scene_t *scene, ecs_entity_t constraint_entity);
static inline bool scene_solver_request_recalculate(ecs_scene_t *scene, ecs_entity_t sketch);
```

From src/scripting/sketch_script_emit.h:
```c
static inline bool sketch_script_emit_for_sketch(...);
```

From src/scripting/sketch_script_apply.h:
```c
static inline bool sketch_script_apply_create_constraints(...);
static inline bool sketch_script_apply_validate_model_links(...);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce owner/pair lifecycle semantics and explicit unsatisfied diagnostics in solver path</name>
  <read_first>@src/ecs/ecs_scene.h, @src/tests/scene_solver_diagnostics_test.c, @.planning/phases/32-explicit-coincidence-authoring-semantics/32-CONTEXT.md</read_first>
  <files>src/ecs/ecs_scene.h, src/tests/scene_solver_diagnostics_test.c</files>
  <action>Implement lifecycle semantics per D-08/D-09: deleting ArcAxisLine/tangency owner must not cascade-delete paired explicit coincidence; deleting paired coincidence must keep owner and force explicit unsatisfied owner diagnostics with no implicit coincidence fallback. Update solver enforcement for `CONSTRAINT_ARC_AXIS_LINE` and `CONSTRAINT_LINE_ARC_ENDPOINT_TANGENCY` to consult pair metadata and treat missing pair as explicit failure state (family-specific deterministic diagnostics), honoring D-06/D-07.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "scene_solver_diagnostics|scene_solver_contract" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`git --no-pager grep -n "D-08\\|D-09\\|Unsatisfied arc-axis-vs-line constraint\\|Unsatisfied line-arc endpoint tangency constraint" src/ecs/ecs_scene.h src/tests/scene_solver_diagnostics_test.c` shows lifecycle/diagnostic coverage anchors; targeted diagnostics+contract ctest regex passes.</acceptance_criteria>
  <done>Owner/pair delete lifecycle is deterministic and explicit, with no hidden implicit re-coupling fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist explicit pair-link metadata through script emit/apply without heuristic recompute</name>
  <read_first>@src/scripting/sketch_script_emit.h, @src/scripting/sketch_script_apply.h, @src/tests/script_roundtrip_tests.c, @.planning/phases/32-explicit-coincidence-authoring-semantics/32-CONTEXT.md</read_first>
  <files>src/scripting/sketch_script_emit.h, src/scripting/sketch_script_apply.h, src/tests/script_roundtrip_tests.c</files>
  <action>Extend script emit schema for constraints to include deterministic pair-link metadata for owner/paired coincidence relationships and extend apply path to restore exact authored linkage (stable IDs/links) per D-10. Reject or explicitly flag malformed linkage rather than heuristic recomputation. Add roundtrip tests proving owner/pair link identity survives emit->apply->emit cycles and repeated reapply runs preserve identical linkage relationships for both ArcAxisLine and line-end/arc-end tangency cases.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "script_roundtrip_tests" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`git --no-pager grep -n "pair\\|paired\\|owner" src/scripting/sketch_script_emit.h src/scripting/sketch_script_apply.h src/tests/script_roundtrip_tests.c` shows pair-link serialization/apply plumbing and tests; repeated invocation of `ctest --test-dir build-vulkan -C Release -R "script_roundtrip_tests" --output-on-failure` returns pass/pass.</acceptance_criteria>
  <done>Script emit/apply now persists explicit pair linkage deterministically with no heuristic reassignment drift.</done>
</task>

  <task type="auto">
  <name>Task 3: Lock deterministic v1.5 targeted gate evidence for explicit-coincidence surfaces</name>
  <read_first>@.planning/phases/32-explicit-coincidence-authoring-semantics/32-VALIDATION.md, @src/tests/scene_solver_contract_test.c, @src/tests/scene_solver_diagnostics_test.c, @src/tests/script_roundtrip_tests.c</read_first>
  <files>src/tests/scene_solver_contract_test.c, src/tests/scene_solver_diagnostics_test.c, src/tests/script_roundtrip_tests.c</files>
  <action>Ensure Phase 32 regression set has deterministic rerun evidence using existing CTest targets only: `scene_solver_contract`, `scene_solver_diagnostics`, and `script_roundtrip_tests` plus canonical v1.5 seven-suite gate (`scene_solver_contract|scene_solver_pass_policy|scene_solver_diagnostics|scene_solver_trigger|scene_solver_drag|endpoint_pick|script_roundtrip_tests`). Keep scope strictly COIN-01/COIN-02: add/adjust only tests validating explicit coincidence authoring durability, lifecycle semantics, diagnostics, and script persistence determinism.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "scene_solver_contract|scene_solver_diagnostics|script_roundtrip_tests" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>Back-to-back execution of `ctest --test-dir build-vulkan -C Release -R "scene_solver_contract|scene_solver_diagnostics|script_roundtrip_tests" --output-on-failure` returns pass/pass during task execution; end-of-plan verification runs canonical seven-suite gate `ctest --test-dir build-vulkan -C Release -R "scene_solver_contract|scene_solver_pass_policy|scene_solver_diagnostics|scene_solver_trigger|scene_solver_drag|endpoint_pick|script_roundtrip_tests" --output-on-failure` and immediate rerun with pass/pass parity; `git --no-pager grep -n "ArcAxisLine\\|Line-End Arc-End Tangency\\|coincident" src/tests/scene_solver_contract_test.c src/tests/scene_solver_diagnostics_test.c src/tests/script_roundtrip_tests.c` shows Phase 32-specific durability assertions.</acceptance_criteria>
  <done>Deterministic targeted + canonical evidence is in place for explicit coincidence lifecycle and persistence behavior.</done>
</task>

</tasks>

<verification>
Run:
- `ctest --test-dir build-vulkan -C Release -R "scene_solver_contract|scene_solver_diagnostics|script_roundtrip_tests" --output-on-failure`
- `ctest --test-dir build-vulkan -C Release -R "scene_solver_contract|scene_solver_pass_policy|scene_solver_diagnostics|scene_solver_trigger|scene_solver_drag|endpoint_pick|script_roundtrip_tests" --output-on-failure`
- Re-run the canonical seven-suite command immediately and confirm deterministic pass/pass parity.
</verification>

<success_criteria>
- COIN-01/COIN-02 durability: explicit pair semantics survive deletes/edits and are never replaced by implicit fallback behavior.
- Diagnostics are explicit, family-specific, and deterministic when owner/pair relationships become unsatisfied.
- Script emit/reapply preserves explicit pair linkage deterministically across repeated runs.
</success_criteria>

<output>
After completion, create `.planning/phases/32-explicit-coincidence-authoring-semantics/32-02-SUMMARY.md`
</output>

