---
phase: 08-undo-editor-utility-migration-and-glue-burn-down
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/math3d.h
  - src/undo_redo_exec.h
  - src/gizmo/gizmo_vertex_mode.h
  - src/ui/ui_scene_hierarchy.h
  - docs/QUICKSTART.md
  - .planning/phases/08-undo-editor-utility-migration-and-glue-burn-down/evidence/glue-inventory.md
requirements: [TRED-01, TAIL-03]
requirements_addressed: [TRED-01, TAIL-03]
autonomous: true
must_haves:
  truths:
    - "Runtime-critical migrated undo/editor paths no longer depend on removable temporary thin-entrypoint glue (TRED-01)."
    - "Any retained glue is explicitly justified and documented for carry-over only when safety requires it (D-05, D-06)."
    - "Glue burn-down remains aggressive but does not introduce runtime regressions in active paths (D-04, D-06)."
  artifacts:
    - path: "src/math3d.h"
      provides: "Reduced temporary glue surface aligned to active consumers."
      contains: "deprecated"
    - path: ".planning/phases/08-undo-editor-utility-migration-and-glue-burn-down/evidence/glue-inventory.md"
      provides: "Removed/retained/deferred glue ledger with rationale and impact."
      contains: "Removed | Retained | Deferred"
    - path: "docs/QUICKSTART.md"
      provides: "Phase 8 glue-burn and focused validation runbook updates."
      contains: "Phase 8"
  key_links:
    - from: "src/gizmo/gizmo_vertex_mode.h"
      to: "src/math3d.h"
      via: "legacy include/call audit"
      pattern: "math3d\\.h|mat4_mul_point|vec3_add"
    - from: "evidence/glue-inventory.md"
      to: "runtime source files"
      via: "non-consumer proof (grep references)"
      pattern: "rg -n"
---

<objective>
Aggressively remove safe temporary thin-entrypoint glue from runtime-critical migrated undo/editor paths and document any justified carry-over.

Purpose: Satisfy TRED-01 while preserving Phase 8 runtime safety and migration parity.
Output: Reduced glue surface, updated call-site boundaries, and auditable glue inventory evidence.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-undo-editor-utility-migration-and-glue-burn-down/08-CONTEXT.md
@.planning/phases/08-undo-editor-utility-migration-and-glue-burn-down/08-RESEARCH.md
@.planning/phases/08-undo-editor-utility-migration-and-glue-burn-down/08-01-SUMMARY.md
@src/math3d.h
@src/undo_redo_exec.h
@src/gizmo/gizmo_vertex_mode.h
@src/ui/ui_scene_hierarchy.h
@docs/QUICKSTART.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and remove proven-safe runtime glue in Phase 8 boundaries (TRED-01)</name>
  <read_first>
    - src/math3d.h
    - src/undo_redo_exec.h
    - src/gizmo/gizmo_vertex_mode.h
    - src/ui/ui_scene_hierarchy.h
    - .planning/phases/08-undo-editor-utility-migration-and-glue-burn-down/08-RESEARCH.md
  </read_first>
  <files>src/math3d.h, src/undo_redo_exec.h, src/gizmo/gizmo_vertex_mode.h, src/ui/ui_scene_hierarchy.h</files>
  <action>Perform a repo-level consumer audit for temporary migration glue referenced by Phase 8 runtime-critical paths, then remove glue entries that have no required runtime consumers (per D-04 and D-06). Keep edits scoped to thin-entrypoint cleanup and boundary updates only; do not modify undo/editor features or UI behavior (D-10). Where removal is unsafe in-phase, keep the symbol and tag with `Phase8 deferred glue:` rationale to support explicit carry-over (D-05).</action>
  <verify>
    <automated>rg -n "math3d\\.h|mat4_mul_point|\\bvec3_add\\b|ray_from_screen|ray_plane_intersect|ray_axis_closest_t" src/undo_redo_exec.h src/gizmo/gizmo_vertex_mode.h src/ui/ui_scene_hierarchy.h && cmake --build build-vulkan --config Release --target mdcad_math_harness</automated>
  </verify>
  <acceptance_criteria>
    - Runtime files in scope no longer reference removed glue symbols (`rg` output reflects migrated helpers only).
    - `rg -n "Phase8 deferred glue:" src/math3d.h` exists only for retained items with explicit rationale.
    - Build command completes successfully after glue removal.
  </acceptance_criteria>
  <done>Safe glue is removed from runtime-critical scoped boundaries and any retained glue is explicitly justified in code comments.</done>
</task>

<task type="auto">
  <name>Task 2: Produce glue inventory evidence and update execution runbook (TRED-01, TAIL-03)</name>
  <read_first>
    - .planning/phases/08-undo-editor-utility-migration-and-glue-burn-down/08-VALIDATION.md
    - docs/QUICKSTART.md
    - src/math3d.h
    - src/undo_redo_exec.h
    - src/gizmo/gizmo_vertex_mode.h
  </read_first>
  <files>.planning/phases/08-undo-editor-utility-migration-and-glue-burn-down/evidence/glue-inventory.md, docs/QUICKSTART.md</files>
  <action>Create/update `.planning/phases/08-undo-editor-utility-migration-and-glue-burn-down/evidence/glue-inventory.md` with three explicit sections: **Removed**, **Retained**, **Deferred-to-Phase-9**. For each item include symbol/file, runtime consumer proof command (`rg -n ...`), and rationale/impact (per D-05, D-06, D-09). Update `docs/QUICKSTART.md` Phase 8 section with exact compile command and focused workflow references used by validation handoff; keep updates procedural only (no new capability docs) per D-10.</action>
  <verify>
    <automated>rg -n "Removed|Retained|Deferred-to-Phase-9|rg -n" .planning/phases/08-undo-editor-utility-migration-and-glue-burn-down/evidence/glue-inventory.md && rg -n "Phase 8|mdcad_math_harness|math-validation|undo/redo transform|gizmo vertex|inspector" docs/QUICKSTART.md</automated>
  </verify>
  <acceptance_criteria>
    - Glue inventory file exists and contains all three required sections with concrete symbol-level rows.
    - Every deferred item includes explicit reason + impact + Phase 9 carry-over note.
    - QUICKSTART contains Phase 8 commands/workflow references matching validation contract.
  </acceptance_criteria>
  <done>Glue burn-down decisions are auditable and handoff-ready for parity validation in 08-03.</done>
</task>

</tasks>

<verification>
Run grep-based non-consumer proof and compile gate; ensure evidence artifact can be used directly by 08-03 without re-auditing.
</verification>

<success_criteria>
- TRED-01 is satisfied for Phase 8 runtime-critical migrated surfaces through proven glue removal.
- Any unresolved glue is explicitly documented for Phase 9 carry-over with rationale and impact.
- Validation runbook/evidence are prepared for mandatory workflow checks.
</success_criteria>

<output>
After completion, create `.planning/phases/08-undo-editor-utility-migration-and-glue-burn-down/08-02-SUMMARY.md`
</output>
