---
phase: 08-undo-editor-utility-migration-and-glue-burn-down
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/math/math_undo_editor.h
  - src/undo_redo_exec.h
  - src/gizmo/gizmo_vertex_mode.h
  - src/ui/ui_entity_inspector.h
requirements: [TAIL-03]
requirements_addressed: [TAIL-03]
autonomous: true
must_haves:
  truths:
    - "Undo/redo transform and editor utility math paths in scope use cglm-backed helpers (TAIL-03)."
    - "Correctness-first migration is implemented in scoped undo/editor surfaces only, with explicit delta markers where behavior changes (D-01, D-02, D-03)."
    - "No new editor capability is added during migration (D-10)."
  artifacts:
    - path: "src/math/math_undo_editor.h"
      provides: "Thin cglm-backed helper boundary for undo/editor math touchpoints."
      contains: "mdcad_undo_editor_"
    - path: "src/undo_redo_exec.h"
      provides: "Migrated apply/unapply math touchpoints."
      contains: "undo_apply_command"
    - path: "src/gizmo/gizmo_vertex_mode.h"
      provides: "Migrated vertex center/delta math path."
      contains: "gizmo_vertex_mode_apply_delta"
    - path: "src/ui/ui_entity_inspector.h"
      provides: "Inspector edit integration with migrated helpers."
      contains: "igIsItemDeactivatedAfterEdit"
  key_links:
    - from: "src/gizmo/gizmo_vertex_mode.h"
      to: "src/math/math_undo_editor.h"
      via: "#include + helper calls"
      pattern: "mdcad_undo_editor_"
    - from: "src/undo_redo_exec.h"
      to: "src/math/math_undo_editor.h"
      via: "#include + helper calls in apply/unapply paths"
      pattern: "mdcad_undo_editor_"
---

<objective>
Migrate Phase 8 undo/editor utility math touchpoints to a thin cglm-backed helper boundary without changing undo feature scope.

Purpose: Deliver TAIL-03 migration cutline with correctness-first behavior in scoped files only.
Output: New undo/editor helper header and migrated call-sites in undo/gizmo/inspector paths.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-undo-editor-utility-migration-and-glue-burn-down/08-CONTEXT.md
@.planning/phases/08-undo-editor-utility-migration-and-glue-burn-down/08-RESEARCH.md
@src/math/cglm_entry.h
@src/math/math_interaction.h
@src/undo_redo_exec.h
@src/gizmo/gizmo_vertex_mode.h
@src/ui/ui_entity_inspector.h
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create thin undo/editor math helper boundary (TAIL-03)</name>
  <read_first>
    - src/math/cglm_entry.h
    - src/math/math_interaction.h
    - src/undo_redo_exec.h
    - src/gizmo/gizmo_vertex_mode.h
    - .planning/phases/08-undo-editor-utility-migration-and-glue-burn-down/08-CONTEXT.md
  </read_first>
  <files>src/math/math_undo_editor.h</files>
  <behavior>
    - Test 1: Helper conversions preserve vec3/mat4 bridging between legacy outward types and cglm internals.
    - Test 2: Helper names stay phase-scoped (`mdcad_undo_editor_*`) and do not create a broad compatibility facade.
  </behavior>
  <action>Create `src/math/math_undo_editor.h` as header-only `static inline` helpers that centralize undo/editor math used by vertex mode + undo execution paths, reusing `cglm_entry` conventions (per D-01 and D-03). Include only helper functions required by current touchpoints (for world/local delta and point transform composition), and explicitly avoid introducing any new editor capability or generic repo-wide facade (per D-10).</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdcad_math_harness</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "mdcad_undo_editor_" src/math/math_undo_editor.h` finds helper definitions.
    - `rg -n "static inline" src/math/math_undo_editor.h` confirms header-only helper style.
    - `rg -n "TODO|stub|unimplemented" src/math/math_undo_editor.h` returns no matches.
  </acceptance_criteria>
  <done>Thin helper boundary exists with concrete cglm-backed helpers required for Phase 8 math migration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate undo/gizmo/inspector touchpoints to helper boundary (TAIL-03)</name>
  <read_first>
    - src/math/math_undo_editor.h
    - src/undo_redo_exec.h
    - src/gizmo/gizmo_vertex_mode.h
    - src/ui/ui_entity_inspector.h
    - .planning/phases/08-undo-editor-utility-migration-and-glue-burn-down/08-RESEARCH.md
  </read_first>
  <files>src/undo_redo_exec.h, src/gizmo/gizmo_vertex_mode.h, src/ui/ui_entity_inspector.h</files>
  <behavior>
    - Test 1: Undo apply/unapply still restores transform/edit state symmetrically while using migrated helpers.
    - Test 2: Gizmo vertex center/delta path uses a single helper family and avoids mixed legacy+cglm arithmetic drift.
  </behavior>
  <action>Replace scoped legacy math touchpoints in `undo_redo_exec.h`, `gizmo_vertex_mode.h`, and inspector edit math call-sites with `mdcad_undo_editor_*` helpers (per D-01 and D-08 workflow coverage). Preserve existing undo command semantics and dirty-flag side effects (`t->dirty`, descendant dirty marking, and render instance dirties) with no command-model redesign. If a mathematically-correct behavior shift is required, annotate the exact location using `Phase8 correctness delta:` comments for later validation evidence (per D-02, D-09). Do not add new command types, UI controls, or editor behaviors (D-10).</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdcad_math_harness</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "mat4_mul_point|\\bvec3_add\\b" src/gizmo/gizmo_vertex_mode.h` returns no matches in migrated center/delta paths.
    - `rg -n "mdcad_undo_editor_" src/undo_redo_exec.h src/gizmo/gizmo_vertex_mode.h src/ui/ui_entity_inspector.h` finds active call-sites.
    - `rg -n "Phase8 correctness delta" src/undo_redo_exec.h src/gizmo/gizmo_vertex_mode.h src/ui/ui_entity_inspector.h` finds markers only where behavior changed intentionally.
  </acceptance_criteria>
  <done>All scoped Phase 8 migration touchpoints are using cglm-backed helper calls with parity-sensitive semantics preserved.</done>
</task>

</tasks>

<verification>
Run compile gate after each task and keep grep output proving helper adoption and legacy touchpoint removal in scoped files.
</verification>

<success_criteria>
- TAIL-03 in-scope undo/editor math paths are migrated to cglm-backed helper calls.
- Correctness-first behavior changes are explicitly marked for later evidence (D-02, D-09).
- No out-of-scope editor capability changes were introduced (D-10).
</success_criteria>

<output>
After completion, create `.planning/phases/08-undo-editor-utility-migration-and-glue-burn-down/08-01-SUMMARY.md`
</output>
