---
phase: 11-constraint-authoring-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/constraint_comp.h
  - src/components/constraint_participant_comp.h
  - src/components/sketch_comp.h
  - src/components/selectable_comp.h
  - src/ecs/ecs_world.h
  - src/ecs/ecs_scene.h
autonomous: true
requirements:
  - CONS-01
  - CONS-05
must_haves:
  truths:
    - "When a user creates a constraint in a sketch, it appears as part of that sketch and stays linked to the selected geometry."
    - "New sketches, sketch geometry, and constraints show readable default names (`Sketch_N`, `[type]_N`) that users can keep or edit."
    - "After users create/delete/edit constraints, participant highlights and sketch constraint totals remain correct with no stale links."
  artifacts:
    - path: "src/components/constraint_comp.h"
      provides: "Constraint entity data contract including type, value, driven, and participants."
    - path: "src/components/constraint_participant_comp.h"
      provides: "Geometry-side back-reference list for constraints."
    - path: "src/ecs/ecs_scene.h"
      provides: "Constraint lifecycle helpers and naming/counter updates."
  key_links:
    - from: "src/ecs/ecs_scene.h"
      to: "src/components/constraint_comp.h"
      via: "scene_add_constraint_* helpers"
      pattern: "ConstraintComp|scene_add_constraint"
    - from: "src/ecs/ecs_scene.h"
      to: "src/components/constraint_participant_comp.h"
      via: "bidirectional link update helpers"
      pattern: "ConstraintParticipantComp|participants"
---

<objective>
Establish Phase 11 constraint data contracts and scene helpers so all later UI/viewport workflows build on consistent ECS ownership and naming behavior.

Purpose: De-risk state coherence before UI wiring.
Output: New constraint components, registered ECS contracts, naming/counter helpers, and constraint lifecycle APIs.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-constraint-authoring-ux/11-CONTEXT.md
@.planning/phases/11-constraint-authoring-ux/11-RESEARCH.md
@.planning/phases/11-constraint-authoring-ux/11-UI-SPEC.md

<interfaces>
From src/components/sketch_comp.h:
```c
typedef struct {
    sketch_status_t status;
    vec4_t color;
    int geometry_count;
    int fixed_geometry_count;
    int constraint_count;
} SketchComp;
```

From src/ecs/ecs_world.h:
```c
static inline void ecs_world_init(ecs_world_state_t *s);
static inline uint32_t ecs_world_alloc_pick_id(ecs_world_state_t *s);
static inline void ecs_world_set_label(ecs_world_state_t *s, ecs_entity_t e, const LabelComp *label);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add constraint ECS component contracts and registration</name>
  <read_first>
    - src/components/sketch_comp.h
    - src/components/selectable_comp.h
    - src/ecs/ecs_world.h
    - .planning/phases/11-constraint-authoring-ux/11-CONTEXT.md
    - .planning/phases/11-constraint-authoring-ux/11-RESEARCH.md
  </read_first>
  <files>src/components/constraint_comp.h, src/components/constraint_participant_comp.h, src/components/selectable_comp.h, src/components/sketch_comp.h, src/ecs/ecs_world.h</files>
  <behavior>
    - Test 1: Constraint component holds constraint type + optional scalar value + driven flag + participant references (per D-02, D-05, CONS-05).
    - Test 2: Geometry entities can store back-reference IDs to applied constraints (per D-02, SKCH-04 foundation).
    - Test 3: Reserved pick-ID constants include a non-overlapping constraint glyph range (per D-03, research collision warning).
  </behavior>
  <action>Create `ConstraintComp` and `ConstraintParticipantComp` headers as first-class components per D-02, including fields for v1.2 set membership, participants, dimensional value storage, and `driven` for LENGTH/ANGLE per CONS-05 (solver semantics explicitly deferred to Phase 12). Register both components in `ecs_world_init` and expose typed getters/setters in `ecs_world.h`. Extend sketch metadata contract to track per-sketch naming counters needed for D-04 label auto-fill and update `selectable_comp.h` with dedicated constraint glyph pick-range constants that do not overlap existing entity/gizmo ranges (per D-03).</action>
  <acceptance_criteria>
    - `src/components/constraint_comp.h` and `src/components/constraint_participant_comp.h` exist and export component structs.
    - `src/ecs/ecs_world.h` includes and registers both components with component IDs and getter/setter helpers.
    - `src/components/selectable_comp.h` contains explicit constraint pick-range constants separate from `GIZMO_PICK_RESERVED_START`.
    - `src/components/sketch_comp.h` contains sketch-level counters/state needed to emit `Sketch_N`, `[type]_N` names per D-04.
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD</automated>
  </verify>
  <done>Constraint ECS contracts compile and are available for scene/UI code without using ad-hoc maps.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement sketch-owned constraint lifecycle and label auto-naming helpers</name>
  <read_first>
    - src/ecs/ecs_scene.h
    - src/components/label_comp.h
    - src/components/constraint_comp.h
    - src/components/constraint_participant_comp.h
    - .planning/phases/11-constraint-authoring-ux/11-CONTEXT.md
  </read_first>
  <files>src/ecs/ecs_scene.h</files>
  <behavior>
    - Test 1: Creating a constraint under a sketch updates sketch `constraint_count` and both direction links (constraint->geometry and geometry->constraint).
    - Test 2: Deleting/removing a constraint clears geometry back-references and keeps metadata consistent.
    - Test 3: New sketch/geometry/constraint entities receive default labels and descriptions matching D-04 naming rules.
  </behavior>
  <action>Add scene helpers in `ecs_scene.h` for constraint creation/removal/update with centralized invariant handling: parent each constraint entity to the sketch (sibling of geometry) per D-02, update `ConstraintParticipantComp` backrefs, refresh sketch counts, and provide helper(s) for dimensional fields including `driven` persistence (CONS-05 model only). Update existing `scene_add_*_to_sketch` paths to pre-fill `LabelComp` name/description for sketch, geometry, and constraints per D-04 (`Sketch_N`, `[type]_N`, description = parent sketch name). Keep operations transactional and aligned with existing undo-friendly helper style.</action>
  <acceptance_criteria>
    - `src/ecs/ecs_scene.h` exports explicit constraint add/remove helper APIs callable from UI/app workflows.
    - All add-to-sketch helpers set `LabelComp` defaults according to D-04 naming format.
    - Constraint add/remove paths update both `ConstraintComp` participants and `ConstraintParticipantComp` backrefs.
    - `SketchComp.constraint_count` updates from real sketch children rather than placeholder behavior.
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD</automated>
  </verify>
  <done>Scene layer guarantees consistent ownership, naming, and linkage for every constraint mutation entrypoint.</done>
</task>

</tasks>

<verification>
Run build after each task; confirm no compile regressions in ECS/world/scene integration.
</verification>

<success_criteria>
- Constraint entities are first-class ECS sketch children with bidirectional geometry links.
- D-04 auto-naming is implemented in scene helper creation paths.
- CONS-05 data fields exist and persist in scene contracts without introducing Phase 12 solver behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/11-constraint-authoring-ux/11-01-SUMMARY.md`
</output>

