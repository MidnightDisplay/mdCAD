---
phase: 11-constraint-authoring-ux
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - src/constraints/constraint_types.h
  - src/ui/ui_entity_inspector.h
autonomous: true
requirements:
  - CONS-01
  - SKCH-04
  - CONS-04
  - CONS-05
must_haves:
  truths:
    - "User can view constraints in ConstraintManager and filter by type/text."
    - "Selecting a constraint row highlights all participating geometry immediately."
    - "User can edit LENGTH/ANGLE values and driven state in manager and updates persist."
  artifacts:
    - path: "src/constraints/constraint_types.h"
      provides: "Constraint enum/name/legality matrix for supported v1.2 initial set."
    - path: "src/ui/ui_entity_inspector.h"
      provides: "ConstraintManager section with list/filter/select/delete/edit interactions."
  key_links:
    - from: "src/ui/ui_entity_inspector.h"
      to: "src/ecs/ecs_scene.h"
      via: "constraint row actions call scene constraint helpers"
      pattern: "scene_.*constraint"
    - from: "src/ui/ui_entity_inspector.h"
      to: "src/components/constraint_comp.h"
      via: "row value/driven editors and participant highlight"
      pattern: "ConstraintComp|driven|value"
---

<objective>
Implement inspector-driven constraint authoring/management workflows for legal constraint creation, participant highlighting, and dimensional editing.

Purpose: Deliver manager-side Phase 11 UX contract before viewport shortcut/glyph integration.
Output: Constraint legality contract and full ConstraintManager section with delete/edit/highlight behavior.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-constraint-authoring-ux/11-CONTEXT.md
@.planning/phases/11-constraint-authoring-ux/11-RESEARCH.md
@.planning/phases/11-constraint-authoring-ux/11-UI-SPEC.md
@.planning/phases/11-constraint-authoring-ux/11-01-SUMMARY.md

<interfaces>
From src/ui/ui_entity_inspector.h:
```c
static inline void ui_entity_inspector_draw_single(ui_entity_inspector_state_t *state, ecs_entity_t e);
```

From planned 11-01 contracts:
```c
typedef struct ConstraintComp ConstraintComp;
typedef struct ConstraintParticipantComp ConstraintParticipantComp;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define v1.2 constraint type set and legality matrix contract</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/11-constraint-authoring-ux/11-CONTEXT.md
    - .planning/phases/11-constraint-authoring-ux/11-RESEARCH.md
    - src/components/geometry_comp.h
  </read_first>
  <files>src/constraints/constraint_types.h</files>
  <behavior>
    - Test 1: Header exposes enum/constants for all CONS-01 required types (FIXED..TANGENTIAL).
    - Test 2: Helper API can answer applicability for a selection signature (geometry types/count and optional sub-entity role).
    - Test 3: Dimensional types are identified as LENGTH/ANGLE for shared UI edit handling.
  </behavior>
  <action>Create `src/constraints/constraint_types.h` with an explicit v1.2 constraint type enum and utility helpers: type name, dimensional predicate, and legality checks for entity/sub-entity combinations required by CONS-01. Encode the legal set exactly as the requirement list (`FIXED`, `COINCIDENT`, `COLLINEAR`, `PARALLEL`, `PERPENDICULAR`, `ALONG X`, `ALONG Y`, `ALONG Z`, `CORADIAL`, `CONCENTRIC`, `LENGTH`, `ANGLE`, `TANGENTIAL`) and do not introduce non-v1.2 extras.</action>
  <acceptance_criteria>
    - `src/constraints/constraint_types.h` exists and enumerates all 13 CONS-01 types.
    - Header exposes legality helper(s) callable by both C-key popup and ConstraintManager creation flow.
    - Header exposes a helper to detect dimensional constraints for shared value/driven UI behavior.
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD</automated>
  </verify>
  <done>Constraint applicability is centralized and reusable rather than duplicated in multiple UI paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ConstraintManager inspector section with filter/select/delete/edit workflows</name>
  <read_first>
    - src/ui/ui_entity_inspector.h
    - src/constraints/constraint_types.h
    - src/ecs/ecs_scene.h
    - .planning/phases/11-constraint-authoring-ux/11-CONTEXT.md
    - .planning/phases/11-constraint-authoring-ux/11-UI-SPEC.md
  </read_first>
  <files>src/ui/ui_entity_inspector.h</files>
  <behavior>
    - Test 1: ConstraintManager section appears after GeometryManager and lists constraints with type/value/participants.
    - Test 2: Selecting a row marks all participant geometry as selected/highlighted (SKCH-04).
    - Test 3: LENGTH/ANGLE row editing and driven toggle mutate source of truth and mirror immediately in the row.
  </behavior>
  <action>Extend inspector composition to insert `ConstraintManager` after `GeometryManager` per D-04 order. Implement flat constraint list rows with type, label, participant names, and dimensional value display; add filter controls (type dropdown + substring search on name/description) per D-04 and UI-SPEC spacing/copy rules. Wire row select to highlight all participants (SKCH-04) by syncing selected tags/selection buffer. Add Delete-only row action with UI-SPEC destructive confirmation copy. For LENGTH/ANGLE rows, add editable value and `Driven` toggle bound to `ConstraintComp` so manager-side edits satisfy CONS-04 and CONS-05 model behavior (solver-effect deferred). Use scene helper APIs from 11-01 for all mutations, not ad-hoc component edits.</action>
  <acceptance_criteria>
    - Inspector order is `Label -> Transform -> SketchManager -> GeometryManager -> ConstraintManager`.
    - ConstraintManager supports both type dropdown and text search filtering.
    - Row selection highlights all participant geometry entities immediately (SKCH-04).
    - Delete confirmation text matches UI-SPEC destructive copy contract.
    - LENGTH/ANGLE values and `Driven` state are editable in manager and persisted in `ConstraintComp`.
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD</automated>
  </verify>
  <done>Manager-driven authoring/inspection/editing/deletion flows satisfy CONS-01/SKCH-04/CONS-04/CONS-05 contracts for inspector path.</done>
</task>

</tasks>

<verification>
After automated commands pass, ensure no compiler warnings/errors from new inspector and constraint type modules.
</verification>

<success_criteria>
- ConstraintManager is present in required inspector order with list/filter/select/delete behavior.
- Participant highlight wiring is functional from constraint row selection.
- Manager-side LENGTH/ANGLE + Driven edits are persisted and visible.
</success_criteria>

<output>
After completion, create `.planning/phases/11-constraint-authoring-ux/11-02-SUMMARY.md`
</output>

