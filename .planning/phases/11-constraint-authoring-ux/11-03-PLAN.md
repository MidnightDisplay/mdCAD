---
phase: 11-constraint-authoring-ux
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - src/constraints/constraint_glyphs.h
  - src/app.c
  - src/ui/ui_entity_inspector.h
autonomous: false
requirements:
  - CONS-02
  - CONS-03
  - CONS-04
must_haves:
  truths:
    - "Pressing C opens a cursor-anchored applicable-only constraint menu and applies one constraint then closes."
    - "Constraint glyphs remain constant-size pickable overlays across zoom and support hover/select."
    - "Double-clicking LENGTH/ANGLE glyph opens popup with Accept/Cancel/Escape and mirrored value updates."
  artifacts:
    - path: "src/constraints/constraint_glyphs.h"
      provides: "Constraint glyph overlay populate/hover/select and dimension popup anchor helpers."
    - path: "src/app.c"
      provides: "C-key trigger, pick pass routing, and viewport popup lifecycle wiring."
    - path: "src/ui/ui_entity_inspector.h"
      provides: "Shared mirrored value path used by manager + popup."
  key_links:
    - from: "src/app.c"
      to: "src/constraints/constraint_glyphs.h"
      via: "pick buffer populate and hover route"
      pattern: "constraint_glyphs_.*pick_buffer|hover"
    - from: "src/app.c"
      to: "src/constraints/constraint_types.h"
      via: "C-key applicable menu generation"
      pattern: "ImGuiKey_C|applicable"
---

<objective>
Deliver in-context/viewport constraint authoring interactions: C-key filtered menu, constant-size glyph picking, and dimensional popup editing.

Purpose: Complete direct manipulation UX requirements for Phase 11.
Output: App-level key/pick wiring and glyph/dimension popup behavior with human validation checkpoint.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-constraint-authoring-ux/11-CONTEXT.md
@.planning/phases/11-constraint-authoring-ux/11-RESEARCH.md
@.planning/phases/11-constraint-authoring-ux/11-UI-SPEC.md
@.planning/phases/11-constraint-authoring-ux/11-VALIDATION.md
@.planning/phases/11-constraint-authoring-ux/11-02-SUMMARY.md

<interfaces>
From src/app.c:
```c
if (igIsKeyPressed_Bool(ImGuiKey_Tab, false)) { ... }  // must remain gizmo toggle
pick_buffer_begin_frame(&state.pick_buffer);
gizmo_populate_pick_buffer(&state.gizmo, &state.pick_buffer, &state.ecs_scene);
```

From src/components/selectable_comp.h:
```c
#define GIZMO_PICK_RESERVED_START ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire C-key applicable constraint menu and one-shot apply flow</name>
  <read_first>
    - src/app.c
    - src/ui/ui_entity_inspector.h
    - src/constraints/constraint_types.h
    - .planning/phases/11-constraint-authoring-ux/11-CONTEXT.md
    - .planning/phases/11-constraint-authoring-ux/11-UI-SPEC.md
  </read_first>
  <files>src/app.c, src/ui/ui_entity_inspector.h</files>
  <behavior>
    - Test 1: Pressing `C` (not Tab) on valid sketch selection opens a cursor-positioned menu (D-01, D-06).
    - Test 2: Menu only includes applicable constraints for current selection legality (CONS-01 + CONS-02).
    - Test 3: Choosing one constraint applies it and closes menu immediately (D-06 one-shot).
  </behavior>
  <action>In `app.c` keyboard routing, add `ImGuiKey_C` trigger per D-01 while leaving existing Tab gizmo toggle unchanged. Open an ImGui popup/menu anchored to mouse cursor position per D-06 only when current selection resolves to sketch geometry/sketch entity context. Populate menu items by legality helper contract from `constraint_types.h` and apply selected constraint through scene helper APIs, then auto-close menu in the same frame (CONS-02). Reuse ConstraintManager refresh paths so new constraint appears immediately; do not implement solver execution behavior.</action>
  <acceptance_criteria>
    - `app.c` contains C-key route and retains Tab gizmo route without behavioral regression.
    - In-context menu anchor is mouse cursor position when opened.
    - Menu list is filtered to currently legal options and closes after one apply.
    - Applied constraint appears in ConstraintManager immediately after menu action.
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD</automated>
  </verify>
  <done>C-key in-context creation flow satisfies D-01/D-06 + CONS-02 without conflicting keybindings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add constraint glyph overlay picking and LENGTH/ANGLE popup editing</name>
  <read_first>
    - src/app.c
    - src/gpu/pick_buffer.h
    - src/gizmo/gizmo.h
    - src/components/selectable_comp.h
    - .planning/phases/11-constraint-authoring-ux/11-CONTEXT.md
    - .planning/phases/11-constraint-authoring-ux/11-UI-SPEC.md
  </read_first>
  <files>src/constraints/constraint_glyphs.h, src/app.c, src/ui/ui_entity_inspector.h</files>
  <behavior>
    - Test 1: Constraint glyphs register in pick buffer overlay pass with dedicated non-overlapping pick range (D-03).
    - Test 2: Hover/select through glyphs works at different zoom levels with constant-size interaction (CONS-03).
    - Test 3: Double-click on LENGTH/ANGLE glyph opens popup with InputFloat + Accept/Cancel/Escape and mirrored manager value updates (D-05, CONS-04).
  </behavior>
  <action>Create `src/constraints/constraint_glyphs.h` for glyph placement/populate/hover helpers using pick buffer overlay primitives like gizmo (per D-03). In `app.c`, call constraint glyph populate in pick pass, route hovered pick IDs via constraint range before entity path, and allow glyph click to select the corresponding constraint and highlight participants. Implement double-click detection for dimensional glyphs to open floating popup near glyph anchor with `InputFloat`, `Accept`, `Cancel`, Escape-dismiss per D-05. Ensure edits write to shared `ConstraintComp` value source and reflect in ConstraintManager row immediately (bidirectional mirror). Keep symbol glyphs screen-flat and dimension glyphs constant-size 3D-anchored per D-03.</action>
  <acceptance_criteria>
    - `src/constraints/constraint_glyphs.h` exists and exports populate/hover/select helpers.
    - Pick-ID range checks route constraint glyph IDs without collision with entity/gizmo ranges.
    - Glyph hover/select remains usable after zoom changes (constant-size interaction).
    - LENGTH/ANGLE popup has Accept/Cancel/Escape behavior and mirrored value update with manager.
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD</automated>
  </verify>
  <done>Viewport glyph and dimension-edit UX satisfy CONS-03 and CONS-04 contracts on top of existing manager data model.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Validate full Phase 11 constraint authoring UX interactions</name>
  <read_first>
    - .planning/phases/11-constraint-authoring-ux/11-VALIDATION.md
    - .planning/phases/11-constraint-authoring-ux/11-UI-SPEC.md
    - .planning/phases/11-constraint-authoring-ux/11-CONTEXT.md
  </read_first>
  <files>src/app.c, src/ui/ui_entity_inspector.h, src/constraints/constraint_glyphs.h</files>
  <action>Run the manual verification flow for C-key menu, legal filtering, glyph hover/select, participant highlighting, and LENGTH/ANGLE popup editing. Log any mismatch against D-01..D-06 and CONS-02/03/04, then return to implementation if failures are observed before approving.</action>
  <acceptance_criteria>
    - Pressing C opens applicable-only menu at cursor and menu auto-hides after apply.
    - Tab still toggles gizmo mode and does not open constraint menu.
    - Constraint glyphs are hoverable/selectable across zoom and select the expected constraint.
    - Selecting constraint via manager or glyph highlights all participating geometry.
    - Double-clicking LENGTH/ANGLE glyph opens popup with Accept/Cancel/Escape and mirrored manager value.
  </acceptance_criteria>
  <what-built>In-context C-key constraint menu, viewport glyph interaction, and dimension popup edit pipeline.</what-built>
  <how-to-verify>
    1. Build and run mdCAD.
    2. Create sketch geometry combinations (line-line, point-line, arc-line, etc.).
    3. Press `C` at cursor with different selections; confirm only legal constraints appear.
    4. Apply one option and confirm menu closes immediately and new constraint appears in manager.
    5. Verify `Tab` still toggles gizmo mode.
    6. Zoom in/out and hover/select constraint glyphs; confirm interaction remains stable and selects correct constraint.
    7. Select constraint row/glyph and confirm all participant geometry highlights.
    8. Double-click LENGTH/ANGLE glyph, edit value, Accept; verify manager row mirrors value. Repeat with Cancel and Escape to confirm dismiss behavior.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or list failing step numbers with observed behavior.</resume-signal>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD</automated>
  </verify>
  <done>Human confirms Phase 11 interaction contract works end-to-end for CONS-02/03/04.</done>
</task>

</tasks>

<verification>
Use automated build/tests plus manual UI checklist in 11-VALIDATION for interaction-heavy behaviors.
</verification>

<success_criteria>
- C-key in-context menu behavior matches locked decisions D-01 and D-06.
- Constraint glyph overlays support reliable hover/select at constant screen size.
- LENGTH/ANGLE popup editing is mirrored with manager values and respects Accept/Cancel/Escape.
</success_criteria>

<output>
After completion, create `.planning/phases/11-constraint-authoring-ux/11-03-SUMMARY.md`
</output>

