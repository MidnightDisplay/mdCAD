---
phase: 30-deterministic-closure-gate-windows-vulkan-solver-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/solver/SOLVER_ARCHITECTURE.md
  - docs/QUICKSTART.md
  - docs/VULKAN_WINDOWS.md
autonomous: true
requirements: [SDOC-01, SDOC-02, SDOC-03]
must_haves:
  truths:
    - "Developer can follow one practical solver flow from authoring to UI feedback without guessing ownership boundaries."
    - "Developer can locate concrete file/function anchors for authoring, recalc, diagnostics, and user-facing feedback."
    - "Developer can use a TL;DR primer to start debugging unsatisfied constraints, drag rollback, and pass-policy stalls immediately."
  artifacts:
    - path: "docs/solver/SOLVER_ARCHITECTURE.md"
      provides: "Locked solver architecture doc with overview, pipeline, diagnostics flow, code anchors, references, and TL;DR debug primer."
      contains: "Overview, Solve pipeline, Diagnostics flow, Code anchors, TL;DR debug primer"
    - path: "docs/QUICKSTART.md"
      provides: "Entry-point cross-link to solver architecture documentation."
      contains: "SOLVER_ARCHITECTURE.md"
    - path: "docs/VULKAN_WINDOWS.md"
      provides: "Windows Vulkan troubleshooting/sign-off docs linking to solver debug architecture map."
      contains: "SOLVER_ARCHITECTURE.md"
  key_links:
    - from: "docs/solver/SOLVER_ARCHITECTURE.md"
      to: "src/ecs/ecs_scene.h"
      via: "stage-by-stage code anchor table"
      pattern: "scene_solver_request_auto|scene_solver_request_recalculate|scene_solver_can_apply_drag"
    - from: "docs/solver/SOLVER_ARCHITECTURE.md"
      to: "src/app.c"
      via: "UI feedback and authoring trigger mapping"
      pattern: "scene_solver_request_auto|diagnostic|status"
    - from: "docs/QUICKSTART.md"
      to: "docs/solver/SOLVER_ARCHITECTURE.md"
      via: "debugging/reference hyperlink"
      pattern: "SOLVER_ARCHITECTURE.md"
---

<objective>
Publish practical, code-anchored solver architecture documentation and wire it into existing docs so future debugging starts from one canonical guide.

Purpose: Close SDOC-01/02/03 with a deterministic, implementation-grounded guide instead of theory-only notes.
Output: New `docs/solver/SOLVER_ARCHITECTURE.md` plus quickstart/Vulkan cross-links.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/30-deterministic-closure-gate-windows-vulkan-solver-docs/30-CONTEXT.md
@.planning/phases/30-deterministic-closure-gate-windows-vulkan-solver-docs/30-RESEARCH.md
@src/ecs/ecs_scene.h
@src/app.c
@src/constraints/constraint_types.h
@docs/QUICKSTART.md
@docs/VULKAN_WINDOWS.md

<interfaces>
From src/ecs/ecs_scene.h:
```c
static inline bool scene_solver_request_auto(ecs_scene_t *scene, ecs_entity_t sketch);
static inline void scene_solver_process_auto_queue(ecs_scene_t *scene);
static inline bool scene_solver_request_recalculate(ecs_scene_t *scene, ecs_entity_t sketch);
static inline bool scene_solver_can_apply_drag(..., scene_solver_drag_decision_t *out_decision);
static inline bool scene_solver_add_diagnostic(...);
```

From src/app.c:
```c
// App-level orchestration triggers solver recalc and surfaces user-facing status/diagnostics.
```

From src/constraints/constraint_types.h:
```c
// Constraint family/legality definitions used by authoring/runtime contracts.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author locked solver architecture doc with practical code anchors</name>
  <read_first>@.planning/phases/30-deterministic-closure-gate-windows-vulkan-solver-docs/30-CONTEXT.md, @.planning/phases/30-deterministic-closure-gate-windows-vulkan-solver-docs/30-RESEARCH.md, @src/ecs/ecs_scene.h, @src/app.c, @src/constraints/constraint_types.h</read_first>
  <files>docs/solver/SOLVER_ARCHITECTURE.md</files>
  <action>Create `docs/solver/SOLVER_ARCHITECTURE.md` per D-08 with locked section order per D-09: `Overview -> Solve pipeline -> Diagnostics flow -> Code anchors -> TL;DR debug primer`. In the Code anchors section, add stage-by-stage file/function mapping per D-10 for authoring, recalculate, diagnostics, and UI feedback. Add a curated references section with 3-6 practical sources and one-line "why this helps mdCAD debugging" rationale per D-11 and D-12. In TL;DR, include explicit "first place to look" bullets for unsatisfied constraints, drag rollback behavior, and pass-policy convergence stalls per D-13. Keep scope doc-only; do not add solver capabilities (deferred).</action>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path docs/solver/SOLVER_ARCHITECTURE.md -Pattern '^## Overview$','^## Solve pipeline$','^## Diagnostics flow$','^## Code anchors$','^## TL;DR debug primer$','unsatisfied constraints','drag rollback','pass-policy','src/ecs/ecs_scene.h','src/app.c','src/constraints/constraint_types.h' | Measure-Object | % Count"</automated>
  </verify>
  <acceptance_criteria>`docs/solver/SOLVER_ARCHITECTURE.md` exists; grep shows all five locked sections in order, includes at least 3 references with rationale lines, and contains all three D-13 debug-entry phrases.</acceptance_criteria>
  <done>SDOC-01/02/03 content contract is fully present in a single practical architecture document.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-link solver architecture doc from existing runbook docs</name>
  <read_first>@docs/QUICKSTART.md, @docs/VULKAN_WINDOWS.md, @docs/solver/SOLVER_ARCHITECTURE.md, @.planning/phases/30-deterministic-closure-gate-windows-vulkan-solver-docs/30-CONTEXT.md</read_first>
  <files>docs/QUICKSTART.md, docs/VULKAN_WINDOWS.md</files>
  <action>Add concise cross-links to `docs/solver/SOLVER_ARCHITECTURE.md` in both `docs/QUICKSTART.md` and `docs/VULKAN_WINDOWS.md` so debug/runbook readers can jump directly to the architecture guide. Keep Windows Vulkan sign-off scope explicit per D-07 and avoid introducing cross-platform closure guidance (deferred). Reference that the linked doc contains authoring/recalc/diagnostics/UI flow anchors per D-10.</action>
  <verify>
    <automated>powershell -NoProfile -Command "Select-String -Path docs/QUICKSTART.md,docs/VULKAN_WINDOWS.md -Pattern 'SOLVER_ARCHITECTURE.md' | Measure-Object | % Count"</automated>
  </verify>
  <acceptance_criteria>Both docs contain exactly one discoverable link/reference to `docs/solver/SOLVER_ARCHITECTURE.md`, and no new cross-platform closure claims are added.</acceptance_criteria>
  <done>Solver architecture guidance is discoverable from standard quickstart and Windows Vulkan docs.</done>
</task>

</tasks>

<verification>
Run:
- `powershell -NoProfile -Command "Select-String -Path docs/solver/SOLVER_ARCHITECTURE.md -Pattern '^## Overview$','^## Solve pipeline$','^## Diagnostics flow$','^## Code anchors$','^## TL;DR debug primer$','unsatisfied constraints','drag rollback','pass-policy','src/ecs/ecs_scene.h','src/app.c','src/constraints/constraint_types.h'"`
- `powershell -NoProfile -Command "Select-String -Path docs/QUICKSTART.md,docs/VULKAN_WINDOWS.md -Pattern 'SOLVER_ARCHITECTURE.md'"`
</verification>

<success_criteria>
- SDOC-01: Architecture overview clearly maps authoring -> solve -> diagnostics -> UI feedback.
- SDOC-02: Stage-by-stage code anchors and curated practical references are present.
- SDOC-03: TL;DR debug primer gives concrete first-debug entry points for all locked failure families.
</success_criteria>

<output>
After completion, create `.planning/phases/30-deterministic-closure-gate-windows-vulkan-solver-docs/30-01-SUMMARY.md`
</output>

