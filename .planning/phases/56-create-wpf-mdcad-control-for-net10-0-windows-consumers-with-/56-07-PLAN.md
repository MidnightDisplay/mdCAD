---
phase: 56-create-wpf-mdcad-control-for-net10-0-windows-consumers-with-
plan: 07
type: execute
wave: 5
depends_on:
  - 56-06
files_modified:
  - samples/wpf-host/WpfHost.csproj
  - samples/wpf-host/App.xaml
  - samples/wpf-host/App.xaml.cs
  - samples/wpf-host/MainWindow.xaml
  - samples/wpf-host/MainWindow.xaml.cs
autonomous: true
requirements:
  - P56-03
must_haves:
  truths:
    - "The repo contains a full diagnostic WPF host that mirrors the Avalonia lifecycle/status harness."
    - "The full host carries a bundled proof JSONL asset into output instead of relying on repo-relative paths."
  artifacts:
    - path: "samples/wpf-host/MainWindow.xaml"
      provides: "Diagnostic WPF harness UI outside the native viewer region."
      contains: "StartAsync"
    - path: "samples/wpf-host/MainWindow.xaml.cs"
      provides: "Host-owned lifecycle and runtime-status logic."
      contains: "mdcad-runtime"
  key_links:
    - from: "samples/wpf-host/WpfHost.csproj"
      to: "samples/wpf-mdcad-control/MdCad.Wpf.Control.csproj"
      via: "diagnostic host consumes the reusable WPF control by project reference"
      pattern: "ProjectReference"
---

<objective>
Add the full diagnostic WPF proof host.

Purpose: Deliver D-08, D-09, and D-10 as a dedicated lifecycle/status harness that stays truthful about the Windows-only runtime seam.
Output: `samples/wpf-host` with the rich operator surface and bundled proof-JSONL content-link.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/56-create-wpf-mdcad-control-for-net10-0-windows-consumers-with-/56-CONTEXT.md
@.planning/phases/56-create-wpf-mdcad-control-for-net10-0-windows-consumers-with-/56-RESEARCH.md
@.planning/phases/56-create-wpf-mdcad-control-for-net10-0-windows-consumers-with-/56-06-PLAN.md
@samples/avalonia-host/MainWindow.axaml
@samples/avalonia-host/MainWindow.axaml.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the rich WPF lifecycle/status harness with bundled proof JSONL</name>
  <files>samples/wpf-host/WpfHost.csproj, samples/wpf-host/App.xaml, samples/wpf-host/App.xaml.cs, samples/wpf-host/MainWindow.xaml, samples/wpf-host/MainWindow.xaml.cs</files>
  <behavior>
    - Test 1: the full WPF host exposes presentation mode, auto-start, live-refresh, viewport-only, JSONL scenario selection, and explicit `StartAsync()` / `StopAsync()`.
    - Test 2: runtime-status and harness-status text stay host-owned and truthful about the external-process boundary.
    - Test 3: the host links/copies the committed proof JSONL asset into output so scenario selection is deployment-rooted rather than repo-rooted.
  </behavior>
  <action>Per D-08, D-09, and D-10, create `samples/wpf-host/` as the rich diagnostic consumer of `MdCad.Wpf.Control`. Mirror the Avalonia harness semantics closely: operator controls, explicit `StartAsync()` / `StopAsync()` buttons, runtime-status text based on `AppContext.BaseDirectory\mdcad-runtime`, and host-owned harness status that never pretends to inspect viewer-side IPC. Explicitly content-link/copy the committed `sample-host-proof.jsonl` proof asset into output so the scenario picker uses bundled content. Keep all chrome outside the hosted HWND rectangle so WPF airspace limits stay truthful.</action>
  <verify>
    <automated>dotnet build .\samples\wpf-host\WpfHost.csproj -c Release</automated>
  </verify>
  <done>The repo has a full WPF diagnostic host that builds, carries bundled proof input, and preserves truthful host-owned lifecycle/runtime messaging.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sample-host UI → reusable control | Host toggles and selected proof inputs become launch snapshots and relaunch requests. |
| Host status text → human operator | The harness must stay truthful about what the host knows versus what mdCAD owns. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-10 | Spoofing | WPF host status lines | mitigate | Keep runtime/harness text host-owned and mirror the existing Avalonia wording instead of claiming viewer-side insight. |
| T-56-11 | Tampering | proof JSONL content-link | mitigate | Link the committed proof JSONL asset into host output so the scenario picker stays deterministic and deployment-rooted. |
| T-56-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs occur in this plan. |
</threat_model>

<verification>
- `dotnet build .\samples\wpf-host\WpfHost.csproj -c Release`
</verification>

<success_criteria>
- The full WPF host builds as the diagnostic proof harness.
- Bundled proof JSONL content is linked into output and host-owned runtime/lifecycle status remains explicit.
</success_criteria>

<output>
Create `.planning/phases/56-create-wpf-mdcad-control-for-net10-0-windows-consumers-with-/56-07-SUMMARY.md` when done
</output>
