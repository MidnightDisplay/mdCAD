---
phase: 56-create-wpf-mdcad-control-for-net10-0-windows-consumers-with-
plan: 06
type: execute
wave: 4
depends_on:
  - 56-03
  - 56-05
files_modified:
  - samples/wpf-mdcad-control/MdCad.Wpf.Control.csproj
  - samples/wpf-mdcad-control/MdCadEmbeddedControl.xaml
  - samples/wpf-mdcad-control/MdCadEmbeddedControl.xaml.cs
  - samples/wpf-mdcad-control/Host/IMdCadEmbedBackend.cs
  - samples/wpf-mdcad-control/Host/MdCadEmbedBackendFactory.cs
  - samples/wpf-mdcad-control/Host/Windows/MdCadHwndHost.cs
  - samples/wpf-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs
  - samples/wpf-mdcad-control.tests/MdCadEmbeddedControlTests.cs
  - samples/wpf-mdcad-control.tests/WindowsMdCadEmbedBackendTests.cs
autonomous: true
requirements:
  - P56-01
  - P56-02
must_haves:
  truths:
    - "The WPF control still embeds mdCAD only through the Windows external-process child-HWND seam."
    - "The WPF control shell, shared coordinator, and Windows backend are explicitly wired together rather than left as separate scaffolds."
    - "Runtime payload/tooling remain single-sourced from the existing canonical bundle and helper."
  artifacts:
    - path: "samples/wpf-mdcad-control/Host/Windows/MdCadHwndHost.cs"
      provides: "WPF placeholder HWND host."
      contains: "BuildWindowCore"
    - path: "samples/wpf-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs"
      provides: "WPF backend preserving external-process attach/resize/teardown behavior."
      contains: "MdCadWindowsStartInfoBuilder"
    - path: "samples/wpf-mdcad-control/MdCadEmbeddedControl.xaml.cs"
      provides: "WPF control shell wired to the shared coordinator and backend factory."
      contains: "StartAsync"
  key_links:
    - from: "samples/wpf-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs"
      to: "samples/mdcad-embed-core/Windows/MdCadWindowsStartInfoBuilder.cs"
      via: "WPF backend reuses the shared Windows start-info builder"
      pattern: "MdCadWindowsStartInfoBuilder"
    - from: "samples/wpf-mdcad-control/MdCadEmbeddedControl.xaml.cs"
      to: "samples/wpf-mdcad-control/Host/MdCadEmbedBackendFactory.cs"
      via: "Control shell creates and drives the backend through the WPF factory/coordinator seam"
      pattern: "MdCadEmbedBackendFactory"
    - from: "samples/wpf-mdcad-control/MdCad.Wpf.Control.csproj"
      to: "samples/avalonia-mdcad-control/runtime/win-x64"
      via: "WPF project links the same canonical runtime payload into output"
      pattern: "mdcad-runtime"
---

<objective>
Implement the WPF backend and single-source runtime packaging.

Purpose: Deliver D-05, D-06, and D-07 without broadening the runtime contract or creating a second runtime toolchain.
Output: `HwndHost` seam, WPF control-to-backend wiring, and runtime-copy/project wiring that points at the canonical Avalonia-managed payload/helper.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/56-create-wpf-mdcad-control-for-net10-0-windows-consumers-with-/56-CONTEXT.md
@.planning/phases/56-create-wpf-mdcad-control-for-net10-0-windows-consumers-with-/56-RESEARCH.md
@.planning/phases/56-create-wpf-mdcad-control-for-net10-0-windows-consumers-with-/56-03-PLAN.md
@.planning/phases/56-create-wpf-mdcad-control-for-net10-0-windows-consumers-with-/56-05-PLAN.md
@samples/avalonia-mdcad-control/Host/Windows/EmbedNativeControlHost.cs
@samples/avalonia-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs
@samples/avalonia-mdcad-control/MdCad.Avalonia.Control.csproj
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the WPF control shell to the `HwndHost` backend and canonical runtime-copy flow</name>
  <files>samples/wpf-mdcad-control/MdCad.Wpf.Control.csproj, samples/wpf-mdcad-control/MdCadEmbeddedControl.xaml, samples/wpf-mdcad-control/MdCadEmbeddedControl.xaml.cs, samples/wpf-mdcad-control/Host/IMdCadEmbedBackend.cs, samples/wpf-mdcad-control/Host/MdCadEmbedBackendFactory.cs, samples/wpf-mdcad-control/Host/Windows/MdCadHwndHost.cs, samples/wpf-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs, samples/wpf-mdcad-control.tests/MdCadEmbeddedControlTests.cs, samples/wpf-mdcad-control.tests/WindowsMdCadEmbedBackendTests.cs</files>
  <behavior>
    - Test 1: the WPF control’s `StartAsync()` / `StopAsync()` and launch-affecting property changes now drive the shared coordinator and the real WPF backend instead of a backend-free scaffold.
    - Test 2: the WPF backend uses a real `HwndHost` placeholder and preserves attach/resize/teardown against an external child HWND.
    - Test 3: the WPF backend consumes the shared Windows start-info builder so arg order and working-directory rules stay identical to Avalonia.
    - Test 4: the WPF project links the existing canonical `mdcad-runtime/**` payload and reuses the existing runtime-refresh helper instead of introducing a second runtime source.
  </behavior>
  <action>Per D-01 through D-07, finish the first working WPF control slice on top of the scaffold from 56-05: add `MdCadHwndHost`, the backend interfaces/factory, and `WindowsMdCadEmbedBackend`, then update `MdCadEmbeddedControl.xaml(.cs)` so the public dependency properties, `StartAsync()`, `StopAsync()`, and launch-affecting changes all flow through the shared coordinator into the WPF backend/factory seam. Wire the WPF project file to copy the same canonical `mdcad-runtime` content and optional refresh helper already used by `samples/avalonia-mdcad-control`, and extend the WPF control tests so they prove both the control-shell wiring and the backend contract. Keep warning/diagnostic chrome outside the hosted HWND region and do not add any IPC or in-process embedding behavior.</action>
  <verify>
    <automated>dotnet test .\samples\wpf-mdcad-control.tests\MdCad.Wpf.Control.Tests.csproj -c Release --filter "FullyQualifiedName~MdCadEmbeddedControlTests|FullyQualifiedName~WindowsMdCadEmbedBackendTests" && dotnet build .\samples\wpf-mdcad-control\MdCad.Wpf.Control.csproj -c Release</automated>
  </verify>
  <done>The WPF control shell is now actually wired to a real Windows backend, preserving the external-process child-HWND truth and canonical runtime-copy/tooling contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WPF backend → external mdCAD process | Launch args, HWND parenting, and teardown logic define the real runtime boundary. |
| Control project → copied runtime payload | MSBuild content links decide which committed runtime files land in consumer output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-56-08 | Elevation of Privilege | `MdCadHwndHost` / Win32 seam | mitigate | Keep the proven child-window model only; do not add message interception or focus-forcing behavior. |
| T-56-09 | Tampering | WPF runtime payload copy contract | mitigate | Reuse the existing canonical runtime folder/helper and prove the WPF project builds against that exact source. |
| T-56-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs occur in this plan. |
</threat_model>

<verification>
- `dotnet test .\samples\wpf-mdcad-control.tests\MdCad.Wpf.Control.Tests.csproj -c Release --filter "FullyQualifiedName~MdCadEmbeddedControlTests|FullyQualifiedName~WindowsMdCadEmbedBackendTests"`
- `dotnet build .\samples\wpf-mdcad-control\MdCad.Wpf.Control.csproj -c Release`
</verification>

<success_criteria>
- The WPF control shell and backend are wired end-to-end through the shared coordinator and the Windows `HwndHost` + external-process child-HWND seam.
- Runtime payload copy and refresh tooling remain single-sourced from the existing canonical flow.
</success_criteria>

<output>
Create `.planning/phases/56-create-wpf-mdcad-control-for-net10-0-windows-consumers-with-/56-06-SUMMARY.md` when done
</output>
