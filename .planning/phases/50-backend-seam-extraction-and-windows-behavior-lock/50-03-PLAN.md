---
phase: 50-backend-seam-extraction-and-windows-behavior-lock
plan: 03
type: execute
wave: 3
depends_on:
  - 50-02
files_modified:
  - .planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-MANUAL-CHECKLIST.md
  - samples/avalonia-mdcad-control.tests/MdCadSessionCoordinatorTests.cs
  - samples/avalonia-mdcad-control.tests/WindowsMdCadEmbedBackendTests.cs
autonomous: false
requirements:
  - WPRS-01
  - WPRS-02
must_haves:
  truths:
    - "The existing Windows diagnostic host remains the authoritative proof surface for launch, attach, stop, relaunch, resize, and sealed/diagnostic equivalence."
    - "Automated tests and the manual checklist both prove that the extracted backend did not change shipped Windows behavior."
    - "Phase 50 closes without absorbing unsupported-platform behavior or plain-`net10.0` compatibility scope."
  artifacts:
    - path: "samples/avalonia-mdcad-control.tests/WindowsMdCadEmbedBackendTests.cs"
      provides: "Final backend-specific regression lock after the seam is fully wired."
      contains: "RecreateSurfaceAsync"
    - path: "samples/avalonia-mdcad-control.tests/MdCadSessionCoordinatorTests.cs"
      provides: "Lifecycle gate proof for attach/stop/relaunch ordering across the new backend seam."
      contains: "StopAsync"
    - path: ".planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-MANUAL-CHECKLIST.md"
      provides: "Recorded Windows host proof for attach, stop, relaunch, resize, and sealed/diagnostic behavior."
      contains: "PASS"
  key_links:
    - from: "samples/avalonia-mdcad-control.tests/MdCadSessionCoordinatorTests.cs"
      to: "samples/avalonia-mdcad-control/Host/MdCadSessionCoordinator.cs"
      via: "coordinator lifecycle assertions remain green after backend extraction"
      pattern: "RecreateCount|StopCount|StartedSnapshots"
    - from: ".planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-MANUAL-CHECKLIST.md"
      to: "samples/avalonia-host/MainWindow.axaml.cs"
      via: "authoritative Windows harness execution with explicit steps and recorded outcomes"
      pattern: "AvaloniaHost.csproj|sealed|diagnostic|resize"
---

<objective>
Re-close Windows regression proof on top of the extracted backend seam and record authoritative harness evidence without changing product behavior.

Purpose: Phase 50 is only complete when WPRS-01 and WPRS-02 are re-proven against the existing diagnostic host. This plan closes the loop with final automated assertions plus a blocking human verification checkpoint.
Output: Finalized backend/coordinator regression tests, a completed `50-MANUAL-CHECKLIST.md`, and explicit Windows host proof results.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-CONTEXT.md
@.planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-RESEARCH.md
@.planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-VALIDATION.md
@.planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-01-SUMMARY.md
@.planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-02-SUMMARY.md
@samples/avalonia-host/MainWindow.axaml.cs
@samples/avalonia-mdcad-control.tests/MdCadSessionCoordinatorTests.cs
@samples/avalonia-mdcad-control.tests/WindowsMdCadEmbedBackendTests.cs

<interfaces>
From samples/avalonia-mdcad-control.tests/MdCadSessionCoordinatorTests.cs:
```csharp
await coordinator.StartAsync();
await coordinator.StopAsync();
await coordinator.RequestReconcileAsync();
Assert.Equal(1, harness.RecreateCount);
Assert.Equal(harness.Snapshot, harness.StartedSnapshots[^1]);
```

From samples/avalonia-host/MainWindow.axaml.cs:
```csharp
_embeddedControl.PresentationMode = GetSelectedPresentationMode();
_embeddedControl.AutoStart = _autoStartCheckBox.IsChecked == true;
_embeddedControl.StartupLiveRefreshEnabled = _liveRefreshCheckBox.IsChecked == true;
_embeddedControl.JsonlPath = _currentJsonlPath;
await _embeddedControl.StartAsync();
await _embeddedControl.StopAsync();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalize automated regression lock for the extracted backend seam</name>
  <read_first>
    - .planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-02-SUMMARY.md
    - samples/avalonia-mdcad-control.tests/WindowsMdCadEmbedBackendTests.cs
    - samples/avalonia-mdcad-control.tests/MdCadSessionCoordinatorTests.cs
    - .planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-VALIDATION.md
  </read_first>
  <files>samples/avalonia-mdcad-control.tests/WindowsMdCadEmbedBackendTests.cs, samples/avalonia-mdcad-control.tests/MdCadSessionCoordinatorTests.cs, .planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-MANUAL-CHECKLIST.md</files>
  <action>Close the remaining WPRS-01/WPRS-02 gaps called out in `50-VALIDATION.md`. Extend `WindowsMdCadEmbedBackendTests.cs` so the extracted backend explicitly proves placeholder recreation and relaunch-safe start-info behavior after `StopSessionAsync` / `RecreateSurfaceAsync`, and keep `MdCadSessionCoordinatorTests.cs` aligned with the backend seam so attach-stop-relaunch sequencing still uses the newest snapshot only. Finalize `50-MANUAL-CHECKLIST.md` with exact commands and expected status text for the existing diagnostic host: build the control tests, build `samples/avalonia-host`, launch the host, verify runtime bundle presence, start, stop, relaunch, resize, and switch between sealed and diagnostic modes without behavior drift. Do not add any unsupported-platform assertions or plain-`net10.0` host checks; those belong to Phases 51-54.</action>
  <verify>
    <automated>dotnet test samples/avalonia-mdcad-control.tests/MdCad.Avalonia.Control.Tests.csproj -c Release &amp;&amp; dotnet build samples/avalonia-host/AvaloniaHost.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - automated tests cover final backend relaunch-safe behavior after full seam wiring
    - `50-MANUAL-CHECKLIST.md` contains exact commands, expected status lines, and PASS/FAIL fields
    - no new checks mention unsupported-platform behavior or TFM widening
  </acceptance_criteria>
  <done>The extracted backend seam has final automated coverage and a ready-to-run manual regression artifact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the authoritative Windows diagnostic-host proof</name>
  <read_first>
    - .planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-MANUAL-CHECKLIST.md
    - .planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-VALIDATION.md
    - samples/avalonia-host/MainWindow.axaml.cs
  </read_first>
  <files>.planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-MANUAL-CHECKLIST.md</files>
  <action>Use the existing Windows diagnostic host as the regression authority per D-06 and D-07. Automate the build/test preflight first, then execute the manual checklist against `samples/avalonia-host` and record PASS/FAIL/BLOCKED results directly in `50-MANUAL-CHECKLIST.md`. Preserve the current shipped behavior only; if verification reveals unsupported-platform or net10-compatibility needs, record them as later-phase notes instead of implementing them here.</action>
  <verify>
    <automated>dotnet test samples/avalonia-mdcad-control.tests/MdCad.Avalonia.Control.Tests.csproj -c Release &amp;&amp; dotnet build samples/avalonia-host/AvaloniaHost.csproj -c Release</automated>
  </verify>
  <what-built>Final Windows behavior-lock proof for the extracted backend seam, validated through the existing diagnostic host.</what-built>
  <how-to-verify>
    1. Run `dotnet run --project samples/avalonia-host/AvaloniaHost.csproj -c Release`.
    2. In the running host, follow `50-MANUAL-CHECKLIST.md` exactly: confirm `mdcad-runtime\mdCAD.exe` is present, launch the viewer, wait for child attach, stop it, relaunch it, resize the host window, and switch between sealed and diagnostic presentation modes.
    3. Confirm the viewer still uses the current status/warning behavior and that relaunch preserves attach/stop behavior without orphaned `mdCAD.exe`.
    4. Record PASS/FAIL/BLOCKED notes in `50-MANUAL-CHECKLIST.md`, then reply with the result.
  </how-to-verify>
  <resume-signal>Reply with "approved" if the checklist passes, or provide the failing checklist item IDs and observed behavior.</resume-signal>
  <acceptance_criteria>
    - `50-MANUAL-CHECKLIST.md` records explicit outcomes for runtime bundle presence, attach, stop, relaunch, resize, and sealed/diagnostic behavior
    - automated preflight stays green before the manual proof
    - the proof surface remains `samples/avalonia-host`, not a new backend-only harness
  </acceptance_criteria>
  <done>Phase 50 is re-proven on Windows and explicitly locked against behavior drift before later phases widen platform or TFM scope.</done>
</task>

</tasks>

<verification>
- `dotnet test samples/avalonia-mdcad-control.tests/MdCad.Avalonia.Control.Tests.csproj -c Release`
- `dotnet build samples/avalonia-host/AvaloniaHost.csproj -c Release`
- Confirm `50-MANUAL-CHECKLIST.md` contains recorded PASS/FAIL/BLOCKED outcomes from the manual checkpoint.
</verification>

<success_criteria>
- Automated tests and the manual checklist both prove the extracted backend preserved WPRS-01 and WPRS-02.
- The existing Windows diagnostic host remains the sole authoritative behavior-proof surface.
- Phase 50 closes cleanly without sneaking in unsupported-platform behavior, public API changes, or TFM widening.
</success_criteria>

<output>
After completion, create `.planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-03-SUMMARY.md`
</output>
