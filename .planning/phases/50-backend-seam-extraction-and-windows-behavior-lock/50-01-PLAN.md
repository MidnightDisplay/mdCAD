---
phase: 50-backend-seam-extraction-and-windows-behavior-lock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - samples/avalonia-mdcad-control.tests/MdCad.Avalonia.Control.Tests.csproj
  - samples/avalonia-mdcad-control.tests/MdCadRuntimeResolverTests.cs
  - samples/avalonia-mdcad-control.tests/WindowsMdCadEmbedBackendTests.cs
  - samples/avalonia-mdcad-control/Host/EmbedNativeControlHost.cs
  - samples/avalonia-mdcad-control/Host/IMdCadEmbedBackend.cs
  - samples/avalonia-mdcad-control/Host/MdCadRuntimeResolver.cs
  - samples/avalonia-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs
  - samples/avalonia-mdcad-control/Host/Windows/Win32NativeMethods.cs
  - .planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-MANUAL-CHECKLIST.md
autonomous: true
requirements:
  - WPRS-01
  - WPRS-02
must_haves:
  truths:
    - "On Windows, the embedded runtime resolves from `AppContext.BaseDirectory\\mdcad-runtime\\mdCAD.exe` when the host launches mdCAD."
    - "On Windows, launching the embedded viewer still uses the copied runtime folder as the working directory and passes `--embedded --parent-hwnd` before any optional launch flags."
    - "Using the existing Windows diagnostic host, an operator can verify launch, attach, stop, relaunch, resize, and sealed-vs-diagnostic mode behavior against the current shipped experience."
  artifacts:
    - path: "samples/avalonia-mdcad-control.tests/MdCadRuntimeResolverTests.cs"
      provides: "Regression lock for the copied-runtime lookup contract."
      contains: "mdcad-runtime"
    - path: "samples/avalonia-mdcad-control.tests/WindowsMdCadEmbedBackendTests.cs"
      provides: "Regression lock for Windows backend launch-spec behavior."
      contains: "CreateStartInfo"
    - path: "samples/avalonia-mdcad-control/Host/IMdCadEmbedBackend.cs"
      provides: "Internal seam contract for later shell/backend extraction."
      contains: "interface IMdCadEmbedBackend"
    - path: ".planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-MANUAL-CHECKLIST.md"
      provides: "Authoritative Windows manual regression checklist for attach/stop/relaunch/resize/sealed-diagnostic proof."
      contains: "attach"
  key_links:
    - from: "samples/avalonia-mdcad-control/Host/IMdCadEmbedBackend.cs"
      to: "samples/avalonia-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs"
      via: "internal backend contract implemented by the Windows-only runtime owner"
      pattern: "IMdCadEmbedBackend|WindowsMdCadEmbedBackend"
    - from: "samples/avalonia-mdcad-control/Host/MdCadRuntimeResolver.cs"
      to: "samples/avalonia-mdcad-control.tests/MdCadRuntimeResolverTests.cs"
      via: "internal testable base-directory overload while preserving `Resolve()`"
      pattern: "AppContext.BaseDirectory|mdcad-runtime|mdCAD.exe"
    - from: "samples/avalonia-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs"
      to: "samples/avalonia-mdcad-control.tests/WindowsMdCadEmbedBackendTests.cs"
      via: "internal launch-spec builder pinned before lifecycle extraction"
      pattern: "WorkingDirectory|--embedded|--parent-hwnd"
    - from: ".planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-MANUAL-CHECKLIST.md"
      to: "samples/avalonia-host/MainWindow.axaml.cs"
      via: "manual proof steps stay bound to the existing Windows diagnostic harness controls"
      pattern: "StartAsync|StopAsync|PresentationMode|AutoStart"
---

<objective>
Create the Phase 50 behavior lock first: backend-specific regression proof, internal seam contracts, and a manual checklist that freezes the current Windows runtime behavior before refactor work begins.

Purpose: Per D-01, D-03, D-05, D-06, and D-07, this phase must be a behavior-preserving refactor. Wave 0 proof comes first so later backend extraction cannot accidentally change runtime lookup, launch arguments, or harness-observed lifecycle behavior.
Output: Test coverage for `MdCadRuntimeResolver` and `WindowsMdCadEmbedBackend`, an internal `IMdCadEmbedBackend` contract, extracted Win32 helpers, and `50-MANUAL-CHECKLIST.md`.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-CONTEXT.md
@.planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-RESEARCH.md
@.planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-VALIDATION.md
@.planning/milestones/v1.8-phases/47-sample-host-workflow-proof/47-02-SUMMARY.md
@.planning/milestones/v1.8-phases/48-reusable-avalonia-mdcad-user-control/48-CONTEXT.md
@samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs
@samples/avalonia-mdcad-control/Host/EmbedNativeControlHost.cs
@samples/avalonia-mdcad-control/Host/MdCadLaunchSnapshot.cs
@samples/avalonia-mdcad-control/Host/MdCadRuntimeResolver.cs
@samples/avalonia-host/MainWindow.axaml.cs

<interfaces>
From samples/avalonia-mdcad-control/Host/MdCadRuntimeResolver.cs:
```csharp
internal readonly record struct MdCadRuntimePaths(string ExecutablePath, string RuntimeRoot);

internal static class MdCadRuntimeResolver
{
    public static MdCadRuntimePaths Resolve();
}
```

From samples/avalonia-mdcad-control/Host/MdCadLaunchSnapshot.cs:
```csharp
internal readonly record struct MdCadLaunchSnapshot(
    string? RequestedJsonlPath,
    string? LaunchJsonlPath,
    bool StartupLiveRefreshEnabled,
    string? WarningText)
{
    public bool ShouldPassJsonlArgument { get; }
    public bool ShouldPassLiveRefreshArgument { get; }
    public static MdCadLaunchSnapshot Create(string? requestedJsonlPath, bool startupLiveRefreshEnabled);
}
```

From samples/avalonia-mdcad-control/Host/EmbedNativeControlHost.cs:
```csharp
internal sealed class EmbedNativeControlHost : NativeControlHost
{
    public event Action<IntPtr>? PlaceholderHandleReady;
    public IntPtr PlaceholderHandle { get; private set; }
    public void ClearPlaceholderHandle();
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave 0 automated proof for runtime lookup and Windows launch construction</name>
  <read_first>
    - .planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-CONTEXT.md
    - .planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-RESEARCH.md
    - .planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-VALIDATION.md
    - samples/avalonia-mdcad-control/Host/MdCadRuntimeResolver.cs
    - samples/avalonia-mdcad-control/Host/MdCadLaunchSnapshot.cs
    - samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs
  </read_first>
  <files>samples/avalonia-mdcad-control.tests/MdCad.Avalonia.Control.Tests.csproj, samples/avalonia-mdcad-control.tests/MdCadRuntimeResolverTests.cs, samples/avalonia-mdcad-control.tests/WindowsMdCadEmbedBackendTests.cs, samples/avalonia-mdcad-control/Host/MdCadRuntimeResolver.cs, samples/avalonia-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs</files>
  <behavior>
    - Test 1: `MdCadRuntimeResolver` resolves only `{baseDirectory}\\mdcad-runtime\\mdCAD.exe` and throws when the folder or executable is missing.
    - Test 2: the Windows backend produces `ProcessStartInfo` with `FileName == runtime.ExecutablePath`, `WorkingDirectory == runtime.RuntimeRoot`, and ordered args `--embedded --parent-hwnd 0x...` per WPRS-01.
    - Test 3: optional JSONL and `--jsonl-live-refresh` forwarding stays identical to current `MdCadLaunchSnapshot` behavior and never appears when the snapshot omits them.
  </behavior>
  <action>Per D-05 and WPRS-01, write failing tests first in `MdCadRuntimeResolverTests.cs` and `WindowsMdCadEmbedBackendTests.cs`. Add an internal testable resolver overload on `MdCadRuntimeResolver` (for example `ResolveFromBaseDirectory(string baseDirectory)`) while keeping the existing parameterless `Resolve()` unchanged for production callers. In `Host/Windows/WindowsMdCadEmbedBackend.cs`, add only the contract-first, non-UI launch builder needed by the tests (for example `internal static ProcessStartInfo CreateStartInfo(...)`); do not move lifecycle ownership out of `MdCadEmbeddedControl` yet. Keep the current bundle folder name, executable name, working directory, parent-HWND hex formatting, and optional JSONL/live-refresh flag order exactly as they ship today.</action>
  <verify>
    <automated>dotnet test samples/avalonia-mdcad-control.tests/MdCad.Avalonia.Control.Tests.csproj -c Release --filter "FullyQualifiedName~MdCadRuntimeResolverTests|FullyQualifiedName~WindowsMdCadEmbedBackendTests"</automated>
  </verify>
  <acceptance_criteria>
    - `MdCadRuntimeResolverTests.cs` explicitly checks `AppContext.BaseDirectory\mdcad-runtime\mdCAD.exe` semantics through a testable internal overload
    - `WindowsMdCadEmbedBackendTests.cs` locks working directory plus required/optional launch args
    - production `Resolve()` still exists and preserves the current behavior-preserving API shape per D-02
  </acceptance_criteria>
  <done>Automated proof exists for the exact Windows runtime lookup and launch contract that Phase 50 must preserve.</done>
</task>

<task type="auto">
  <name>Task 2: Define the internal backend seam and bind it to the authoritative manual regression checklist</name>
  <read_first>
    - samples/avalonia-mdcad-control/Host/EmbedNativeControlHost.cs
    - samples/avalonia-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs
    - samples/avalonia-host/MainWindow.axaml.cs
    - .planning/milestones/v1.8-phases/47-sample-host-workflow-proof/47-02-SUMMARY.md
    - .planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-RESEARCH.md
    - .planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-MANUAL-CHECKLIST.md
  </read_first>
  <files>samples/avalonia-mdcad-control/Host/EmbedNativeControlHost.cs, samples/avalonia-mdcad-control/Host/IMdCadEmbedBackend.cs, samples/avalonia-mdcad-control/Host/Windows/Win32NativeMethods.cs, samples/avalonia-mdcad-control/Host/Windows/WindowsMdCadEmbedBackend.cs, .planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-MANUAL-CHECKLIST.md</files>
  <action>Per D-03, create `Host/IMdCadEmbedBackend.cs` with the exact internal members the next plan will wire: `Control Surface`, `bool CanStartSession`, `IntPtr PlaceholderHandle`, `bool HasActiveSession`, `event Action StateChanged`, `event Action<string> UnexpectedSessionLoss`, `Task StartSessionAsync(MdCadLaunchSnapshot snapshot, CancellationToken cancellationToken)`, `Task StopSessionAsync(CancellationToken cancellationToken)`, and `Task RecreateSurfaceAsync(CancellationToken cancellationToken)`. Also extract `Win32NativeMethods` from `Host/EmbedNativeControlHost.cs` into `Host/Windows/Win32NativeMethods.cs` without changing any P/Invoke behavior yet. In `.planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-MANUAL-CHECKLIST.md`, create the Windows proof script the final checkpoint will execute: runtime bundle presence, launch/attach, stop, relaunch, resize continuity, and sealed-vs-diagnostic equivalence through `samples/avalonia-host` per D-06 and D-07. Do not add unsupported-platform logic, public API changes, new TFMs, or host-facing capability APIs.</action>
  <verify>
    <automated>dotnet test samples/avalonia-mdcad-control.tests/MdCad.Avalonia.Control.Tests.csproj -c Release --filter "FullyQualifiedName~MdCadRuntimeResolverTests|FullyQualifiedName~WindowsMdCadEmbedBackendTests" &amp;&amp; dotnet build samples/avalonia-host/AvaloniaHost.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `IMdCadEmbedBackend.cs` exists under `Host/` and remains internal-only
    - `Win32NativeMethods.cs` exists under `Host/Windows/` with the current P/Invoke surface unchanged
    - `50-MANUAL-CHECKLIST.md` includes attach, stop, relaunch, resize, and sealed/diagnostic verification steps against `samples/avalonia-host`
  </acceptance_criteria>
  <done>The refactor has a concrete internal target seam and a manual Windows regression contract before any lifecycle ownership moves.</done>
</task>

</tasks>

<verification>
- `dotnet test samples/avalonia-mdcad-control.tests/MdCad.Avalonia.Control.Tests.csproj -c Release --filter "FullyQualifiedName~MdCadRuntimeResolverTests|FullyQualifiedName~WindowsMdCadEmbedBackendTests"`
- `dotnet build samples/avalonia-host/AvaloniaHost.csproj -c Release`
- Confirm `50-MANUAL-CHECKLIST.md` names the existing diagnostic host as the proof surface instead of inventing a new harness.
</verification>

<success_criteria>
- Wave 0 gaps from `50-VALIDATION.md` are closed with executable automated proof files and a manual checklist scaffold.
- Internal seam names and file layout are defined without changing public control API, TFM, or current Windows behavior.
- Phase 50 can proceed into lifecycle extraction with WPRS-01/WPRS-02 already pinned.
</success_criteria>

<output>
After completion, create `.planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-01-SUMMARY.md`
</output>
