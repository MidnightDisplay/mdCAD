---
phase: 52.1-automate-windows-runtime-refresh-from-build-vulkan-with-a-dotnet-managed-post-build-helper
plan: 03
type: execute
wave: 3
depends_on:
  - "52.1-02"
files_modified:
  - .planning/phases/52.1-automate-windows-runtime-refresh-from-build-vulkan-with-a-dotnet-managed-post-build-helper/52.1-VALIDATION.md
  - samples/avalonia-mdcad-control.tests/RuntimeRefreshIntegrationTests.cs
autonomous: true
requirements:
  - WPRS-04
must_haves:
  truths:
    - "A real host build can consume the refreshed runtime through the existing `AppContext.BaseDirectory\\mdcad-runtime\\mdCAD.exe` contract."
    - "After refresh, the `build-vulkan` executable, committed runtime executable, and consumer output executable are byte-identical."
    - "`imgui.embedded.ini` remains present as part of the bundle contract without Phase 52.1 force-overwriting consumer state."
  artifacts:
    - path: "samples/avalonia-mdcad-control.tests/RuntimeRefreshIntegrationTests.cs"
      provides: "Repository-aware hash/path verification for the refreshed runtime chain."
      contains: "GetFileHash"
    - path: ".planning/phases/52.1-automate-windows-runtime-refresh-from-build-vulkan-with-a-dotnet-managed-post-build-helper/52.1-VALIDATION.md"
      provides: "Final integration closeout record for validation row 52.1-03-01."
      contains: "52.1-03-01"
  key_links:
    - from: "samples/avalonia-host/AvaloniaHost.csproj"
      to: "samples/avalonia-mdcad-control/MdCad.Avalonia.Control.csproj"
      via: "ProjectReference-driven host build exercises the opt-in control target"
      pattern: "ProjectReference Include=\"..\\\\avalonia-mdcad-control\\\\MdCad.Avalonia.Control.csproj\""
    - from: "samples/avalonia-mdcad-control.tests/RuntimeRefreshIntegrationTests.cs"
      to: "samples/avalonia-mdcad-control/Host/MdCadRuntimeResolver.cs"
      via: "integration test reasserts output-rooted runtime resolution"
      pattern: "ResolveFromBaseDirectory|mdcad-runtime"
    - from: "samples/avalonia-mdcad-control.tests/RuntimeRefreshIntegrationTests.cs"
      to: "build-vulkan/bin/Release/mdCAD.exe"
      via: "hash comparison against committed bundle and host output"
      pattern: "mdCAD\\.exe"
---

<objective>
Close Phase 52.1 with host-level integration proof and hash/path verification, without drifting into broader Phase 53 proof scope.

Purpose: WPRS-04 is only complete when the helper-driven refresh flow is proven end-to-end through a real host build that still resolves `mdcad-runtime\mdCAD.exe` from consumer output.
Output: Runtime-refresh integration tests plus a green closeout for validation row `52.1-03-01`.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/52.1-automate-windows-runtime-refresh-from-build-vulkan-with-a-dotnet-managed-post-build-helper/52.1-RESEARCH.md
@.planning/phases/52.1-automate-windows-runtime-refresh-from-build-vulkan-with-a-dotnet-managed-post-build-helper/52.1-VALIDATION.md
@.planning/phases/52.1-automate-windows-runtime-refresh-from-build-vulkan-with-a-dotnet-managed-post-build-helper/52.1-02-PLAN.md
@samples/avalonia-host/AvaloniaHost.csproj
@samples/avalonia-mdcad-control/Host/MdCadRuntimeResolver.cs
@samples/avalonia-mdcad-control.tests/MdCadRuntimeResolverTests.cs

<interfaces>
Existing host/control linkage:
```xml
<ProjectReference Include="..\avalonia-mdcad-control\MdCad.Avalonia.Control.csproj" />
```

Existing runtime resolver contract:
```csharp
internal static MdCadRuntimePaths ResolveFromBaseDirectory(string baseDirectory)
{
    string runtimeRoot = Path.GetFullPath(Path.Combine(baseDirectory, "mdcad-runtime"));
    string executablePath = Path.Combine(runtimeRoot, "mdCAD.exe");
    ...
}
```

Current committed runtime bundle contents:
```text
samples/avalonia-mdcad-control/runtime/win-x64/
  imgui.embedded.ini
  mdCAD.exe
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add repository-aware integration tests for hash equality, runtime path resolution, and ini preservation</name>
  <files>samples/avalonia-mdcad-control.tests/RuntimeRefreshIntegrationTests.cs</files>
  <behavior>
    - Test 1: after a refresh-enabled build, `build-vulkan\bin\Release\mdCAD.exe`, `samples\avalonia-mdcad-control\runtime\win-x64\mdCAD.exe`, and the host output `mdcad-runtime\mdCAD.exe` have matching hashes.
    - Test 2: the host output still satisfies `MdCadRuntimeResolver.ResolveFromBaseDirectory(...)` rooted at `AppContext.BaseDirectory\mdcad-runtime\mdCAD.exe`.
    - Test 3: `imgui.embedded.ini` still exists in the committed bundle/consumer output contract and the test does not assert forced overwrite semantics for it.
  </behavior>
  <action>Create `RuntimeRefreshIntegrationTests.cs` in the existing test project and keep the class/test names under the `RuntimeRefresh` filter used by `52.1-VALIDATION.md`. Make the tests repository-aware by locating repo-relative paths, computing hashes in managed code, and asserting the exact path chain the phase must preserve. Keep the assertions Windows-only and fail clearly if the expected host-output/runtime artifacts are missing after the refresh-enabled build. Do not turn this into broader attach/start/stop proof; stay on refresh, path, and hash closure only.</action>
  <verify>
    <automated>dotnet test .\samples\avalonia-mdcad-control.tests\MdCad.Avalonia.Control.Tests.csproj -c Release --filter "FullyQualifiedName~RuntimeRefresh"</automated>
  </verify>
  <done>The test project can independently prove the refreshed executable chain and runtime-root contract without relying on manual inspection or PowerShell hashing.</done>
</task>

<task type="auto">
  <name>Task 2: Run the host integration lane and close validation row 52.1-03-01</name>
  <files>.planning/phases/52.1-automate-windows-runtime-refresh-from-build-vulkan-with-a-dotnet-managed-post-build-helper/52.1-VALIDATION.md</files>
  <action>Run the full Phase 52.1 closeout lane from `52.1-VALIDATION.md`: targeted runtime-refresh tests plus `dotnet build .\samples\avalonia-host\AvaloniaHost.csproj -c Release -p:MdCadRefreshWindowsRuntime=true`. Use the integration tests to verify the host output matches the committed bundle and `build-vulkan` binary, then update validation row `52.1-03-01` to green with the exact commands used. Keep the closeout scoped to WPRS-04 only: no host UX proof, no attach/stop/relaunch verification, and no documentation updates.</action>
  <verify>
    <automated>dotnet test .\samples\avalonia-mdcad-control.tests\MdCad.Avalonia.Control.Tests.csproj -c Release --filter "FullyQualifiedName~RuntimeRefresh" && dotnet build .\samples\avalonia-host\AvaloniaHost.csproj -c Release -p:MdCadRefreshWindowsRuntime=true</automated>
  </verify>
  <done>Phase 52.1 has automated evidence that the helper refreshes `build-vulkan`, the committed bundle, and a real host consumer output while preserving the existing runtime-root lookup contract.</done>
</task>

</tasks>

<verification>
- `dotnet test .\samples\avalonia-mdcad-control.tests\MdCad.Avalonia.Control.Tests.csproj -c Release --filter "FullyQualifiedName~RuntimeRefresh" && dotnet build .\samples\avalonia-host\AvaloniaHost.csproj -c Release -p:MdCadRefreshWindowsRuntime=true`
- Confirm `MdCadRuntimeResolver.cs` still resolves from `AppContext.BaseDirectory\mdcad-runtime\mdCAD.exe` with no repo-root fallback.
</verification>

<success_criteria>
- The refresh chain is proven end-to-end through the real host build.
- Hash equality closes the loop between `build-vulkan`, the committed runtime bundle, and the consumer output.
- Phase 52.1 finishes without drifting into Phase 53 lifecycle proof or Phase 54 docs work.
</success_criteria>

<output>
After completion, create `.planning/phases/52.1-automate-windows-runtime-refresh-from-build-vulkan-with-a-dotnet-managed-post-build-helper/52.1-03-SUMMARY.md`
</output>
