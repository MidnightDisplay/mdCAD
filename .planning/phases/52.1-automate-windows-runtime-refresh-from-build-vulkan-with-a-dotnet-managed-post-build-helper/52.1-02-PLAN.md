---
phase: 52.1-automate-windows-runtime-refresh-from-build-vulkan-with-a-dotnet-managed-post-build-helper
plan: 02
type: execute
wave: 2
depends_on:
  - "52.1-01"
files_modified:
  - .planning/phases/52.1-automate-windows-runtime-refresh-from-build-vulkan-with-a-dotnet-managed-post-build-helper/52.1-VALIDATION.md
  - samples/avalonia-mdcad-control/MdCad.Avalonia.Control.csproj
autonomous: true
requirements:
  - WPRS-04
must_haves:
  truths:
    - "The control project can opt into runtime refresh from MSBuild without making native rebuilds mandatory for every build."
    - "A refreshed control build updates the committed `runtime/win-x64/mdCAD.exe` and the just-built output `mdcad-runtime\\mdCAD.exe` in the same invocation."
    - "The post-build flow stays Windows-only and preserves `imgui.embedded.ini` as a seeded/runtime-state file."
  artifacts:
    - path: "samples/avalonia-mdcad-control/MdCad.Avalonia.Control.csproj"
      provides: "Opt-in MSBuild target and properties for helper-driven runtime refresh."
      contains: "MdCadRefreshWindowsRuntime"
    - path: ".planning/phases/52.1-automate-windows-runtime-refresh-from-build-vulkan-with-a-dotnet-managed-post-build-helper/52.1-VALIDATION.md"
      provides: "Execution record for control-project wiring validation row 52.1-02-01."
      contains: "52.1-02-01"
  key_links:
    - from: "samples/avalonia-mdcad-control/MdCad.Avalonia.Control.csproj"
      to: "samples/avalonia-mdcad-control/tools/MdCad.WindowsRuntimeRefresh/MdCad.WindowsRuntimeRefresh.csproj"
      via: "opt-in post-build Exec invokes the helper with absolute paths"
      pattern: "dotnet run --project"
    - from: "samples/avalonia-mdcad-control/MdCad.Avalonia.Control.csproj"
      to: "samples/avalonia-mdcad-control/runtime/win-x64/mdCAD.exe"
      via: "helper argument for committed bundle refresh"
      pattern: "runtime\\\\win-x64"
    - from: "samples/avalonia-mdcad-control/MdCad.Avalonia.Control.csproj"
      to: "$(TargetDir)mdcad-runtime\\mdCAD.exe"
      via: "helper output-runtime-dir argument keeps the current build output fresh"
      pattern: "output-runtime-dir|TargetDir"
---

<objective>
Wire the helper into the control project as the single opt-in post-build entrypoint for WPRS-04.

Purpose: The control project already owns the committed runtime bundle and the `mdcad-runtime\...` copy contract, so it is the right place to trigger helper-driven refresh without spreading logic to hosts.
Output: A Windows-only, design-time-safe opt-in target in `MdCad.Avalonia.Control.csproj` and a green validation ledger entry for row `52.1-02-01`.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/52.1-automate-windows-runtime-refresh-from-build-vulkan-with-a-dotnet-managed-post-build-helper/52.1-RESEARCH.md
@.planning/phases/52.1-automate-windows-runtime-refresh-from-build-vulkan-with-a-dotnet-managed-post-build-helper/52.1-VALIDATION.md
@.planning/phases/52.1-automate-windows-runtime-refresh-from-build-vulkan-with-a-dotnet-managed-post-build-helper/52.1-01-PLAN.md
@samples/avalonia-mdcad-control/MdCad.Avalonia.Control.csproj

<interfaces>
Helper CLI contract from Plan 52.1-01:
```text
dotnet run --project <repo>\samples\avalonia-mdcad-control\tools\MdCad.WindowsRuntimeRefresh\MdCad.WindowsRuntimeRefresh.csproj -- \
  --repo-root <abs-repo-root> \
  --build-dir <abs-build-vulkan> \
  --runtime-dir <abs-runtime-win-x64> \
  --output-runtime-dir <abs-targetdir-mdcad-runtime> \
  --configuration Release
```

Existing content-copy contract that must remain intact:
```xml
<Content Include="runtime\win-x64\**\*">
  <Link>mdcad-runtime\%(RecursiveDir)%(Filename)%(Extension)</Link>
  <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
  <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
</Content>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add opt-in helper properties and a Windows-only post-build target to the control project</name>
  <files>samples/avalonia-mdcad-control/MdCad.Avalonia.Control.csproj</files>
  <action>Extend `samples/avalonia-mdcad-control/MdCad.Avalonia.Control.csproj` with default-false `MdCadRefreshWindowsRuntime` plus any narrowly scoped override properties needed for build dir/configuration, then add an `AfterTargets="Build"` target that runs only when `$(OS)` is `Windows_NT`, `$(DesignTimeBuild)` is not true, and `MdCadRefreshWindowsRuntime=true`. Invoke the helper created in Plan 52.1-01 with explicit absolute paths rooted from `$(MSBuildProjectDirectory)` and pass `$(TargetDir)mdcad-runtime` so the just-built output receives the refreshed executable too. Keep the flow centered on this csproj per research, do not wire hosts directly, do not make the target default-on, do not reintroduce PowerShell, and do not alter the existing `Content Include="runtime\win-x64\**\*"` contract.</action>
  <verify>
    <automated>dotnet build .\samples\avalonia-mdcad-control\MdCad.Avalonia.Control.csproj -c Release -p:MdCadRefreshWindowsRuntime=true</automated>
  </verify>
  <done>The control project exposes a guarded opt-in runtime refresh path that invokes the helper only on Windows/non-design-time builds and passes both committed-bundle and current-output paths.</done>
</task>

<task type="auto">
  <name>Task 2: Re-close control-project refresh behavior and record validation row 52.1-02-01</name>
  <files>.planning/phases/52.1-automate-windows-runtime-refresh-from-build-vulkan-with-a-dotnet-managed-post-build-helper/52.1-VALIDATION.md</files>
  <action>Run the control-project build lane with `-p:MdCadRefreshWindowsRuntime=true`, confirm the helper refreshed `samples/avalonia-mdcad-control/runtime/win-x64/mdCAD.exe` and the current control output `mdcad-runtime\mdCAD.exe`, and preserve `imgui.embedded.ini` as a non-force-overwritten seeded file. Update `52.1-VALIDATION.md` row `52.1-02-01` to green with the exact build command and any short note needed to capture that the flow remained opt-in and Windows-only. Do not broaden this task into host proof, docs, or consumer-behavior changes reserved for later phases.</action>
  <verify>
    <automated>dotnet build .\samples\avalonia-mdcad-control\MdCad.Avalonia.Control.csproj -c Release -p:MdCadRefreshWindowsRuntime=true</automated>
  </verify>
  <done>The validation ledger shows `52.1-02-01` green and the control build path is proven to refresh the committed bundle plus current output without overwriting `imgui.embedded.ini`.</done>
</task>

</tasks>

<verification>
- `dotnet build .\samples\avalonia-mdcad-control\MdCad.Avalonia.Control.csproj -c Release -p:MdCadRefreshWindowsRuntime=true`
- Confirm the target is inactive for ordinary builds unless `MdCadRefreshWindowsRuntime=true`.
</verification>

<success_criteria>
- The control project is the only post-build hook point for the new runtime refresh flow.
- The refresh behavior remains opt-in, Windows-only, and safe for design-time builds.
- The current output receives a fresh `mdCAD.exe` in the same build that refreshes the committed bundle.
</success_criteria>

<output>
After completion, create `.planning/phases/52.1-automate-windows-runtime-refresh-from-build-vulkan-with-a-dotnet-managed-post-build-helper/52.1-02-SUMMARY.md`
</output>
