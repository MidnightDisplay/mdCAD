---
phase: 52.1-automate-windows-runtime-refresh-from-build-vulkan-with-a-dotnet-managed-post-build-helper
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/52.1-automate-windows-runtime-refresh-from-build-vulkan-with-a-dotnet-managed-post-build-helper/52.1-VALIDATION.md
  - samples/avalonia-mdcad-control/tools/MdCad.WindowsRuntimeRefresh/MdCad.WindowsRuntimeRefresh.csproj
  - samples/avalonia-mdcad-control/tools/MdCad.WindowsRuntimeRefresh/Program.cs
  - samples/avalonia-mdcad-control/tools/MdCad.WindowsRuntimeRefresh/RuntimeRefreshOptions.cs
  - samples/avalonia-mdcad-control/tools/MdCad.WindowsRuntimeRefresh/RuntimeRefreshOrchestrator.cs
  - samples/avalonia-mdcad-control.tests/MdCad.Avalonia.Control.Tests.csproj
  - samples/avalonia-mdcad-control.tests/RuntimeRefreshHelperTests.cs
autonomous: true
requirements:
  - WPRS-04
must_haves:
  truths:
    - "A maintainer can run a repo-local dotnet-managed helper to rebuild mdCAD from the existing `build-vulkan` directory without invoking PowerShell."
    - "The helper fails fast when `build-vulkan` is not already configured instead of trying to configure native tooling from scratch."
    - "The helper's copy policy refreshes `mdCAD.exe` while preserving the seeded/runtime-state role of `imgui.embedded.ini`."
  artifacts:
    - path: "samples/avalonia-mdcad-control/tools/MdCad.WindowsRuntimeRefresh/MdCad.WindowsRuntimeRefresh.csproj"
      provides: "Repo-local net10 console helper for WPRS-04."
      contains: "<OutputType>Exe</OutputType>"
    - path: "samples/avalonia-mdcad-control/tools/MdCad.WindowsRuntimeRefresh/RuntimeRefreshOrchestrator.cs"
      provides: "Argument validation, cmake build orchestration, and targeted copy behavior."
      contains: "cmake"
    - path: "samples/avalonia-mdcad-control.tests/RuntimeRefreshHelperTests.cs"
      provides: "Automated coverage for helper path, fail-fast, and copy-policy behavior."
      contains: "RuntimeRefresh"
  key_links:
    - from: "samples/avalonia-mdcad-control/tools/MdCad.WindowsRuntimeRefresh/Program.cs"
      to: "samples/avalonia-mdcad-control/tools/MdCad.WindowsRuntimeRefresh/RuntimeRefreshOrchestrator.cs"
      via: "CLI argument parsing forwards to orchestrator"
      pattern: "RunAsync|RuntimeRefreshOptions"
    - from: "samples/avalonia-mdcad-control/tools/MdCad.WindowsRuntimeRefresh/RuntimeRefreshOrchestrator.cs"
      to: "build-vulkan/CMakeCache.txt"
      via: "fail-fast check before `cmake --build`"
      pattern: "CMakeCache\\.txt"
    - from: "samples/avalonia-mdcad-control.tests/RuntimeRefreshHelperTests.cs"
      to: "samples/avalonia-mdcad-control/tools/MdCad.WindowsRuntimeRefresh/RuntimeRefreshOrchestrator.cs"
      via: "unit tests assert no-PowerShell cmake invocation and targeted copy manifest"
      pattern: "mdCAD\\.exe|imgui\\.embedded\\.ini"
---

<objective>
Create the dotnet-managed helper foundation for WPRS-04 and lock its path/copy-policy rules before any MSBuild wiring.

Purpose: Phase 52.1 must replace the existing PowerShell-only refresh flow with a C# helper while preserving the AppContext-rooted runtime contract and the committed runtime bundle under `samples/avalonia-mdcad-control/runtime/win-x64`.
Output: A repo-local helper project, test-project reference, and green targeted helper tests aligned to validation row `52.1-01-01`.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/52.1-automate-windows-runtime-refresh-from-build-vulkan-with-a-dotnet-managed-post-build-helper/52.1-RESEARCH.md
@.planning/phases/52.1-automate-windows-runtime-refresh-from-build-vulkan-with-a-dotnet-managed-post-build-helper/52.1-VALIDATION.md
@.planning/phases/52-plain-net10-control-compatibility/52-03-SUMMARY.md
@samples/avalonia-mdcad-control/MdCad.Avalonia.Control.csproj
@samples/avalonia-mdcad-control/Host/MdCadRuntimeResolver.cs
@scripts/refresh-pinned-mdcad-runtime.ps1
@samples/avalonia-mdcad-control.tests/MdCad.Avalonia.Control.Tests.csproj

<interfaces>
Existing runtime-copy contract to preserve:
```xml
<Content Include="runtime\win-x64\**\*">
  <Link>mdcad-runtime\%(RecursiveDir)%(Filename)%(Extension)</Link>
  <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
  <CopyToPublishDirectory>PreserveNewest</CopyToPublishDirectory>
</Content>
```

Existing runtime-resolution contract to preserve:
```csharp
internal static class MdCadRuntimeResolver
{
    private const string RuntimeDirectoryName = "mdcad-runtime";
    private const string ExecutableName = "mdCAD.exe";

    internal static MdCadRuntimePaths ResolveFromBaseDirectory(string baseDirectory)
    {
        string runtimeRoot = Path.GetFullPath(Path.Combine(baseDirectory, RuntimeDirectoryName));
        string executablePath = Path.Combine(runtimeRoot, ExecutableName);
        ...
    }
}
```

Replacement scope from the current PowerShell script:
```powershell
$requiredFiles = @(
    "mdCAD.exe",
    "imgui.embedded.ini"
)
```
Phase 52.1 must not reproduce the wholesale recopy behavior above; preserve `imgui.embedded.ini` instead of force-refreshing it.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the helper project, option contract, and path-policy tests for validation row 52.1-01-01</name>
  <files>samples/avalonia-mdcad-control/tools/MdCad.WindowsRuntimeRefresh/MdCad.WindowsRuntimeRefresh.csproj, samples/avalonia-mdcad-control/tools/MdCad.WindowsRuntimeRefresh/Program.cs, samples/avalonia-mdcad-control/tools/MdCad.WindowsRuntimeRefresh/RuntimeRefreshOptions.cs, samples/avalonia-mdcad-control/tools/MdCad.WindowsRuntimeRefresh/RuntimeRefreshOrchestrator.cs, samples/avalonia-mdcad-control.tests/MdCad.Avalonia.Control.Tests.csproj, samples/avalonia-mdcad-control.tests/RuntimeRefreshHelperTests.cs</files>
  <behavior>
    - Test 1: required helper arguments (`--repo-root`, `--build-dir`, `--runtime-dir`, `--configuration`) are validated and normalized to absolute paths.
    - Test 2: the helper derives `build-vulkan\bin\Release\mdCAD.exe` as the refresh source and treats `output-runtime-dir` as optional current-build output wiring, not a replacement for the committed runtime bundle.
    - Test 3: the copy manifest includes `mdCAD.exe` for committed and current output refresh, but does not force-copy `imgui.embedded.ini`.
  </behavior>
  <action>Create `samples/avalonia-mdcad-control/tools/MdCad.WindowsRuntimeRefresh/` as a small `net10.0` console helper project that is directly project-referenceable from the xUnit test project. Split the helper into a thin `Program.cs`, a `RuntimeRefreshOptions` contract, and an orchestrator class so tests can cover path/copy decisions without shelling out. In `samples/avalonia-mdcad-control.tests/MdCad.Avalonia.Control.Tests.csproj`, add the project reference needed to test the helper. Per the locked phase constraints, do not invoke `powershell`, `.ps1`, `cmd /c`, or any quoted shell wrapper, do not change `MdCadRuntimeResolver`, do not imply cross-platform runtime embedding, and do not widen scope into Phase 53 proof or Phase 54 docs.</action>
  <verify>
    <automated>dotnet test .\samples\avalonia-mdcad-control.tests\MdCad.Avalonia.Control.Tests.csproj -c Release --filter "FullyQualifiedName~RuntimeRefresh"</automated>
  </verify>
  <done>The helper project exists, tests compile against it, and the test suite proves the intended path/copy contract before native-build execution logic is added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement no-PowerShell rebuild orchestration with fail-fast configured-build checks</name>
  <files>.planning/phases/52.1-automate-windows-runtime-refresh-from-build-vulkan-with-a-dotnet-managed-post-build-helper/52.1-VALIDATION.md, samples/avalonia-mdcad-control/tools/MdCad.WindowsRuntimeRefresh/Program.cs, samples/avalonia-mdcad-control/tools/MdCad.WindowsRuntimeRefresh/RuntimeRefreshOrchestrator.cs, samples/avalonia-mdcad-control.tests/RuntimeRefreshHelperTests.cs</files>
  <behavior>
    - Test 1: when `build-vulkan\CMakeCache.txt` is missing, the helper exits with a clear fail-fast error instead of trying to configure CMake.
    - Test 2: the helper launches `cmake --build <build-dir> --config <configuration> --target mdCAD` through `ProcessStartInfo.ArgumentList`.
    - Test 3: after the build completes, the helper requires `mdCAD.exe` to exist and copies it into the committed runtime bundle, plus `output-runtime-dir\mdCAD.exe` when that optional argument is supplied.
  </behavior>
  <action>Finish `RuntimeRefreshOrchestrator` so it validates the existing `build-vulkan` directory, runs `cmake --build` directly through `System.Diagnostics.ProcessStartInfo.ArgumentList`, checks that the built `mdCAD.exe` exists, and copies only that executable into `samples/avalonia-mdcad-control/runtime/win-x64` plus any supplied output runtime directory. Keep `imgui.embedded.ini` untouched in both committed/runtime output locations, treat a missing `CMakeCache.txt` as the explicit fail-fast path the research called for, and do not add first-run generator selection, SDK detection, or other setup automation. After the targeted tests pass, update `52.1-VALIDATION.md` row `52.1-01-01` to green with the exact command used.</action>
  <verify>
    <automated>dotnet test .\samples\avalonia-mdcad-control.tests\MdCad.Avalonia.Control.Tests.csproj -c Release --filter "FullyQualifiedName~RuntimeRefresh"</automated>
  </verify>
  <done>The helper can rebuild mdCAD from the existing `build-vulkan` cache, refresh only `mdCAD.exe`, and the validation ledger records `52.1-01-01` as green.</done>
</task>

</tasks>

<verification>
- `dotnet test .\samples\avalonia-mdcad-control.tests\MdCad.Avalonia.Control.Tests.csproj -c Release --filter "FullyQualifiedName~RuntimeRefresh"`
- Confirm the helper never shells through PowerShell and still leaves `MdCadRuntimeResolver` unchanged on `AppContext.BaseDirectory\mdcad-runtime\mdCAD.exe`.
</verification>

<success_criteria>
- A reusable C# helper exists under the repo and fully replaces PowerShell for the refresh implementation path.
- The helper is fail-fast against an unconfigured `build-vulkan` directory instead of silently expanding scope.
- The committed runtime bundle contract and `imgui.embedded.ini` preservation policy are locked by automated tests.
</success_criteria>

<output>
After completion, create `.planning/phases/52.1-automate-windows-runtime-refresh-from-build-vulkan-with-a-dotnet-managed-post-build-helper/52.1-01-SUMMARY.md`
</output>
