---
phase: 47-sample-host-workflow-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - samples/avalonia-host/AvaloniaHost.csproj
  - samples/avalonia-host/resources/examples/sample-host-proof.jsonl
  - samples/avalonia-host/MainWindow.axaml.cs
autonomous: true
requirements:
  - HOST-02
must_haves:
  truths:
    - "Launching the sample host resolves a bundled JSONL example from its deployed `resources/examples` directory and passes an absolute `--jsonl` path to mdCAD."
    - "The sample host keeps startup live refresh explicit and default-off unless the host intentionally adds `--jsonl-live-refresh`."
    - "If the bundled example is missing, the host reports that failure before launching mdCAD instead of falling back to a relative-path guess."
  artifacts:
    - path: "samples/avalonia-host/AvaloniaHost.csproj"
      provides: "Build output copy contract for bundled example JSONL files."
      contains: "resources\\examples\\**\\*"
    - path: "samples/avalonia-host/resources/examples/sample-host-proof.jsonl"
      provides: "Bundled end-to-end JSONL proof fixture for the sample host."
      contains: "\"$type\":\"Geo.Point3D\""
    - path: "samples/avalonia-host/MainWindow.axaml.cs"
      provides: "Absolute bundled-example resolution and CLI launch argument wiring for `--jsonl` plus optional `--jsonl-live-refresh`."
      contains: "--jsonl"
  key_links:
    - from: "samples/avalonia-host/AvaloniaHost.csproj"
      to: "samples/avalonia-host/resources/examples/sample-host-proof.jsonl"
      via: "content-copy rule into the host output directory"
      pattern: "CopyToOutputDirectory"
    - from: "samples/avalonia-host/MainWindow.axaml.cs"
      to: "src/app_launch_config.h"
      via: "existing absolute-path launch contract using `--jsonl` and optional `--jsonl-live-refresh`"
      pattern: "--jsonl|--jsonl-live-refresh"
---

<objective>
Add the bundled example resource and host launch wiring that turns the Avalonia sample into a real startup-JSONL proof harness.

Purpose: HOST-02 is the first missing end-to-end contract for Phase 47; the host must launch mdCAD with a real bundled file path, not a repo-relative assumption or a new IPC seam.
Output: A copied-on-build example JSONL fixture plus `MainWindow.axaml.cs` launch wiring that resolves `resources/examples/sample-host-proof.jsonl` from `AppContext.BaseDirectory` and appends the correct CLI flags.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/47-sample-host-workflow-proof/47-RESEARCH.md
@.planning/phases/47-sample-host-workflow-proof/47-VALIDATION.md
@.planning/phases/43-embed-contract-child-window-bootstrap/43-03-SUMMARY.md
@.planning/phases/45-startup-jsonl-auto-import/45-01-SUMMARY.md
@.planning/phases/46-launch-time-live-refresh/46-01-SUMMARY.md
@samples/avalonia-host/AvaloniaHost.csproj
@samples/avalonia-host/MainWindow.axaml.cs
@src/app_launch_config.h

<interfaces>
From samples/avalonia-host/MainWindow.axaml.cs:
```csharp
public sealed class HostLaunchOptions
{
    public string? MdcadExePath { get; }
    public EmbedTestMode TestMode { get; }
    public string? ParseError { get; }
    public static HostLaunchOptions Parse(string[] args);
}

private bool TryResolveMdcadExecutable(out string? mdCadExePath, out string failureMessage)
private void LaunchEmbeddedViewer()
```

From src/app_launch_config.h:
```c
typedef struct {
    bool embedded;
    uintptr_t parent_hwnd_value;
    bool startup_jsonl_live_refresh;
    char startup_jsonl_path[MDCAD_STARTUP_JSONL_PATH_MAX];
} mdcad_launch_config_t;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the bundled example JSONL fixture and copy it into the host output</name>
  <files>samples/avalonia-host/AvaloniaHost.csproj, samples/avalonia-host/resources/examples/sample-host-proof.jsonl</files>
  <read_first>samples/avalonia-host/AvaloniaHost.csproj, .planning/phases/47-sample-host-workflow-proof/47-RESEARCH.md, .planning/phases/47-sample-host-workflow-proof/47-VALIDATION.md, src/tests/startup_jsonl_import_controller_test.c</read_first>
  <action>Create `samples/avalonia-host/resources/examples/sample-host-proof.jsonl` as a tiny valid flat JSONL proof fixture using the same element shape already exercised by `src/tests/startup_jsonl_import_controller_test.c`: `{"Name":"Entry","Elements":[{"Name":"P","Description":"","Colour":"White","Element":{"$type":"Geo.Point3D","X":1,"Y":2,"Z":3}}]}` on its own line. Update `samples/avalonia-host/AvaloniaHost.csproj` to copy `resources/examples/**` into the build output with `CopyToOutputDirectory=PreserveNewest`. Do not embed the file as a managed resource and do not introduce temp-file extraction; mdCAD already needs a real absolute filesystem path.</action>
  <verify>
    <automated>dotnet build samples/avalonia-host/AvaloniaHost.csproj -c Release &amp;&amp; powershell -Command "if (!(Test-Path 'samples/avalonia-host/bin/Release/net8.0-windows10.0.19041.0/resources/examples/sample-host-proof.jsonl')) { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - `samples/avalonia-host/AvaloniaHost.csproj` contains `Include="resources\examples\**\*"`
    - `samples/avalonia-host/AvaloniaHost.csproj` contains `<CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>`
    - `samples/avalonia-host/resources/examples/sample-host-proof.jsonl` contains `"Element":{"$type":"Geo.Point3D","X":1,"Y":2,"Z":3}`
  </acceptance_criteria>
  <done>The host output folder contains a real bundled JSONL proof file under `resources/examples`, ready to be resolved to an absolute launch path.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the bundled example from the deployed host and append the existing JSONL launch flags</name>
  <files>samples/avalonia-host/MainWindow.axaml.cs</files>
  <read_first>samples/avalonia-host/MainWindow.axaml.cs, samples/avalonia-host/AvaloniaHost.csproj, src/app_launch_config.h, .planning/phases/47-sample-host-workflow-proof/47-RESEARCH.md, .planning/phases/47-sample-host-workflow-proof/47-VALIDATION.md, .planning/phases/43-embed-contract-child-window-bootstrap/43-03-SUMMARY.md, .planning/phases/45-startup-jsonl-auto-import/45-01-SUMMARY.md, .planning/phases/46-launch-time-live-refresh/46-01-SUMMARY.md</read_first>
  <action>Extend `HostLaunchOptions` and the launch path in `MainWindow.axaml.cs` so the sample host resolves `sample-host-proof.jsonl` from `Path.Combine(AppContext.BaseDirectory, "resources", "examples", "sample-host-proof.jsonl")`, converts it with `Path.GetFullPath(...)`, and fails in-host before process start if the file does not exist. Build `ProcessStartInfo.Arguments` from the existing embedded contract plus `--jsonl "<absolute-path>"`; append `--jsonl-live-refresh` only when the host explicitly opts in, keeping default-off behavior unchanged. Before launch, update the host-owned JSONL status/detail text to report that the bundled example resolved successfully and include the exact absolute path being requested so HOST-02 can be proven without IPC. Preserve the existing `--mdcad-exe` override and `--embed-test-mode` handling, keep the no-new-IPC boundary, and keep JSONL/live-refresh wording host-inferred only (requested/off/missing), not claimed import success.</action>
  <verify>
    <automated>dotnet build samples/avalonia-host/AvaloniaHost.csproj -c Release &amp;&amp; ctest --test-dir build-vulkan -C Release --output-on-failure -R "(embed_launch_config_test|startup_jsonl_import_controller_test)"</automated>
  </verify>
  <acceptance_criteria>
    - `samples/avalonia-host/MainWindow.axaml.cs` contains `Path.Combine(AppContext.BaseDirectory, "resources", "examples", "sample-host-proof.jsonl")`
    - `samples/avalonia-host/MainWindow.axaml.cs` contains `"--jsonl"`
    - `samples/avalonia-host/MainWindow.axaml.cs` contains `"--jsonl-live-refresh"`
    - `samples/avalonia-host/MainWindow.axaml.cs` contains `Path.GetFullPath`
    - `samples/avalonia-host/MainWindow.axaml.cs` contains `example resolved`
    - `samples/avalonia-host/MainWindow.axaml.cs` contains `sample-host-proof.jsonl`
  </acceptance_criteria>
  <done>The sample host launches mdCAD with the bundled example's absolute path through the existing CLI contract, reports that resolved path before launch, and keeps live refresh explicit/default-off.</done>
</task>

</tasks>

<verification>
- `dotnet build samples/avalonia-host/AvaloniaHost.csproj -c Release`
- `powershell -Command "if (!(Test-Path 'samples/avalonia-host/bin/Release/net8.0-windows10.0.19041.0/resources/examples/sample-host-proof.jsonl')) { exit 1 }"`
- `ctest --test-dir build-vulkan -C Release --output-on-failure -R "(embed_launch_config_test|startup_jsonl_import_controller_test)"`
</verification>

<success_criteria>
- The sample host deploys `resources/examples/sample-host-proof.jsonl` beside the built executable.
- `MainWindow.axaml.cs` resolves that file from `AppContext.BaseDirectory` and launches mdCAD with `--jsonl <absolute-path>`.
- Startup live refresh remains opt-in via `--jsonl-live-refresh`; plain bundled launch stays default-off.
</success_criteria>

<output>
After completion, create `.planning/phases/47-sample-host-workflow-proof/47-01-SUMMARY.md`
</output>
