---
phase: 53-consumer-proof-and-windows-regression-closure
plan: 03
type: execute
wave: 3
depends_on:
  - 53-02
files_modified:
  - .planning/phases/53-consumer-proof-and-windows-regression-closure/53-MANUAL-CHECKLIST.md
  - .planning/phases/53-consumer-proof-and-windows-regression-closure/53-VALIDATION.md
autonomous: false
requirements:
  - WPRS-03
must_haves:
  truths:
    - "Phase 53 has a fresh, phase-local manual proof artifact for the real Windows host lifecycle."
    - "The manual proof explicitly checks copied-runtime presence plus attach, stop, and relaunch on `samples/avalonia-host`."
    - "The artifact uses current `StartAsync` / `StopAsync` wording and stays separate from both `PROOF-01` and the historical Phase 50 record."
  artifacts:
    - path: ".planning/phases/53-consumer-proof-and-windows-regression-closure/53-MANUAL-CHECKLIST.md"
      provides: "The authoritative Phase 53 runtime proof record."
      contains: "StartAsync"
    - path: ".planning/phases/53-consumer-proof-and-windows-regression-closure/53-VALIDATION.md"
      provides: "Validation row 53-03-01 for the manual WPRS-03 proof."
      contains: "53-03-01"
    - path: "samples/avalonia-host/MainWindow.axaml.cs"
      provides: "The current runtime and harness status strings the checklist must verify."
      contains: "runtime: copied bundle present ->"
  key_links:
    - from: ".planning/phases/53-consumer-proof-and-windows-regression-closure/53-MANUAL-CHECKLIST.md"
      to: "samples/avalonia-host/AvaloniaHost.csproj"
      via: "manual run command and recorded lifecycle evidence"
      pattern: "dotnet run --project .\\\\samples\\\\avalonia-host\\\\AvaloniaHost.csproj -c Release"
    - from: ".planning/phases/53-consumer-proof-and-windows-regression-closure/53-MANUAL-CHECKLIST.md"
      to: "samples/avalonia-host/MainWindow.axaml.cs"
      via: "expected copied-runtime and harness status text"
      pattern: "runtime: copied bundle present ->|action: StartAsync requested|action: StopAsync requested"
---

<objective>
Create and complete a Phase 53 manual proof artifact for the real Windows attach/stop/relaunch lifecycle on `samples/avalonia-host`.

Purpose: Close the last `WPRS-03` obligation with an authoritative phase-local checklist instead of mutating Phase 50 history or drifting into Phase 54 docs work.
Output: A new `53-MANUAL-CHECKLIST.md`, a human-approved lifecycle run on the diagnostic host, and a green `53-03-01` validation row when the proof passes.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/53-consumer-proof-and-windows-regression-closure/53-RESEARCH.md
@.planning/phases/53-consumer-proof-and-windows-regression-closure/53-VALIDATION.md
@.planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-MANUAL-CHECKLIST.md
@samples/avalonia-host/AvaloniaHost.csproj
@samples/avalonia-host/MainWindow.axaml
@samples/avalonia-host/MainWindow.axaml.cs
@samples/avalonia-mdcad-control/QUICKSTART.md

<interfaces>
From samples/avalonia-host/MainWindow.axaml:
```xml
<Button x:Name="StartViewerButton"
        Content="StartAsync"
        Click="OnStartViewerClick" />
<Button x:Name="StopViewerButton"
        Content="StopAsync"
        Click="OnStopViewerClick" />
```

From samples/avalonia-host/MainWindow.axaml.cs:
```csharp
private string BuildRuntimeStatus()
{
    string executablePath = Path.GetFullPath(Path.Combine(AppContext.BaseDirectory, "mdcad-runtime", RuntimeExecutableName));
    return File.Exists(executablePath)
        ? $"runtime: copied bundle present -> {executablePath}"
        : $"runtime: copied bundle missing -> {executablePath}";
}

private string BuildHarnessStatus()
{
    string note = string.IsNullOrWhiteSpace(_lastActionNote) ? string.Empty : $" | action: {_lastActionNote}";
    return $"harness: mode={modeText}; AutoStart={autoStartText}; live refresh={liveRefreshText}; {_currentJsonlStatus}{note}";
}
```

From .planning/phases/50-backend-seam-extraction-and-windows-behavior-lock/50-MANUAL-CHECKLIST.md:
```markdown
- In the running host, confirm the runtime line starts with:
  `runtime: copied bundle present ->`
- Click **Close Session** and confirm the viewer exits cleanly.
- Click **Launch Session** again and confirm a second attach succeeds in the same host window.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a Phase 53 manual checklist that matches the current host UI and proof boundary</name>
  <files>.planning/phases/53-consumer-proof-and-windows-regression-closure/53-MANUAL-CHECKLIST.md</files>
  <action>Create `.planning/phases/53-consumer-proof-and-windows-regression-closure/53-MANUAL-CHECKLIST.md` as a new phase-local artifact. Use Phase 50's checklist only as a structural source, but update the wording to the current host UI (`StartAsync` / `StopAsync`) and current status text (`runtime: copied bundle present ->`, `action: StartAsync requested`, `action: StopAsync requested`). Include the automated preflight from row `53-02-01` as required prep, then document copied-runtime presence, first attach, stop, no-stray-process check, and relaunch steps for `samples/avalonia-host`. Do not edit `50-MANUAL-CHECKLIST.md`, and do not use this plan to clean up `samples/avalonia-mdcad-control/QUICKSTART.md` or other Phase 54 docs.</action>
  <verify>
    <automated>Select-String -Path ".\.planning\phases\53-consumer-proof-and-windows-regression-closure\53-MANUAL-CHECKLIST.md" -Pattern "StartAsync|StopAsync|runtime: copied bundle present ->|53-02-01"</automated>
  </verify>
  <done>The new Phase 53 checklist exists, uses the current UI/status wording, and clearly separates the manual Windows proof from the compile-only proof.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the real Windows attach/stop/relaunch proof on samples/avalonia-host</name>
  <files>.planning/phases/53-consumer-proof-and-windows-regression-closure/53-MANUAL-CHECKLIST.md</files>
  <action>Launch the diagnostic host and execute the new Phase 53 checklist end to end. This checkpoint verifies the real native child-HWND lifecycle that automated tests do not cover; do not replace it with additional synthetic harness work.</action>
  <what-built>Phase-local manual proof instructions for the authoritative Windows host lifecycle surface.</what-built>
  <how-to-verify>
    1. Run `dotnet run --project .\samples\avalonia-host\AvaloniaHost.csproj -c Release`.
    2. Confirm the runtime line starts with `runtime: copied bundle present ->` and points to the copied host output bundle.
    3. Confirm the harness line reflects the expected launch mode and JSONL status, then click **StartAsync** if the session did not auto-start.
    4. Verify the embedded mdCAD window attaches inside the host and the status area reports an attached session.
    5. Click **StopAsync** and confirm the viewer exits cleanly, the host returns to idle, and no stray `mdCAD.exe` from the copied runtime remains before relaunch.
    6. Click **StartAsync** again and confirm a second attach succeeds in the same host window using the copied runtime bundle.
    7. Record PASS/FAIL/BLOCKED notes directly in `53-MANUAL-CHECKLIST.md`.
  </how-to-verify>
  <verify>
    <automated>dotnet run --project .\samples\avalonia-host\AvaloniaHost.csproj -c Release</automated>
  </verify>
  <done>The user confirms the real host lifecycle proof passes, or returns concrete failures captured in the checklist for follow-up work.</done>
  <resume-signal>Type "approved" when the checklist passes, or describe the exact lifecycle failure observed.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the Phase 53 manual proof outcome in the checklist and validation row 53-03-01</name>
  <files>.planning/phases/53-consumer-proof-and-windows-regression-closure/53-MANUAL-CHECKLIST.md, .planning/phases/53-consumer-proof-and-windows-regression-closure/53-VALIDATION.md</files>
  <action>After the checkpoint resumes, finalize the Phase 53 proof record. If the user approved the run, mark the checklist PASS with date/notes and update validation row `53-03-01` to green. If the user reports a failure, record the failure or blocked state in the checklist and validation instead of forcing a green row. Preserve the explicit separation among `53-01-01` compile proof, `53-02-01` automated Windows preflight, and `53-03-01` real Windows lifecycle proof.</action>
  <verify>
    <automated>Select-String -Path ".\.planning\phases\53-consumer-proof-and-windows-regression-closure\53-MANUAL-CHECKLIST.md",".\.planning\phases\53-consumer-proof-and-windows-regression-closure\53-VALIDATION.md" -Pattern "\[X\] PASS|53-03-01|✅ green|FAIL|BLOCKED"</automated>
  </verify>
  <done>The phase-local checklist and validation ledger both reflect the authoritative outcome of the manual Windows lifecycle proof.</done>
</task>

</tasks>

<verification>
- `Select-String -Path ".\.planning\phases\53-consumer-proof-and-windows-regression-closure\53-MANUAL-CHECKLIST.md" -Pattern "StartAsync|StopAsync|runtime: copied bundle present ->|53-02-01"`
- `dotnet run --project .\samples\avalonia-host\AvaloniaHost.csproj -c Release`
- `Select-String -Path ".\.planning\phases\53-consumer-proof-and-windows-regression-closure\53-MANUAL-CHECKLIST.md",".\.planning\phases\53-consumer-proof-and-windows-regression-closure\53-VALIDATION.md" -Pattern "\[X\] PASS|53-03-01|✅ green|FAIL|BLOCKED"`
</verification>

<success_criteria>
- Phase 53 has a new checklist artifact instead of mutating Phase 50 history.
- The checklist uses current `StartAsync` / `StopAsync` wording and records copied-runtime presence plus attach/stop/relaunch on `samples/avalonia-host`.
- Validation row `53-03-01` reflects the actual manual proof outcome and remains distinct from both compile proof and automated preflight.
</success_criteria>

<output>
After completion, create `.planning/phases/53-consumer-proof-and-windows-regression-closure/53-03-SUMMARY.md`
</output>
