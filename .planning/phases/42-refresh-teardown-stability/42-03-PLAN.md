---
phase: 42-refresh-teardown-stability
plan: 03
type: execute
wave: 3
depends_on: [42-01, 42-02]
files_modified:
  - .planning/phases/42-refresh-teardown-stability/42-MANUAL-CHECKLIST.md
autonomous: false
requirements: [OBSF-07, OBSF-08, PERF-04, PERF-05]
must_haves:
  truths:
    - "The real `lamp_11.jsonl` acceptance run explicitly covers linked auto-reload, repeated manual re-import, delete after settled refresh history, and delete while refresh is active."
    - "Manual evidence records viewport coverage, Scene Hierarchy totals, and slot-buffer state for every lifecycle scenario required by D-09 and D-10."
    - "Phase 42 closes only when deterministic automation is green and the real-file checklist has no unresolved `TBD` markers."
  artifacts:
    - path: ".planning/phases/42-refresh-teardown-stability/42-MANUAL-CHECKLIST.md"
      provides: "Four-scenario lamp_11 manual lifecycle script and recorded evidence"
      contains: "Delete while refresh is active"
  key_links:
    - from: "src/ui/ui_entity_inspector.h"
      to: ".planning/phases/42-refresh-teardown-stability/42-MANUAL-CHECKLIST.md"
      via: "`Observe automatically`, `Re-import now`, and `Last refresh result` evidence capture"
      pattern: "Observe automatically|Re-import now|Last refresh result"
    - from: "src/ui/ui_scene_hierarchy.h"
      to: ".planning/phases/42-refresh-teardown-stability/42-MANUAL-CHECKLIST.md"
      via: "root totals, delete action, and `(refreshing)` hierarchy state"
      pattern: "Delete will cancel the running flat refresh.|refreshing"
    - from: "src/ui/ui_slot_buffer_debug.h"
      to: ".planning/phases/42-refresh-teardown-stability/42-MANUAL-CHECKLIST.md"
      via: "slot occupancy evidence for exact settle and delete cleanup"
      pattern: "Total slots: %d  |  Capacity: %d  |  Free: %d"
---

<objective>
Capture the blocking real-file Phase 42 acceptance proof across all four lifecycle scenarios on `lamp_11.jsonl`.

Purpose: Turn the runtime fixes from 42-01 and 42-02 into explicit D-09/D-10 closure evidence instead of relying on memory.
Output: One manual checklist plus one blocking human verification checkpoint for the full linked refresh/delete lifecycle.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-refresh-teardown-stability/42-CONTEXT.md
@.planning/phases/41-linked-import-convergence/41-MANUAL-CHECKLIST.md
@.planning/phases/42-refresh-teardown-stability/42-01-PLAN.md
@.planning/phases/42-refresh-teardown-stability/42-02-PLAN.md
@src/ui/ui_entity_inspector.h
@src/ui/ui_scene_hierarchy.h
@src/ui/ui_slot_buffer_debug.h

<interfaces>
From `src/ui/ui_entity_inspector.h`:
```c
static inline void ui_entity_inspector_draw_jsonl_flat_observer_controls(ui_entity_inspector_state_t *state,
                                                                         ecs_world_state_t *w,
                                                                         ecs_entity_t root_entity,
                                                                         ecs_scene_t *scene);
```

From `src/ui/ui_slot_buffer_debug.h`:
```c
static inline void ui_slot_buffer_debug_draw(ui_slot_buffer_debug_state_t* state);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the four-scenario lamp_11 lifecycle checklist for Phase 42 closeout</name>
  <read_first>
    - .planning/phases/42-refresh-teardown-stability/42-CONTEXT.md
    - .planning/phases/41-linked-import-convergence/41-MANUAL-CHECKLIST.md
    - src/ui/ui_entity_inspector.h
    - src/ui/ui_scene_hierarchy.h
    - src/ui/ui_slot_buffer_debug.h
  </read_first>
  <files>.planning/phases/42-refresh-teardown-stability/42-MANUAL-CHECKLIST.md</files>
  <action>Create `42-MANUAL-CHECKLIST.md` as the exact D-09/D-10 closeout script. Use the required source path `C:\Users\RodionRadchenko\source\repos\ParkerSteel-GenerateDimensionedDrawing\ParkerSteel.Drawing.Test\bin\Debug\net8.0-windows\lamp_11.jsonl`, require the `build-vulkan` Release app build, and create four named scenario sections: `Scenario 1 - linked auto-reload`, `Scenario 2 - repeated manual Re-import now`, `Scenario 3 - delete after settled refresh history`, and `Scenario 4 - delete while refresh is active`. For Scenario 1, instruct the human to keep `Observe automatically` on and externally regenerate or rewrite the same watched `lamp_11.jsonl` path once so auto-reload actually fires, then record viewport coverage, first-settled vs final-settled Scene Hierarchy totals, Slot Buffer Debug `Total slots / Capacity / Free`, and the latest `Last refresh result` message (D-09/D-10). For Scenario 2, require at least three presses of `Re-import now` and record that coverage and slot occupancy return to the same settled footprint after each run (D-01/D-02/D-03). For Scenario 3, require deleting the linked root after settled refresh history and recording that viewport geometry disappears, Scene Hierarchy entry vanishes, and slot-buffer ownership for the root is gone in the same action (D-04). For Scenario 4, require starting a manual refresh, deleting while the inspector shows `Refresh in progress...` or the hierarchy row shows `(refreshing)`, then recording cancel behavior, leftover state, and an undo result that restores only the last committed import state (D-05/D-06). Give every scenario explicit result fields named `Viewport result:`, `Scene Hierarchy evidence:`, `Slot Buffer evidence:`, `Observer/Inspector evidence:`, and `Scenario verdict:` plus a final `Overall verdict:` line, all initialized to `TBD`.</action>
  <acceptance_criteria>`42-MANUAL-CHECKLIST.md` exists and contains the exact lamp path, all four scenario headers, the exact field names `Viewport result:`, `Scene Hierarchy evidence:`, `Slot Buffer evidence:`, `Observer/Inspector evidence:`, `Scenario verdict:`, and `Overall verdict:`, and at least one `TBD` marker for each scenario before the checkpoint runs.</acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; t=Path('.planning/phases/42-refresh-teardown-stability/42-MANUAL-CHECKLIST.md').read_text(encoding='utf-8'); required=['Scenario 1 - linked auto-reload','Scenario 2 - repeated manual Re-import now','Scenario 3 - delete after settled refresh history','Scenario 4 - delete while refresh is active','Overall verdict:']; assert all(x in t for x in required); assert t.count('TBD') >= 5"</automated>
  </verify>
  <done>The full Phase 42 real-file acceptance run is written down with no ambiguity about which lifecycle scenarios or evidence fields are required.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the lamp_11 lifecycle pass and record the blocking closeout evidence</name>
  <read_first>
    - .planning/phases/42-refresh-teardown-stability/42-MANUAL-CHECKLIST.md
    - src/ui/ui_entity_inspector.h
    - src/ui/ui_scene_hierarchy.h
    - src/ui/ui_slot_buffer_debug.h
  </read_first>
  <files>.planning/phases/42-refresh-teardown-stability/42-MANUAL-CHECKLIST.md</files>
  <what-built>Phase 42 automated refresh/delete fixes plus a four-scenario real-file checklist covering D-09 and D-10.</what-built>
  <action>Execute the checklist exactly as written. First confirm the targeted automated regression command from 42-01 and 42-02 passes. Then run all four `lamp_11.jsonl` scenarios in order: (1) trigger one real watched-file rewrite while `Observe automatically` stays ON and record the settled viewport/hierarchy/slot-buffer outcome, (2) press `Re-import now` at least three times and record whether every settle returns to the same final footprint, (3) delete the linked root after refresh history and record whether viewport, Scene Hierarchy, and slot-buffer evidence all retire in the same action, and (4) start a manual refresh, delete while it is visibly running, then record whether cancellation leaves no leftovers and whether undo restores only the last committed import state. Replace every `TBD`, mark each `Scenario verdict:` as `PASS` or `FAIL`, and set `Overall verdict:` to `PASS` only if all four scenarios pass and the evidence shows no geometry collapse, no late churn, and no orphaned slot-buffer state.</action>
  <how-to-verify>
    1) Run `ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_(auto_safety_test|manual_refresh_test|inspector_contract_test)" --output-on-failure"` and confirm it passes.
    2) Launch the Release app, import the exact `lamp_11.jsonl` path with linking enabled, and keep `Observe automatically` ON for Scenario 1.
    3) Regenerate or otherwise rewrite the same watched `lamp_11.jsonl` path once so auto-reload fires; wait for settle and fill Scenario 1 evidence.
    4) Press `Re-import now` at least three times, waiting for each settle; fill Scenario 2 evidence.
    5) Delete the linked root after the repeated-refresh history; fill Scenario 3 evidence.
    6) Re-import, trigger one more manual refresh, delete while the UI shows refresh is active, undo once, and fill Scenario 4 evidence.
    7) Ensure the checklist contains no `TBD` markers and ends with `Overall verdict: PASS` or `Overall verdict: FAIL`.
  </how-to-verify>
  <acceptance_criteria>`42-MANUAL-CHECKLIST.md` contains no remaining `TBD` markers, includes filled evidence for all four scenarios, and ends with an explicit `Overall verdict: PASS` or `Overall verdict: FAIL` line.</acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; t=Path('.planning/phases/42-refresh-teardown-stability/42-MANUAL-CHECKLIST.md').read_text(encoding='utf-8'); assert 'TBD' not in t; assert ('Overall verdict: PASS' in t) or ('Overall verdict: FAIL' in t)"</automated>
  </verify>
  <done>The real-file Phase 42 acceptance evidence is captured in one place and can block or approve milestone closure on explicit lifecycle proof.</done>
  <resume-signal>Type "approved" after the checklist is fully filled and acceptable, or reply with the recorded failure notes.</resume-signal>
</task>

</tasks>

<verification>
Run:
- `ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_(auto_safety_test|manual_refresh_test|inspector_contract_test)" --output-on-failure`
- `python -c "from pathlib import Path; t=Path('.planning/phases/42-refresh-teardown-stability/42-MANUAL-CHECKLIST.md').read_text(encoding='utf-8'); assert 'TBD' not in t"`
</verification>

<success_criteria>
- All four lifecycle scenarios are recorded against the real `lamp_11.jsonl` file.
- Manual evidence explicitly covers viewport, Scene Hierarchy, slot-buffer, and observer-status signals.
- Phase 42 only closes after both targeted automation and the four-scenario manual pass are green.
</success_criteria>

<output>
After completion, create `.planning/phases/42-refresh-teardown-stability/42-03-SUMMARY.md`
</output>
