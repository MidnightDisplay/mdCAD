---
phase: 42-refresh-teardown-stability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/jsonl_observer_system.h
  - src/tests/jsonl_flat_observer_auto_safety_test.c
  - src/tests/jsonl_flat_observer_manual_refresh_test.c
autonomous: true
requirements: [OBSF-07, OBSF-08, PERF-05]
must_haves:
  truths:
    - "Observer-driven refresh and manual `Re-import now` both settle back to the exact live line/point footprint instead of leaving 2x occupancy, tail-only geometry, or points-only remnants."
    - "After a linked refresh settles, rendered coverage and hierarchy counts stop changing until the next real source change or manual refresh."
    - "If refresh cleanup/commit invariants fail, the last committed import stays visible, staged refresh content is discarded, automatic observe turns OFF, and manual `Re-import now` still remains possible."
  artifacts:
    - path: "src/jsonl_observer_system.h"
      provides: "Shared flat-refresh settle invariant enforcement, anomaly fallback, and one-shot test seam"
      contains: "jsonl_observer_flat_refresh_occupancy_matches_root"
    - path: "src/tests/jsonl_flat_observer_auto_safety_test.c"
      provides: "Observer auto-refresh regression coverage for exact settled occupancy and no late churn"
      contains: "test_flat_observe_auto_refresh_settles_to_exact_live_footprint"
    - path: "src/tests/jsonl_flat_observer_manual_refresh_test.c"
      provides: "Manual re-import and anomaly fallback regressions for exact settled occupancy"
      contains: "test_flat_manual_refresh_repeated_cycles_return_to_exact_live_footprint"
  key_links:
    - from: "src/jsonl_observer_system.h"
      to: "src/tests/jsonl_flat_observer_auto_safety_test.c"
      via: "observer-driven refresh completion and idle-after-settle assertions"
      pattern: "jsonl_observer_system_tick|jsonl_observer_tick_flat_refreshes"
    - from: "src/jsonl_observer_system.h"
      to: "src/tests/jsonl_flat_observer_manual_refresh_test.c"
      via: "manual refresh completion path and anomaly fallback seam"
      pattern: "jsonl_observer_request_flat_refresh|jsonl_observer_force_next_flat_cleanup_failure_for_tests"
---

<objective>
Lock exact settle-time refresh cleanup so linked large flat imports stop collapsing, stop growing slot occupancy, and fail safe under cleanup anomalies.

Purpose: Close the shared refresh-contract gap behind OBSF-07, OBSF-08, and PERF-05 before delete/undo work builds on it.
Output: Deterministic regressions plus one runtime choke point that enforces D-01, D-02, D-03, D-07, and D-08 for both auto and manual refresh.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-refresh-teardown-stability/42-CONTEXT.md
@.planning/phases/39-automatic-observer-safety-loop/39-02-SUMMARY.md
@.planning/phases/40-large-dump-stability-interaction-coherence/40-01-SUMMARY.md
@.planning/phases/41-linked-import-convergence/41-01-SUMMARY.md
@.planning/phases/41-linked-import-convergence/41-02-SUMMARY.md
@src/jsonl_observer_system.h
@src/components/jsonl_observer_comp.h
@src/tests/jsonl_flat_observer_auto_safety_test.c
@src/tests/jsonl_flat_observer_manual_refresh_test.c

<interfaces>
From `src/jsonl_observer_system.h`:
```c
typedef struct {
    bool active;
    bool pending_rerun;
    ecs_world_state_t *world;
    ecs_entity_t target_root;
    ecs_entity_t stage_root;
    uint64_t started_at_ms;
    jsonl_import_job_t job;
    selection_buffer_t *selection;
} jsonl_flat_refresh_slot_t;

static inline bool jsonl_observer_request_flat_refresh(ecs_scene_t *scene,
                                                       ecs_entity_t root_entity,
                                                       selection_buffer_t *selection);
static inline bool jsonl_observer_is_flat_refresh_running(ecs_scene_t *scene, ecs_entity_t root_entity);
static inline void jsonl_observer_tick_flat_refreshes(ecs_scene_t *scene);
```

From `src/components/jsonl_observer_comp.h`:
```c
typedef struct {
    bool linked;
    bool observe_enabled;
    bool source_state_valid;
    uint32_t retry_count;
    uint32_t max_retries;
    char source_path[JSONL_OBSERVER_PATH_MAX];
    uint32_t message_count;
    uint8_t message_severity[JSONL_OBSERVER_MESSAGE_HISTORY];
    char messages[JSONL_OBSERVER_MESSAGE_HISTORY][JSONL_OBSERVER_MESSAGE_MAX];
} JsonlObserverComp;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add exact-footprint and anomaly regressions for linked refresh settle</name>
  <read_first>
    - .planning/phases/42-refresh-teardown-stability/42-CONTEXT.md
    - src/tests/jsonl_flat_observer_auto_safety_test.c
    - src/tests/jsonl_flat_observer_manual_refresh_test.c
    - src/jsonl_observer_system.h
    - src/components/jsonl_observer_comp.h
  </read_first>
  <files>src/tests/jsonl_flat_observer_auto_safety_test.c, src/tests/jsonl_flat_observer_manual_refresh_test.c</files>
  <behavior>
    - Test 1: A linked auto-refresh triggered by an actual file rewrite settles with `used_line_slots == live_line_slots_under_root` and `used_point_slots == live_point_slots_under_root`, then stays unchanged across extra observer ticks (D-01, D-02, D-03, per OBSF-07/PERF-05).
    - Test 2: Repeating manual `jsonl_observer_request_flat_refresh(...)` against equal-footprint and changed-footprint fixtures returns to exact live occupancy after every settle instead of accumulating slack across cycles (D-01, D-02, D-03, per OBSF-08/PERF-05).
    - Test 3: Forcing one cleanup/commit anomaly keeps the previously committed geometry count, sets `observe_enabled == false`, records a warning, and still allows a later manual refresh attempt to start because `source_path` and `linked` remain intact (D-07, D-08).
  </behavior>
  <action>Add deterministic coverage in the existing flat observer test binaries. In `src/tests/jsonl_flat_observer_auto_safety_test.c`, create `test_flat_observe_auto_refresh_settles_to_exact_live_footprint` that imports a linked fixture, rewrites the same source path to a known new payload, runs `jsonl_observer_system_tick(...)` plus `jsonl_observer_tick_flat_refreshes(...)` until idle, then asserts exact settled ownership by comparing `(scene->batches.lines.instance_buffer.count - free_count)` and `(scene->batches.points.instance_buffer.count - free_count)` against helper totals computed from renderable descendants under the linked root; immediately run extra unchanged-source ticks and assert those counts do not change again (per D-01/D-03). In `src/tests/jsonl_flat_observer_manual_refresh_test.c`, add `test_flat_manual_refresh_repeated_cycles_return_to_exact_live_footprint` that performs at least three `jsonl_observer_request_flat_refresh(...)` cycles and asserts no cumulative slot growth even when the fixture alternates between same-footprint and changed-footprint payloads (D-01/D-02/D-03). Also add `test_flat_refresh_cleanup_anomaly_keeps_last_good_and_disables_observe` that uses a one-shot runtime test seam named `jsonl_observer_force_next_flat_cleanup_failure_for_tests()` to force the next refresh to take the D-07/D-08 safety branch, then asserts the old geometry stays committed, `observe_enabled` flips OFF, the warning mentions safety disable, and a later manual refresh call can still start.</action>
  <acceptance_criteria>`src/tests/jsonl_flat_observer_auto_safety_test.c` contains `test_flat_observe_auto_refresh_settles_to_exact_live_footprint`; `src/tests/jsonl_flat_observer_manual_refresh_test.c` contains `test_flat_manual_refresh_repeated_cycles_return_to_exact_live_footprint` and `test_flat_refresh_cleanup_anomaly_keeps_last_good_and_disables_observe`; and the new assertions explicitly reference line/point slot usage, `observe_enabled`, and post-settle idle stability.</acceptance_criteria>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_(auto_safety_test|manual_refresh_test|inspector_contract_test)" --output-on-failure</automated>
  </verify>
  <done>Phase 42 has deterministic failing tests for the exact settle contract and for the cleanup anomaly safety path before runtime fixes land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce exact settled occupancy and anomaly fallback in the shared flat refresh path</name>
  <read_first>
    - .planning/phases/42-refresh-teardown-stability/42-CONTEXT.md
    - src/jsonl_observer_system.h
    - src/components/jsonl_observer_comp.h
    - src/tests/jsonl_flat_observer_auto_safety_test.c
    - src/tests/jsonl_flat_observer_manual_refresh_test.c
  </read_first>
  <files>src/jsonl_observer_system.h</files>
  <behavior>
    - Test 1: Both observer-driven and manual refresh completion use the same exact-occupancy settle check.
    - Test 2: Successful settle stamps the source baseline and leaves no cumulative slack in line/point slot ownership for the linked root.
    - Test 3: Cleanup anomaly preserves last-good content, discards stage content, disables auto-observe, and keeps manual refresh available.
  </behavior>
  <action>Refactor the flat refresh completion path in `src/jsonl_observer_system.h` around a new helper named `jsonl_observer_flat_refresh_occupancy_matches_root(...)` plus a one-shot test seam `jsonl_observer_force_next_flat_cleanup_failure_for_tests()`. Use the helper from `jsonl_observer_tick_flat_refreshes(...)` so both auto-reload and manual `Re-import now` share the exact same settle contract per D-01 and D-02. The helper must compute live descendant slot spans for the target root and reject settle completion if the final line/point usage does not return to the exact live footprint, if old child ownership remains, or if the one-shot test seam is armed (D-03). When a refresh succeeds, keep the existing source-state stamp and success message path; when the helper reports a cleanup anomaly, discard `stage_root`, leave the previously committed root children untouched, set `obs->observe_enabled = false`, keep `obs->linked = true` and `obs->source_path` unchanged, and push the exact warning string `Refresh cleanup anomaly; kept last-good content, automatic observe disabled.` so the safety branch is grep-visible (D-07, D-08). Do not add any alternate manual-refresh path; manual `jsonl_observer_request_flat_refresh(...)` must keep working off the retained `source_path` after this branch.</action>
  <acceptance_criteria>`src/jsonl_observer_system.h` contains `jsonl_observer_flat_refresh_occupancy_matches_root`, `jsonl_observer_force_next_flat_cleanup_failure_for_tests`, and the exact warning text `Refresh cleanup anomaly; kept last-good content, automatic observe disabled.`; the file still routes both auto and manual refresh completion through `jsonl_observer_tick_flat_refreshes(...)`; and the anomaly branch leaves `linked`/`source_path` intact while setting `observe_enabled = false`.</acceptance_criteria>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_(auto_safety_test|manual_refresh_test|inspector_contract_test)" --output-on-failure</automated>
  </verify>
  <done>Linked flat refresh settles to exact live occupancy with no late churn, and the cleanup anomaly fallback matches D-07/D-08 for both observer and manual refresh paths.</done>
</task>

</tasks>

<verification>
Run:
- `ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_(auto_safety_test|manual_refresh_test|inspector_contract_test)" --output-on-failure`
</verification>

<success_criteria>
- Auto-reload and manual refresh share one exact-footprint settle contract.
- Repeated settled refreshes no longer accumulate slack or late count churn.
- Cleanup anomalies preserve last-good content, disable automatic observe, and still leave manual refresh available.
</success_criteria>

<output>
After completion, create `.planning/phases/42-refresh-teardown-stability/42-01-SUMMARY.md`
</output>
