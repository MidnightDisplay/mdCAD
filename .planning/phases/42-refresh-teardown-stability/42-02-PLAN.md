---
phase: 42-refresh-teardown-stability
plan: 02
type: execute
wave: 2
depends_on: [42-01]
files_modified:
  - src/jsonl_observer_system.h
  - src/ui/ui_scene_hierarchy.h
  - src/app.c
  - src/tests/jsonl_flat_observer_manual_refresh_test.c
autonomous: true
requirements: [OBSF-08, PERF-04]
must_haves:
  truths:
    - "Deleting a linked flat import after refresh history removes all of that root's visible geometry and slot-buffer ownership in the same action."
    - "Deleting a linked flat import while refresh is running cancels staged/in-flight work and leaves no viewport or slot-buffer leftovers."
    - "Undo after deleting during refresh restores only the last committed import state and never resurrects canceled staged refresh content."
  artifacts:
    - path: "src/jsonl_observer_system.h"
      provides: "Refresh-cancel helper for root deletion"
      contains: "jsonl_observer_cancel_flat_refresh_for_root"
    - path: "src/ui/ui_scene_hierarchy.h"
      provides: "Hierarchy delete path that remains available during refresh and records undo first"
      contains: "Delete will cancel the running flat refresh."
    - path: "src/app.c"
      provides: "Keyboard delete path that cancels active linked refresh before removal"
      contains: "jsonl_observer_cancel_flat_refresh_for_root"
    - path: "src/tests/jsonl_flat_observer_manual_refresh_test.c"
      provides: "Delete-after-refresh and delete-during-refresh regressions including undo"
      contains: "test_flat_delete_during_refresh_cancels_stage_and_undo_restores_last_committed"
  key_links:
    - from: "src/app.c"
      to: "src/jsonl_observer_system.h"
      via: "Delete/Backspace hotkey path"
      pattern: "ImGuiKey_Delete|jsonl_observer_cancel_flat_refresh_for_root"
    - from: "src/ui/ui_scene_hierarchy.h"
      to: "src/jsonl_observer_system.h"
      via: "Hierarchy context-menu delete path"
      pattern: "Delete will cancel the running flat refresh.|jsonl_observer_cancel_flat_refresh_for_root"
    - from: "src/tests/jsonl_flat_observer_manual_refresh_test.c"
      to: "src/undo_redo_exec.h"
      via: "bulk-delete snapshot then undo restore of last committed root only"
      pattern: "undo_cmd_bulk_delete_entities|undo_redo_undo"
---

<objective>
Make linked-root deletion transactional with refresh cancellation so post-refresh cleanup, active-refresh delete, and undo all retire the correct geometry and only the committed geometry.

Purpose: Close the delete/teardown half of OBSF-08 and PERF-04 on top of the exact settle contract from 42-01.
Output: Deterministic delete lifecycle regressions and shared cancel-before-delete behavior in keyboard and hierarchy delete paths.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-refresh-teardown-stability/42-CONTEXT.md
@.planning/phases/40-large-dump-stability-interaction-coherence/40-02-SUMMARY.md
@.planning/phases/41-linked-import-convergence/41-02-SUMMARY.md
@.planning/phases/42-refresh-teardown-stability/42-01-PLAN.md
@src/jsonl_observer_system.h
@src/ui/ui_scene_hierarchy.h
@src/app.c
@src/tests/jsonl_flat_observer_manual_refresh_test.c
@src/undo_redo_exec.h
@src/undo_redo.h

<interfaces>
From `src/jsonl_observer_system.h`:
```c
static inline bool jsonl_observer_request_flat_refresh(ecs_scene_t *scene,
                                                       ecs_entity_t root_entity,
                                                       selection_buffer_t *selection);
static inline bool jsonl_observer_is_flat_refresh_running(ecs_scene_t *scene, ecs_entity_t root_entity);
static inline void jsonl_observer_tick_flat_refreshes(ecs_scene_t *scene);
```

From `src/undo_redo_exec.h`:
```c
static inline void undo_cmd_bulk_delete_entities(undo_redo_t *ur, ecs_entity_t *entities, int count);
```

From `src/undo_redo.h`:
```c
static inline void undo_redo_init(undo_redo_t *ur, void *scene, int capacity);
static inline bool undo_redo_undo(undo_redo_t *ur);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add delete-after-refresh and delete-during-refresh regressions with undo</name>
  <read_first>
    - .planning/phases/42-refresh-teardown-stability/42-CONTEXT.md
    - src/tests/jsonl_flat_observer_manual_refresh_test.c
    - src/jsonl_observer_system.h
    - src/undo_redo_exec.h
    - src/undo_redo.h
  </read_first>
  <files>src/tests/jsonl_flat_observer_manual_refresh_test.c</files>
  <behavior>
    - Test 1: After at least one settled refresh cycle, deleting the linked root leaves no live geometry under that root, no owned line/point slots for the deleted entities, and no active refresh slot (D-04, per PERF-04).
    - Test 2: Deleting during an active refresh cancels the slot, discards `stage_root`, removes the committed root, and leaves no leftover line/point ownership or viewport-equivalent geometry state (D-05, per PERF-04).
    - Test 3: Undo after the active-refresh delete restores the last committed root geometry count, but not the staged payload that was in flight when delete happened (D-06).
  </behavior>
  <action>Extend `src/tests/jsonl_flat_observer_manual_refresh_test.c` with `test_flat_delete_after_refresh_history_clears_geometry_and_slots` and `test_flat_delete_during_refresh_cancels_stage_and_undo_restores_last_committed`. Reuse the existing import/refresh helpers, add slot-ownership helpers that scan `scene->batches.lines.instance_buffer.slot_to_entity` and `scene->batches.points.instance_buffer.slot_to_entity`, and assert those arrays contain no deleted root or descendant entity IDs after deletion. For the active-refresh case, start `jsonl_observer_request_flat_refresh(...)`, wait until `jsonl_observer_is_flat_refresh_running(...)` is true, record the last committed geometry count, push a bulk-delete undo snapshot with `undo_cmd_bulk_delete_entities(...)`, then perform the same cancel/delete order the runtime will use; finally call `undo_redo_undo(...)` and assert the restored root matches the last committed count rather than the larger staged payload that was being built when delete happened (D-04/D-05/D-06).</action>
  <acceptance_criteria>`src/tests/jsonl_flat_observer_manual_refresh_test.c` contains `test_flat_delete_after_refresh_history_clears_geometry_and_slots` and `test_flat_delete_during_refresh_cancels_stage_and_undo_restores_last_committed`; the tests explicitly scan `slot_to_entity`; and the active-refresh delete test calls both `undo_cmd_bulk_delete_entities` and `undo_redo_undo`.</acceptance_criteria>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_(manual_refresh_test|auto_safety_test|inspector_contract_test)" --output-on-failure</automated>
  </verify>
  <done>There is deterministic automation for delete-after-refresh, delete-during-refresh, and undo-last-committed-only behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route keyboard and hierarchy delete through refresh cancellation without losing undo fidelity</name>
  <read_first>
    - .planning/phases/42-refresh-teardown-stability/42-CONTEXT.md
    - src/jsonl_observer_system.h
    - src/ui/ui_scene_hierarchy.h
    - src/app.c
    - src/tests/jsonl_flat_observer_manual_refresh_test.c
  </read_first>
  <files>src/jsonl_observer_system.h, src/ui/ui_scene_hierarchy.h, src/app.c</files>
  <behavior>
    - Test 1: Active linked refresh deletion always cancels stage work before root removal.
    - Test 2: Hierarchy delete remains available during refresh, but drag/drop and reparent actions stay locked.
    - Test 3: Keyboard and hierarchy delete both take undo snapshots from the committed entity tree only.
  </behavior>
  <action>Add `jsonl_observer_cancel_flat_refresh_for_root(ecs_scene_t *scene, ecs_entity_t root_entity)` to `src/jsonl_observer_system.h`. The helper must find the active slot for that root, abort `stage_root`, reset the import job, clear `pending_rerun`, and push the exact INFO message `Refresh canceled because the linked root was deleted.` without resurrecting or serializing staged entities (D-05/D-06). In `src/app.c`, stop treating the Delete/Backspace path as a bare `scene_remove_entity(...)` loop: route each selected root through the same shared undo-capable delete helper/order used by the hierarchy delete path, or otherwise perform the identical ordering inline, so keyboard delete first records `undo_cmd_bulk_delete_entities(...)` from the still-committed root, then calls `jsonl_observer_cancel_flat_refresh_for_root(...)`, then clears selection and removes the entity. The keyboard path must never snapshot or serialize `stage_root` / in-flight staged refresh entities, so undo after Delete/Backspace during refresh restores only the last committed import content per D-06. In `src/ui/ui_scene_hierarchy.h`, keep drag/drop, `Make Child of Selected`, and `Unparent` disabled while `locked_by_refresh` is true, but enable both context-menu Delete actions even during refresh; before deleting, create a one-entity undo snapshot with `undo_cmd_bulk_delete_entities(state->undo_redo, &e, 1)`, call `jsonl_observer_cancel_flat_refresh_for_root(state->scene, e)`, then remove selection and delete the entity. When `locked_by_refresh` is true, add the exact explanatory text `Delete will cancel the running flat refresh.` above the delete item so the runtime contract is explicit (D-04/D-05/D-06).</action>
  <acceptance_criteria>`src/jsonl_observer_system.h` contains `jsonl_observer_cancel_flat_refresh_for_root` and the exact message `Refresh canceled because the linked root was deleted.`; `src/app.c` routes Delete/Backspace through the same undo-capable cancel/delete order as the hierarchy path, with `undo_cmd_bulk_delete_entities(...)` occurring before `jsonl_observer_cancel_flat_refresh_for_root(...)` and `scene_remove_entity(...)` so keyboard undo restores only the last committed root and never canceled staged refresh content; and `src/ui/ui_scene_hierarchy.h` contains the exact text `Delete will cancel the running flat refresh.` plus `undo_cmd_bulk_delete_entities(state->undo_redo, &e, 1)` in both delete menu paths.</acceptance_criteria>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_(manual_refresh_test|auto_safety_test|inspector_contract_test)" --output-on-failure</automated>
  </verify>
  <done>Delete-after-refresh and delete-during-refresh now share a cancel-before-delete contract across keyboard and hierarchy surfaces, while undo still restores only committed content.</done>
</task>

</tasks>

<verification>
Run:
- `ctest --test-dir build-vulkan -C Release -R "jsonl_flat_observer_(manual_refresh_test|auto_safety_test|inspector_contract_test)" --output-on-failure`
</verification>

<success_criteria>
- Deleting a refreshed linked root removes all related geometry and slot ownership in one action.
- Deleting during refresh cancels staged work and leaves no leftovers.
- Undo restores only the last committed import state, never canceled staged refresh content.
</success_criteria>

<output>
After completion, create `.planning/phases/42-refresh-teardown-stability/42-02-SUMMARY.md`
</output>
