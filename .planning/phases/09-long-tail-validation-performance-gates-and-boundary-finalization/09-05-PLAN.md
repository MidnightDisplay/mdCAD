---
phase: 09-long-tail-validation-performance-gates-and-boundary-finalization
plan: 05
type: execute
wave: 4
depends_on: [09-03]
files_modified:
  - src/ply_loader.h
  - src/ply_import_job.h
  - .planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/evidence/manual/long-tail-smoke-report.md
autonomous: false
requirements: [VAL-03]
gap_closure: true
must_haves:
  truths:
    - "The LT-VAL03-02 PLY point-cloud sample (`pc1_Wednesday, 17 December 2025 at 15_08_15 Greenwich Mean Time.ply`) imports successfully on Windows Vulkan."
    - "Manual VAL-03 rows for macOS Metal are completed from native execution (not left BLOCKED)."
    - "VAL-03 report contains explicit PASS/FAIL outcomes for serializer/import/undo/editor on both required native targets."
  artifacts:
    - path: "src/ply_loader.h"
      provides: "PLY parser/loader behavior used by point-cloud import"
      contains: "ply_parse_vertices_chunk"
    - path: "src/ply_import_job.h"
      provides: "Runtime point-cloud import workflow and error handling"
      contains: "ply_import_job_tick"
    - path: ".planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/evidence/manual/long-tail-smoke-report.md"
      provides: "VAL-03 required workflow outcomes by target"
      contains: "LT-VAL03-02"
  key_links:
    - from: "src/ply_loader.h"
      to: "src/ply_import_job.h"
      via: "chunked parser state consumed by import job tick loop"
      pattern: "ply_parse_vertices_chunk|parse_state.error"
    - from: "manual checklist execution"
      to: "long-tail-smoke-report.md"
      via: "LT-VAL03-01..04 row status updates"
      pattern: "PASS|FAIL|BLOCKED"
---

<objective>
Close the VAL-03 verification gaps by fixing the known PLY import regression and completing required native manual evidence.

Purpose: Restore end-to-end import confidence and remove host-blocked manual rows that currently prevent phase closure.
Output: Updated PLY import implementation (if needed) and a fully populated VAL-03 smoke report for Windows + macOS required workflows.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/09-CONTEXT.md
@.planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/09-VERIFICATION.md
@.planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/evidence/manual/long-tail-smoke-checklist.md
@.planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/evidence/manual/long-tail-smoke-report.md
@src/ply_loader.h
@src/ply_import_job.h
@src/ui/ui_scene_hierarchy.h
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reproduce and fix LT-VAL03-02 point-cloud import regression for the failing PLY sample</name>
  <read_first>
    - .planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/09-VERIFICATION.md
    - .planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/evidence/manual/long-tail-smoke-report.md
    - src/ply_loader.h
    - src/ply_import_job.h
  </read_first>
  <files>src/ply_loader.h, src/ply_import_job.h</files>
  <behavior>
    - Test 1: The known failing sample (`C:\dev\pc1_Wednesday, 17 December 2025 at 15_08_15 Greenwich Mean Time.ply`) reaches import completion in the point-cloud workflow instead of failing after parse.
    - Test 2: Existing control sample (`C:\dev\1m.ply`) remains import-successful (no regression introduced).
  </behavior>
  <action>Implement a focused parser/import-job fix for LT-VAL03-02 only, preserving current staged scope and architecture per D-11. Keep failure semantics explicit per D-03 (do not mask parse/runtime errors). Do not introduce unrelated importer refactors; limit changes to the minimum code path that causes the post-parse failure for the named sample.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD mdcad_math_harness &amp;&amp; cmake --build build-vulkan --config Release --target math-validation</automated>
  </verify>
  <acceptance_criteria>Code paths used by PLY point-cloud import are updated with a targeted fix and project validation targets still build/run without new failures.</acceptance_criteria>
  <done>Regression fix is implemented and ready for manual LT-VAL03-02 re-check on required targets.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Re-run Windows manual LT-VAL03-02 import check and update VAL-03 report</name>
  <read_first>
    - .planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/evidence/manual/long-tail-smoke-checklist.md
    - .planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/evidence/manual/long-tail-smoke-report.md
  </read_first>
  <files>.planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/evidence/manual/long-tail-smoke-report.md</files>
  <action>Run the Windows Vulkan manual import check for both the previously failing sample and control sample, then update LT-VAL03-02 row and notes in the smoke report with truthful PASS/FAIL status per D-03 and end-to-end scope per D-07.</action>
  <verify>
    <automated>rg -n "LT-VAL03-02|pc1_Wednesday|1m\\.ply|PASS|FAIL|BLOCKED" .planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/evidence/manual/long-tail-smoke-report.md</automated>
  </verify>
  <what-built>Updated Windows evidence row for the required import regression workflow in the canonical VAL-03 report.</what-built>
  <how-to-verify>
    1) Launch `.\build-vulkan\bin\Release\mdCAD.exe`.
    2) Import `C:\dev\pc1_Wednesday, 17 December 2025 at 15_08_15 Greenwich Mean Time.ply` using the same LT-VAL03-02 workflow path.
    3) Import control sample `C:\dev\1m.ply` in the same mode/options.
    4) Record exact outcome and issue notes in LT-VAL03-02 row (PASS if regression resolved, otherwise FAIL with concrete error observation).
  </how-to-verify>
  <resume-signal>Reply with "approved" and the updated LT-VAL03-02 status, or provide the observed failure text.</resume-signal>
  <acceptance_criteria>Windows LT-VAL03-02 row is no longer stale and reflects current post-fix behavior with concrete evidence notes and explicit `PASS|FAIL` status.</acceptance_criteria>
  <done>Windows import-regression evidence is current and auditable.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Execute required macOS manual serializer/import/undo/editor rows and finalize VAL-03 status</name>
  <read_first>
    - docs/QUICKSTART.md
    - .planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/evidence/manual/long-tail-smoke-checklist.md
    - .planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/evidence/manual/long-tail-smoke-report.md
  </read_first>
  <files>.planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/evidence/manual/long-tail-smoke-report.md</files>
  <action>On a native macOS Metal host, execute LT-VAL03-01..04 workflows and replace BLOCKED rows with real PASS/FAIL outcomes. This directly closes the verification gap for missing required-target manual evidence per D-06 and D-07, and must retain D-03 blocking semantics if any required row fails.</action>
  <verify>
    <automated>rg -n "LT-VAL03-01|LT-VAL03-02|LT-VAL03-03|LT-VAL03-04|macOS Metal|BLOCKED|PASS|FAIL|Overall status" .planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/evidence/manual/long-tail-smoke-report.md</automated>
  </verify>
  <how-to-verify>
    1) On macOS, build and launch `./build/bin/mdCAD`.
    2) Execute serializer/import/undo/editor checklist items exactly as listed for LT-VAL03-01..04.
    3) Update macOS column statuses in the report (no placeholder values).
    4) Recompute and update report `Overall status` from required-row outcomes.
  </how-to-verify>
  <resume-signal>Reply with macOS evidence commit hash (or paste completed row statuses) to resume execution.</resume-signal>
  <acceptance_criteria>All required macOS rows are executed and documented with concrete status/notes; unresolved rows are not left as host-unavailable placeholders; rows contain explicit `PASS|FAIL` status.</acceptance_criteria>
  <done>macOS side of VAL-03 is fully populated with native evidence.</done>
</task>

<task type="auto">
  <name>Task 4: Remediate any remaining VAL-03 failures and rerun manual checks to all-PASS</name>
  <read_first>
    - .planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/evidence/manual/long-tail-smoke-report.md
    - .planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/evidence/manual/long-tail-smoke-checklist.md
    - .planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/09-VERIFICATION.md
    - src/scene_serializer.h
    - src/undo_redo.h
    - src/undo_redo_exec.h
    - src/ui/ui_entity_inspector.h
    - src/ui/ui_scene_hierarchy.h
  </read_first>
  <files>src/ply_loader.h, src/ply_import_job.h, src/scene_serializer.h, src/undo_redo.h, src/undo_redo_exec.h, src/ui/ui_entity_inspector.h, src/ui/ui_scene_hierarchy.h, .planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/evidence/manual/long-tail-smoke-report.md</files>
  <action>If any required VAL-03 row remains `FAIL`, keep this plan open. Apply targeted, in-scope fixes only for failing workflow IDs using this bounded mapping: LT-VAL03-01 -> `src/scene_serializer.h`; LT-VAL03-02 -> `src/ply_loader.h`/`src/ply_import_job.h`; LT-VAL03-03 -> `src/undo_redo.h`/`src/undo_redo_exec.h`; LT-VAL03-04 -> `src/ui/ui_entity_inspector.h`/`src/ui/ui_scene_hierarchy.h`. Rerun only the affected manual checks on required targets, then update the report until all required rows are `PASS` and `Overall status` is `PASS`.</action>
  <verify>
    <automated>rg -n "LT-VAL03-01|LT-VAL03-02|LT-VAL03-03|LT-VAL03-04|Windows Vulkan|macOS Metal|Overall status" .planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/evidence/manual/long-tail-smoke-report.md &amp;&amp; rg -n "Overall status.*PASS" .planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/evidence/manual/long-tail-smoke-report.md</automated>
  </verify>
  <acceptance_criteria>Required VAL-03 rows for both Windows and macOS are all `PASS`, and report `Overall status` is `PASS`.</acceptance_criteria>
  <done>VAL-03 is closed with auditable dual-target manual PASS evidence.</done>
</task>

</tasks>

<verification>
Confirm that:
- LT-VAL03-02 Windows row reflects post-fix behavior for the named regression file,
- macOS LT-VAL03-01..04 rows are populated with real execution outcomes,
- report overall status aligns with D-03 blocking policy.
</verification>

<success_criteria>
- Verification gap item “LT-VAL03-02 import FAIL” is resolved to PASS on Windows Vulkan.
- Verification gap item “macOS manual rows blocked” is closed by real native execution evidence.
- LT-VAL03-01..04 required rows for both Windows Vulkan and macOS Metal are PASS, and report `Overall status` is PASS.
</success_criteria>

<output>
After completion, create `.planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/09-05-SUMMARY.md`
</output>

