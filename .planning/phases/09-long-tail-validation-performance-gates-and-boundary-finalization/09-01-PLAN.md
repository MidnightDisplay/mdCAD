---
phase: 09-long-tail-validation-performance-gates-and-boundary-finalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/math_harness.c
  - .planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/evidence/coverage/harness-coverage-map.md
  - .planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/evidence/coverage/compare-strict.txt
autonomous: true
requirements: [VAL-01]
must_haves:
  truths:
    - "Strict compare coverage explicitly maps long-tail serializer/import/undo/editor touchpoints."
    - "Strict compare gate passes on the required parity cases."
    - "Coverage decisions are auditable and aligned to harness-first gating."
  artifacts:
    - path: "src/math_harness.c"
      provides: "Compare-case catalog covering required long-tail parity surfaces"
      contains: "mdcad_compare_cases"
    - path: ".planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/evidence/coverage/harness-coverage-map.md"
      provides: "VAL-01 touchpoint-to-case traceability map"
      contains: "VAL-01 coverage map"
    - path: ".planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/evidence/coverage/compare-strict.txt"
      provides: "Strict compare execution proof"
      contains: "COMPARE PASS"
  key_links:
    - from: "src/math_harness.c"
      to: "mdcad_math_harness --mode compare --strict"
      via: "mdcad_compare_cases execution"
      pattern: "COMPARE PASS|COMPARE FAIL"
    - from: "harness-coverage-map.md"
      to: "VAL-01"
      via: "touchpoint -> compare/manual mapping rows"
      pattern: "serializer|import|undo|editor"
---

<objective>
Close `VAL-01` by proving that strict compare coverage explicitly includes long-tail migration touchpoints and that required parity cases pass.

Purpose: Satisfy harness-first closure policy and prevent ambiguous “coverage assumed” sign-off.
Output: Updated harness compare catalog (only if gaps are real), strict compare output artifact, and a concrete coverage map.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/09-CONTEXT.md
@.planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/09-RESEARCH.md
@.planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/09-VALIDATION.md
@src/math_harness.c
@src/CMakeLists.txt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Audit and close strict compare coverage gaps for long-tail touchpoints</name>
  <read_first>
    - .planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/09-CONTEXT.md
    - .planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/09-RESEARCH.md
    - src/math_harness.c
  </read_first>
  <files>src/math_harness.c</files>
  <behavior>
    - Test 1: `mdcad_math_harness --list` includes compare IDs that cover serializer/import/undo/editor-required parity surfaces.
    - Test 2: `mdcad_math_harness --mode compare --strict` exits successfully with no new FAIL rows for required cases.
  </behavior>
  <action>Run a coverage audit against `VAL-01` touchpoints and update `mdcad_compare_cases` only where true gaps exist, keeping existing harness architecture unchanged per D-01 and no scope creep per D-11. If research suggests broader refactors, do not apply them; retain harness-first pattern per D-02. Keep compare cases behavior-focused and parity-oriented (not aggregate perf logic, per D-04).</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdcad_math_harness &amp;&amp; .\build-vulkan\bin\Release\mdcad_math_harness.exe --list &amp;&amp; .\build-vulkan\bin\Release\mdcad_math_harness.exe --mode compare --strict</automated>
  </verify>
  <acceptance_criteria>Harness compare catalog includes all required long-tail parity touchpoints and strict compare runs green on required cases.</acceptance_criteria>
  <done>Any real VAL-01 compare-case gaps are closed in `src/math_harness.c`, with strict compare passing.</done>
</task>

<task type="auto">
  <name>Task 2: Produce VAL-01 coverage/evidence artifacts for auditability</name>
  <read_first>
    - .planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/09-RESEARCH.md
    - .planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/09-VALIDATION.md
    - src/math_harness.c
  </read_first>
  <files>.planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/evidence/coverage/harness-coverage-map.md, .planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/evidence/coverage/compare-strict.txt</files>
  <action>Create `harness-coverage-map.md` mapping each long-tail touchpoint (serializer/import/undo/editor) to compare case IDs or explicit manual fallback links, and capture fresh strict compare output in `compare-strict.txt`. Annotate mapping rationale using D-02 (practical gate style) and D-03 (blocking failures). Do not include deferred platform-expansion work (PLAT-01/PLAT-02) per D-11.</action>
  <verify>
    <automated>.\build-vulkan\bin\Release\mdcad_math_harness.exe --mode compare --strict | Out-File -Encoding utf8 .planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/evidence/coverage/compare-strict.txt; rg -n "serializer|import|undo|editor|VAL-01" .planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/evidence/coverage/harness-coverage-map.md</automated>
  </verify>
  <acceptance_criteria>`harness-coverage-map.md` explicitly traces required touchpoints, and `compare-strict.txt` contains current strict compare results.</acceptance_criteria>
  <done>VAL-01 traceability is explicit, reproducible, and backed by fresh strict compare evidence.</done>
</task>

</tasks>

<verification>
Run strict compare and confirm:
- no unresolved required-case FAIL rows,
- coverage map includes serializer/import/undo/editor mappings,
- artifacts exist under phase 09 evidence/coverage.
</verification>

<success_criteria>
- `VAL-01` has explicit coverage mapping and passing strict compare evidence.
- Harness-first closure policy is preserved with no out-of-scope framework changes.
</success_criteria>

<output>
After completion, create `.planning/phases/09-long-tail-validation-performance-gates-and-boundary-finalization/09-01-SUMMARY.md`.
</output>

