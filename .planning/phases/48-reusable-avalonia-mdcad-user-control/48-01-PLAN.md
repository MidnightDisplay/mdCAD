---
phase: 48-reusable-avalonia-mdcad-user-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - samples/avalonia-mdcad-control/MdCad.Avalonia.Control.csproj
  - samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml
  - samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs
  - samples/avalonia-mdcad-control/Host/EmbedNativeControlHost.cs
  - samples/avalonia-mdcad-control/Host/MdCadPresentationMode.cs
autonomous: true
requirements:
  - P48-01
must_haves:
  truths:
    - "A Windows-only Avalonia control library exists and builds independently from the sample host."
    - "The reusable control exposes bindable startup/configuration properties instead of a constructor-only options object."
    - "The native placeholder-HWND seam is owned by the reusable library rather than remaining trapped inside `MainWindow.axaml.cs`."
  artifacts:
    - path: "samples/avalonia-mdcad-control/MdCad.Avalonia.Control.csproj"
      provides: "Windows-only Avalonia control library project."
      contains: "net8.0-windows10.0.19041.0"
    - path: "samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs"
      provides: "Public control contract with bindable properties and start/stop surface."
      contains: "StyledProperty"
    - path: "samples/avalonia-mdcad-control/Host/EmbedNativeControlHost.cs"
      provides: "Reusable Win32 `NativeControlHost` placeholder seam."
      contains: "CreateNativeControlCore"
  key_links:
    - from: "samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs"
      to: "src/app_launch_config.h"
      via: "property contract reserved for the existing `--embedded` / `--parent-hwnd` / `--jsonl` launch model"
      pattern: "JsonlPath|StartupLiveRefreshEnabled|AutoStart|PresentationMode"
    - from: "samples/avalonia-mdcad-control/Host/EmbedNativeControlHost.cs"
      to: "samples/avalonia-host/MainWindow.axaml.cs"
      via: "same HWND placeholder create/destroy behavior proven in Phase 47"
      pattern: "CreateNativeControlCore|DestroyNativeControlCore"
---

<objective>
Create the reusable Avalonia control library foundation and public control contract that external Windows apps will reference directly.

Purpose: P48-01 must land before packaging or lifecycle work; the host/proof code from Phases 43-47 needs a reusable home that preserves the existing separate-process, no-IPC, Windows-only boundary.
Output: A new `samples/avalonia-mdcad-control` project with `MdCadEmbeddedControl`, the public bindable property surface, and an extracted `EmbedNativeControlHost` seam.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/48-reusable-avalonia-mdcad-user-control/48-CONTEXT.md
@.planning/phases/48-reusable-avalonia-mdcad-user-control/48-RESEARCH.md
@.planning/phases/48-reusable-avalonia-mdcad-user-control/48-VALIDATION.md
@.planning/phases/43-embed-contract-child-window-bootstrap/43-CONTEXT.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-CONTEXT.md
@.planning/phases/47-sample-host-workflow-proof/47-02-SUMMARY.md
@samples/avalonia-host/MainWindow.axaml.cs
@src/app_launch_config.h

<interfaces>
From samples/avalonia-host/MainWindow.axaml.cs:
```csharp
public sealed class EmbedNativeControlHost : NativeControlHost
{
    public event Action<IntPtr>? PlaceholderHandleReady;
    public IntPtr PlaceholderHandle { get; private set; }
    public void ClearPlaceholderHandle();
}
```

From src/app_launch_config.h:
```c
typedef struct {
    bool embedded;
    uintptr_t parent_hwnd_value;
    bool startup_jsonl_live_refresh;
    char startup_jsonl_path[MDCAD_STARTUP_JSONL_PATH_MAX];
} mdcad_launch_config_t;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Windows-only reusable control project and public bindable contract</name>
  <read_first>
    - .planning/phases/48-reusable-avalonia-mdcad-user-control/48-CONTEXT.md
    - .planning/phases/48-reusable-avalonia-mdcad-user-control/48-RESEARCH.md
    - .planning/phases/48-reusable-avalonia-mdcad-user-control/48-VALIDATION.md
    - samples/avalonia-host/MainWindow.axaml.cs
    - src/app_launch_config.h
  </read_first>
  <files>samples/avalonia-mdcad-control/MdCad.Avalonia.Control.csproj, samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml, samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs, samples/avalonia-mdcad-control/Host/MdCadPresentationMode.cs</files>
  <action>Create `samples/avalonia-mdcad-control/MdCad.Avalonia.Control.csproj` as a Windows-only Avalonia class library targeting `net8.0-windows10.0.19041.0` on the repo-resolved Avalonia 11.3.x line from research; do not turn Phase 48 into an Avalonia upgrade phase. Add `MdCadEmbeddedControl.axaml` + code-behind as the reusable public surface and register bindable `StyledProperty`s for `JsonlPath`, `StartupLiveRefreshEnabled`, `AutoStart` (default `true` per D-01 and D-02), and `PresentationMode` (default `Sealed` per D-09 and D-10). Add a public `MdCadPresentationMode` enum with exactly `Sealed` and `Diagnostic`. Expose explicit `StartAsync` / `StopAsync` entrypoints for the later explicit-start flow per D-01, but keep implementation skeletal in this plan. Do not introduce a constructor-only options object, public runtime-path override, IPC/events API, or any non-Windows target.</action>
  <verify>
    <automated>dotnet build samples/avalonia-mdcad-control/MdCad.Avalonia.Control.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `MdCad.Avalonia.Control.csproj` targets `net8.0-windows10.0.19041.0`
    - `MdCadEmbeddedControl.axaml.cs` contains `JsonlPathProperty`, `StartupLiveRefreshEnabledProperty`, `AutoStartProperty`, and `PresentationModeProperty`
    - `AutoStart` defaults to `true`
    - `PresentationMode` defaults to `MdCadPresentationMode.Sealed`
    - public start/stop methods exist for later explicit-start wiring
  </acceptance_criteria>
  <done>The reusable control project builds on its own and already encodes the locked bindable/public surface without reopening the no-IPC boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Extract the reusable placeholder-HWND host seam into the new library</name>
  <read_first>
    - samples/avalonia-host/MainWindow.axaml.cs
    - .planning/phases/44-embedded-resize-focus-viewer-layout/44-CONTEXT.md
    - .planning/phases/47-sample-host-workflow-proof/47-02-SUMMARY.md
  </read_first>
  <files>samples/avalonia-mdcad-control/Host/EmbedNativeControlHost.cs, samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs</files>
  <action>Copy the proven `EmbedNativeControlHost` implementation from `samples/avalonia-host/MainWindow.axaml.cs` into `samples/avalonia-mdcad-control/Host/EmbedNativeControlHost.cs` so the reusable library owns Win32 placeholder HWND creation/destruction, `PlaceholderHandleReady`, and `ClearPlaceholderHandle`. Wire `MdCadEmbeddedControl` to compose this internal host rather than keeping the placeholder seam local to `MainWindow`. Preserve the exact Windows-only `NativeControlHost` behavior established in Phases 43-47: create a child placeholder HWND, destroy it on teardown, and do not add any host-side cross-process focus forcing or standalone fallback logic.</action>
  <verify>
    <automated>dotnet build samples/avalonia-mdcad-control/MdCad.Avalonia.Control.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `Host/EmbedNativeControlHost.cs` contains `PlaceholderHandleReady`, `ClearPlaceholderHandle`, `CreateNativeControlCore`, and `DestroyNativeControlCore`
    - `MdCadEmbeddedControl.axaml.cs` instantiates or references `EmbedNativeControlHost`
    - no public API exposes test-mode hooks such as `invalid-parent` or `destroy-after-attach`
  </acceptance_criteria>
  <done>The new library owns the HWND placeholder seam required for later launch/relaunch work, and that seam still matches the already-validated child-window lifecycle behavior.</done>
</task>

</tasks>

<verification>
- `dotnet build samples/avalonia-mdcad-control/MdCad.Avalonia.Control.csproj -c Release`
- Confirm the public control contract contains only bindable properties, presentation mode enum, and explicit start/stop methods needed for later plans.
</verification>

<success_criteria>
- A consumer-ready Windows-only Avalonia control project exists under `samples/avalonia-mdcad-control`.
- The control contract reflects D-01, D-02, D-03, D-09, and D-10 without adding deferred IPC/runtime-path scope.
- The placeholder-HWND seam is reusable from the library instead of being trapped inside the sample host window.
</success_criteria>

<output>
After completion, create `.planning/phases/48-reusable-avalonia-mdcad-user-control/48-01-SUMMARY.md`
</output>
