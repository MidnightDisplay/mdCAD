---
phase: 48-reusable-avalonia-mdcad-user-control
plan: 03
type: execute
wave: 3
depends_on:
  - 48-01
  - 48-02
files_modified:
  - samples/avalonia-mdcad-control.tests/MdCad.Avalonia.Control.Tests.csproj
  - samples/avalonia-mdcad-control.tests/MdCadLaunchSnapshotTests.cs
  - samples/avalonia-mdcad-control.tests/MdCadSessionCoordinatorTests.cs
  - samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs
  - samples/avalonia-mdcad-control/Host/MdCadLaunchSnapshot.cs
  - samples/avalonia-mdcad-control/Host/MdCadSessionCoordinator.cs
autonomous: true
requirements:
  - P48-03
  - P48-04
  - P48-05
must_haves:
  truths:
    - "A XAML-dropped control auto-starts by default once both bindings and the placeholder HWND are ready."
    - "Hosts can opt out with `AutoStart=false` and start/stop the embedded session explicitly."
    - "Changing launch-affecting properties while mdCAD is running produces one serialized relaunch using the newest snapshot only."
    - "Unset `JsonlPath` launches silently without `--jsonl`, while bad requested paths remain visibly tracked instead of being swallowed."
  artifacts:
    - path: "samples/avalonia-mdcad-control/Host/MdCadLaunchSnapshot.cs"
      provides: "Pure launch snapshot and JSONL path normalization rules."
      contains: "JsonlPath"
    - path: "samples/avalonia-mdcad-control/Host/MdCadSessionCoordinator.cs"
      provides: "Serialized start/stop/relaunch coordinator."
      contains: "SemaphoreSlim"
    - path: "samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs"
      provides: "Property-change wiring from the public control surface into the coordinator."
      contains: "AutoStart"
  key_links:
    - from: "samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs"
      to: "samples/avalonia-mdcad-control/Host/MdCadSessionCoordinator.cs"
      via: "control property change handlers and explicit StartAsync/StopAsync calls"
      pattern: "RequestReconcile|StartAsync|StopAsync"
    - from: "samples/avalonia-mdcad-control/Host/MdCadSessionCoordinator.cs"
      to: "samples/avalonia-mdcad-control/Host/MdCadRuntimeResolver.cs"
      via: "launches always resolve the copied pinned runtime bundle before starting the process"
      pattern: "Resolve|WorkingDirectory|mdcad-runtime"
---

<objective>
Implement the control-owned launch/relaunch lifecycle so the reusable control actually manages mdCAD startup, explicit start/stop, and property-driven relaunches without IPC.

Purpose: P48-03, P48-04, and P48-05 are the core reusable-control behaviors. They must preserve the Phase 47 lifecycle/orphan seams while switching control ownership from the sample window to the reusable control itself.
Output: A managed test seam, `MdCadLaunchSnapshot`, `MdCadSessionCoordinator`, and `MdCadEmbeddedControl` property wiring for auto-start, explicit start, and coalesced relaunch.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/48-reusable-avalonia-mdcad-user-control/48-CONTEXT.md
@.planning/phases/48-reusable-avalonia-mdcad-user-control/48-RESEARCH.md
@.planning/phases/48-reusable-avalonia-mdcad-user-control/48-VALIDATION.md
@.planning/phases/47-sample-host-workflow-proof/47-02-SUMMARY.md
@samples/avalonia-host/MainWindow.axaml.cs
@src/app_launch_config.h
@src/embed_layout_state.h
@src/imgui_storage.h

<interfaces>
Public control contract from Plan 48-01:
```csharp
public static readonly StyledProperty<string?> JsonlPathProperty;
public static readonly StyledProperty<bool> StartupLiveRefreshEnabledProperty;
public static readonly StyledProperty<bool> AutoStartProperty;
public static readonly StyledProperty<MdCadPresentationMode> PresentationModeProperty;
public Task StartAsync();
public Task StopAsync();
```

From src/app_launch_config.h:
```c
static inline bool mdcad_launch_config_path_is_absolute(const char* path);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the managed test seam and lock launch-snapshot semantics before control wiring</name>
  <read_first>
    - .planning/phases/48-reusable-avalonia-mdcad-user-control/48-CONTEXT.md
    - .planning/phases/48-reusable-avalonia-mdcad-user-control/48-RESEARCH.md
    - .planning/phases/48-reusable-avalonia-mdcad-user-control/48-VALIDATION.md
    - src/app_launch_config.h
    - samples/avalonia-host/MainWindow.axaml.cs
  </read_first>
  <files>samples/avalonia-mdcad-control.tests/MdCad.Avalonia.Control.Tests.csproj, samples/avalonia-mdcad-control.tests/MdCadLaunchSnapshotTests.cs, samples/avalonia-mdcad-control/Host/MdCadLaunchSnapshot.cs</files>
  <behavior>
    - Test 1: empty or whitespace `JsonlPath` produces no `--jsonl` argument and no warning per D-14.
    - Test 2: a requested relative/non-canonicalizable path produces a visible warning and omits `--jsonl` because the native contract only accepts absolute paths per D-16.
    - Test 3: an absolute-but-missing/unreadable path is preserved as the requested `--jsonl` value while surfacing a warning so the viewer still launches usable per D-15.
  </behavior>
  <action>Create `samples/avalonia-mdcad-control.tests` as a lightweight .NET test project referencing the control library. Write the failing tests first, then implement `MdCadLaunchSnapshot` so it captures the launch-affecting values (`JsonlPath`, live-refresh flag, and any derived warning text) without touching process code yet. The snapshot helper must normalize/validate the bound JSONL request in one place using the Phase 45 absolute-path contract from `src/app_launch_config.h`. Keep the warning model internal to the control library — do not invent machine-readable host IPC/events — and reserve it for later sealed/diagnostic rendering.</action>
  <verify>
    <automated>dotnet test samples/avalonia-mdcad-control.tests/MdCad.Avalonia.Control.Tests.csproj -c Release --filter MdCadLaunchSnapshotTests</automated>
  </verify>
  <acceptance_criteria>
    - the repo contains a runnable `samples/avalonia-mdcad-control.tests` project
    - `MdCadLaunchSnapshotTests` cover unset, invalid-relative, and absolute-missing JSONL requests
    - `MdCadLaunchSnapshot.cs` centralizes JSONL launch-argument and warning decisions instead of scattering them through property setters
  </acceptance_criteria>
  <done>The launch snapshot rules are executable, tested, and ready for the real control/coordinator to consume.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the serialized relaunch coordinator and wire the control to it</name>
  <read_first>
    - samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs
    - samples/avalonia-mdcad-control/Host/MdCadRuntimeResolver.cs
    - samples/avalonia-host/MainWindow.axaml.cs
    - .planning/phases/47-sample-host-workflow-proof/47-02-SUMMARY.md
    - .planning/phases/48-reusable-avalonia-mdcad-user-control/48-RESEARCH.md
    - .planning/phases/48-reusable-avalonia-mdcad-user-control/48-VALIDATION.md
  </read_first>
  <files>samples/avalonia-mdcad-control.tests/MdCadSessionCoordinatorTests.cs, samples/avalonia-mdcad-control/MdCadEmbeddedControl.axaml.cs, samples/avalonia-mdcad-control/Host/MdCadSessionCoordinator.cs</files>
  <behavior>
    - Test 1: `AutoStart=true` waits for visual attachment + placeholder readiness, then launches once; `AutoStart=false` stays idle until `StartAsync` is called.
    - Test 2: changing `JsonlPath` and/or `StartupLiveRefreshEnabled` while running causes one serialized relaunch using the newest snapshot only per D-04.
    - Test 3: `StopAsync`/close tears down the active process bookkeeping cleanly and recreates the placeholder before the next launch attempt.
  </behavior>
  <action>Write failing coordinator tests first, then implement `MdCadSessionCoordinator` using one lifecycle gate (for example `SemaphoreSlim`) plus generation-based coalescing, as recommended by research. Reuse the proven Phase 47 lifecycle behaviors: resolve the runtime from `MdCadRuntimeResolver`, launch mdCAD as a separate process with `--embedded`, `--parent-hwnd`, optional `--jsonl`, and optional `--jsonl-live-refresh`, wait for graceful exit on teardown, recreate the placeholder/native host seam before relaunch, and never add IPC or host-side cross-process focus forcing. Wire `MdCadEmbeddedControl.axaml.cs` so only launch-affecting properties (`JsonlPath`, `StartupLiveRefreshEnabled`) trigger reconcile/relaunch, while `AutoStart` controls initial start semantics and explicit `StartAsync` / `StopAsync` forward into the coordinator. Preserve the D-14/D-15/D-16 warning outcomes from `MdCadLaunchSnapshot` rather than silently swallowing bad requested paths.</action>
  <verify>
    <automated>dotnet test samples/avalonia-mdcad-control.tests/MdCad.Avalonia.Control.Tests.csproj -c Release &amp;&amp; dotnet build samples/avalonia-host/AvaloniaHost.csproj -c Release</automated>
  </verify>
  <acceptance_criteria>
    - `MdCadSessionCoordinatorTests` cover auto-start, explicit start, and coalesced relaunch behavior
    - `MdCadSessionCoordinator.cs` serializes lifecycle work instead of launching directly from property setters
    - `MdCadEmbeddedControl.axaml.cs` calls into the coordinator for `StartAsync`, `StopAsync`, and property-change reconciliation
    - launch commands still use only the existing CLI flags and copied runtime bundle
  </acceptance_criteria>
  <done>The reusable control now owns mdCAD session lifecycle behavior, including default auto-start, explicit start/stop, and safe property-driven relaunch.</done>
</task>

</tasks>

<verification>
- `dotnet test samples/avalonia-mdcad-control.tests/MdCad.Avalonia.Control.Tests.csproj -c Release`
- `dotnet build samples/avalonia-host/AvaloniaHost.csproj -c Release`
- Confirm coordinator code uses serialized snapshot reconciliation rather than immediate per-setter launches.
</verification>

<success_criteria>
- The library has automated coverage for launch snapshot semantics and relaunch coordination.
- `MdCadEmbeddedControl` defaults to auto-start but also supports `AutoStart=false` plus explicit `StartAsync`.
- Launch-affecting property changes are coalesced into one safe relaunch using the newest requested snapshot.
</success_criteria>

<output>
After completion, create `.planning/phases/48-reusable-avalonia-mdcad-user-control/48-03-SUMMARY.md`
</output>
