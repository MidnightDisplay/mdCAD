---
phase: 22-solver-trigger-recalculate-determinism
plan: 03
type: execute
wave: 3
depends_on: [22-02]
files_modified:
  - src/ui/ui_entity_inspector.h
  - src/ecs/ecs_scene.h
  - src/tests/scene_solver_pass_policy_test.c
autonomous: false
requirements: [SRLV-02, SRLV-03, SRLV-05]
must_haves:
  truths:
    - "User can view and edit position tolerance, angle tolerance, and max passes in sketch solver UI."
    - "Max passes defaults to 400 in UI and maps to scene solver config used by recalculate."
    - "Manual recalc from UI remains immediate/authoritative and diagnostics remain explicit."
    - "UI does not re-implement solver policy and remains a thin caller into scene APIs."
  artifacts:
    - path: "src/ui/ui_entity_inspector.h"
      provides: "Solver config controls for tolerance and pass cap + manual recalc wiring"
    - path: "src/ecs/ecs_scene.h"
      provides: "Set/get API surface consumed by inspector controls"
    - path: "src/tests/scene_solver_pass_policy_test.c"
      provides: "Coverage that UI-exposed config values affect pass-loop behavior"
  key_links:
    - from: "src/ui/ui_entity_inspector.h"
      to: "src/ecs/ecs_scene.h"
      via: "scene_solver_set/get config calls"
      pattern: "scene_solver_.*tolerance|scene_solver_.*max_pass"
    - from: "Recalculate Sketch button"
      to: "scene_solver_request_recalculate"
      via: "button click handler"
      pattern: "igButton\\(\"Recalculate Sketch\".*scene_solver_request_recalculate"
---

<objective>
Expose Phase 22 solver pass/tolerance controls in inspector while preserving scene-owned solver authority and validating user-visible runtime behavior.

Purpose: Complete SRLV-03 UI contract and ensure manual recalc authority + diagnostics remain consistent with D-04/D-05/D-06/D-07/D-12.
Output: Inspector controls wired to scene solver config APIs, updated targeted tests, and final human verification checkpoint.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-solver-trigger-recalculate-determinism/22-CONTEXT.md
@.planning/phases/22-solver-trigger-recalculate-determinism/22-RESEARCH.md
@.planning/phases/22-solver-trigger-recalculate-determinism/22-VALIDATION.md
@.planning/phases/22-solver-trigger-recalculate-determinism/22-02-PLAN.md
@src/ui/ui_entity_inspector.h
@src/ecs/ecs_scene.h
@src/tests/scene_solver_pass_policy_test.c
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add inspector controls for position/angle tolerances and max passes</name>
  <read_first>
    - src/ui/ui_entity_inspector.h
    - src/ecs/ecs_scene.h
    - .planning/phases/22-solver-trigger-recalculate-determinism/22-CONTEXT.md
  </read_first>
  <files>src/ui/ui_entity_inspector.h, src/ecs/ecs_scene.h</files>
  <behavior>
    - Solver panel shows editable position tolerance and angle tolerance fields (D-05).
    - Solver panel shows editable max passes field defaulting to 400 (D-06).
    - Recalculate button remains thin caller to scene recalc API and does not duplicate solve logic (D-12).
  </behavior>
  <action>Add inspector UI controls directly in the existing Solver section using stable ImGui IDs (`##sketch_solver_position_tol`, `##sketch_solver_angle_tol`, `##sketch_solver_max_passes`). Route value read/write through scene-level solver config accessors in `ecs_scene.h` (create accessors if missing). Keep manual recalc action calling `scene_solver_request_recalculate(scene, e)` exactly once per click and preserve diagnostic clear button semantics unchanged (D-11).</action>
  <acceptance_criteria>
    - `git --no-pager grep -n "sketch_solver_position_tol\\|sketch_solver_angle_tol\\|sketch_solver_max_passes" src/ui/ui_entity_inspector.h` returns all three control IDs.
    - `git --no-pager grep -n "scene_solver_.*position.*tolerance\\|scene_solver_.*angle.*tolerance\\|scene_solver_.*max_pass" src/ecs/ecs_scene.h` returns config accessors used by UI.
    - `git --no-pager grep -n "Recalculate Sketch" src/ui/ui_entity_inspector.h` still routes to `scene_solver_request_recalculate`.
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD scene_solver_pass_policy && ctest -R scene_solver_pass_policy --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>Inspector displays and edits solver tolerances + max passes through scene APIs with no policy leakage into UI layer.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify solver controls and deterministic recalc UX</name>
  <read_first>
    - src/ui/ui_entity_inspector.h
    - .planning/phases/22-solver-trigger-recalculate-determinism/22-VALIDATION.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <what-built>Inspector solver control surface (position tolerance, angle tolerance, max passes default 400) wired to scene solver runtime with authoritative Recalculate behavior.</what-built>
  <action>Execute the manual verification sequence after Task 1 automation completes; capture pass/fail per step and only approve when all required D-04/D-05/D-06/D-08/D-09/D-10 behaviors are observed exactly.</action>
  <how-to-verify>
    1) Launch app and open a sketch in Entity Inspector.
    2) In Solver panel, confirm controls for position tolerance, angle tolerance, and max passes exist; max passes shows 400 by default (D-05, D-06).
    3) Enable Auto-solve, perform rapid sketch mutations, then click Recalculate once; confirm immediate deterministic result and no stale delayed auto solve effect afterward (D-04).
    4) Configure a contradictory driving LENGTH/ANGLE setup and run Recalculate; confirm explicit failure diagnostic + implication highlighting with no visible geometry mutation (D-09).
    5) Resolve the contradiction and run Recalculate; confirm solve success clears implication immediately and geometry updates in one successful commit outcome (D-08, D-10).
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms all 5 verification steps pass.
    - Any failure report includes exact step number and observed behavior for follow-up fix.
  </acceptance_criteria>
  <verify>
    <automated>ctest -R "scene_solver_trigger|scene_solver_pass_policy|scene_solver_contract|scene_solver_diagnostics|scene_solver_drag" --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <resume-signal>Type "approved" when all checks pass, or provide failed step numbers with observations.</resume-signal>
  <done>Human confirms SRLV-03/SRLV-05 visible behaviors and manual recalc authority in running UI.</done>
</task>

</tasks>

<verification>
Before checkpoint handoff, run:
`ctest -R "scene_solver_trigger|scene_solver_pass_policy|scene_solver_contract|scene_solver_diagnostics|scene_solver_drag" --test-dir build-vulkan -C Release --output-on-failure`
</verification>

<success_criteria>
- Inspector exposes required tolerance/pass controls with max-pass default 400.
- UI remains thin and scene solver keeps runtime authority.
- Human verification confirms deterministic manual recalc and explicit dimensional failure/success outcomes.
</success_criteria>

<output>
After completion, create `.planning/phases/22-solver-trigger-recalculate-determinism/22-03-SUMMARY.md`
</output>

