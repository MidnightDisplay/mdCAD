---
phase: 22-solver-trigger-recalculate-determinism
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - src/components/sketch_comp.h
  - src/ecs/ecs_scene.h
  - src/app.c
autonomous: true
requirements: [SRLV-01, SRLV-02, SRLV-03, SRLV-05]
must_haves:
  truths:
    - "Committed sketch mutations enqueue auto-solve through a 50ms debounced, coalesced scene-owned queue."
    - "Manual recalculate runs immediately and cancels any pending auto-solve from the queue."
    - "Recalculate loop stops only by tolerance success or max-pass cap, default cap 10 with explicit max-pass error reason."
    - "Driving LENGTH/ANGLE solves either commit atomically on success or leave geometry unchanged with explicit failure implication."
    - "Success clears implication immediately while diagnostics ring behavior remains dedupe+cap100+explicit-clear only."
  artifacts:
    - path: "src/components/sketch_comp.h"
      provides: "Per-sketch debounce/tolerance/pass configuration state with defaults"
    - path: "src/ecs/ecs_scene.h"
      provides: "Scene-owned auto queue processing, deterministic pass loop, and driving dimensional solve contract"
      exports: ["scene_solver_request_auto", "scene_solver_request_recalculate", "scene_solver_apply_status"]
    - path: "src/app.c"
      provides: "Per-frame call to scene-owned auto queue processor without moving solver authority to UI"
  key_links:
    - from: "src/app.c"
      to: "src/ecs/ecs_scene.h"
      via: "frame loop call"
      pattern: "scene_solver_process_auto_queue\\(&state\\.ecs_scene"
    - from: "scene_solver_request_recalculate"
      to: "scene_solver_apply_status"
      via: "successful solve path"
      pattern: "scene_solver_apply_status\\(scene, sketch, SKETCH_STATUS_SOLVED\\)"
    - from: "scene_solver_request_recalculate"
      to: "scene_solver_set_failure_implication"
      via: "unsat path for LENGTH/ANGLE or max-pass"
      pattern: "scene_solver_set_failure_implication\\("
---

<objective>
Implement scene-level deterministic solve orchestration: debounced auto queue, authoritative manual recalc override, bounded pass policy, and driving LENGTH/ANGLE atomicity/no-mutation behavior.

Purpose: Make solver outcomes trustworthy, repeatable, and explicit per SRLV-01/02/03/05 and D-01..D-12.
Output: Runtime solver state + orchestration updates in scene/component/app layers.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-solver-trigger-recalculate-determinism/22-CONTEXT.md
@.planning/phases/22-solver-trigger-recalculate-determinism/22-RESEARCH.md
@.planning/phases/22-solver-trigger-recalculate-determinism/22-VALIDATION.md
@.planning/phases/22-solver-trigger-recalculate-determinism/22-01-PLAN.md
@src/components/sketch_comp.h
@src/ecs/ecs_scene.h
@src/app.c

<interfaces>
From src/components/sketch_comp.h (current baseline):
```c
typedef struct {
    bool auto_solve_enabled;
    bool auto_solve_pending;
    uint32_t solve_request_serial;
    uint32_t solve_completed_serial;
    uint64_t last_solve_timestamp_ms;
} SketchComp;
```

From src/ecs/ecs_scene.h (existing contracts):
```c
static inline bool scene_solver_request_auto(ecs_scene_t *scene, ecs_entity_t sketch);
static inline bool scene_solver_request_recalculate(ecs_scene_t *scene, ecs_entity_t sketch);
static inline bool scene_solver_apply_status(ecs_scene_t *scene, ecs_entity_t sketch, sketch_status_t status);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add sketch solver runtime config for debounce/tolerances/max-pass defaults</name>
  <read_first>
    - src/components/sketch_comp.h
    - src/ecs/ecs_scene.h
    - .planning/phases/22-solver-trigger-recalculate-determinism/22-CONTEXT.md
  </read_first>
  <files>src/components/sketch_comp.h, src/ecs/ecs_scene.h</files>
  <behavior>
    - Config defaults include debounce=50ms (D-02), max_passes=10 (D-06), and explicit position/angle tolerances for recalc convergence (D-05).
    - Manual recalc clears pending auto queue metadata before immediate solve run (D-04).
  </behavior>
  <action>Extend `SketchComp` with explicit solver config/state fields required for D-01..D-07 (for example: `auto_solve_debounce_ms`, queue timestamp/token, `solver_position_tolerance`, `solver_angle_tolerance`, `solver_max_passes`). Set defaults in `sketch_comp_default()` to debounce `50`, max passes `10`, and deterministic tolerance constants. Add/adjust scene helpers in `ecs_scene.h` so manual recalc clears pending queue state instead of allowing stale delayed runs.</action>
  <acceptance_criteria>
    - `git --no-pager grep -n "auto_solve_debounce_ms\\|solver_position_tolerance\\|solver_angle_tolerance\\|solver_max_passes" src/components/sketch_comp.h` returns added fields.
    - `git --no-pager grep -n "50\\|10" src/components/sketch_comp.h` shows default debounce/max-pass literals.
    - `git --no-pager grep -n "auto_solve_pending = false" src/ecs/ecs_scene.h` shows manual recalc queue-clear path.
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target scene_solver_contract scene_solver_trigger scene_solver_pass_policy</automated>
  </verify>
  <done>Runtime sketch solver config holds required defaults and recalc queue-clear contract is encoded in scene APIs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement scene-owned debounce processing and deterministic bounded recalc behavior</name>
  <read_first>
    - src/ecs/ecs_scene.h
    - src/app.c
    - src/tests/scene_solver_trigger_test.c
    - src/tests/scene_solver_pass_policy_test.c
    - src/tests/scene_solver_contract_test.c
  </read_first>
  <files>src/ecs/ecs_scene.h, src/app.c</files>
  <behavior>
    - Auto-solve requests coalesce and execute once after 50ms debounce (D-01, D-02, D-03).
    - Manual recalc immediate run cancels queued auto solve (D-04).
    - Recalc iterates deterministically until tolerance success or max-pass error with explicit "max passes reached" diagnostic (D-07).
    - On success implication clears immediately through status sink (D-08).
    - Driving LENGTH/ANGLE follows atomic success commit or unsat no-mutation+diagnostic+implication (D-09, D-10).
    - Diagnostics ring dedupe/cap/clear behavior remains unchanged (D-11) and solve authority remains in `scene_solver_*` (D-12).
  </behavior>
  <action>Add a scene API processor (`scene_solver_process_auto_queue(...)`) that checks sketch pending queue state and executes queued solve only after debounce expiry; call it once per frame from `frame()` in `src/app.c` immediately after scene update while keeping all policy in scene solver code. Refactor `scene_solver_request_recalculate(...)` into deterministic pass-bounded loop using sketch tolerances and max passes, emit explicit max-pass error diagnostic, preserve existing diagnostic ring implementation, and implement LENGTH/ANGLE driving branches with transactional stage+single commit on success and explicit no-mutation implication on unsat.</action>
  <acceptance_criteria>
    - `git --no-pager grep -n "scene_solver_process_auto_queue" src/ecs/ecs_scene.h src/app.c` shows both API definition and frame-loop call.
    - `git --no-pager grep -n "max passes reached" src/ecs/ecs_scene.h` returns explicit diagnostic text.
    - `git --no-pager grep -n "CONSTRAINT_LENGTH\\|CONSTRAINT_ANGLE" src/ecs/ecs_scene.h` returns driving constraint solve paths.
    - `git --no-pager grep -n "scene_solver_clear_failure_implication" src/ecs/ecs_scene.h` confirms success-clear path remains active.
  </acceptance_criteria>
  <verify>
    <automated>ctest -R "scene_solver_trigger|scene_solver_pass_policy|scene_solver_contract|scene_solver_diagnostics|scene_solver_drag" --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>Debounced queue processing, manual override, bounded pass policy, and LENGTH/ANGLE deterministic contracts are implemented and pass targeted solver tests.</done>
</task>

</tasks>

<verification>
Run Phase 22 quick gate:
`ctest -R "scene_solver_trigger|scene_solver_pass_policy|scene_solver_contract|scene_solver_diagnostics|scene_solver_drag" --test-dir build-vulkan -C Release --output-on-failure`
</verification>

<success_criteria>
- Auto queue behavior is debounced/coalesced at 50ms and manual recalc clears queue.
- Solver pass loop uses configurable tolerances and default max-pass=10 with explicit max-pass error diagnostics.
- LENGTH/ANGLE behaviors match atomic-success and unsat-no-mutation contracts.
- Solver authority remains scene-owned; app only invokes processing API once per frame.
</success_criteria>

<output>
After completion, create `.planning/phases/22-solver-trigger-recalculate-determinism/22-02-SUMMARY.md`
</output>

