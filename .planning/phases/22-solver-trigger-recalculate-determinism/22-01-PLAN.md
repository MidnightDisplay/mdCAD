---
phase: 22-solver-trigger-recalculate-determinism
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tests/scene_solver_trigger_test.c
  - src/tests/scene_solver_pass_policy_test.c
  - src/tests/scene_solver_contract_test.c
  - src/CMakeLists.txt
autonomous: true
requirements: [SRLV-01, SRLV-02, SRLV-03, SRLV-05]
must_haves:
  truths:
    - "Developer can run dedicated solver trigger and pass-policy tests from CTest."
    - "Phase 22 behaviors are asserted by executable tests before runtime implementation changes."
    - "Repeated recalc + driving LENGTH/ANGLE contracts are captured in deterministic test cases."
  artifacts:
    - path: "src/tests/scene_solver_trigger_test.c"
      provides: "Debounce/coalescing/manual-override test coverage for D-01/D-02/D-03/D-04"
    - path: "src/tests/scene_solver_pass_policy_test.c"
      provides: "Tolerance-stop and max-pass-stop coverage for D-05/D-06/D-07"
    - path: "src/tests/scene_solver_contract_test.c"
      provides: "Deterministic recalc idempotence and LENGTH/ANGLE atomicity assertions for D-08/D-09/D-10"
    - path: "src/CMakeLists.txt"
      provides: "CTest target registration for new Phase 22 test binaries"
  key_links:
    - from: "src/CMakeLists.txt"
      to: "src/tests/scene_solver_trigger_test.c"
      via: "add_executable + add_test"
      pattern: "add_test\\(NAME scene_solver_trigger COMMAND scene_solver_trigger\\)"
    - from: "src/CMakeLists.txt"
      to: "src/tests/scene_solver_pass_policy_test.c"
      via: "add_executable + add_test"
      pattern: "add_test\\(NAME scene_solver_pass_policy COMMAND scene_solver_pass_policy\\)"
---

<objective>
Create Wave-0 executable coverage for Phase 22 solver contracts so implementation can be validated continuously instead of relying on manual interpretation.

Purpose: Lock phase behavior with deterministic test gates tied to D-01..D-12 before runtime edits.
Output: Two new CTest binaries, extended solver contract tests, and CMake registration.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-solver-trigger-recalculate-determinism/22-CONTEXT.md
@.planning/phases/22-solver-trigger-recalculate-determinism/22-RESEARCH.md
@.planning/phases/22-solver-trigger-recalculate-determinism/22-VALIDATION.md
@src/CMakeLists.txt
@src/tests/scene_solver_contract_test.c
@src/tests/scene_solver_diagnostics_test.c
@src/tests/scene_solver_drag_test.c

<interfaces>
From src/ecs/ecs_scene.h:
```c
static inline bool scene_solver_request_auto(ecs_scene_t *scene, ecs_entity_t sketch);
static inline bool scene_solver_request_recalculate(ecs_scene_t *scene, ecs_entity_t sketch);
static inline bool scene_solver_apply_status(ecs_scene_t *scene, ecs_entity_t sketch, sketch_status_t status);
```

From src/components/sketch_comp.h:
```c
typedef struct {
    bool auto_solve_enabled;
    bool auto_solve_pending;
    uint32_t solve_request_serial;
    uint32_t solve_completed_serial;
    uint64_t last_solve_timestamp_ms;
    uint32_t diagnostics_count;
    uint32_t diagnostics_head;
    sketch_solver_diagnostic_t diagnostics[SKETCH_SOLVER_DIAGNOSTICS_MAX];
} SketchComp;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dedicated trigger/pass-policy test binaries and CTest registration</name>
  <read_first>
    - src/CMakeLists.txt
    - src/tests/scene_solver_contract_test.c
    - src/tests/scene_solver_diagnostics_test.c
    - .planning/phases/22-solver-trigger-recalculate-determinism/22-VALIDATION.md
  </read_first>
  <files>src/tests/scene_solver_trigger_test.c, src/tests/scene_solver_pass_policy_test.c, src/CMakeLists.txt</files>
  <behavior>
    - Test 1: Auto-solve request debounce defaults to 50ms and coalesces burst requests into one pending solve (D-01, D-02, D-03).
    - Test 2: Manual recalc path clears pending auto queue and prevents stale delayed solve from executing (D-04).
    - Test 3: Recalculate stops by tolerance or explicit max-pass reached contract with default cap 10 (D-05, D-06, D-07).
  </behavior>
  <action>Create `scene_solver_trigger_test.c` and `scene_solver_pass_policy_test.c` with deterministic setup helpers mirroring existing scene solver tests. Register both in `src/CMakeLists.txt` using `add_executable(...)`, include dirs, link libs (`libsokol`, `libcimgui`, `flecs_static`, `cjson`), and `add_test(NAME ...)` entries named exactly `scene_solver_trigger` and `scene_solver_pass_policy`.</action>
  <acceptance_criteria>
    - `git --no-pager grep -n "add_test(NAME scene_solver_trigger" src/CMakeLists.txt` returns one registration.
    - `git --no-pager grep -n "add_test(NAME scene_solver_pass_policy" src/CMakeLists.txt` returns one registration.
    - `src/tests/scene_solver_trigger_test.c` and `src/tests/scene_solver_pass_policy_test.c` exist and compile as C11 test targets.
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target scene_solver_trigger scene_solver_pass_policy && ctest -R "scene_solver_trigger|scene_solver_pass_policy" --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>Both new test binaries are registered and executable through CTest by exact target names.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend solver contract tests for deterministic recalc and driving LENGTH/ANGLE contracts</name>
  <read_first>
    - src/tests/scene_solver_contract_test.c
    - src/ecs/ecs_scene.h
    - .planning/phases/22-solver-trigger-recalculate-determinism/22-CONTEXT.md
  </read_first>
  <files>src/tests/scene_solver_contract_test.c</files>
  <behavior>
    - Test 1: Re-running manual recalc on unchanged sketch yields identical status/geometry/serial outcomes (SRLV-02).
    - Test 2: Unsatisfiable driving LENGTH/ANGLE keeps geometry unchanged and yields explicit failure implication contract (D-09).
    - Test 3: Satisfiable driving LENGTH/ANGLE commits geometry atomically in one successful solve completion (D-10).
  </behavior>
  <action>Add focused test cases to `scene_solver_contract_test.c` for idempotent recalc and driving dimensional constraints using deterministic fixture data and strict float tolerances. Tag assertions/comments with decision IDs (`D-09`, `D-10`) to keep traceability explicit.</action>
  <acceptance_criteria>
    - `git --no-pager grep -n "D-09\\|D-10\\|idempotent" src/tests/scene_solver_contract_test.c` returns new test anchors.
    - Contract test includes explicit "no mutation on unsat" assertions comparing pre/post geometry coordinates.
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target scene_solver_contract && ctest -R scene_solver_contract --test-dir build-vulkan -C Release --output-on-failure</automated>
  </verify>
  <done>Contract tests codify deterministic recalc and LENGTH/ANGLE success/failure behavior with executable assertions.</done>
</task>

</tasks>

<verification>
Run the Wave-0 gate from 22-VALIDATION after task completion:
`ctest -R "scene_solver_trigger|scene_solver_pass_policy|scene_solver_contract|scene_solver_diagnostics|scene_solver_drag" --test-dir build-vulkan -C Release --output-on-failure`
</verification>

<success_criteria>
- Dedicated `scene_solver_trigger` and `scene_solver_pass_policy` targets exist and run.
- Solver contract tests include deterministic recalc + LENGTH/ANGLE atomicity/no-mutation assertions.
- No deferred Phase 23/24 constraint families are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/22-solver-trigger-recalculate-determinism/22-01-SUMMARY.md`
</output>

