---
phase: 43-embed-contract-child-window-bootstrap
plan: 02
type: execute
wave: 1
depends_on: [43-01]
files_modified:
  - src/platform/win32_embed.h
  - vendors/libsokol/patches/0001-win32-embed-child-window-bootstrap.patch
  - src/app.c
autonomous: true
requirements: [EMBD-02, EMBD-03]
must_haves:
  truths:
    - "An embedded mdCAD launch creates a true `WS_CHILD` window under the supplied parent HWND from birth instead of reparenting a normal top-level window later."
    - "If child-window startup fails, mdCAD exits with a clear embed-specific failure instead of silently showing a standalone window."
    - "Embedded mode boots into a trimmed viewer baseline with `Pick Buffer Debug`, `Slot Buffer Debug`, and `FPS Debug` all OFF by default."
  artifacts:
    - path: "src/platform/win32_embed.h"
      provides: "Shared Win32 embed bootstrap state and fail-fast helpers consumed by app startup and the Sokol patch"
      contains: "mdcad_win32_embed_fail_startup"
    - path: "vendors/libsokol/patches/0001-win32-embed-child-window-bootstrap.patch"
      provides: "Pinned Win32 Sokol patch for child-window creation at CreateWindowExW time"
      contains: "WS_CHILD | WS_VISIBLE | WS_CLIPSIBLINGS | WS_CLIPCHILDREN"
    - path: "src/app.c"
      provides: "Embedded startup wiring and trimmed viewer defaults"
      contains: "state.ui_visible = false"
  key_links:
    - from: "src/app.c"
      to: "src/platform/win32_embed.h"
      via: "pre-window embed config handoff from parsed argv to Win32 bootstrap state"
      pattern: "mdcad_win32_embed_set_config|mdcad_win32_embed_is_enabled"
    - from: "vendors/libsokol/patches/0001-win32-embed-child-window-bootstrap.patch"
      to: "src/platform/win32_embed.h"
      via: "patched Win32 CreateWindowExW branch calls embed helper instead of standalone fallback"
      pattern: "mdcad_win32_embed_parent_hwnd|mdcad_win32_embed_fail_startup"
    - from: "src/app.c"
      to: "src/ui/ui_pick_debug.h"
      via: "embedded-mode defaults force debug viewers closed at startup"
      pattern: "state.pick_debug.window_open = false|state.slot_buffer_debug.window_open = false|state.fps_debug.window_open = false"
---

<objective>
Create the real embedded Win32 child-window bootstrap path and the immediate embedded viewer baseline.

Purpose: Convert the Wave 0 contract into a strict no-fallback child-window startup flow per D-02, D-03, D-06, and D-07 without pulling in Phase 44 resize/focus/input hardening.
Output: A checked-in Sokol patch, Win32 embed helper glue, and embedded startup defaults that trim standalone chrome immediately.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-embed-contract-child-window-bootstrap/43-CONTEXT.md
@.planning/phases/43-embed-contract-child-window-bootstrap/43-RESEARCH.md
@.planning/phases/43-embed-contract-child-window-bootstrap/43-VALIDATION.md
@.planning/phases/43-embed-contract-child-window-bootstrap/43-01-SUMMARY.md
@src/app_launch_config.h
@src/app.c
@vendors/libsokol/CMakeLists.txt

<interfaces>
From `src/app_launch_config.h`:
```c
typedef struct {
    bool embedded;
    uintptr_t parent_hwnd_value;
} mdcad_launch_config_t;

static inline bool mdcad_launch_config_parse(int argc,
                                             char** argv,
                                             mdcad_launch_config_t* out_cfg,
                                             char* error_buffer,
                                             size_t error_buffer_size);
```

Plan 43-02 must introduce in `src/platform/win32_embed.h`:
```c
static inline void mdcad_win32_embed_set_config(const mdcad_launch_config_t* cfg);
static inline bool mdcad_win32_embed_is_enabled(void);
static inline HWND mdcad_win32_embed_parent_hwnd(void);
static inline void mdcad_win32_embed_fail_startup(const char* reason);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the checked-in Win32 child-window patch and embed bootstrap helper</name>
  <read_first>
    - .planning/phases/43-embed-contract-child-window-bootstrap/43-RESEARCH.md
    - .planning/phases/43-embed-contract-child-window-bootstrap/43-01-SUMMARY.md
    - src/app_launch_config.h
    - build-vulkan/_deps/libsokol-src/sokol_app.h
  </read_first>
  <files>src/platform/win32_embed.h, vendors/libsokol/patches/0001-win32-embed-child-window-bootstrap.patch</files>
  <action>Create `src/platform/win32_embed.h` as the shared Win32-only bootstrap helper used by both mdCAD and the pinned Sokol patch. It must expose `mdcad_win32_embed_set_config(...)`, `mdcad_win32_embed_is_enabled()`, `mdcad_win32_embed_parent_hwnd()`, and `mdcad_win32_embed_fail_startup(...)`; on non-Windows builds these helpers must compile as no-ops so other targets stay untouched. Then create `vendors/libsokol/patches/0001-win32-embed-child-window-bootstrap.patch` against the pinned `2356d22badb8b02b5b4fc745216023a2a699d840` baseline. Patch `_sapp_win32_create_window()` so the embedded branch validates `IsWindow(parent_hwnd)`, uses `GetClientRect(parent_hwnd, ...)`, and creates the mdCAD window with `WS_CHILD | WS_VISIBLE | WS_CLIPSIBLINGS | WS_CLIPCHILDREN`, `hWndParent = parent_hwnd`, `x = 0`, `y = 0`, and client-sized width/height from birth. In that embedded branch, do not call or introduce `SetParent`, do not preserve the standalone caption/sysmenu/sizebox styles, and do not fall back to the old top-level path when validation or `CreateWindowExW` fails; call `mdcad_win32_embed_fail_startup(\"Embedded startup failed: ...\")` instead so EMBD-02 and D-03 stay strict.</action>
  <acceptance_criteria>`src/platform/win32_embed.h` exports `mdcad_win32_embed_fail_startup`; `vendors/libsokol/patches/0001-win32-embed-child-window-bootstrap.patch` contains `WS_CHILD | WS_VISIBLE | WS_CLIPSIBLINGS | WS_CLIPCHILDREN`, `GetClientRect`, and `mdcad_win32_embed_parent_hwnd`; and neither file introduces `SetParent` as an implementation path.</acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; patch=Path('vendors/libsokol/patches/0001-win32-embed-child-window-bootstrap.patch').read_text(encoding='utf-8'); helper=Path('src/platform/win32_embed.h').read_text(encoding='utf-8'); assert 'WS_CHILD | WS_VISIBLE | WS_CLIPSIBLINGS | WS_CLIPCHILDREN' in patch; assert 'GetClientRect' in patch; assert 'SetParent' not in patch; assert 'mdcad_win32_embed_fail_startup' in helper"</automated>
  </verify>
  <done>The repo contains the exact pinned Win32 patch and bootstrap helper needed to create mdCAD as a child window from birth with no standalone fallback path.</done>
</task>

<task type="auto">
  <name>Task 2: Wire embedded startup and immediate viewer trimming in app.c</name>
  <read_first>
    - .planning/phases/43-embed-contract-child-window-bootstrap/43-CONTEXT.md
    - .planning/phases/43-embed-contract-child-window-bootstrap/43-01-SUMMARY.md
    - src/app.c
    - src/app_launch_config.h
    - src/platform/win32_embed.h
  </read_first>
  <files>src/app.c</files>
  <action>Update `src/app.c` so the parsed launch config from Plan 43-01 is handed to `mdcad_win32_embed_set_config(...)` before `sokol_main()` returns the `sapp_desc`. Persist `embedded` launch state inside the app so later phases have a hook, but do not implement Phase 44 resize/focus/input behavior here. In `init()`, when embedded mode is active, set `state.ui_visible = false` immediately per D-06 so the standalone-heavy panels (`Controls`, `Visibility`, `Scene Hierarchy`, `Entity Inspector`, script/editor windows, and similar chrome guarded by `state.ui_visible`) stay hidden while `ui_viewport_draw(...)` continues rendering the viewer. After `ui_pick_debug_init`, `ui_slot_buffer_debug_init`, and `ui_fps_debug_init`, force `state.pick_debug.window_open = false`, `state.slot_buffer_debug.window_open = false`, and `state.fps_debug.window_open = false` under the same embedded condition per D-07. Keep the behavior strict: do not add fallback branches that clear the embed flag after child startup trouble, do not add `SetParent`, and do not pull in `--jsonl` / `--jsonl-live-refresh` parsing from later phases.</action>
  <acceptance_criteria>`src/app.c` calls `mdcad_win32_embed_set_config`, sets `state.ui_visible = false` under the embedded condition, and contains explicit `state.pick_debug.window_open = false`, `state.slot_buffer_debug.window_open = false`, and `state.fps_debug.window_open = false` assignments for embedded startup without introducing `SetParent` or JSONL launch flags.</acceptance_criteria>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD</automated>
  </verify>
  <done>Embedded mdCAD boots into a true child-window path with the immediate trimmed viewer baseline and the requested debug windows OFF by default.</done>
</task>

</tasks>

<verification>
Run:
- `cmake --build build-vulkan --config Release --target mdCAD`
- `ctest --test-dir build-vulkan -C Release --output-on-failure -R embed_launch_config_test`
</verification>

<success_criteria>
- The pinned Sokol patch creates mdCAD as a child window under the supplied parent HWND from birth.
- Any child-window startup failure exits explicitly instead of showing a standalone window.
- Embedded startup hides standalone-heavy chrome immediately and starts with Pick/Slot/FPS debug windows OFF.
</success_criteria>

<output>
After completion, create `.planning/phases/43-embed-contract-child-window-bootstrap/43-02-SUMMARY.md`
</output>
