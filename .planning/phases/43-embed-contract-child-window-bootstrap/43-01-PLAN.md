---
phase: 43-embed-contract-child-window-bootstrap
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - vendors/libsokol/CMakeLists.txt
  - src/app_launch_config.h
  - src/tests/embed_launch_config_test.c
  - src/CMakeLists.txt
  - src/app.c
autonomous: true
requirements: [EMBD-01, EMBD-03]
must_haves:
  truths:
    - "Embed bootstrap work builds against a pinned Sokol revision instead of floating `master`."
    - "mdCAD parses `--embedded` plus `--parent-hwnd` before window creation and exits non-zero with a clear error when that contract is invalid."
    - "Phase 43 has a dedicated `embed_launch_config_test` target that locks the strict no-fallback launch contract before Win32 child-window patching begins."
  artifacts:
    - path: "vendors/libsokol/CMakeLists.txt"
      provides: "Pinned Sokol revision plus checked-in patch-hosting seam for Win32 embed work"
      contains: "2356d22badb8b02b5b4fc745216023a2a699d840"
    - path: "src/app_launch_config.h"
      provides: "Pre-window embed launch config parser and error contract"
      contains: "mdcad_launch_config_parse"
    - path: "src/tests/embed_launch_config_test.c"
      provides: "Automated parser coverage for valid and invalid embed argv cases"
      contains: "test_embed_launch_config_valid_hex_parent_hwnd"
    - path: "src/CMakeLists.txt"
      provides: "CTest registration for embed bootstrap parser coverage"
      contains: "embed_launch_config_test"
  key_links:
    - from: "vendors/libsokol/CMakeLists.txt"
      to: "vendors/libsokol/patches/0001-win32-embed-child-window-bootstrap.patch"
      via: "checked-in patch application seam"
      pattern: "0001-win32-embed-child-window-bootstrap.patch"
    - from: "src/app.c"
      to: "src/app_launch_config.h"
      via: "pre-window parse before returning sapp_desc"
      pattern: "mdcad_launch_config_parse|Embedded startup failed:"
    - from: "src/tests/embed_launch_config_test.c"
      to: "src/app_launch_config.h"
      via: "CTests for valid hex/decimal HWND and strict invalid-contract failure cases"
      pattern: "mdcad_launch_config_parse"
---

<objective>
Lock the reproducible bootstrap foundation for Phase 43 before any Win32 child-window patching starts.

Purpose: Cover the Wave 0 prerequisites the phase depends on — Sokol pinning/patch-hosting and parser test scaffolding — while also landing the strict launch-contract parser per D-01, D-02, and D-03.
Output: A pinned Sokol baseline, a checked-in patch seam, and a tested `--embedded` / `--parent-hwnd` parser wired into `sokol_main()`.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-embed-contract-child-window-bootstrap/43-CONTEXT.md
@.planning/phases/43-embed-contract-child-window-bootstrap/43-RESEARCH.md
@.planning/phases/43-embed-contract-child-window-bootstrap/43-VALIDATION.md
@vendors/libsokol/CMakeLists.txt
@src/CMakeLists.txt
@src/app.c

<interfaces>
From `src/app.c`:
```c
sapp_desc sokol_main(int argc, char* argv[]);
```

Phase 43 must introduce in `src/app_launch_config.h`:
```c
typedef struct {
    bool embedded;
    uintptr_t parent_hwnd_value;
} mdcad_launch_config_t;

static inline bool mdcad_launch_config_parse(int argc,
                                             char** argv,
                                             mdcad_launch_config_t* out_cfg,
                                             char* error_buffer,
                                             size_t error_buffer_size);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin Sokol and lock the repo-hosted Win32 patch seam</name>
  <read_first>
    - .planning/phases/43-embed-contract-child-window-bootstrap/43-CONTEXT.md
    - .planning/phases/43-embed-contract-child-window-bootstrap/43-RESEARCH.md
    - vendors/libsokol/CMakeLists.txt
    - build-vulkan/_deps/libsokol-src/sokol_app.h
  </read_first>
  <files>vendors/libsokol/CMakeLists.txt</files>
  <action>Update `vendors/libsokol/CMakeLists.txt` so Phase 43 stops consuming floating `master`. Replace the current `GIT_TAG master` with the already-fetched Windows baseline commit `2356d22badb8b02b5b4fc745216023a2a699d840` from `build-vulkan/_deps/libsokol-src` to make the upcoming Win32 patch reproducible. Add `find_package(Git REQUIRED)` plus a checked-in patch hook for `vendors/libsokol/patches/0001-win32-embed-child-window-bootstrap.patch`, and expose `${CMAKE_SOURCE_DIR}/src` in `target_include_directories(libsokol PRIVATE ...)` so the later Sokol patch can include `platform/win32_embed.h` without touching transient `_deps` output by hand. Make the patch hook conditional on the patch file existing so this Wave 0 plan lands the hosting choice now and Plan 43-02 can drop in the actual patch content later. This task implements the required Sokol pinning / patch-hosting choice prerequisite for EMBD-02 while preserving D-03's no-ad-hoc-fallback direction.</action>
  <acceptance_criteria>`vendors/libsokol/CMakeLists.txt` contains `2356d22badb8b02b5b4fc745216023a2a699d840`, references `vendors/libsokol/patches/0001-win32-embed-child-window-bootstrap.patch`, and adds `${CMAKE_SOURCE_DIR}/src` to the `libsokol` private include path so repo-hosted Win32 embed glue can be compiled from the pinned baseline.</acceptance_criteria>
  <verify>
    <automated>python -c "from pathlib import Path; t=Path('vendors/libsokol/CMakeLists.txt').read_text(encoding='utf-8'); assert '2356d22badb8b02b5b4fc745216023a2a699d840' in t; assert '0001-win32-embed-child-window-bootstrap.patch' in t; assert '${CMAKE_SOURCE_DIR}/src' in t"</automated>
  </verify>
  <done>Sokol is pinned to the known Windows baseline and the repo now has a reproducible place to host the Win32 child-window patch instead of editing build-tree files directly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add and wire the strict embed launch parser with CTest coverage</name>
  <read_first>
    - .planning/phases/43-embed-contract-child-window-bootstrap/43-CONTEXT.md
    - .planning/phases/43-embed-contract-child-window-bootstrap/43-RESEARCH.md
    - .planning/phases/43-embed-contract-child-window-bootstrap/43-VALIDATION.md
    - src/app.c
    - src/CMakeLists.txt
  </read_first>
  <files>src/app_launch_config.h, src/tests/embed_launch_config_test.c, src/CMakeLists.txt, src/app.c</files>
  <behavior>
    - Test 1: No embed flags yields `embedded == false` and leaves unrelated future flags untouched so Phase 45 hooks remain possible.
    - Test 2: `--embedded --parent-hwnd 0x12345678` and `--embedded --parent-hwnd 305419896` both parse successfully into the same non-zero parent HWND value per D-01.
    - Test 3: `--embedded` without `--parent-hwnd`, `--parent-hwnd` without `--embedded`, zero / malformed HWND values, and duplicate embed flags all return a clear parser error per D-02 and D-03.
  </behavior>
  <action>Create `src/app_launch_config.h` as the Wave 0 embed-bootstrap contract. Export `mdcad_launch_config_t` plus `mdcad_launch_config_parse(...)`; keep the implementation local to the header or otherwise self-contained so `src/app.c` and the new test target can both compile it without extra runtime plumbing. Parse only `--embedded` and `--parent-hwnd` in this phase: accept both decimal and `0x`-prefixed HWND values, ignore unrelated future flags, require `--parent-hwnd` when `--embedded` is present, reject `--parent-hwnd` when `--embedded` is absent, and reject zero / malformed / duplicate embed values. Add `src/tests/embed_launch_config_test.c` with named coverage including `test_embed_launch_config_valid_hex_parent_hwnd`, `test_embed_launch_config_valid_decimal_parent_hwnd`, and explicit invalid-contract cases. Register the test in `src/CMakeLists.txt` as `embed_launch_config_test`, then call `mdcad_launch_config_parse(...)` at the top of `sokol_main()` in `src/app.c`; if parsing fails for the embed contract, print `Embedded startup failed: ...` to stderr and `exit(2)` before returning a `sapp_desc`, per D-01, D-02, and D-03. Do not add `--jsonl` or `--jsonl-live-refresh` handling here; leave those flags untouched for Phase 45 and Phase 46.</action>
  <acceptance_criteria>`src/app_launch_config.h` exports `mdcad_launch_config_parse`; `src/tests/embed_launch_config_test.c` contains `test_embed_launch_config_valid_hex_parent_hwnd` and `test_embed_launch_config_valid_decimal_parent_hwnd`; `src/CMakeLists.txt` registers `embed_launch_config_test`; and `src/app.c` emits `Embedded startup failed:` before `exit(2)` when the embed contract is invalid.</acceptance_criteria>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release --output-on-failure -R embed_launch_config_test</automated>
  </verify>
  <done>The strict no-fallback embed launch contract is enforced before window creation and protected by a dedicated fast CTest target.</done>
</task>

</tasks>

<verification>
Run:
- `ctest --test-dir build-vulkan -C Release --output-on-failure -R embed_launch_config_test`
</verification>

<success_criteria>
- Sokol is pinned to a reproducible baseline and the repo-hosted patch seam is in place.
- `sokol_main()` validates `--embedded` / `--parent-hwnd` before any window can exist.
- Invalid embed launches fail non-zero with explicit stderr output and no opportunity for standalone fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/43-embed-contract-child-window-bootstrap/43-01-SUMMARY.md`
</output>
