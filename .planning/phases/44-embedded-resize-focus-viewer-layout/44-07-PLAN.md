---
phase: 44-embedded-resize-focus-viewer-layout
plan: 07
type: execute
wave: 1
depends_on:
  - 44-03
  - 44-05
files_modified:
  - samples/avalonia-host/MainWindow.axaml
  - samples/avalonia-host/MainWindow.axaml.cs
autonomous: true
gap_closure: true
requirements:
  - INPT-02
must_haves:
  truths:
    - "Embedded attach still does not auto-focus mdCAD."
    - "One deliberate click inside the embedded viewer transfers Win32 keyboard focus into the attached mdCAD child HWND so text entry, Tab, clipboard shortcuts, and undo work."
    - "Clicking host chrome returns keyboard focus to the Avalonia host instead of leaving it stuck on the embedded child."
  artifacts:
    - path: "samples/avalonia-host/MainWindow.axaml"
      provides: "A focusable host-chrome surface that can deliberately return focus to the Avalonia host."
      contains: "Focusable="
    - path: "samples/avalonia-host/MainWindow.axaml.cs"
      provides: "Pointer-driven embedded-child focus handoff and host-focus return helpers."
      contains: "OnEmbedSurfacePointerPressed|RequestEmbeddedKeyboardFocus|OnHostChromePointerPressed"
  key_links:
    - from: "samples/avalonia-host/MainWindow.axaml"
      to: "samples/avalonia-host/MainWindow.axaml.cs"
      via: "PointerPressed handlers on the embed surface and host chrome"
      pattern: "PointerPressed"
    - from: "samples/avalonia-host/MainWindow.axaml.cs"
      to: "_attachedChildHwnd"
      via: "deliberate-click Win32 activation/focus helper"
      pattern: "SetFocus|SetActiveWindow|RequestEmbeddedKeyboardFocus"
---

<objective>
Close the remaining first-click keyboard-ownership gap in the Avalonia host.

Purpose: Honor D-01, D-02, D-03, and D-04 after the human re-check showed that mdCAD still never receives Win32 keyboard focus from the host-side click path.
Output: The host gains an explicit deliberate-click focus handoff into the attached mdCAD child HWND without auto-focusing at attach.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-CONTEXT.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-HUMAN-UAT.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-MANUAL-CHECKLIST.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-03-SUMMARY.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-05-SUMMARY.md
@samples/avalonia-host/MainWindow.axaml
@samples/avalonia-host/MainWindow.axaml.cs

<interfaces>
From samples/avalonia-host/MainWindow.axaml:
```xml
<StackPanel Spacing="4">
  <TextBlock x:Name="StatusTextBlock" />
  <TextBlock x:Name="ModeTextBlock" />
  <TextBlock x:Name="FailureTextBlock" />
</StackPanel>
<local:EmbedNativeControlHost x:Name="EmbedSurface" />
```

From samples/avalonia-host/MainWindow.axaml.cs:
```csharp
private readonly EmbedNativeControlHost _embedSurface;
private IntPtr _placeholderHwnd;
private IntPtr _attachedChildHwnd;
private void OnPlaceholderHandleReady(IntPtr hwnd);
private void OnAttachTimerTick(object? sender, EventArgs e);
private static IntPtr FindChildWindowForProcess(IntPtr parentHwnd, int processId);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a deliberate-click Win32 focus handoff for the attached mdCAD child HWND</name>
  <files>samples/avalonia-host/MainWindow.axaml, samples/avalonia-host/MainWindow.axaml.cs</files>
  <action>Update the Avalonia host so Scenario 2 closes exactly per D-01, D-02, D-03, and D-04. In `samples/avalonia-host/MainWindow.axaml`, make the host chrome area focusable and wire explicit pointer-press handlers for the embed surface versus host chrome if that is the cleanest way to distinguish child-focus versus host-focus clicks. In `samples/avalonia-host/MainWindow.axaml.cs`, add a deliberate-click activation helper that runs only after `_attachedChildHwnd` is valid and only in response to a real pointer press inside `EmbedSurface`; that helper must verify the child is still parented to `_placeholderHwnd`, then transfer keyboard focus into `_attachedChildHwnd` with Win32 activation/focus calls from the click path so the same click that starts mouse interaction also enables text entry, `Tab`, clipboard shortcuts, and undo. Do not auto-focus during attach, launch, resize sync, or timers. Keep the host/child boundary intact: no keyboard proxying, no synthetic keystroke forwarding, and no standalone fallback logic. If host-chrome clicks need explicit help to satisfy D-02, restore focus to the Avalonia side from the status/chrome pointer path instead of adding new visible controls.</action>
  <verify>
    <automated>dotnet build samples/avalonia-host/AvaloniaHost.csproj -c Release &amp;&amp; python -c "from pathlib import Path; t=Path(r'samples/avalonia-host/MainWindow.axaml.cs').read_text(encoding='utf-8'); assert ('SetFocus' in t or 'SetActiveWindow' in t), 'missing Win32 focus handoff'; assert ('PointerPressed' in t or 'AddHandler' in t), 'missing deliberate click hook'; print('embedded-focus-handoff-wired')"</automated>
  </verify>
  <done>The host no longer relies on passive first-click behavior: attach stays unfocused, one deliberate click moves keyboard ownership into mdCAD, and clicking host chrome can return focus to the host.</done>
</task>

</tasks>

<verification>
Re-run Human UAT Scenario 2 with `dotnet run --project samples/avalonia-host/AvaloniaHost.csproj -c Release`: confirm attach does not auto-focus mdCAD, one deliberate click inside the viewer enables typing plus `Tab`/clipboard/undo inside mdCAD, and clicking the host status/chrome area returns focus to the host.
</verification>

<success_criteria>
- Human UAT Gap 1 is closed.
- The host now performs an explicit deliberate-click focus transfer instead of assuming the child will naturally gain keyboard ownership.
- D-04 remains intact: mdCAD is still not auto-focused at attach.
</success_criteria>

<output>
After completion, create `.planning/phases/44-embedded-resize-focus-viewer-layout/44-07-SUMMARY.md`
</output>
