---
phase: 44-embedded-resize-focus-viewer-layout
plan: 03
type: execute
wave: 2
depends_on:
  - 44-01
files_modified:
  - samples/avalonia-host/MainWindow.axaml.cs
  - .planning/phases/44-embedded-resize-focus-viewer-layout/44-MANUAL-CHECKLIST.md
autonomous: true
requirements:
  - EMBD-04
  - INPT-01
must_haves:
  truths:
    - "Host resize keeps the child HWND synced to the placeholder region during normal resize, snap-layout, and restore/maximize transitions."
    - "Destroying the placeholder or closing the host exits embedded mdCAD immediately with no orphaned `mdCAD.exe` and no standalone fallback."
    - "Phase 44 manual verification can deterministically exercise parent-invalid teardown without inventing new steps during execution."
  artifacts:
    - path: "samples/avalonia-host/MainWindow.axaml.cs"
      provides: "Host lifecycle monitor, deterministic placeholder-destroy harness mode, and immediate process teardown"
      contains: "embed lifecycle and resize sync"
    - path: ".planning/phases/44-embedded-resize-focus-viewer-layout/44-MANUAL-CHECKLIST.md"
      provides: "Concrete resize/orphan verification steps bound to the updated host harness"
      contains: "destroy-after-attach"
  key_links:
    - from: "samples/avalonia-host/MainWindow.axaml.cs"
      to: "mdCAD.exe"
      via: "Process.Kill(true) on close/placeholder invalidation"
      pattern: "Kill\\(true\\)"
    - from: "samples/avalonia-host/MainWindow.axaml.cs"
      to: "_attachedChildHwnd"
      via: "MoveWindow resize sync and window-validity monitoring"
      pattern: "MoveWindow"
    - from: ".planning/phases/44-embedded-resize-focus-viewer-layout/44-MANUAL-CHECKLIST.md"
      to: "samples/avalonia-host/MainWindow.axaml.cs"
      via: "documented harness test mode"
      pattern: "destroy-after-attach"
---

<objective>
Finish the host-side lifecycle proof so resize authority and orphan teardown stay correct even when the placeholder/control lifecycle ends before the whole host process closes, while the checklist explicitly verifies the matching mdCAD-side parent-ended exit path from 44-02.

Purpose: Close EMBD-04 and INPT-01 with deterministic host harness behavior before Phases 45-47 build on the sample host.
Output: A resize/orphan-aware Avalonia harness and a checklist that can prove those paths.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-CONTEXT.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-RESEARCH.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-VALIDATION.md
@.planning/phases/43-embed-contract-child-window-bootstrap/43-03-SUMMARY.md
@samples/avalonia-host/MainWindow.axaml.cs
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-MANUAL-CHECKLIST.md

<interfaces>
From samples/avalonia-host/MainWindow.axaml.cs:
```csharp
private readonly DispatcherTimer _resizeSyncTimer;
private IntPtr _placeholderHwnd;
private IntPtr _attachedChildHwnd;
private Process? _mdcadProcess;

private void SyncAttachedChildBounds();
private void OnResizeSyncTick(object? sender, EventArgs e);
private void OnWindowClosed(object? sender, EventArgs e);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deterministic placeholder teardown and orphan-kill handling to the Avalonia host</name>
  <files>samples/avalonia-host/MainWindow.axaml.cs</files>
  <read_first>
samples/avalonia-host/MainWindow.axaml.cs
.planning/phases/43-embed-contract-child-window-bootstrap/43-03-SUMMARY.md
.planning/phases/44-embedded-resize-focus-viewer-layout/44-CONTEXT.md
.planning/phases/44-embedded-resize-focus-viewer-layout/44-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    - The host supports a deterministic placeholder-destroy mode after attach so EMBD-04 can be reproduced without closing the full host window.
    - The resize timer or equivalent lifecycle monitor stops and kills the child process immediately when the placeholder or child HWND becomes invalid.
    - Existing resize sync via `MoveWindow(...)` remains intact for the valid attached path.
    - `dotnet build samples/avalonia-host/AvaloniaHost.csproj -c Release` succeeds.
  </acceptance_criteria>
  <action>Extend `samples/avalonia-host/MainWindow.axaml.cs` with one additional harness mode dedicated to Phase 44 orphan testing (use the literal CLI token `destroy-after-attach` so it matches the manual checklist). After the child attaches, destroy the placeholder HWND or otherwise remove the native control without closing the host window, then immediately detect the invalid placeholder/child relationship and kill `mdCAD.exe` with `Process.Kill(true)` so D-10 holds. Keep the existing host-side `MoveWindow(...)` sync as the resize authority for INPT-01, but harden `OnResizeSyncTick()` and related lifecycle code so invalid placeholder/child handles stop the timer, update status text, and never leave an attached orphan process running. Do not add host-side keyboard/mouse proxying.</action>
  <verify>
    <automated>dotnet build samples/avalonia-host/AvaloniaHost.csproj -c Release</automated>
  </verify>
  <done>The sample host can deterministically trigger placeholder teardown after attach and immediately cleans up the embedded mdCAD process while preserving valid-path resize sync.</done>
</task>

<task type="auto">
  <name>Task 2: Bind the manual checklist to the real Phase 44 host harness</name>
  <files>.planning/phases/44-embedded-resize-focus-viewer-layout/44-MANUAL-CHECKLIST.md</files>
  <read_first>
.planning/phases/44-embedded-resize-focus-viewer-layout/44-MANUAL-CHECKLIST.md
samples/avalonia-host/MainWindow.axaml.cs
.planning/phases/44-embedded-resize-focus-viewer-layout/44-VALIDATION.md
  </read_first>
  <acceptance_criteria>
    - The checklist contains runnable commands for `valid`, `invalid-parent`, `destroyed-parent`, and `destroy-after-attach`.
    - The resize scenario instructs snap-layout/fullscreen/restore coverage and ties expected results to INPT-01.
    - The teardown scenarios explicitly require “no standalone fallback”, “no surviving mdCAD.exe”, and a note on whether the observed exit came from mdCAD-side parent-invalid quit or host-side `Process.Kill(true)`.
  </acceptance_criteria>
  <action>Replace any placeholder wording in `44-MANUAL-CHECKLIST.md` with the exact host-harness commands and expected observations produced by the updated sample host. Keep the checklist format concrete: command, observed states, resize/focus/orphan expectations, and scenario verdict. Ensure the orphan scenarios cover both host close and the new `destroy-after-attach` path so EMBD-04 is not left to ad hoc testing later, and explicitly distinguish the expected `destroyed-parent` / host-ended mdCAD-side `sapp_quit()` path from the host-side fallback cleanup path used by `destroy-after-attach`.</action>
  <verify>
    <automated>python -c "from pathlib import Path; t=Path(r'.planning/phases/44-embedded-resize-focus-viewer-layout/44-MANUAL-CHECKLIST.md').read_text(encoding='utf-8'); required=['destroy-after-attach','no standalone fallback','no surviving mdCAD.exe','snap']; missing=[x for x in required if x not in t]; assert not missing, missing; print('manual-checklist-bound-ok')"</automated>
  </verify>
  <done>The Phase 44 manual checklist is bound to the real host harness modes and explicitly covers resize and orphan teardown evidence.</done>
</task>

</tasks>

<verification>
Run `dotnet build samples/avalonia-host/AvaloniaHost.csproj -c Release`, then execute the checklist scenarios for valid attach resize coverage and teardown coverage.
</verification>

<success_criteria>
- EMBD-04 has a deterministic host-side repro path beyond whole-window close, and the checklist makes the native parent-ended exit expectation explicit.
- INPT-01 keeps host-owned rectangle sync authoritative and testable.
</success_criteria>

<output>
After completion, create `.planning/phases/44-embedded-resize-focus-viewer-layout/44-03-SUMMARY.md`
</output>
