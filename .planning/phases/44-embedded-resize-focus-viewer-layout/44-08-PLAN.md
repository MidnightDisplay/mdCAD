---
phase: 44-embedded-resize-focus-viewer-layout
plan: 08
type: execute
wave: 2
depends_on:
  - 44-06
  - 44-07
files_modified:
  - samples/avalonia-host/MainWindow.axaml.cs
autonomous: true
gap_closure: true
requirements:
  - EMBD-04
  - INPT-04
must_haves:
  truths:
    - "Closing the host normally invalidates the embedded parent/placeholder before the host starts waiting on mdCAD exit."
    - "Normal host close gives embedded mdCAD a real chance to observe the destroyed parent chain, self-exit, and flush `build-vulkan\\bin\\Release\\imgui.embedded.ini`."
    - "Fallback `Process.Kill(true)` remains a last resort instead of the default close path."
  artifacts:
    - path: "samples/avalonia-host/MainWindow.axaml.cs"
      provides: "A shared placeholder invalidation path reused by destroyed-parent mode and normal host close before graceful-exit waiting."
      contains: "WaitForGracefulExitOnClose|OnWindowClosed|DestroyWindow"
  key_links:
    - from: "samples/avalonia-host/MainWindow.axaml.cs"
      to: "_placeholderHwnd"
      via: "normal host close destroys or clears the placeholder before waiting for process exit"
      pattern: "OnWindowClosed|DestroyWindow|ClearPlaceholderHandle"
    - from: "samples/avalonia-host/MainWindow.axaml.cs"
      to: "mdCAD.exe"
      via: "graceful wait first, `Kill(true)` only after self-exit fails"
      pattern: "WaitForExit|Kill\\(true\\)"
---

<objective>
Close the remaining normal-close embedded-layout persistence gap in the Avalonia host.

Purpose: Honor D-08 and D-10 after human UAT showed that normal host close still bypasses the invalid-parent shutdown signal and therefore often misses the embedded ini flush.
Output: Normal host close destroys or detaches the placeholder first, then waits for mdCAD self-exit before fallback cleanup.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-CONTEXT.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-HUMAN-UAT.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-VERIFICATION.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-MANUAL-CHECKLIST.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-06-SUMMARY.md
@samples/avalonia-host/MainWindow.axaml.cs
@src/app.c

<interfaces>
From samples/avalonia-host/MainWindow.axaml.cs:
```csharp
private void BeginPostAttachTeardown();
private void WaitForGracefulExitOnClose(string detail);
private void EnsureAttachedProcessTeardown(string detail);
private void FinalizeTeardownStatus(int exitCode);
private void OnWindowClosed(object? sender, EventArgs e);
```

From src/app.c:
```c
static bool mdcad_handle_embedded_runtime_guards(void);
// invalid parent chain -> state.embed_quit_requested = true; sapp_quit();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invalidate the placeholder on normal host close before waiting for graceful mdCAD exit</name>
  <files>samples/avalonia-host/MainWindow.axaml.cs</files>
  <action>Refactor `samples/avalonia-host/MainWindow.axaml.cs` so the normal `OnWindowClosed(...)` path reuses the destroyed-parent invalidation seam before it waits for process exit. Extract or reuse a single helper that clears `_placeholderHwnd`, calls `_embedSurface.ClearPlaceholderHandle()`, destroys the real placeholder HWND when it still exists, and records an accurate teardown reason. Call that helper from normal host close before `WaitForGracefulExitOnClose(...)` so mdCAD sees the same invalid-parent condition that already works in destroyed-parent mode. Keep the graceful wait path authoritative after invalidation, update teardown messaging so self-exit versus fallback kill stays truthful, and keep `Process.Kill(true)` as a last resort only after the post-invalidation wait proves mdCAD is still alive. Do not reopen the standalone fallback path, do not change `src/app.c`, and do not short-circuit the embedded layout flush with an immediate unconditional kill.</action>
  <verify>
    <automated>dotnet build samples/avalonia-host/AvaloniaHost.csproj -c Release &amp;&amp; python -c "from pathlib import Path; import re; t=Path(r'samples/avalonia-host/MainWindow.axaml.cs').read_text(encoding='utf-8'); assert 'WaitForGracefulExitOnClose' in t, 'missing graceful close wait'; assert 'ClearPlaceholderHandle' in t and 'DestroyWindow' in t, 'missing placeholder invalidation'; assert re.search(r'OnWindowClosed\\s*\\([^)]*\\)\\s*\\{[\\s\\S]*?(ClearPlaceholderHandle|DestroyWindow)[\\s\\S]*?WaitForGracefulExitOnClose', t), 'host close does not invalidate placeholder before waiting'; print('host-close-invalidation-wired')"</automated>
  </verify>
  <done>Normal host close now destroys/detaches the placeholder before waiting, giving mdCAD the same invalid-parent shutdown signal that allows `imgui.embedded.ini` to flush before fallback cleanup.</done>
</task>

</tasks>

<verification>
Re-run Human UAT Scenario 7 exactly as recorded in `44-MANUAL-CHECKLIST.md`: remove `build-vulkan\bin\Release\imgui.embedded.ini`, launch the host, rearrange panels, close the host normally, relaunch, and confirm the rearranged embedded layout persisted via `build-vulkan\bin\Release\imgui.embedded.ini` while standalone `mdCAD.exe` still uses `imgui.ini`.
</verification>

<success_criteria>
- Human UAT Gap 2 is closed.
- Normal host close now triggers the same parent-ended shutdown condition that destroyed-parent mode already proves.
- Embedded layout persistence survives a normal user close without merging into standalone layout state.
</success_criteria>

<output>
After completion, create `.planning/phases/44-embedded-resize-focus-viewer-layout/44-08-SUMMARY.md`
</output>
