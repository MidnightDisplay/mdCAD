---
phase: 44-embedded-resize-focus-viewer-layout
plan: 02
type: execute
wave: 2
depends_on:
  - 44-01
files_modified:
  - src/app.c
  - src/orbit_camera.h
  - src/platform/win32_embed.h
  - vendors/libsokol/patches/0001-win32-embed-child-window-bootstrap.patch
autonomous: true
requirements:
  - INPT-02
  - INPT-03
must_haves:
  truths:
    - "Embedded mdCAD does not steal keyboard focus on attach, but the first deliberate click inside the child window enables mdCAD-owned shortcuts."
    - "Once mdCAD owns focus, `Tab` remains inside mdCAD until the user clicks back into host UI."
    - "Alt+Tab, host deactivation, focus loss, or capture loss cancels active camera/gizmo interaction immediately with no stuck drag state."
  artifacts:
    - path: "src/app.c"
      provides: "Embedded input ownership state, shortcut gating, and gizmo cancel path"
      contains: "embedded input state and cancel handling"
    - path: "src/orbit_camera.h"
      provides: "Explicit orbit camera cancel/reset helper"
      contains: "orbit_camera_cancel"
    - path: "vendors/libsokol/patches/0001-win32-embed-child-window-bootstrap.patch"
      provides: "Vendor seam for capture-loss and deactivate bookkeeping"
      contains: "WM_CAPTURECHANGED"
    - path: "src/platform/win32_embed.h"
      provides: "Win32 embed runtime helpers shared with patched Sokol"
      contains: "runtime focus/cancel helper state"
  key_links:
    - from: "vendors/libsokol/patches/0001-win32-embed-child-window-bootstrap.patch"
      to: "src/app.c"
      via: "Sokol focus/capture events cleared before app-level cancel runs"
      pattern: "WM_(CAPTURECHANGED|CANCELMODE|ACTIVATEAPP)"
    - from: "src/app.c"
      to: "src/orbit_camera.h"
      via: "explicit cancel helper call on unfocus/deactivate"
      pattern: "orbit_camera_.*cancel"
    - from: "src/app.c"
      to: "src/embed_input_state.h"
      via: "embedded keyboard-ownership reducer"
      pattern: "embed_input_state"
---

<objective>
Harden the native mdCAD child-window input contract so focus, keyboard ownership, capture loss, and drag cancellation follow D-01..D-06 without adding host-side input proxying.

Purpose: Close the highest-risk embedded interaction failures before layout or startup-import work builds on top.
Output: Sokol vendor capture-loss fix, runtime input-state gating, and explicit orbit/gizmo cancel behavior.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-CONTEXT.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-RESEARCH.md
@.planning/phases/44-embedded-resize-focus-viewer-layout/44-VALIDATION.md
@.planning/phases/43-embed-contract-child-window-bootstrap/43-02-SUMMARY.md
@src/app.c
@src/orbit_camera.h
@src/platform/win32_embed.h
@vendors/libsokol/patches/0001-win32-embed-child-window-bootstrap.patch

<interfaces>
From src/orbit_camera.h:
```c
typedef struct {
    orbit_camera_action_t current_action;
    bool drag_started_in_viewport;
    bool was_dragging;
} orbit_camera_t;

static inline void orbit_camera_begin_frame(orbit_camera_t* cam, bool any_mouse_down);
static inline void orbit_camera_handle_input(orbit_camera_t* cam,
                                             bool hovered,
                                             float dx, float dy, float wheel,
                                             bool left, bool middle, bool shift);
```

From src/platform/win32_embed.h:
```c
static inline void mdcad_win32_embed_set_config(const mdcad_launch_config_t* cfg);
static inline bool mdcad_win32_embed_is_enabled(void);
static inline HWND mdcad_win32_embed_parent_hwnd(void);
```

From configured Sokol Win32 seam:
```c
_SOKOL_PRIVATE void _sapp_win32_capture_mouse(uint8_t btn_mask);
_SOKOL_PRIVATE void _sapp_win32_release_mouse(uint8_t btn_mask);
case WM_SETFOCUS:
case WM_KILLFOCUS:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix the embedded focus/capture seam without host-side proxy input</name>
  <files>src/app.c, src/platform/win32_embed.h, vendors/libsokol/patches/0001-win32-embed-child-window-bootstrap.patch</files>
  <read_first>
src/app.c
src/platform/win32_embed.h
vendors/libsokol/patches/0001-win32-embed-child-window-bootstrap.patch
src/embed_input_state.h
src/tests/embed_input_state_test.c
.planning/phases/44-embedded-resize-focus-viewer-layout/44-CONTEXT.md
.planning/phases/44-embedded-resize-focus-viewer-layout/44-RESEARCH.md
  </read_first>
  <behavior>
    - D-04: attach/focus does not enable mdCAD keyboard ownership until the first deliberate click inside the child HWND.
    - D-01/D-02/D-03: after that first click, mdCAD-owned shortcuts (including `Tab`) continue until host focus is taken elsewhere.
    - D-05/D-06: deactivation, unfocus, capture loss, or cancel mode clears Sokol capture bookkeeping and requests runtime interaction cancel immediately.
  </behavior>
  <acceptance_criteria>
    - The Sokol patch clears capture bookkeeping on `WM_CAPTURECHANGED`, `WM_CANCELMODE`, and app deactivation instead of waiting only for button-up.
    - `src/app.c` stops processing embedded keyboard shortcuts before first click, but keeps `Tab` in mdCAD once embedded ownership is active.
    - No host-side keyboard or mouse proxy code is introduced in the Avalonia host or elsewhere.
    - `embed_input_state_test` still passes after the runtime wiring is added.
  </acceptance_criteria>
  <action>Update `vendors/libsokol/patches/0001-win32-embed-child-window-bootstrap.patch` so the Win32 path zeroes `capture_mask` and releases capture when the embedded child loses capture or the host app deactivates; cover at least `WM_CAPTURECHANGED`, `WM_CANCELMODE`, and `WM_ACTIVATEAPP` with `wParam == FALSE`, and keep the existing child-window bootstrap intact. Extend `src/platform/win32_embed.h` only with minimal runtime helper state needed by the patched seam—do not add host-side proxy APIs. In `src/app.c`, add embedded input-ownership state backed by `src/embed_input_state.h`: embedded attach starts with keyboard ownership disabled per D-04, `SAPP_EVENTTYPE_MOUSE_DOWN` inside mdCAD promotes ownership per D-01, focus loss or app deactivation clears it per D-02/D-06, and the shortcut gate around undo/redo/Tab/Delete uses this ownership state instead of assuming `io->WantCaptureKeyboard` alone is a focus model.</action>
  <verify>
    <automated>cmake -S . -B build-vulkan -DUSE_VULKAN=ON &amp;&amp; cmake --build build-vulkan --config Release --target mdCAD embed_input_state_test &amp;&amp; ctest --test-dir build-vulkan -C Release --output-on-failure -R "embed_launch_config_test|embed_input_state_test"</automated>
  </verify>
  <done>The vendor seam and app event flow enforce first-click ownership and clean capture-loss behavior with no host-side input forwarding.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add explicit orbit/gizmo cancel and rollback on embedded deactivate</name>
  <files>src/app.c, src/orbit_camera.h</files>
  <read_first>
src/app.c
src/orbit_camera.h
src/embed_input_state.h
src/tests/embed_input_state_test.c
.planning/phases/44-embedded-resize-focus-viewer-layout/44-RESEARCH.md
  </read_first>
  <behavior>
    - Active orbit drags cancel immediately on unfocus/deactivate and leave no residual drag or inertia state.
    - Active gizmo drags roll scene data back to drag-start snapshots on cancel instead of committing partial transforms without undo.
    - Normal mouse-up commit behavior remains unchanged when the host stays active.
  </behavior>
  <acceptance_criteria>
    - `src/orbit_camera.h` exports an explicit cancel/reset helper that clears active action, viewport drag latch, and transient velocity as needed.
    - `src/app.c` has a cancel path distinct from normal mouse-up commit and does not push undo commands during cancel.
    - Canceling a gizmo drag restores entity positions, line endpoints, or vertex snapshots from the stored drag-start buffers before freeing them.
    - `embed_input_state_test` still passes and `mdCAD` builds successfully.
  </acceptance_criteria>
  <action>Add an `orbit_camera_cancel_interaction(...)` helper in `src/orbit_camera.h` that clears `current_action`, `drag_started_in_viewport`, `was_dragging`, and transient motion so deactivation cannot leave a latent drag. In `src/app.c`, extract the existing gizmo drag cleanup into a reusable cancel path that restores the pre-drag snapshots already stored in `state.gizmo_drag_*`, invalidates picking, marks the hierarchy dirty, and frees every drag buffer without creating undo records or preserving partial edits. Call this cancel path whenever the embedded reducer reports deactivation/unfocus/capture-loss cancellation, while preserving the existing mouse-up commit path for normal releases.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdCAD embed_input_state_test &amp;&amp; ctest --test-dir build-vulkan -C Release --output-on-failure -R embed_input_state_test</automated>
  </verify>
  <done>Embedded deactivation no longer leaves stuck camera/gizmo state, and canceled drags revert instead of partially mutating the scene.</done>
</task>

</tasks>

<verification>
Run `ctest --test-dir build-vulkan -C Release --output-on-failure -R "embed_launch_config_test|embed_input_state_test"` plus the manual checklist rows for first-click ownership, click-away return, and Alt+Tab cancel.
</verification>

<success_criteria>
- D-01..D-06 are implemented in native runtime code with a patched capture-loss seam.
- No host-side keyboard/mouse proxy layer is added.
</success_criteria>

<output>
After completion, create `.planning/phases/44-embedded-resize-focus-viewer-layout/44-02-SUMMARY.md`
</output>
