---
phase: 02-direct-adoption-tooling-and-validation-harness
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - src/CMakeLists.txt
  - docs/QUICKSTART.md
autonomous: true
requirements:
  - FOUND-02
  - PERF-01
user_setup: []
must_haves:
  truths:
    - "The native workflow exposes repeatable `math-regression`, `math-bench`, and `math-validation` commands around the standalone harness"
    - "Quickstart documentation tells another agent exactly how to run the Phase 2 pilot on macOS and how to mirror it on Windows Vulkan"
    - "App smoke remains secondary to harness-driven validation in the documented workflow"
  artifacts:
    - path: "src/CMakeLists.txt"
      provides: "Custom native validation targets"
      contains: "add_custom_target(math-regression"
    - path: "src/CMakeLists.txt"
      provides: "Aggregate validation target"
      contains: "add_custom_target(math-validation"
    - path: "docs/QUICKSTART.md"
      provides: "Native math validation commands for the Phase 2 pilot"
      contains: "Native Math Validation (Phase 2 pilot)"
  key_links:
    - from: "src/CMakeLists.txt"
      to: "src/math_harness.c"
      via: "custom validation targets execute the standalone harness"
      pattern: "mdcad_math_harness"
    - from: "docs/QUICKSTART.md"
      to: "src/CMakeLists.txt"
      via: "documented commands match the custom target names"
      pattern: "math-regression|math-bench|math-validation"
    - from: "docs/QUICKSTART.md"
      to: ".planning/phases/02-direct-adoption-tooling-and-validation-harness/02-CONTEXT.md"
      via: "docs preserve the harness-first and smoke-second validation posture"
      pattern: "secondary"
---

<objective>
Wire the standalone harness into a lightweight, repeatable native validation workflow and document the commands.

Purpose: `PERF-01` needs repeatable execution hooks, not just a harness target on disk, and `FOUND-02` requires the thin entrypoint workflow to remain practical for later subsystem-by-subsystem rollout.
Output: Custom CMake validation targets plus Quickstart instructions for the Phase 2 pilot workflow.
</objective>

<execution_context>
@/Users/rodionradchenko/Documents/repos/mdCAD/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rodionradchenko/Documents/repos/mdCAD/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-direct-adoption-tooling-and-validation-harness/02-CONTEXT.md
@.planning/phases/02-direct-adoption-tooling-and-validation-harness/02-RESEARCH.md
@.planning/phases/02-direct-adoption-tooling-and-validation-harness/02-02-SUMMARY.md
@docs/QUICKSTART.md
@src/CMakeLists.txt
@src/math_harness.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add lightweight native validation targets around the harness</name>
  <files>src/CMakeLists.txt</files>
  <read_first>src/CMakeLists.txt, src/math_harness.c, .planning/phases/02-direct-adoption-tooling-and-validation-harness/02-CONTEXT.md, .planning/phases/02-direct-adoption-tooling-and-validation-harness/02-RESEARCH.md</read_first>
  <action>Extend `src/CMakeLists.txt` so native desktop builds expose these exact custom targets around `mdcad_math_harness`: `math-regression`, `math-bench`, and `math-validation`. Define `math-regression` with `COMMAND $<TARGET_FILE:mdcad_math_harness> --mode compare --strict`, `DEPENDS mdcad_math_harness`, and `USES_TERMINAL`. Define `math-bench` with `COMMAND $<TARGET_FILE:mdcad_math_harness> --mode bench --iterations 20000`, `DEPENDS mdcad_math_harness`, and `USES_TERMINAL`. Define `math-validation` as an aggregate target that depends on `math-regression`, `math-bench`, and `${PROJECT_NAME}` so the app binary is still built as part of the full workflow. Keep these targets behind the same native-desktop platform guard used for `mdcad_math_harness`. Do not add `enable_testing()`, `add_test(...)`, or any CTest integration in this phase.</action>
  <verify>
    <automated>cmake -B build -G Ninja && ninja -C build math-regression math-bench math-validation && rg -n "add_custom_target\\(math-regression|add_custom_target\\(math-bench|add_custom_target\\(math-validation|--mode compare --strict|--mode bench --iterations 20000" src/CMakeLists.txt</automated>
  </verify>
  <acceptance_criteria>
    - `src/CMakeLists.txt` contains `add_custom_target(math-regression`
    - `src/CMakeLists.txt` contains `COMMAND $<TARGET_FILE:mdcad_math_harness> --mode compare --strict`
    - `src/CMakeLists.txt` contains `add_custom_target(math-bench`
    - `src/CMakeLists.txt` contains `COMMAND $<TARGET_FILE:mdcad_math_harness> --mode bench --iterations 20000`
    - `src/CMakeLists.txt` contains `add_custom_target(math-validation`
    - `src/CMakeLists.txt` does not contain `enable_testing(` or `add_test(`
    - `cmake -B build -G Ninja && ninja -C build math-validation` exits 0
  </acceptance_criteria>
  <done>The harness is now wired into repeatable native validation targets without escalating Phase 2 into a framework migration.</done>
</task>

<task type="auto">
  <name>Task 2: Document the Phase 2 pilot validation workflow in Quickstart</name>
  <files>docs/QUICKSTART.md</files>
  <read_first>docs/QUICKSTART.md, src/CMakeLists.txt, .planning/phases/02-direct-adoption-tooling-and-validation-harness/02-CONTEXT.md, .planning/phases/02-direct-adoption-tooling-and-validation-harness/02-RESEARCH.md</read_first>
  <action>Update `docs/QUICKSTART.md` with a new section titled exactly `## Native Math Validation (Phase 2 pilot)`. Under that heading, add a macOS/Ninja subsection with these exact commands: `cmake -B build -G Ninja && ninja -C build mdcad_math_harness`, `cmake -B build -G Ninja && ninja -C build math-regression`, `cmake -B build -G Ninja && ninja -C build math-bench`, `cmake -B build -G Ninja && ninja -C build math-validation`, and `./build/bin/mdCAD`. Add a Windows Vulkan subsection with these exact commands: `cmake -B build-vulkan -G "Visual Studio 18" -DUSE_VULKAN=ON`, `cmake --build build-vulkan --config Release --target mdcad_math_harness`, `cmake --build build-vulkan --config Release --target math-regression`, `cmake --build build-vulkan --config Release --target math-bench`, and `cmake --build build-vulkan --config Release --target math-validation`. In prose, state that the standalone harness is the primary regression and benchmark surface for this phase, and launching `mdCAD` afterward is an optional manual smoke pass that remains secondary.</action>
  <verify>
    <automated>rg -n "Native Math Validation \\(Phase 2 pilot\\)|mdcad_math_harness|math-regression|math-bench|math-validation|secondary" docs/QUICKSTART.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/QUICKSTART.md` contains `## Native Math Validation (Phase 2 pilot)`
    - `docs/QUICKSTART.md` contains `cmake -B build -G Ninja && ninja -C build math-regression`
    - `docs/QUICKSTART.md` contains `cmake --build build-vulkan --config Release --target math-validation`
    - `docs/QUICKSTART.md` contains `standalone harness is the primary regression and benchmark surface for this phase`
    - `docs/QUICKSTART.md` contains `optional manual smoke pass that remains secondary`
  </acceptance_criteria>
  <done>The repo now tells future agents exactly how to run the Phase 2 pilot workflow on macOS and how to mirror it on Windows Vulkan.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmake -B build -G Ninja && ninja -C build math-validation` succeeds
- [ ] `docs/QUICKSTART.md` documents the exact target names `mdcad_math_harness`, `math-regression`, `math-bench`, and `math-validation`
- [ ] The documented workflow explicitly keeps the harness primary and app smoke secondary
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 2 ends with a repeatable native validation workflow that makes later subsystem rollout measurable and operational
  </success_criteria>

<output>
After completion, create `.planning/phases/02-direct-adoption-tooling-and-validation-harness/02-03-SUMMARY.md`
</output>
