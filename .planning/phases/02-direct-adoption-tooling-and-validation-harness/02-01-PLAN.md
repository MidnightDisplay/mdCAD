---
phase: 02-direct-adoption-tooling-and-validation-harness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/math/cglm_entry.h
  - src/math/math_compare.h
  - src/math/math_validate.h
  - src/math/math_bench.h
autonomous: true
requirements:
  - FOUND-02
user_setup: []
must_haves:
  truths:
    - "`src/math/cglm_entry.h` centralizes vendor config and convention includes without becoming a wrapper API"
    - "Phase 2 exposes migration-only compare, validate, and bench helpers under `src/math/` without aliasing vendor types"
    - "The entrypoint compile-checks the expected right-handed zero-to-one clip-control mode"
  artifacts:
    - path: "src/math/cglm_entry.h"
      provides: "Configured thin cglm entrypoint for direct adoption"
      contains: "CGLM_FORCE_DEPTH_ZERO_TO_ONE"
    - path: "src/math/math_compare.h"
      provides: "Legacy-to-cglm comparison helpers"
      contains: "mdcad_compare_mat4_close"
    - path: "src/math/math_validate.h"
      provides: "Harness validation reporting helpers"
      contains: "mdcad_validation_expect"
    - path: "src/math/math_bench.h"
      provides: "Harness timing helpers"
      contains: "mdcad_bench_run"
  key_links:
    - from: "src/math/cglm_entry.h"
      to: "vendors/cglm/include/cglm/common.h"
      via: "entrypoint config forces the vendor zero-to-one clip mode"
      pattern: "CGLM_FORCE_DEPTH_ZERO_TO_ONE"
    - from: "src/math/math_compare.h"
      to: "src/math3d.h"
      via: "comparison helpers bridge legacy vec3_t / mat4_t values without typedef aliases"
      pattern: "mat4_t"
    - from: "src/math/math_compare.h"
      to: "src/math/cglm_entry.h"
      via: "compare helpers read the same configured cglm boundary the harness will use"
      pattern: "cglm_entry\\.h"
---

<objective>
Convert `src/math/` from a compile-anchor-only boundary into a configured thin entrypoint plus migration-only helper headers.

Purpose: `FOUND-02` requires one project-owned math boundary that centralizes config and conventions without re-wrapping vendor math types.
Output: A configured include-only `cglm` entrypoint and adjacent compare/validate/bench helper headers under `src/math/`.
</objective>

<execution_context>
@/Users/rodionradchenko/Documents/repos/mdCAD/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rodionradchenko/Documents/repos/mdCAD/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-direct-adoption-tooling-and-validation-harness/02-CONTEXT.md
@.planning/phases/02-direct-adoption-tooling-and-validation-harness/02-RESEARCH.md
@docs/MATH_BACKEND_DECISION.md
@docs/MATH_CONVENTIONS.md
@src/math/cglm_entry.h
@src/math/math_conventions.h
@src/math3d.h
@vendors/cglm/include/cglm/common.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden `cglm_entry.h` as the configured thin entrypoint</name>
  <files>src/math/cglm_entry.h</files>
  <read_first>src/math/cglm_entry.h, src/math/math_conventions.h, docs/MATH_CONVENTIONS.md, docs/MATH_BACKEND_DECISION.md, vendors/cglm/include/cglm/common.h, .planning/phases/02-direct-adoption-tooling-and-validation-harness/02-CONTEXT.md, .planning/phases/02-direct-adoption-tooling-and-validation-harness/02-RESEARCH.md</read_first>
  <action>Update `src/math/cglm_entry.h` so it remains include-only while becoming the real vendor-config choke point for Phase 2. Keep `#include "math_conventions.h"` first. Before any vendor include, add `#ifndef CGLM_FORCE_DEPTH_ZERO_TO_ONE` / `#define CGLM_FORCE_DEPTH_ZERO_TO_ONE` / `#endif`. Do not define `CGLM_FORCE_LEFT_HANDED`. Keep the existing `#include <cglm/cglm.h>` and `#include <cglm/struct.h>` lines. Add `_Static_assert(CGLM_CONFIG_CLIP_CONTROL == CGLM_CLIP_CONTROL_RH_ZO, "mdCAD cglm entrypoint must use RH_ZO clip control");` after the vendor includes so the build fails if the vendor config drifts from the project contract. Retain the existing size/layout asserts for `mat4` and `mat4s`, and add a short comment stating that compare, validate, and bench helpers must live in adjacent headers rather than inside `cglm_entry.h`.</action>
  <verify>
    <automated>rg -n "CGLM_FORCE_DEPTH_ZERO_TO_ONE|CGLM_CONFIG_CLIP_CONTROL == CGLM_CLIP_CONTROL_RH_ZO|compare, validate, and bench helpers must live in adjacent headers|_Static_assert\\(sizeof\\(mat4\\) == sizeof\\(float\\) \\* 16|_Static_assert\\(sizeof\\(mat4s\\) == sizeof\\(mat4\\)" src/math/cglm_entry.h</automated>
  </verify>
  <acceptance_criteria>
    - `src/math/cglm_entry.h` contains `CGLM_FORCE_DEPTH_ZERO_TO_ONE`
    - `src/math/cglm_entry.h` contains `CGLM_CONFIG_CLIP_CONTROL == CGLM_CLIP_CONTROL_RH_ZO`
    - `src/math/cglm_entry.h` contains `compare, validate, and bench helpers must live in adjacent headers`
    - `src/math/cglm_entry.h` still contains `#include <cglm/cglm.h>` and `#include <cglm/struct.h>`
    - `src/math/cglm_entry.h` does not contain `typedef vec3s` or `typedef mat4s`
  </acceptance_criteria>
  <done>The entrypoint now centralizes Phase-2 vendor config and compile-checks the expected clip-control mode without becoming a wrapper API.</done>
</task>

<task type="auto">
  <name>Task 2: Add migration-only compare, validate, and bench helper headers</name>
  <files>src/math/math_compare.h, src/math/math_validate.h, src/math/math_bench.h</files>
  <read_first>src/math3d.h, src/math/cglm_entry.h, src/orbit_camera.h, src/gizmo/gizmo.h, .planning/phases/02-direct-adoption-tooling-and-validation-harness/02-CONTEXT.md, .planning/phases/02-direct-adoption-tooling-and-validation-harness/02-RESEARCH.md</read_first>
  <action>Create three new header-only helpers under `src/math/`. In `src/math/math_compare.h`, include `"cglm_entry.h"` and `"../math3d.h"` and add these exact `static inline` helpers: `mdcad_compare_float_close(float lhs, float rhs, float epsilon)`, `mdcad_compare_vec3_close(vec3_t legacy, const float candidate[3], float epsilon)`, `mdcad_compare_mat4_close(mat4_t legacy, const float candidate[16], float epsilon)`, and `mdcad_compare_ray_close(ray_t legacy, const float candidate_origin[3], const float candidate_direction[3], float epsilon)`. In `src/math/math_validate.h`, add `typedef struct { int checks_run; int checks_failed; } mdcad_validation_report_t;`, plus `mdcad_validation_expect(mdcad_validation_report_t *report, bool condition, const char *label)` and `mdcad_validation_exit_code(const mdcad_validation_report_t *report)`. In `src/math/math_bench.h`, include `<stdint.h>` and `<time.h>`, add `typedef void (*mdcad_bench_fn)(void *ctx);`, `typedef struct { const char *name; uint64_t iterations; double elapsed_ms; } mdcad_bench_result_t;`, `mdcad_bench_now_ms(void)` implemented with `timespec_get`, and `mdcad_bench_run(const char *name, uint64_t iterations, mdcad_bench_fn fn, void *ctx)`. Do not introduce any mdCAD typedef aliases for `vec3s`, `mat4s`, `mat4`, or other vendor types.</action>
  <verify>
    <automated>rg -n "mdcad_compare_float_close|mdcad_compare_vec3_close|mdcad_compare_mat4_close|mdcad_compare_ray_close|mdcad_validation_expect|mdcad_validation_exit_code|mdcad_bench_now_ms|mdcad_bench_run|timespec_get" src/math/math_compare.h src/math/math_validate.h src/math/math_bench.h</automated>
  </verify>
  <acceptance_criteria>
    - `src/math/math_compare.h` contains `mdcad_compare_mat4_close(mat4_t legacy, const float candidate[16], float epsilon)`
    - `src/math/math_compare.h` contains `mdcad_compare_ray_close(ray_t legacy, const float candidate_origin[3], const float candidate_direction[3], float epsilon)`
    - `src/math/math_validate.h` contains `typedef struct { int checks_run; int checks_failed; } mdcad_validation_report_t;`
    - `src/math/math_validate.h` contains `mdcad_validation_expect`
    - `src/math/math_bench.h` contains `timespec_get`
    - `src/math/math_bench.h` contains `mdcad_bench_run(const char *name, uint64_t iterations, mdcad_bench_fn fn, void *ctx)`
    - None of the new headers contain `typedef vec3s`, `typedef mat4s`, or `typedef versor`
  </acceptance_criteria>
  <done>Phase 2 now has a thin comparison/validation/benchmark helper surface under `src/math/` that supports both struct and array `cglm` call sites without becoming a compatibility facade.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "CGLM_FORCE_DEPTH_ZERO_TO_ONE|CGLM_CONFIG_CLIP_CONTROL == CGLM_CLIP_CONTROL_RH_ZO|mdcad_compare_mat4_close|mdcad_validation_expect|mdcad_bench_run" src/math/cglm_entry.h src/math/math_compare.h src/math/math_validate.h src/math/math_bench.h` succeeds
- [ ] `src/math/cglm_entry.h` still reads as an include-only entrypoint, not a wrapper API
- [ ] The new helper headers contain no mdCAD alias typedefs for vendor math types
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 2 has a concrete `FOUND-02` math boundary that later harness and migration work can build on
  </success_criteria>

<output>
After completion, create `.planning/phases/02-direct-adoption-tooling-and-validation-harness/02-01-SUMMARY.md`
</output>
