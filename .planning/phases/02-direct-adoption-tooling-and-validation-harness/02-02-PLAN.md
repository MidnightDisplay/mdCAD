---
phase: 02-direct-adoption-tooling-and-validation-harness
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/CMakeLists.txt
  - src/math_harness.c
  - src/math/math_compare.h
  - src/math/math_validate.h
  - src/math/math_bench.h
autonomous: true
requirements:
  - FOUND-02
  - PERF-01
user_setup: []
must_haves:
  truths:
    - "A standalone target named `mdcad_math_harness` builds in the native workflow without depending on the full app runtime"
    - "The harness can run compare-mode workflow checks and bench-mode primitive timings from the command line"
    - "Phase 2's first compare cases explicitly protect camera/view, projection-sensitive behavior, screen-ray reconstruction, and transform composition"
    - "Phase 2's compare harness is intentionally struct-first for camera/app/gizmo-shaped cases, while ECS and GPU-heavy follow-on migration remains free to adopt the array API where it measures better"
  artifacts:
    - path: "src/CMakeLists.txt"
      provides: "Standalone native harness target wiring"
      contains: "add_executable(mdcad_math_harness"
    - path: "src/math_harness.c"
      provides: "Standalone compare + bench executable"
      contains: "--mode compare"
    - path: "src/math_harness.c"
      provides: "Workflow compare case identifiers"
      contains: "orbit-camera-view"
    - path: "src/math_harness.c"
      provides: "Primitive benchmark identifiers"
      contains: "legacy-mat4-mul"
  key_links:
    - from: "src/math_harness.c"
      to: "src/orbit_camera.h"
      via: "harness compares the legacy orbit-camera view path against cglm struct API output"
      pattern: "orbit_camera_get_view_matrix"
    - from: "src/math_harness.c"
      to: "src/math/math_compare.h"
      via: "all case assertions use the shared comparison helpers"
      pattern: "mdcad_compare_mat4_close"
    - from: "src/CMakeLists.txt"
      to: "src/math_harness.c"
      via: "native build wires the standalone harness target"
      pattern: "mdcad_math_harness"
---

<objective>
Add the standalone native harness and populate it with workflow comparisons plus primitive microbenchmarks.

Purpose: `PERF-01` requires repeatable comparison and benchmark entry points before hotspot migration begins, and `FOUND-02` requires those tools to sit behind the thin `src/math/` boundary.
Output: A buildable `mdcad_math_harness` executable with compare, bench, and case-listing modes, using a struct-first bias for camera/app/gizmo-facing cases without imposing a universal API-family rule on later ECS or GPU migration.
</objective>

<execution_context>
@/Users/rodionradchenko/Documents/repos/mdCAD/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rodionradchenko/Documents/repos/mdCAD/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-direct-adoption-tooling-and-validation-harness/02-CONTEXT.md
@.planning/phases/02-direct-adoption-tooling-and-validation-harness/02-RESEARCH.md
@.planning/phases/02-direct-adoption-tooling-and-validation-harness/02-01-SUMMARY.md
@docs/MATH_CONVENTIONS.md
@src/CMakeLists.txt
@src/math3d.h
@src/orbit_camera.h
@src/app.c
@src/gizmo/gizmo.h
@src/math/cglm_entry.h
@src/math/math_compare.h
@src/math/math_validate.h
@src/math/math_bench.h
@vendors/cglm/include/cglm/struct/cam.h
@vendors/cglm/include/cglm/struct/mat4.h
@vendors/cglm/include/cglm/struct/clipspace/project_zo.h
@vendors/cglm/include/cglm/struct/affine.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the standalone `mdcad_math_harness` target and CLI skeleton</name>
  <files>src/CMakeLists.txt, src/math_harness.c</files>
  <read_first>src/CMakeLists.txt, CMakeLists.txt, .planning/phases/02-direct-adoption-tooling-and-validation-harness/02-CONTEXT.md, .planning/phases/02-direct-adoption-tooling-and-validation-harness/02-RESEARCH.md</read_first>
  <action>In `src/CMakeLists.txt`, add a native-desktop-only target named `mdcad_math_harness` backed by `math_harness.c`. Keep it lightweight: do not link `libsokol`, `libcimgui`, `flecs_static`, or `cjson` unless the source truly requires them. Place the target behind a platform guard that skips Android and iOS. Reuse the vendored `../vendors/cglm/include` include directory for this target and add `target_include_directories(mdcad_math_harness PRIVATE ${CMAKE_CURRENT_SOURCE_DIR} ${CMAKE_CURRENT_SOURCE_DIR}/../vendors/cglm/include)`. On non-Apple Unix platforms, add `target_link_libraries(mdcad_math_harness PRIVATE m)` so the standalone target resolves `math.h` functions. Create `src/math_harness.c` with `main(int argc, char **argv)` that supports these exact flags and modes: `--list`, `--mode compare`, `--mode bench`, `--iterations <N>`, and `--strict`. `--list` must print available compare and bench identifiers without running them. Compare mode must return non-zero when any comparison check fails. Bench mode must print named timing rows but still exit 0 on success.</action>
  <verify>
    <automated>cmake -B build -G Ninja && ninja -C build mdcad_math_harness && ./build/bin/mdcad_math_harness --list | rg -n "orbit-camera-view|view-projection-roundtrip|screen-ray-unproject|transform-compose|legacy-mat4-mul|cglm-mat4-mul"</automated>
  </verify>
  <acceptance_criteria>
    - `src/CMakeLists.txt` contains `add_executable(mdcad_math_harness`
    - `src/CMakeLists.txt` contains `target_include_directories(mdcad_math_harness PRIVATE ${CMAKE_CURRENT_SOURCE_DIR} ${CMAKE_CURRENT_SOURCE_DIR}/../vendors/cglm/include)`
    - `src/math_harness.c` contains `--mode compare`
    - `src/math_harness.c` contains `--mode bench`
    - `src/math_harness.c` contains `--list`
    - `cmake -B build -G Ninja && ninja -C build mdcad_math_harness` exits 0
    - `./build/bin/mdcad_math_harness --list` prints all named compare and bench cases
  </acceptance_criteria>
  <done>The repo now builds a standalone native harness target with a concrete CLI surface for Phase 2 math validation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement workflow compare suites and primitive benchmark suites</name>
  <files>src/math_harness.c, src/math/math_compare.h, src/math/math_validate.h, src/math/math_bench.h</files>
  <read_first>src/math_harness.c, src/math3d.h, src/orbit_camera.h, src/app.c, src/gizmo/gizmo.h, src/math/math_compare.h, src/math/math_validate.h, src/math/math_bench.h, vendors/cglm/include/cglm/struct/cam.h, vendors/cglm/include/cglm/struct/mat4.h, vendors/cglm/include/cglm/struct/clipspace/project_zo.h, vendors/cglm/include/cglm/struct/affine.h, .planning/phases/02-direct-adoption-tooling-and-validation-harness/02-CONTEXT.md, .planning/phases/02-direct-adoption-tooling-and-validation-harness/02-RESEARCH.md</read_first>
  <action>Populate `src/math_harness.c` with both compare-mode workflow suites and bench-mode primitive suites. Add these exact compare identifiers: `orbit-camera-view`, `view-projection-roundtrip`, `screen-ray-unproject`, and `transform-compose`. Implement `orbit-camera-view` by constructing the same eye/target/up inputs used by `orbit_camera_get_view_matrix` and comparing the legacy view matrix against `glms_lookat_rh_zo(...)` using `mdcad_compare_mat4_close(..., 1e-4f)`. Implement `view-projection-roundtrip` by composing a legacy `mat4_perspective(...)` and view matrix, composing the new path with `glms_perspective_rh_zo(...)` and `glms_mat4_mul(...)`, projecting the same sample points through both paths, and comparing NDC X/Y after perspective divide instead of requiring raw matrix equality. Implement `screen-ray-unproject` by comparing legacy `ray_from_screen(...)` against a new path that computes near/far world points with `glms_unprojecti_zo(...)` for viewport `{0.0f, 0.0f, 1280.0f, 720.0f}`, then normalizes the resulting world-space direction and compares origin/direction with `mdcad_compare_ray_close(..., 1e-4f)`. Implement `transform-compose` by comparing a legacy compose path using `mat4_translate`, `mat4_rotate_x`, `mat4_rotate_y`, `mat4_rotate_z`, `mat4_scale`, `mat4_mul`, and `mat4_mul_point` against a `cglm` compose path using `glms_translate_make`, `glms_rotate_x`, `glms_rotate_y`, `glms_rotate_z`, `glms_scale_make`, and `glms_mat4_mul` on the same sample point set. Add these exact primitive bench identifiers: `legacy-mat4-mul`, `cglm-mat4-mul`, `legacy-mat4-inverse`, `cglm-mat4-inv`, `legacy-screen-ray`, and `cglm-screen-ray`. Emit compare results with `COMPARE PASS` / `COMPARE FAIL` prefixes and bench results with `BENCH` prefixes so scripts can grep them reliably.</action>
  <verify>
    <automated>cmake -B build -G Ninja && ninja -C build mdcad_math_harness && ./build/bin/mdcad_math_harness --mode compare --strict && ./build/bin/mdcad_math_harness --mode bench --iterations 1000 | rg -n "^BENCH (legacy-mat4-mul|cglm-mat4-mul|legacy-mat4-inverse|cglm-mat4-inv|legacy-screen-ray|cglm-screen-ray)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/math_harness.c` contains `orbit-camera-view`, `view-projection-roundtrip`, `screen-ray-unproject`, and `transform-compose`
    - `src/math_harness.c` contains `glms_lookat_rh_zo`, `glms_perspective_rh_zo`, `glms_mat4_mul`, and `glms_unprojecti_zo`
    - `src/math_harness.c` contains `legacy-mat4-mul`, `cglm-mat4-mul`, `legacy-mat4-inverse`, `cglm-mat4-inv`, `legacy-screen-ray`, and `cglm-screen-ray`
    - `./build/bin/mdcad_math_harness --mode compare --strict` exits 0
    - `./build/bin/mdcad_math_harness --mode bench --iterations 1000` prints `BENCH` lines for all six primitive suites
  </acceptance_criteria>
  <done>Phase 2 now has a standalone harness that proves both behavior-level comparisons and primitive microbench entry points exist for the upcoming migration slices.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cmake -B build -G Ninja && ninja -C build mdcad_math_harness` succeeds
- [ ] `./build/bin/mdcad_math_harness --mode compare --strict` succeeds
- [ ] `./build/bin/mdcad_math_harness --list` includes all four compare cases and all six bench cases
- [ ] Compare mode protects behavior-sensitive workflows instead of relying on raw projection-matrix equality alone
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 2 has a real standalone compare-and-bench harness that satisfies the first half of `PERF-01`
  </success_criteria>

<output>
After completion, create `.planning/phases/02-direct-adoption-tooling-and-validation-harness/02-02-SUMMARY.md`
</output>
