---
phase: 36-flat-import-entry-configuration
plan: 02
type: execute
wave: 2
depends_on: [36-01]
files_modified:
  - src/ui/ui_scene_hierarchy.h
  - src/jsonl_import_job.h
  - src/tests/jsonl_flat_import_ui_contract_test.c
  - src/tests/jsonl_flat_import_options_contract_test.c
autonomous: true
requirements: [FIMP-01, FIMP-02]
must_haves:
  truths:
    - "User can launch flat JSONL import from a dedicated explicit File action for large dumps."
    - "User can configure units/scale, colour behavior, shift-to-CoM, XYZ rotation, and mesh mode before import starts."
    - "Import start honors selected options and preserves existing tiny-file sync + non-trivial async progress behavior."
    - "Flat-flow observer link control is optional and defaults OFF."
  artifacts:
    - path: "src/ui/ui_scene_hierarchy.h"
      provides: "Dedicated flat-large file action, popup contract, and import-start wiring."
      contains: "Import JSONL (Flat Large Dump)..."
    - path: "src/jsonl_import_job.h"
      provides: "Forward-compatible import-time observer contract capture for flat flow."
      contains: "jsonl_import_job_set_observer_contract"
    - path: "src/tests/jsonl_flat_import_options_contract_test.c"
      provides: "Green verification for option mapping and start-path contract."
      contains: "test_flat_import_options_to_job_start_contract"
  key_links:
    - from: "src/ui/ui_scene_hierarchy.h"
      to: "src/jsonl_import_job.h"
      via: "jsonl_import_job_start + observer-contract setter during submit"
      pattern: "jsonl_import_job_start|jsonl_import_job_set_observer_contract"
    - from: "src/ui/ui_scene_hierarchy.h"
      to: "jsonl_quick_scan_mesh"
      via: "file select -> quick scan -> flat options popup"
      pattern: "jsonl_quick_scan_mesh"
---

<objective>
Implement Phase 36 flat-import entry path and configuration contract without pulling in Phase 37+ ingest/refresh implementation.

Purpose: Deliver user-visible entry + option semantics for FIMP-01/FIMP-02 while preserving existing JSONL flows.
Output: Dedicated flat-large menu flow, mirrored options popup with observer opt-in default OFF, and contract tests passing.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/36-flat-import-entry-configuration/36-CONTEXT.md
@.planning/phases/36-flat-import-entry-configuration/36-RESEARCH.md
@.planning/phases/36-flat-import-entry-configuration/36-VALIDATION.md
@.planning/phases/36-flat-import-entry-configuration/36-01-SUMMARY.md
@src/ui/ui_scene_hierarchy.h
@src/jsonl_import_job.h
@src/jsonl_loader.h
@src/components/jsonl_observer_comp.h

<interfaces>
From src/jsonl_import_job.h:
```c
static inline bool jsonl_import_job_start(jsonl_import_job_t *job,
                                          const char *filepath,
                                          float scale,
                                          bool use_jsonl_colours,
                                          vec4_t default_colour,
                                          bool shift_to_com,
                                          float rotation_x,
                                          float rotation_y,
                                          float rotation_z);
static inline bool jsonl_import_job_should_sync(const jsonl_import_job_t *job);
static inline bool jsonl_import_job_tick(jsonl_import_job_t *job, ecs_scene_t *scene);
static inline void jsonl_import_job_set_mesh_mode(jsonl_import_job_t *job, int mode);
```

From src/jsonl_loader.h:
```c
static inline jsonl_error_t jsonl_quick_scan_mesh(const char *filepath,
                                                  int *out_entry_count,
                                                  int *out_element_count,
                                                  bool *out_has_mesh,
                                                  int *out_mesh_vertices,
                                                  int *out_mesh_faces);
```

From src/components/jsonl_observer_comp.h:
```c
#define JSONL_OBSERVER_PATH_MAX 1024
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dedicated flat-large menu/browser/dialog contract in scene hierarchy UI</name>
  <read_first>@src/ui/ui_scene_hierarchy.h, @.planning/phases/36-flat-import-entry-configuration/36-CONTEXT.md, @src/tests/jsonl_flat_import_ui_contract_test.c, @src/tests/jsonl_flat_import_options_contract_test.c</read_first>
  <files>src/ui/ui_scene_hierarchy.h, src/tests/jsonl_flat_import_ui_contract_test.c</files>
  <behavior>
    - Test 1: New File action `Import JSONL (Flat Large Dump)...` is present (D-01).
    - Test 2: Existing `Import JSONL Geometry Log...` and `Import JSONL as Sketch` remain unchanged (D-02).
    - Test 3: Flat dialog mirrors units/colours/default colour/shift-to-CoM/XYZ rotation/mesh-mode controls (D-03).
    - Test 4: Flat dialog includes `Link file for refresh (optional)` with default OFF (D-05, D-06).
  </behavior>
  <action>Add separate flat-large browser/popup state fields in `ui_scene_hierarchy_state_t` to avoid state collision with existing JSONL flows. Implement dedicated menu action and file-picker path, quick scan via `jsonl_quick_scan_mesh`, and flat options popup that mirrors current JSONL options per D-03 while adding observer opt-in toggle text per D-05 with OFF default per D-06. Keep existing JSONL Geometry Log and JSONL as Sketch flows intact per D-02.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "jsonl_flat_import_ui_contract_test" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>UI contract test passes and source contains dedicated flat-large action plus separate popup state fields, with no removal/rename of the two existing JSONL actions.</acceptance_criteria>
  <done>FIMP-01 dedicated-entry behavior is implemented and regression-protected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire option-to-import-start mapping and forward-compatible observer capture</name>
  <read_first>@src/jsonl_import_job.h, @src/ui/ui_scene_hierarchy.h, @src/tests/jsonl_flat_import_options_contract_test.c, @.planning/phases/36-flat-import-entry-configuration/36-RESEARCH.md</read_first>
  <files>src/jsonl_import_job.h, src/ui/ui_scene_hierarchy.h, src/tests/jsonl_flat_import_options_contract_test.c</files>
  <behavior>
    - Test 1: Selected units/rotation/shift/colour/mesh mode are mapped into import-job start contract (D-03).
    - Test 2: Observer opt-in choice/path are captured in a forward-compatible job contract shape with default OFF (D-06, D-07).
    - Test 3: Tiny-file sync fast path and async progress-popup behavior remain active exactly as existing patterns (D-08, D-09).
  </behavior>
  <action>Introduce a minimal forward-compatible observer-contract payload on `jsonl_import_job_t` (e.g., link-enabled flag + source path + capture marker) plus a setter helper such as `jsonl_import_job_set_observer_contract(...)`; use it at flat import submit to store user choice/path per D-07 without implementing Phase 38 refresh logic. Map dialog controls into `jsonl_import_job_start(...)` and `jsonl_import_job_set_mesh_mode(...)`. Preserve existing sync-threshold semantics (`jsonl_import_job_should_sync`) and existing progress-popup style/messages for async path per D-04/D-08/D-09. Do not implement anchor ingest details (Phase 37) or refresh loop/manual refresh engine (Phases 38/39).</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "jsonl_flat_import_options_contract_test|jsonl_flat_import_ui_contract_test" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>Both Phase 36 contract tests pass and code shows observer opt-in contract captured at submit boundary with default OFF while preserving existing job sync/async behavior.</acceptance_criteria>
  <done>FIMP-02 option-contract behavior is implemented and import start uses selected settings with preserved progress model.</done>
</task>

</tasks>

<verification>
- `cmake --build build-vulkan --config Release`
- `ctest --test-dir build-vulkan -C Release -R "jsonl_flat_import_ui_contract_test|jsonl_flat_import_options_contract_test|jsonl_loader_limits_test|jsonl_sketch_import_test|jsonl_observer_state_test" --output-on-failure`
</verification>

<success_criteria>
- Dedicated flat-large import entry exists and is discoverable from File menu.
- Flat import dialog exposes full required option contract and observer link toggle default OFF.
- Import start path honors selected options while preserving established sync/async progress behavior.
- Scope remains strictly Phase 36 (entry + configuration contract only).
</success_criteria>

<output>
After completion, create `.planning/phases/36-flat-import-entry-configuration/36-02-SUMMARY.md`
</output>
