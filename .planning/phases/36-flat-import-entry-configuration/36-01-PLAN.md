---
phase: 36-flat-import-entry-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/CMakeLists.txt
  - src/tests/jsonl_flat_import_ui_contract_test.c
  - src/tests/jsonl_flat_import_options_contract_test.c
autonomous: true
requirements: [FIMP-01, FIMP-02]
must_haves:
  truths:
    - "Phase 36 has executable automated tests that lock dedicated flat-import entry behavior and option-contract behavior before implementation."
    - "CTest can discover dedicated Phase 36 flat-import contract tests by name."
  artifacts:
    - path: "src/tests/jsonl_flat_import_ui_contract_test.c"
      provides: "Contract coverage for dedicated menu/import-mode entry and non-regression of existing JSONL actions."
      contains: "test_flat_import_menu_entry_contract"
    - path: "src/tests/jsonl_flat_import_options_contract_test.c"
      provides: "Contract coverage for mirrored option controls, observer opt-in default OFF, and import-start behavior mapping."
      contains: "test_flat_import_options_to_job_start_contract"
    - path: "src/CMakeLists.txt"
      provides: "CTest wiring for both Phase 36 contract tests."
      contains: "add_test(NAME jsonl_flat_import_ui_contract_test"
  key_links:
    - from: "src/CMakeLists.txt"
      to: "src/tests/jsonl_flat_import_ui_contract_test.c"
      via: "add_executable/add_test"
      pattern: "jsonl_flat_import_ui_contract_test"
    - from: "src/CMakeLists.txt"
      to: "src/tests/jsonl_flat_import_options_contract_test.c"
      via: "add_executable/add_test"
      pattern: "jsonl_flat_import_options_contract_test"
---

<objective>
Create Wave 0 executable test contracts for Phase 36 flat import entry + configuration behavior.

Purpose: Lock FIMP-01/FIMP-02 behavior before implementation and satisfy Nyquist automated-verification expectations.
Output: Two dedicated Phase 36 contract tests registered in CTest.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/36-flat-import-entry-configuration/36-CONTEXT.md
@.planning/phases/36-flat-import-entry-configuration/36-RESEARCH.md
@.planning/phases/36-flat-import-entry-configuration/36-VALIDATION.md
@src/CMakeLists.txt
@src/ui/ui_scene_hierarchy.h
@src/jsonl_import_job.h
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author RED-first flat-import contract tests for entry and options</name>
  <read_first>@.planning/phases/36-flat-import-entry-configuration/36-CONTEXT.md, @.planning/phases/36-flat-import-entry-configuration/36-VALIDATION.md, @src/tests/jsonl_sketch_import_test.c, @src/tests/jsonl_loader_limits_test.c, @src/ui/ui_scene_hierarchy.h</read_first>
  <files>src/tests/jsonl_flat_import_ui_contract_test.c, src/tests/jsonl_flat_import_options_contract_test.c</files>
  <behavior>
    - Test 1: Dedicated menu action label `Import JSONL (Flat Large Dump)...` exists and existing JSONL actions remain unchanged (D-01, D-02).
    - Test 2: Flat dialog contract includes units, colour/default colour, shift-to-CoM, XYZ rotation, and mesh mode when mesh exists (D-03).
    - Test 3: Flat dialog includes `Link file for refresh (optional)` default OFF and captures opt-in intent only (D-05, D-06, D-07).
    - Test 4: Submit preserves tiny-file sync fast path and async progress-popup path for non-trivial imports (D-08, D-09).
  </behavior>
  <action>Create both Phase 36 test files with deterministic contract assertions and explicit failure messages. Keep tests scoped strictly to Phase 36 entry/configuration contract (no anchor ingest internals, no refresh-loop implementation). Mark as RED-first where implementation does not yet exist.</action>
  <verify>
    <automated>git --no-pager grep -n "test_flat_import_\\|Import JSONL (Flat Large Dump)\\|Link file for refresh (optional)" src/tests/jsonl_flat_import_ui_contract_test.c src/tests/jsonl_flat_import_options_contract_test.c</automated>
  </verify>
  <acceptance_criteria>Both new test files exist, each contains at least one `static int test_...` contract function, and assertions explicitly reference D-01..D-09 behaviors relevant to Phase 36 scope.</acceptance_criteria>
  <done>Executable RED-first contract tests exist for FIMP-01/FIMP-02 behavior boundaries.</done>
</task>

<task type="auto">
  <name>Task 2: Register Phase 36 contract tests in CTest</name>
  <read_first>@src/CMakeLists.txt, @src/tests/jsonl_sketch_import_test.c, @src/tests/jsonl_observer_state_test.c</read_first>
  <files>src/CMakeLists.txt</files>
  <action>Add `add_executable(...)`, `target_include_directories(...)`, `target_link_libraries(...)`, and `add_test(...)` entries for `jsonl_flat_import_ui_contract_test` and `jsonl_flat_import_options_contract_test` following existing jsonl_* test wiring patterns.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -N -R "jsonl_flat_import_ui_contract_test|jsonl_flat_import_options_contract_test"</automated>
  </verify>
  <acceptance_criteria>CTest discovery lists both new tests by exact name with no wiring errors.</acceptance_criteria>
  <done>Phase 36 test contracts are runnable by targeted CTest commands.</done>
</task>

</tasks>

<verification>
- `cmake --build build-vulkan --config Release`
- `ctest --test-dir build-vulkan -C Release -N -R "jsonl_flat_import_ui_contract_test|jsonl_flat_import_options_contract_test"`
</verification>

<success_criteria>
- Dedicated Phase 36 UI/options contract tests exist and are wired.
- FIMP-01 and FIMP-02 are represented by executable tests before feature implementation.
</success_criteria>

<output>
After completion, create `.planning/phases/36-flat-import-entry-configuration/36-01-SUMMARY.md`
</output>
