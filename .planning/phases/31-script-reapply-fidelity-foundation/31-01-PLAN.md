---
phase: 31-script-reapply-fidelity-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/scripting/sketch_script_capability_registry.h
  - src/scripting/sketch_script_contract.h
  - src/constraints/constraint_types.h
  - src/ecs/ecs_scene.h
  - src/tests/script_roundtrip_tests.c
  - src/tests/scene_solver_contract_test.c
autonomous: true
requirements: [SCRI-01]
must_haves:
  truths:
    - "Valid scripted participant signatures are accepted consistently by both script validation and solver legality checks."
    - "Unsupported or out-of-contract script signatures fail with explicit deterministic contract errors instead of unsupported-participant drift."
    - "Script apply no longer relies on duplicated script-only allowlists for geometry/constraint capabilities."
  artifacts:
    - path: "src/scripting/sketch_script_capability_registry.h"
      provides: "Single-source registry for supported geometry/constraint types and legal participant role signatures."
      contains: "registry lookup helpers shared by script contract and solver legality paths"
    - path: "src/scripting/sketch_script_contract.h"
      provides: "Script declaration validation delegated to shared registry (no local hardcoded allowlists)."
      contains: "registry-backed entity/constraint declaration checks"
    - path: "src/ecs/ecs_scene.h"
      provides: "Solver-side legality/admission checks consume shared registry-derived capability verdicts."
      contains: "explicit deterministic contract error mapping for unsupported combinations"
    - path: "src/tests/scene_solver_contract_test.c"
      provides: "Regression assertions proving script/solver capability parity from the shared registry."
      contains: "deterministic failure taxonomy assertions for unsupported signatures"
  key_links:
    - from: "src/scripting/sketch_script_contract.h"
      to: "src/scripting/sketch_script_capability_registry.h"
      via: "script declaration validators"
      pattern: "sketch_script_capability_"
    - from: "src/ecs/ecs_scene.h"
      to: "src/scripting/sketch_script_capability_registry.h"
      via: "constraint legality admission path"
      pattern: "sketch_script_capability_"
    - from: "src/tests/scene_solver_contract_test.c"
      to: "src/scripting/sketch_script_capability_registry.h"
      via: "parity and deterministic error assertions"
      pattern: "capability|deterministic|contract"
---

<objective>
Establish a shared script+solver capability registry and deterministic contract-failure surface so script re-apply legality is a single-source truth.

Purpose: Eliminate script/solver allowlist drift and enforce explicit deterministic failures for unsupported signatures per D-13, D-14, D-15 and D-09.
Output: Shared registry module, contract and solver legality wiring, plus parity regression coverage.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-script-reapply-fidelity-foundation/31-CONTEXT.md
@.planning/phases/31-script-reapply-fidelity-foundation/31-RESEARCH.md
@.planning/phases/31-script-reapply-fidelity-foundation/31-VALIDATION.md
@src/scripting/sketch_script_contract.h
@src/constraints/constraint_types.h
@src/ecs/ecs_scene.h
@src/tests/scene_solver_contract_test.c
@src/tests/script_roundtrip_tests.c

<interfaces>
From src/scripting/sketch_script_contract.h:
```c
static inline bool sketch_script_contract_validate_entity_decl(...);
static inline bool sketch_script_contract_validate_constraint_decl(...);
```

From src/constraints/constraint_types.h:
```c
static inline bool constraint_type_is_selection_legal(constraint_selection_signature_t *sig,
                                                      constraint_type_t type);
static inline uint32_t constraint_type_min_participants(constraint_type_t type);
```

From src/ecs/ecs_scene.h:
```c
static inline ecs_entity_t scene_add_constraint_to_sketch_with_descriptors(...);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared capability registry and remove dual-maintained allowlists</name>
  <read_first>@.planning/phases/31-script-reapply-fidelity-foundation/31-CONTEXT.md, @src/scripting/sketch_script_contract.h, @src/constraints/constraint_types.h, @src/ecs/ecs_scene.h</read_first>
  <files>src/scripting/sketch_script_capability_registry.h, src/scripting/sketch_script_contract.h, src/constraints/constraint_types.h, src/ecs/ecs_scene.h</files>
  <behavior>
    - Test 1: Shared registry reports identical support decisions for script contract and solver legality for valid participant-role signatures (per D-13, D-14).
    - Test 2: Unsupported/out-of-contract participant-role signatures produce explicit deterministic contract failures sourced from registry helpers (per D-15, D-09).
    - Test 3: No script-side hardcoded geometry/constraint allowlists remain in `sketch_script_contract.h` (per D-14).
  </behavior>
  <action>Create `sketch_script_capability_registry.h` as the single source for supported geometry kinds, supported constraint types, required participant role signatures, and minimum participant counts (per D-13). Refactor `sketch_script_contract.h` to remove local hardcoded allowlists and delegate declaration validation to registry helpers (per D-14). Update solver legality/admission touchpoints in `constraint_types.h` and `ecs_scene.h` to consult the same registry-derived capability checks before creating constraints, and emit deterministic explicit contract errors for unsupported combinations (per D-15 and D-09). Do not keep any entity-only fallback compatibility path (per D-03).</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "scene_solver_contract_test|scene_solver_contract|script_roundtrip_tests" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`git --no-pager grep -n "point/line/arc/circle" src/scripting/sketch_script_contract.h` no longer shows hardcoded allowlist branches; `sketch_script_capability_registry.h` exists and is included by both script contract and solver legality codepaths; targeted ctest slice passes.</acceptance_criteria>
  <done>Shared capability registry is the only contract authority for script+solver legality and unsupported signatures fail deterministically.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add parity and deterministic taxonomy regression coverage for shared registry</name>
  <read_first>@src/tests/scene_solver_contract_test.c, @src/tests/script_roundtrip_tests.c, @src/scripting/sketch_script_capability_registry.h, @.planning/phases/31-script-reapply-fidelity-foundation/31-VALIDATION.md</read_first>
  <files>src/tests/scene_solver_contract_test.c, src/tests/script_roundtrip_tests.c</files>
  <behavior>
    - Test 1: Registry-backed script validation and solver legality both accept the same valid descriptor signature set (per D-13).
    - Test 2: Registry-backed script validation rejects unsupported signatures with a deterministic explicit contract error string (per D-15, D-09).
    - Test 3: Reapplying script with unsupported signature fails before mutation and does not emit unsupported-participant drift messages (per D-02, D-07, D-08).
  </behavior>
  <action>Add focused tests in `scene_solver_contract_test.c` and `script_roundtrip_tests.c` that compare script validation outcomes against solver legality outcomes for the same signatures (parity assertions per D-13). Add deterministic error taxonomy assertions that match exact expected prefixes/messages for unsupported signatures and missing descriptor intent (per D-02, D-09, D-15). Include atomicity checks proving scene geometry/constraint counts are unchanged when registry contract validation rejects input (per D-07, D-08).</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "script_roundtrip_tests|scene_solver_contract_test|scene_solver_contract" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>New tests with names containing `registry_parity`, `deterministic_contract_error`, and `atomic_reject` are present and green; failure assertions compare explicit message text fragments rather than only success/failure booleans.</acceptance_criteria>
  <done>Regression suite locks shared-registry parity and deterministic contract failure behavior for SCRI-01 foundation.</done>
</task>

</tasks>

<verification>
Run:
- `ctest --test-dir build-vulkan -C Release -R "script_roundtrip_tests|scene_solver_contract_test|scene_solver_contract" --output-on-failure`
</verification>

<success_criteria>
- SCRI-01 legality contracts are single-source across script and solver.
- Unsupported combinations now fail with deterministic explicit contract taxonomy, not participant-type drift.
- Shared registry parity tests are green and prevent future dual-maintenance drift.
</success_criteria>

<output>
After completion, create `.planning/phases/31-script-reapply-fidelity-foundation/31-01-SUMMARY.md`
</output>

