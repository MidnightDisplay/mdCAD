---
phase: 31-script-reapply-fidelity-foundation
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - src/scripting/sketch_script_parse.h
  - src/scripting/sketch_script_apply.h
  - src/scripting/sketch_script_emit.h
  - src/tests/script_roundtrip_tests.c
  - src/tests/scene_solver_diagnostics_test.c
  - .planning/phases/31-script-reapply-fidelity-foundation/31-VALIDATION.md
autonomous: true
requirements: [SCRI-01, SCRI-02, SCRI-03]
must_haves:
  truths:
    - "User re-applying valid scripts keeps participant role/sub-index intent intact across parse/apply/emit cycles."
    - "User re-applying scripts keeps script-managed point/line/arc colors exactly preserved instead of resetting to white."
    - "User running repeated reapply gets deterministic emitted script and deterministic diagnostics parity across same-process loops and fresh reruns."
  artifacts:
    - path: "src/scripting/sketch_script_parse.h"
      provides: "Descriptor-aware participant schema parsing (`id`, `role`, `sub_index`) and script color parsing."
      contains: "deterministic parse errors for missing/ambiguous descriptor intent"
    - path: "src/scripting/sketch_script_apply.h"
      provides: "Descriptor-preserving constraint participant construction and script-color canonical entity creation."
      contains: "transactional rollback on unresolved descriptor/color contract failures"
    - path: "src/scripting/sketch_script_emit.h"
      provides: "Deterministic emission of descriptor participants and color metadata for script-managed entities."
      contains: "stable canonical ordering and numeric formatting for repeat parity"
    - path: "src/tests/script_roundtrip_tests.c"
      provides: "Descriptor fidelity, color fidelity, atomic failure taxonomy, and repeated reapply determinism coverage."
      contains: "same-process repeated apply assertions over emitted script equality"
    - path: "src/tests/scene_solver_diagnostics_test.c"
      provides: "Deterministic diagnostics parity assertions under repeated script reapply."
      contains: "stable message/count/order checks"
  key_links:
    - from: "src/scripting/sketch_script_parse.h"
      to: "src/scripting/sketch_script_apply.h"
      via: "descriptor object model (id/role/sub_index) passed without entity-only collapse"
      pattern: "participant.*role|sub_index"
    - from: "src/scripting/sketch_script_apply.h"
      to: "src/scripting/sketch_script_emit.h"
      via: "script-managed color and descriptor roundtrip canonicalization"
      pattern: "color|participant_descriptors"
    - from: "src/tests/script_roundtrip_tests.c"
      to: "src/tests/scene_solver_diagnostics_test.c"
      via: "determinism parity gate for emitted script + diagnostics"
      pattern: "repeat|deterministic|reapply"
---

<objective>
Implement descriptor and color fidelity end-to-end in script parse/apply/emit with deterministic repeat-apply verification gates.

Purpose: Close SCRI-01, SCRI-02, SCRI-03 by preserving participant intent, preserving script-managed color metadata, and proving deterministic parity under repeated reapply.
Output: Descriptor-aware script schema + apply/emit fidelity + deterministic parity tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/31-script-reapply-fidelity-foundation/31-CONTEXT.md
@.planning/phases/31-script-reapply-fidelity-foundation/31-RESEARCH.md
@.planning/phases/31-script-reapply-fidelity-foundation/31-VALIDATION.md
@.planning/phases/31-script-reapply-fidelity-foundation/31-01-SUMMARY.md
@src/scripting/sketch_script_parse.h
@src/scripting/sketch_script_apply.h
@src/scripting/sketch_script_emit.h
@src/tests/script_roundtrip_tests.c
@src/tests/scene_solver_diagnostics_test.c

<interfaces>
From src/scripting/sketch_script_parse.h:
```c
typedef struct {
    char id[SCRIPT_LOCAL_ID_MAX];
    constraint_type_t type;
    uint32_t participant_count;
    // extend to descriptor model: id + role + sub_index
} sketch_script_constraint_model_t;
```

From src/scripting/sketch_script_apply.h:
```c
static inline bool sketch_script_apply_validate_model_links(...);
static inline bool sketch_script_apply_commit_model(...);
```

From src/scripting/sketch_script_emit.h:
```c
static inline bool sketch_script_emit_for_sketch(...);
static inline bool scene_script_emit_for_sketch(...);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement descriptor-fidelity participant pipeline and strict deterministic failure taxonomy</name>
  <read_first>@src/scripting/sketch_script_parse.h, @src/scripting/sketch_script_apply.h, @src/scripting/sketch_script_emit.h, @.planning/phases/31-script-reapply-fidelity-foundation/31-CONTEXT.md</read_first>
  <files>src/scripting/sketch_script_parse.h, src/scripting/sketch_script_apply.h, src/scripting/sketch_script_emit.h, src/tests/script_roundtrip_tests.c</files>
  <behavior>
    - Test 1: Descriptor participants `{id, role, sub_index}` parse -> apply -> emit -> reparse without role/sub-index loss (per D-01, D-03).
    - Test 2: Missing/ambiguous descriptor intent fails with explicit deterministic contract taxonomy and zero scene mutation (per D-02, D-07, D-08, D-09).
    - Test 3: Valid descriptor-required constraints do not degrade to entity-only participants and do not produce unsupported-participant drift (per D-01, D-03).
  </behavior>
  <action>Extend parser participant model from string IDs to descriptor objects with explicit `id`, `role`, and `sub_index`, and reject missing/ambiguous descriptor intent using deterministic contract errors (per D-01, D-02, D-09). In apply path, construct `constraint_participant_descriptor_t` directly from parsed role/sub-index and remove entity-role hardcoding/fallback logic (per D-01, D-03). In emitter, output canonical participant descriptor objects with stable order/format so emit/apply/emit parity is deterministic (per D-10, D-11). Add or update roundtrip tests that assert exact descriptor role/sub-index equality across cycles and assert deterministic failure error text for malformed descriptor input.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "script_roundtrip_tests" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`script_roundtrip_tests` includes descriptor roundtrip and malformed-descriptor taxonomy cases; grep of `sketch_script_apply.h` no longer contains hardcoded `CONSTRAINT_PARTICIPANT_ROLE_ENTITY` assignment for parsed participants; targeted test command passes.</acceptance_criteria>
  <done>Participant descriptor fidelity is preserved end-to-end and malformed descriptor input fails atomically with deterministic taxonomy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement script color canonical roundtrip for script-managed entities only</name>
  <read_first>@src/scripting/sketch_script_parse.h, @src/scripting/sketch_script_apply.h, @src/scripting/sketch_script_emit.h, @src/tests/script_roundtrip_tests.c</read_first>
  <files>src/scripting/sketch_script_parse.h, src/scripting/sketch_script_apply.h, src/scripting/sketch_script_emit.h, src/tests/script_roundtrip_tests.c</files>
  <behavior>
    - Test 1: Script-managed point/line/arc color values are parsed and applied exactly, then preserved on re-emit/reapply cycles (per D-04, D-05).
    - Test 2: Non-script entities retain their existing colors after script reapply (per D-06).
    - Test 3: Color mismatch/invalid color schema fails atomically with deterministic error and no partial mutation (per D-07, D-08, D-09).
  </behavior>
  <action>Add parser support for entity color metadata (`{r,g,b,a}`) on point/line/arc entities and validate schema deterministically (per D-04, D-05, D-09). In apply entity creation, replace default-white constructors with parsed script color for script-managed entities only, leaving non-script entities untouched (per D-04, D-05, D-06). In emitter, include canonical color output for script-managed entities with deterministic number formatting aligned to existing emitter policy. Add roundtrip tests covering point/line/arc color preservation, non-script color non-interference, and atomic rollback on invalid color contract input.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "script_roundtrip_tests" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>Tests named with `color_roundtrip` and `non_script_color_untouched` pass; grep shows `vec4_make(1, 1, 1, 1)` no longer used for script-managed point/line/arc creation in `sketch_script_apply.h` default path.</acceptance_criteria>
  <done>Script-managed color metadata roundtrips canonically without touching non-script entities.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Enforce deterministic repeat-apply and fresh-rerun parity gate for script emit + diagnostics</name>
  <read_first>@src/tests/script_roundtrip_tests.c, @src/tests/scene_solver_diagnostics_test.c, @.planning/phases/31-script-reapply-fidelity-foundation/31-VALIDATION.md, @.planning/phases/31-script-reapply-fidelity-foundation/31-RESEARCH.md</read_first>
  <files>src/tests/script_roundtrip_tests.c, src/tests/scene_solver_diagnostics_test.c, .planning/phases/31-script-reapply-fidelity-foundation/31-VALIDATION.md</files>
  <behavior>
    - Test 1: Same process repeated apply (N>=5) yields identical emitted script text each iteration (per D-10, D-11, D-12).
    - Test 2: Same process repeated apply yields stable diagnostics count/order/message payload for identical input (per D-10, D-11).
    - Test 3: Validation contract explicitly requires immediate rerun parity of the same command, not just single-pass success (per D-10, D-12).
  </behavior>
  <action>Add deterministic repeat-apply tests to `script_roundtrip_tests.c` asserting exact emitted script equality across at least 5 identical reapply iterations in one process (per D-10, D-11). Add diagnostics parity assertions in `scene_solver_diagnostics_test.c` that compare deterministic count/order/message payload stability under repeated reapply (per D-11). Update `31-VALIDATION.md` per-task map/status rows to include these new parity tests and keep canonical quick/full commands aligned with the new deterministic checks; do not weaken to crash-only checks (per D-12).</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "script_roundtrip_tests|scene_solver_diagnostics" --output-on-failure && ctest --test-dir build-vulkan -C Release -R "script_roundtrip_tests|scene_solver_diagnostics" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>Back-to-back identical ctest command returns pass/pass; new tests include `repeat_apply_deterministic` and `diagnostic_parity_reapply`; `31-VALIDATION.md` task map rows reference deterministic parity probes instead of crash-only checks.</acceptance_criteria>
  <done>Determinism parity gate for reapply is codified and executable for SCRI-03 closure evidence.</done>
</task>

</tasks>

<verification>
Run:
- `ctest --test-dir build-vulkan -C Release -R "script_roundtrip_tests|scene_solver_diagnostics" --output-on-failure`
- Re-run the identical command immediately to confirm deterministic parity.
</verification>

<success_criteria>
- SCRI-01: Descriptor role/sub-index intent survives reapply and no entity-only downgrade path remains.
- SCRI-02: Script-managed point/line/arc colors are preserved exactly across reapply cycles while non-script entities remain untouched.
- SCRI-03: Repeat-apply and immediate rerun parity produce stable emitted script + diagnostics outcomes for identical inputs.
</success_criteria>

<output>
After completion, create `.planning/phases/31-script-reapply-fidelity-foundation/31-02-SUMMARY.md`
</output>

