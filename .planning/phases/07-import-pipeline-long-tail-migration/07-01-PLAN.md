---
phase: 07-import-pipeline-long-tail-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/math/math_import.h
  - src/jsonl_loader.h
  - src/jsonl_import_job.h
  - src/ply_loader.h
  - src/ply_import_job.h
  - src/ply_mesh_import_job.h
autonomous: true
requirements: [TAIL-02]
must_haves:
  truths:
    - "JSONL and PLY importer math paths in scope use cglm-backed helpers (D-04, D-05)."
    - "Importer transform behavior prioritizes correctness over legacy order when conflicts are found, with explicit implementation-analysis and delta hooks prepared for validation evidence (D-01, D-02, D-03, D-09)."
    - "Chunked import/progress semantics remain intact for JSONL and PLY state machines (D-10, D-11)."
  artifacts:
    - path: "src/math/math_import.h"
      provides: "Thin importer-focused cglm-backed helper boundary without broad facade (D-06)."
      contains: "mdcad_import_"
    - path: "src/jsonl_import_job.h"
      provides: "JSONL CoM/rotation/scale transform path migrated to importer helper."
      contains: "jsonl_import_job_apply_transforms"
    - path: "src/ply_import_job.h"
      provides: "PLY point import transform path migrated to importer helper."
      contains: "ply_import_job_apply_transforms"
    - path: "src/ply_mesh_import_job.h"
      provides: "PLY mesh transform path migrated to importer helper."
      contains: "ply_mesh_import_job_apply_transforms"
    - path: "src/jsonl_loader.h"
      provides: "JSONL parser/basis math path migrated per phase cutline."
      contains: "jsonl_parse_arc3d"
    - path: "src/ply_loader.h"
      provides: "PLY parser math touchpoints migrated per phase cutline."
      contains: "ply_"
  key_links:
    - from: "src/jsonl_import_job.h"
      to: "src/math/math_import.h"
      via: "#include + helper calls"
      pattern: "mdcad_import_"
    - from: "src/ply_import_job.h"
      to: "src/math/math_import.h"
      via: "#include + helper calls"
      pattern: "mdcad_import_"
    - from: "src/ply_mesh_import_job.h"
      to: "src/math/math_import.h"
      via: "#include + helper calls"
      pattern: "mdcad_import_"
    - from: "src/jsonl_import_job.h"
      to: "existing chunk/progress macros"
      via: "macro constants preserved"
      pattern: "JSONL_(PARSE|ENTITY|PARENT)_CHUNK_SIZE"
    - from: "src/ply_import_job.h"
      to: "existing chunk/progress macros"
      via: "macro constants preserved"
      pattern: "PLY_(PARSE|ENTITY)_CHUNK_SIZE"
---

<objective>
Migrate all importer math touchpoints in Phase 7 scope to thin cglm-backed helpers while keeping importer runtime semantics stable.

Purpose: Complete the core TAIL-02 migration cutline (parser + import-job paths) without expanding scope or regressing chunked UX.
Output: Shared importer helper boundary in `src/math/` plus migrated JSONL/PLY loader and import-job math paths.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-import-pipeline-long-tail-migration/07-CONTEXT.md
@.planning/phases/07-import-pipeline-long-tail-migration/07-RESEARCH.md
@src/math/cglm_entry.h
@src/jsonl_loader.h
@src/jsonl_import_job.h
@src/ply_loader.h
@src/ply_import_job.h
@src/ply_mesh_import_job.h

<interfaces>
From `src/jsonl_import_job.h`:
- `static inline void jsonl_import_job_apply_transforms(jsonl_import_job_t *job);`
- Preserve `JSONL_PARSE_CHUNK_SIZE`, `JSONL_ENTITY_CHUNK_SIZE`, `JSONL_PARENT_CHUNK_SIZE` constants (D-10).

From `src/ply_import_job.h`:
- `static inline void ply_import_job_apply_transforms(ply_import_job_t *job);`
- Preserve `PLY_PARSE_CHUNK_SIZE`, `PLY_ENTITY_CHUNK_SIZE` constants (D-10).

From `src/ply_mesh_import_job.h`:
- `static inline void ply_mesh_import_job_apply_transforms(ply_mesh_import_job_t *job);`
- Preserve `PLY_MESH_PARSE_CHUNK_SIZE`, `PLY_MESH_ENTITY_CHUNK_SIZE` constants (D-10).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create thin importer helper boundary and migrate import-job transform math</name>
  <read_first>
    - src/math/cglm_entry.h
    - src/jsonl_import_job.h
    - src/ply_import_job.h
    - src/ply_mesh_import_job.h
    - .planning/phases/07-import-pipeline-long-tail-migration/07-CONTEXT.md
  </read_first>
  <files>src/math/math_import.h, src/jsonl_import_job.h, src/ply_import_job.h, src/ply_mesh_import_job.h</files>
  <behavior>
    - Test 1: For non-zero rotation inputs, JSONL/PLY transform paths still apply CoM shift -> rotation -> scale in that order unless corrected with explicit rationale (D-01, D-03).
    - Test 2: Import jobs keep chunk/progress constants and state transitions unchanged after math-path migration (D-10).
  </behavior>
  <action>Create `src/math/math_import.h` with importer-focused cglm-backed helpers (for normalize-safe, rotation composition, point transform, and CoM accumulation helpers) and use them in `jsonl_import_job_apply_transforms`, `ply_import_job_apply_transforms`, and `ply_mesh_import_job_apply_transforms`. Keep current job-state machine transitions and progress semantics intact (per D-10/D-11). Do not add any broad compatibility facade; stay thin-entrypoint style per D-06.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdcad_math_harness</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "mdcad_import_" src/jsonl_import_job.h src/ply_import_job.h src/ply_mesh_import_job.h src/math/math_import.h` finds helper definitions/call sites.
    - `rg -n "JSONL_(PARSE|ENTITY|PARENT)_CHUNK_SIZE" src/jsonl_import_job.h` returns existing chunk constants.
    - `rg -n "PLY_(PARSE|ENTITY)_CHUNK_SIZE|PLY_MESH_(PARSE|ENTITY)_CHUNK_SIZE" src/ply_import_job.h src/ply_mesh_import_job.h` returns existing chunk constants.
  </acceptance_criteria>
  <done>Import-job transform functions are migrated to thin shared helper calls and compile cleanly without state-machine/progress drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate loader/parser math touchpoints in JSONL and PLY paths</name>
  <read_first>
    - src/jsonl_loader.h
    - src/ply_loader.h
    - src/math/math_import.h
    - .planning/phases/07-import-pipeline-long-tail-migration/07-RESEARCH.md
  </read_first>
  <files>src/jsonl_loader.h, src/ply_loader.h, src/math/math_import.h</files>
  <behavior>
    - Test 1: JSONL arc/basis parsing keeps expected orientation semantics or clearly marks corrected behavior backed by implementation analysis of current behavior (D-01, D-02, D-09).
    - Test 2: PLY parse-path vec/bounds math in scope uses migrated helpers and no new feature behavior is introduced (D-12).
  </behavior>
  <action>Migrate parser-side math touchpoints in `jsonl_loader.h` and `ply_loader.h` to cglm-backed importer helper calls where practical per D-04, keeping importer-specific logic local when clarity requires it. Keep file-format behavior and parser state-machine flow unchanged (no new importer capabilities per D-12). Before applying any semantic correction, analyze the current importer transform behavior in the touched code path (existing order/basis handling) and annotate code comments with `Phase7 correctness delta` markers that summarize why the fix is mathematically correct; these notes are required inputs to Phase 7 validation artifacts per D-02/D-09.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release --target mdcad_math_harness</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "Phase7 correctness delta" src/jsonl_loader.h src/ply_loader.h` finds markers when behavior adjustments are intentional, or no marker if no delta exists.
    - `rg -n "math3d.h" src/jsonl_loader.h src/ply_loader.h src/jsonl_import_job.h src/ply_import_job.h src/ply_mesh_import_job.h` returns no in-scope legacy helper include/call regressions after migration.
  </acceptance_criteria>
  <done>All five scoped importer files are migrated on parser/import-job math paths with correctness-first hooks and no scope creep.</done>
</task>

<task type="auto">
  <name>Task 3: Run migration boundary audit for TAIL-02 import scope</name>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/07-import-pipeline-long-tail-migration/07-CONTEXT.md
    - src/jsonl_loader.h
    - src/jsonl_import_job.h
    - src/ply_loader.h
    - src/ply_import_job.h
    - src/ply_mesh_import_job.h
  </read_first>
  <files>src/math/math_import.h, src/jsonl_loader.h, src/jsonl_import_job.h, src/ply_loader.h, src/ply_import_job.h, src/ply_mesh_import_job.h</files>
  <action>Perform a source audit to ensure in-scope importer surfaces no longer depend on migrated `math3d` helper equivalents (TAIL-02), and confirm chunk/progress constants remain unchanged (D-10). Keep this audit mechanical and grep-verifiable so 07-03 can reference exact deltas without re-discovery.</action>
  <verify>
    <automated>rg -n "math3d\\.h|\\b(vec3_|mat4_)" src/jsonl_loader.h src/jsonl_import_job.h src/ply_loader.h src/ply_import_job.h src/ply_mesh_import_job.h && cmake --build build-vulkan --config Release --target mdcad_math_harness</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "math3d.h" src/jsonl_loader.h src/jsonl_import_job.h src/ply_loader.h src/ply_import_job.h src/ply_mesh_import_job.h` returns zero matches.
    - `rg -n "JSONL_SYNC_THRESHOLD|PLY_SYNC_THRESHOLD|PLY_MESH_SYNC_THRESHOLD" src/jsonl_import_job.h src/ply_import_job.h src/ply_mesh_import_job.h` still returns existing sync-threshold constants.
  </acceptance_criteria>
  <done>Migration boundary is mechanically auditable: no in-scope legacy include fallback and no chunk/progress semantic constant drift.</done>
</task>

</tasks>

<verification>
Run compile gate after each task and keep grep-based boundary checks attached to task output for traceable TAIL-02 proof.
</verification>

<success_criteria>
- JSONL/PLY loader + import-job math touchpoints are migrated to cglm-backed helper paths.
- Import chunk/progress semantics remain intact and grep-verifiable.
- No broad compatibility facade or out-of-scope importer feature additions were introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/07-import-pipeline-long-tail-migration/07-01-SUMMARY.md`.
</output>
