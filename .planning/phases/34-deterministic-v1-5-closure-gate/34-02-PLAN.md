---
phase: 34-deterministic-v1-5-closure-gate
plan: 02
type: execute
wave: 2
depends_on: [34-01]
files_modified:
  - src/ecs/ecs_scene.h
  - .planning/phases/34-deterministic-v1-5-closure-gate/34-VERIFICATION.md
autonomous: true
requirements: [DIAG-03]
gap_closure: true
must_haves:
  truths:
    - "Developer can run the canonical Windows Vulkan 7-test gate and get a 7/7 baseline pass."
    - "Developer can immediately rerun the identical canonical gate command and get the same pass outcome."
    - "Phase 34 verification artifact explicitly marks DIAG-03 satisfied from pass+parity evidence."
  artifacts:
    - path: "src/ecs/ecs_scene.h"
      provides: "Parallel/ALONG drag-authority behavior that no longer regresses the two failing canonical gate tests."
      contains: "parallel pair authority resolution path used during recalculate after drag anchor updates"
    - path: ".planning/phases/34-deterministic-v1-5-closure-gate/34-VERIFICATION.md"
      provides: "Updated baseline+rereun closure evidence with explicit DIAG-03 satisfied verdict."
      contains: "Baseline: 7/7 passed"
  key_links:
    - from: "src/ecs/ecs_scene.h"
      to: "src/tests/scene_solver_contract_test.c"
      via: "test_parallel_pair_along_interaction_switches_authority_without_lock_in"
      pattern: "scene_solver_contract"
    - from: "src/ecs/ecs_scene.h"
      to: "src/tests/scene_solver_drag_test.c"
      via: "test_drag_parallel_pair_ab_moves_cd_follows"
      pattern: "scene_solver_drag"
    - from: ".planning/phases/34-deterministic-v1-5-closure-gate/34-VERIFICATION.md"
      to: "DIAG-03 closure decision"
      via: "canonical baseline + immediate rerun command summaries"
      pattern: "Baseline|Immediate rerun|Outcome parity|DIAG-03 SATISFIED"
---

<objective>
Close the Phase 34 verification gap by restoring canonical gate pass behavior and recapturing deterministic baseline + immediate rerun evidence.

Purpose: Satisfy DIAG-03 closure preconditions (baseline pass and immediate rerun parity) using the locked Windows Vulkan canonical command contract.
Output: Solver authority behavior stabilized for the two failing tests, and `34-VERIFICATION.md` updated with pass+parity closure evidence.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-deterministic-v1-5-closure-gate/34-CONTEXT.md
@.planning/phases/34-deterministic-v1-5-closure-gate/34-VALIDATION.md
@.planning/phases/34-deterministic-v1-5-closure-gate/34-VERIFICATION.md
@.planning/phases/34-deterministic-v1-5-closure-gate/34-01-SUMMARY.md
@src/tests/scene_solver_contract_test.c
@src/tests/scene_solver_drag_test.c
@src/ecs/ecs_scene.h

<interfaces>
From src/tests/scene_solver_contract_test.c:
```c
static int test_parallel_pair_along_interaction_switches_authority_without_lock_in(void);
```

From src/tests/scene_solver_drag_test.c:
```c
static int test_drag_parallel_pair_ab_moves_cd_follows(void);
```

From src/ecs/ecs_scene.h:
```c
static inline bool scene_solver_set_drag_anchor(ecs_scene_t *scene,
                                                ecs_entity_t sketch_entity,
                                                ecs_entity_t owner_entity,
                                                constraint_participant_role_t role,
                                                int sub_index);
static inline bool scene_solver_request_recalculate(ecs_scene_t *scene,
                                                    ecs_entity_t sketch_entity);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stabilize parallel/ALONG authority switching so canonical contract+drag tests pass</name>
  <read_first>@.planning/phases/34-deterministic-v1-5-closure-gate/34-CONTEXT.md, @.planning/phases/34-deterministic-v1-5-closure-gate/34-VERIFICATION.md, @src/tests/scene_solver_contract_test.c, @src/tests/scene_solver_drag_test.c, @src/ecs/ecs_scene.h</read_first>
  <files>src/ecs/ecs_scene.h</files>
  <behavior>
    - Test 1: `scene_solver_contract` passes `test_parallel_pair_along_interaction_switches_authority_without_lock_in` with no lock-in after alternating drag anchors (per D-04 deterministic closure expectations).
    - Test 2: `scene_solver_drag` passes `test_drag_parallel_pair_ab_moves_cd_follows` with dragged AB moving and CD following while preserving parallel directionality.
    - Test 3: Immediate rerun of these two tests yields identical pass outcome (per D-07 hard-fail on divergence/flakiness).
  </behavior>
  <action>Patch only the parallel-pair authority resolution path in `scene_solver_request_recalculate`/related helper logic so drag-anchor intent remains authoritative for the active drag operation while still honoring external-constraint weighting fallback. Keep the fix minimal and deterministic per D-01 and D-03 (no broad solver refactor, no new non-canonical test gates, no retry-until-green logic). Preserve existing diagnostic taxonomy behavior from Phase 33 while removing the regression causing `scene_solver_contract` and `scene_solver_drag` failures.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release && ctest --test-dir build-vulkan -C Release -R "scene_solver_contract|scene_solver_drag" --output-on-failure && ctest --test-dir build-vulkan -C Release -R "scene_solver_contract|scene_solver_drag" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>Both previously failing tests pass on baseline and immediate rerun with identical pass outcome; no divergence or flake is observed in the two-test stabilization gate.</acceptance_criteria>
  <done>Canonical blocker tests are green and deterministic, enabling full 7-test closure recapture.</done>
</task>

<task type="auto">
  <name>Task 2: Re-capture canonical 7-test baseline + immediate rerun evidence and satisfy DIAG-03</name>
  <read_first>@.planning/phases/34-deterministic-v1-5-closure-gate/34-VALIDATION.md, @.planning/phases/34-deterministic-v1-5-closure-gate/34-VERIFICATION.md, @.planning/REQUIREMENTS.md</read_first>
  <files>.planning/phases/34-deterministic-v1-5-closure-gate/34-VERIFICATION.md</files>
  <action>Run the locked canonical command sequence exactly per D-01, D-02, D-03, D-04, and D-05: build once, run the 7-test gate once as Baseline, then immediately rerun the identical gate command. Update `34-VERIFICATION.md` with exact command strings and concise result summaries showing 7/7 pass on baseline and rerun, explicit outcome parity, and explicit `DIAG-03 SATISFIED` conclusion. Keep raw logs optional per D-06 and keep D-09 wording conditional/non-blocking once DIAG-03 is satisfied.</action>
  <verify>
    <automated>cmake --build build-vulkan --config Release && ctest --test-dir build-vulkan -C Release -R "scene_solver_contract|scene_solver_pass_policy|scene_solver_diagnostics|scene_solver_trigger|scene_solver_drag|endpoint_pick|script_roundtrip_tests" --output-on-failure && ctest --test-dir build-vulkan -C Release -R "scene_solver_contract|scene_solver_pass_policy|scene_solver_diagnostics|scene_solver_trigger|scene_solver_drag|endpoint_pick|script_roundtrip_tests" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`34-VERIFICATION.md` records canonical baseline and immediate rerun as 7/7 pass with explicit parity and `DIAG-03 SATISFIED` closure verdict.</acceptance_criteria>
  <done>Phase 34 closure evidence is re-established and DIAG-03 is satisfiable with canonical pass+parity proof.</done>
</task>

</tasks>

<verification>
- `cmake --build build-vulkan --config Release`
- `ctest --test-dir build-vulkan -C Release -R "scene_solver_contract|scene_solver_pass_policy|scene_solver_diagnostics|scene_solver_trigger|scene_solver_drag|endpoint_pick|script_roundtrip_tests" --output-on-failure`
- Immediately rerun the same canonical command and confirm same 7/7 pass result.
</verification>

<success_criteria>
- Canonical baseline on the locked Windows Vulkan 7-test gate is 7/7 pass.
- Immediate rerun parity is re-captured as identical 7/7 pass.
- `34-VERIFICATION.md` explicitly marks DIAG-03 satisfied using the canonical pass+parity evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/34-deterministic-v1-5-closure-gate/34-02-SUMMARY.md`
</output>

