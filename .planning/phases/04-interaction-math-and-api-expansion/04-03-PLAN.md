---
phase: 04-interaction-math-and-api-expansion
plan: 03
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
files_modified:
  - src/math/math_quat.h
  - src/math/math_interaction.h
  - src/math_harness.c
  - docs/QUICKSTART.md
  - src/math3d.h
autonomous: true
requirements:
  - HOT-03
  - EXP-01
  - EXP-02
user_setup: []
must_haves:
  truths:
    - "Quaternion-capable helper APIs are exposed under src/math/ without adding vendor type wrappers"
    - "Migrated interaction slices no longer depend on equivalent math3d helpers (`ray_from_screen`, `ray_axis_closest_t`, `ray_plane_intersect`)"
    - "Harness compare and bench flows include interaction + quaternion helper checks used as phase gate"
  artifacts:
    - path: "src/math/math_quat.h"
      provides: "project-owned quaternion helper surface for expansion"
      contains: "mdcad_quat_from_axis_angle"
    - path: "src/math_harness.c"
      provides: "compare and bench coverage for quaternion + interaction helpers"
      contains: "quat-rotate-vector"
    - path: "docs/QUICKSTART.md"
      provides: "phase 4 validation workflow including harness and manual interaction smoke"
      contains: "Phase 4 interaction parity smoke"
    - path: "src/math3d.h"
      provides: "legacy helper retirement markers for migrated interaction slices"
      contains: "MDCAD_MATH3D_INTERACTION_DEPRECATED"
  key_links:
    - from: "src/math/math_quat.h"
      to: "src/math/cglm_entry.h"
      via: "quaternion helpers are thin wrappers over cglm without vendor type aliasing"
      pattern: "glms_quat"
    - from: "src/app.c"
      to: "src/math/math_interaction.h"
      via: "screen-ray migration no longer calls math3d interaction helpers"
      pattern: "mdcad_interaction_screen_ray_from_viewport"
    - from: "src/gizmo/gizmo.h"
      to: "src/math/math_interaction.h"
      via: "gizmo drag migration no longer calls legacy ray helpers"
      pattern: "mdcad_interaction_ray_axis_closest_t|mdcad_interaction_ray_plane_intersect"
---

<objective>
Add expanded helper coverage (including quaternion capability), retire redundant local helpers in migrated interaction slices, and lock phase-level validation.

Purpose: `EXP-01` and `EXP-02` require a richer math helper surface and retirement of equivalent `math3d` helpers in migrated paths, while `HOT-03` still requires interaction parity gates.
Output: quaternion helper module, scoped interaction-helper retirement in migrated files, expanded compare/bench cases, and an explicit Phase 4 validation runbook.
</objective>

<execution_context>
@/Users/rodionradchenko/Documents/repos/mdCAD/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rodionradchenko/Documents/repos/mdCAD/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-interaction-math-and-api-expansion/04-CONTEXT.md
@.planning/phases/04-interaction-math-and-api-expansion/04-RESEARCH.md
@.planning/phases/04-interaction-math-and-api-expansion/04-VALIDATION.md
@src/math/cglm_entry.h
@src/math/math_interaction.h
@src/math3d.h
@src/math_harness.c
@docs/QUICKSTART.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add quaternion-capable helper module and parity checks</name>
  <files>src/math/math_quat.h, src/math_harness.c</files>
  <read_first>src/math/cglm_entry.h, src/math/math_interaction.h, src/math_harness.c, src/math3d.h, .planning/phases/04-interaction-math-and-api-expansion/04-RESEARCH.md, .planning/phases/04-interaction-math-and-api-expansion/04-VALIDATION.md</read_first>
  <action>Create `src/math/math_quat.h` as a header-only project module that includes `"cglm_entry.h"` and defines these exact APIs: `mdcad_quat_from_axis_angle(...)`, `mdcad_quat_normalize(...)`, `mdcad_quat_rotate_vec3(...)`, and `mdcad_quat_mul(...)`. Use cglm quaternion operations directly (`glms_quat`, `glms_quat_normalize`, `glms_quat_rotatev`, `glms_quat_mul`) and keep external signatures explicit with project-owned structs/arguments. Extend `src/math_harness.c` with compare cases named exactly `quat-rotate-vector` and `quat-compose-order` that validate quaternion helper outputs against frozen baseline calculations.</action>
  <verify>
    <automated>cmake -B build -G Ninja && ninja -C build mdcad_math_harness && rg -n "mdcad_quat_from_axis_angle|mdcad_quat_normalize|mdcad_quat_rotate_vec3|mdcad_quat_mul|glms_quat|glms_quat_normalize|glms_quat_rotatev|glms_quat_mul" src/math/math_quat.h && ./build/bin/mdcad_math_harness --mode compare --strict | rg -n "^COMPARE PASS (quat-rotate-vector|quat-compose-order)"</automated>
  </verify>
  <acceptance_criteria>
    - `src/math/math_quat.h` contains `mdcad_quat_from_axis_angle`
    - `src/math/math_quat.h` contains `mdcad_quat_normalize`
    - `src/math/math_quat.h` contains `mdcad_quat_rotate_vec3`
    - `src/math/math_quat.h` contains `mdcad_quat_mul`
    - `src/math/math_quat.h` contains `glms_quat`
    - `src/math/math_quat.h` contains `glms_quat_normalize`
    - `src/math/math_quat.h` contains `glms_quat_rotatev`
    - `src/math/math_quat.h` contains `glms_quat_mul`
    - `src/math_harness.c` contains `quat-rotate-vector`
    - `src/math_harness.c` contains `quat-compose-order`
    - `./build/bin/mdcad_math_harness --mode compare --strict` prints `COMPARE PASS quat-rotate-vector`
    - `./build/bin/mdcad_math_harness --mode compare --strict` prints `COMPARE PASS quat-compose-order`
  </acceptance_criteria>
  <done>Quaternion-capable helper APIs are available through src/math/ and covered by compare parity checks.</done>
</task>

<task type="auto">
  <name>Task 2: Retire equivalent legacy interaction helpers in migrated slices and lock phase-4 validation workflow</name>
  <files>src/math/math_interaction.h, src/math3d.h, src/math_harness.c, docs/QUICKSTART.md</files>
  <read_first>src/app.c, src/gpu/pick_buffer.h, src/gizmo/gizmo.h, src/gizmo/gizmo_vertex_mode.h, src/math/math_interaction.h, src/math3d.h, src/math_harness.c, docs/QUICKSTART.md, .planning/phases/04-interaction-math-and-api-expansion/04-VALIDATION.md</read_first>
  <action>Retire legacy helper dependence for migrated slices by ensuring `src/app.c`, `src/gpu/pick_buffer.h`, `src/gizmo/gizmo.h`, and `src/gizmo/gizmo_vertex_mode.h` do not call `ray_from_screen(...)`, `ray_axis_closest_t(...)`, or `ray_plane_intersect(...)`. In `src/math3d.h`, add a scoped migration marker macro named `MDCAD_MATH3D_INTERACTION_DEPRECATED` and attach deprecation comments to those helper declarations so future edits do not reintroduce them into migrated slices. Extend `src/math_harness.c` bench mode with cases named exactly `bench-interaction-ray`, `bench-interaction-drag`, and `bench-quat-ops`. Update `docs/QUICKSTART.md` with a section titled exactly `## Phase 4 interaction parity smoke` that documents: `cmake -B build -G Ninja && ninja -C build math-validation`, `./build/bin/mdcad_math_harness --mode compare --strict`, `./build/bin/mdcad_math_harness --mode bench`, and manual app interaction smoke using `./build/bin/mdCAD`.</action>
  <verify>
    <automated>if rg -n "\bray_from_screen\(|\bray_axis_closest_t\(|\bray_plane_intersect\(" src/app.c src/gpu/pick_buffer.h src/gizmo/gizmo.h src/gizmo/gizmo_vertex_mode.h; then echo "Legacy interaction helper usage found in migrated slices"; exit 1; fi; ./build/bin/mdcad_math_harness --mode bench | rg -n "bench-interaction-ray|bench-interaction-drag|bench-quat-ops" && rg -n "Phase 4 interaction parity smoke|mdcad_math_harness --mode compare --strict|mdcad_math_harness --mode bench|./build/bin/mdCAD|MDCAD_MATH3D_INTERACTION_DEPRECATED" docs/QUICKSTART.md src/math3d.h</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "\bray_from_screen\(" src/app.c src/gpu/pick_buffer.h src/gizmo/gizmo.h src/gizmo/gizmo_vertex_mode.h` returns no matches
    - `rg -n "\bray_axis_closest_t\(" src/app.c src/gpu/pick_buffer.h src/gizmo/gizmo.h src/gizmo/gizmo_vertex_mode.h` returns no matches
    - `rg -n "\bray_plane_intersect\(" src/app.c src/gpu/pick_buffer.h src/gizmo/gizmo.h src/gizmo/gizmo_vertex_mode.h` returns no matches
    - `src/math3d.h` contains `MDCAD_MATH3D_INTERACTION_DEPRECATED`
    - `src/math_harness.c` contains `bench-interaction-ray`
    - `src/math_harness.c` contains `bench-interaction-drag`
    - `src/math_harness.c` contains `bench-quat-ops`
    - `./build/bin/mdcad_math_harness --mode bench` prints `bench-interaction-ray`
    - `./build/bin/mdcad_math_harness --mode bench` prints `bench-interaction-drag`
    - `./build/bin/mdcad_math_harness --mode bench` prints `bench-quat-ops`
    - `docs/QUICKSTART.md` contains `## Phase 4 interaction parity smoke`
    - `docs/QUICKSTART.md` contains `./build/bin/mdcad_math_harness --mode compare --strict`
    - `docs/QUICKSTART.md` contains `./build/bin/mdcad_math_harness --mode bench`
    - `docs/QUICKSTART.md` contains `./build/bin/mdCAD`
  </acceptance_criteria>
  <done>Migrated interaction slices are protected against legacy-helper regression, and Phase 4 validation is documented with compare + bench + manual smoke commands.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "mdcad_quat_from_axis_angle|mdcad_quat_normalize|mdcad_quat_rotate_vec3|mdcad_quat_mul" src/math/math_quat.h` succeeds
- [ ] `if rg -n "\bray_from_screen\(|\bray_axis_closest_t\(|\bray_plane_intersect\(" src/app.c src/gpu/pick_buffer.h src/gizmo/gizmo.h src/gizmo/gizmo_vertex_mode.h; then exit 1; fi` succeeds
- [ ] `cmake -B build -G Ninja && ninja -C build mdcad_math_harness` succeeds
- [ ] `./build/bin/mdcad_math_harness --mode compare --strict` prints `COMPARE PASS quat-rotate-vector` and `COMPARE PASS quat-compose-order`
- [ ] `./build/bin/mdcad_math_harness --mode bench` prints `bench-interaction-ray`, `bench-interaction-drag`, and `bench-quat-ops`
- [ ] `docs/QUICKSTART.md` contains `## Phase 4 interaction parity smoke`
- [ ] Manual macOS HOT-03 interaction smoke passes (`./build/bin/mdCAD`; validate pick + gizmo migration behavior end-to-end) and `04-03-SUMMARY.md` records `HOT-03 macOS interaction smoke: PASS|FAIL`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 4 closes HOT-03, EXP-01, and EXP-02 with helper expansion and scoped legacy retirement in migrated interaction paths
  </success_criteria>

<output>
After completion, create `.planning/phases/04-interaction-math-and-api-expansion/04-03-SUMMARY.md`
Also mirror this plan to `.plans/PLAN_04-03.md` (canonical plan remains under `.planning/phases/...`).
</output>
