---
phase: 25-regression-and-reliability-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/25-regression-and-reliability-closure/25-VERIFICATION.md
  - src/CMakeLists.txt
  - src/ecs/ecs_scene.h
  - src/tests/scene_solver_trigger_test.c
  - src/tests/scene_solver_pass_policy_test.c
autonomous: true
requirements: [V13-01]
must_haves:
  truths:
    - "Developer can run deterministic trigger + pass-policy regression checks on Windows Vulkan without widening scope."
    - "Any trigger/pass-policy failure or flake is fixed in-phase before continuing."
    - "Verification evidence records exact command + pass/fail summary for closure traceability."
  artifacts:
    - path: ".planning/phases/25-regression-and-reliability-closure/25-VERIFICATION.md"
      provides: "Phase 25 command/result evidence for baseline runs"
    - path: "src/tests/scene_solver_trigger_test.c"
      provides: "Auto/manual trigger queue reliability contracts"
    - path: "src/tests/scene_solver_pass_policy_test.c"
      provides: "Tolerance/pass-cap deterministic policy contracts"
  key_links:
    - from: "src/CMakeLists.txt"
      to: "scene_solver_trigger, scene_solver_pass_policy"
      via: "CTest target registration"
      pattern: "add_test\\(NAME scene_solver_(trigger|pass_policy)"
    - from: ".planning/phases/25-regression-and-reliability-closure/25-VERIFICATION.md"
      to: "Windows Vulkan ctest execution"
      via: "command + summary evidence"
      pattern: "ctest --test-dir build-vulkan -C Release"
---

<objective>
Stabilize trigger integrity and iterative pass-policy reliability as the first closure slice for V13-01.

Purpose: enforce strict pass/fail behavior early so regressions are fixed before full closure (per D-04, D-05, D-06).
Output: green trigger/pass-policy gate evidence and in-scope fixes with no feature expansion (per D-11, D-12).
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-regression-and-reliability-closure/25-CONTEXT.md
@.planning/phases/25-regression-and-reliability-closure/25-RESEARCH.md
@.planning/phases/25-regression-and-reliability-closure/25-VALIDATION.md
@src/CMakeLists.txt
@src/tests/scene_solver_trigger_test.c
@src/tests/scene_solver_pass_policy_test.c
@src/ecs/ecs_scene.h
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run and evidence the trigger/pass-policy reliability gate slice</name>
  <read_first>@.planning/phases/25-regression-and-reliability-closure/25-CONTEXT.md, @.planning/phases/25-regression-and-reliability-closure/25-VALIDATION.md, @src/CMakeLists.txt</read_first>
  <files>.planning/phases/25-regression-and-reliability-closure/25-VERIFICATION.md</files>
  <action>Verify `src/CMakeLists.txt` still registers the locked seven-test baseline exactly (per D-01, D-02, D-03), then run the focused reliability slice for `scene_solver_trigger` and `scene_solver_pass_policy` on `build-vulkan` and append command + result summary to `25-VERIFICATION.md` (per D-08, D-13). If either test is non-deterministic across immediate rerun, record it as failure (per D-06), not warning.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "scene_solver_trigger|scene_solver_pass_policy" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>Verification artifact includes exact command text, timestamped result summary, and explicit note that any flake is treated as failure.</acceptance_criteria>
  <done>Trigger/pass-policy baseline evidence exists and is reproducible on Windows Vulkan scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix trigger/pass-policy regressions and re-run until deterministic green</name>
  <read_first>@src/tests/scene_solver_trigger_test.c, @src/tests/scene_solver_pass_policy_test.c, @src/ecs/ecs_scene.h, @.planning/phases/25-regression-and-reliability-closure/25-CONTEXT.md</read_first>
  <files>src/ecs/ecs_scene.h, src/tests/scene_solver_trigger_test.c, src/tests/scene_solver_pass_policy_test.c, .planning/phases/25-regression-and-reliability-closure/25-VERIFICATION.md</files>
  <action>If Task 1 reveals failures or flakes, implement only minimal regression fixes in solver/test ownership files that restore existing trigger and pass-policy contracts (per D-05) without adding new capability (per D-11, D-12). Re-run the same focused gate until two consecutive identical green results are achieved to satisfy anti-flake closure posture (per D-06), and append final command + result summary to `25-VERIFICATION.md` (per D-08).</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "scene_solver_trigger|scene_solver_pass_policy" --output-on-failure && ctest --test-dir build-vulkan -C Release -R "scene_solver_trigger|scene_solver_pass_policy" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>Trigger/pass-policy slice is green in two consecutive runs, and any code changes are regression-only with no new feature behavior.</acceptance_criteria>
  <done>Trigger integrity and iterative pass-policy behavior are deterministic and closure-ready.</done>
</task>

</tasks>

<verification>
Run focused reliability slice twice back-to-back:
`ctest --test-dir build-vulkan -C Release -R "scene_solver_trigger|scene_solver_pass_policy" --output-on-failure`
</verification>

<success_criteria>
- D-01..D-03 preserved (no gate expansion, no extra sentinels).
- D-04..D-06 enforced for trigger/pass-policy reliability.
- Evidence for this slice is explicit and reproducible (D-08, D-13).
</success_criteria>

<output>
After completion, create `.planning/phases/25-regression-and-reliability-closure/25-01-SUMMARY.md`
</output>

