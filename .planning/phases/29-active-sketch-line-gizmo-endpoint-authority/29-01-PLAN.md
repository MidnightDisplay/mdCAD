---
phase: 29-active-sketch-line-gizmo-endpoint-authority
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app.c
  - src/gizmo/gizmo.h
  - src/ecs/ecs_scene.h
  - src/tests/endpoint_pick_test.c
  - src/tests/scene_solver_drag_test.c
autonomous: true
requirements: [GZM-01, GZM-02, GZM-03]
must_haves:
  truths:
    - "User sees gizmo center at the midpoint of one selected active-sketch line."
    - "User sees one gizmo center at the average of selected active-sketch line midpoints for multi-line selection."
    - "Dragging in transform mode (and geometry mode with vertex mode inactive) moves eligible active-sketch line endpoints A/B together by the same projected delta."
    - "Mixed selection drags update eligible active-sketch lines via endpoint-authority while non-active or non-line entities continue existing transform/vertex semantics."
  artifacts:
    - path: "src/gizmo/gizmo.h"
      provides: "Midpoint-anchored center computation for eligible active-sketch lines."
    - path: "src/app.c"
      provides: "Eligibility split + drag routing that applies one projected delta to line-authority subset and legacy subset."
    - path: "src/ecs/ecs_scene.h"
      provides: "Scene-level active-sketch line rigid-translation endpoint apply helper."
    - path: "src/tests/endpoint_pick_test.c"
      provides: "Regression coverage for midpoint anchor, multi-midpoint average, mixed-selection guardrails."
    - path: "src/tests/scene_solver_drag_test.c"
      provides: "Projected-delta and constrained-drag compatibility checks for active-sketch line endpoint-authority."
  key_links:
    - from: "src/gizmo/gizmo.h"
      to: "src/app.c"
      via: "gizmo_update center policy consumed in drag lifecycle"
      pattern: "midpoint|average"
    - from: "src/app.c"
      to: "src/ecs/ecs_scene.h"
      via: "eligible line subset routed through endpoint-authority helper"
      pattern: "scene_apply_active_sketch_line"
    - from: "src/app.c"
      to: "src/ecs/ecs_scene.h"
      via: "constrained drag projection reused by both subsets"
      pattern: "scene_solver_can_apply_drag"
---

<objective>
Deliver active-sketch line midpoint anchoring and endpoint-authoritative drag semantics for runtime interaction while preserving existing behavior for non-eligible entities.

Purpose: Satisfy GZM-01/GZM-02/GZM-03 by implementing D-01..D-09 guardrails exactly.
Output: Updated gizmo center policy, eligibility/routing helpers, scene endpoint apply path, and regression tests.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-active-sketch-line-gizmo-endpoint-authority/29-CONTEXT.md
@.planning/phases/29-active-sketch-line-gizmo-endpoint-authority/29-RESEARCH.md
@.planning/phases/29-active-sketch-line-gizmo-endpoint-authority/29-VALIDATION.md
@src/app.c
@src/gizmo/gizmo.h
@src/ecs/ecs_scene.h
@src/tests/endpoint_pick_test.c
@src/tests/scene_solver_drag_test.c

<interfaces>
From src/gizmo/gizmo.h:
```c
static inline vec3_t gizmo_entity_world_center(ecs_world_state_t *world, ecs_entity_t entity);
static inline void gizmo_update(gizmo_t *g,
                                const selection_buffer_t *selection,
                                void *scene_ptr,
                                vec3_t cam_pos,
                                float fov,
                                float ecs_point_size);
```

From src/ecs/ecs_scene.h:
```c
static inline bool scene_apply_endpoint_point_world_delta(ecs_scene_t *scene,
                                                           ecs_entity_t endpoint_entity,
                                                           vec3_t world_delta);
static inline bool scene_apply_transform_delta_for_selection(ecs_scene_t *scene,
                                                             const ecs_entity_t *entities,
                                                             int entity_count,
                                                             vec3_t world_delta);
static inline bool scene_solver_can_apply_drag(ecs_scene_t *scene,
                                                ecs_entity_t sketch,
                                                const ecs_entity_t *drag_entities,
                                                int drag_entity_count,
                                                vec3_t requested_delta,
                                                scene_solver_drag_decision_t *out_decision);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for midpoint anchor and eligibility-guarded endpoint authority</name>
  <read_first>@.planning/phases/29-active-sketch-line-gizmo-endpoint-authority/29-CONTEXT.md, @src/tests/endpoint_pick_test.c, @src/tests/scene_solver_drag_test.c, @src/gizmo/gizmo.h, @src/app.c</read_first>
  <files>src/tests/endpoint_pick_test.c, src/tests/scene_solver_drag_test.c</files>
  <behavior>
    - Test 1 (D-08, GZM-01): single active-sketch line center equals midpoint `(a+b)*0.5f`.
    - Test 2 (D-09, GZM-01): multi active-sketch line center equals arithmetic mean of selected line midpoints.
    - Test 3 (D-01, D-02, D-05, D-06, GZM-02): eligible active-sketch line drag applies identical projected delta to both endpoints A and B.
    - Test 4 (D-03, D-04, D-07, GZM-03): mixed selection keeps non-active/non-line/vertex-active behavior unchanged while active-sketch lines use endpoint-authority path.
  </behavior>
  <action>Add new regression cases with explicit numeric expectations (epsilon `1e-4f`) for midpoint and rigid endpoint translation. Include at least one mixed-selection fixture combining an active-sketch line + a non-sketch line (or non-line entity) and assert only eligible line endpoints change through the endpoint-authority path. Add a vertex-mode-active negative case that proves no takeover by line-authority path (D-07). Keep tests failing before implementation.</action>
  <verify>
    <automated>ctest --test-dir build --output-on-failure -R "endpoint_pick|scene_solver_drag"</automated>
  </verify>
  <acceptance_criteria>New test names include `gizmo_midpoint`, `active_sketch_line_rigid_delta`, and `mixed_selection_guardrail` (grep-visible), and at least one fails before runtime code changes.</acceptance_criteria>
  <done>Phase 29 behavioral contracts for GZM-01/02/03 are codified as failing tests first.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement midpoint-centered gizmo and mixed-subset drag routing for active-sketch lines</name>
  <read_first>@src/app.c, @src/gizmo/gizmo.h, @src/ecs/ecs_scene.h, @.planning/phases/29-active-sketch-line-gizmo-endpoint-authority/29-RESEARCH.md, @src/tests/endpoint_pick_test.c, @src/tests/scene_solver_drag_test.c</read_first>
  <files>src/app.c, src/gizmo/gizmo.h, src/ecs/ecs_scene.h</files>
  <behavior>
    - Eligible condition is exactly: active-sketch `GEOM_LINE` and mode is `GIZMO_TRANSFORM_MODE` OR (`GIZMO_GEOMETRY_MODE` and `!vertex_mode.active`) (D-01, D-05, D-06, D-07).
    - Single-line center uses line midpoint; multi-line center uses average of line midpoints (D-08, D-09).
    - Drag update uses one resolved delta from `scene_solver_can_apply_drag`, then applies that same delta to eligible line subset and fallback subset (D-02, D-03).
    - Non-active sketch lines and non-line selections remain on existing behavior paths (D-04).
  </behavior>
  <action>In `src/app.c`, add helper(s) that partition current selection into eligible active-sketch lines vs fallback entities and reuse one projected drag delta for both groups. In `src/gizmo/gizmo.h`, add midpoint-based center contribution for eligible active-sketch line entities with explicit formula `mid=(a+b)*0.5f`, and average across selected eligible lines. In `src/ecs/ecs_scene.h`, add a line-level world-delta helper that updates endpoints A/B as rigid translation through endpoint-aware geometry update path so `a' = a + delta_local` and `b' = b + delta_local`, then sync endpoint entities and trigger solver/script refresh once per touched owner. Do not replace existing transform/vertex behavior for non-eligible entities (D-03, D-04).</action>
  <verify>
    <automated>ctest --test-dir build --output-on-failure -R "endpoint_pick|scene_solver_drag"</automated>
  </verify>
  <acceptance_criteria>`ctest` targeted slice passes; grep confirms new active-sketch line routing helpers in `src/app.c` and midpoint path in `src/gizmo/gizmo.h`; mixed-selection tests pass without modifying non-eligible semantics.</acceptance_criteria>
  <done>Runtime interaction now satisfies midpoint anchoring and endpoint-authority scope semantics for eligible active-sketch lines.</done>
</task>

</tasks>

<verification>
Run targeted Phase 29 interaction slice:
`ctest --test-dir build --output-on-failure -R "endpoint_pick|scene_solver_drag"`
</verification>

<success_criteria>
- GZM-01: Midpoint anchor behavior is correct for single and multi active-sketch line selection.
- GZM-02: Eligible active-sketch line drags translate A/B rigidly by one resolved delta.
- GZM-03: Non-eligible and vertex-active semantics are preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/29-active-sketch-line-gizmo-endpoint-authority/29-01-SUMMARY.md`
</output>

