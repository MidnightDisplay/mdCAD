---
phase: 29-active-sketch-line-gizmo-endpoint-authority
plan: 02
type: execute
wave: 2
depends_on: [29-01]
files_modified:
  - src/app.c
  - src/undo_redo.h
  - src/undo_redo_exec.h
  - src/tests/endpoint_pick_test.c
autonomous: true
requirements: [GZM-04]
must_haves:
  truths:
    - "One completed active-sketch line drag is undoable in one undo step."
    - "Redo of that interaction restores the exact post-drag line endpoints for all affected lines."
    - "Undo replay restores exact pre-drag line endpoints for all affected lines and refreshes endpoint entities/solver/script outputs."
  artifacts:
    - path: "src/undo_redo.h"
      provides: "Command model for grouped line-endpoint interaction payload."
    - path: "src/undo_redo_exec.h"
      provides: "Apply/unapply replay logic for grouped endpoint interaction with endpoint sync + solver/script triggers."
    - path: "src/app.c"
      provides: "Single drag-end command emission for all changed eligible line endpoints."
    - path: "src/tests/endpoint_pick_test.c"
      provides: "Roundtrip tests proving one command per drag and exact geometry restore."
  key_links:
    - from: "src/app.c"
      to: "src/undo_redo_exec.h"
      via: "drag-end snapshot packaging into one undo command"
      pattern: "undo_cmd_.*bulk|group"
    - from: "src/undo_redo_exec.h"
      to: "src/ecs/ecs_scene.h"
      via: "replay side effects"
      pattern: "scene_sync_endpoint_entities_for_owner|scene_solver_request_auto|scene_script_reemit_for_sketch"
---

<objective>
Implement grouped undo/redo for active-sketch line endpoint-authority drags so one drag is one coherent history interaction with exact endpoint restoration.

Purpose: Satisfy GZM-04 and locked D-10/D-11 without broad undo system redesign.
Output: New grouped undo command path, drag-end aggregation in app, and regression tests proving one-step undo/redo fidelity.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/29-active-sketch-line-gizmo-endpoint-authority/29-CONTEXT.md
@.planning/phases/29-active-sketch-line-gizmo-endpoint-authority/29-RESEARCH.md
@.planning/phases/29-active-sketch-line-gizmo-endpoint-authority/29-01-PLAN.md
@src/app.c
@src/undo_redo.h
@src/undo_redo_exec.h
@src/tests/endpoint_pick_test.c

<interfaces>
From src/undo_redo.h:
```c
typedef enum {
  CMD_SET_LINE_ENDPOINTS,
  CMD_MOVE_ENDPOINT_PARTICIPANT,
} undo_cmd_type_t;

typedef struct {
  uint64_t owner_entity_id;
  constraint_participant_role_t role;
  uint8_t sub_index;
  vec3_t old_local_point;
  vec3_t new_local_point;
} cmd_move_endpoint_participant_t;
```

From src/undo_redo_exec.h:
```c
static inline void undo_cmd_move_endpoint_participant(undo_redo_t *ur,
                                                       ecs_entity_t owner_entity,
                                                       constraint_participant_role_t role,
                                                       uint8_t sub_index,
                                                       vec3_t old_local_point,
                                                       vec3_t new_local_point);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing grouped undo/redo regression tests for active-sketch line drags</name>
  <read_first>@.planning/phases/29-active-sketch-line-gizmo-endpoint-authority/29-CONTEXT.md, @src/tests/endpoint_pick_test.c, @src/app.c, @src/undo_redo_exec.h</read_first>
  <files>src/tests/endpoint_pick_test.c</files>
  <behavior>
    - Test 1 (D-10, GZM-04): one completed multi-line active-sketch drag adds one undo command entry.
    - Test 2 (D-11, GZM-04): undo restores exact pre-drag A/B endpoints for all touched lines (epsilon `1e-6f`).
    - Test 3 (D-11, GZM-04): redo restores exact post-drag A/B endpoints for all touched lines and endpoint-owner sync side effects are visible.
  </behavior>
  <action>Add tests that simulate an endpoint-authority eligible drag on 2+ active-sketch lines, then inspect undo stack count/current transitions to prove single grouped command behavior. Assert exact endpoint tuples before drag, after drag, after undo, after redo. Include assertions that endpoint entities are synchronized with owner geometry after replay (no stale endpoint points).</action>
  <verify>
    <automated>ctest --test-dir build --output-on-failure -R endpoint_pick</automated>
  </verify>
  <acceptance_criteria>New tests include names containing `single_interaction_undo` and `redo_exact_endpoints`, and fail before grouped command implementation.</acceptance_criteria>
  <done>GZM-04 interaction coherence is captured in failing tests before runtime changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement grouped drag undo command and wire drag-end recording</name>
  <read_first>@src/undo_redo.h, @src/undo_redo_exec.h, @src/app.c, @src/tests/endpoint_pick_test.c, @.planning/phases/29-active-sketch-line-gizmo-endpoint-authority/29-RESEARCH.md</read_first>
  <files>src/undo_redo.h, src/undo_redo_exec.h, src/app.c</files>
  <behavior>
    - Exactly one grouped command is pushed per completed eligible drag (D-10).
    - Replay/unplay restores exact endpoint geometry across all captured lines (D-11).
    - Replay/unplay triggers endpoint sync + solver auto request + sketch script reemit for touched owners (D-11).
  </behavior>
  <action>Introduce one grouped command payload (e.g., bulk endpoint-participant moves or bulk line-endpoint snapshots) in `undo_redo.h` and implement apply/unapply replay in `undo_redo_exec.h` with explicit owner endpoint sync and solver/script refresh calls after geometry restoration. In `src/app.c` drag-end path, collect start/end endpoint snapshots for every eligible active-sketch line moved during the interaction and push one grouped command only if at least one endpoint changed beyond epsilon. Keep non-eligible drag undo behavior unchanged; this task is Phase 29-only and must not add Phase 30 validation/docs work.</action>
  <verify>
    <automated>ctest --test-dir build --output-on-failure -R endpoint_pick</automated>
  </verify>
  <acceptance_criteria>Grouped command type and replay handlers exist (grep-visible in `undo_redo.h` and `undo_redo_exec.h`), endpoint_pick grouped undo tests pass, and undo stack increments by one for one completed drag.</acceptance_criteria>
  <done>Active-sketch line endpoint-authority drag is recorded and replayed as one coherent undo/redo interaction.</done>
</task>

<task type="auto">
  <name>Task 3: Run deterministic rerun gate for Phase 29 interaction slice</name>
  <read_first>@src/tests/endpoint_pick_test.c, @src/tests/scene_solver_drag_test.c, @.planning/phases/29-active-sketch-line-gizmo-endpoint-authority/29-VALIDATION.md</read_first>
  <files>src/tests/endpoint_pick_test.c, src/tests/scene_solver_drag_test.c</files>
  <action>Execute the targeted Phase 29 regression slice twice in sequence to ensure deterministic repeatability after grouped undo integration. Use the same test regex each run and resolve only Phase 29 interaction regressions if flaky behavior appears.</action>
  <verify>
    <automated>ctest --test-dir build --output-on-failure -R "endpoint_pick|scene_solver_drag" && ctest --test-dir build --output-on-failure -R "endpoint_pick|scene_solver_drag"</automated>
  </verify>
  <acceptance_criteria>Two consecutive targeted runs pass with identical green outcomes and no intermittent failures in endpoint-authority tests.</acceptance_criteria>
  <done>Phase 29 has deterministic automated evidence for midpoint/endpoint-authority/undo interaction behavior.</done>
</task>

</tasks>

<verification>
`ctest --test-dir build --output-on-failure -R "endpoint_pick|scene_solver_drag" && ctest --test-dir build --output-on-failure -R "endpoint_pick|scene_solver_drag"`
</verification>

<success_criteria>
- GZM-04: One drag = one undo entry, undo/redo roundtrip restores exact endpoint geometry and side effects.
- No regressions to existing non-eligible drag undo semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/29-active-sketch-line-gizmo-endpoint-authority/29-02-SUMMARY.md`
</output>

