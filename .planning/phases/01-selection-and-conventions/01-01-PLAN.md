---
phase: 01-selection-and-conventions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
  - README.md
  - docs/MATH_BACKEND_DECISION.md
autonomous: true
requirements:
  - FOUND-01
user_setup: []
must_haves:
  truths:
    - "Project planning docs consistently name cglm 0.9.6 as the chosen backend for the active milestone"
    - "No active milestone requirement or roadmap phase still demands a broad compatibility facade before hotspot migration begins"
    - "The repo introduction reflects the active math-foundation migration work"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Updated phase map aligned to direct cglm adoption"
      contains: "Direct Adoption"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Requirement wording aligned to thin entrypoint rather than compatibility facade"
      contains: "thin project-owned math entrypoint"
    - path: "docs/MATH_BACKEND_DECISION.md"
      provides: "Pinned backend/version/adoption decision record"
      contains: "Selected backend: cglm 0.9.6"
    - path: "README.md"
      provides: "Current project blurb including math-foundation migration"
      contains: "math foundation"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Phase 2 wording and FOUND-02 language match the direct-adoption decision"
      pattern: "thin project-owned math entrypoint"
    - from: "docs/MATH_BACKEND_DECISION.md"
      to: ".planning/ROADMAP.md"
      via: "backend, version, and adoption mode wording are identical"
      pattern: "cglm 0\\.9\\.6"
    - from: "README.md"
      to: ".planning/PROJECT.md"
      via: "project introduction mirrors the active modernization focus"
      pattern: "math foundation"
---

<objective>
Reconcile the phase map and project-facing docs with the locked backend/adoption decisions.

Purpose: Downstream planning and execution must stop assuming a wrapper-first migration path before any build or code work begins.
Output: Updated roadmap/requirements/state files, a backend decision record, and a README intro that matches the active math-foundation milestone.
</objective>

<execution_context>
@/Users/rodionradchenko/Documents/repos/mdCAD/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rodionradchenko/Documents/repos/mdCAD/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-selection-and-conventions/01-CONTEXT.md
@.planning/phases/01-selection-and-conventions/01-RESEARCH.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile roadmap and requirements with the locked adoption direction</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md, .planning/phases/01-selection-and-conventions/01-CONTEXT.md, .planning/phases/01-selection-and-conventions/01-RESEARCH.md</read_first>
  <action>Update the planning files to implement D-01 through D-05 exactly. In `.planning/ROADMAP.md`, keep Phase 1 as the cglm selection/convention phase but rename Phase 2 from `Compatibility Layer and Validation Harness` to `Direct Adoption Tooling and Validation Harness`, rewrite its goal so it creates a thin project-owned entrypoint plus validation tooling instead of a compatibility facade, and update its plan bullets to reference direct cglm adoption. In `.planning/REQUIREMENTS.md`, rewrite `FOUND-02` to `mdCAD exposes a thin project-owned math entrypoint for convention/config includes without re-wrapping vendor math types during the staged rollout`, and rewrite `TAIL-02` so it refers to removing or shrinking temporary thin entrypoint glue rather than a compatibility layer. In `.planning/STATE.md`, add explicit text that the active phase is following direct `cglm` adoption, `cglm 0.9.6` is locked, and no active fallback candidate is being carried forward.</action>
  <verify>
    <automated>rg -n "cglm 0\\.9\\.6|Direct Adoption Tooling and Validation Harness|thin project-owned math entrypoint|direct cglm adoption|no active fallback" .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/ROADMAP.md` contains `Phase 2: Direct Adoption Tooling and Validation Harness`
    - `.planning/REQUIREMENTS.md` contains `FOUND-02: mdCAD exposes a thin project-owned math entrypoint for convention/config includes without re-wrapping vendor math types during the staged rollout`
    - `.planning/REQUIREMENTS.md` does not contain `compatibility layer that isolates vendor math headers and types`
    - `.planning/STATE.md` contains `cglm 0.9.6` and `direct cglm adoption`
  </acceptance_criteria>
  <done>Roadmap, requirements, and state all reflect D-01 through D-05 with no lingering wrapper-first language for the active milestone.</done>
</task>

<task type="auto">
  <name>Task 2: Record the backend decision and refresh the project introduction</name>
  <files>docs/MATH_BACKEND_DECISION.md, README.md</files>
  <read_first>README.md, .planning/PROJECT.md, .planning/phases/01-selection-and-conventions/01-CONTEXT.md, .planning/research/SUMMARY.md, .planning/research/STACK.md</read_first>
  <action>Create `docs/MATH_BACKEND_DECISION.md` with these exact sections and statements: `Selected backend: cglm 0.9.6`, `Reopen triggers: build-system blocker or license blocker only`, `Fallback posture: no active fallback candidate`, `Adoption model: direct cglm use with a thin project-owned convention/config entrypoint`, and `API posture: struct or array cglm API may be chosen per subsystem` (implements D-01, D-02, D-03, D-04, D-06, D-15, and D-17). Update the first paragraph of `README.md` so it still describes mdCAD as a cross-platform CAD viewer/editor but explicitly adds that the current milestone is modernizing the math foundation by replacing `src/math3d.h` with vendored `cglm`.</action>
  <verify>
    <automated>rg -n "Selected backend: cglm 0\\.9\\.6|Reopen triggers: build-system blocker or license blocker only|Fallback posture: no active fallback candidate|direct cglm use with a thin project-owned convention/config entrypoint|math foundation" docs/MATH_BACKEND_DECISION.md README.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/MATH_BACKEND_DECISION.md` contains `Selected backend: cglm 0.9.6`
    - `docs/MATH_BACKEND_DECISION.md` contains `Fallback posture: no active fallback candidate`
    - `docs/MATH_BACKEND_DECISION.md` contains `API posture: struct or array cglm API may be chosen per subsystem`
    - `README.md` contains `replacing \`src/math3d.h\` with vendored \`cglm\``
  </acceptance_criteria>
  <done>The repo has a concrete backend decision record and the README intro matches the active migration work.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "cglm 0\\.9\\.6|Direct Adoption Tooling and Validation Harness|thin project-owned math entrypoint|math foundation" .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md docs/MATH_BACKEND_DECISION.md README.md` returns matches for every target file
- [ ] `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` no longer describe Phase 2 as a compatibility facade phase
- [ ] README intro still reads as a concise project blurb, not an implementation changelog
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 1 planning docs align with the user's locked backend/adoption decisions
  </success_criteria>

<output>
After completion, create `.planning/phases/01-selection-and-conventions/01-01-SUMMARY.md`
</output>
