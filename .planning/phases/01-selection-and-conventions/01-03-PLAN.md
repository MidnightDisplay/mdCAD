---
phase: 01-selection-and-conventions
plan: 03
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - src/math/math_conventions.h
  - src/math/cglm_entry.h
  - docs/MATH_CONVENTIONS.md
autonomous: true
requirements:
  - FOUND-03
user_setup: []
must_haves:
  truths:
    - "The repo has one code-owned source of truth for matrix layout, handedness, clip depth, and alignment policy"
    - "The math contract explicitly names column-major, right-handed, clip depth 0..1, and a performance-first alignment policy"
    - "The rollout guidance preserves visible camera/picking/gizmo behavior while the internal convention changes land"
  artifacts:
    - path: "src/math/math_conventions.h"
      provides: "Compiled convention and alignment contract"
      contains: "MDCAD_MATH_CLIP_DEPTH_ZERO_TO_ONE 1"
    - path: "docs/MATH_CONVENTIONS.md"
      provides: "Human-readable explanation of the math contract and hotspot guidance"
      contains: "Visible behavior normalization during rollout"
    - path: "src/math/cglm_entry.h"
      provides: "Entry point wired to the convention contract"
      contains: "#include \"math_conventions.h\""
  key_links:
    - from: "src/math/cglm_entry.h"
      to: "src/math/math_conventions.h"
      via: "entrypoint includes the convention header before cglm headers"
      pattern: "math_conventions\\.h"
    - from: "docs/MATH_CONVENTIONS.md"
      to: "src/math/math_conventions.h"
      via: "doc and header define the same clip-depth and alignment policy"
      pattern: "0\\.\\.1|performance-first"
    - from: "docs/MATH_CONVENTIONS.md"
      to: "src/orbit_camera.h"
      via: "doc names runtime hotspots that must preserve visible behavior during rollout"
      pattern: "src/orbit_camera\\.h"
---

<objective>
Create the authoritative convention/alignment contract and wire it into the cglm entrypoint.

Purpose: FOUND-03 requires one project-owned place that defines the migration's math semantics before hotspot work starts.
Output: A compiled convention header, a supporting design doc, and an entrypoint that includes the contract before vendored cglm headers.
</objective>

<execution_context>
@/Users/rodionradchenko/Documents/repos/mdCAD/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rodionradchenko/Documents/repos/mdCAD/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-selection-and-conventions/01-CONTEXT.md
@.planning/phases/01-selection-and-conventions/01-RESEARCH.md
@.planning/phases/01-selection-and-conventions/01-02-SUMMARY.md
@src/math3d.h
@src/components/component_types.h
@src/components/transform_comp.h
@src/gpu/geometry_batch.h
@src/gpu/pick_buffer.h
@src/math/cglm_entry.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the convention and alignment contract</name>
  <files>src/math/math_conventions.h, docs/MATH_CONVENTIONS.md</files>
  <read_first>src/math3d.h, src/components/component_types.h, src/components/transform_comp.h, src/gpu/geometry_batch.h, src/gpu/pick_buffer.h, .planning/phases/01-selection-and-conventions/01-CONTEXT.md, .planning/phases/01-selection-and-conventions/01-RESEARCH.md</read_first>
  <action>Create `src/math/math_conventions.h` as the single code-owned contract for D-07, D-08, D-09, D-10, D-11, D-12, D-13, and D-14. Define these exact macros with value `1`: `MDCAD_MATH_MATRIX_COLUMN_MAJOR`, `MDCAD_MATH_HANDEDNESS_RIGHT_HANDED`, `MDCAD_MATH_CLIP_DEPTH_ZERO_TO_ONE`, `MDCAD_MATH_ALIGNMENT_POLICY_PERFORMANCE_FIRST`, `MDCAD_MATH_NATIVE_GATE_BLOCK_ON_POLICY_BREAK`, and `MDCAD_MATH_NORMALIZE_VISIBLE_BEHAVIOR`. In comments and the paired doc, explicitly state that data-layout refactors are allowed when needed (D-08), one global alignment policy is mandatory (D-09), migration slices must block rather than silently falling back when a native gate cannot honor the policy (D-10), the new convention replaces `src/math3d.h` as the internal baseline (D-11), and backend glue may adapt but semantics may not fork per backend (D-13). Add comments naming the rollout-sensitive hotspots `src/orbit_camera.h`, `src/app.c`, `src/ecs/ecs_scene.h`, `src/gpu/pick_buffer.h`, and `src/gizmo/gizmo.h`. Create `docs/MATH_CONVENTIONS.md` with a matching table that explains the same decisions and includes a `Visible behavior normalization during rollout` section describing how camera, picking, and gizmo feel must remain stable even while the internal clip-depth convention moves to `0..1`.</action>
  <verify>
    <automated>rg -n "MDCAD_MATH_MATRIX_COLUMN_MAJOR 1|MDCAD_MATH_HANDEDNESS_RIGHT_HANDED 1|MDCAD_MATH_CLIP_DEPTH_ZERO_TO_ONE 1|MDCAD_MATH_ALIGNMENT_POLICY_PERFORMANCE_FIRST 1|MDCAD_MATH_NATIVE_GATE_BLOCK_ON_POLICY_BREAK 1|MDCAD_MATH_NORMALIZE_VISIBLE_BEHAVIOR 1|Visible behavior normalization during rollout|src/orbit_camera\\.h" src/math/math_conventions.h docs/MATH_CONVENTIONS.md</automated>
  </verify>
  <acceptance_criteria>
    - `src/math/math_conventions.h` contains `MDCAD_MATH_CLIP_DEPTH_ZERO_TO_ONE 1`
    - `src/math/math_conventions.h` contains `MDCAD_MATH_ALIGNMENT_POLICY_PERFORMANCE_FIRST 1`
    - `src/math/math_conventions.h` contains `MDCAD_MATH_NATIVE_GATE_BLOCK_ON_POLICY_BREAK 1`
    - `docs/MATH_CONVENTIONS.md` contains `Visible behavior normalization during rollout`
    - `docs/MATH_CONVENTIONS.md` contains `src/orbit_camera.h`, `src/app.c`, `src/ecs/ecs_scene.h`, `src/gpu/pick_buffer.h`, and `src/gizmo/gizmo.h`
  </acceptance_criteria>
  <done>The repo has one compiled convention contract plus one matching doc that encode D-07 through D-14 explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the convention contract into the cglm entrypoint</name>
  <files>src/math/cglm_entry.h</files>
  <read_first>src/math/cglm_entry.h, src/math/math_conventions.h, docs/MATH_CONVENTIONS.md, .planning/phases/01-selection-and-conventions/01-CONTEXT.md</read_first>
  <action>Update `src/math/cglm_entry.h` so it includes `math_conventions.h` before any `cglm` include and so the entrypoint documents D-06 and D-12 through D-17 directly. Add a comment block stating `Subsystems may choose the cglm array API or struct API per file, but all must honor the global contract in math_conventions.h.` Keep the direct-adoption posture: do not add mdCAD typedef aliases. Add `_Static_assert(sizeof(mat4) == sizeof(float) * 16, "cglm mat4 must remain 16 floats");` and `_Static_assert(sizeof(mat4s) == sizeof(mat4), "cglm struct API must stay layout-compatible with mat4");` so alignment/layout assumptions become visible at compile time.</action>
  <verify>
    <automated>rg -n "#include \"math_conventions.h\"|Subsystems may choose the cglm array API or struct API per file|_Static_assert\\(sizeof\\(mat4\\) == sizeof\\(float\\) \\* 16|_Static_assert\\(sizeof\\(mat4s\\) == sizeof\\(mat4\\)" src/math/cglm_entry.h</automated>
  </verify>
  <acceptance_criteria>
    - `src/math/cglm_entry.h` contains `#include "math_conventions.h"`
    - `src/math/cglm_entry.h` contains `Subsystems may choose the cglm array API or struct API per file`
    - `src/math/cglm_entry.h` contains `_Static_assert(sizeof(mat4) == sizeof(float) * 16, "cglm mat4 must remain 16 floats");`
    - `src/math/cglm_entry.h` contains `_Static_assert(sizeof(mat4s) == sizeof(mat4), "cglm struct API must stay layout-compatible with mat4");`
    - `src/math/cglm_entry.h` does not contain `typedef vec3s` or `typedef mat4s`
  </acceptance_criteria>
  <done>The cglm entrypoint now enforces the project-owned convention contract and keeps direct vendor types exposed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "MDCAD_MATH_CLIP_DEPTH_ZERO_TO_ONE 1|MDCAD_MATH_ALIGNMENT_POLICY_PERFORMANCE_FIRST 1|Visible behavior normalization during rollout" src/math/math_conventions.h docs/MATH_CONVENTIONS.md src/math/cglm_entry.h` succeeds
- [ ] `src/math/cglm_entry.h` includes `math_conventions.h` before vendor headers
- [ ] The convention header/doc explicitly describe rollout normalization for camera, picking, and gizmo behavior
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 1 ends with one authoritative convention/alignment contract wired into the cglm entrypoint
  </success_criteria>

<output>
After completion, create `.planning/phases/01-selection-and-conventions/01-03-SUMMARY.md`
</output>
