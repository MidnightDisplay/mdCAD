---
phase: 01-selection-and-conventions
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - vendors/cglm/include/cglm/cglm.h
  - vendors/cglm/include/cglm/struct.h
  - vendors/cglm/LICENSE
  - vendors/cglm/VERSION.txt
  - src/CMakeLists.txt
  - src/math/cglm_entry.h
  - src/app.c
autonomous: true
requirements:
  - FOUND-01
user_setup: []
must_haves:
  truths:
    - "The mdCAD native build compiles with vendored cglm headers without adding a C++ dependency or build-time fetch step"
    - "At least one compiled translation unit includes the project-owned cglm entrypoint so the integration is real, not just staged on disk"
    - "The first adoption path stays header-only and lightweight"
  artifacts:
    - path: "vendors/cglm/VERSION.txt"
      provides: "Pinned upstream version marker"
      contains: "0.9.6"
    - path: "vendors/cglm/include/cglm/cglm.h"
      provides: "Vendored cglm inline API"
    - path: "src/math/cglm_entry.h"
      provides: "Thin project-owned include entrypoint for direct cglm adoption"
      contains: "mdcad_cglm_compile_anchor"
    - path: "src/CMakeLists.txt"
      provides: "Native target include-path wiring for cglm"
      contains: "vendors/cglm/include"
  key_links:
    - from: "src/CMakeLists.txt"
      to: "vendors/cglm/include/cglm/cglm.h"
      via: "target include path on the mdCAD target"
      pattern: "vendors/cglm/include"
    - from: "src/math/cglm_entry.h"
      to: "vendors/cglm/include/cglm/cglm.h"
      via: "direct include of cglm headers"
      pattern: "cglm/cglm\\.h"
    - from: "src/app.c"
      to: "src/math/cglm_entry.h"
      via: "compile anchor include/call in a compiled translation unit"
      pattern: "mdcad_cglm_compile_anchor"
---

<objective>
Vendor `cglm` into the repo and prove the main native target can compile it through a thin project-owned entrypoint.

Purpose: FOUND-01 requires an actual build-path proof, not just a backend decision.
Output: Vendored `cglm` `0.9.6`, minimal include-path wiring, and a compile anchor used by the native app target.
</objective>

<execution_context>
@/Users/rodionradchenko/Documents/repos/mdCAD/.codex/get-shit-done/workflows/execute-plan.md
@/Users/rodionradchenko/Documents/repos/mdCAD/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-selection-and-conventions/01-CONTEXT.md
@.planning/phases/01-selection-and-conventions/01-RESEARCH.md
@.planning/phases/01-selection-and-conventions/01-01-SUMMARY.md
@docs/MATH_BACKEND_DECISION.md
@CMakeLists.txt
@src/CMakeLists.txt
@src/app.c
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor cglm 0.9.6 into vendors/cglm</name>
  <files>vendors/cglm/include/cglm/cglm.h, vendors/cglm/include/cglm/struct.h, vendors/cglm/LICENSE, vendors/cglm/VERSION.txt</files>
  <read_first>docs/MATH_BACKEND_DECISION.md, vendors/CMakeLists.txt, CMakeLists.txt, src/CMakeLists.txt, .planning/phases/01-selection-and-conventions/01-CONTEXT.md</read_first>
  <action>Vendor the upstream `cglm` `0.9.6` header tree into `vendors/cglm/include/` and keep the upstream license text in `vendors/cglm/LICENSE`. Add a local `vendors/cglm/VERSION.txt` file containing exactly `0.9.6` so later verification can prove the pinned version without network access. Do not add `FetchContent`, submodules, or download-at-build-time logic; this task implements D-01, D-02, D-03, D-15, D-16, and D-17 by keeping the backend pinned, local, header-only, and free of an active fallback path.</action>
  <verify>
    <automated>test -f vendors/cglm/include/cglm/cglm.h && test -f vendors/cglm/include/cglm/struct.h && test -f vendors/cglm/LICENSE && rg -n "^0\\.9\\.6$" vendors/cglm/VERSION.txt</automated>
  </verify>
  <acceptance_criteria>
    - `vendors/cglm/include/cglm/cglm.h` exists
    - `vendors/cglm/include/cglm/struct.h` exists
    - `vendors/cglm/LICENSE` exists
    - `vendors/cglm/VERSION.txt` contains exactly `0.9.6`
  </acceptance_criteria>
  <done>The repo contains a pinned, locally vendored cglm 0.9.6 tree with license text and a version marker.</done>
</task>

<task type="auto">
  <name>Task 2: Add the minimal build wiring and compile anchor</name>
  <files>src/CMakeLists.txt, src/math/cglm_entry.h, src/app.c</files>
  <read_first>src/CMakeLists.txt, src/app.c, .planning/phases/01-selection-and-conventions/01-CONTEXT.md, .planning/phases/01-selection-and-conventions/01-RESEARCH.md, docs/MATH_BACKEND_DECISION.md</read_first>
  <action>Implement D-06, D-15, D-16, and D-17 in code. In `src/CMakeLists.txt`, add the mdCAD target include path for `../vendors/cglm/include` using `target_include_directories(${PROJECT_NAME} PRIVATE ...)` and keep the change minimal. Create `src/math/cglm_entry.h` as a thin entrypoint, not a wrapper API: include `cglm/cglm.h` and `cglm/struct.h`, do not add `typedef vec3s vec3_t` or `typedef mat4s mat4_t`, and add `static inline void mdcad_cglm_compile_anchor(void)` that references both `mat4` and `mat4s` so compilation fails if either header is unavailable. In `src/app.c`, include `math/cglm_entry.h` and call `mdcad_cglm_compile_anchor();` during initialization so the main target actually compiles the vendored headers.</action>
  <verify>
    <automated>cmake -B build -G Ninja && ninja -C build && rg -n "vendors/cglm/include|cglm/cglm\\.h|cglm/struct\\.h|mdcad_cglm_compile_anchor" src/CMakeLists.txt src/math/cglm_entry.h src/app.c</automated>
  </verify>
  <acceptance_criteria>
    - `src/CMakeLists.txt` contains `vendors/cglm/include`
    - `src/math/cglm_entry.h` contains `#include <cglm/cglm.h>` and `#include <cglm/struct.h>`
    - `src/math/cglm_entry.h` contains `mdcad_cglm_compile_anchor`
    - `src/math/cglm_entry.h` does not contain `typedef vec3s` or `typedef mat4s`
    - `src/app.c` contains `#include "math/cglm_entry.h"` and `mdcad_cglm_compile_anchor();`
    - `cmake -B build -G Ninja && ninja -C build` exits 0
  </acceptance_criteria>
  <done>The mdCAD target builds successfully with vendored cglm headers through a thin project-owned entrypoint and no wrapper typedef surface.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f vendors/cglm/include/cglm/cglm.h && test -f vendors/cglm/include/cglm/struct.h && rg -n "^0\\.9\\.6$" vendors/cglm/VERSION.txt` succeeds
- [ ] `cmake -B build -G Ninja && ninja -C build` succeeds
- [ ] `src/math/cglm_entry.h` still acts as an entrypoint only and does not introduce project-owned math typedef aliases
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo contains a real, compiled, header-only cglm integration that satisfies FOUND-01
  </success_criteria>

<output>
After completion, create `.planning/phases/01-selection-and-conventions/01-02-SUMMARY.md`
</output>
