---
phase: 33-large-jump-robustness-and-parallel-along-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ecs/ecs_scene.h
  - src/tests/scene_solver_drag_test.c
  - src/tests/scene_solver_contract_test.c
  - src/tests/scene_solver_diagnostics_test.c
autonomous: true
requirements: [SROB-01, SROB-02, SROB-03, DIAG-01]
must_haves:
  truths:
    - "User can perform feasible quarter-arc closed-loop large jumps without requiring incremental wiggle/latch drags."
    - "User gets all-or-nothing transactional behavior on infeasible large jumps with no partial geometry corruption."
    - "User can immediately drag/edit again after an infeasible attempt and the solver responds without stale lock behavior."
    - "User repeating the same large-jump operation sequence gets the same failure implication ordering and diagnostics ordering."
  artifacts:
    - path: "src/ecs/ecs_scene.h"
      provides: "Two-stage large-jump staging + iterative solve handoff with transactional failure epilogue."
      contains: "large-jump staging gate + no-writeback failure path"
    - path: "src/tests/scene_solver_drag_test.c"
      provides: "Large-jump drag feasibility and post-failure responsiveness regression coverage."
      contains: "quarter-arc large-jump and immediate-followup drag tests"
    - path: "src/tests/scene_solver_contract_test.c"
      provides: "Transactional rollback assertions for large-jump unsatisfied cases."
      contains: "no-partial-mutation checks after unsatisfied large-jump attempts"
    - path: "src/tests/scene_solver_diagnostics_test.c"
      provides: "Deterministic implication/diagnostic ordering checks for large-jump failures."
      contains: "repeat-run ordering assertions for identical operation sequences"
  key_links:
    - from: "src/ecs/ecs_scene.h"
      to: "src/tests/scene_solver_drag_test.c"
      via: "scene_solver_can_apply_drag staged/projection decision path"
      pattern: "scene_solver_can_apply_drag|SCENE_SOLVER_DRAG_"
    - from: "src/ecs/ecs_scene.h"
      to: "src/tests/scene_solver_contract_test.c"
      via: "scene_solver_request_recalculate failure branch rollback/no-writeback behavior"
      pattern: "solve_failed|converged|scene_solver_set_failure_implication"
    - from: "src/ecs/ecs_scene.h"
      to: "src/tests/scene_solver_diagnostics_test.c"
      via: "deterministic implication participant ordering and diagnostics append ordering"
      pattern: "scene_solver_set_failure_implication|scene_solver_add_diagnostic"
---

<objective>
Implement Phase 33 large-jump robustness core behavior with deterministic transactional failure semantics and post-failure edit responsiveness.

Purpose: Deliver SROB reliability outcomes first so parity work in Plan 02 builds on stable staged-solve and rollback behavior.
Output: Two-stage large-jump solver path, deterministic failure ordering hardening, and regression fixtures proving no wiggle-latch, transactional rollback, and immediate recovery.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-large-jump-robustness-and-parallel-along-parity/33-CONTEXT.md
@.planning/phases/33-large-jump-robustness-and-parallel-along-parity/33-RESEARCH.md
@.planning/phases/33-large-jump-robustness-and-parallel-along-parity/33-VALIDATION.md
@.planning/phases/32-explicit-coincidence-authoring-semantics/32-02-SUMMARY.md
@src/ecs/ecs_scene.h
@src/tests/scene_solver_drag_test.c
@src/tests/scene_solver_contract_test.c
@src/tests/scene_solver_diagnostics_test.c

<interfaces>
From src/ecs/ecs_scene.h:
```c
static inline bool scene_solver_request_recalculate(ecs_scene_t *scene, ecs_entity_t sketch);
static inline bool scene_solver_set_failure_implication(ecs_scene_t *scene, ecs_entity_t sketch, ...);
static inline void scene_solver_clear_failure_implication(ecs_scene_t *scene, ecs_entity_t sketch, bool clear_diagnostics);
static inline bool scene_solver_can_apply_drag(ecs_scene_t *scene, ecs_entity_t sketch, ...);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add two-stage large-jump staging in solver recalc path</name>
  <read_first>@src/ecs/ecs_scene.h, @src/tests/scene_solver_drag_test.c, @src/tests/scene_solver_contract_test.c, @.planning/phases/33-large-jump-robustness-and-parallel-along-parity/33-CONTEXT.md</read_first>
  <files>src/ecs/ecs_scene.h</files>
  <behavior>
    - Test 1: Feasible quarter-arc large-jump uses staged/preprojected movement and converges without requiring incremental wiggle attempts.
    - Test 2: Infeasible large-jump leaves geometry identical to pre-attempt state (transactional rollback).
    - Test 3: Fixed participants remain anchors during staged solve and non-fixed participants absorb movement.
  </behavior>
  <action>In `scene_solver_request_recalculate(...)` and adjacent drag feasibility plumbing, implement explicit two-stage handling per D-05: stage-1 large-jump preprojection candidate generation, then stage-2 normal iterative passes. Preserve D-01 and D-07 by ensuring no candidate writeback occurs on unsatisfied outcomes (`solve_failed` or `!converged`). Keep fixed-anchor behavior per D-02 by preventing staged movement from mutating fixed participants. Explicitly avoid non-deterministic retry/jitter loops and avoid implicit coincidence fallback per D-04.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "scene_solver_drag|scene_solver_contract" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`git --no-pager grep -n "scene_solver_request_recalculate\\|scene_solver_can_apply_drag\\|solve_failed\\|converged" src/ecs/ecs_scene.h` shows staged-solve and rollback anchors; `ctest --test-dir build-vulkan -C Release -R "scene_solver_drag|scene_solver_contract" --output-on-failure` passes.</acceptance_criteria>
  <done>Large-jump solve path is staged and transactional, with no partial mutation on failure and no wiggle-to-latch dependency for feasible fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add large-jump robustness regressions for wiggle, rollback, and immediate recovery</name>
  <read_first>@src/tests/scene_solver_drag_test.c, @src/tests/scene_solver_contract_test.c, @src/ecs/ecs_scene.h, @.planning/phases/33-large-jump-robustness-and-parallel-along-parity/33-VALIDATION.md</read_first>
  <files>src/tests/scene_solver_drag_test.c, src/tests/scene_solver_contract_test.c</files>
  <behavior>
    - Test 1: Quarter-arc closed-loop large-jump reaches feasible solution in a single operation sequence (no required follow-up wiggle drag).
    - Test 2: Unsatisfied large-jump attempt does not partially move dependent participants.
    - Test 3: Immediate next feasible edit after unsatisfied attempt succeeds without stale lock/deadlock.
  </behavior>
  <action>Add explicit SROB fixtures per D-06/D-07/D-09: quarter-arc large-jump case in `scene_solver_drag_test.c`, transactional no-partial-corruption fixture in `scene_solver_contract_test.c`, and follow-up responsiveness continuation on the same sketch state after an infeasible attempt. Use deterministic tolerances and explicit expected coordinates/deltas; avoid vague proximity-only assertions.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "scene_solver_drag|scene_solver_contract" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`git --no-pager grep -n "quarter_arc\\|large_jump\\|transactional\\|post_failure\\|followup" src/tests/scene_solver_drag_test.c src/tests/scene_solver_contract_test.c` returns new SROB fixtures; targeted ctest command passes twice consecutively.</acceptance_criteria>
  <done>SROB-01/02/03 behaviors are locked by deterministic regression tests across drag and contract suites.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock deterministic implication and diagnostics ordering for large-jump failures</name>
  <read_first>@src/ecs/ecs_scene.h, @src/tests/scene_solver_diagnostics_test.c, @.planning/phases/33-large-jump-robustness-and-parallel-along-parity/33-CONTEXT.md</read_first>
  <files>src/ecs/ecs_scene.h, src/tests/scene_solver_diagnostics_test.c</files>
  <behavior>
    - Test 1: Identical large-jump unsatisfied sequences produce identical implication participant ordering.
    - Test 2: Identical large-jump unsatisfied sequences produce identical diagnostics ordering.
    - Test 3: Failure diagnostics remain family/reason specific, not generic participant-type errors.
  </behavior>
  <action>Extend deterministic failure taxonomy and ordering checks per D-03, D-08, D-12, and D-13 by hardening `scene_solver_set_failure_implication(...)`/diagnostics append order for large-jump unsatisfied classes. Add diagnostics tests that rerun identical operation sequences and assert stable message ordering and family+reason classification. Keep messages actionable and avoid collapsing to generic unsupported-participant text (DIAG-02 guardrail baseline for Plan 02).</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "scene_solver_diagnostics|scene_solver_drag" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`git --no-pager grep -n "scene_solver_set_failure_implication\\|diagnostic\\|large_jump\\|unsatisfied" src/ecs/ecs_scene.h src/tests/scene_solver_diagnostics_test.c` shows deterministic taxonomy/order anchors; targeted diagnostics+drag ctest slice passes.</acceptance_criteria>
  <done>Large-jump failures now emit deterministic, actionable implication+diagnostic outputs for identical operation sequences.</done>
</task>

</tasks>

<verification>
Run:
- `ctest --test-dir build-vulkan -C Release -R "scene_solver_drag|scene_solver_contract|scene_solver_diagnostics" --output-on-failure`
- Immediately rerun the exact same command and confirm pass/pass parity.
</verification>

<success_criteria>
- Feasible quarter-arc large-jump edits no longer require user wiggle/latch behavior.
- Infeasible large-jump attempts preserve pre-attempt geometry exactly (transactional rollback).
- Immediate follow-up edits after unsatisfied attempt remain responsive and solvable when feasible.
- Deterministic implication and diagnostics ordering is stable across repeated identical runs.
</success_criteria>

<output>
After completion, create `.planning/phases/33-large-jump-robustness-and-parallel-along-parity/33-01-SUMMARY.md`
</output>

