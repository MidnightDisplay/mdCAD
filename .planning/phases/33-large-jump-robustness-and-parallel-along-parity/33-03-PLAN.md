---
phase: 33-large-jump-robustness-and-parallel-along-parity
plan: 03
type: execute
wave: 3
depends_on: [33-02]
files_modified:
  - src/ecs/ecs_scene.h
  - src/tests/scene_solver_drag_test.c
  - src/tests/scene_solver_contract_test.c
  - src/tests/scene_solver_pass_policy_test.c
autonomous: true
requirements: [PARI-01, PARI-02]
gap_closure: true
must_haves:
  truths:
    - "User dragging AB in a PARALLEL pair causes CD to follow to preserve parallelism."
    - "User dragging CD in the same PARALLEL pair causes AB to follow to preserve parallelism (no permanent anchor participant)."
    - "Additional constraints (for example ALONG X/Y/Z on one participant) can limit components of motion but do not force a permanent anchor-priority role."
    - "Mirrored/reordered equivalent setups remain deterministic and parity-consistent under bidirectional drag authority."
  artifacts:
    - path: "src/ecs/ecs_scene.h"
      provides: "Equal-priority PARALLEL authority resolution based on active operation context."
      contains: "PARALLEL participant authority selection without permanent anchor bias"
    - path: "src/tests/scene_solver_drag_test.c"
      provides: "Bidirectional PARALLEL drag-follow regression coverage."
      contains: "AB->CD and CD->AB deterministic follow tests"
    - path: "src/tests/scene_solver_contract_test.c"
      provides: "Mixed-constraint PARALLEL+ALONG non-anchor-lock contract coverage."
      contains: "external-constraint participant can drive without deadlock-prone lock-in"
    - path: "src/tests/scene_solver_pass_policy_test.c"
      provides: "Deterministic mirrored/reordered parity assertions for updated authority policy."
      contains: "equivalent setup ordering invariants under bidirectional authority"
  key_links:
    - from: "src/ecs/ecs_scene.h"
      to: "src/tests/scene_solver_drag_test.c"
      via: "drag feasibility/projection authority path for PARALLEL participant selection"
      pattern: "PARALLEL|drag|projected_delta|authority"
    - from: "src/ecs/ecs_scene.h"
      to: "src/tests/scene_solver_contract_test.c"
      via: "recalculate branch honoring external ALONG constraints without permanent anchor lock"
      pattern: "CONSTRAINT_PARALLEL|CONSTRAINT_ALONG_"
    - from: "src/tests/scene_solver_drag_test.c"
      to: "src/tests/scene_solver_pass_policy_test.c"
      via: "shared mirrored/reordered deterministic parity fixtures"
      pattern: "mirrored|reordered|parity"
---

<objective>
Close the UAT-identified gap where PARALLEL behaves with anchor-priority and causes deadlock-prone UX when one participant has additional constraints.

Purpose: Restore equal-priority PARALLEL behavior so active drag context (or stronger external constraint context) determines who drives motion, bidirectionally.
Output: Solver policy update plus deterministic regressions covering AB→CD, CD→AB, and ALONG interaction without permanent anchor role.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-large-jump-robustness-and-parallel-along-parity/33-CONTEXT.md
@.planning/phases/33-large-jump-robustness-and-parallel-along-parity/33-RESEARCH.md
@.planning/phases/33-large-jump-robustness-and-parallel-along-parity/33-VERIFICATION.md
@.planning/phases/33-large-jump-robustness-and-parallel-along-parity/33-HUMAN-UAT.md
@.planning/phases/33-large-jump-robustness-and-parallel-along-parity/33-02-SUMMARY.md
@src/ecs/ecs_scene.h
@src/tests/scene_solver_drag_test.c
@src/tests/scene_solver_contract_test.c
@src/tests/scene_solver_pass_policy_test.c

<interfaces>
From src/ecs/ecs_scene.h:
```c
static inline bool scene_solver_request_recalculate(ecs_scene_t *scene, ecs_entity_t sketch);
static inline bool scene_solver_can_apply_drag(ecs_scene_t *scene, ecs_entity_t sketch, ...);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove permanent anchor-priority from PARALLEL and apply operation-context authority</name>
  <read_first>@src/ecs/ecs_scene.h, @.planning/phases/33-large-jump-robustness-and-parallel-along-parity/33-HUMAN-UAT.md, @.planning/phases/33-large-jump-robustness-and-parallel-along-parity/33-CONTEXT.md</read_first>
  <files>src/ecs/ecs_scene.h</files>
  <behavior>
    - Test 1: Active drag on AB causes CD to follow while preserving PARALLEL.
    - Test 2: Active drag on CD causes AB to follow while preserving PARALLEL.
    - Test 3: If one participant has ALONG X/Y/Z constraints, it can still be authority when operation context indicates, rather than being permanently treated as slave/anchor.
  </behavior>
  <action>Refactor PARALLEL participant motion resolution to equal-priority behavior per UAT Gap 1 and D-10/D-11: authority is selected from active operation context (drag source/edited participant) and, when needed for tied operations, strongest external-constraint context in that operation. Keep D-01 transactional semantics and D-03 deterministic ordering intact; do not reintroduce fixed global anchor bias for PARALLEL participant pairs.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "scene_solver_drag|scene_solver_contract|scene_solver_pass_policy" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`git --no-pager grep -n "CONSTRAINT_PARALLEL\\|drag\\|authority\\|anchor" src/ecs/ecs_scene.h` shows operation-context authority logic without permanent participant anchoring; targeted ctest slice passes.</acceptance_criteria>
  <done>PARALLEL authority is bidirectional and context-driven, eliminating permanent anchor-priority behavior that caused deadlock-prone UX.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic regressions for bidirectional PARALLEL motion and ALONG interaction</name>
  <read_first>@src/tests/scene_solver_drag_test.c, @src/tests/scene_solver_contract_test.c, @src/tests/scene_solver_pass_policy_test.c, @src/ecs/ecs_scene.h, @.planning/phases/33-large-jump-robustness-and-parallel-along-parity/33-HUMAN-UAT.md</read_first>
  <files>src/tests/scene_solver_drag_test.c, src/tests/scene_solver_contract_test.c, src/tests/scene_solver_pass_policy_test.c</files>
  <behavior>
    - Test 1: AB moved -> CD follows.
    - Test 2: CD moved -> AB follows.
    - Test 3: Additional ALONG constraint interactions do not force permanent anchor role across repeated/mirrored/reordered runs.
  </behavior>
  <action>Add deterministic regression fixtures for the exact UAT gap scenarios: (a) drag AB in AB||CD pair and assert CD follow behavior, (b) drag CD and assert AB follow behavior, (c) add ALONG X/Y/Z on one participant and assert no permanent anchor lock-in across mirrored/reordered variants. Assert outcome class plus explicit participant motion deltas, not only feasibility booleans.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "scene_solver_drag|scene_solver_contract|scene_solver_pass_policy" --output-on-failure && ctest --test-dir build-vulkan -C Release -R "scene_solver_drag|scene_solver_contract|scene_solver_pass_policy" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`git --no-pager grep -n "AB.*CD\\|CD.*AB\\|parallel\\|along\\|mirrored\\|reordered" src/tests/scene_solver_drag_test.c src/tests/scene_solver_contract_test.c src/tests/scene_solver_pass_policy_test.c` returns new deterministic parity fixtures; both consecutive ctest runs pass.</acceptance_criteria>
  <done>The three required regressions are automated and deterministic: AB→CD follow, CD→AB follow, and ALONG interaction without permanent anchor behavior.</done>
</task>

</tasks>

<verification>
Run:
- `ctest --test-dir build-vulkan -C Release -R "scene_solver_drag|scene_solver_contract|scene_solver_pass_policy" --output-on-failure`
- Immediately rerun the exact same command to confirm deterministic parity.
</verification>

<success_criteria>
- UAT gap is closed in code: PARALLEL participants behave equal-priority and bidirectional under drag/edit context.
- Regression coverage explicitly proves AB drives CD and CD drives AB.
- Mixed PARALLEL + ALONG interactions do not lock one participant into permanent anchor role.
- Targeted suites pass on baseline and immediate rerun.
</success_criteria>

<output>
After completion, create `.planning/phases/33-large-jump-robustness-and-parallel-along-parity/33-03-SUMMARY.md`
</output>

