---
phase: 33-large-jump-robustness-and-parallel-along-parity
plan: 02
type: execute
wave: 2
depends_on: [33-01]
files_modified:
  - src/ecs/ecs_scene.h
  - src/tests/scene_solver_pass_policy_test.c
  - src/tests/scene_solver_contract_test.c
  - src/tests/scene_solver_diagnostics_test.c
  - src/tests/scene_solver_drag_test.c
autonomous: true
requirements: [PARI-01, PARI-02, DIAG-01, DIAG-02]
must_haves:
  truths:
    - "User gets the same feasibility/outcome class for geometrically equivalent PARALLEL and ALONG setups."
    - "User dragging mirrored/participant-order variants of equivalent PARALLEL/ALONG setups gets parity-consistent results."
    - "User sees actionable family+reason diagnostics for parity-related or unsatisfied outcomes, not misleading participant-type errors."
    - "User repeating equivalent PARALLEL/ALONG operation sequences gets deterministic parity outcomes and deterministic diagnostic ordering."
  artifacts:
    - path: "src/ecs/ecs_scene.h"
      provides: "Parity classification and branch alignment between PARALLEL and ALONG solve paths."
      contains: "equivalence-class outcome handling and deterministic reason assignment"
    - path: "src/tests/scene_solver_pass_policy_test.c"
      provides: "Mirrored/order/base parity matrix for PARALLEL↔ALONG feasibility class assertions."
      contains: "explicit base/mirrored/reordered parity fixtures"
    - path: "src/tests/scene_solver_contract_test.c"
      provides: "Contract-level parity equivalence and transactional unsatisfied consistency checks."
      contains: "parallel-vs-along equivalent setup outcome comparisons"
    - path: "src/tests/scene_solver_diagnostics_test.c"
      provides: "Family+reason diagnostic assertions for parity and large-jump mixed-constraint failures."
      contains: "no generic participant-type fallback checks"
    - path: "src/tests/scene_solver_drag_test.c"
      provides: "Drag feasibility parity checks across equivalent PARALLEL and ALONG setups."
      contains: "mirrored linked vertex drag parity fixtures"
  key_links:
    - from: "src/ecs/ecs_scene.h"
      to: "src/tests/scene_solver_pass_policy_test.c"
      via: "PARALLEL and ALONG branch outcome-class parity matrix"
      pattern: "CONSTRAINT_PARALLEL|CONSTRAINT_ALONG_"
    - from: "src/ecs/ecs_scene.h"
      to: "src/tests/scene_solver_diagnostics_test.c"
      via: "family+reason taxonomy and deterministic diagnostic ordering"
      pattern: "Unsatisfied parallel constraint|Unsatisfied driving ALONG"
    - from: "src/tests/scene_solver_pass_policy_test.c"
      to: "src/tests/scene_solver_drag_test.c"
      via: "shared parity fixtures for static recalc and interactive drag feasibility"
      pattern: "mirrored|order|equivalent"
---

<objective>
Enforce PARALLEL/ALONG parity across equivalent geometries and lock deterministic actionable diagnostics for mixed-constraint outcomes.

Purpose: Complete Phase 33 parity and diagnostics requirements on top of Plan 01 staged-solver robustness baseline.
Output: Parity-class solve alignment, parity matrix regression suite, and deterministic family+reason diagnostics coverage.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-large-jump-robustness-and-parallel-along-parity/33-CONTEXT.md
@.planning/phases/33-large-jump-robustness-and-parallel-along-parity/33-RESEARCH.md
@.planning/phases/33-large-jump-robustness-and-parallel-along-parity/33-VALIDATION.md
@.planning/phases/33-large-jump-robustness-and-parallel-along-parity/33-01-SUMMARY.md
@src/ecs/ecs_scene.h
@src/tests/scene_solver_pass_policy_test.c
@src/tests/scene_solver_contract_test.c
@src/tests/scene_solver_diagnostics_test.c
@src/tests/scene_solver_drag_test.c

<interfaces>
From src/ecs/ecs_scene.h:
```c
// Existing parity-related surfaces to align
// PARALLEL branch and ALONG_X/ALONG_Y/ALONG_Z branch logic inside scene_solver_request_recalculate(...)
static inline bool scene_solver_request_recalculate(ecs_scene_t *scene, ecs_entity_t sketch);
```

From src/tests/scene_solver_pass_policy_test.c:
```c
// Existing deterministic rerun policy tests for ALONG that parity matrix should extend
static int test_alin04_pass_policy_mixed_along_x_length_angle_connectivity_rerun_deterministic(void);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Align PARALLEL and ALONG equivalent-geometry outcome-class logic in solver core</name>
  <read_first>@src/ecs/ecs_scene.h, @src/tests/scene_solver_pass_policy_test.c, @src/tests/scene_solver_contract_test.c, @.planning/phases/33-large-jump-robustness-and-parallel-along-parity/33-CONTEXT.md</read_first>
  <files>src/ecs/ecs_scene.h</files>
  <behavior>
    - Test 1: Equivalent PARALLEL and ALONG setups resolve to the same feasible/unsatisfied class.
    - Test 2: Mirrored and participant-order variants preserve the same outcome class.
    - Test 3: Unsatisfied cases remain transactional and diagnostics classification remains deterministic.
  </behavior>
  <action>In `scene_solver_request_recalculate(...)`, normalize parity outcome handling per D-10/D-11 so equivalent geometry classes in PARALLEL and ALONG branches map to the same feasibility classification and solve-policy behavior. Preserve transactional semantics from D-01/D-07 and deterministic ordering from D-13. Keep legacy ALONG single-line descriptor coverage intact; avoid introducing branch-specific heuristics that diverge PARALLEL vs ALONG behavior.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "scene_solver_pass_policy|scene_solver_contract" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`git --no-pager grep -n "CONSTRAINT_PARALLEL\\|CONSTRAINT_ALONG_X\\|CONSTRAINT_ALONG_Y\\|CONSTRAINT_ALONG_Z" src/ecs/ecs_scene.h` plus `git --no-pager grep -n "equivalent\\|mirrored\\|order" src/tests/scene_solver_pass_policy_test.c src/tests/scene_solver_contract_test.c` show parity alignment anchors; targeted ctest pass-policy+contract slice passes.</acceptance_criteria>
  <done>Solver core maps equivalent PARALLEL/ALONG setups to consistent outcome classes across base/mirrored/order variants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add explicit PARALLEL↔ALONG parity matrix regressions across pass-policy/contract/drag suites</name>
  <read_first>@src/tests/scene_solver_pass_policy_test.c, @src/tests/scene_solver_contract_test.c, @src/tests/scene_solver_drag_test.c, @.planning/phases/33-large-jump-robustness-and-parallel-along-parity/33-VALIDATION.md</read_first>
  <files>src/tests/scene_solver_pass_policy_test.c, src/tests/scene_solver_contract_test.c, src/tests/scene_solver_drag_test.c</files>
  <behavior>
    - Test 1: Base equivalent PARALLEL vs ALONG fixtures report same feasibility/outcome class.
    - Test 2: Mirrored equivalent fixtures report same feasibility/outcome class.
    - Test 3: Participant-order permutations report same feasibility/outcome class.
    - Test 4: Drag feasibility parity holds for equivalent mirrored linked vertex moves.
  </behavior>
  <action>Add a parity matrix (base + mirrored + participant-order variants) covering PARI-01/PARI-02 in `scene_solver_pass_policy_test.c` and `scene_solver_contract_test.c`, then add drag feasibility parity fixtures in `scene_solver_drag_test.c`. Use shared fixture builders/constants so parity comparisons are value-explicit and grep-verifiable. Ensure assertions compare outcome class and deterministic rerun stability, not just approximate geometry similarity.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "scene_solver_pass_policy|scene_solver_contract|scene_solver_drag" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`git --no-pager grep -n "parallel.*along\\|along.*parallel\\|mirrored\\|order\\|parity" src/tests/scene_solver_pass_policy_test.c src/tests/scene_solver_contract_test.c src/tests/scene_solver_drag_test.c` returns added matrix fixtures/assertions; targeted ctest slice passes twice consecutively.</acceptance_criteria>
  <done>PARALLEL/ALONG parity behavior is regression-locked for equivalent static and drag workflows, including mirrored/order variants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Harden DIAG family+reason taxonomy and deterministic ordering for parity and mixed-constraint failures</name>
  <read_first>@src/tests/scene_solver_diagnostics_test.c, @src/ecs/ecs_scene.h, @.planning/phases/33-large-jump-robustness-and-parallel-along-parity/33-CONTEXT.md</read_first>
  <files>src/tests/scene_solver_diagnostics_test.c, src/ecs/ecs_scene.h</files>
  <behavior>
    - Test 1: Parity-related unsatisfied cases emit family+reason diagnostics (not generic participant-type errors).
    - Test 2: Large-jump mixed-constraint failures emit deterministic ordered diagnostics for identical operation sequences.
    - Test 3: Immediate rerun of diagnostics-focused suite preserves exact pass/fail set and ordering expectations.
  </behavior>
  <action>Extend diagnostics taxonomy assertions per D-12/D-13 and DIAG-02 by adding explicit checks for PARALLEL and ALONG failure reason classes and mixed-constraint large-jump reason class ordering. If any solver diagnostic path still falls back to misleading generic participant-type diagnostics for targeted cases, rewire to family+reason-specific messages while preserving deterministic append and implication ordering from Plan 01.</action>
  <verify>
    <automated>ctest --test-dir build-vulkan -C Release -R "scene_solver_diagnostics|scene_solver_pass_policy" --output-on-failure</automated>
  </verify>
  <acceptance_criteria>`git --no-pager grep -n "Unsatisfied parallel constraint\\|Unsatisfied driving ALONG\\|diagnostic\\|reason" src/tests/scene_solver_diagnostics_test.c src/ecs/ecs_scene.h` shows family+reason taxonomy anchors; diagnostics+pass-policy ctest slice passes and immediate rerun also passes.</acceptance_criteria>
  <done>Diagnostics are actionable, deterministic, and parity-consistent across targeted large-jump and PARALLEL/ALONG mixed-constraint failures.</done>
</task>

</tasks>

<verification>
Run:
- `ctest --test-dir build-vulkan -C Release -R "scene_solver_contract|scene_solver_drag|scene_solver_pass_policy|scene_solver_diagnostics" --output-on-failure`
- Immediately rerun the identical command and confirm pass/pass parity.
</verification>

<success_criteria>
- PARI-01/PARI-02: Equivalent PARALLEL and ALONG setups now produce consistent feasibility behavior across base, mirrored, and reordered variants.
- DIAG-01: Deterministic outcome + diagnostic ordering is stable across immediate reruns for targeted parity fixtures.
- DIAG-02: Diagnostics for targeted large-jump/parity failures are family+reason specific and avoid misleading participant-type fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/33-large-jump-robustness-and-parallel-along-parity/33-02-SUMMARY.md`
</output>

