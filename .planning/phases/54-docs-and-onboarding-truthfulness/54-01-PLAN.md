---
phase: 54-docs-and-onboarding-truthfulness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - PROOF-02
must_haves:
  truths:
    - "README tells consumers they can reference MdCad.Avalonia.Control from a plain net10.0 Avalonia host."
    - "README keeps samples/avalonia-host-minimal as compile/build proof only."
    - "README keeps samples/avalonia-host as the Windows runtime proof surface."
    - "README states that embedded mdCAD viewing remains Windows-only at runtime."
  artifacts:
    - path: "README.md"
      provides: "Top-level support contract for v1.9 onboarding."
      contains: "net10.0"
    - path: "samples/avalonia-host-minimal/AvaloniaHostMinimal.csproj"
      provides: "Compile/build proof surface for the plain-net10 host contract."
      contains: "<TargetFramework>net10.0</TargetFramework>"
    - path: "samples/avalonia-host/AvaloniaHost.csproj"
      provides: "Windows runtime proof surface."
      contains: "<TargetFramework>net10.0-windows10.0.19041.0</TargetFramework>"
  key_links:
    - from: "README.md"
      to: "samples/avalonia-host-minimal/AvaloniaHostMinimal.csproj"
      via: "compile/build proof wording"
      pattern: "net10\\.0|minimal host"
    - from: "README.md"
      to: "samples/avalonia-host/AvaloniaHost.csproj"
      via: "Windows runtime proof wording"
      pattern: "Windows-only|diagnostic host|runtime proof"
---

<objective>
Rewrite the top-level README so it matches the proven Phase 52/53 support boundary without reopening runtime implementation work.

Purpose: Make the first onboarding surface truthful about plain `net10.0` host compatibility versus Windows-only embedded runtime support.
Output: A README that separates compile/build proof from runtime proof and sets correct expectations for supported/unsupported platforms.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/54-docs-and-onboarding-truthfulness/54-RESEARCH.md
@.planning/phases/54-docs-and-onboarding-truthfulness/54-VALIDATION.md
@.planning/phases/53-consumer-proof-and-windows-regression-closure/53-01-SUMMARY.md
@.planning/phases/53-consumer-proof-and-windows-regression-closure/53-03-SUMMARY.md
@README.md
@samples/avalonia-host-minimal/AvaloniaHostMinimal.csproj
@samples/avalonia-host/AvaloniaHost.csproj
@samples/avalonia-mdcad-control/Host/MdCadUnsupportedRuntime.cs

<interfaces>
From samples/avalonia-host-minimal/AvaloniaHostMinimal.csproj:
```xml
<TargetFramework>net10.0</TargetFramework>
<ProjectReference Include="..\avalonia-mdcad-control\MdCad.Avalonia.Control.csproj" />
```

From samples/avalonia-host/AvaloniaHost.csproj:
```xml
<TargetFramework>net10.0-windows10.0.19041.0</TargetFramework>
<ProjectReference Include="..\avalonia-mdcad-control\MdCad.Avalonia.Control.csproj" />
```

From samples/avalonia-mdcad-control/Host/MdCadUnsupportedRuntime.cs:
```csharp
internal const string UnsupportedRuntimeMessage =
    "This host/control is valid, but embedded mdCAD viewing is Windows-only and will not launch on the current platform.";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the README sample-host contract around the proven Phase 53 split</name>
  <files>README.md</files>
  <action>Update the embedding-related feature bullet and sample-host quickstart sections so README states the current repo truth from Phase 52/53: `MdCad.Avalonia.Control` can be referenced from a plain `net10.0` Avalonia host, `samples/avalonia-host-minimal` is the compile/build proof surface, and `samples/avalonia-host` remains the Windows runtime proof surface. Keep the minimal-host command language centered on `dotnet build`; if `dotnet run` remains mentioned, label it as optional and not proof of cross-platform runtime embedding. Do not broaden into general build-doc cleanup or reopen runtime/backend code.</action>
  <verify>
    <automated>rg -n "net10\.0|runtime proof|minimal host|diagnostic host" README.md</automated>
  </verify>
  <done>README no longer claims both sample hosts are Windows-targeted and clearly identifies which sample proves compile/build compatibility versus real runtime behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit Windows-only runtime and unsupported-platform truth to README</name>
  <files>README.md</files>
  <action>Make README explicitly say the embedded mdCAD viewer remains Windows-only at runtime even though the host/control contract is plain `net10.0`. Reuse the canonical unsupported-platform meaning already implemented by the control: the host/control is valid, but embedded viewing will not launch on unsupported platforms. Keep JSONL/live-refresh discussion truthful and informational only; do not imply new non-Windows runtime support, new backend work, or any Phase 52/52.1/53 redesign.</action>
  <verify>
    <automated>rg -n "net10\.0|Windows-only|runtime proof|minimal host|diagnostic host" README.md</automated>
  </verify>
  <done>README contains an explicit, consumer-facing statement that plain `net10.0` referenceability and Windows-only embedded runtime support are separate truths.</done>
</task>

</tasks>

<verification>
- `rg -n "net10\.0|Windows-only|runtime proof|minimal host|diagnostic host" README.md`
</verification>

<success_criteria>
- README states the control/reference contract as plain `net10.0`.
- README treats `samples/avalonia-host-minimal` as compile/build proof only.
- README treats `samples/avalonia-host` as the Windows runtime proof surface.
- README makes unsupported-platform runtime behavior explicit without promising new backend/runtime support.
</success_criteria>

<output>
After completion, create `.planning/phases/54-docs-and-onboarding-truthfulness/54-01-SUMMARY.md`
</output>
