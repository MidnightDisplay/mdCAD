---
phase: 54-docs-and-onboarding-truthfulness
plan: 02
type: execute
wave: 2
depends_on:
  - 54-01
files_modified:
  - samples/avalonia-mdcad-control/QUICKSTART.md
  - samples/avalonia-host-minimal/ViewModels/MainWindowViewModel.cs
autonomous: true
requirements:
  - PROOF-02
must_haves:
  truths:
    - "The control quickstart tells consumers their host may target plain net10.0."
    - "The quickstart says embedded mdCAD viewing is Windows-only at runtime and unsupported platforms show a truthful warning instead of launching."
    - "The minimal sample no longer ships a misleading personal JSONL path as if it were universal onboarding data."
  artifacts:
    - path: "samples/avalonia-mdcad-control/QUICKSTART.md"
      provides: "Consumer wiring guide for MdCad.Avalonia.Control."
      contains: "net10.0"
    - path: "samples/avalonia-host-minimal/ViewModels/MainWindowViewModel.cs"
      provides: "Minimal sample JsonlPath default used by onboarding."
      contains: "JsonlPath"
    - path: "samples/avalonia-host-minimal/AvaloniaHostMinimal.csproj"
      provides: "Build proof lane that must remain green after onboarding cleanup."
      contains: "<TargetFramework>net10.0</TargetFramework>"
  key_links:
    - from: "samples/avalonia-mdcad-control/QUICKSTART.md"
      to: "samples/avalonia-host-minimal/ViewModels/MainWindowViewModel.cs"
      via: "shared JsonlPath onboarding example"
      pattern: "JsonlPath|replace"
    - from: "samples/avalonia-mdcad-control/QUICKSTART.md"
      to: "samples/avalonia-mdcad-control/Host/MdCadUnsupportedRuntime.cs"
      via: "unsupported-platform wording"
      pattern: "Windows-only|unsupported"
---

<objective>
Align the reusable-control quickstart and minimal sample onboarding path with the real Phase 54 support contract.

Purpose: Make the consumer wiring guide truthful without changing the locked runtime/backend implementation from Phases 52-53.
Output: Updated QUICKSTART guidance plus a scoped minimal-sample `JsonlPath` cleanup if needed for truthful onboarding.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
@.github/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/54-docs-and-onboarding-truthfulness/54-RESEARCH.md
@.planning/phases/54-docs-and-onboarding-truthfulness/54-VALIDATION.md
@.planning/phases/53-consumer-proof-and-windows-regression-closure/53-01-SUMMARY.md
@.planning/phases/53-consumer-proof-and-windows-regression-closure/53-03-SUMMARY.md
@samples/avalonia-mdcad-control/QUICKSTART.md
@samples/avalonia-host-minimal/ViewModels/MainWindowViewModel.cs
@samples/avalonia-host-minimal/AvaloniaHostMinimal.csproj
@samples/avalonia-mdcad-control/Host/MdCadUnsupportedRuntime.cs

<interfaces>
From samples/avalonia-host-minimal/ViewModels/MainWindowViewModel.cs:
```csharp
public sealed class MainWindowViewModel
{
    public string JsonlPath { get; } =
        @"C:\Users\RodionRadchenko\source\repos\GeoMate\artifacts\repl\geo-mate-model.jsonl";
}
```

From samples/avalonia-mdcad-control/Host/MdCadUnsupportedRuntime.cs:
```csharp
internal const string UnsupportedRuntimeMessage =
    "This host/control is valid, but embedded mdCAD viewing is Windows-only and will not launch on the current platform.";
```

From samples/avalonia-host-minimal/AvaloniaHostMinimal.csproj:
```xml
<TargetFramework>net10.0</TargetFramework>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite QUICKSTART around the plain-net10 host contract and unsupported-runtime truth</name>
  <files>samples/avalonia-mdcad-control/QUICKSTART.md</files>
  <action>Update the quickstart so Step 1 and all surrounding guidance say the consumer Avalonia host can target plain `net10.0`, while the embedded mdCAD viewer remains Windows-only at runtime. Add an explicit unsupported-platform note sourced from the implemented control behavior: the host/control remains valid, but launching the viewer is blocked and the warning is intentional. Keep the guide scoped to consumer wiring; do not redesign the control API, do not reopen Windows runtime code, and do not turn the minimal host into a new runtime-proof surface. Mention `samples/avalonia-host` only as the existing diagnostic runtime proof surface when that distinction helps clarify the boundary.</action>
  <verify>
    <automated>rg -n "net10\.0|Windows-only|unsupported|JsonlPath" samples/avalonia-mdcad-control/QUICKSTART.md</automated>
  </verify>
  <done>QUICKSTART tells consumers the host contract is plain `net10.0`, explains unsupported runtime behavior truthfully, and stops calling the control itself Windows-only.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up the minimal sample JsonlPath example only as far as truthfulness requires</name>
  <files>samples/avalonia-host-minimal/ViewModels/MainWindowViewModel.cs</files>
  <action>If QUICKSTART still points readers at the minimal sample as the onboarding reference, replace the developer-specific absolute `GeoMate` path with the smallest truthful alternative: an obvious replace-me placeholder path or a no-file default that keeps the sample build green. Preserve the existing property name `JsonlPath` and avoid adding runtime file-discovery logic, copied sample assets, or new backend/runtime behavior. Keep the change scoped to onboarding truth only.</action>
  <verify>
    <automated>rg -n "net10\.0|Windows-only|unsupported|replace|JsonlPath" samples/avalonia-mdcad-control/QUICKSTART.md samples/avalonia-host-minimal/ViewModels/MainWindowViewModel.cs && dotnet build .\samples\avalonia-host-minimal\AvaloniaHostMinimal.csproj -c Release</automated>
  </verify>
  <done>The quickstart and minimal sample use a truthful, clearly replaceable `JsonlPath` example and the plain-net10 minimal host still builds in Release.</done>
</task>

</tasks>

<verification>
- `rg -n "net10\.0|Windows-only|unsupported|replace|JsonlPath" samples/avalonia-mdcad-control/QUICKSTART.md samples/avalonia-host-minimal/ViewModels/MainWindowViewModel.cs && dotnet build .\samples\avalonia-host-minimal\AvaloniaHostMinimal.csproj -c Release`
</verification>

<success_criteria>
- QUICKSTART says the host project can target plain `net10.0`.
- QUICKSTART explicitly states unsupported-platform runtime behavior without implying new viewer support.
- The minimal sample no longer presents a developer-specific personal JSONL path as repo-wide onboarding truth.
- `samples/avalonia-host-minimal` still builds successfully after the docs/sample cleanup.
</success_criteria>

<output>
After completion, create `.planning/phases/54-docs-and-onboarding-truthfulness/54-02-SUMMARY.md`
</output>
